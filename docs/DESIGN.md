# DESIGN.md — 프론트엔드 디자인 프로세스 (`docs/DESIGN.md`, import 금지)

이 문서는 확정된 디자인이 아니다. **프로젝트 목적 → 아이디어 발산 → 한 방향으로 수렴 → 모든 화면에 통일 적용**까지의 절차다.
확정된 결과물은 여기가 아니라 `docs/design/` 아래에 쌓인다.

```
docs/design/BRIEF.md      ← §1 결과: 이 프로젝트의 디자인 브리프
docs/design/EXPLORE.md    ← §2 결과: 발산한 아이디어와 비교표
docs/design/SYSTEM.md     ← §3 결과: 확정 방향 + 토큰 + 컴포넌트 규칙 (모든 화면의 기준)
docs/design/tokens.css    ← SYSTEM.md의 토큰을 코드로. 컴포넌트는 이 변수만 쓴다
docs/design/DECISIONS.md  ← 날짜별 "무엇을 왜 정했나 / 버렸나"
```
현재 단계 판단: SYSTEM.md가 없으면 §1→§2→§3 순서로. 있으면 §4만 따른다.

## 1. 입력 — 프로젝트 목적에서 브리프 만들기
GSD `.planning/PROJECT.md`·`REQUIREMENTS.md`를 읽고 아래를 채운다. 비어 있는 항목은 사용자에게 묻는다. 추측으로 채우지 않는다.

```
제품이 하는 일(한 문장):
주 사용자 / 사용 상황 (하루 몇 번, 어떤 기기, 급한가 느긋한가):
가장 잦은 작업 3개:
성공 기준 (빠름? 신뢰감? 즐거움? 설득?):
브랜드·회사 톤 (있으면 색·로고·기존 자료 경로):
제약 (기존 시스템, 접근성 요구, 다크모드, 다국어, 인쇄):
피해야 할 것 (경쟁사와 닮음, 특정 인상):
디자인의 핵심 질문 — "이 화면이 사용자에게 무엇을 느끼게 해야 하나":
```
→ `docs/design/BRIEF.md` 저장. 이후 모든 판단은 이 브리프로 돌아와 검증한다.

## 2. 발산 — 서로 다른 아이디어를 넓게
목표는 "좋은 안 하나"가 아니라 **의도적으로 다른 안 3–5개**. 비슷한 안이 나오면 다시 뽑는다.

### 2-1. 탐색 축 — 각 안은 축마다 다른 값을 고른다
| 축 | 양 끝 |
|---|---|
| 톤 | 절제·중립 ↔ 표현적·개성 |
| 밀도 | 여백 중심 ↔ 정보 밀도 중심 |
| 색 전략 | 무채 + 포인트 1 ↔ 다색 팔레트 ↔ 톤온톤 |
| 타이포 | 산세리프 단일 ↔ 세리프 헤드+산세리프 ↔ 모노 ↔ 디스플레이 강조 |
| 형태 | 직각·선 ↔ 큰 라운드·면 ↔ 기하 도형 |
| 구조 | 격자 정렬 ↔ 비대칭 여백 ↔ 카드·패널 ↔ 편집 레이아웃 |
| 모션 | 없음 ↔ 응답형만 ↔ 연출 1회 ↔ 리듬 있음 |
| 메타포 | 도구(콘솔·계기판) ↔ 문서(종이·잡지) ↔ 공간(방·선반) ↔ 대화 |

### 2-2. 아이디어 씨앗 — 브리프의 세계에서 끌어온다
- 사용자의 실제 업무 도구·물건(장부, 견적서, 현장 장비, 무대, 인쇄물)에서 색·질감·리듬을 가져온다
- 회사·업종의 언어(문서 양식, 용어, 단위)를 그대로 UI 어휘로 쓴다
- 가장 잦은 작업 1개를 히어로로 삼는다 — 첫 화면이 그 작업을 바로 시작하게
- "빠름"이 목표면 밀도·단축키·즉시 피드백을, "신뢰"가 목표면 정렬·일관 여백·절제된 색을 우선
- 한 안은 반드시 **위험한 안**(과감함 한 곳)으로, 한 안은 **가장 안전한 안**으로

### 2-3. 각 안의 기록 형식 (`docs/design/EXPLORE.md`)
```
안 A — 이름 [한 단어]
축 값: 톤/밀도/색/타입/형태/구조/모션/메타포
색 4–6개(hex+용도) · 서체(한글/라틴) · 레이아웃 한 문장 + ASCII 와이어프레임
이 안이 브리프의 핵심 질문에 답하는 방식:
과감함을 쓴 한 곳:
위험·비용:
```
### 2-4. gstack으로 실물 보기
`/design-consultation`으로 브리프 검토 → `/design-shotgun`에 **안 A·B·C의 축 값을 그대로 명시**해 3안 생성 → 비교 보드에서 확인.
축을 명시하지 않으면 세 안이 비슷해진다. 필요하면 안 D·E로 한 번 더.

## 3. 수렴 — 하나로 정하고 시스템으로 고정
### 3-1. 평가 (각 1–5점, EXPLORE.md 비교표)
브리프 적합 ·  주 작업 효율 ·  차별성(경쟁사·AI 기본값과 다른가) ·  구현·유지 비용 ·  접근성 달성 가능성 ·  6개월 뒤에도 유효한가
- 점수는 참고. 최종 결정은 사용자가 한다. 두 안이 근접하면 **혼합하지 말고** 장점을 한 안에 흡수한다
- 채택하지 않은 안도 "왜 버렸나" 한 줄과 함께 DECISIONS.md에 남긴다

### 3-2. `docs/design/SYSTEM.md` 작성 — 이후 모든 화면의 유일한 기준
```
방향 이름 · 한 줄 원칙(이 제품이 남과 다른 한 가지) · 채택 이유 3줄
색: --bg --fg --muted --accent(1개) --surface --success/--warning/--danger  (hex, 용도, 대비 검증값)
타입: 한글 서체 / 라틴·숫자 서체 / 스케일 5–7단계 / 행간 / 자간 / 줄 길이 / tabular-nums 규칙
간격: 기본 단위(4 또는 8) · 스케일 · 컨테이너 최대폭 · 그리드
형태: radius 위계표(버튼/입력/카드/모달) · 테두리 · 그림자 단계와 색
모션: 허용 목록(무엇에, 몇 ms, 이징) · 금지 목록 · reduce-motion 동작
레이아웃 템플릿: 목록 화면 / 상세 화면 / 폼 화면 / 대시보드 — ASCII 와이어프레임
컴포넌트 규칙: 버튼 위계(1·2·3차) · 입력·오류 표시 · 테이블 · 빈/로딩/오류 상태 · 토스트
카피 규칙: 버튼=실제 동작, 토스트=같은 단어, 오류=원인+다음 행동, 사과 없음, 문장형 대소문자
아이콘: 세트 1개, 크기 2단계, 굵기 고정
```
토큰은 `tokens.css`(또는 프레임워크 설정)로 옮기고, 컴포넌트는 그 변수만 참조한다.

## 4. 통일 — 새 화면·컴포넌트마다 (SYSTEM.md 확정 후)
1. SYSTEM.md의 레이아웃 템플릿 중 하나에서 시작한다. 새 템플릿이 필요하면 먼저 SYSTEM.md에 추가
2. 새 색·서체·radius·그림자를 **만들지 않는다**. 필요하면 토큰 추가 제안 → 승인 → tokens.css → 사용
3. 기존 컴포넌트를 먼저 찾는다. 변형이 필요하면 variant로, 새 컴포넌트는 두 곳 이상에서 쓰일 때만
4. 카피는 카피 규칙대로. 같은 행동은 앱 전체에서 같은 단어
5. 완료 전 §6 확인 + `/design-review`로 SYSTEM.md와의 일관성 검사
6. 시스템을 벗어나야 할 때는 DECISIONS.md에 이유를 남기고 SYSTEM.md를 고친다 — 화면 하나만 예외로 두지 않는다

## 5. 안티슬롭 — 브리프가 요구하지 않으면 발산 단계에서 제외
- 크림 배경 + 세리프 + 테라코타 · 거의 검정 + 형광 단일 포인트 · `#111`류 틴트 검정
- 모든 내용을 같은 카드·같은 radius·회색 그림자로 자르기 · 장식 그라디언트·블러 오브
- 큰 숫자 히어로 기본값 · 순서 아닌데 `01/02/03` · 한 단어만 강조 · 대문자 아이브로우
- `A · B · C` · `WORD — 조각` · 소형 라벨 모노 폰트 · 버튼 끝 `→` · "제출" 같은 빈 CTA
- 전 섹션 fade-up, 전 카드 hover 확대
검토 질문: "다른 브리프에도 똑같이 만들었을까?" → 예면 기본값. 바꾼다. 대담함은 한 곳에만.

## 6. 품질 바닥 (모든 화면)
- 360–1440px · 키보드 전 행동 · `:focus-visible` · 대비 텍스트 4.5:1 / UI 3:1 · `prefers-reduced-motion`
- 실제 데이터(긴 이름, 큰 금액, 빈 값, 한글·영문 혼용)로 확인 · `/browse` 스크린샷으로 자기 비평
- 한글: `word-break: keep-all`, 행간 1.6+, 자간 -0.01~-0.02em, 숫자·금액·날짜 형식 SYSTEM.md에 고정

## 7. gstack·Superpowers 연결
| 단계 | 명령 |
|---|---|
| §1 브리프 | `/superpowers:brainstorm`(사용자·작업·제약 추출) → `/design-consultation` |
| §2 발산 | `/design-shotgun` (축 값 명시, 3–5안) |
| §3 수렴 | 사용자 승인 → `/design-html`(채택안 실물) → SYSTEM.md 작성 → `/plan-design-review` |
| §4 통일 | 화면마다 `/design-review` → `/qa` |
| 개선 | `/browse` 스크린샷 → §5 대조 → 수정 → `/qa` |
