---
phase: 06-ai
plan: 08
type: execute
wave: 6
depends_on: ["06-07"]
files_modified:
  - src/worker/ai/client.ts
  - src/worker/ai/gateway.ts
  - tests/unit/ai-client-errors.test.ts
  - tests/e2e/ai-fallback.e2e.ts
  - tests/e2e/ai-safety.e2e.ts
autonomous: true
requirements: [AI-06, AI-05]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "AI 호출이 키 거부(401·403), 연결 실패(429·5xx·네트워크 오류·시간 초과), JSON이 아닌 답, 목록 밖 답, 거절(refusal)로 끝나면 그 답을 버리고 자주 누른 순서 번호표와 원인별 안내(\"원인. 다음 행동.\" 꼴)를 보인다(AI-06, D-19, D-20, ROADMAP 성공 기준 3)"
    - "AI가 고른 요소에 번호표가 붙어도 페이지(맨 위와 iframe)에 click·pointerdown·focus·input이 하나도 생기지 않고 초점 요소가 그대로다(AI-06, D-08)"
    - "AI가 위험한 버튼 \"삭제\"를 골라도 그 번호를 누르면 Phase 1 확인 화면이 먼저 뜨고, Enter 전에는 눌리지 않는다(D-08)"
    - "화면 정리가 실패하거나 키가 없어도 번호표(F)·명령판(0)·자석 커서가 그대로 동작한다(AI-05, D-21)"
    - "사이트가 만든 가짜 키 입력(0·9·7)으로는 AI 요청이 나가지 않는다(D-02)"
  prohibitions:
    - statement: "AI 결과로 요소가 눌리거나 초점을 받거나 입력되는 경로를 만들지 않는다 — AI 답은 번호 순서만 정하고, 번호로 누르는 것은 이용자의 진짜 키 입력뿐이며, 위험한 버튼 보호는 AI가 골라도 그대로다(D-08, REQUIREMENTS Out of Scope \"AI가 요소를 직접 누르기\")"
      requirement_id: AI-06
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/worker/ai/client.ts"
      provides: "오류 종류를 key-rejected·call-failed·canceled로 가르는 호출 래퍼"
      exports: ["requestCleanupPicks", "classifyClientError"]
    - path: "tests/unit/ai-client-errors.test.ts"
      provides: "오류 가르기 단위 시험(시간 초과·취소 포함)"
    - path: "tests/e2e/ai-fallback.e2e.ts"
      provides: "실패 원인별 대체 번호표와 안내 e2e"
    - path: "tests/e2e/ai-safety.e2e.ts"
      provides: "AI 결과로 눌림 없음·위험 버튼 보호·가짜 키 무시·실패 뒤 다른 기능 정상 e2e"
  key_links:
    - from: "src/worker/ai/client.ts"
      to: "src/worker/ai/gateway.ts"
      via: "ClientOutcome.failure → CleanupResult.reason"
      pattern: "classifyClientError"
    - from: "tests/e2e/ai-safety.e2e.ts"
      to: "tests/practice-site/ai-cleanup.html"
      via: "window.__events와 data-count로 페이지 이벤트가 없음을 잰다"
      pattern: "__events"
---

## Phase Goal

**As a** 이용자, **I want to** 처음 보는 복잡한 화면에서 명령판의 "화면 정리"를 누르면 중요한 요소 9개에 번호가 붙기를, **so that** AI가 없거나 실패해도 나머지 기능은 그대로이고, 개인정보와 입력 값은 밖으로 나가지 않는다.

(ROADMAP Phase 6 **Goal** 문장을 그대로 세 칸에 옮긴 것이다. 새 내용은 더하지 않았다. `/gsd-mvp-phase 6`으로 다듬을 수 있다.)

<objective>
AI가 실패하는 모든 길에서 답을 버리고 자주 누른 순서로 대신하며 원인을 알려 주고, AI 결과가 무엇도 누르지 않으며 다른 기능을 막지 않음을 시험으로 못 박는다(ROADMAP 성공 기준 3).

Purpose: D-02·D-08·D-19·D-20·D-21, AI-06, AI-05의 "나머지는 모두 동작".
Output: 오류 가르기(`classifyClientError`)와 그 단위 시험, 실패 원인별 e2e, 안전 e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai/06-CONTEXT.md
@.planning/phases/06-ai/06-RESEARCH.md
@.planning/phases/06-ai/06-01-SUMMARY.md
@.planning/phases/06-ai/06-04-SUMMARY.md
@.planning/phases/06-ai/06-06-SUMMARY.md
@.planning/phases/06-ai/06-07-SUMMARY.md
</context>

## 앞선 단계 의존

- 06-01: `cleanup-copy.ts`의 원인별 문구, `fake-anthropic.ts`(각본 `raw`·`text`·`network-error`).
- 06-04: `client.ts`(SDK 또는 승인 거절 시 fetch — 06-01-SUMMARY의 Task 1 선택을 따른다), `gateway.ts`의 AI 호출 분기, `ai-cleanup.html`(모든 요소가 이벤트를 `window.__events`와 `data-count`에 기록, 위험 버튼 `btn-delete`, iframe — Task 2).
- 06-06: 창구 흐름(저장된 결과 → 한도 예약 → 요청). 사용량은 보낸 요청마다 1.
- 06-07: 이용자 취소(`canceled`)와 `gateway.cancel`의 AbortController 표. 이 계획은 취소 경로를 고치지 않는다.
- Phase 1: 위험 버튼 보호와 확인 화면(`tests/e2e/danger.e2e.ts`·`confirm.e2e.ts`의 확인 방식), 자석 테두리(`tests/e2e/magnet.e2e.ts`의 `readRing`·`ringMatches`), 모드 표시 글자(`indicatorText`).
- Phase 3: 명령판(0). 06-09와 같은 wave에서 병렬로 돈다 — `messages.ts`, `background.ts`, `storage-writer.ts`, 옵션 페이지는 고치지 않는다(06-09 소유).

## 실행 규칙 (모든 작업 공통)

- 패키지 매니저는 pnpm만. TypeScript strict, `any` 금지, 시크릿을 코드·커밋에 넣지 않는다(CLAUDE.md). 새 패키지 없음.
- 구현 전에 Superpowers `test-driven-development`를, 시험 실패를 쫓기 전에 `systematic-debugging`을, "완료" 전에 `verification-before-completion`을 **호출**한다. 서브에이전트에 맡기면 프롬프트에 세 스킬과 `model`을 적는다.
- RED 커밋 `test(06-08): …` → GREEN 커밋 `feat(06-08): …`. 제목은 영어 접두어, 본문은 한국어.
- 완료 판정은 `CI=true` e2e다. 자동 시험은 진짜 Anthropic API를 부르지 않고 시험 키는 `FAKE_AI_KEY`뿐이다(D-28).
- SDK 오류 클래스 이름과 생성 방법은 추측하지 않는다 — `node_modules/@anthropic-ai/sdk`의 README와 오류 정의 파일을 읽고 쓴다.
- 실제 회사 시스템에 접속하거나 시험하지 않는다(D-29).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 실패 원인 가르기와 원인별 대체 (키 거부·연결 실패·시간 초과·취소·형식·목록 밖·거절)</name>
  <reversibility rating="reversible">`client.ts` 한 파일의 오류 분류와 시험이다.</reversibility>
  <files>src/worker/ai/client.ts, src/worker/ai/gateway.ts, tests/unit/ai-client-errors.test.ts, tests/e2e/ai-fallback.e2e.ts</files>
  <read_first>
    - src/worker/ai/client.ts, src/worker/ai/gateway.ts (06-04·06-06·06-07 결과)
    - node_modules/@anthropic-ai/sdk README "Handling errors"·"Retries"·"Timeouts" 절과 오류 정의 파일(승인 거절이면 생략)
    - tests/e2e/fake-anthropic.ts, tests/e2e/ai-cleanup.e2e.ts
    - .planning/phases/06-ai/06-CONTEXT.md (D-19, D-20, D-21, D-23 카피 규칙)
    - .planning/phases/06-ai/COVERAGE.md (typed errors and retries, request timeout, AbortSignal, stop_reason 행)
  </read_first>
  <behavior>
    - 단위 `classifyClientError`: 인증 오류(401)·권한 오류(403) → `key-rejected`; 요청 한도(429)·서버 오류(500·529)·연결 오류·연결 시간 초과 → `call-failed`; 이용자 취소(abort) → `canceled`; 그 밖의 API 오류(400·404)와 알 수 없는 예외 → `call-failed`
    - 단위: 시간 초과와 이용자 취소를 서로 헷갈리지 않는다(둘 다 abort 계열이라도 결과가 다르다)
    - e2e: 가짜 AI가 401을 주면 "AI 키가 맞지 않아요. 자주 누른 순서로 번호를 붙였어요", 403도 같다
    - e2e: 429, 500, 네트워크 오류면 "AI에 연결하지 못했어요. 자주 누른 순서로 번호를 붙였어요"(재시도 1회 뒤)
    - e2e: 답 글자가 JSON이 아님, 목록 밖 임시 번호가 하나 섞임, stop_reason `refusal`이면 "AI 답을 쓸 수 없었어요. 자주 누른 순서로 번호를 붙였어요"
    - e2e: 모든 경우 번호표가 붙고, 미리 두 번 누른 "목록"의 번호가 1이며(자주 누른 순서), `blockedRequests`가 비어 있다
  </behavior>
  <action>
behavior의 단위 경우로 `tests/unit/ai-client-errors.test.ts`를, e2e 경우로 `tests/e2e/ai-fallback.e2e.ts`를 먼저 쓰고 RED 커밋. e2e는 경우마다 `setAiConfig`(가짜 키, 한도 넉넉히) → 번호표로 "목록"을 두 번 눌러 기록 → 가짜 AI 각본 설정(`raw` 상태 코드, `network-error`, `text`) → `pressCleanupCard` → 안내 글자(`indicatorText` 또는 06-01이 쓴 안내 읽기 방식)와 `numberForElement(page, '<목록 버튼 id>') === '1'`을 단언한다.

`src/worker/ai/client.ts`: 순수 함수 `classifyClientError(error: unknown): 'key-rejected' | 'call-failed' | 'canceled'`를 export하고 `requestCleanupPicks`의 catch가 이것만 쓴다. SDK 경로는 SDK가 export하는 오류 클래스로 `instanceof` 판정한다(인증·권한 → key-rejected, 이용자 abort → canceled, 연결 시간 초과·연결·요청 한도·서버·그 밖 → call-failed). 이용자 취소는 우리 `signal.aborted`로도 확인해 시간 초과와 섞이지 않게 한다. 승인 거절(fetch) 경로면 HTTP 상태(401·403 → key-rejected, 그 밖 비정상 → call-failed)와 우리 신호의 abort 여부로 가른다. 단위 시험의 오류 값은 SDK 오류 정의가 허용하는 방법으로 만든다. 요청 옵션은 06-04 그대로(`timeout: 15000`, `maxRetries: 1`) — SDK가 429·5xx·연결 오류를 한 번 다시 보내므로 e2e는 요청이 2개일 수 있음을 허용한다. 재시도가 있어도 사용량은 화면 정리 한 번에 1이다(06-06 예약은 한 번).

`src/worker/ai/gateway.ts`: 응답에 text 블록이 없으면 `invalid-response`, `failure`는 같은 이름의 대체 사유로 옮긴다(06-04가 이미 그렇다면 고치지 않는다). 대체 번호는 Phase 1 순서(고정 → 자주 누름 → 커서 근처) 그대로다(D-20).

로그를 남긴다면 원인 종류만 남기고 키·요청 본문·응답 글자는 남기지 않는다. GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/ai-client-errors.test.ts</automated>
    <fails_when>non-zero exit, or the Tests summary line reports fewer than 9 passed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-fallback.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report at least 8 passed, or the output contains "No tests found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/worker/ai/client.ts`가 `classifyClientError`를 export하고 `requestCleanupPicks`의 catch가 그것을 부른다
    - `tests/unit/ai-client-errors.test.ts`에 401, 403, 429, 500, 연결 오류, 연결 시간 초과, 이용자 취소, 400, 알 수 없는 예외 경우가 있다
    - `tests/e2e/ai-fallback.e2e.ts`에 401, 403, 429, 500, 네트워크 오류, JSON 아님, 목록 밖 답, refusal 여덟 경우가 있고 각각 원인별 문구와 "목록" 1번을 단언한다
    - 두 명령이 모두 통과한다
  </acceptance_criteria>
  <done>실패 원인마다 답을 버리고 자주 누른 순서로 대신하며 원인에 맞는 안내가 보인다. 시간 초과와 이용자 취소가 구별된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 안전 — AI 결과로 눌림 없음·위험 버튼 보호·가짜 키 무시·실패 뒤 다른 기능 정상</name>
  <files>tests/e2e/ai-safety.e2e.ts</files>
  <read_first>
    - tests/practice-site/ai-cleanup.html, tests/practice-site/ai-cleanup-frame.html (이벤트 기록 방식)
    - tests/e2e/danger.e2e.ts, tests/e2e/confirm.e2e.ts (확인 화면 확인 방식), tests/e2e/magnet.e2e.ts (readRing, ringMatches)
    - src/entrypoints/content.ts (runCleanup — AI 결과가 openHints·setHint만 부르는지)
    - .planning/phases/06-ai/06-CONTEXT.md (D-02, D-08, D-21)
  </read_first>
  <behavior>
    - AI 성공(9개 중 iframe 요소 포함) 뒤: 카드를 누르기 전과 번호표가 뜬 뒤의 `window.__events` 길이가 맨 위·iframe 모두 같고, `document.activeElement`가 맨 위·iframe 모두 그대로다
    - AI가 "삭제"(`btn-delete`)를 고르면 번호표가 붙고, 그 번호를 누르면 Phase 1 확인 화면이 뜨며 `btn-delete`의 `data-count`는 0, Enter를 누른 뒤에야 1
    - 페이지 스크립트가 만든 키 이벤트(`Digit0`·`Digit9`·`Digit7` keydown/keyup, isTrusted 아님)를 보내면 가짜 AI 요청이 0개다
    - 가짜 AI가 네트워크 오류를 낸 뒤: F를 누르면 번호표가 붙고, 0을 누르면 명령판이 열리며, 커서를 버튼 근처로 옮기면 자석 테두리가 그 버튼에 맞는다
    - 키가 없을 때도 같은 세 기능이 동작한다
  </behavior>
  <action>
behavior 경우로 `tests/e2e/ai-safety.e2e.ts`를 쓴다. 이벤트 기록은 연습 페이지가 이미 하므로 시험은 `page.evaluate`·`frame.evaluate`로 `window.__events.length`와 활성 요소를 읽는다(맨 위와 other.test iframe 각각). 위험 버튼·확인 화면·자석 확인은 Phase 1 e2e의 확인 방식을 그대로 쓴다(새 판정 기준을 만들지 않는다). 기존 제품 코드가 이미 통과시키면 RED 커밋 없이 시험만 커밋하고, 실패하는 경우가 나오면 `systematic-debugging`으로 원인을 찾아 제품 코드를 고친 뒤(재현 → 원인 → 수정 → 이 시험이 회귀 시험) 커밋한다.

정적 확인도 이 작업의 완료 조건이다: AI 쪽 코드(`src/core/ai`, `src/worker/ai`, `src/page/collector/ai-text.ts`)에 요소를 누르거나 초점을 주거나 이벤트를 만드는 호출이 없고, content의 `runCleanup`은 AI 결과로 번호표 열기와 안내 표시만 한다(D-08). 제품 코드에 시험용 분기를 더하지 않는다.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-safety.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report at least 5 passed, or the output contains "No tests found"</fails_when>
    <automated>! grep -rnE '\.click\(|\.focus\(|dispatchEvent|showPicker' src/core/ai src/worker/ai src/page/collector/ai-text.ts</automated>
    <fails_when>non-zero exit because grep printed a line — AI code presses, focuses, or synthesizes events</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/danger.e2e.ts tests/e2e/confirm.e2e.ts tests/e2e/magnet.e2e.ts</automated>
    <fails_when>non-zero exit, or any test in the summary is reported as failed (Phase 1 safety regressed)</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/ai-safety.e2e.ts`가 behavior 다섯 경우를 맨 위·iframe 양쪽에서 단언한다
    - AI 쪽 코드에 누르기·초점·이벤트 만들기 호출이 없다(verify의 grep)
    - `CI=true pnpm exec playwright test tests/e2e/ai-safety.e2e.ts`가 5개 이상 통과하고 Phase 1 위험·확인·자석 e2e가 그대로 통과한다
  </acceptance_criteria>
  <done>AI 결과는 번호만 붙이고 아무것도 누르지 않으며, 위험 버튼 보호가 그대로이고, 가짜 키로는 AI가 불리지 않으며, 실패·키 없음에서도 다른 기능이 정상이다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- AI-06 (주 계획): edge probe `unclassified — review manually`. 가정: "호출이 실패"는 키 거부·연결 실패(요청 한도·서버 오류·네트워크·시간 초과 15초, 재시도 1회 뒤)·답 형식 틀림·목록 밖 답·거절·잘림을 모두 포함하고, 모두 답 전체를 버린 뒤 자주 누른 순서로 대신한다. 이용자 취소(Esc)는 실패가 아니라 번호표 없이 원래 상태로 돌아간다(06-07). "직접 눌리는 일 없음"은 페이지 이벤트·초점 측정(e2e)과 정적 grep으로 확인한다.
- AI-05 (일부 — 나머지 기능 정상): 가정: "나머지"는 이 phase 시점에 있는 번호표·명령판·자석 커서를 대표로 확인한다. 키 저장·동기화 금지와 설정 화면은 06-09가 맡는다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI 답 → content 번호표 | 신뢰할 수 없는 모델 출력이 번호 순서를 정한다 |
| 페이지 스크립트 → content script | 사이트가 가짜 키 이벤트를 만들 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Elevation of Privilege | AI 결과 → 요소 누르기 | high | mitigate | AI 결과는 `openHints`의 순서 인자로만 쓰이고 누르기·초점·이벤트 호출이 AI 쪽 코드에 없음(grep), e2e가 맨 위·iframe 이벤트 0개와 초점 불변을 잰다(D-08) |
| T-06-16 | Tampering | 위험 버튼을 AI가 고르게 유도 | medium | mitigate | 번호로 눌러도 Phase 1 확인 화면이 먼저, Enter 전에는 안 눌림을 e2e로 확인 |
| T-06-17 | Spoofing | 사이트의 가짜 키 이벤트로 AI 호출(비용·전송) | medium | mitigate | isTrusted 키만 받는 입력 파이프라인(D-02), e2e가 요청 0개 확인 |
| T-06-18 | Denial of Service | AI 실패가 다른 기능을 멈춤 | medium | mitigate | 모든 실패가 대체 결과로 끝나고(예외가 content로 새지 않음), e2e가 실패·키 없음 뒤 번호표·명령판·자석 동작 확인(D-21) |
| T-06-19 | Information Disclosure | 오류 로그 | low | mitigate | 원인 종류만 남기고 키·본문·응답 글자를 남기지 않음 |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/ai-client-errors.test.ts` 통과
- `CI=true pnpm exec playwright test tests/e2e/ai-fallback.e2e.ts tests/e2e/ai-safety.e2e.ts` 통과
- `CI=true pnpm exec playwright test tests/e2e/danger.e2e.ts tests/e2e/confirm.e2e.ts tests/e2e/magnet.e2e.ts` 통과
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 실패 원인 여덟 가지가 모두 자주 누른 순서 대체와 원인별 안내로 끝남이 CI=true e2e로 확인됐다
- AI 결과로 눌림·초점·입력이 없고 위험 버튼 보호가 그대로임이 확인됐다
- RED(`test(06-08)`)·GREEN(`feat(06-08)`) 커밋이 있다(Task 2가 처음부터 초록이면 그 사실을 SUMMARY에)
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/unit/ai-client-errors.test.ts`, `tests/e2e/ai-fallback.e2e.ts`, `tests/e2e/ai-safety.e2e.ts`
- 심볼: `classifyClientError`(`src/worker/ai/client.ts`)

<output>
Create `.planning/phases/06-ai/06-08-SUMMARY.md` when done (오류 → 사유 표, 재시도 동안 관찰한 요청 수 포함)
</output>
