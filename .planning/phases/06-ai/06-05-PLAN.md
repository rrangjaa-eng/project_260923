---
phase: 06-ai
plan: 05
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - docs/design/SYSTEM.md
  - docs/design/DECISIONS.md
  - src/entrypoints/options/ai-section.ts
  - src/entrypoints/options/main.ts
  - src/shared/messages.ts
  - src/worker/storage-writer.ts
  - src/entrypoints/background.ts
  - tests/e2e/ai-settings.e2e.ts
autonomous: true
requirements: [AI-05, AI-04]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "이용자가 설정 화면의 \"화면 정리(AI)\" 부분에서 AI 키를 넣고 [저장]하면 키가 `chrome.storage.local`에만 저장되고, `chrome.storage.sync`와 설정 파일 내보내기에는 없으며, 칸은 비워지고 끝 4자리만 보인다(AI-05, D-18, D-24)"
    - "이용자가 [지우기]로 키를 지우면 화면 정리 카드는 AI 요청 없이 자주 누른 순서로 대신하고 나머지 기능은 그대로다(AI-05, D-17)"
    - "이용자가 한 달 한도(0~10000번)를 바꾸면 다음 화면 정리부터 그 한도가 적용되고, 설정 화면에 \"이번 달 AI 요청 N번 / 한도 M번\"이 보인다(AI-04, D-16, D-24)"
    - "화면 정리의 불러오는 중·성공·원인별 실패 문구가 `docs/design/SYSTEM.md` 상태 표에 적혀 있고 `src/core/ai/cleanup-copy.ts`의 문구와 글자 하나까지 같다(D-22, D-23)"
    - "AI 설정을 바꾸는 저장 요청은 확장 페이지에서 온 것만 받는다 — content script·웹 페이지에서 온 요청은 거부된다(D-18)"
  prohibitions:
    - statement: "AI 키를 storage.sync·설정 파일 내보내기·content script·웹 페이지·로그·메시지 응답으로 넘기지 않는다 — 키를 읽는 코드는 service worker의 AI 창구·저장자와 확장 설정 화면뿐이다(D-18, 설계 7장)"
      requirement_id: AI-05
      category: security
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/entrypoints/options/ai-section.ts"
      provides: "설정 화면의 AI 키(가려 넣기·끝 4자리·지우기)·한 달 한도·이번 달 사용량 부분"
      exports: ["mountAiSection"]
    - path: "src/worker/storage-writer.ts"
      provides: "updateAiConfig — 키·한도 저장을 단일 저장자 큐로"
      contains: "updateAiConfig"
    - path: "docs/design/SYSTEM.md"
      provides: "화면 정리(AI) 상태 표 문구와 설정 화면 AI 부분 템플릿"
      contains: "화면을 정리하는 중"
    - path: "tests/e2e/ai-settings.e2e.ts"
      provides: "키 local 전용·sync·내보내기 부재·지우기·한도·사용량 표시·거부 e2e"
  key_links:
    - from: "src/entrypoints/options/ai-section.ts"
      to: "src/entrypoints/background.ts"
      via: "chrome.runtime.sendMessage storage/request { op: updateAiConfig } — 확장 페이지 주소일 때만 받음"
      pattern: "updateAiConfig"
    - from: "src/entrypoints/background.ts"
      to: "src/worker/storage-writer.ts"
      via: "writer.updateAiConfig(patch) → AiConfigV1 검사 뒤 storage.local"
      pattern: "updateAiConfig"
    - from: "src/core/ai/cleanup-copy.ts"
      to: "docs/design/SYSTEM.md"
      via: "상태 표 문구와 글자 하나까지 같음(verify의 비교 스크립트)"
      pattern: "자주 누른 순서로 번호를 붙였어요"
---

## Phase Goal

**As a** 이용자, **I want to** 처음 보는 복잡한 화면에서 명령판의 "화면 정리"를 누르면 중요한 요소 9개에 번호가 붙기를, **so that** AI가 없거나 실패해도 나머지 기능은 그대로이고, 개인정보와 입력 값은 밖으로 나가지 않는다.

(ROADMAP Phase 6 **Goal** 문장을 그대로 세 칸에 옮긴 것이다. 새 내용은 더하지 않았다. `/gsd-mvp-phase 6`으로 다듬을 수 있다.)

<objective>
화면 정리의 상태 문구를 디자인 시스템에 먼저 적고(D-22·D-23·D-24, DESIGN.md §4), Phase 3이 만든 설정 화면에 AI 키와 한 달 한도 칸을 더한다. 키는 이 PC에만 두고 어디로도 넘기지 않는다(AI-05, D-18).

Purpose: D-16·D-17·D-18·D-22·D-23·D-24, AI-05, AI-04의 "이용자가 정한 한도".
Output: SYSTEM.md·DECISIONS.md 갱신, `src/entrypoints/options/ai-section.ts`, 저장자의 `updateAiConfig`, `tests/e2e/ai-settings.e2e.ts`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai/06-CONTEXT.md
@.planning/phases/06-ai/06-01-SUMMARY.md
@.planning/phases/06-ai/06-03-SUMMARY.md
</context>

## 앞선 단계 의존

- Phase 3: 설정 화면(옵션 페이지) `src/entrypoints/options/main.ts`와 그 화면의 SYSTEM.md 템플릿(Phase 3 D-38), 설정 파일 내보내기(Phase 3 D-35 — storage.sync만 담음), 옵션 페이지의 저장 요청 방식(`storage/request`). 06-01 Task 1이 이름을 확인했다. 06-01-SUMMARY에 다른 이름이 적혀 있으면 그것을 쓴다. 옵션 페이지 e2e가 페이지를 여는 주소와 내보내기 버튼 이름은 Phase 3 e2e에서 가져온다.
- 06-01: `src/core/ai/ai-storage.ts`(`AI_CONFIG_KEY`, `AiConfigV1`, `defaultAiConfig`, `DEFAULT_AI_MONTHLY_LIMIT`), `src/core/ai/cleanup-copy.ts`, `tests/e2e/fake-anthropic.ts`.
- 06-03: `aiUsageKey`, `monthKeyOf`, `AiUsageV1`, 로딩 문구 `AI_LOADING_HINT`, 한도 적용 흐름.
- 이 계획은 06-04와 같은 wave에서 병렬로 돈다. `client.ts`, `gateway.ts`, `fake-anthropic.ts`는 고치지 않는다(시험 도움 함수가 더 필요하면 이 계획의 e2e 파일 안에 둔다).

## 실행 규칙 (모든 작업 공통)

- 패키지 매니저는 pnpm만. TypeScript strict, `any` 금지, 시크릿을 코드·커밋에 넣지 않는다(CLAUDE.md). 새 패키지·권한 없음.
- 화면은 `docs/design/SYSTEM.md`와 `docs/design/tokens.css`만 따른다. 새 색·서체·radius·그림자를 만들지 않는다(D-23, CLAUDE.md 프론트엔드). 새 화면 부분은 DESIGN.md §4 순서대로 — 템플릿이 없으면 SYSTEM.md에 먼저 더한다.
- 구현 전에 Superpowers `test-driven-development`를, 시험 실패를 쫓기 전에 `systematic-debugging`을, "완료" 전에 `verification-before-completion`을 **호출**한다. 서브에이전트에 맡기면 프롬프트에 세 스킬과 `model`을 적는다.
- RED 커밋 `test(06-05): …` → GREEN 커밋 `feat(06-05): …`, 문서는 `docs(06-05): …`. 제목은 영어 접두어, 본문은 한국어.
- 완료 판정은 `CI=true` e2e다. UI 최종 판정은 06-06의 독립 DOM 감사다. 자동 시험은 진짜 Anthropic API를 부르지 않고 시험 키는 `FAKE_AI_KEY`뿐이다(D-28).
- 실제 회사 시스템에 접속하거나 시험하지 않는다(D-29).

<tasks>

<task type="auto">
  <name>Task 1: SYSTEM.md에 화면 정리 상태 문구와 설정 화면 AI 부분 템플릿 적기</name>
  <files>docs/design/SYSTEM.md, docs/design/DECISIONS.md</files>
  <read_first>
    - docs/design/SYSTEM.md (상태 표 "화면 정리(AI)" 행, 카피 규칙, 크기 규칙, Phase 3이 더한 설정 화면 템플릿)
    - docs/DESIGN.md (§4 통일, §6 품질 바닥)
    - docs/design/DECISIONS.md (기록 형식)
    - src/core/ai/cleanup-copy.ts (06-01·06-03 문구)
    - .planning/phases/06-ai/06-CONTEXT.md (D-22, D-23, D-24)
  </read_first>
  <action>
`docs/design/SYSTEM.md` 상태 표 "화면 정리(AI)" 행을 구체 문구로 채운다(D-22 뜻은 그대로): 불러오는 중 = `화면을 정리하는 중 · Esc 취소`(0.3초 안에 끝나면 표시 안 함), 성공 = 번호표 + 모드 표시 `AI가 고른 번호`, 실패 = 원인별 다섯 문구(`cleanup-copy.ts`와 글자 하나까지 같게: 키 없음, 한도 초과, 키 틀림, 연결 못 함, 답을 쓸 수 없음) 4초 표시 후 자주 누른 순서 번호표, Esc 취소 = 표시 없이 원래대로. 문구가 카피 규칙(알림 "…했어요", 원인 먼저, 키 이름 영어)과 맞는지 확인한다.

Phase 3이 SYSTEM.md에 둔 설정 화면 템플릿에 "화면 정리(AI)" 부분을 더한다(D-24). 템플릿이 없거나 이 부분을 덮지 못하면 DESIGN.md §4-1대로 템플릿을 먼저 적는다. 내용: 제목 "화면 정리(AI)", 안내 한 줄("화면 정리를 누르면 이 화면의 버튼·링크 글자와 대략의 위치가 Anthropic AI로 가요. 입력한 값과 페이지 주소는 보내지 않아요."), AI 키 칸(가려 넣기, 저장 뒤 비움, "저장된 키: 끝 4자리 ••••abcd" 또는 "저장된 키 없음", [저장]·[지우기] 2차 버튼), 한 달 한도 칸(숫자 0~10000, 단위 "번", 오류 문구 "한도는 0부터 10000까지 숫자예요. 다시 넣어 주세요."), 사용량 문장("이번 달 AI 요청 N번 / 한도 M번", 읽을 수 없으면 "이번 달 사용량을 읽을 수 없어요"). 글자 15px 이상, 누르는 대상 높이 56px 이상, 기존 토큰만.

`docs/design/DECISIONS.md`에 한 항목: 왜 실패 문구를 원인별로 나눴는지(D-23 "원인. 다음 행동.")와 설정 화면 AI 부분을 더한 것. 한도 단위(횟수)는 사용자 확인 대기임을 적는다(06-03 "사용자 확인 대기 선택" 1).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/core/ai/cleanup-copy.ts','utf8');const s=fs.readFileSync('docs/design/SYSTEM.md','utf8');const m=[...c.matchAll(/'([^']*[가-힣][^']*)'/g)].map(x=>x[1]);if(m.length<7||m.some(t=>!s.includes(t)))process.exit(1)"</automated>
    <fails_when>non-zero exit — fewer than 7 Korean strings were found in cleanup-copy.ts, or at least one of them does not appear verbatim in docs/design/SYSTEM.md</fails_when>
    <automated>grep -q '이번 달 AI 요청' docs/design/SYSTEM.md && grep -q '화면 정리' docs/design/DECISIONS.md</automated>
    <fails_when>non-zero exit — the settings AI template or the DECISIONS entry is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - SYSTEM.md 상태 표 "화면 정리(AI)" 행에 `cleanup-copy.ts`의 문구 일곱 개가 글자 그대로 있다
    - SYSTEM.md 설정 화면 템플릿에 AI 키·한 달 한도·사용량 문장이 있다
    - DECISIONS.md에 이번 변경의 이유 항목이 있다
    - 새 색·서체·radius·그림자 토큰이 더해지지 않았다(`docs/design/tokens.css` 변경 없음)
  </acceptance_criteria>
  <done>화면 정리의 모든 상태 문구와 설정 화면 AI 부분이 디자인 시스템에 먼저 적혔다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 설정 화면 AI 부분 — 키(이 PC만)·지우기·한 달 한도·이번 달 사용량</name>
  <reversibility rating="costly">키를 local에만 두고 내보내기에서 빼는 규칙은 이용자 PC의 저장 위치를 정한다. 뒤집으려면 저장된 키를 옮겨야 한다.</reversibility>
  <files>src/entrypoints/options/ai-section.ts, src/entrypoints/options/main.ts, src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/background.ts, tests/e2e/ai-settings.e2e.ts</files>
  <read_first>
    - src/entrypoints/options/main.ts와 Phase 3 옵션 페이지의 다른 부분(렌더링 팩토리, 저장 요청 방식), Phase 3 옵션 페이지 e2e(페이지 주소, 내보내기 버튼)
    - src/shared/messages.ts (storage/request 연산 유니온, UpdateSettingsOp), src/worker/storage-writer.ts, src/entrypoints/background.ts
    - src/core/ai/ai-storage.ts (AiConfigV1, AiUsageV1, aiUsageKey, monthKeyOf)
    - docs/design/SYSTEM.md (Task 1 결과), docs/design/tokens.css
    - tests/e2e/fake-anthropic.ts (FAKE_AI_KEY, installFakeAnthropic, pressCleanupCard)
  </read_first>
  <behavior>
    - 설정 화면에서 AI 키 칸에 `FAKE_AI_KEY`를 넣고 [저장]하면 storage.local `ai-config`의 키가 그 값이고, `chrome.storage.sync.get(null)`을 문자열로 만든 것에 그 값이 없으며, 칸이 비고 "끝 4자리"로 `real`이 보이고, 설정 화면 DOM(속성 포함 HTML)에 키 전체가 없다
    - Phase 3 설정 파일 내보내기로 받은 파일 내용에 키가 없다
    - [지우기] 뒤 연습 페이지에서 카드를 누르면 AI 요청 0개와 "AI 키가 없어요. 자주 누른 순서로 번호를 붙였어요", 그 뒤 F 번호표가 붙는다
    - 한도를 1로 저장하고 한 번 화면 정리한 뒤 설정 화면에 "이번 달 AI 요청 1번 / 한도 1번"이 보이고, 다른 화면에서 누르면 요청 없이 한도 초과 문구
    - 한도에 `10001`이나 `-1`이나 `abc`를 넣으면 저장되지 않고 오류 문구가 보이며 기존 한도가 그대로다
    - 연습 페이지(content script 쪽)에서 보낸 AI 설정 저장 요청은 거부되어 storage.local이 바뀌지 않는다
  </behavior>
  <action>
behavior 경우로 `tests/e2e/ai-settings.e2e.ts`를 먼저 쓰고 RED 커밋. 설정 화면은 Phase 3 e2e가 여는 확장 페이지 주소로 연다. 내보내기 파일은 `page.waitForEvent('download')`로 받는다. 거부 경우는 시험이 `serviceWorker.evaluate` 안에서 `chrome.scripting.executeScript({ target: { tabId: <연습 페이지 탭> }, world: 'ISOLATED', func })`로 연습 페이지에 content script 자격의 코드를 넣어 `updateAiConfig` 저장 요청을 보내게 하고(발신자 주소 = 웹 페이지), storage.local `ai-config`가 그대로인지 본다. 이미 있는 `scripting` 권한을 시험에서만 쓰는 것이라 제품 코드에 시험 전용 메시지를 더하지 않는다.

- `src/shared/messages.ts`: 기존 `storage/request` 연산 유니온에 `updateAiConfig { patch: { apiKey?: string(1..500) | null, monthlyLimit?: 정수 0..10000 } }`를 더한다. 응답은 `{ ok: true } | { ok: false, reason: 'invalid-patch' }`이고 키를 되돌려 싣지 않는다.
- `src/entrypoints/background.ts`: `updateAiConfig`는 발신자 주소가 확장 자신의 주소(`chrome.runtime.getURL('')`로 시작)일 때만 저장자로 넘긴다. content script(발신자 주소가 웹 페이지)에서 온 것은 거부한다(D-18).
- `src/worker/storage-writer.ts`: `updateAiConfig(patch)`를 `enqueue`로. 저장된 `ai-config`를 `AiConfigV1`로 읽고(없거나 읽을 수 없으면 `defaultAiConfig()`에서 시작 — 이용자가 직접 저장하는 것이라 읽을 수 없던 값을 바꾼다), `apiKey: null`이면 키를 지우고, 문자열이면 앞뒤 공백을 뺀 값으로, `monthlyLimit`이 있으면 바꾼 뒤 `AiConfigV1`로 검사해 `chrome.storage.local`에 쓴다. storage.sync에는 절대 쓰지 않는다.
- `src/entrypoints/options/ai-section.ts`: `mountAiSection(container)`. Task 1 템플릿대로 Phase 3 옵션 페이지의 렌더링 방식(순수 DOM)과 토큰만 쓴다. 키 칸은 `type=password`, `autocomplete=off`. [저장]은 `updateAiConfig({ apiKey })` 뒤 칸을 비우고 상태를 다시 읽는다. 끝 4자리는 storage.local의 `ai-config`를 읽어 보이고(설정 화면은 키를 읽어도 되는 곳, D-18), 키 전체를 DOM 속성·글자·콘솔에 두지 않는다. [지우기]는 `updateAiConfig({ apiKey: null })`. 한도 칸은 정수 0~10000만 저장하고 아니면 Task 1 오류 문구. 사용량 문장은 `aiUsageKey(monthKeyOf(new Date()))`를 읽어 `AiUsageV1`로 검사한다(읽기만, 쓰기는 저장자만).
- `src/entrypoints/options/main.ts`: 기존 부분들 뒤에 `mountAiSection`을 부르는 한 줄(자리는 Phase 3 템플릿 순서).
GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-settings.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report at least 6 passed, or the output contains "No tests found"</fails_when>
    <automated>test -z "$(grep -rlE 'AI_CONFIG_KEY|ai-config' src | grep -vE '^src/(core/ai/ai-storage\.ts|worker/|entrypoints/options/)')"</automated>
    <fails_when>non-zero exit — a file outside ai-storage.ts, src/worker/ and the options page (for example content.ts or src/page/) references the AI config key</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit, or any "@typescript-eslint/no-explicit-any" error in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/entrypoints/options/ai-section.ts`가 `mountAiSection`을 export하고 `main.ts`가 부른다
    - `src/worker/storage-writer.ts`의 `updateAiConfig`가 `chrome.storage.local`에만 쓴다(같은 함수 안에 `storage.sync` 없음)
    - `src/entrypoints/background.ts`가 `updateAiConfig`를 확장 페이지 발신자일 때만 넘긴다
    - AI 설정 키를 참조하는 파일이 `ai-storage.ts`, `src/worker/`, 옵션 페이지뿐이다(verify의 grep)
    - `CI=true pnpm exec playwright test tests/e2e/ai-settings.e2e.ts`가 6개 이상 통과한다
  </acceptance_criteria>
  <done>이용자가 설정 화면에서 키를 넣고 지우고 한도를 바꿀 수 있고, 키는 이 PC에만 있으며 동기화·내보내기·페이지 쪽 어디에도 없고, 한도와 사용량이 화면에 보인다.</done>
</task>

</tasks>

<!-- planner-discipline-allow: ai-config -->
<!-- planner-discipline-allow: AI_CONFIG_KEY -->

## 가정 (spec-less edge probe)

- AI-05 (주 계획): edge probe `unclassified — review manually`. 가정: "키는 이 PC에만"은 storage.local 저장 + sync·내보내기 부재 + content·페이지 쪽 코드가 키 저장 이름을 모름(grep)으로 확인한다. 키 형식(접두어 등)은 검사하지 않는다 — 틀린 키는 첫 호출에서 "AI 키가 맞지 않아요"로 알린다(06-04). 키를 가려 보이는 방법은 끝 4자리다.
- AI-04 (일부 — 이용자가 정하는 한도): 가정: 한도는 0~10000번 정수, 기본 100번, 0이면 AI를 쓰지 않는다. 단위(횟수)는 사용자 확인 대기다 — 금액으로 바뀌면 이 계획 Task 2의 한도 칸·사용량 문장과 Task 1의 SYSTEM.md 문구를 바꾼다(06-03 "사용자 확인 대기 선택" 1).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 설정 화면(확장 페이지) → service worker | 이용자가 넣은 키·한도가 저장자로 간다 |
| content script(웹 페이지 안) → service worker | 페이지 쪽 코드가 설정 저장 요청을 흉내 낼 수 있다 |
| storage.local → 설정 파일 내보내기 | 내보내기가 담는 범위 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-20 | Information Disclosure | AI 키 저장 위치 | high | mitigate | storage.local에만 쓰고 sync에 쓰지 않음, e2e가 sync 전체와 내보내기 파일에 키 부재 확인(D-18) |
| T-06-21 | Information Disclosure | 설정 화면 DOM·콘솔 | medium | mitigate | 끝 4자리만 표시, 저장 뒤 칸 비움, e2e가 DOM HTML에 키 전체 부재 확인, 로그 없음 |
| T-06-22 | Elevation of Privilege | `updateAiConfig` 저장 요청 | high | mitigate | 발신자 주소가 확장 페이지일 때만 처리, content script 발신 거부를 시험으로 확인 |
| T-06-23 | Information Disclosure | content·페이지 코드가 키에 닿음 | high | mitigate | 키 저장 이름을 참조하는 파일을 ai-storage·worker·옵션 페이지로 제한(grep), 메시지 응답에 키 없음 |
| T-06-24 | Tampering | 한도 값 | low | mitigate | 정수 0~10000 zod 검사, 틀린 값은 저장하지 않고 기존 값 유지 |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/ai-settings.e2e.ts` 통과
- `cleanup-copy.ts` 문구 일곱 개가 SYSTEM.md에 글자 그대로 있다(Task 1 verify 스크립트)
- AI 설정 키 참조 파일 제한 grep 통과
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 디자인 시스템이 화면보다 먼저 갱신됐다(DESIGN.md §4 순서)
- 키가 이 PC에만 있고 sync·내보내기·페이지 쪽에 없음이 CI=true e2e로 확인됐다
- 한도 변경·사용량 표시·키 지우기 뒤 대체가 확인됐다
- `docs(06-05)`·RED(`test(06-05)`)·GREEN(`feat(06-05)`) 커밋이 있다
</success_criteria>

## Artifacts this phase produces

- 파일: `src/entrypoints/options/ai-section.ts`, `tests/e2e/ai-settings.e2e.ts`
- 심볼: `mountAiSection`, `StorageWriter.updateAiConfig`, `storage/request` 연산 `updateAiConfig`
- 문서: `docs/design/SYSTEM.md` 상태 표 "화면 정리(AI)" 행과 설정 화면 AI 부분, `docs/design/DECISIONS.md` 항목

<output>
Create `.planning/phases/06-ai/06-05-SUMMARY.md` when done (Phase 3 옵션 페이지 실제 이름, 발신자 거부 시험 방법 포함)
</output>
