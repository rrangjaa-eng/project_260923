---
phase: 06-ai
plan: 05
type: tdd
wave: 4
depends_on: ["06-04"]
coupling_justified: ["06-06: 같은 wave — 이 계획은 buildAiPayload의 글자 값(가림·민감칸 표시)만 바꾸고 결과 모양 { entries, idToKey }는 그대로다. 06-06은 그 함수를 부르는 창구 흐름(저장된 결과·한도)만 바꾼다. 가림 표시·민감칸 표시는 빈 글자가 아니어서 06-06의 \"보낼 목록이 비면 대체\" 판단을 바꾸지 않고, 이 계획의 개인정보 e2e는 첫 화면 정리(저장 없음, 기본 한도 100)라 06-06 변경 전후 어느 쪽에서도 요청 1개가 나간다 — 두 순서 모두 결과가 같다"]
files_modified:
  - src/core/ai/mask.ts
  - tests/unit/ai-mask.test.ts
  - src/page/collector/ai-text.ts
  - tests/unit/ai-text.test.ts
  - src/core/ai/request-builder.ts
  - tests/unit/ai-request-builder.test.ts
  - tests/e2e/ai-privacy.e2e.ts
  - tests/practice-site/ai-privacy.html
  - tests/practice-site/ai-privacy-frame.html
autonomous: true
requirements: [AI-02]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "AI로 가는 요청 본문에 입력칸·textarea·contenteditable·select의 값이 하나도 없고, 민감칸(비밀번호·주민등록번호 칸)의 값도 없다 — e2e가 가짜 AI가 받은 요청 본문으로 확인한다(AI-02, D-10, D-11, D-13)"
    - "요소 글자 속 이메일은 `[이메일]`, 한국 휴대폰·일반 전화·+82 번호는 `[전화번호]`, 하이픈·공백으로 이어진 것을 포함한 9자리 이상 숫자 줄은 `[숫자]`로 가려져 나가고, 날짜 `2026-09-23`와 8자리 이하 숫자는 그대로 나간다(단위 시험, D-10, D-27)"
    - "가림은 수집할 때(content)와 보내기 직전(service worker의 요청 만들기) 두 번 하고, 이미 가린 글자를 다시 가려도 결과가 같다(D-10)"
    - "요청에는 요소 종류·보이는 글자·대략의 위치(9구역 이름)·임시 번호만 있고, 페이지 주소·제목·id·name·href·요소 식별 묶음이 없다(D-09)"
    - "Phase 3의 한 곳의 민감칸 판별 함수(`isSensitive`)가 민감칸으로 판정한 칸은 요청에 종류와 고정 표시 `[입력칸]`만 나가고, 그 칸의 라벨 글자(예: \"비밀번호\")도 값도 나가지 않는다 — 단위 시험과 e2e가 확인한다(D-11, Phase 3 D-26, PRIV-01)"
  prohibitions:
    - statement: "AI 요청에 입력칸 값, 페이지 주소(쿼리 포함)·제목, 요소 속성 값(id·name·href 등), 요소 식별 묶음을 넣는 경로를 만들지 않는다 — 보내는 것은 종류·가린 보이는 글자·9구역 이름·임시 번호뿐이다(D-09, D-10, PRIV-01)"
      requirement_id: AI-02
      category: security
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/core/ai/mask.ts"
      provides: "개인정보 모양 가림(이메일 → 전화 → 긴 숫자 순서, 멱등)"
      exports: ["maskPii"]
    - path: "src/page/collector/ai-text.ts"
      provides: "요소 하나의 AI용 글자 — 글자 칸이면 라벨·aria·`placeholder` 속성만, 값은 읽지 않음, 민감칸은 고정 표시, 가림 적용"
      exports: ["aiTextOf", "SENSITIVE_FIELD_TEXT"]
    - path: "src/core/ai/request-builder.ts"
      provides: "보내기 직전 가림을 한 번 더 한 보낼 목록"
      contains: "maskPii"
    - path: "tests/e2e/ai-privacy.e2e.ts"
      provides: "가로챈 요청 본문에 값·개인정보·주소·제목·속성 값이 없음을 확인하는 e2e(iframe 포함)"
  key_links:
    - from: "src/page/collector/ai-text.ts"
      to: "src/core/ai/mask.ts"
      via: "aiTextOf가 돌려주기 전에 maskPii"
      pattern: "maskPii"
    - from: "src/core/ai/request-builder.ts"
      to: "src/core/ai/mask.ts"
      via: "buildAiPayload가 자르기 전에 maskPii — SW에서 한 번 더"
      pattern: "maskPii"
    - from: "src/page/collector/ai-text.ts"
      to: "src/core/sensitive.ts"
      via: "글자 칸이면 Phase 3 isSensitive로 판정해 민감칸은 SENSITIVE_FIELD_TEXT만 돌려준다"
      pattern: "isSensitive"
---

## Phase Goal

**As a** 이용자, **I want to** 처음 보는 복잡한 화면에서 명령판의 "화면 정리"를 누르면 중요한 요소 9개에 번호가 붙기를, **so that** AI가 없거나 실패해도 나머지 기능은 그대로이고, 개인정보와 입력 값은 밖으로 나가지 않는다.

(ROADMAP Phase 6 **Goal** 문장을 그대로 세 칸에 옮긴 것이다. 새 내용은 더하지 않았다. `/gsd-mvp-phase 6`으로 다듬을 수 있다.)

<objective>
AI로 나가는 요소 목록에서 개인정보와 입력 값을 빼는 규칙을 단위 시험으로 고정하고(가림 규칙, 요소 하나의 AI용 글자와 민감칸 처리, 보낼 목록 모양), 실제로 가로챈 요청 본문으로 다시 확인한다(ROADMAP 성공 기준 2).

Purpose: D-09·D-10·D-11·D-13·D-27(가림·값 없음·주소 없음·민감칸 단위 시험), AI-02, PRIV-01의 AI 전송 부분. D-11은 제품 경로에서 지킨다 — AI용 글자를 만드는 `aiTextOf`가 Phase 3의 한 곳의 판별 함수 `isSensitive`를 불러, 민감칸이면 라벨 글자 대신 고정 표시 `[입력칸]`만 보낸다(계획 점검 반영).
Output: `src/core/ai/mask.ts`, 가림·민감칸 처리가 들어간 `ai-text.ts`, 가림이 들어간 `request-builder.ts`, 단위 시험 3개(2개는 06-02·06-03 파일에 경우를 더함), 개인정보 연습 페이지, `tests/e2e/ai-privacy.e2e.ts`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai/06-CONTEXT.md
@.planning/phases/06-ai/06-RESEARCH.md
@.planning/phases/06-ai/06-01-SUMMARY.md
@.planning/phases/06-ai/06-03-SUMMARY.md
@.planning/phases/06-ai/06-04-SUMMARY.md
</context>

## 앞선 단계 의존

- 06-01: `tests/e2e/fake-anthropic.ts`(`installFakeAnthropic`, `setAiConfig`, `pressCleanupCard`, `FAKE_AI_KEY`)와 06-01-SUMMARY의 가로채기 방법, Task 1 precondition이 찾은 Phase 3 실제 이름.
- 06-02: `src/core/ai/request-builder.ts`(`buildAiPayload`, `AiCandidate`)와 `tests/unit/ai-request-builder.test.ts`(모양·순서·상한·구역 경우 — 이 계획은 가림 경우를 더한다).
- 06-03: `src/page/collector/ai-text.ts`(`aiTextOf(el, { name, labelText })`, 값 안 읽기)와 `tests/unit/ai-text.test.ts`(글자 칸 규칙 경우 — 이 계획은 가림·민감칸 경우를 더한다).
- 06-04: 화면 정리 요청이 AI 창구 → 가짜 AI까지 가는 경로, `tests/practice-site/ai-cleanup.html`.
- Phase 3: `src/core/sensitive.ts`의 `isSensitive`(Phase 3 D-26 "한 곳의 판별 함수") — 입력 모양은 Phase 3이 정한 대로 쓴다. 06-01 Task 1이 이 이름을 확인했다. 이름이 달랐으면 06-01-SUMMARY에 적힌 실제 이름을 쓴다(Task 2의 precondition과 대안 참고).
- Phase 1: `src/page/collector/collector.ts`의 `computeName`, `kindOf`, `labelTextOf`.
- 이 계획은 06-06과 같은 wave에서 병렬로 돈다(`coupling_justified` 참고). `messages.ts`, `content.ts`, `gateway.ts`, `background.ts`, `fake-anthropic.ts`, `storage-writer.ts`는 고치지 않는다(06-06·06-07 소유).

## 실행 규칙 (모든 작업 공통)

- 패키지 매니저는 pnpm만. TypeScript strict, `any` 금지, 시크릿을 코드·커밋에 넣지 않는다(CLAUDE.md). 새 패키지 없음.
- 구현 전에 Superpowers `test-driven-development`를, 시험 실패를 쫓기 전에 `systematic-debugging`을, "완료" 전에 `verification-before-completion`을 **호출**한다. 서브에이전트에 맡기면 프롬프트에 세 스킬과 `model`을 적는다.
- 각 작업은 RED 커밋 `test(06-05): …` → GREEN 커밋 `feat(06-05): …` → 필요하면 `refactor(06-05): …`. 제목은 영어 접두어, 본문은 한국어.
- 완료 판정은 `CI=true` e2e다. 자동 시험은 진짜 Anthropic API를 부르지 않고 시험 키는 `FAKE_AI_KEY`뿐이다(D-28).
- 실제 회사 시스템에 접속하거나 시험하지 않는다(D-29). 연습 페이지의 이름·번호·주소는 모두 지어낸 값이다.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 개인정보 모양 가림 `maskPii` (순수 함수)</name>
  <reversibility rating="reversible">가림 규칙은 순수 함수 하나와 그 단위 시험이다. 저장 형식에 들어가지 않는다.</reversibility>
  <files>src/core/ai/mask.ts, tests/unit/ai-mask.test.ts</files>
  <read_first>
    - .planning/phases/06-ai/06-CONTEXT.md (D-10, D-13, D-27, discretion "가릴 모양의 정확한 규칙")
    - .planning/phases/06-ai/06-RESEARCH.md ("Code Examples" 요소 목록 → 마스킹된 요청 페이로드, Validation "Wave 0 Gaps")
    - .planning/phases/06-ai/06-PATTERNS.md (`src/core/ai/mask.ts` 절)
  </read_first>
  <behavior>
    - `'hong.gildong+a@example.co.kr 에게'` → `'[이메일] 에게'`
    - `'010-1234-5678'`, `'01012345678'`, `'010 1234 5678'`, `'02-123-4567'`, `'031-123-4567'`, `'070-1234-5678'`, `'+82-10-1234-5678'`, `'+82 2 123 4567'` → 각각 `'[전화번호]'`
    - `'900101-1234567'`(주민번호 모양), `'1234-5678-9012-3456'`(카드), `'1234 5678 9012 3456'`, `'123-456789-01-234'`(계좌), `'123456789'` → 각각 `'[숫자]'`
    - 그대로 두는 것: `'2026-09-23'`, `'12345678'`, `'1,234,567,890원'`(쉼표·점은 이어 주지 않음), `'3.14159265'`, `'결재 7건'`
    - 섞인 글자: `'담당 홍길동 010-9876-5432 / hong@example.com'` → `'담당 홍길동 [전화번호] / [이메일]'`
    - 멱등: 어떤 입력이든 `maskPii(maskPii(x)) === maskPii(x)`
  </behavior>
  <action>
behavior 경우마다 시험을 먼저 쓰고 실패를 확인한다(RED). 그다음 `src/core/ai/mask.ts`에 `maskPii(text: string): string`을 만든다(GREEN). 순서는 이메일 → 전화번호 → 긴 숫자다(전화번호가 긴 숫자 규칙에 먼저 먹히지 않게). 규칙(D-10 discretion을 이렇게 정한다):
- 이메일: 로컬부 `[A-Za-z0-9._%+-]+`, `@`, 도메인 `[A-Za-z0-9.-]+`, 점, 2글자 이상 영문 끝.
- 전화번호: 앞뒤가 숫자가 아닌 자리에서, `0` 또는 `+82`(뒤에 하이픈·공백 하나 허용)로 시작하고 지역·이동 번호(`1[016789]`, `2`, `[3-6][1-5]`, `70`) 뒤에 3~4자리, 4자리가 오며 사이에 하이픈·공백·점 하나까지 허용.
- 긴 숫자: 앞뒤가 숫자가 아닌 자리에서, 숫자 사이에 하이픈이나 공백 하나까지만 끼어 이어진 숫자가 9자리 이상. 쉼표·점은 잇는 문자로 보지 않는다. 날짜+시각(`2026-09-23 10:30`)은 공백으로 이어져 가려질 수 있다 — 개인정보 쪽으로 기운 선택으로 시험에 한 줄 적어 둔다.
바꿀 표시 `[이메일]`, `[전화번호]`, `[숫자]`에는 숫자와 `@`가 없어 멱등이 성립한다. 주민번호·계좌·카드는 따로 판별하지 않고 긴 숫자 규칙이 덮는다(민감칸 판별 함수를 새로 만들지 않는다, D-11).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/ai-mask.test.ts</automated>
    <fails_when>non-zero exit, or the Tests summary line reports fewer than 20 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/ai/mask.ts`가 `maskPii`를 export한다
    - `tests/unit/ai-mask.test.ts`에 behavior의 전화번호 8개, 긴 숫자 5개, 그대로 두는 것 5개, 섞인 글자, 멱등 경우가 있다
    - `pnpm exec vitest run tests/unit/ai-mask.test.ts`가 20개 이상 통과
  </acceptance_criteria>
  <done>가림 규칙이 경우별 단위 시험으로 고정됐고 이메일·전화·긴 숫자가 가려지며 날짜·짧은 숫자는 남는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 요소 하나의 AI용 글자(민감칸 처리 포함)와 보낼 목록의 가림 (값 없음·두 번 가림)</name>
  <precondition>Phase 3의 한 곳의 민감칸 판별 함수가 있다 — `grep -q "export function isSensitive" src/core/sensitive.ts`가 0으로 끝나거나, 06-01-SUMMARY에 다른 실제 이름·위치가 적혀 있고 그 파일에서 그 이름이 export된다. 둘 다 아니면 멈추고 보고한다(두 번째 판별 함수를 만들지 않는다, D-11).</precondition>
  <reversibility rating="reversible">content의 글자 추출과 SW의 요청 만들기 순수 함수다. 저장 형식이 아니다.</reversibility>
  <files>src/page/collector/ai-text.ts, tests/unit/ai-text.test.ts, src/core/ai/request-builder.ts, tests/unit/ai-request-builder.test.ts</files>
  <read_first>
    - src/page/collector/ai-text.ts, tests/unit/ai-text.test.ts (06-03 결과), src/core/ai/request-builder.ts, tests/unit/ai-request-builder.test.ts (06-02 결과)
    - src/core/sensitive.ts (Phase 3 `isSensitive`의 이름·입력 모양 — 요소를 받는지, 설명 객체를 받는지)
    - .planning/phases/06-ai/06-01-SUMMARY.md (Phase 3 실제 이름)
    - .planning/phases/06-ai/06-CONTEXT.md (D-09, D-10, D-11)
    - .planning/phases/06-ai/06-PATTERNS.md (`request-builder.ts` 절)
  </read_first>
  <behavior>
    - ai-text: 비밀번호 칸(type=password, 라벨 "비밀번호", 값 있음)과 주민등록번호 칸(라벨 "주민등록번호", 값 `900101-1234567`)은 Phase 3 `isSensitive`가 민감으로 판정하는 고정물이고(시험 전제 확인 — 먼저 단언), `aiTextOf`는 두 칸 모두 `SENSITIVE_FIELD_TEXT`(`'[입력칸]'`)를 돌려준다 — 라벨 "비밀번호"·"주민등록번호"도 값도 결과에 없다
    - ai-text: 민감하지 않은 글자 칸(라벨 "이메일", 값 `hong@example.com`)은 그대로 라벨 `'이메일'`을 돌려준다(06-03 규칙 유지)
    - ai-text: 버튼 글자 `'담당 010-9876-5432'` → `'담당 [전화번호]'`(가림 적용), 라벨 `'hong@example.com 주소'`인 칸 → `'[이메일] 주소'`
    - ai-text: `SENSITIVE_FIELD_TEXT`는 `maskPii`를 거쳐도 그대로다(숫자·`@` 없음)
    - request-builder: 가리지 않은 글자(`'hong@example.com'`)가 들어와도 entries의 text는 `'[이메일]'`(SW에서 한 번 더 가림), 가린 뒤 60자로 자른다(가림 표시가 잘려 반쪽 숫자가 남지 않는다)
    - request-builder: 후보 객체에 요소 식별 묶음(id `'id-SECRET-1'`, name, 라벨)이 덧붙어 와도 `JSON.stringify(buildAiPayload(...).entries)`에 그 값들이 없다
  </behavior>
  <action>
behavior 경우를 `tests/unit/ai-text.test.ts`와 `tests/unit/ai-request-builder.test.ts`에 더하고 RED 커밋.

`src/page/collector/ai-text.ts`(D-10·D-11):
- 민감칸(D-11, 계획 점검 반영 — Phase 3 D-26 "AI 전송도 이 함수를 쓴다"를 제품 경로에서 지킨다): 06-03의 글자 칸 판정에 걸린 요소마다 Phase 3의 `isSensitive`를 부르고, 민감칸이면 라벨·aria·`placeholder` 속성 대신 고정 표시 `SENSITIVE_FIELD_TEXT = '[입력칸]'`을 돌려준다. 그 요소는 보낼 목록에 종류와 이 표시로만 남는다(라벨 글자 없음, 값 없음). `isSensitive`는 DOM 요소가 있는 이곳(각 프레임의 content)에서 부른다 — service worker의 요청 만들기에는 요소가 없다. 이 파일에 민감칸 판별 규칙을 새로 쓰지 않는다(판별은 Phase 3 함수 한 곳).
- Phase 3 함수의 이름·입력 모양이 가정과 다를 때: 06-01-SUMMARY에 적힌 실제 이름을 쓴다. 요소 대신 설명 객체(예: type·name·autocomplete·라벨 글자)를 받으면 `aiTextOf`가 요소의 속성과 넘겨받은 `labelText`로 그 설명을 만들어 넘긴다(값은 넣지 않는다). 어느 쪽이든 SUMMARY에 실제 이름과 입력 모양을 적는다.
- 가림: 돌려주기 전에 `maskPii`를 적용한다(민감칸 표시도 거치지만 바뀌지 않는다).
- 06-03의 규칙(글자 칸이면 라벨 → aria-label → `placeholder` 속성, 값·선택된 option·안 글자를 읽지 않음, 그 밖은 보이는 이름)은 그대로 둔다.

`src/core/ai/request-builder.ts`의 `buildAiPayload`: 각 후보 글자에 `maskPii`를 먼저 하고 60자로 자른다(D-10 "보내기 직전 한 번 더"). 결과 항목은 06-02대로 `{ id, kind, text, zone }` 네 키로만 새로 만든다(D-09). GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/ai-text.test.ts tests/unit/ai-request-builder.test.ts</automated>
    <fails_when>non-zero exit, or the output reports "No test files found", or any test in the summary is failed</fails_when>
    <automated>! grep -nE '\.value\b|selectedOptions|innerText|textContent' src/page/collector/ai-text.ts</automated>
    <fails_when>non-zero exit because grep printed a line — ai-text.ts reads a field value, selected option, or inner text</fails_when>
    <automated>grep -q "isSensitive" src/page/collector/ai-text.ts && test "$(grep -rl 'function isSensitive' src)" = "src/core/sensitive.ts"</automated>
    <fails_when>non-zero exit — ai-text.ts does not call Phase 3's isSensitive, or a second sensitive-field detector exists outside src/core/sensitive.ts (if 06-01-SUMMARY recorded a different real name, the executor substitutes that name and file here and notes it in SUMMARY)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/collector/ai-text.ts`가 Phase 3의 `isSensitive`(또는 06-01-SUMMARY의 실제 이름)를 import해 부르고, `SENSITIVE_FIELD_TEXT`를 export한다
    - `src/page/collector/ai-text.ts`와 `src/core/ai/request-builder.ts`가 모두 `maskPii`를 부른다
    - `ai-text.ts`에 값·선택 항목·안 글자를 읽는 코드가 없고, 민감칸 판별 함수는 `src/core/sensitive.ts` 한 곳뿐이다(verify의 grep)
    - `tests/unit/ai-text.test.ts`가 `isSensitive`로 비밀번호·주민등록번호 고정물이 민감칸임을 먼저 확인한 뒤, `aiTextOf` 결과가 `'[입력칸]'`이고 라벨 글자가 없음을 단언한다
    - `tests/unit/ai-request-builder.test.ts`가 가림 뒤 자르기와 덧붙은 식별 묶음 값 부재를 단언한다
    - 두 단위 시험 파일이 모두 통과한다
  </acceptance_criteria>
  <done>요소 하나의 AI용 글자가 값 없이 가려져 만들어지고, 민감칸은 Phase 3 판별 함수로 골라 종류와 고정 표시만 남으며, SW가 보내기 직전 다시 가린다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 가로챈 요청 본문으로 확인하는 개인정보 e2e (iframe 포함)</name>
  <files>tests/e2e/ai-privacy.e2e.ts, tests/practice-site/ai-privacy.html, tests/practice-site/ai-privacy-frame.html</files>
  <read_first>
    - tests/e2e/fake-anthropic.ts, tests/e2e/ai-cleanup.e2e.ts (06-01·06-04 결과, 가로채기 방법)
    - tests/unit/ai-text.test.ts (Task 2의 민감칸 고정물 속성)
    - tests/e2e/fixtures.ts (servePage, blockedRequests, expectNoExternalRequests)
    - .planning/phases/06-ai/06-CONTEXT.md (D-09, D-10, D-11, D-13)
  </read_first>
  <behavior>
    - 개인정보 연습 페이지에서 가짜 키로 화면 정리를 누르면 가짜 AI가 요청 1개를 받는다
    - 그 요청 본문 전체(JSON 문자열)에 입력칸 값 `hong@example.com`·`010-1234-5678`, 비밀번호 값 `pw-secret-123`, 주민등록번호 값 `900101-1234567`, textarea 글자 `메모 비밀 내용 abc`, contenteditable 글자 `편집 가능한 비밀 글`, 선택된 option 글자 `선택된 비밀 옵션`, iframe 안 입력칸 값 `frame-secret-value`가 하나도 없다
    - 본문에 페이지 제목 `TITLE-XYZ`, 쿼리 `QUERY-SECRET`, 호스트 `practice.test`·`other.test`, 요소 id `id-SECRET-`가 없다
    - 버튼 글자의 개인정보는 가려져 본문에 `[이메일]`, `[전화번호]`, `[숫자]`가 모두 있다
    - 민감칸 두 개(비밀번호·주민등록번호)는 본문에 고정 표시 `[입력칸]`으로만 나타나고(두 번 이상), 그 라벨 글자 `비밀번호`·`주민등록번호`가 본문에 없다(D-11)
    - `blockedRequests`가 비어 있다
  </behavior>
  <action>
연습 페이지를 먼저 만든다: `ai-privacy.html`(제목 `비밀 제목 TITLE-XYZ`) — behavior에 적힌 값이 든 입력칸들(각각 라벨 있음: 이메일, 연락처, 비밀번호(type=password), 주민등록번호), textarea, contenteditable div, select, 개인정보가 든 버튼 글자 셋(`홍길동 hong@example.com 에게 보내기`, `담당 010-9876-5432`, `계좌 123-456789-01-234 확인`), 모든 요소 id는 `id-SECRET-` 접두어, `<iframe src="http://other.test/ai-privacy-frame.html">`. 비밀번호·주민등록번호 칸은 Task 2 단위 시험 고정물과 같은 속성으로 만든다(Phase 3 `isSensitive`가 민감으로 판정하는 모양). 페이지의 다른 글자(버튼·제목·다른 라벨)에는 `비밀번호`·`주민등록번호`라는 낱말을 쓰지 않는다(라벨 부재 단언이 그 칸만 가리키게). `ai-privacy-frame.html` — 값 `frame-secret-value`가 든 입력칸(라벨 "프레임 입력")과 버튼 하나. 페이지는 `http://practice.test/ai-privacy.html?token=QUERY-SECRET`로 연다. 모든 이름·번호는 지어낸 값이다. 입력칸 값은 HTML 속성이 아니라 시험이 `page.fill`로 넣는다(수집 뒤 바뀐 값도 새지 않는지 보려고 화면 정리 직전에 넣는다).

`tests/e2e/ai-privacy.e2e.ts`: `setAiConfig`로 `FAKE_AI_KEY`를 넣고 가짜 AI를 `picks` 각본(임시 번호 `t0` 하나)으로 둔 뒤 `pressCleanupCard` → 요청 1개를 받을 때까지 기다리고 behavior를 단언한다. 이 시험은 처음부터 초록일 수 있다(06-03·Task 1·2가 이미 막음) — 그러면 RED 커밋 대신, 가림을 잠시 끄면, 그리고 민감칸 처리를 잠시 끄면 이 시험이 각각 실패하는지 로컬에서 한 번씩 확인하고 그 사실을 SUMMARY에 적는다(시험이 실제로 무언가를 잡는다는 증거). 끈 코드는 커밋하지 않는다.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-privacy.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report "1 passed", or the output contains "No tests found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/ai-privacy.e2e.ts`가 behavior의 값 여덟 개, 제목·쿼리·호스트·id 접두어가 본문에 없음과 세 가림 표시가 있음을 단언한다
    - iframe 안 입력칸 값이 단언 목록에 있다
    - `[입력칸]`이 두 번 이상 있고 `비밀번호`·`주민등록번호`가 본문에 없음을 단언한다
    - `CI=true pnpm exec playwright test tests/e2e/ai-privacy.e2e.ts`가 1 passed이고 `blockedRequests`가 비어 있다
    - 가림을 끄면, 민감칸 처리를 끄면 각각 시험이 실패함을 확인한 결과가 06-05-SUMMARY에 있다
  </acceptance_criteria>
  <done>실제로 나간 요청 본문에서 입력 값·민감칸 값과 라벨·주소·제목·속성 값이 없고 개인정보 모양이 가려진 것이 CI=true e2e로 확인됐다(ROADMAP 성공 기준 2).</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- AI-02 (주 계획): edge probe `unclassified — review manually`. 가정: (1) "대략의 위치"는 화면 9구역 영어 이름으로 보낸다(좌표·크기는 보내지 않음). (2) 가릴 모양은 Task 1의 규칙으로 정하고, 주민번호·계좌·카드 모양은 9자리 이상 숫자 규칙이 덮는다. (3) 민감칸(D-11)은 제품 경로에서 Phase 3 `isSensitive`로 판정하고, 민감칸은 종류와 고정 표시 `[입력칸]`만 보낸다(라벨 글자도 값도 보내지 않음). 두 번째 판별 함수는 만들지 않는다. Phase 3 함수의 이름·입력 모양이 다르면 Task 2의 대안대로 맞추고, 함수가 없으면 멈춘다. (4) 민감하지 않은 칸의 라벨 글자는 값이 아니므로 가려서 보낸다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 페이지 DOM → content script | 사이트가 요소 글자·라벨에 개인정보를 둘 수 있다 |
| service worker → api.anthropic.com | 요소 목록이 확장 밖으로 나가는 유일한 길(D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Information Disclosure | `aiTextOf` 입력칸 처리 | high | mitigate | 글자 칸은 라벨·aria·`placeholder` 속성만, 값·선택 항목·안 글자를 읽는 코드가 없음을 grep으로 확인, e2e가 값 여덟 개 부재를 확인 |
| T-06-37 | Information Disclosure | 민감칸(비밀번호·주민등록번호 칸)의 라벨·값 | high | mitigate | `aiTextOf`가 Phase 3 `isSensitive` 한 곳으로 판정해 민감칸은 고정 표시 `[입력칸]`만 돌려준다(D-11, Phase 3 D-26). 단위 시험이 고정물 판정과 결과를, e2e가 본문의 `[입력칸]`과 라벨·값 부재를 확인. 두 번째 판별 함수 없음(grep) |
| T-06-07 | Information Disclosure | 요소 글자 속 이메일·전화·긴 숫자 | high | mitigate | `maskPii`를 수집 때와 SW 요청 만들기 때 두 번, 규칙을 단위 시험 20개 이상으로 고정 |
| T-06-08 | Information Disclosure | 페이지 주소·제목·속성 값 | medium | mitigate | 보낼 목록 항목을 `id`·`kind`·`text`·`zone` 네 키로 제한(단위 시험), e2e가 제목·쿼리·호스트·id 부재 확인 |
| T-06-09 | Information Disclosure | 가림 규칙이 못 잡는 개인정보 모양(이름, 주소 글자 등) | medium | accept | 설계 6.10이 가릴 모양을 이메일·전화·긴 숫자로 정했다. 사람 이름·주소 글자는 버튼 글자로 나갈 수 있음을 SUMMARY와 SYSTEM.md 안내(06-09)에 남긴다 |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/ai-mask.test.ts tests/unit/ai-text.test.ts tests/unit/ai-request-builder.test.ts` 통과
- `CI=true pnpm exec playwright test tests/e2e/ai-privacy.e2e.ts` 1 passed
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 가림 규칙·AI용 글자(민감칸 고정 표시 포함)·보낼 목록의 가림이 단위 시험으로 고정됐다
- 가로챈 실제 요청 본문에 값·민감칸 값과 라벨·주소·제목·속성 값이 없고 가림 표시와 민감칸 고정 표시가 있다(D-11)
- 작업마다 RED(`test(06-05)`)·GREEN(`feat(06-05)`) 커밋이 있다(Task 3은 처음부터 초록이면 그 사실과 가림 끄기 확인을 SUMMARY에)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/ai/mask.ts`, `tests/unit/ai-mask.test.ts`, `tests/unit/ai-text.test.ts`, `tests/unit/ai-request-builder.test.ts`, `tests/e2e/ai-privacy.e2e.ts`, `tests/practice-site/ai-privacy.html`, `tests/practice-site/ai-privacy-frame.html`
- 심볼: `maskPii`, `SENSITIVE_FIELD_TEXT`
- 바뀐 동작: `aiTextOf`와 `buildAiPayload`가 가림을 적용하고, `aiTextOf`가 Phase 3 `isSensitive`로 민감칸을 골라 고정 표시만 돌려준다

<output>
Create `.planning/phases/06-ai/06-05-SUMMARY.md` when done (가림 규칙 표, D-11 적용 — Phase 3 판별 함수의 실제 이름·입력 모양과 고정 표시, 가림·민감칸 처리 끄기 확인 결과 포함)
</output>
