---
phase: 06-ai
plan: 06
type: execute
wave: 4
depends_on: ["06-04", "06-05"]
files_modified:
  - tests/e2e/ai-dom-audit.e2e.ts
  - src/entrypoints/options/ai-section.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05, AI-06]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "화면 정리의 불러오는 중·성공·실패 표시와 설정 화면 AI 부분이 SYSTEM.md와 tokens.css를 따르는 것을, 실행자가 아닌 별도 감사 에이전트가 CI=true로 DOM을 실측해 판정했다(D-30, D-22, D-23)"
    - "CI=true 전체 게이트(단위 → e2e 전부)가 한 번에 통과한다(D-27, D-28, D-30)"
    - "확장 전체에서 네트워크 호출 코드는 `src/worker/ai/client.ts`에만 있다 — 밖으로 나가는 요청은 화면 정리뿐이다(D-12)"
    - "`COVERAGE.md`의 API 범위 표가 seal 게이트(`api-coverage.verify-pre`)를 통과한다"
  prohibitions:
    - statement: "Phase 6의 시험·확인은 로컬 연습 사이트에서만 한다 — 실제 회사 시스템에 접속하거나 그 위에서 화면 정리를 시험하지 않는다(D-29, 설계 10장·11장 ①, 2026-09-23 사용자 결정)"
      requirement_id: AI-01
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "tests/e2e/ai-dom-audit.e2e.ts"
      provides: "감사 에이전트가 쓴 화면 정리 표시·설정 화면 AI 부분 DOM 실측 감사"
  key_links:
    - from: "tests/e2e/ai-dom-audit.e2e.ts"
      to: "docs/design/SYSTEM.md"
      via: "상태 표 문구·크기·토큰 값 집합을 기준으로 계산된 스타일을 잰다"
      pattern: "SYSTEM.md"
    - from: ".planning/phases/06-ai/COVERAGE.md"
      to: "src/worker/ai/client.ts"
      via: "INTEGRATE 행(구조화 출력·오류·재시도·시간 초과·취소·stop_reason)이 client.ts에 있음"
      pattern: "output_config"
---

## Phase Goal

**As a** 이용자, **I want to** 처음 보는 복잡한 화면에서 명령판의 "화면 정리"를 누르면 중요한 요소 9개에 번호가 붙기를, **so that** AI가 없거나 실패해도 나머지 기능은 그대로이고, 개인정보와 입력 값은 밖으로 나가지 않는다.

(ROADMAP Phase 6 **Goal** 문장을 그대로 세 칸에 옮긴 것이다. 새 내용은 더하지 않았다. `/gsd-mvp-phase 6`으로 다듬을 수 있다.)

<objective>
저장소 규칙의 화면 검증 순서(싼 게이트 → 별도 에이전트의 독립 DOM 감사 → 수정 → 전체 게이트 한 번)로 Phase 6을 닫는다(D-30, CLAUDE.md). API 범위 표와 "밖으로 나가는 요청은 화면 정리뿐"을 기계로 확인하고, 이용자 본인 키로 하는 연습 사이트 확인을 phase 끝 사람 확인으로 남긴다.

Purpose: D-12·D-22·D-23·D-27~D-30, AI-01~AI-06 전체의 마지막 확인.
Output: `tests/e2e/ai-dom-audit.e2e.ts`(감사 에이전트 작성), 감사 어긋남 수정, 전체 게이트 결과.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai/06-CONTEXT.md
@.planning/phases/06-ai/COVERAGE.md
@.planning/phases/06-ai/06-04-SUMMARY.md
@.planning/phases/06-ai/06-05-SUMMARY.md
</context>

## 앞선 단계 의존

- 06-01~06-05 완료. 특히 06-05가 SYSTEM.md에 적은 상태 문구·설정 화면 AI 템플릿이 감사 기준이다.
- Phase 1 계획 16의 감사 방식(`tests/e2e/dom-audit.e2e.ts` — 색·radius 토큰 집합, `box-shadow: none`, 서체, 번호표 ≥28×28, 대비)과 Phase 3 옵션 페이지 감사 방식이 있으면 같은 판정 방법을 쓴다.

## 실행 규칙 (모든 작업 공통)

- 패키지 매니저는 pnpm만. TypeScript strict, `any` 금지, 시크릿을 코드·커밋에 넣지 않는다(CLAUDE.md). 새 패키지 없음.
- "완료"라고 말하기 전에 Superpowers `verification-before-completion`을, 실패를 쫓기 전에 `systematic-debugging`을 **호출**한다.
- 감사는 이 계획 실행자가 아니라 **별도 에이전트**가 한다. 스크린샷 육안 판정은 하지 않는다(CLAUDE.md 프론트엔드). 실행자는 감사 시험의 기준을 바꾸지 않는다.
- 전체 게이트(`CI=true pnpm test`)는 수정이 끝난 뒤 **한 번** 돌린다.
- 자동 시험은 진짜 Anthropic API를 부르지 않는다. 진짜 키는 phase 끝 사람 확인에서 이용자가 설정 화면에 직접 넣을 뿐, 파일·명령·커밋·대화 기록에 남기지 않는다(D-28).
- 실제 회사 시스템에 접속하거나 시험하지 않는다(D-29).

<tasks>

<task type="auto">
  <name>Task 1: 싼 게이트 → 독립 DOM 감사(별도 에이전트, Opus 5) → 수정</name>
  <files>tests/e2e/ai-dom-audit.e2e.ts, src/entrypoints/options/ai-section.ts</files>
  <read_first>
    - CLAUDE.md ("프론트엔드 — 화면 검증 순서", "캐시·컨텍스트 규칙 — 모델 선택")
    - docs/design/SYSTEM.md (상태 표 "화면 정리(AI)" 행, 설정 화면 AI 부분, 크기·카피 규칙), docs/design/tokens.css, docs/DESIGN.md (§6 품질 바닥)
    - tests/e2e/dom-audit.e2e.ts (Phase 1 감사 판정 방식)
    - tests/e2e/fake-anthropic.ts, tests/e2e/fixtures.ts
  </read_first>
  <action>
1. 싼 게이트: `pnpm lint && pnpm typecheck && CI=true pnpm build`. 실패하면 고치고 다시.

2. 독립 DOM 감사: **이 계획 실행자가 아닌 별도 감사 에이전트**(Task 도구, `model: "opus"` — 점검·판단은 Opus 5, CLAUDE.md. 프롬프트에 `verification-before-completion` 스킬 명시)에게 맡긴다. 감사 에이전트는 SYSTEM.md·tokens.css·DESIGN.md §6만 기준으로 `tests/e2e/ai-dom-audit.e2e.ts`를 쓰고 `CI=true pnpm exec playwright test tests/e2e/ai-dom-audit.e2e.ts`로 DOM을 실측해 판정한다. 가짜 AI(`installFakeAnthropic`, `FAKE_AI_KEY`)만 쓴다. 감사 항목(최소):
   - 연습 페이지 `ai-cleanup.html`에서 — 불러오는 중: 2초 지연 각본일 때 모드 표시 글자가 SYSTEM.md 상태 표의 불러오는 중 문구와 같고 300ms 전에는 없다. 성공: 번호표 9개, 각 28×28 이상, AI 번호표의 계산된 스타일(글자 크기·색·배경·테두리·radius·서체·`box-shadow`)이 F 번호표와 같다, 모드 표시 "AI가 고른 번호"가 18px. 실패: 키 틀림(401)·연결 못 함(네트워크 오류) 문구가 SYSTEM.md와 글자 그대로이고 약 4초 뒤 사라진다.
   - 모든 화면 정리 표시 부품: 색은 tokens.css 값 집합 안(`color`·`background-color`·`border-*-color`·`outline-color`), radius는 radius 토큰 집합 안, `box-shadow`는 `none`, 서체가 "IBM Plex Sans KR"로 시작, 한글 `word-break: keep-all`.
   - 설정 화면 AI 부분: 글자 15px 이상, 버튼·입력칸 높이 56px 이상, 키 칸 `type=password`, 저장 뒤 DOM 어디에도 키 전체가 없음, 색·radius 토큰 집합 안, `box-shadow: none`, Tab 초점에서 `:focus-visible` 남색 3px, 360px·1440px 폭에서 가로 넘침 없음, 글자 대비 4.5:1 이상·UI 3:1 이상.
   - 모든 감사 시험에서 `blockedRequests`가 비어 있다.
   감사 에이전트는 어긋남 목록(파일·부품·실측값·기준값)을 돌려준다.

3. 수정: 어긋남마다 해당 파일을 고친다(대개 `src/entrypoints/options/ai-section.ts`. 목록 밖 파일을 고치면 SUMMARY에 편차로 적는다). SYSTEM.md를 벗어나야 하는 경우는 고치지 말고 멈춰서 DECISIONS.md 기록·SYSTEM.md 수정 승인을 요청한다(DESIGN.md §4-6). 감사 시험을 다시 돌려 통과를 확인한다.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && CI=true pnpm build</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-dom-audit.e2e.ts</automated>
    <fails_when>non-zero exit, or the output contains "No tests found", or fewer than 10 tests passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/ai-dom-audit.e2e.ts`가 있고, SUMMARY에 "감사 에이전트(model: opus)가 작성·판정"과 어긋남 목록·수정 결과가 적혀 있다
    - `CI=true pnpm exec playwright test tests/e2e/ai-dom-audit.e2e.ts`가 10개 이상 통과
    - 감사 시험 파일의 판정 기준을 실행자가 바꾼 커밋이 없다(감사 에이전트 커밋 뒤 이 파일을 고친 커밋이 없음)
  </acceptance_criteria>
  <done>화면 정리 표시와 설정 화면 AI 부분이 SYSTEM.md와 일관됨을 별도 에이전트가 DOM 실측으로 통과시켰다.</done>
</task>

<task type="auto">
  <name>Task 2: 전체 게이트 한 번 + 밖으로 나가는 요청 확인 + API 범위 표 게이트</name>
  <files>없음 — 게이트만 돌린다. 게이트 실패를 고치면 고친 파일을 SUMMARY에 편차로 적는다</files>
  <read_first>
    - .planning/phases/06-ai/COVERAGE.md
    - src/worker/ai/client.ts
    - CLAUDE.md ("코딩 규칙", Post-build 절차)
  </read_first>
  <action>
1. 전체 게이트 한 번: `CI=true pnpm test`(단위 → e2e 전부). 실패하면 `systematic-debugging`으로 원인을 찾아 고치고, 고친 뒤 이 게이트를 다시 한 번 돌린다(고친 내용은 SUMMARY에).

2. 밖으로 나가는 요청(D-12): `src`에서 네트워크 API(`fetch` 호출, `XMLHttpRequest`, `WebSocket`, `sendBeacon`, `EventSource`)가 `src/worker/ai/client.ts` 밖에 없음을 확인한다. SDK를 승인했다면 client.ts에도 직접 호출이 없을 수 있다 — 그것도 통과다. Phase 1의 같은 확인(01-16 "src에 네트워크 API 없음")은 이 phase부터 이 규칙으로 바뀐다고 SUMMARY에 적는다.

3. API 범위 표: `node .claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/06-ai`가 통과해야 한다. `COVERAGE.md`의 INTEGRATE 행(messages.create, system, 구조화 출력, 키 인증, 오류·재시도, 시간 초과, 취소, stop_reason, `dangerouslyAllowBrowser`)이 client.ts·response.ts에 실제로 있는지 읽어서 확인하고 결과를 SUMMARY에 적는다. 승인 거절(fetch)이었다면 `dangerouslyAllowBrowser` 행은 같은 능력인 직접 부름 헤더(`anthropic-dangerous-direct-browser-access`)로 본다. `COVERAGE.md`는 고치지 않는다 — 표와 코드가 어긋나면 멈추고 보고한다.

4. 이 작업은 파일을 새로 만들지 않는다.

다음 단계는 CLAUDE.md Post-build다: `/review` → `/qa` → `/cso`(이 phase는 외부 전송·키·외부 입력을 다루므로 필수) → `/ship`. 즉석 검증으로 대신하지 않는다.
  </action>
  <verify>
    <automated>CI=true pnpm test</automated>
    <fails_when>non-zero exit, or any test reported failed or flaky</fails_when>
    <automated>test -z "$(grep -rnE '\bfetch\(|XMLHttpRequest|WebSocket|sendBeacon|EventSource' src/ | grep -v '^src/worker/ai/client.ts:')"</automated>
    <fails_when>non-zero exit — a network API call exists in src/ outside src/worker/ai/client.ts</fails_when>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/06-ai</automated>
    <fails_when>non-zero exit, or the JSON output contains "passed": false or "block": true</fails_when>
    <human-check>phase 끝 사람 확인(선택, 이용자 본인만): 이용자가 `pnpm build`한 확장을 크롬에 올리고 설정 화면 "화면 정리(AI)"에 **자기** Anthropic 키를 직접 넣은 뒤, 크롬 확장 세부 정보에서 "파일 URL에 대한 액세스 허용"을 켜고 `tests/practice-site/ai-cleanup.html`을 열어 명령판 0 → 9 → 7을 누른다. 진짜 모델이 번호 1~9를 붙이고 "AI가 고른 번호"가 보이는지, 설정 화면의 이번 달 요청 수가 1 늘었는지 본다. 끝나면 [지우기]로 키를 지워도 된다. 회사 시스템에서는 하지 않는다(D-29). 키는 대화·파일·명령에 적지 않는다.</human-check>
  </verify>
  <acceptance_criteria>
    - `CI=true pnpm test`가 한 번에 전부 통과(수정 뒤 다시 돌렸다면 그 사실과 이유가 SUMMARY에)
    - 네트워크 API가 `src/worker/ai/client.ts` 밖에 없다
    - `api-coverage.verify-pre`가 `"passed": true`
    - SUMMARY에 다음 단계(`/review` → `/qa` → `/cso` → `/ship`)가 적혀 있다
  </acceptance_criteria>
  <done>전체 게이트가 CI=true로 한 번에 통과했고, 밖으로 나가는 요청은 화면 정리 한 곳뿐이며, API 범위 표가 seal 게이트를 통과했다. 다음은 Post-build 네 단계다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

이 계획은 요구사항 여섯 개 전체를 마지막으로 확인한다. 요구사항별 경계 가정은 각 주 계획에 있다: AI-01 → 06-01, AI-02 → 06-02, AI-03·AI-04 → 06-03, AI-05 → 06-05, AI-06 → 06-04. 이 계획이 더하는 가정: 진짜 모델로 하는 확인은 자동 시험이 아니라 이용자 본인의 선택적 사람 확인이다(D-28 "자동 시험은 진짜 AI를 부르지 않는다").

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장소·CI → 시험 실행 | 시험이 쓰는 키와 요청 대상 |
| 이용자 → 설정 화면(사람 확인) | 진짜 키가 이 PC의 확장에 들어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-25 | Information Disclosure | 사람 확인의 진짜 키 | high | mitigate | 이용자가 설정 화면에 직접 넣고, 실행자는 키를 묻거나 파일·명령·커밋에 적지 않는다. 확인 뒤 [지우기] 안내 |
| T-06-26 | Information Disclosure | 확장의 외부 전송 범위 | high | mitigate | 네트워크 API가 client.ts 밖에 없음을 grep으로, 모든 e2e의 `blockedRequests` 비어 있음으로 확인(D-12) |
| T-06-27 | Repudiation | 감사 판정 | low | mitigate | 감사는 별도 에이전트가 쓰고, 실행자가 기준을 바꾼 커밋이 없음을 acceptance로 확인 |
</threat_model>

<verification>
- `pnpm lint && pnpm typecheck && CI=true pnpm build` 통과
- `CI=true pnpm exec playwright test tests/e2e/ai-dom-audit.e2e.ts` 10개 이상 통과
- `CI=true pnpm test` 한 번에 통과
- 네트워크 API가 client.ts 밖에 없음, `api-coverage.verify-pre` 통과
</verification>

<success_criteria>
- 화면 검증 순서(싼 게이트 → 독립 DOM 감사 → 수정 → 전체 게이트 한 번)를 지켰다
- 전체 게이트·외부 전송 확인·API 범위 표 게이트가 통과했다
- 다음 단계로 `/review` → `/qa` → `/cso` → `/ship`이 SUMMARY에 적혀 있다
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/e2e/ai-dom-audit.e2e.ts`
- 결과: 감사 어긋남 목록과 수정, 전체 게이트 결과, 외부 전송 확인 규칙 갱신(네트워크 API는 `src/worker/ai/client.ts`만)

<output>
Create `.planning/phases/06-ai/06-06-SUMMARY.md` when done (감사 에이전트 모델, 어긋남·수정 목록, 전체 게이트 결과, Post-build 다음 단계 포함)
</output>
