---
phase: 06-ai
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/worker/ai/client.ts
  - src/worker/ai/gateway.ts
  - src/shared/messages.ts
  - src/entrypoints/content.ts
  - tests/e2e/ai-cleanup.e2e.ts
  - tests/practice-site/ai-cleanup.html
  - tests/practice-site/ai-cleanup-frame.html
autonomous: true
requirements: [AI-01, AI-06]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`@anthropic-ai/sdk`는 06-01 Task 1에서 이용자가 승인했을 때만 승인한 정확한 버전(`^` 없음)으로 설치되어 있다. 거절했다면 설치되어 있지 않고 `src/worker/ai/client.ts`만 fetch로 부른다(D-25)"
    - "연습 사이트에서 AI 키가 있을 때 이용자가 명령판 둘째 장 7번 \"화면 정리(AI)\"를 누르면, 가짜 AI가 고른 요소(iframe 안 요소 포함)에 고른 순서대로 1~9 번호표가 붙고 \"AI가 고른 번호\"가 보이며, 번호 키로 누르면 그 요소가 눌린다(AI-01, D-01, D-05, D-06)"
    - "AI 요청은 service worker의 한 곳(`gateway.ts` → `client.ts`)에서만 나가고, SDK와 API 주소를 아는 파일은 `client.ts` 하나이며, 요청은 model `claude-haiku-4-5`·max_tokens 256·구조화 출력 `json_schema`다(D-04, D-07, D-12)"
    - "AI 답이 검사를 통과하지 못하면 AI 번호 대신 자주 누른 순서 번호표와 \"AI 답을 쓸 수 없었어요. 자주 누른 순서로 번호를 붙였어요\"가 보인다(AI-06, D-19, D-20)"
    - "SDK가 MV3 service worker 프로덕션 빌드에서 동작하는지(스파이크 A)의 결과가 06-04-SUMMARY에 있다(RESEARCH Open Question 1)"
  prohibitions:
    - statement: "AI 요청 본문에 요소 열쇠·요소 식별 묶음·좌표·페이지 주소·제목을 싣지 않는다 — 창구는 메시지 항목을 06-02의 `AiCandidate`로 옮겨 `buildAiPayload`에만 넘기고, 열쇠는 SW 안의 `idToKey`에만 남는다(D-09, D-12)"
      requirement_id: AI-01
      category: security
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/worker/ai/client.ts"
      provides: "Anthropic Messages API를 부르는 유일한 파일(SDK 또는 승인 거절 시 fetch)"
      exports: ["requestCleanupPicks", "ClientOutcome"]
    - path: "src/worker/ai/gateway.ts"
      provides: "키 확인 → 보낼 목록 → client 호출 → 답 검사 → 결과(번호 또는 대체 사유)"
      contains: "requestCleanupPicks"
    - path: "src/shared/messages.ts"
      provides: "요청 항목의 종류·글자, 결과의 AI 번호 변형"
      contains: "itemKeys"
    - path: "tests/practice-site/ai-cleanup.html"
      provides: "화면 정리 연습 페이지(버튼 약 14개, 위험 버튼, 이벤트 기록, other.test iframe) — 06-06·06-08·06-10이 다시 쓴다"
    - path: "tests/e2e/ai-cleanup.e2e.ts"
      provides: "AI 성공 경로(맨 위·iframe)와 쓸 수 없는 답 대체 e2e"
  key_links:
    - from: "src/worker/ai/gateway.ts"
      to: "src/worker/ai/client.ts"
      via: "requestCleanupPicks({ apiKey, system, userContent, signal })"
      pattern: "requestCleanupPicks"
    - from: "src/worker/ai/gateway.ts"
      to: "src/core/ai/response.ts"
      via: "judgeAiAnswer(outcome, sentIds)가 null이면 invalid-response 대체"
      pattern: "judgeAiAnswer"
    - from: "src/worker/ai/gateway.ts"
      to: "src/core/ai/request-builder.ts"
      via: "메시지 항목 → AiCandidate → buildAiPayload → buildUserContent, idToKey로 답을 요소 열쇠로"
      pattern: "buildAiPayload"
    - from: "src/entrypoints/content.ts"
      to: "src/core/hint-order.ts"
      via: "openHints({ aiPickIds, composed }) → orderHints({ ..., aiPickIds })"
      pattern: "aiPickIds"
    - from: "src/entrypoints/content.ts"
      to: "src/core/frame-tree.ts"
      via: "ComposedItem의 kind·aiText를 요청 항목의 kind·text로"
      pattern: "aiText"
---

## Phase Goal

**As a** 이용자, **I want to** 처음 보는 복잡한 화면에서 명령판의 "화면 정리"를 누르면 중요한 요소 9개에 번호가 붙기를, **so that** AI가 없거나 실패해도 나머지 기능은 그대로이고, 개인정보와 입력 값은 밖으로 나가지 않는다.

(ROADMAP Phase 6 **Goal** 문장을 그대로 세 칸에 옮긴 것이다. 새 내용은 더하지 않았다. `/gsd-mvp-phase 6`으로 다듬을 수 있다.)

<objective>
06-01이 연 키 없음 경로에 "키가 있으면 AI가 고른 번호" 경로를 잇는다: 승인된 SDK 설치 → 스파이크 A(SDK가 MV3 SW 프로덕션 빌드에서 동작하는가) → AI 창구가 06-02의 보낼 목록·답 검사로 가짜 AI를 부름 → 06-02의 번호 순서로 1~9 번호표 + "AI가 고른 번호". 이어서 iframe 안 요소도 같은 경로로 고르고 누를 수 있음을 확인한다.

Purpose: D-01·D-04·D-05·D-06·D-07·D-08·D-12·D-19·D-20·D-25·D-28, AI-01(주 계획), AI-06의 "쓸 수 없는 답 → 대체".
Output: 승인된 의존성, `src/worker/ai/client.ts`, 창구의 AI 호출 단계, 메시지의 종류·글자와 AI 결과, content의 AI 번호 표시, 연습 페이지 2개, e2e 3개 더.

재분할 메모(계획 점검 반영): 원래 06-01 tracer의 "AI 호출·번호표" 부분이다. 작업 하나가 파일 10개 이상을 만지지 않게 맨 위 프레임(Task 1)과 iframe(Task 2)으로 나눴다.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai/06-CONTEXT.md
@.planning/phases/06-ai/06-RESEARCH.md
@.planning/phases/06-ai/06-PATTERNS.md
@.planning/phases/06-ai/COVERAGE.md
@.planning/phases/06-ai/06-01-SUMMARY.md
@.planning/phases/06-ai/06-02-SUMMARY.md
@.planning/phases/06-ai/06-03-SUMMARY.md
</context>

## 앞선 단계 의존

- 06-01: Task 1의 SDK 설치 선택(06-01-SUMMARY), `gateway.ts`(`createAiGateway`, 키 없음 대체와 키가 있을 때의 `call-failed` 분기), `messages.ts`의 `ai/cleanup-request`·`CleanupResultSchema`, `ai-storage.ts`, `cleanup-copy.ts`(`AI_SUCCESS_HINT`, `fallbackMessage`), content의 `runCleanup`·`collectComposed`·`openHints({ composed })`, `fake-anthropic.ts`와 06-01-SUMMARY의 가로채기 방법.
- 06-02: `response.ts`(`CLEANUP_OUTPUT_SCHEMA`, `judgeAiAnswer`), `request-builder.ts`(`AI_MODEL`, `AI_MAX_TOKENS`, `CLEANUP_SYSTEM_PROMPT`, `AiCandidate`, `buildAiPayload`, `buildUserContent`), `orderHints`의 `aiPickIds`.
- 06-03: `ComposedItem`의 `kind?`·`aiText?`(iframe 보고 포함).

## 실행 규칙 (모든 작업 공통)

- 패키지 매니저는 pnpm만. TypeScript strict, `any` 금지, 시크릿을 코드·커밋에 넣지 않는다(CLAUDE.md).
- 06-01 Task 1에서 승인받은 패키지·버전 말고는 설치하지 않는다. 새 권한은 없다(D-26). 필요해지면 멈추고 이유 한 줄과 함께 승인을 요청한다.
- 구현 전에 Superpowers `test-driven-development`를, 시험 실패를 쫓기 전에 `systematic-debugging`을, "완료" 전에 `verification-before-completion`을 **호출**한다. 서브에이전트에 맡기면 프롬프트에 세 스킬과 `model`을 적는다(점검·판단은 Opus 5).
- RED 커밋 `test(06-04): …` → GREEN 커밋 `feat(06-04): …`, 설치는 `chore(06-04): …`. 제목은 영어 접두어, 본문은 한국어.
- 완료 판정은 `CI=true`로 돌린 e2e(프로덕션 빌드)다. 로컬 dev 통과는 완료 신호가 아니다.
- **자동 시험은 진짜 Anthropic API를 부르지 않는다.** 시험 키는 `FAKE_AI_KEY` 하나뿐이다(D-28).
- 실제 회사 시스템에 접속하거나 시험하지 않는다(D-29).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 승인된 SDK 설치 + 스파이크 A + "키가 있으면 AI가 고른 번호" (맨 위 프레임)</name>
  <precondition>06-01 Task 1의 선택이 기록되어 있다 — `grep -qE 'approve-sdk|reject-fetch' .planning/phases/06-ai/06-01-SUMMARY.md`가 0으로 끝나지 않으면(선택이 없거나 revise 뒤 결론이 없으면) 멈추고 이용자에게 선택을 묻는다.</precondition>
  <reversibility rating="costly">SDK를 런타임 의존성으로 들이는 결정은 번들과 공급망에 남는다. 되돌리려면 `client.ts`를 fetch로 다시 짜고 의존성을 뺀다(다른 파일은 SDK를 모른다).</reversibility>
  <files>package.json, pnpm-lock.yaml, src/worker/ai/client.ts, src/worker/ai/gateway.ts, src/shared/messages.ts, src/entrypoints/content.ts, tests/e2e/ai-cleanup.e2e.ts, tests/practice-site/ai-cleanup.html</files>
  <read_first>
    - .planning/phases/06-ai/06-01-SUMMARY.md (Task 1 선택·버전·모델 ID, 가로채기 방법)
    - .planning/phases/06-ai/06-CONTEXT.md (D-04~D-08, D-12, D-19, D-20, D-25)
    - .planning/phases/06-ai/06-RESEARCH.md (Pitfall 1·3·6, "Code Examples" 구조화 출력 요청, Open Question 1)
    - .planning/phases/06-ai/06-PATTERNS.md (client.ts, gateway.ts 절)
    - src/worker/ai/gateway.ts, src/shared/messages.ts, src/entrypoints/content.ts (06-01 결과), src/core/ai/request-builder.ts, src/core/ai/response.ts, src/core/hint-order.ts (06-02 결과)
    - tests/e2e/fake-anthropic.ts, tests/e2e/ai-cleanup.e2e.ts (06-01 결과), tests/e2e/hints.e2e.ts (labelTexts, numberForElement)
    - 승인 시 node_modules/@anthropic-ai/sdk 의 README(클라이언트 옵션·요청 옵션·오류 클래스), claude-api 스킬 TypeScript 문서(구조화 출력 `output_config.format`)
  </read_first>
  <behavior>
    - e2e 성공(맨 위): `setAiConfig`로 가짜 키를 넣고 가짜 AI 각본을 `pick-texts`(맨 위 프레임 버튼 글자 9개를 정한 순서로)로 두면, `pressCleanupCard` 뒤 번호표가 정확히 9개이고, 고른 순서의 k번째 요소 번호가 k이며, `Digit3`을 누르면 셋째로 고른 요소의 `data-count`가 1이 되고, 모드 표시에 "AI가 고른 번호"가 보인다
    - e2e 성공(요청 모양): 가짜 AI가 받은 요청이 정확히 1개이고 model `claude-haiku-4-5`, max_tokens 256, `output_config.format.type` `json_schema`, 헤더 `x-api-key`가 `FAKE_AI_KEY`이며, 본문 elements의 글자에 연습 페이지 버튼 글자("상신", "목록" 등)가 있고, `blockedRequests`가 비어 있다
    - e2e 쓸 수 없는 답: 번호표로 "목록"을 두 번 눌러 기록을 만든 뒤 각본을 `{ kind: 'text', text: 'not json' }`으로 두고 카드를 누르면, "목록"의 번호가 1이고 "AI 답을 쓸 수 없었어요. 자주 누른 순서로 번호를 붙였어요"가 보인다
    - 06-01의 두 시험(가로채기 확인, 키 없음 요청 0개)이 그대로 통과한다
  </behavior>
  <action>
**설치(06-01 Task 1 선택대로, D-25):** approve-sdk면 승인한 정확한 버전만 `pnpm add --save-exact @anthropic-ai/sdk@<승인한 버전>`으로 설치하고, 설치 전에 `npm view @anthropic-ai/sdk@<승인한 버전> scripts`에 `postinstall`·`preinstall`·`install`이 없음을 확인한다(있으면 멈추고 보고). `pnpm-lock.yaml`을 함께 커밋한다(`chore(06-04): …`). reject-fetch면 아무것도 설치하지 않고 이 단계를 건너뛴다.

**스파이크 (A) — SDK가 MV3 service worker 프로덕션 빌드에서 동작하는가(RESEARCH Open Question 1). 결과를 SUMMARY에 적는다.** `client.ts`에서 호출마다 `new Anthropic({ apiKey, dangerouslyAllowBrowser: true })`를 만들고, 아래 성공 e2e를 `CI=true`(프로덕션 빌드)로 돌려 호출이 가짜 AI까지 가는지 본다. 번들·생성·호출 중 하나라도 안 되면 멈추고 보고한다 — fetch로 바꾸는 것은 이용자 확인 뒤에만(D-25). 가짜 AI 도구가 SDK의 요청을 못 잡으면 06-01-SUMMARY의 가로채기 방법 (b)(SW fetch 바꿔 끼우기)를 이 요청에도 적용하고, `fake-anthropic.ts`를 고쳐야 하면 그 파일 한 곳만 고친 뒤 SUMMARY에 편차로 적는다. reject-fetch면 이 스파이크는 건너뛰고 06-01 Task 1 context의 fetch 경로로 짠다.

**RED:** `tests/practice-site/ai-cleanup.html`을 만든다 — 버튼 약 14개(모두 id·name, 글자 "상신", "임시저장", "검색", "결재선", "목록", "첨부", "인쇄", "이전", "다음", "새로고침", "도움말", "닫기" 등), 위험 버튼 "삭제"(`id=btn-delete`), aria-label이 있는 링크 몇 개. 모든 요소는 click·pointerdown·focus·input을 `window.__events`에 쌓고 `data-count`를 올린다(06-08 안전 시험이 다시 쓴다). fixtures의 `servePage`로 `http://practice.test/ai-cleanup.html`에 올린다. iframe은 Task 2가 더한다. `tests/e2e/ai-cleanup.e2e.ts`에 behavior의 새 e2e 세 개를 더하고 실패를 확인한다(RED 커밋).

**GREEN:**
- 창구 호출(`src/worker/ai/client.ts`, D-25): `requestCleanupPicks({ apiKey, system, userContent, signal })` → `ClientOutcome` = `{ ok: true, text, stopReason }` 또는 `{ ok: false, failure: 'key-rejected' | 'call-failed' | 'canceled' }`. SDK 경로: 호출마다 클라이언트를 만들고 `messages.create({ model: AI_MODEL, max_tokens: AI_MAX_TOKENS, system, messages: [{ role: 'user', content: userContent }], output_config: { format: { type: 'json_schema', schema: CLEANUP_OUTPUT_SCHEMA } } }, { signal, timeout: 15000, maxRetries: 1 })`, 첫 text 블록의 글자와 `stop_reason`을 돌려준다(text 블록이 없으면 빈 글자). 이 작업에서는 모든 예외를 `call-failed`로 돌린다(오류 종류 가르기는 06-08). SDK import와 API 주소는 이 파일에만 있다(D-04). 로그에 키·요청 본문·응답 글자를 남기지 않는다.
- 메시지(`src/shared/messages.ts`): `ai/cleanup-request`의 항목에 `kind: string(≤40)`, `text: string(≤200)`을 더한다. `CleanupResultSchema`를 `kind` 판별 유니온으로 바꿔 `{ kind: 'ai', itemKeys: string[] (최대 9) }`를 더한다. 결과에 키·요청 본문을 싣지 않는다(D-18).
- 창구(`src/worker/ai/gateway.ts`, D-04·D-19·D-20): 06-01이 키가 있을 때 `call-failed`를 돌려주던 분기를 AI 호출로 바꾼다 — 메시지 항목을 `AiCandidate`(`{ key, kind, text, rect }`)로 옮김 → `buildAiPayload(candidates, message.viewport)` → entries가 비면(글자 있는 요소 0개) 요청 없이 `invalid-response` → `requestCleanupPicks({ apiKey, system: CLEANUP_SYSTEM_PROMPT, userContent: buildUserContent(entries) })` → 실패면 그 사유로 대체 → `judgeAiAnswer(outcome, entries의 id 집합)`이 null이면 `invalid-response` → 임시 번호를 `idToKey`로 요소 열쇠로 바꿔 `{ kind: 'ai', itemKeys }`. 저장된 결과·한 달 한도는 06-06이, 취소는 06-07이 이 흐름에 더한다.
- content(`src/entrypoints/content.ts`, 맨 위 프레임만): 요청 항목에 `kind: 합친 항목의 kind ?? ''`, `text: 합친 항목의 aiText ?? ''`를 싣는다. `openHints(options?)`에 `aiPickIds?`를 더해 `orderHints`로 넘긴다. `ai` 결과면 `openHints({ aiPickIds: itemKeys, composed })` 뒤 `setHint(AI_SUCCESS_HINT)`. AI 결과로 클릭·포커스·입력을 만드는 코드는 쓰지 않는다 — 번호로 누르는 것은 이용자 키뿐이다(D-08).
GREEN 커밋 뒤 필요하면 refactor 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-cleanup.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report "5 passed", or the output contains "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit, or any "@typescript-eslint/no-explicit-any" error in the output</fails_when>
    <automated>test -z "$(grep -rlE --include='*.ts' 'anthropic-ai/sdk|api\.anthropic\.com' src | grep -v '^src/worker/ai/client.ts$')"</automated>
    <fails_when>non-zero exit — some file under src other than src/worker/ai/client.ts imports the SDK or names the API host</fails_when>
    <automated>node -e "const d=require('./package.json').dependencies||{};const v=d['@anthropic-ai/sdk'];if(v!==undefined&&!/^[0-9]+\.[0-9]+\.[0-9]+$/.test(v))process.exit(1)"</automated>
    <fails_when>non-zero exit — the SDK is declared with a range (for example a caret) instead of the exact approved version</fails_when>
    <automated>! git grep --untracked -nE 'sk-ant-[A-Za-z0-9_-]{20,}' -- src tests package.json</automated>
    <fails_when>non-zero exit because git grep printed a line that looks like a real Anthropic key</fails_when>
  </verify>
  <acceptance_criteria>
    - 06-01 Task 1이 approve-sdk면 `package.json` dependencies에 `"@anthropic-ai/sdk": "<승인한 정확한 버전>"`(`^` 없음)이 있고, reject-fetch면 그 줄이 없다
    - `src/worker/ai/client.ts`가 `requestCleanupPicks`를 export하고, SDK import와 API 주소가 `client.ts` 밖의 `src` 파일에 없다
    - `src/worker/ai/gateway.ts`가 `buildAiPayload`·`requestCleanupPicks`·`judgeAiAnswer`를 부르고, 06-01의 키 있음 `call-failed` 분기가 남아 있지 않다
    - `src/shared/messages.ts`의 `CleanupResultSchema`에 `itemKeys` 변형이 있다
    - `CI=true pnpm exec playwright test tests/e2e/ai-cleanup.e2e.ts`가 5 passed이고 모든 시험이 `blockedRequests`가 비어 있음을 단언한다
    - 스파이크 (A)의 결과(또는 reject-fetch로 건너뜀)가 `06-04-SUMMARY.md`에 있다
  </acceptance_criteria>
  <done>키가 있으면 명령판 카드 하나로 가짜 AI가 고른 9개에 AI 순서로 번호가 붙고, 답을 쓸 수 없으면 자주 누른 순서로 대신한다. SDK(또는 승인된 대안)가 SW 프로덕션 빌드에서 동작함이 확인됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: iframe 안 요소도 AI가 고르고 번호로 누른다</name>
  <reversibility rating="reversible">연습 페이지와 e2e만 더한다.</reversibility>
  <files>tests/practice-site/ai-cleanup-frame.html, tests/practice-site/ai-cleanup.html, tests/e2e/ai-cleanup.e2e.ts</files>
  <read_first>
    - tests/practice-site/ai-cleanup.html, tests/e2e/ai-cleanup.e2e.ts (Task 1 결과)
    - tests/e2e/frames.e2e.ts (교차 출처 iframe 요소를 번호로 누르는 확인 방식), tests/e2e/fixtures.ts (servePage)
    - .planning/phases/06-ai/06-CONTEXT.md (D-01)
  </read_first>
  <behavior>
    - 가짜 AI 각본을 `pick-texts`(맨 위 버튼 8개 + iframe 안 버튼 "결재 요청"을 셋째로)로 두면 번호표가 정확히 9개이고, `Digit3`을 누르면 iframe 안 "결재 요청"의 `data-count`가 1이 되며 맨 위 요소의 `data-count`는 그대로다
    - 가짜 AI가 받은 요청 본문 elements의 글자에 iframe 안 버튼 글자("결재 요청", "파일 선택")가 있다
    - `blockedRequests`가 비어 있다
  </behavior>
  <action>
`tests/practice-site/ai-cleanup-frame.html`을 만든다 — 버튼 3~4개(예: "결재 요청", "파일 선택"), 맨 위 페이지와 같은 방식으로 이벤트를 `window.__events`와 `data-count`에 기록한다. `ai-cleanup.html`에 `<iframe src="http://other.test/ai-cleanup-frame.html">`을 더하고 두 페이지를 fixtures의 `servePage`로 올린다. behavior를 `tests/e2e/ai-cleanup.e2e.ts`에 더한다. 06-03(보고의 글자)과 Task 1(요청·번호)이 이미 이 경로를 덮으므로 처음부터 초록일 수 있다 — 그러면 그 사실을 SUMMARY에 적는다. 빨간불이면 `systematic-debugging`으로 원인(재현 → 원인 → 수정)을 찾아 고치고, 이 계획 밖 파일을 고쳤으면 SUMMARY에 편차로 적는다. Task 1의 맨 위 시험들은 iframe이 생겨도 통과해야 한다(각본이 맨 위 글자만 고르므로).
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-cleanup.e2e.ts tests/e2e/frames.e2e.ts</automated>
    <fails_when>non-zero exit, or any test in the summary is reported as failed, or the ai-cleanup file reports fewer than 6 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/practice-site/ai-cleanup-frame.html`이 있고 `ai-cleanup.html`이 `http://other.test/ai-cleanup-frame.html` iframe을 담는다
    - `tests/e2e/ai-cleanup.e2e.ts`에 iframe 요소를 셋째로 고르고 `Digit3`으로 누르는 시험과 요청 본문에 iframe 글자가 있음을 보는 단언이 있다
    - 위 명령이 통과하고 ai-cleanup e2e가 6개 이상 통과한다
  </acceptance_criteria>
  <done>iframe 안 요소도 AI가 받은 목록에 들어가고, AI가 고르면 번호표가 붙어 이용자 키로 누를 수 있다.</done>
</task>

</tasks>

<!-- planner-discipline-allow: anthropic-ai/sdk -->
<!-- planner-discipline-allow: api.anthropic.com -->

## 가정 (spec-less edge probe)

- AI-01 (주 계획): edge probe `unclassified — review manually`. 가정: "중요한 요소 9개"는 AI가 1~9개를 고르는 것으로 본다 — 화면에 요소가 9개보다 적거나 AI가 9개 미만을 고르면 고른 만큼 AI 순서로 번호를 붙이고 남은 번호는 Phase 1 순서(고정 → 자주 누름 → 커서 근처)로 채운다(06-02 Task 3). 요소가 9개 이하인 화면에서도 AI를 부른다(06-CONTEXT discretion — 이용자가 키를 눌렀으므로). 글자 있는 요소가 하나도 없으면 AI를 부르지 않고 대체한다. "같은 페이지 다시 열면 저장된 번호" 경계는 06-06이 맡는다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → 저장소 | 설치하는 SDK 코드가 확장 SW 안에서 이용자 키와 함께 실행된다 |
| service worker → api.anthropic.com | 요소 목록과 이용자 키가 확장 밖으로 나간다(확장 전체에서 유일, D-12) |
| AI 답 → service worker → content | 신뢰할 수 없는 모델 출력이 번호 순서를 정한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | `pnpm add @anthropic-ai/sdk` | high | mitigate | 06-01 Task 1 사람 승인·npm 페이지 진위 확인(blocking-human) 뒤에만, precondition이 기록된 선택을 확인. 정확한 버전 고정(verify), `pnpm-lock.yaml` 커밋, 설치 전 install 스크립트 없음 확인. 거절 시 설치 없이 fetch |
| T-06-33 | Information Disclosure | SDK·API 주소가 `client.ts` 밖으로 퍼짐 | medium | mitigate | verify의 grep — SDK import·API 주소는 `client.ts` 한 곳(D-04, D-12) |
| T-06-34 | Information Disclosure | 요청 본문의 요소 열쇠·식별 묶음·좌표 | medium | mitigate | 창구가 `AiCandidate`로만 옮기고 `buildAiPayload`가 네 키만 만든다(06-02 단위 시험). `ai` 결과는 `itemKeys`만 담고 키·본문이 없다. 06-05가 실제 요청 본문 e2e로 다시 확인 |
| T-06-35 | Denial of Service | 느리거나 멈춘 API | low | mitigate | 시간 초과 15초, 재시도 1회, max_tokens 256. 실패는 대체 결과로 끝나 다른 기능을 막지 않는다(06-08이 e2e로 확인) |
| T-06-36 | Information Disclosure | 오류·디버그 로그 | low | mitigate | `client.ts`·`gateway.ts`가 키·요청 본문·응답 글자를 로그에 남기지 않는다 |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/ai-cleanup.e2e.ts tests/e2e/frames.e2e.ts` 통과(ai-cleanup 6개 이상)
- `pnpm typecheck && pnpm lint` 통과
- SDK·API 주소가 `client.ts` 밖에 없고, SDK 버전이 정확히 고정됐으며, 진짜 키 모양 문자열이 추적 파일에 없다
</verification>

<success_criteria>
- 06-01 Task 1의 선택대로만 설치했다(승인 없이 설치된 패키지 없음)
- 스파이크 (A) 결과가 SUMMARY에 있고, SDK(또는 fetch)가 SW 프로덕션 빌드에서 가짜 AI까지 갔다
- AI가 고른 번호(맨 위·iframe)와 쓸 수 없는 답 대체가 CI=true e2e로 확인됐다
- RED(`test(06-04)`)·GREEN(`feat(06-04)`) 커밋이 있다
</success_criteria>

## Artifacts this phase produces

- 파일: `src/worker/ai/client.ts`, `tests/practice-site/ai-cleanup.html`, `tests/practice-site/ai-cleanup-frame.html`
- 심볼: `requestCleanupPicks`, `ClientOutcome`, `CleanupResultSchema`의 `ai` 변형(`itemKeys`), `ai/cleanup-request` 항목의 `kind`·`text`, `openHints`의 `aiPickIds`
- 의존성: `@anthropic-ai/sdk`(06-01 Task 1이 승인한 경우만, 정확한 버전)

<output>
Create `.planning/phases/06-ai/06-04-SUMMARY.md` when done (설치한 버전 또는 fetch 선택, 스파이크 (A) 결과, SDK 요청을 가로챈 방법, iframe 시험이 처음부터 초록이었는지 포함)
</output>
