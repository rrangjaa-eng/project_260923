---
phase: 06-ai
plan: 02
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/core/ai/response.ts
  - tests/unit/ai-response.test.ts
  - src/core/ai/request-builder.ts
  - tests/unit/ai-request-builder.test.ts
  - src/core/hint-order.ts
  - tests/unit/hint-order.test.ts
autonomous: true
requirements: [AI-06, AI-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "AI 답이 보낸 목록에 없는 임시 번호를 하나라도 고르거나, 0개·10개 이상을 고르거나, picks 밖의 키가 있거나, JSON이 아니거나, stop_reason이 end_turn이 아니면 답 전체가 버려진다(부분 필터 아님) — 설계 10장 \"AI 답 검사\" 단위 시험(AI-06, D-19, D-27)"
    - "보낼 목록은 글자가 빈 요소를 빼고 읽는 순서(위→아래, 왼→오른)로 최대 150개, 글자는 60자까지이며, 항목은 임시 번호·종류·글자·9구역 이름 네 가지뿐이다 — 요소 열쇠·식별 묶음·좌표는 들어가지 않는다(D-06, D-09)"
    - "AI가 고른 요소는 이용자가 고정한 번호를 건드리지 않고 비어 있는 1~9 번호를 고른 순서대로 채우며, AI 순서를 주지 않으면 번호 순서가 지금(Phase 1)과 똑같다(D-06, D-20)"
  prohibitions:
    - statement: "AI 답에서 목록 밖 번호만 걸러 내고 나머지를 쓰는 부분 받아들이기 경로를 만들지 않는다 — 하나라도 틀리면 답 전체를 버리고 자주 누른 순서로 대신한다(D-19, D-20)"
      requirement_id: AI-06
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/core/ai/response.ts"
      provides: "구조화 출력 JSON schema와 zod 답 검사(하나라도 틀리면 전체 버림)"
      exports: ["CLEANUP_OUTPUT_SCHEMA", "AiResponseSchema", "parseAiText", "validateAiResponse", "judgeAiAnswer"]
    - path: "src/core/ai/request-builder.ts"
      provides: "모델·지시문·후보 요소 → 임시 번호가 붙은 보낼 목록(순수 함수)"
      exports: ["AI_MODEL", "AI_MAX_TOKENS", "CLEANUP_SYSTEM_PROMPT", "AiCandidate", "buildAiPayload", "buildUserContent"]
    - path: "src/core/hint-order.ts"
      provides: "orderHints의 선택 인자 aiPickIds — 고정 번호 다음, 자주 누름보다 앞"
      contains: "aiPickIds"
    - path: "tests/unit/ai-response.test.ts"
      provides: "설계 10장 \"AI 답 검사\" 단위 시험 행렬"
    - path: "tests/unit/ai-request-builder.test.ts"
      provides: "보낼 목록 모양·순서·상한·구역 단위 시험(06-05가 가림 경우를 더한다)"
  key_links:
    - from: "src/core/ai/response.ts"
      to: "src/core/ai/request-builder.ts"
      via: "validateAiResponse(raw, sentIds)의 sentIds는 buildAiPayload가 만든 entries의 id 집합이다"
      pattern: "sentIds"
    - from: "tests/unit/hint-order.test.ts"
      to: "src/core/hint-order.ts"
      via: "aiPickIds를 주는 경우와 빼는 경우(기존 결과와 같음)"
      pattern: "aiPickIds"
---

## Phase Goal

**As a** 이용자, **I want to** 처음 보는 복잡한 화면에서 명령판의 "화면 정리"를 누르면 중요한 요소 9개에 번호가 붙기를, **so that** AI가 없거나 실패해도 나머지 기능은 그대로이고, 개인정보와 입력 값은 밖으로 나가지 않는다.

(ROADMAP Phase 6 **Goal** 문장을 그대로 세 칸에 옮긴 것이다. 새 내용은 더하지 않았다. `/gsd-mvp-phase 6`으로 다듬을 수 있다.)

<objective>
화면 정리의 순수 규칙 세 가지를 실패하는 단위 시험부터 만든다: AI 답 검사(설계 10장), 보낼 목록 만들기, 번호 순서의 AI 단계. 06-04 Task 1이 이 셋을 AI 창구와 content에 잇는다.

Purpose: D-06·D-07·D-09·D-19·D-20·D-27(답 검사 단위 시험), AI-06의 "목록 밖 답은 전체 버림", AI-01의 "고른 요소에 1~9 번호".
Output: `src/core/ai/response.ts`, `src/core/ai/request-builder.ts`, `orderHints`의 `aiPickIds`, 단위 시험 3개.

재분할 메모(계획 점검 반영): 원래 06-01 tracer 안에 있던 순수 함수들과 06-01 Task 3의 답 검사 행렬을 이 계획으로 옮겼다. 06-03과 같은 wave에서 병렬로 돈다(파일이 겹치지 않는다).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai/06-CONTEXT.md
@.planning/phases/06-ai/06-RESEARCH.md
@.planning/phases/06-ai/06-PATTERNS.md
</context>

## 앞선 단계 의존

- 06-01 완료: 승인 체크포인트가 이 phase의 첫 실행 단계로 끝났다(이 계획이 06-01 뒤에 도는 이유 — 파일은 겹치지 않는다). 이 계획은 06-01의 파일을 쓰지 않는다.
- Phase 1: `src/core/hint-order.ts`의 `orderHints`(고정 번호 → 자주 누름 → 커서 근처)와 `tests/unit/hint-order.test.ts`, `src/core/grid-index.ts`의 `Rect`.
- 이 계획은 06-03과 같은 wave에서 병렬로 돈다. `collector.ts`, `ai-text.ts`, `frame-tree.ts`, `messages.ts`는 고치지 않는다(06-03 소유).

## 실행 규칙 (모든 작업 공통)

- 패키지 매니저는 pnpm만. TypeScript strict, `any` 금지, 시크릿을 코드·커밋에 넣지 않는다(CLAUDE.md). 새 패키지 없음.
- 구현 전에 Superpowers `test-driven-development`를, 시험 실패를 쫓기 전에 `systematic-debugging`을, "완료" 전에 `verification-before-completion`을 **호출**한다. 서브에이전트에 맡기면 프롬프트에 세 스킬과 `model`을 적는다(점검·판단은 Opus 5).
- 각 작업은 RED 커밋 `test(06-02): …` → GREEN 커밋 `feat(06-02): …` → 필요하면 `refactor(06-02): …`. 제목은 영어 접두어, 본문은 한국어.
- 세 파일 모두 순수 함수다 — `chrome`·`document`·`window`를 참조하지 않는다.
- 실제 회사 시스템에 접속하거나 시험하지 않는다(D-29).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: "AI 답 검사" — 구조화 출력 형식과 전체 버림 검사 (설계 10장)</name>
  <reversibility rating="reversible">순수 함수 하나와 그 단위 시험이다. 저장 형식에 들어가지 않는다.</reversibility>
  <files>src/core/ai/response.ts, tests/unit/ai-response.test.ts</files>
  <read_first>
    - .planning/phases/06-ai/06-RESEARCH.md (Pattern 2 "답 검사는 전체 버림", Pitfall 2, "Code Examples" 구조화 출력 요청)
    - .planning/phases/06-ai/06-CONTEXT.md (D-19, D-20, D-27, specifics "구조화 출력 제약")
    - .planning/phases/06-ai/06-PATTERNS.md (`src/core/ai/response.ts` 절)
  </read_first>
  <behavior>
    - 보낸 id 9개를 순서대로 고르면 그 9개를 그 순서로 돌려준다; 1개만 골라도 받아들인다
    - 8개는 맞고 1개가 보낸 목록에 없으면 null(부분 필터 아님)
    - picks가 빈 배열이면 null, 10개면 null
    - `['t1', 't1', 't2']`는 `['t1', 't2']`
    - picks 밖의 키가 더 있으면 null(strict), picks가 배열이 아니거나 문자열이 아닌 값이 있으면 null
    - `parseAiText('not json')`은 null, 올바른 JSON 글자는 객체
    - `judgeAiAnswer`: stopReason `refusal`·`max_tokens`면 글자가 올바라도 null, `end_turn`이면 검사 결과
    - `CLEANUP_OUTPUT_SCHEMA`는 `required: ['picks']`, `additionalProperties: false`이고 `minItems`·`maxItems`·`maxLength` 같은 제약 키가 없다
  </behavior>
  <action>
behavior 경우마다 한 시험으로 `tests/unit/ai-response.test.ts`를 먼저 쓰고 실패를 확인한다(RED). 시험 이름에 "AI 답 검사"를 넣는다(설계 10장 이름과 맞춤). 그다음 `src/core/ai/response.ts`를 만든다(GREEN, D-19):
- `CLEANUP_OUTPUT_SCHEMA`: picks 문자열 배열 하나만 있는 JSON schema(`type: 'object'`, `properties.picks = { type: 'array', items: { type: 'string' } }`, `required: ['picks']`, `additionalProperties: false`). 구조화 출력이 지원하지 않는 개수·길이 제약은 넣지 않는다(06-CONTEXT specifics) — 개수는 우리 zod 검사가 본다.
- `AiResponseSchema` = zod strict `{ picks: string[] 1~9개 }`.
- `parseAiText(text)`: JSON이 아니면 null.
- `validateAiResponse(raw, sentIds: ReadonlySet<string>)`: 스키마 실패 → null, 보낸 id가 아닌 것이 하나라도 있으면 → null(부분 필터 아님), 중복은 처음 순서대로 하나만 남긴다. 검사 순서는 zod 스키마 → 보낸 목록 포함 여부 → 중복 제거.
- `judgeAiAnswer({ text, stopReason }, sentIds)`: stopReason이 `end_turn`이 아니면 null, 아니면 parse → validate.
구조화 출력이 형식을 보장하더라도 우리 검사를 건너뛰지 않는다(RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/ai-response.test.ts</automated>
    <fails_when>non-zero exit, or the Tests summary line reports fewer than 11 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/ai/response.ts`가 `CLEANUP_OUTPUT_SCHEMA`, `AiResponseSchema`, `parseAiText`, `validateAiResponse`, `judgeAiAnswer`를 export한다
    - `tests/unit/ai-response.test.ts`에 목록 밖 id 하나 섞임 → null, 0개 → null, 10개 → null, 중복 제거, 추가 키 → null, JSON 아님 → null, refusal → null, max_tokens → null, 스키마 모양 경우가 각각 있다
    - `pnpm exec vitest run tests/unit/ai-response.test.ts`가 11개 이상 통과
  </acceptance_criteria>
  <done>설계 10장 "AI 답 검사"가 경우별 단위 시험으로 고정됐고, 목록 밖 답·형식 틀림·거절·잘림이 모두 전체 버림으로 확인된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 보낼 목록 — 종류·글자·9구역 이름·임시 번호만, 읽는 순서 150개</name>
  <reversibility rating="reversible">SW에서 쓰는 순수 함수와 상수다. 저장 형식이 아니다.</reversibility>
  <files>src/core/ai/request-builder.ts, tests/unit/ai-request-builder.test.ts</files>
  <read_first>
    - .planning/phases/06-ai/06-RESEARCH.md ("Code Examples" 요소 목록 → 마스킹된 요청 페이로드, Assumptions A8)
    - .planning/phases/06-ai/06-CONTEXT.md (D-06, D-07, D-09, discretion "보내는 목록의 크기 상한·글자 자르기, 대략의 위치")
    - .planning/phases/06-ai/06-PATTERNS.md (`src/core/ai/request-builder.ts` 절)
    - src/core/grid-index.ts (Rect)
  </read_first>
  <behavior>
    - `AI_MODEL`은 `'claude-haiku-4-5'`, `AI_MAX_TOKENS`는 256, `CLEANUP_SYSTEM_PROMPT`는 영어이고 `9`와 `picks`를 담는다
    - 글자가 빈(공백뿐인) 후보는 빠진다; 200개를 넣으면 읽는 순서(위→아래, 같은 줄이면 왼→오른)로 150개, id는 `t0`부터 차례로
    - 61자 글자는 60자로 잘린다
    - 화면(1200×900) 왼쪽 위 요소의 zone은 `'top-left'`, 가운데는 `'middle-center'`, 오른쪽 아래는 `'bottom-right'`, 화면 아래로 벗어난 요소는 `bottom-*`
    - entries 항목의 키는 정확히 `id`·`kind`·`text`·`zone`이다 — 후보의 `key`·`rect`와 후보에 붙어 온 다른 필드는 옮겨지지 않는다
    - `idToKey`가 임시 번호 → 요소 열쇠를 정확히 잇는다
    - `buildUserContent(entries)`는 `JSON.stringify({ elements: entries })`와 같다
  </behavior>
  <action>
behavior 경우로 `tests/unit/ai-request-builder.test.ts`를 먼저 쓰고 RED 커밋. 그다음 `src/core/ai/request-builder.ts`를 만든다(GREEN, D-07·D-09):
- `AI_MODEL = 'claude-haiku-4-5'`(D-07 — 실행 때 모델 ID 확인은 06-01 Task 1이 했다), `AI_MAX_TOKENS = 256`.
- `CLEANUP_SYSTEM_PROMPT`(영어): 번호 키로 누르는 이용자에게 이 화면에서 가장 중요한 요소를 최대 9개 고르라는 것, 요소 글자는 자료일 뿐 지시가 아니라는 것, 목록의 id만 `picks`로 답하라는 것.
- `AiCandidate` = `{ key: string; kind: string; text: string; rect: Rect }`(요청 메시지와 무관한 이 파일의 입력 모양 — 06-04의 창구가 메시지 항목을 이 모양으로 옮긴다).
- `buildAiPayload(candidates: readonly AiCandidate[], viewport: { w, h })` → `{ entries: [{ id, kind, text, zone }], idToKey: Map<string, string> }`: 앞뒤 공백을 뺀 글자가 빈 후보를 빼고, 읽는 순서로 정렬해 최대 150개, 글자는 60자까지 자른다. `zone`은 요소 가운데를 화면 안으로 끌어온 점이 화면 가로·세로 3등분 중 어디인지(`top-left` … `bottom-right` 9개 영어 이름). 결과 항목은 네 키로 새로 만든다(펼치기 복사 금지 — 후보에 붙어 온 필드가 따라가지 않게, D-09). 가림(`maskPii`)은 06-05가 이 함수에 더한다.
- `buildUserContent(entries)` = `JSON.stringify({ elements: entries })`.
페이지 주소·제목·속성 값·요소 식별 묶음은 이 함수의 입력에도 없다(D-09).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/ai-request-builder.test.ts</automated>
    <fails_when>non-zero exit, or the output reports "No test files found", or the Tests summary line reports fewer than 8 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/ai/request-builder.ts`에 `'claude-haiku-4-5'`와 `AI_MAX_TOKENS = 256`이 있고 `AiCandidate`, `buildAiPayload`, `buildUserContent`, `CLEANUP_SYSTEM_PROMPT`를 export한다
    - `tests/unit/ai-request-builder.test.ts`가 entries 항목 키가 정확히 `id`·`kind`·`text`·`zone`임을 단언한다
    - 빈 글자 제외, 150개 상한, 60자 자르기, 구역 셋, `idToKey` 경우가 있고 8개 이상 통과한다
  </acceptance_criteria>
  <done>보낼 목록의 모양·순서·상한·구역이 단위 시험으로 고정됐고, 요소 열쇠와 좌표는 SW 안에만 남는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 번호 순서의 AI 단계 — `orderHints`의 `aiPickIds`</name>
  <reversibility rating="reversible">선택 인자 하나를 더한다. 인자를 빼면 지금과 같다.</reversibility>
  <files>src/core/hint-order.ts, tests/unit/hint-order.test.ts</files>
  <read_first>
    - src/core/hint-order.ts (orderHints, HintItem, Pin, PressCount, HintEntry, 첫 장 1~9 번호 채우기)
    - tests/unit/hint-order.test.ts (기존 경우와 고정물 만드는 방식)
    - .planning/phases/06-ai/06-RESEARCH.md (Pattern 3 "hint-order.ts에 AI 픽을 새 우선순위 단계로 끼워 넣기")
    - .planning/phases/06-ai/06-CONTEXT.md (D-06, D-20, discretion "고정 번호와 AI가 고른 번호가 겹칠 때")
  </read_first>
  <behavior>
    - `aiPickIds`를 주면 고정 번호는 제자리이고, AI가 고른 요소가 비어 있는 1~9 번호를 고른 순서대로 채운다
    - 고정 번호가 3번이고 AI가 5개를 고르면 AI 요소는 1·2·4·5·6번이다
    - 번호를 못 받은 AI 요소(고정 번호가 많아 자리가 없음)는 나머지 줄의 맨 앞에 선다
    - 목록에 없는 id와 이미 쓴 id(고정된 요소·중복)는 건너뛴다
    - AI 요소 다음 빈 번호는 기존 순서(자주 누름 → 커서 근처)로 채운다
    - `aiPickIds`를 빼거나 빈 배열이면 결과가 지금과 같다(기존 시험 그대로 통과)
  </behavior>
  <action>
behavior 경우를 `tests/unit/hint-order.test.ts`에 더하고 RED 커밋. 그다음 `orderHints`에 선택 인자 `aiPickIds?: readonly string[]`를 더한다(GREEN, RESEARCH Pattern 3): 고정 번호가 먼저 자리를 지키고(이용자가 고정한 번호가 우선 — 06-CONTEXT discretion 기본 제안), AI가 고른 요소가 비어 있는 1~9 번호를 고른 순서대로 채우며, 번호를 못 받은 AI 요소는 나머지 줄의 맨 앞에 선다. 남은 자리는 지금 규칙(자주 누름 → 커서 근처) 그대로다. 인자가 없으면 기존 코드 경로를 그대로 탄다. 06-04 Task 1이 content의 `openHints`에서 이 인자를 넘긴다.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/hint-order.test.ts</automated>
    <fails_when>non-zero exit, or any test in the summary is reported as failed (including the pre-existing hint-order cases)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/hint-order.ts`의 `orderHints` 인자에 `aiPickIds`가 있다
    - `tests/unit/hint-order.test.ts`에 behavior 여섯 경우가 더해졌고 기존 경우까지 모두 통과한다
  </acceptance_criteria>
  <done>AI가 고른 요소가 고정 번호를 건드리지 않고 1~9를 채우는 규칙이 단위 시험으로 고정됐고, 인자가 없으면 Phase 1 순서 그대로다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- AI-06 (일부 — 답 검사): edge probe `unclassified — review manually`. 가정: "목록에 없는 요소"는 보낸 임시 번호가 아닌 값으로 판정하고, 하나라도 있으면 전체를 버린다. 형식 틀림·거절·잘림도 같은 대체로 본다. 오류 종류별 대체와 "직접 눌리지 않음"은 06-08이 맡는다.
- AI-01 (일부 — 번호 순서): 가정: AI가 9개 미만을 고르면 고른 만큼 AI 순서로 번호를 붙이고 남은 번호는 Phase 1 순서(고정 → 자주 누름 → 커서 근처)로 채운다. 이용자가 고정한 번호가 AI보다 앞선다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI 답 → service worker | 신뢰할 수 없는 모델 출력이 번호 순서를 정한다 |
| 페이지 요소 글자 → 모델 지시문 | 사이트가 요소 글자에 지시처럼 보이는 글을 둘 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-28 | Tampering | `validateAiResponse`·`judgeAiAnswer` | high | mitigate | zod strict(1~9개, 문자열, 추가 키 없음) → 보낸 임시 번호만 → 하나라도 틀리면 전체 버림 → stop_reason `end_turn`만. 단위 시험 11개 이상(D-19) |
| T-06-03 | Tampering | 요소 글자를 통한 지시 주입 | medium | mitigate | 지시문이 요소 글자를 자료로만 다루게 하고, 답은 보낸 임시 번호만 받아들이며 하나라도 틀리면 전체 버림. AI는 번호만 붙이고 누르지 않는다(D-08, 06-08이 e2e로 확인). 남는 위험(페이지가 자기 요소의 순서를 흔듦)은 accept — 누르는 것은 이용자 키뿐이고 위험 버튼 보호가 그대로다 |
| T-06-29 | Denial of Service | 보낼 목록 크기 | low | mitigate | 150개·60자 상한과 `AI_MAX_TOKENS = 256`, 단위 시험으로 고정 |
| T-06-30 | Tampering | AI 순서가 이용자 고정 번호를 밀어냄 | low | mitigate | 고정 번호가 먼저 자리를 지키고 AI는 빈 번호만 채운다(단위 시험) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/ai-response.test.ts tests/unit/ai-request-builder.test.ts tests/unit/hint-order.test.ts` 통과
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- "AI 답 검사"·보낼 목록·번호 순서의 AI 단계가 단위 시험으로 고정됐다
- 작업마다 RED(`test(06-02)`)·GREEN(`feat(06-02)`) 커밋이 있다
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/ai/response.ts`, `src/core/ai/request-builder.ts`, `tests/unit/ai-response.test.ts`, `tests/unit/ai-request-builder.test.ts`
- 심볼: `CLEANUP_OUTPUT_SCHEMA`, `AiResponseSchema`, `parseAiText`, `validateAiResponse`, `judgeAiAnswer`, `AI_MODEL`, `AI_MAX_TOKENS`, `CLEANUP_SYSTEM_PROMPT`, `AiCandidate`, `buildAiPayload`, `buildUserContent`, `orderHints`의 `aiPickIds`

<output>
Create `.planning/phases/06-ai/06-02-SUMMARY.md` when done (답 검사 순서, 구역 계산 규칙, 고정 번호와 AI 번호가 겹친 경우 관찰 포함)
</output>
