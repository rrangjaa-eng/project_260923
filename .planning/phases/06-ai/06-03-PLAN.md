---
phase: 06-ai
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/core/ai/ai-storage.ts
  - tests/unit/ai-storage.test.ts
  - src/worker/storage-writer.ts
  - src/worker/ai/gateway.ts
  - tests/e2e/fake-anthropic.ts
  - tests/e2e/ai-saved-limit.e2e.ts
  - src/core/ai/cleanup-copy.ts
  - src/entrypoints/content.ts
  - src/shared/messages.ts
  - src/worker/relay.ts
  - src/entrypoints/background.ts
  - tests/e2e/ai-cancel.e2e.ts
autonomous: true
requirements: [AI-03, AI-04]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "같은 화면(같은 사이트 + 쿼리를 뺀 페이지 주소)에서 \"화면 정리\"를 다시 누르거나, 페이지를 다시 열고 누르면 AI 요청 없이 저장된 번호가 같은 요소에 다시 붙는다(AI-03, D-14, D-15, ROADMAP 성공 기준 1)"
    - "맨 위 주소가 같아도 화면의 요소가 바뀌어 저장된 요소가 절반 미만만 맞으면 저장을 쓰지 않고 AI를 다시 부르며, 원래 화면으로 돌아오면 먼저 저장한 번호를 다시 쓴다(iframe 본문 회사 시스템 대비)"
    - "이번 달(이 PC 달력) AI 요청 수가 이용자가 정한 한도에 이르면 AI를 부르지 않고 \"이번 달 AI 사용 한도를 넘었어요. 자주 누른 순서로 번호를 붙였어요\"를 보인 뒤 자주 누른 순서 번호표를 붙인다. 다음 달이 되면 다시 부른다(AI-04, D-16)"
    - "AI 요청은 이용자가 명령판 카드를 누를 때만 나간다 — 페이지를 열 때, 번호표(F)를 켤 때, 저장된 번호를 쓸 때는 0개다(D-05)"
    - "요청이 0.3초 넘게 걸리면 \"화면을 정리하는 중 · Esc 취소\"가 보이고, Esc를 누르면(초점이 iframe 안이어도) 요청이 끊기고 번호표·안내 없이 원래 상태로 돌아간다. 0.3초 안에 끝나면 이 표시는 나오지 않는다(D-22)"
    - "AI 사용량·결과 저장은 service worker의 단일 저장자 큐로만 쓰이고, 읽을 수 없는 저장 값은 덮어쓰지 않는다(D-03)"
  prohibitions:
    - statement: "이용자가 명령판의 \"화면 정리\" 카드를 진짜 키로 누르지 않았는데 AI를 부르는 경로를 만들지 않는다 — 페이지 열기·번호표 켜기·저장된 결과 낡음·타이머·사이트가 만든 가짜 입력으로는 AI 요청이 나가지 않는다(D-02, D-05)"
      requirement_id: AI-03
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/core/ai/ai-storage.ts"
      provides: "저장 열쇠·한 달 사용량·저장된 결과 형식(schemaVersion 1)과 맞추기(순수 함수)"
      exports: ["pageKeyOf", "monthKeyOf", "aiUsageKey", "AiUsageV1", "isOverLimit", "AI_RESULTS_KEY", "AiResultsV1", "matchSavedPicks", "findSaved", "addResult"]
    - path: "src/worker/storage-writer.ts"
      provides: "AI 사용량 예약(+1)과 결과 저장을 기존 순차 큐로"
      contains: "recordAiCall"
    - path: "src/worker/ai/gateway.ts"
      provides: "키 → 저장된 결과 → 한도 예약 → 요청 → 검사 → 저장 흐름과 취소"
      contains: "findSaved"
    - path: "tests/e2e/ai-saved-limit.e2e.ts"
      provides: "저장 재사용·낡음·한도·달 바뀜·F 키 요청 0개 e2e"
    - path: "tests/e2e/ai-cancel.e2e.ts"
      provides: "0.3초 로딩 표시와 Esc 취소(iframe 초점 포함) e2e"
  key_links:
    - from: "src/worker/ai/gateway.ts"
      to: "src/worker/storage-writer.ts"
      via: "보내기 전에 writer.recordAiCall(month, limit), 성공 뒤 writer.saveAiResult(page, picks)"
      pattern: "recordAiCall"
    - from: "src/worker/ai/gateway.ts"
      to: "src/core/ai/ai-storage.ts"
      via: "pageKeyOf(ctx.url) → findSaved(results, page, items)"
      pattern: "findSaved"
    - from: "src/entrypoints/content.ts"
      to: "src/worker/ai/gateway.ts"
      via: "Esc → ai/cleanup-cancel { requestId } → background → gateway.cancel(tabId, requestId)"
      pattern: "ai/cleanup-cancel"
    - from: "src/worker/relay.ts"
      to: "src/entrypoints/content.ts"
      via: "ai/cleanup-state { pending }를 탭의 모든 프레임에 방송 — 자식 프레임이 Esc를 hints/key로 넘김"
      pattern: "ai/cleanup-state"
---

## Phase Goal

**As a** 이용자, **I want to** 처음 보는 복잡한 화면에서 명령판의 "화면 정리"를 누르면 중요한 요소 9개에 번호가 붙기를, **so that** AI가 없거나 실패해도 나머지 기능은 그대로이고, 개인정보와 입력 값은 밖으로 나가지 않는다.

(ROADMAP Phase 6 **Goal** 문장을 그대로 세 칸에 옮긴 것이다. 새 내용은 더하지 않았다. `/gsd-mvp-phase 6`으로 다듬을 수 있다.)

<objective>
화면 정리 결과를 이 PC에 저장해 같은 화면에서는 AI를 다시 부르지 않고(AI-03), 이용자가 정한 한 달 한도를 넘으면 AI를 부르지 않고 알리며(AI-04), 요청이 길어지면 로딩 표시와 Esc 취소를 준다(D-22).

Purpose: D-02·D-03·D-05·D-14·D-15·D-16·D-22·D-27(저장 열쇠·사용량·한도·형식 단위 시험)·D-28(같은 화면 다시 → AI 안 부름, 한도 초과 대체).
Output: `ai-storage.ts`의 저장 형식과 순수 함수, 단일 저장자의 AI 쓰기 두 가지, 창구 흐름 완성과 취소, 로딩 표시, e2e 2개.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai/06-CONTEXT.md
@.planning/phases/06-ai/06-RESEARCH.md
@.planning/phases/06-ai/06-PATTERNS.md
@.planning/phases/06-ai/06-01-SUMMARY.md
</context>

## 앞선 단계 의존

- 06-01 완료: `gateway.ts`(`createAiGateway`, `handleCleanup`), `client.ts`(`requestCleanupPicks`, signal 받음), `ai-storage.ts`(`AI_CONFIG_KEY`, `AiConfigV1`), `cleanup-copy.ts`, `content.ts`의 `runCleanup`·`collectComposed`, `messages.ts`의 `ai/cleanup-request`·`CleanupResultSchema`, `fake-anthropic.ts`(`delayMs` 각본 포함).
- Phase 1: `src/worker/storage-writer.ts`의 `enqueue` 순차 큐와 "검사 실패 시 원본 보존" 방식(01-14 결과 가정), `src/core/fingerprint.ts`의 `isSameElement`, `src/worker/relay.ts`의 `hints/state` 방송·`hints/key` 전달, 입력 파이프라인의 취소 키(`keymap.cancel`, 기본 `Escape`).
- 이 계획은 06-02와 같은 wave에서 병렬로 돈다. `ai-text.ts`, `request-builder.ts`, `mask.ts`는 고치지 않는다(06-02 소유).

## 사용자 확인 대기 선택 (격리)

두 가지는 이용자 답을 기다리는 중이라 기본 제안으로 계획했다(06-CONTEXT "Claude's Discretion"). 답이 다르면 아래 자리만 바꾼다.

1. **한 달 한도의 단위 — 계획: 호출 횟수.** 보낸 요청 하나 = 1. 금액으로 바뀌면 바뀌는 곳: 이 계획 Task 1(`src/core/ai/ai-storage.ts`의 `AiUsageV1` 필드와 `isOverLimit`), 이 계획 Task 2(`src/worker/ai/gateway.ts`의 기록 시점 — 보내기 전 예약 대신 응답의 사용 토큰 × 가격을 응답 뒤 기록, `src/worker/storage-writer.ts`의 `recordAiCall` 인자), 06-04 Task 1(`src/worker/ai/client.ts`가 `usage` 토큰을 돌려줌), 06-05 Task 2(`src/entrypoints/options/ai-section.ts`의 한도 칸 이름·단위·입력 범위와 사용량 문장), `COVERAGE.md`의 `response usage tokens` 행(OPT-OUT → INTEGRATE).
2. **저장된 AI 번호를 쓰는 때 — 계획: "화면 정리"를 다시 누를 때만.** 평소 번호표(F)는 Phase 1 순서 그대로다. 평소 번호표에도 쓰기로 바뀌면 바뀌는 곳: 이 계획 Task 2 — `src/entrypoints/content.ts`의 `openHints()`가 SW에 저장된 번호를 묻는 새 메시지(요청 없이 저장만 읽음)와 그 처리(`src/shared/messages.ts`, `src/entrypoints/background.ts`, `src/worker/ai/gateway.ts`), 그리고 `tests/e2e/ai-saved-limit.e2e.ts`의 "F 키 → 저장된 번호" 경우. AI를 부르지 않는다는 점은 그대로다.

## 실행 규칙 (모든 작업 공통)

- 패키지 매니저는 pnpm만. TypeScript strict, `any` 금지, 시크릿을 코드·커밋에 넣지 않는다(CLAUDE.md). 새 패키지·권한 없음.
- 구현 전에 Superpowers `test-driven-development`를, 시험 실패를 쫓기 전에 `systematic-debugging`을, "완료" 전에 `verification-before-completion`을 **호출**한다. 서브에이전트에 맡기면 프롬프트에 세 스킬과 `model`을 적는다.
- RED 커밋 `test(06-03): …` → GREEN 커밋 `feat(06-03): …`. 제목은 영어 접두어, 본문은 한국어.
- 완료 판정은 `CI=true` e2e다. 자동 시험은 진짜 Anthropic API를 부르지 않고 시험 키는 `FAKE_AI_KEY`뿐이다(D-28).
- 저장은 SW의 단일 저장자만 한다(D-03). content·옵션 페이지가 `ai-usage:*`·`ai-results`를 직접 쓰지 않는다.
- 실제 회사 시스템에 접속하거나 시험하지 않는다(D-29).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 저장 열쇠·한 달 사용량·저장된 결과 형식과 맞추기 (순수 함수)</name>
  <reversibility rating="costly">`ai-usage:YYYY-MM`·`ai-results` 키 이름과 schemaVersion 1 형식은 이후 버전이 읽는다. 형식 버전으로 바꿀 수 있지만 읽는 곳(창구·설정 화면)을 함께 바꿔야 한다.</reversibility>
  <files>src/core/ai/ai-storage.ts, tests/unit/ai-storage.test.ts</files>
  <read_first>
    - src/core/ai/ai-storage.ts (06-01 결과), src/core/settings-schema.ts (FingerprintSchema, 형식 버전 스타일)
    - src/core/fingerprint.ts (isSameElement)
    - .planning/phases/06-ai/06-CONTEXT.md (D-03, D-14, D-15, D-16, D-27, discretion "페이지 주소 열쇠", "저장된 요소가 안 보일 때")
    - .planning/phases/06-ai/06-RESEARCH.md (Pitfall 4, Pitfall 5)
  </read_first>
  <behavior>
    - `pageKeyOf('http://practice.test/a/b?token=x#/list')` = `'http://practice.test/a/b#/list'`(쿼리 버림, 해시 유지); 경로가 다르면 다른 열쇠; `http`·`https`가 아니거나 주소가 틀리면 null
    - `monthKeyOf(new Date(2026, 8, 30, 23, 59))` = `'2026-09'`, `new Date(2026, 9, 1, 0, 0)` = `'2026-10'`, `new Date(2027, 0, 5)` = `'2027-01'`(이 PC 시간대)
    - `aiUsageKey('2026-09')` = `'ai-usage:2026-09'`; `isOverLimit(99, 100)` false, `isOverLimit(100, 100)` true, `isOverLimit(0, 0)` true
    - `AiUsageV1`은 음수·소수 count를, `AiResultsV1`은 picks 0개·10개와 entries 201개를, `AiConfigV1`은 한도 10001·음수를 거부한다
    - `matchSavedPicks`: 저장된 9개가 모두 맞으면 저장 순서대로 지금 요소 열쇠 9개; 5개 맞으면 맞은 5개의 열쇠; 4개만 맞으면 null(낡음); 한 요소를 두 번 쓰지 않는다
    - `findSaved`: 같은 페이지 열쇠에 화면이 다른 저장 두 개가 있으면 지금 요소와 맞는 쪽을 고른다; 다른 페이지면 null
    - `addResult`: 새 저장을 맨 앞에 넣고, 같은 페이지는 5개, 전체는 200개까지 남기며 오래된 것부터 버린다
  </behavior>
  <action>
behavior 경우로 `tests/unit/ai-storage.test.ts`를 먼저 쓰고 RED 커밋. 그다음 `src/core/ai/ai-storage.ts`에 더한다(GREEN):
- `pageKeyOf(url)`: `origin + pathname + hash`, 쿼리는 버린다(세션 토큰 같은 값이 저장 열쇠에 남지 않게, RESEARCH Pitfall 4). 가리지 않는다 — 이 PC의 storage.local에만 있고 밖으로 나가지 않는다(D-14).
- `monthKeyOf(date)`: 이 PC 달력 기준 `YYYY-MM`(`getFullYear`·`getMonth`, UTC 변환 없음).
- `aiUsageKey(month)` = `'ai-usage:' + month`, `AiUsageV1` = zod `{ schemaVersion: 1, data: { count: 0 이상 정수 } }`, `isOverLimit(count, limit)` = `count >= limit`(한도 0이면 AI를 부르지 않는다).
- `AI_RESULTS_KEY = 'ai-results'`, `AiResultsV1` = zod `{ schemaVersion: 1, data: { entries: 최대 200개 [{ page: string, savedAt: number, picks: FingerprintSchema 1~9개 }] } }`. 요소는 Phase 1의 요소 식별 묶음으로 기억한다(D-15).
- `matchSavedPicks(picks, items)`: `items`는 `{ key, fingerprint }` 목록. 저장 순서대로 아직 안 쓴 요소 중 `isSameElement`가 참인 첫 요소의 열쇠를 모은다. 맞은 수가 저장 수의 절반(올림) 미만이면 null.
- `findSaved(results, page, items)`: `page`가 같은 저장들 중 `matchSavedPicks`가 null이 아니고 맞은 수가 가장 많은 것의 열쇠 목록, 없으면 null. 한 주소에 여러 화면을 두는 이유: 회사 시스템처럼 맨 위 주소는 같고 본문이 iframe 안에서 바뀌는 경우(06-CONTEXT discretion).
- `addResult(results, entry)`: 맨 앞에 넣고 같은 페이지 5개·전체 200개 상한.
세 저장 값 모두 `chrome.storage.local` 전용이다(D-14, D-16).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/ai-storage.test.ts</automated>
    <fails_when>non-zero exit, or the Tests summary line reports fewer than 15 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/ai/ai-storage.ts`가 `pageKeyOf`, `monthKeyOf`, `aiUsageKey`, `AiUsageV1`, `isOverLimit`, `AI_RESULTS_KEY`, `AiResultsV1`, `matchSavedPicks`, `findSaved`, `addResult`를 export한다
    - `tests/unit/ai-storage.test.ts`에 쿼리 버림, 달 경계 셋, 한도 경계, 스키마 거부, 절반 기준(5 맞음/4 맞음), 한 주소 두 화면, 상한 경우가 있다
    - `pnpm exec vitest run tests/unit/ai-storage.test.ts`가 15개 이상 통과
  </acceptance_criteria>
  <done>저장 열쇠·사용량·한도·저장된 결과·낡음 판정이 단위 시험으로 고정됐다(D-27).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 창구 흐름 완성 — 키 → 저장된 결과 → 한도 예약 → 요청 → 검사 → 저장</name>
  <reversibility rating="costly">사용량을 세는 시점(보내기 전 예약)과 저장 쓰기는 이용자 PC에 쌓이는 값을 정한다. 단위가 금액으로 바뀌면 위 "사용자 확인 대기 선택" 1의 자리를 바꾼다.</reversibility>
  <files>src/worker/storage-writer.ts, src/worker/ai/gateway.ts, tests/e2e/fake-anthropic.ts, tests/e2e/ai-saved-limit.e2e.ts</files>
  <read_first>
    - src/worker/storage-writer.ts (enqueue, recordPress의 검사 실패 처리)
    - src/worker/ai/gateway.ts, tests/e2e/fake-anthropic.ts, tests/e2e/ai-cleanup.e2e.ts (06-01 결과)
    - tests/practice-site/ai-cleanup.html (06-01 결과 — 이 시험이 요소 묶음을 바꿔 끼운다)
    - .planning/phases/06-ai/06-PATTERNS.md ("단일 저장자 + 순차 큐", "판별 유니온 + 검사 실패 시 원본 보존")
  </read_first>
  <behavior>
    - 저장 재사용: 가짜 키로 카드를 누르면 요청 1개, 번호표를 닫고 다시 누르면 요청은 그대로 1개이고 세 요소(`numberForElement`)의 번호가 처음과 같다; 페이지를 다시 열고 누르면 요청은 여전히 1개(ROADMAP 성공 기준 1)
    - 사용량: 첫 요청 뒤 `ai-usage:<이번 달>`의 count가 1이고, 저장을 쓴 두 번째는 1 그대로다
    - 낡음과 한 주소 여러 화면: 저장 뒤 시험이 페이지의 버튼 묶음을 다른 글자·id로 바꿔 끼우고 누르면 요청 2개, 원래 묶음으로 되돌리고 누르면 요청은 2개 그대로이고 번호가 처음 저장과 같다
    - 한도: 한도 1로 두고 한 화면에서 누르면 요청 1개, 해시를 바꾼 다른 화면에서 누르면 새 요청 없이 "이번 달 AI 사용 한도를 넘었어요. 자주 누른 순서로 번호를 붙였어요"와 자주 누른 순서 번호표(미리 두 번 누른 "목록"이 1)
    - 달 바뀜: `ai-usage:<지난달>`만 한도만큼 채워 두면 요청이 나가고, `ai-usage:<이번 달>`을 한도만큼 채우면 나가지 않는다(달은 SW의 `new Date()`로 계산)
    - 읽을 수 없는 이번 달 사용량 값(형식 틀림)이 있으면 요청을 보내지 않고(한도 초과로 봄) 그 값을 덮어쓰지 않는다
    - F 키 번호표를 켜고, 페이지를 새로 열어도 요청은 0개(D-05)
  </behavior>
  <action>
behavior 경우로 `tests/e2e/ai-saved-limit.e2e.ts`를 먼저 쓰고 RED 커밋. `tests/e2e/fake-anthropic.ts`에 `setAiUsage(serviceWorker, month, value)`(시험 준비용 storage.local 쓰기 — 형식이 틀린 값도 넣을 수 있게 unknown을 받음)와 `readAiUsage(serviceWorker, month)`를 더한다. 버튼 묶음 바꿔 끼우기는 `page.evaluate`로 한다(연습 페이지 파일은 06-01 소유라 고치지 않는다).

`src/worker/storage-writer.ts`(D-03)에 기존 `enqueue`로 두 쓰기를 더한다:
- `recordAiCall(month, limit)` → `{ ok: true } | { ok: false, reason: 'over-limit' | 'invalid-usage' }`: 한 큐 작업 안에서 `ai-usage:<month>`를 읽어 `AiUsageV1` 검사 — 형식이 틀리면 쓰지 않고 `invalid-usage`, `isOverLimit`면 `over-limit`, 아니면 count+1을 쓴다. 읽기·판단·쓰기가 한 작업이라 동시에 누른 두 요청이 한도를 함께 넘지 못한다(RESEARCH Pitfall 5).
- `saveAiResult(entry)` → `{ ok: true } | { ok: false, reason: 'invalid-results' }`: `ai-results`를 읽어 `AiResultsV1` 검사 — 틀리면 쓰지 않고 원본을 둔다. 맞으면(없으면 빈 목록에서) `addResult`로 더해 쓴다.

`src/worker/ai/gateway.ts`의 `handleCleanup` 순서(D-14, D-16, D-17, D-20):
1. `ai-config` 검사 — 키 없음·형식 틀림 → `no-key`(06-01 그대로). 키를 먼저 보는 이유: 키가 없으면 화면 정리가 꺼진 것이고(D-17) 저장된 번호도 쓰지 않는다.
2. `pageKeyOf(ctx.url)`가 있고 `ai-results`가 형식에 맞으면 `findSaved(results, page, items)` — 열쇠 목록이 나오면 요청·사용량 없이 `{ kind: 'ai', itemKeys }`.
3. 보낼 목록이 비면(글자 있는 요소 0개) 요청 없이 `invalid-response`로 대체한다.
4. `writer.recordAiCall(monthKeyOf(new Date()), config.monthlyLimit)` — 실패면 `over-limit` 대체(형식 틀림도 한도 초과로 본다 — 사용량을 모를 때 부르지 않는 쪽).
5. 요청 → `judgeAiAnswer` → 성공이면 고른 요소의 `fingerprint`로 `writer.saveAiResult({ page, savedAt: Date.now(), picks })`(page가 null이면 저장하지 않음, 저장 실패는 결과에 영향 없음) → `{ kind: 'ai', itemKeys }`.
사용량은 **보낸 요청마다** 센다: 실패·목록 밖 답·취소도 요청이 나갔으면 1이다(한도는 비용을 막으려는 것이고 API는 나간 요청에 비용을 매긴다 — RESEARCH Pitfall 5의 "성공만 셈" 제안과 다른 선택, SUMMARY에 적는다). 저장된 결과를 쓰는 경로·키 없음·한도 초과는 세지 않는다. 낡은 저장 때문에 AI를 다시 부르는 것도 이용자가 카드를 눌렀을 때뿐이다(D-05).
GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-saved-limit.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report at least 6 passed, or the output contains "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-cleanup.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report "2 passed" (the 06-01 path regressed)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/worker/storage-writer.ts`가 `recordAiCall`과 `saveAiResult`를 `enqueue` 안에서 한다
    - `src/worker/ai/gateway.ts`가 `findSaved`를 `recordAiCall`보다 먼저, `recordAiCall`을 `requestCleanupPicks`보다 먼저 부른다
    - `tests/e2e/ai-saved-limit.e2e.ts`에 behavior 일곱 경우가 있고 모두 `blockedRequests`가 비어 있음을 단언한다
    - `CI=true pnpm exec playwright test tests/e2e/ai-saved-limit.e2e.ts`가 6개 이상 통과하고 `ai-cleanup.e2e.ts`가 여전히 2 passed
  </acceptance_criteria>
  <done>같은 화면에서는 저장된 번호를 요청 없이 다시 쓰고, 화면이 바뀌면 다시 부르며, 한 달 한도를 넘으면 부르지 않고 알린다. 사용량·결과는 단일 저장자 큐로만 쓰인다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 0.3초 로딩 표시와 Esc 취소 (iframe 초점 포함)</name>
  <reversibility rating="reversible">메시지 두 개와 content 상태 하나다. 저장 형식이 아니다.</reversibility>
  <files>src/core/ai/cleanup-copy.ts, src/entrypoints/content.ts, src/shared/messages.ts, src/worker/relay.ts, src/entrypoints/background.ts, src/worker/ai/gateway.ts, tests/e2e/ai-cancel.e2e.ts</files>
  <read_first>
    - src/entrypoints/content.ts (runCleanup, 취소 키 처리, hints/key 전달 조건)
    - src/worker/relay.ts (hints/state 방송, hints/key를 frameId 0으로 전달)
    - src/entrypoints/background.ts, src/shared/messages.ts, src/worker/ai/gateway.ts
    - docs/design/SYSTEM.md (상태 표 "화면 정리(AI)" 행)
    - .planning/phases/06-ai/06-CONTEXT.md (D-22, D-23, discretion "Esc 취소 때 요청을 끊는 방법")
  </read_first>
  <behavior>
    - 가짜 AI가 2초 늦게 답하면: 100ms 때 모드 표시에 "화면을 정리하는 중"이 없고, 600ms 때 있다; Esc를 누르면 그 표시가 사라지고, 3초가 지나도 번호표가 0개이며 대체 안내도 없다
    - 가짜 AI가 바로 답하면: 누른 뒤 번호표가 뜰 때까지 50ms 간격으로 읽은 모드 표시 글자에 "화면을 정리하는 중"이 한 번도 없다
    - 초점이 iframe 안 입력칸이 아닌 요소(iframe 본문 클릭)에 있을 때 Esc도 같은 취소가 된다
    - 취소한 뒤 카드를 다시 누르면 새 요청이 나가고 번호표 9개가 붙는다
    - 취소한 요청도 이미 보냈으면 사용량 1로 센다(Task 2 규칙)
  </behavior>
  <action>
behavior 경우로 `tests/e2e/ai-cancel.e2e.ts`를 먼저 쓰고 RED 커밋. 모드 표시 글자는 hints.e2e.ts의 `indicatorText` 방식으로 읽는다.

- `src/core/ai/cleanup-copy.ts`: `AI_LOADING_HINT = '화면을 정리하는 중 · Esc 취소'`(D-22, 키 이름은 영어 `Esc`, D-23).
- `src/shared/messages.ts`: `ai/cleanup-cancel { requestId }`(맨 위 프레임 → SW), `ai/cleanup-state { pending: boolean }`(맨 위 프레임 → SW → 탭의 모든 프레임).
- `src/worker/relay.ts`: `ai/cleanup-state`를 `hints/state`와 같은 방식으로 보낸 탭의 모든 프레임에 방송한다(맨 위 프레임에서 온 것만).
- `src/entrypoints/background.ts`: `ai/cleanup-cancel`은 `sender.frameId === 0`일 때 `gateway.cancel(sender.tab.id, requestId)`로, `ai/cleanup-state`는 relay로 넘긴다.
- `src/worker/ai/gateway.ts`: 진행 중 요청을 `탭 id + requestId` 열쇠의 `AbortController` 표로 들고, `cancel(tabId, requestId)`이 abort한다(다른 탭이 끊지 못함). 신호는 `requestCleanupPicks`의 `signal`로 간다. 보내기 전에 이미 취소됐으면 사용량 예약 없이 `canceled`로 끝낸다. 끝나면 표에서 지운다.
- `src/entrypoints/content.ts`(맨 위 프레임): `runCleanup`이 요청을 보낸 뒤 300ms 타이머를 걸어 그때도 진행 중이면 `setHint(AI_LOADING_HINT)`, 답이 오면 타이머를 지우고 06-01 흐름대로 표시를 바꾼다. 진행 중에 이용자의 진짜 취소 키(`keymap.cancel`, 기본 `Escape`)가 오면 — 맨 위 프레임에서 직접이든 자식 프레임이 `hints/key`로 넘긴 것이든 — 다른 Esc 처리보다 먼저 `ai/cleanup-cancel`을 보내고, 표시를 지우고, 이 요청의 늦게 온 답(성공이어도)은 버린다. 진행 시작·끝에 `ai/cleanup-state`를 보낸다. 자식 프레임은 `ai/cleanup-state`로 진행 중임을 알면 진짜 취소 키를 삼켜 `hints/key`로 맨 위에 넘긴다(번호표가 열렸을 때와 같은 길). 진행 중이 아닐 때 Esc 동작은 지금과 같다.
GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-cancel.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report at least 4 passed, or the output contains "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/ai-cleanup.e2e.ts tests/e2e/ai-saved-limit.e2e.ts tests/e2e/hints.e2e.ts tests/e2e/frames.e2e.ts</automated>
    <fails_when>non-zero exit, or any test in the summary is reported as failed (Esc or hint routing regressed)</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/ai/cleanup-copy.ts`에 `'화면을 정리하는 중 · Esc 취소'`가 있다
    - `src/shared/messages.ts`에 `'ai/cleanup-cancel'`과 `'ai/cleanup-state'`가 있다
    - `src/worker/ai/gateway.ts`가 `AbortController`를 탭 id와 requestId로 찾는다
    - `CI=true pnpm exec playwright test tests/e2e/ai-cancel.e2e.ts`가 4개 이상 통과하고, 기존 hints·frames e2e가 그대로 통과한다
  </acceptance_criteria>
  <done>0.3초 넘는 요청에만 로딩 표시가 나오고, Esc(초점이 iframe 안이어도)로 요청을 끊으면 번호표·안내 없이 원래대로 돌아간다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- AI-03 (주 계획): edge probe `unclassified — review manually`. 가정: "같은 페이지 주소"는 `origin + pathname + hash`(쿼리 버림)이고, 같은 주소 안에서도 저장된 요소가 절반 미만만 맞으면 다른 화면으로 보고 다시 부른다(이용자가 카드를 눌렀을 때만). 한 주소에 화면 5개, 전체 200개까지 기억한다. 저장된 번호는 "화면 정리"를 다시 누를 때만 쓴다(사용자 확인 대기 선택 2). 요소 식별 묶음이 비슷한 요소가 많은 화면(글자만 있는 같은 링크 여러 개)에서는 저장된 번호가 다른 요소로 맞춰지거나 맞지 않을 수 있다 — Phase 1 `isSameElement`의 한계로 SUMMARY에 적는다.
- AI-04 (주 계획): edge probe `unclassified — review manually`. 가정: 한도 단위는 보낸 요청 수(사용자 확인 대기 선택 1), 기본 한도 100번, 한 달은 이 PC 달력의 달, 한도 0은 "AI를 쓰지 않음", 사용량을 읽을 수 없으면 한도 초과로 본다. 한도 초과 알림은 누를 때마다 보인다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage.local → service worker | 옛 버전·손상된 사용량·결과 값이 들어올 수 있다 |
| content script(모든 프레임) → service worker | 취소·상태 메시지를 보낸다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Denial of Service | 한 달 한도(비용) | medium | mitigate | 읽기·판단·+1을 한 큐 작업으로(동시 요청이 한도를 함께 못 넘음), 보낸 요청마다 셈, 사용량을 읽을 수 없으면 부르지 않음 |
| T-06-11 | Tampering | `ai-usage:*`·`ai-results` 손상 값 | medium | mitigate | zod 검사 실패 시 덮어쓰지 않고 원본 보존(D-03), 결과 저장 실패는 화면 정리 결과에 영향 없음 |
| T-06-12 | Spoofing | `ai/cleanup-cancel`·`ai/cleanup-state` | low | mitigate | `sender.id` 확인, 맨 위 프레임(frameId 0)에서 온 것만, 취소 열쇠에 `sender.tab.id` 포함 |
| T-06-13 | Information Disclosure | 저장 열쇠의 페이지 주소 | low | accept | storage.local(이 PC)에만 있고 밖으로 나가지 않는다(D-14). 쿼리(토큰 자리)는 버린다 |
| T-06-14 | Elevation of Privilege | 사이트가 만든 가짜 키로 AI 호출 | medium | mitigate | 카드·Esc 모두 입력 파이프라인의 isTrusted 키만(D-02), 저장 낡음·페이지 열기로는 부르지 않음(e2e 요청 0개 확인) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/ai-storage.test.ts` 통과
- `CI=true pnpm exec playwright test tests/e2e/ai-saved-limit.e2e.ts tests/e2e/ai-cancel.e2e.ts tests/e2e/ai-cleanup.e2e.ts` 통과
- `CI=true pnpm exec playwright test tests/e2e/hints.e2e.ts tests/e2e/frames.e2e.ts` 통과(Esc·번호표 전달 회귀 없음)
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 같은 화면 재사용·다시 열기·화면 바뀜·한도·달 바뀜·F 키 요청 0개가 CI=true e2e로 확인됐다
- 로딩 표시 0.3초 규칙과 Esc 취소(iframe 초점 포함)가 확인됐다
- 작업마다 RED(`test(06-03)`)·GREEN(`feat(06-03)`) 커밋이 있다
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/unit/ai-storage.test.ts`, `tests/e2e/ai-saved-limit.e2e.ts`, `tests/e2e/ai-cancel.e2e.ts`
- 심볼(`src/core/ai/ai-storage.ts`): `pageKeyOf`, `monthKeyOf`, `aiUsageKey`, `AiUsageV1`, `isOverLimit`, `AI_RESULTS_KEY`, `AiResultsV1`, `matchSavedPicks`, `findSaved`, `addResult`
- 심볼: `StorageWriter.recordAiCall`, `StorageWriter.saveAiResult`, `gateway.cancel`, `AI_LOADING_HINT`, 시험 도구 `setAiUsage`·`readAiUsage`
- 메시지: `ai/cleanup-cancel`, `ai/cleanup-state`
- 저장 키: `ai-usage:YYYY-MM`, `ai-results`(storage.local, schemaVersion 1)

<output>
Create `.planning/phases/06-ai/06-03-SUMMARY.md` when done (사용량을 세는 시점과 그 이유, 저장 열쇠 규칙, 요소 식별 한계 관찰 포함)
</output>
