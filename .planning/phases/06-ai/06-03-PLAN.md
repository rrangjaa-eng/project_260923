---
phase: 06-ai
plan: 03
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/page/collector/ai-text.ts
  - tests/unit/ai-text.test.ts
  - src/page/collector/collector.ts
  - src/core/frame-tree.ts
  - tests/unit/frame-tree.test.ts
  - src/shared/messages.ts
autonomous: true
requirements: [AI-01, AI-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "글자를 쓰는 칸(input·textarea·contenteditable·select, role textbox·combobox·searchbox·spinbutton)의 AI용 글자는 라벨 → aria-label → `placeholder` 속성 중 처음 있는 것뿐이고, 칸의 값·선택된 option 글자·안 글자는 읽지 않는다. 그 밖의 요소는 수집기가 이미 만든 보이는 이름이다(D-09, D-10)"
    - "모든 프레임(iframe 포함)의 요소 보고에 요소 종류와 AI용 글자가 실려 맨 위 프레임의 `composeTree` 결과까지 그대로 온다(D-01, D-06)"
    - "요소 보고의 새 필드는 길이 상한(종류 40자, AI용 글자 200자)을 지키고, 필드가 없는 옛 보고도 그대로 받아들여진다(D-03, D-09)"
  prohibitions:
    - statement: "AI용 글자를 만드는 코드가 입력칸의 값·선택된 option 글자·contenteditable 안 글자를 읽지 않는다 — 글자 칸에서는 수집기의 보이는 이름도 쓰지 않는다(D-10)"
      requirement_id: AI-02
      category: security
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/page/collector/ai-text.ts"
      provides: "요소 하나의 AI용 글자 — 글자 칸이면 라벨·aria·`placeholder` 속성만, 값은 읽지 않음(가림·민감칸 처리는 06-05가 더한다)"
      exports: ["aiTextOf"]
    - path: "src/core/frame-tree.ts"
      provides: "FrameReport·ComposedItem의 선택 필드 kind·aiText를 composeTree가 그대로 옮김"
      contains: "aiText"
    - path: "tests/unit/ai-text.test.ts"
      provides: "AI용 글자 규칙 단위 시험(happy-dom, 06-05가 가림·민감칸 경우를 더한다)"
  key_links:
    - from: "src/page/collector/collector.ts"
      to: "src/page/collector/ai-text.ts"
      via: "요소를 모을 때 aiTextOf(el, { name, labelText })로 Item.aiText를 만들고 buildFrameReport가 kind·aiText를 싣는다"
      pattern: "aiTextOf"
    - from: "src/core/frame-tree.ts"
      to: "src/shared/messages.ts"
      via: "자식 프레임 보고는 FrameReportSchema(kind ≤40, aiText ≤200, 둘 다 선택)를 거쳐 composeTree로 온다"
      pattern: "aiText"
---

## Phase Goal

**As a** 이용자, **I want to** 처음 보는 복잡한 화면에서 명령판의 "화면 정리"를 누르면 중요한 요소 9개에 번호가 붙기를, **so that** AI가 없거나 실패해도 나머지 기능은 그대로이고, 개인정보와 입력 값은 밖으로 나가지 않는다.

(ROADMAP Phase 6 **Goal** 문장을 그대로 세 칸에 옮긴 것이다. 새 내용은 더하지 않았다. `/gsd-mvp-phase 6`으로 다듬을 수 있다.)

<objective>
모든 프레임의 요소 수집기가 요소마다 "종류"와 "AI용 글자"를 만들어 프레임 보고에 싣고, 맨 위 프레임이 합친 목록(`ComposedItem`)까지 그대로 오게 한다. AI용 글자는 입력칸의 값을 읽지 않는 규칙으로 만든다. 06-04 Task 1이 이 글자를 AI 창구로 보낸다.

Purpose: D-01(iframe 안 요소 포함)·D-06(종류·보이는 글자)·D-09·D-10(값을 보내지 않음), AI-02의 "보내는 것은 종류·보이는 글자뿐".
Output: `src/page/collector/ai-text.ts`, 수집기·프레임 합성·프레임 보고 메시지의 `kind`·`aiText`, 단위 시험 2개.

재분할 메모(계획 점검 반영): 원래 06-01 tracer 안에 있던 "요소 보고의 종류·AI용 글자"를 이 계획으로 옮겼다. 06-02와 같은 wave에서 병렬로 돈다(파일이 겹치지 않는다).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai/06-CONTEXT.md
@.planning/phases/06-ai/06-RESEARCH.md
@.planning/phases/06-ai/06-PATTERNS.md
</context>

## 앞선 단계 의존

- 06-01 완료: 승인 체크포인트가 먼저 끝났고, `src/shared/messages.ts`에 `ai/cleanup-request`가 있다(이 계획도 `messages.ts`를 고치므로 06-01 뒤에 돈다).
- Phase 1: `src/page/collector/collector.ts`(`Item`, `computeName`, `kindOf`, `labelTextOf`, `buildFrameReport`, `createCollector`), `src/core/frame-tree.ts`(`RawFrameReport`, `FrameReport`, `ComposedItem`, `resolveReports`, `composeTree`), `src/shared/messages.ts`(`FrameReportSchema`), 기존 happy-dom 단위 시험의 파일 머리 설정.
- 이 계획은 06-02와 같은 wave에서 병렬로 돈다. `response.ts`, `request-builder.ts`, `hint-order.ts`는 고치지 않는다(06-02 소유).

## 실행 규칙 (모든 작업 공통)

- 패키지 매니저는 pnpm만. TypeScript strict, `any` 금지, 시크릿을 코드·커밋에 넣지 않는다(CLAUDE.md). 새 패키지 없음.
- 구현 전에 Superpowers `test-driven-development`를, 시험 실패를 쫓기 전에 `systematic-debugging`을, "완료" 전에 `verification-before-completion`을 **호출**한다. 서브에이전트에 맡기면 프롬프트에 세 스킬과 `model`을 적는다(점검·판단은 Opus 5).
- 각 작업은 RED 커밋 `test(06-03): …` → GREEN 커밋 `feat(06-03): …` → 필요하면 `refactor(06-03): …`. 제목은 영어 접두어, 본문은 한국어.
- 완료 판정에는 Phase 1 e2e 회귀 확인(`CI=true`)을 포함한다 — 수집기와 프레임 합성은 번호표 전체가 쓴다.
- 실제 회사 시스템에 접속하거나 시험하지 않는다(D-29).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 요소 하나의 AI용 글자 `aiTextOf` — 값을 읽지 않는 규칙</name>
  <reversibility rating="reversible">content 쪽 순수 DOM 읽기 함수 하나다. 저장 형식이 아니다.</reversibility>
  <files>src/page/collector/ai-text.ts, tests/unit/ai-text.test.ts</files>
  <read_first>
    - src/page/collector/collector.ts (computeName, kindOf, labelTextOf, ariaOf, textOrUndefined)
    - tests/unit/ 안의 기존 happy-dom 단위 시험 하나(파일 머리 환경 설정 방식)
    - .planning/phases/06-ai/06-CONTEXT.md (D-09, D-10)
    - .planning/phases/06-ai/06-PATTERNS.md (`ai-text`·수집기 절)
  </read_first>
  <behavior>
    - 라벨 "이메일", 값 `hong@example.com`인 입력칸 → `'이메일'`
    - 라벨·aria 없이 `placeholder` 속성 "검색어"인 입력칸(값 있음) → `'검색어'`
    - 라벨·aria·`placeholder` 속성이 없는 textarea(글자 있음) → `''`
    - 글자가 든 contenteditable(aria-label "메모") → `'메모'`; aria도 없으면 `''`
    - 선택된 option이 있는 select(라벨 "부서") → `'부서'`
    - role=textbox·combobox·searchbox·spinbutton인 요소도 같은 규칙
    - 버튼·링크처럼 글자 칸이 아닌 요소는 넘겨받은 보이는 이름을 돌려준다; `type=submit`·`button`·`checkbox`인 input도 글자 칸이 아니다
    - 결과는 앞뒤 공백을 뺀 200자 이하다
  </behavior>
  <action>
behavior 경우로 `tests/unit/ai-text.test.ts`(happy-dom 환경, 기존 happy-dom 단위 시험의 파일 머리 설정을 따른다)를 먼저 쓰고 RED 커밋. 그다음 `src/page/collector/ai-text.ts`에 `aiTextOf(el: Element, known: { name: string; labelText: string | undefined }): string`을 만든다(GREEN, D-09·D-10):
- 글자 칸 판정: 태그 textarea·select, 글자를 쓰는 input(`type` 속성이 button·submit·reset·checkbox·radio·image·file·range·color·hidden이 아닌 것), contenteditable 요소(`isContentEditable`), role textbox·combobox·searchbox·spinbutton.
- 글자 칸이면 `known.labelText`(수집기의 `labelTextOf`가 이미 만든 라벨 글자) → `aria-label` 속성 → `placeholder` 속성 중 처음 있는 것, 없으면 빈 문자열. 글자 칸에서는 `known.name`(보이는 이름)도 쓰지 않는다 — contenteditable·textarea의 이름이 안 글자일 수 있다.
- 그 밖의 요소는 `known.name`.
- 결과는 앞뒤 공백을 빼고 200자로 자른다.
이 파일에는 칸의 값·선택된 option·요소 안 글자를 읽는 속성 접근이 하나도 없어야 한다 — 라벨 글자는 수집기가 넘겨주는 `labelText`로만 받는다(verify의 grep이 확인하고, 06-05도 같은 grep을 쓴다). 가림(`maskPii`)과 민감칸 처리(Phase 3 `isSensitive`, D-11)는 06-05 Task 2가 이 함수에 더한다.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/ai-text.test.ts</automated>
    <fails_when>non-zero exit, or the output reports "No test files found", or the Tests summary line reports fewer than 9 passed</fails_when>
    <automated>! grep -nE '\.value\b|selectedOptions|innerText|textContent' src/page/collector/ai-text.ts</automated>
    <fails_when>non-zero exit because grep printed a line — ai-text.ts reads a field value, selected option, or inner text</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/collector/ai-text.ts`가 `aiTextOf`를 export한다
    - `ai-text.ts`에 값·선택 항목·안 글자를 읽는 코드가 없다(verify의 grep)
    - `tests/unit/ai-text.test.ts`에 behavior 경우가 모두 있고 9개 이상 통과한다
  </acceptance_criteria>
  <done>요소 하나의 AI용 글자가 값을 읽지 않고 라벨·aria·`placeholder` 속성·보이는 이름만으로 만들어진다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 요소 보고에 종류·AI용 글자를 싣고 맨 위 프레임까지 옮기기 (iframe 보고 포함)</name>
  <reversibility rating="reversible">프레임 보고에 선택 필드 두 개를 더한다. 필드가 없는 옛 보고도 받아들이므로 되돌려도 다른 기능이 깨지지 않는다.</reversibility>
  <files>src/page/collector/collector.ts, src/core/frame-tree.ts, tests/unit/frame-tree.test.ts, src/shared/messages.ts</files>
  <read_first>
    - src/page/collector/collector.ts (Item, createCollector 안의 Item 만들기, buildFrameReport)
    - src/core/frame-tree.ts (RawFrameReport, FrameReport, ComposedItem, resolveReports, composeTree), tests/unit/frame-tree.test.ts
    - src/shared/messages.ts (FrameReportSchema, frame/report·frames/reports)
    - src/page/collector/ai-text.ts (Task 1 결과)
  </read_first>
  <behavior>
    - 단위(frame-tree): 맨 위 보고와 자식 프레임 보고의 항목에 든 `kind`·`aiText`가 `resolveReports` → `composeTree` 결과 `ComposedItem`에 그대로 실린다(자식 프레임 항목도)
    - 단위(frame-tree): `kind`·`aiText`가 없는 보고 항목은 `ComposedItem`에서도 없다(옛 보고 호환)
    - 기존 frame-tree 경우가 그대로 통과한다
  </behavior>
  <action>
behavior 경우를 `tests/unit/frame-tree.test.ts`에 더하고 RED 커밋. 그다음(GREEN, D-01·D-06):
- `src/page/collector/collector.ts`: `Item`에 `aiText: string`을 더하고, 요소를 모을 때 `aiTextOf(el, { name, labelText: 그 요소의 식별 묶음 labelText })`로 채운다. `buildFrameReport`가 보고 항목에 `kind`(기존 `kindOf` 값, 40자까지)와 `aiText`를 싣는다.
- `src/core/frame-tree.ts`: `RawFrameReport`·`FrameReport`의 items와 `ComposedItem`에 선택 필드 `kind?: string`·`aiText?: string`을 더하고, `resolveReports`·`composeTree`가 그대로 옮기게 한다(좌표·잘림 계산은 그대로).
- `src/shared/messages.ts`: `FrameReportSchema` 항목에 `kind: z.string().max(40).optional()`, `aiText: z.string().max(200).optional()`을 더한다(자식 프레임 보고가 SW를 거쳐 올 때 새 필드가 떨어지지 않게).
content의 `runCleanup`이 이 필드를 요청에 싣는 일은 06-04 Task 1이 한다. 이 작업은 번호표 동작을 바꾸지 않는다.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/frame-tree.test.ts tests/unit/ai-text.test.ts</automated>
    <fails_when>non-zero exit, or any test in the summary is reported as failed (including the pre-existing frame-tree cases)</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/hints.e2e.ts tests/e2e/frames.e2e.ts tests/e2e/ai-cleanup.e2e.ts</automated>
    <fails_when>non-zero exit, or any test in the summary is reported as failed (collector or frame composition regressed)</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit, or any "@typescript-eslint/no-explicit-any" error in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/collector/collector.ts`가 `aiTextOf`를 부르고 `buildFrameReport`의 항목에 `kind`·`aiText`가 있다
    - `src/core/frame-tree.ts`의 `ComposedItem`에 `aiText?`와 `kind?`가 있다
    - `src/shared/messages.ts`의 `FrameReportSchema` 항목에 `aiText: z.string().max(200).optional()`이 있다
    - frame-tree 단위 시험과 Phase 1 hints·frames e2e, 06-01의 ai-cleanup e2e가 그대로 통과한다
  </acceptance_criteria>
  <done>모든 프레임의 요소 보고가 종류와 값 없는 AI용 글자를 맨 위 프레임까지 싣고 오며, 번호표 동작은 그대로다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- AI-02 (일부 — 보이는 글자의 출처): edge probe `unclassified — review manually`. 가정: "보이는 글자"는 수집기의 보이는 이름이고, 글자 칸은 값 대신 라벨·aria·`placeholder` 속성을 쓴다. 가림과 민감칸 처리, 보낸 요청 본문 확인은 06-05가 맡는다.
- AI-01 (일부 — iframe): 가정: iframe 안 요소는 Phase 1의 프레임 보고 경로로 맨 위 프레임에 오므로 같은 경로에 필드만 더한다. 교차 출처 iframe도 같다(각 프레임의 수집기가 자기 요소의 글자를 만든다).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 페이지 DOM → content script(각 프레임) | 사이트가 입력칸 값·요소 글자에 개인정보를 둘 수 있다 |
| 자식 프레임 content script → service worker → 맨 위 프레임 | 다른 출처 iframe 안에서 도는 수집기가 보고를 보낸다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-31 | Information Disclosure | `aiTextOf`의 글자 칸 처리 | high | mitigate | 글자 칸은 수집기가 넘긴 라벨 글자·aria·`placeholder` 속성만, 값·선택 항목·안 글자를 읽는 코드가 없음을 grep으로 확인(D-10). 06-05가 가림·민감칸 처리와 요청 본문 e2e를 더한다 |
| T-06-32 | Tampering | 자식 프레임 보고의 `kind`·`aiText` | low | mitigate | zod 길이 상한(40·200자), 선택 필드라 옛 보고 호환. 보고 글자는 AI에 자료로만 가고(T-06-03) 답은 보낸 임시 번호만 받아들인다(06-02) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/ai-text.test.ts tests/unit/frame-tree.test.ts` 통과
- `CI=true pnpm exec playwright test tests/e2e/hints.e2e.ts tests/e2e/frames.e2e.ts tests/e2e/ai-cleanup.e2e.ts` 통과
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- AI용 글자 규칙과 프레임 보고의 새 필드가 단위 시험으로 고정됐다
- 번호표·프레임 합성의 기존 e2e가 그대로 통과한다
- 작업마다 RED(`test(06-03)`)·GREEN(`feat(06-03)`) 커밋이 있다
</success_criteria>

## Artifacts this phase produces

- 파일: `src/page/collector/ai-text.ts`, `tests/unit/ai-text.test.ts`
- 심볼: `aiTextOf`, `Item.aiText`, `FrameReport`·`ComposedItem`의 `kind?`·`aiText?`, `FrameReportSchema` 항목의 `kind`·`aiText`

<output>
Create `.planning/phases/06-ai/06-03-SUMMARY.md` when done (글자 칸 판정 목록, 라벨 글자를 받는 방법, 옛 보고 호환 확인 포함)
</output>
