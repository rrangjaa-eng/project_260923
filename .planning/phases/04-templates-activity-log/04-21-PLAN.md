---
phase: 04-templates-activity-log
plan: 21
type: execute
wave: 19
depends_on: ["04-20"]
files_modified:
  - docs/design/SYSTEM.md
  - src/core/template-file.ts
  - tests/unit/template-file.test.ts
  - src/shared/messages.ts
  - src/entrypoints/background.ts
  - src/worker/storage-writer.ts
  - src/entrypoints/options/templates-section.ts
  - src/entrypoints/options/main.ts
  - tests/e2e/template-file.e2e.ts
autonomous: false
requirements: [TMPL-16]
estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "이용자가 옵션 페이지에서 틀을 파일로 내보낼 수 있고, 파일을 만들기 전에 '이 파일에는 틀에 저장된 고정 값이 들어 있습니다'를 먼저 보고 확인한다 — Esc면 파일이 생기지 않는다(D-31, TMPL-16)"
    - "내보낸 파일에는 형식 종류와 버전이 붙고, 이름은 tremor-helper-templates-YYYY-MM-DD.json이다(D-31)"
    - "이용자가 파일을 골라 틀을 가져올 수 있고, 가져온 틀은 틀 목록(0 → 틀 실행)에 나온다(TMPL-16)"
    - "가져온 파일은 zod로 검사하고 형식 변환을 거친다. 크기 초과·JSON 아님·다른 종류·더 새 버전·내용 오류 중 하나라도 있으면 지금 틀을 하나도 바꾸지 않고 이유와 다음 행동을 알린다(D-31, 설계 7장)"
    - "가져온 틀의 시작 주소는 http/https만 받는다 — javascript:·data: 주소가 든 파일은 통째로 거절된다(RESEARCH V5)"
    - "파일 저장·고르기는 새 권한 없이 확장 페이지의 Blob + a download와 파일 고르기로 한다(D-31, D-05)"
    - "틀 가져오기 저장 요청은 옵션 페이지(확장 페이지)에서 온 것만 받는다 — 웹 페이지 쪽 content script가 보낸 같은 요청은 거절된다"
  artifacts:
    - path: "src/core/template-file.ts"
      provides: "틀 파일 형식·만들기·읽기·합치기(순수)"
      exports: ["TemplateFileV1", "TEMPLATE_FILE_KIND", "MAX_IMPORT_BYTES", "buildTemplateFile", "parseTemplateFile", "mergeImported", "templateFileName"]
    - path: "src/entrypoints/options/templates-section.ts"
      provides: "옵션 페이지 '틀' 절(목록·내보내기 경고·가져오기) — Phase 3 절 모양"
      exports: ["templatesSection"]
    - path: "tests/e2e/template-file.e2e.ts"
      provides: "내보내기·가져오기 e2e"
  key_links:
    - from: "src/entrypoints/options/templates-section.ts"
      to: "src/page/export/download-file.ts"
      via: "openOptionsConfirm의 onResult(확인) 안에서 동기로 downloadJson(templateFileName(now), file) — 파일 내용은 경고를 띄우기 전에 만들어 둠"
      pattern: "downloadJson\\("
    - from: "src/entrypoints/background.ts"
      to: "src/core/template-file.ts"
      via: "storage/request op importTemplates → Phase 3 isExtensionPage(sender) 뒤에만 → parseTemplateFile(text) → writer.importTemplates"
      pattern: "parseTemplateFile\\("
    - from: "src/worker/storage-writer.ts"
      to: "src/core/template-file.ts"
      via: "importTemplates: enqueue 안에서 migrate로 읽기 → mergeImported → TemplatesV1 zod 검사 → 한 번에 쓰기(실패면 쓰지 않음)"
      pattern: "mergeImported\\("
  prohibitions:
    - statement: "경고를 확인하기 전에는 틀 파일을 만들지 않는다"
      status: resolved
      verification: test
    - statement: "가져오기가 실패하면 지금 틀을 하나도 바꾸지 않는다"
      status: resolved
      verification: test
    - statement: "웹 페이지 쪽(content script)에서 온 메시지로는 틀을 가져오거나 내보내지 않는다"
      status: resolved
      verification: test
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 반복 업무를 한 번 해 보여 틀로 저장하고 이후 "틀 고르기 1번 + 바뀌는 값 2~3번 + 제출 확인 1번"으로 끝내기를, **so that** 되돌릴 수 없는 제출은 항상 확인 뒤에 한 번만 일어나고 문제가 생기면 활동 기록으로 추적할 수 있다.

<objective>
틀을 파일로 내보내고(경고 먼저) 파일에서 가져온다. 파일 형식은 한 번 밖으로 나가면 되돌릴 수 없으므로 사람이 먼저 고른다.

Purpose: TMPL-16(D-31). 다른 PC로 옮기기·백업. 가져온 파일은 외부 입력이므로 검사·원본 보존(설계 7장, RESEARCH V5·V12).
Output: 형식 결정(checkpoint), `template-file.ts` + 단위 시험, 옵션 페이지 틀 영역(SYSTEM.md 템플릿 먼저), 가져오기 검사·합치기, e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-activity-log/04-CONTEXT.md
@.planning/phases/04-templates-activity-log/04-RESEARCH.md
@.planning/phases/04-templates-activity-log/04-06-SUMMARY.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 패키지·새 확장 권한 없음(D-05 — `downloads` 권한 쓰지 않음). 필요해지면 멈추고 `checkpoint:decision`으로 이유 한 줄 + 승인을 받는다.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- tracer·`tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(04-21): …` → 최소 구현 → GREEN 커밋 `feat(04-21): …` → 정리하면 `refactor(04-21): …`. 제목 영어 접두어, 본문 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드).
- 새 화면은 먼저 SYSTEM.md에 템플릿을 더한다(D-37). `docs/design/tokens.css` 변수만(D-36). 카피는 SYSTEM.md 카피 규칙(D-38).
- 저장 API 쓰기·지우기는 `src/worker/storage-writer.ts`의 `enqueue()` 안에만(D-03).

## Assumes from Phase 3 (브랜치 `origin/claude/phase3-plans-ng6f32`의 계획 이름 — 실제 이름이 다르면 실행 전에 고친다)

- 옵션 페이지: `src/entrypoints/options/main.ts`가 `createOptionsUi({ root, sections })`(`ui.ts`)에 절 `{ number, title, render(ctx) }`를 넘긴다(절 안 번호 줄, Esc = 절 목록, 토스트). 04-06이 `submitSection`·`templateSpeedSection`을 같은 방식으로 더했다. 이 계획의 절 번호는 그 다음 빈 번호.
- 확인 화면: `src/entrypoints/options/confirm.ts`의 `openOptionsConfirm({ title, body, onResult })` — SYSTEM.md 확인 화면 템플릿 + `createConfirmGuard` 1초 보호(진짜 키만). `onResult`는 확인 키 처리 안에서 동기로 불린다 — 아니면 Task 2 e2e 1의 download 이벤트가 알려 준다(그때는 멈추고 SUMMARY에 적은 뒤 `checkpoint:decision`).
- 설정 파일(03-22 `file-section.ts`)이 옵션 페이지에서 Blob + `a[download]`와 `<input type="file">`(진짜 키 처리 안에서 엶)로 동작한다(D-31 "같은 방식"). 이 계획은 04-14의 `src/page/export/download-file.ts` `downloadJson(fileName, data)`를 옵션 페이지에서 import한다.
- 저장 요청: `storage/request`의 op 유니온(`src/shared/messages.ts`)과 `background.ts`의 `isExtensionPage(sender)`(확장 URL로 시작) 뒤에서만 받는 설정 화면 op(03-18). 옵션 페이지는 저장소를 **읽기**만 직접 한다(03-22가 `chrome.storage.sync.get(null)`로 읽는 것과 같음).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 결정 — 틀 파일(TemplateFileV1) 모양</name>
  <read_first>
    - src/core/template-schema.ts (TemplateV1Schema, TemplatesV1, templatesSpec — 04-03)
    - .planning/phases/04-templates-activity-log/04-CONTEXT.md (D-31)
    - .planning/phases/04-templates-activity-log/04-RESEARCH.md (Pattern 12, Security Domain V5·V12)
  </read_first>
  <decision>이용자가 PC 밖으로 가져가는 틀 파일의 모양을 무엇으로 할까</decision>
  <context>
내보낸 파일은 이용자 손에 있어 되돌려 받을 수 없다. 한 번 정한 모양은 앞으로의 모든 도우미 버전이 계속 읽어야 한다(one-way). 저장 형식(storage.local `templates`)과 같은 모양을 쓰면 형식 변환이 한 곳이고, 다른 모양을 쓰면 저장 형식을 파일과 상관없이 바꿀 수 있다. 어느 쪽이든 파일 이름(`tremor-helper-templates-YYYY-MM-DD.json`), 경고 문구, 1MB 상한, zod 검사, 실패 시 원본 보존은 같다.
  </context>
  <options>
    <option id="option-a">
      <name>저장 형식을 그대로 싣기(추천)</name>
      <pros>`{ kind: 'tremor-helper-templates', schemaVersion: <templates 저장 형식 버전>, exportedAt: ISO 시각, data: { templates: TemplateV1[] } }`. 가져오기가 04-03 `templatesSpec`의 `migrate()`를 그대로 거쳐 옛 파일도 저장 형식이 올라갈 때 함께 변환된다. 스키마 하나, 변환 경로 하나.</pros>
      <cons>요소 지문(domPath 등) 같은 내부 필드가 파일에 그대로 나간다. 저장 형식을 바꿀 때마다 migrations를 반드시 채워야 한다(어차피 필요).</cons>
    </option>
    <option id="option-b">
      <name>따로 정한 전달 모양</name>
      <pros>`{ kind, fileVersion: 1, templates: TransferTemplate[] }` — 파일에 싣는 필드를 골라 저장 형식과 떼어 둔다. 저장 형식은 파일과 상관없이 바꿀 수 있다.</pros>
      <cons>스키마 둘과 양쪽 변환기를 함께 유지해야 한다. 변환 누락이 조용한 데이터 손실이 된다.</cons>
    </option>
  </options>
  <action>두 안을 이용자에게 보여 주고 고른 안을 이 계획 SUMMARY의 "결정" 절에 한 줄로 적는다(`TemplateFileV1: option-a` 또는 `option-b`). Task 2·3은 option-a 기준으로 쓰여 있다. option-b를 고르면 Task 2의 `buildTemplateFile`과 Task 3의 `parseTemplateFile`에 `TransferTemplate` ↔ `TemplateV1` 변환 한 쌍과 그 단위 시험을 더한다 — 파일 이름·경고·검사·원본 보존 규칙은 그대로다.</action>
  <acceptance_criteria>
    - `.planning/phases/04-templates-activity-log/04-21-SUMMARY.md`에 `TemplateFileV1: option-a` 또는 `TemplateFileV1: option-b` 줄이 있다
  </acceptance_criteria>
  <resume-signal>option-a 또는 option-b를 입력</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: 끝에서 끝까지 — 옵션 페이지 틀 영역 → 내보내기 → 경고 → Enter → 파일 / 그 파일 가져오기 → 틀 목록에 나옴</name>
  <reversibility rating="one-way">틀 파일 모양(TemplateFileV1)이 이용자 PC에 남는다 — 이후 모든 버전이 이 모양을 읽어야 한다. Task 1 결정대로</reversibility>
  <files>docs/design/SYSTEM.md, src/core/template-file.ts, tests/unit/template-file.test.ts, src/shared/messages.ts, src/entrypoints/background.ts, src/worker/storage-writer.ts, src/entrypoints/options/templates-section.ts, src/entrypoints/options/main.ts, tests/e2e/template-file.e2e.ts</files>
  <read_first>
    - src/entrypoints/options/main.ts, src/entrypoints/options/ui.ts, src/entrypoints/options/confirm.ts (Phase 3 절 모양·`openOptionsConfirm`), src/entrypoints/options/submit-section.ts (04-06 — 절을 더하는 방식)
    - src/entrypoints/options/file-section.ts (Phase 3 설정 파일 — Blob 저장·파일 고르기 방식)
    - src/page/export/download-file.ts (04-14 downloadJson)
    - src/core/template-schema.ts (TemplatesV1, templatesSpec, isWebUrl — 04-03)
    - src/core/activity-log-schema.ts (localDateKey — 04-09)
    - src/shared/messages.ts, src/entrypoints/background.ts (`storage/request` op 유니온, `isExtensionPage(sender)` 뒤 op 분기 — 03-18·04-06)
    - src/worker/storage-writer.ts (saveTemplate의 migrate·preserved-original 처리 — 04-03)
    - tests/e2e/fixtures.ts (extensionId, serviceWorker), tests/e2e/template-helpers.ts (openPaletteSlot, readStorage, writeStorage — 04-02), tests/e2e/options-template.e2e.ts (04-06 — 옵션 페이지 열기·content script 쪽 요청 거절 시험 방식)
    - docs/design/SYSTEM.md (확인 화면 템플릿, 옵션 화면 절 규칙, 상태 표, 카피 규칙)
  </read_first>
  <behavior>
    - 단위: `buildTemplateFile([t1, t2], new Date('2026-09-24T10:00:00+09:00'))` → `kind` 'tremor-helper-templates', `schemaVersion` 1, `data.templates` 길이 2, 입력 배열 불변
    - 단위: `templateFileName(new Date(2026, 8, 24, 23, 30))` → "tremor-helper-templates-2026-09-24.json"(이 PC 시각 기준 날짜)
    - 단위: `parseTemplateFile(JSON.stringify(buildTemplateFile([t1], now)))` → `{ ok: true, templates: [t1과 같은 내용] }`
    - 단위: `mergeImported([], [t1], makeId)` → 템플릿 하나, id는 makeId가 준 새 값
    - e2e 1: 시험 준비로 `writeStorage`가 storage.local `templates`에 올바른 틀 둘("지출결의서 상신", "출장 신청" — 목록·파일만 보므로 실행은 하지 않음)을 넣은 뒤 옵션 페이지에서 "틀" 절을 열면 두 이름과 단계 수 → 번호 1 "내보내기" → 확인 화면 "틀을 파일로 내보낼까요? Enter = 예"와 "이 파일에는 틀에 저장된 고정 값이 들어 있습니다" → 1.5초 동안 download 이벤트 없음(1초 보호 중 Enter 포함) → 보호가 끝난 뒤 Enter → download 이벤트, 파일 이름 "tremor-helper-templates-{오늘}.json", 내용 JSON의 kind·schemaVersion·`data.templates` 길이 2 → 토스트 "내보냈어요"
    - e2e 2: 번호 1 → 확인 화면 → Esc → 2초 동안 download 이벤트 없음, 확인 화면 닫힘
    - e2e 3: e2e 1에서 받은 파일을 준비 → storage.local `templates`를 비움(`writeStorage`) → 번호 2 "가져오기" → filechooser에 그 파일 → 토스트 "틀 2개를 가져왔어요" → 저장된 두 이름이 같다 → 연습 페이지에서 명령판 틀 실행 목록에 두 이름
  </behavior>
  <action>
RED 먼저: 단위 behavior를 새 `tests/unit/template-file.test.ts`에, e2e 1~3을 새 `tests/e2e/template-file.e2e.ts`에 쓰고 실패 확인 후 RED 커밋.

1) SYSTEM.md(D-37, DESIGN.md §4-1 — 화면보다 먼저): "옵션 화면 — 틀 절"을 더한다: Phase 3 옵션 화면 절 규칙 그대로 — 절 제목 "틀", 번호 줄 "1 내보내기"·"2 가져오기", 그 아래 틀마다 글 한 줄(이름 + "단계 N개" — 번호 없음), 경고는 기존 "확인 화면" 템플릿(`openOptionsConfirm` — 가림막 + 가운데 카드 + 1초 막대), 알림은 옵션 화면 토스트(오류는 `--danger` 글자). 상태 표에 "틀 파일" 줄: 비어 있음 "저장된 틀이 없어요"(번호 1 없음), 실패 "원인. 다음 행동.", 성공 "내보냈어요"·"틀 N개를 가져왔어요". 새 토큰 없음.

2) `src/core/template-file.ts`(Task 1 결정 — option-a 기준, D-31): `TEMPLATE_FILE_KIND = 'tremor-helper-templates'`, `MAX_IMPORT_BYTES = 1_048_576`, `TemplateFileV1 = { kind, schemaVersion, exportedAt, data: { templates: TemplateV1[] } }`, `buildTemplateFile(templates, now)`(schemaVersion은 templatesSpec의 현재 버전, exportedAt은 `now.toISOString()`), `templateFileName(now)`(`tremor-helper-templates-${localDateKey(now)}.json`), `parseTemplateFile(text): { ok: true; templates } | { ok: false; reason }` — 이 작업에서는 JSON.parse → kind 확인 → `{ schemaVersion, data }`를 01-14 `migrate(raw, templatesSpec)`에 넘겨 통과하면 ok, 아니면 reason 'invalid'(세부 이유는 Task 3). `mergeImported(existing, incoming, makeId)` — 이 작업에서는 가져온 틀마다 새 id를 주어 뒤에 붙임(이름 겹침은 Task 3). 모두 순수. 요청 출처 확인은 새로 만들지 않고 Phase 3 `isExtensionPage(sender)`를 쓴다.

3) 저장 요청(`messages.ts`·`background.ts`, Phase 3 방식): `storage/request` op 유니온에 `importTemplates { text: string }`(답 `{ ok: true, added: number, renamed: number } | { ok: false, reason }`). background는 이 op를 `isExtensionPage(sender)` 확인 뒤의 설정 화면 op 분기에만 두고(웹 페이지 쪽 content script에서 온 같은 op는 Phase 3 규칙대로 거절), `parseTemplateFile(text)` → `writer.importTemplates(templates)`. 내보내기는 저장이 아니므로 메시지를 만들지 않는다 — 옵션 페이지가 storage.local `templates`를 직접 읽어 01-14 `migrate(raw, templatesSpec)`로 올리고 `buildTemplateFile(templates, new Date())`(읽기 실패·원본 보존 상태면 "저장된 틀을 읽지 못했어요. 지금 틀은 그대로예요.").

4) `storage-writer.ts`: `importTemplates(incoming)` — `enqueue()` 안에서 `templates`를 migrate로 읽고(실패면 쓰지 않고 `{ ok: false, reason: 'preserved-original' }` + 01-14 notice), `mergeImported(existing, incoming, () => crypto.randomUUID())`, 결과 전체를 TemplatesV1 zod로 검사해 통과할 때만 한 번에 쓴다.

5) `src/entrypoints/options/templates-section.ts`: `templatesSection = { number, title: '틀', render(ctx) }`(Phase 3 절 모양, 번호는 04-06 절 다음 빈 번호) — `render`는 storage.local `templates`를 읽어 목록을 글 줄로 그리고(이름은 글자 노드로만 — 파일에서 온 이름을 HTML로 해석하지 않게), 번호 줄 1 "내보내기"·2 "가져오기". 1이면 먼저 파일 내용을 만들어 둔 뒤 `openOptionsConfirm({ title: '틀을 파일로 내보낼까요? Enter = 예', body: '이 파일에는 틀에 저장된 고정 값이 들어 있습니다', onResult })`(D-31 문구 그대로, 1초 보호는 Phase 3 확인 화면이 함). `onResult`가 확인이면 **그 호출 안에서 동기로** `downloadJson(templateFileName(new Date()), file)`(RESEARCH Pattern 12 — 사이에 await 없음) 후 토스트 "내보냈어요"; 취소면 닫기만. 2는 번호 키 처리 안에서 숨긴 `<input type="file" accept=".json,application/json">`의 `click()`(Phase 3 file-section과 같은 방식); 파일이 오면 `text()`로 읽어 `importTemplates` op로 보내고 답에 따라 토스트(성공 "틀 N개를 가져왔어요", 실패 문구는 Task 3). 성공 뒤 목록을 다시 그린다. `main.ts`가 이 절을 `sections`에 더한다(`index.html`은 바꾸지 않음). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/template-file.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or any test reported as failed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-file.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "3 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/template-file.ts`에 `export function buildTemplateFile`, `export function parseTemplateFile`, `export function mergeImported`, `export function templateFileName`, `TEMPLATE_FILE_KIND`, `MAX_IMPORT_BYTES`가 있다
    - `src/entrypoints/options/templates-section.ts`에 문자열 "이 파일에는 틀에 저장된 고정 값이 들어 있습니다"와 `openOptionsConfirm(`, `downloadJson(`이 있고, `src/entrypoints/options/main.ts`에 `templatesSection`이 있다
    - `grep -vE '^\s*(//|\*|/\*)' src/entrypoints/options/templates-section.ts | grep -c "innerHTML"`가 0이다
    - `src/entrypoints/background.ts`에서 `importTemplates` 처리 줄이 `isExtensionPage(` 확인 줄보다 뒤에 있다(`grep -n`)
    - `.output/chrome-mv3/manifest.json`(빌드 뒤)의 permissions에 `downloads`가 없다
    - `CI=true pnpm exec playwright test tests/e2e/template-file.e2e.ts` 3 passed
  </acceptance_criteria>
  <done>옵션 페이지에서 경고를 본 뒤에만 틀 파일이 만들어지고, 그 파일을 가져오면 틀 목록에 나온다(TMPL-16).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 가져오기 검사 — 1MB, JSON 아님, 다른 종류, 더 새 버전, 내용 오류, javascript: 주소, 이름 겹침, 전부 아니면 아무것도</name>
  <files>src/core/template-file.ts, tests/unit/template-file.test.ts, src/entrypoints/options/templates-section.ts, tests/e2e/template-file.e2e.ts</files>
  <read_first>
    - src/core/template-file.ts, tests/unit/template-file.test.ts (Task 2)
    - src/core/settings-schema.ts (01-14 migrate 결과 모양 — 'newer-version'·'invalid')
    - src/core/template-schema.ts (TemplateV1Schema — name 1~60자, steps 1~300, startUrl isWebUrl)
    - .planning/phases/04-templates-activity-log/04-RESEARCH.md (Security Domain V5·V12)
  </read_first>
  <behavior>
    - `parseTemplateFile(text)`: UTF-8 바이트 길이가 `MAX_IMPORT_BYTES`를 넘으면 'too-large'(JSON을 풀기 전에); JSON이 아니면 'not-json'; 객체가 아니거나 kind가 없거나 다르면 'wrong-kind'; schemaVersion이 현재보다 크면 'newer-version'; 틀 하나라도 스키마에 어긋나면(이름 빈 값, 61자 이름, steps 0개·301개, 모르는 단계 kind, startUrl "javascript:alert(1)"·"data:text/html,x") 'invalid'; `data.templates`가 빈 배열이면 'empty'
    - 맨 앞 BOM("﻿")이 붙은 올바른 파일은 ok(BOM만 떼고 읽음)
    - 이름에 NFC가 아닌(자모 분리) 한글이 있으면 NFC로 바꿔 받는다 — 같은 이름 비교도 NFC 기준
    - `mergeImported(existing, incoming, makeId)`: 이름이 기존과 같으면 "{이름} (가져옴)", 그것도 있으면 "{이름} (가져옴 2)", "(가져옴 3)" …; 60자를 넘으면 원래 이름 쪽을 잘라 꼬리가 들어가게; 가져오는 파일 안끼리 같은 이름도 같은 규칙; 결과 `{ templates, added, renamed }`; existing·incoming 배열과 원소를 바꾸지 않음(freeze한 입력으로 시험)
    - e2e 4: 두 틀 중 하나의 startUrl이 "javascript:alert(1)"인 파일 → 알림 "틀 파일 내용이 올바르지 않아요. 지금 틀은 그대로예요." → storage.local `templates`가 가져오기 전과 깊은 비교로 같다
    - e2e 5: 1.2MB 파일 → "파일이 너무 커요(1MB까지). 다른 파일을 고르세요." → 틀 그대로
    - e2e 6: schemaVersion 99 파일 → "더 새 도우미에서 만든 파일이에요. 도우미를 업데이트한 뒤 다시 가져오세요." → 틀 그대로
    - e2e 7: 이미 "지출결의서 상신"이 있을 때 같은 이름의 틀 파일 → "틀 1개를 가져왔어요 · 이름이 겹친 1개는 '(가져옴)'을 붙였어요" → 목록에 "지출결의서 상신"과 "지출결의서 상신 (가져옴)"
    - e2e 8: 연습 페이지 content script 쪽에서 `chrome.runtime.sendMessage({ type: 'storage/request', op: { kind: 'importTemplates', text: <올바른 틀 파일> } })`(04-06 e2e와 같은 방식)를 보내면 거절되고 storage.local `templates`가 그대로
  </behavior>
  <action>
RED 먼저: behavior를 `tests/unit/template-file.test.ts`(이 작업에서 최소 20개가 되도록)와 e2e 4~8을 `tests/e2e/template-file.e2e.ts`에 더하고 실패 확인 후 RED 커밋.

`parseTemplateFile`(D-31, RESEARCH V5·V12): 순서 — `new TextEncoder().encode(text).length > MAX_IMPORT_BYTES`면 'too-large' → 맨 앞 BOM 제거 → JSON.parse 실패면 'not-json' → kind 확인 'wrong-kind' → migrate 결과 'newer-version'이면 그대로, 'invalid'면 'invalid' → 템플릿 이름 NFC 정규화 → 빈 목록이면 'empty'. startUrl 거절은 04-03 `TemplateV1Schema`의 `isWebUrl` refine이 한다(새 규칙을 따로 만들지 않음 — 실행 때 `tabs.update` 직전 재검사도 04-07 그대로). `mergeImported`는 behavior 규칙대로. background는 text 길이를 다시 재지 않고 `parseTemplateFile`에 맡긴다(한 곳). e2e 8은 Task 2의 op 분기 위치를 확인한다(코드 변경 없이 통과해야 함 — 실패면 op가 확인 앞에 놓인 것).

옵션 페이지 알림(D-38 "원인. 다음 행동."): too-large "파일이 너무 커요(1MB까지). 다른 파일을 고르세요.", not-json·wrong-kind "틀 파일이 아니에요. 도우미에서 내보낸 파일을 고르세요.", newer-version "더 새 도우미에서 만든 파일이에요. 도우미를 업데이트한 뒤 다시 가져오세요.", invalid "틀 파일 내용이 올바르지 않아요. 지금 틀은 그대로예요.", empty "파일에 틀이 없어요. 다른 파일을 고르세요.", preserved-original "저장된 틀을 읽지 못했어요. 지금 틀은 그대로예요.", 성공 "틀 N개를 가져왔어요"(renamed가 있으면 뒤에 " · 이름이 겹친 M개는 '(가져옴)'을 붙였어요"). 옵션 페이지는 파일을 읽기 전에 `file.size > MAX_IMPORT_BYTES`면 보내지 않고 같은 알림(큰 파일을 메시지로 보내지 않게). 통과하면 GREEN, 정리하면 REFACTOR 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/template-file.test.ts</automated>
    <fails_when>non-zero exit, or any test reported as failed, or fewer than 20 tests reported</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-file.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "8 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/unit/template-file.test.ts`에 'too-large'·'not-json'·'wrong-kind'·'newer-version'·'invalid'·'empty' 각각을 기대하는 시험이 있고 20개 이상 통과
    - `src/core/template-file.ts`에 `TextEncoder`와 `normalize('NFC')`가 있다
    - `src/entrypoints/options/templates-section.ts`에 "틀 파일 내용이 올바르지 않아요. 지금 틀은 그대로예요."와 "파일이 너무 커요(1MB까지). 다른 파일을 고르세요."가 있다
    - `CI=true pnpm exec playwright test tests/e2e/template-file.e2e.ts` 8 passed(e2e 4·5·6·8의 깊은 비교 포함)
  </acceptance_criteria>
  <done>잘못되었거나 너무 크거나 더 새 형식인 파일은 지금 틀을 하나도 바꾸지 않고 이유와 다음 행동을 알리며, 이름이 겹치면 "(가져옴)"을 붙여 둘 다 남긴다(D-31).</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- TMPL-16: edge probe `unclassified — review manually`. 가정한 경계(플래그):
  - 가져온 틀은 늘 새 id를 받는다 — 같은 파일을 두 번 가져오면 틀이 둘이 되고 둘째는 "(가져옴)"이 붙는다(덮어쓰기 없음 — 되돌릴 수 없는 덮어쓰기보다 중복이 안전하다).
  - 가져오는 틀 개수 상한은 따로 두지 않는다 — 파일 1MB가 상한이다.
  - 파일 인코딩은 UTF-8만(맨 앞 BOM 허용). 다른 인코딩 파일은 'not-json' 또는 'invalid'로 거절된다.
  - 내보내는 파일에는 틀만 들어간다 — 틀별 최근 값(`template-recent:*`), 실행 기록, 활동 기록은 넣지 않는다. 민감칸 값은 애초에 틀에 없다(D-10, 04-03·04-19).
  - 저장된 틀이 0개면 번호 1 "내보내기"를 그리지 않고 "저장된 틀이 없어요"와 번호 2 "가져오기"만 보인다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 이용자가 고른 파일 → 옵션 페이지 → SW | 누가 만들었는지 모르는 JSON |
| 확장 → 이용자 PC 파일 | 틀의 고정 값이 PC 밖으로 나갈 수 있다 |
| content script → SW | 웹 페이지에 붙은 쪽에서 오는 메시지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-54 | Tampering | 가져온 틀의 시작 주소 | high | mitigate | `TemplateV1Schema`의 `isWebUrl` refine으로 파일 통째 거절(e2e 4) + 실행 때 `tabs.update` 직전 재검사(04-07) |
| T-04-55 | Denial of Service | 큰 파일 | medium | mitigate | 옵션 페이지 `file.size`와 SW `parseTemplateFile`의 UTF-8 바이트 길이 두 곳에서 1MB 상한(e2e 5) |
| T-04-56 | Information Disclosure | 내보내기 | medium | mitigate | D-31 경고를 1초 보호와 함께 먼저 보여 주고 확인 뒤에만 파일 생성(e2e 1·2); 민감칸 값은 틀에 없음(D-10) |
| T-04-57 | Tampering | 가져오기 중 기존 틀 | high | mitigate | writer 한 번의 enqueue 안에서 읽기 → 합치기 → 전체 zod 검사 → 한 번에 쓰기, 실패면 쓰지 않음; 저장된 틀을 못 읽으면 원본 보존(01-14) |
| T-04-58 | Elevation of Privilege | importTemplates op·내보내기 | high | mitigate | op는 Phase 3 `isExtensionPage(sender)` 뒤에만(acceptance 줄 순서 + e2e 8) — 웹 페이지에 붙은 content script 경로로는 틀을 바꿀 수 없고, 내보내기는 메시지 없이 옵션 페이지 안에서만 일어난다 |
| T-04-59 | Tampering | 파일에서 온 틀 이름 표시 | medium | mitigate | 옵션 페이지·오버레이 모두 글자 노드로만 그림(acceptance가 옵션 영역 파일의 HTML 문자열 대입 0 확인) |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 설치 없음(D-05) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/template-file.test.ts tests/unit/template-schema.test.ts` 통과
- `CI=true pnpm exec playwright test tests/e2e/template-file.e2e.ts tests/e2e/options-template.e2e.ts` 통과
- `pnpm typecheck && pnpm lint && pnpm build` 통과, manifest 권한 불변(`downloads` 없음)
</verification>

<success_criteria>
- 이용자가 틀을 파일로 내보내고 가져올 수 있고, 내보낼 때 "이 파일에는 틀에 저장된 고정 값이 들어 있습니다"를 먼저 본다(TMPL-16)
- 잘못된 파일은 지금 틀을 바꾸지 않고 알린다(D-31)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/template-file.ts`, `src/entrypoints/options/templates-section.ts`, `tests/unit/template-file.test.ts`, `tests/e2e/template-file.e2e.ts`
- 심볼: `TemplateFileV1`, `TEMPLATE_FILE_KIND`, `MAX_IMPORT_BYTES`, `buildTemplateFile()`, `parseTemplateFile()`, `mergeImported()`, `templateFileName()`, 옵션 절 `templatesSection`, writer의 `importTemplates()`
- 저장 요청: `storage/request` op `importTemplates { text }`(설정 화면 전용)
- 파일 형식: `tremor-helper-templates-YYYY-MM-DD.json`(kind `tremor-helper-templates`, Task 1 결정)
- SYSTEM.md: "옵션 화면 — 틀 절", 상태 표 "틀 파일" 줄
- 결정 기록: SUMMARY "결정" 절의 `TemplateFileV1: option-…`

<output>
Create `.planning/phases/04-templates-activity-log/04-21-SUMMARY.md` when done
</output>
