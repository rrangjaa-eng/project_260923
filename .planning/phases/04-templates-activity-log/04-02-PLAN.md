---
phase: 04-templates-activity-log
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/practice-site/workflow.html
  - tests/practice-site/workflow-form.html
  - tests/practice-site/workflow-result.html
  - tests/practice-site/workflow-frame.html
  - tests/practice-site/workflow-ambiguous.html
  - tests/e2e/template-helpers.ts
  - tests/e2e/practice-workflow.e2e.ts
autonomous: true
requirements: [TMPL-01, TMPL-04, TMPL-06]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "연습 사이트에 메뉴 → 양식 → 상신 → 결과로 이어지는 업무 흐름이 있다: workflow.html의 '지출결의' 링크 → workflow-form.html(일자·금액·연락처·적요·비밀번호 칸, 임시저장·상신 버튼) → workflow-result.html(열릴 때마다 상신 횟수 +1)(D-39)"
    - "비밀번호 칸(#pw)에는 name 속성이 없어 상신(GET) 주소에 그 값이 실리지 않는다 — 뒤 계획의 '값이 어디에도 없음' 검사가 주소 때문에 흐려지지 않는다(D-10, D-29)"
    - "iframe 안 양식 연습 페이지가 같은 출처(#form-frame)와 다른 출처(http://other.test, #other-frame)를 함께 가지며, ?late=1이면 두 iframe이 700ms 뒤에 붙고 ?noframe=1이면 iframe이 없다(D-39)"
    - "결과가 불분명한 제출 버튼 페이지(workflow-ambiguous.html)는 누르면 누른 횟수만 오르고 이동·글자 변화가 없다(D-39)"
    - "뒤 계획의 e2e가 같은 조작 순서를 쓰도록 명령판 열기·장 넘기기·번호 고르기와 확장 저장소 읽기·쓰기가 tests/e2e/template-helpers.ts 한 곳에 있다 — 명령판 키 순서는 Phase 3 구현(keymap.openPalette, 9 = 다음 장)을 따른다"
    - "연습 흐름 전체에서 practice.test·other.test 밖으로 나가는 요청이 없다(D-39)"
  artifacts:
    - path: "tests/practice-site/workflow-form.html"
      provides: "지출결의서 양식(날짜·금액·전화·적요·비밀번호, 임시저장·상신, ?altsubmit=1·?framed=1)"
    - path: "tests/practice-site/workflow-frame.html"
      provides: "같은 출처·다른 출처 iframe 양식(?late=1·?noframe=1)"
    - path: "tests/practice-site/workflow-ambiguous.html"
      provides: "결과가 불분명한 제출 버튼"
    - path: "tests/e2e/template-helpers.ts"
      provides: "Phase 4 e2e 공통 조작"
      exports: ["openPalette", "openPaletteSlot", "readStorage", "writeStorage"]
  key_links:
    - from: "tests/e2e/practice-workflow.e2e.ts"
      to: "tests/e2e/fixtures.ts"
      via: "practice.test·other.test 라우팅(tests/practice-site/ 파일) + expectNoExternalRequests()"
      pattern: "expectNoExternalRequests\\("
    - from: "tests/e2e/template-helpers.ts"
      to: "Phase 3 명령판"
      via: "keymap.openPalette(기본 0) → 9를 (장-1)번 → 번호"
      pattern: "openPaletteSlot"
  prohibitions:
    - statement: "연습 페이지는 practice.test·other.test 밖의 주소를 부르지 않는다"
      status: resolved
      verification: test
    - statement: "연습 페이지의 비밀번호 칸 값은 제출 주소에 실리지 않는다"
      status: resolved
      verification: test
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 반복 업무를 한 번 해 보여 틀로 저장하고 이후 "틀 고르기 1번 + 바뀌는 값 2~3번 + 제출 확인 1번"으로 끝내기를, **so that** 되돌릴 수 없는 제출은 항상 확인 뒤에 한 번만 일어나고 문제가 생기면 활동 기록으로 추적할 수 있다.

<objective>
Phase 4의 기록·실행 조각이 모두 올라탈 연습 업무 흐름(메뉴 → 양식 → 상신 → 결과)과 iframe 양식, 결과가 불분명한 제출 페이지를 만들고, 뒤 계획 e2e가 함께 쓰는 조작 도우미를 한 파일에 모은다. 연습 페이지는 이용자 입력(Playwright 실제 입력)으로 한 바퀴 돌려 모양을 고정한다.

Purpose: D-39(연습 사이트에서만 시험). TMPL-01(기록)·TMPL-04(시작 페이지로 곧장)·TMPL-06(실행)의 시험 바탕 — 요구 자체는 04-03·04-04(기록), 04-07·04-08(실행)이 채운다. 04-01 스파이크와 파일이 겹치지 않아 같은 wave에서 돈다.
Output: 연습 페이지 다섯, `template-helpers.ts`, `practice-workflow.e2e.ts`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-activity-log/04-CONTEXT.md
@tests/e2e/fixtures.ts
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 패키지·새 확장 권한 없음(D-05). 필요해지면 멈추고 `checkpoint:decision`으로 이유 한 줄 + 승인을 받는다.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- tracer·`tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(04-02): …` → 최소 구현 → GREEN 커밋 `feat(04-02): …` → 정리하면 `refactor(04-02): …`. 제목 영어 접두어, 본문 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드).
- 연습 사이트에서만 시험(D-39). 연습 페이지 스크립트는 인라인이고 바깥 주소를 부르지 않는다. 기존 파일은 이 계획이 필요한 줄만 바꾼다.

## Assumes from Phase 3 (브랜치 `origin/claude/phase3-plans-ng6f32`의 03-01~03-03 계획 이름 — 실제 이름이 다르면 실행 전에 고친다)

- 명령판은 `keymap.openPalette`(기본 `Digit0`)로 열리고, 첫째·둘째 장의 9는 다음 장, 셋째 장의 9는 첫 장이다(`src/page/palette/palette-controller.ts` `createPaletteController`, 03-02). 입력칸에 포커스가 있으면(입력 중 모드) 0은 글자로 들어가므로 도우미는 먼저 포커스를 풀고 연다.
- 명령판 카드는 `src/page/overlay/palette.ts`의 `.palette-card[data-number]`로 그려지고 카드 글자는 `COMMAND_LABELS`(`src/core/palette-slots.ts`)다 — 둘째 장 4 = "틀 기록"(`template-record`), 첫 장 3 = "틀 실행"(`template-run`), 셋째 장 3 = "기록 내보내기"(`export-log`).

<tasks>

<task type="tracer">
  <name>Task 1: 끝에서 끝까지 — 연습 업무 흐름 한 바퀴(메뉴 → 양식 → 임시저장 → 상신 → 결과 횟수), 외부 요청 0</name>
  <files>tests/practice-site/workflow.html, tests/practice-site/workflow-form.html, tests/practice-site/workflow-result.html, tests/e2e/practice-workflow.e2e.ts</files>
  <read_first>
    - tests/e2e/fixtures.ts (practice.test·other.test 라우팅, servePage, expectNoExternalRequests, blockedRequests)
    - tests/practice-site/ 의 기존 페이지 하나(인라인 스크립트·label 모양)
    - .planning/phases/04-templates-activity-log/04-CONTEXT.md (D-39 연습 사이트 목록, specifics)
  </read_first>
  <behavior>
    - e2e 1: 새 컨텍스트에서 `http://practice.test/workflow.html`(제목 "업무 메뉴") → `#menu-expense`("지출결의") 클릭 → 제목 "지출결의서" → `#date`에 "2026-09-24", `#amount`에 "128,000", `#pw`에 "Pw-0000" 입력 → `#submit`("상신") 클릭 → 제목 "상신 완료", 본문에 "상신되었습니다", `#count`가 "1" → 주소에 "Pw-0000"과 "pw="가 없다 → `expectNoExternalRequests()`
    - e2e 2: 양식에서 `#save-draft`("임시저장") 클릭 → `#status`가 "임시저장했습니다", 주소 그대로
  </behavior>
  <action>
RED 먼저: `tests/e2e/practice-workflow.e2e.ts`에 behavior 둘을 쓰고 실패 확인 후 RED 커밋.

연습 페이지(D-39, 인라인 스크립트만):
- `workflow.html`: title "업무 메뉴", `a#menu-expense` "지출결의" → `workflow-form.html`.
- `workflow-form.html`: title "지출결의서", `form`(method GET, action `workflow-result.html`) 안에 label로 이어진 칸 — `#date`(label "일자", name date), `#amount`("금액", name amount), `#phone`("연락처", name phone), `#memo`("적요", name memo), `#pw`("비밀번호", type password, **name 속성 없음** — GET 주소에 값이 실리지 않게), `button#save-draft` "임시저장"(type button, 누르면 `#status`에 "임시저장했습니다"), `button#submit` "상신"(type submit). `?altsubmit=1`이면 `#submit` 글자를 "결재올림"으로 바꾸고(04-06 사이트별 제출 단어 시험), `?framed=1`이면 제목 줄을 숨긴다(iframe 안에서 쓰임 — 칸과 버튼은 같다).
- `workflow-result.html`: title "상신 완료", 본문 "상신되었습니다", 열릴 때마다 `localStorage.submitCount`를 1 올려 `#count`에 표시.
통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/practice-workflow.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "2 passed", or "No tests found", or the blockedRequests list printed on failure is non-empty</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/practice-site/workflow.html`, `workflow-form.html`, `workflow-result.html`이 있다
    - `grep -c 'id="pw"' tests/practice-site/workflow-form.html`이 1이고, `#pw` 요소 줄에 `name=`이 없다(`grep 'id="pw"' tests/practice-site/workflow-form.html | grep -c 'name='`가 0)
    - `grep -rnE 'https?://' tests/practice-site/workflow*.html | grep -vE 'practice\.test|other\.test'`가 아무것도 출력하지 않는다
    - `CI=true pnpm exec playwright test tests/e2e/practice-workflow.e2e.ts` 2 passed
  </acceptance_criteria>
  <done>연습 업무 흐름을 실제 입력으로 한 바퀴 돌 수 있고, 비밀번호 값은 주소에 실리지 않으며, 바깥 요청이 없다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: iframe 양식·결과 불분명 페이지·변형(?altsubmit·?late·?noframe) + e2e 공통 도우미(명령판·저장소)</name>
  <files>tests/practice-site/workflow-frame.html, tests/practice-site/workflow-ambiguous.html, tests/e2e/template-helpers.ts, tests/e2e/practice-workflow.e2e.ts</files>
  <read_first>
    - tests/practice-site/workflow-form.html (Task 1 결과)
    - tests/practice-site/frames.html (Phase 1 iframe 연습 페이지 모양)
    - tests/e2e/fixtures.ts (serviceWorker 픽스처 — 확장 SW에서 evaluate)
    - Phase 3: src/page/palette/palette-controller.ts, src/page/overlay/palette.ts, src/core/palette-slots.ts (명령판 열기·장 넘기기·카드 DOM)
  </read_first>
  <behavior>
    - e2e 3: `workflow-form.html?altsubmit=1`의 `#submit` 글자가 "결재올림"
    - e2e 4: `workflow-frame.html`(제목 "지출결의(프레임)")에 `iframe#form-frame`(src `workflow-form.html?framed=1`)과 `iframe#other-frame`(src `http://other.test/workflow-form.html?framed=1`)이 있고, 두 프레임 안에 `#amount`가 있다; `?late=1`이면 300ms 때 iframe 0개, 1500ms 안에 2개; `?noframe=1`이면 iframe 0개
    - e2e 5: `workflow-ambiguous.html`(제목 "경비 제출")에서 `#amount` 입력 뒤 `#submit`("제출") 클릭 → `#clicks`가 "1", 주소·본문 글자 그대로
    - e2e 6: `openPalette(page)` 뒤 `.palette`가 보이고, `openPaletteSlot(page, 2, 4)`는 둘째 장으로 넘긴 뒤 4를 누른다 — 누르기 직전 둘째 장 카드 4의 글자에 "틀 기록"이 있다(흐림 여부는 보지 않는다 — Phase 4 뒤 계획이 바꾼다)
    - e2e 7: `writeStorage(serviceWorker, 'session', { probe: 1 })` 뒤 `readStorage(serviceWorker, 'session', 'probe')`가 1
  </behavior>
  <action>
RED 먼저: behavior 다섯을 `practice-workflow.e2e.ts`에 더하고 실패 확인 후 RED 커밋.

연습 페이지(D-39):
- `workflow-frame.html`: title "지출결의(프레임)", `iframe#form-frame`(같은 출처)과 `iframe#other-frame`(`http://other.test/…`). `?late=1`이면 두 iframe을 700ms 뒤에 붙이는 인라인 스크립트(04-08 늦은 프레임 시험), `?noframe=1`이면 iframe을 붙이지 않는다(04-11 사라진 프레임 막힘 시험).
- `workflow-ambiguous.html`: title "경비 제출", label "금액" `#amount`, `button#submit` "제출"(type button — 누르면 `#clicks`만 1 오름, 이동·글자 없음; 04-17 확인 필요·04-18 재개 시험).

`tests/e2e/template-helpers.ts`(시험 도구, 제품 코드 아님): `openPalette(page)` — 활성 요소의 포커스를 풀고 Phase 3 `keymap.openPalette` 기본 키(0)를 눌러 `.palette`가 보일 때까지 기다림; `openPaletteSlot(page, chapter: 1 | 2 | 3, number: number)` — `openPalette` 뒤 9를 `chapter - 1`번, 그다음 `number`; `readStorage(serviceWorker, area: 'local' | 'session' | 'sync', key?: string)`와 `writeStorage(serviceWorker, area, items: Record<string, unknown>)` — SW에서 `chrome.storage[area]`를 evaluate로 읽고 쓴다(시험 준비 코드 — 제품의 단일 저장자 규칙 D-03 밖). 뒤 계획은 명령판 순서·저장소 조작을 이 네 함수로만 한다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/practice-workflow.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "7 passed"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/template-helpers.ts`에 `export async function openPalette`, `export async function openPaletteSlot`, `export async function readStorage`, `export async function writeStorage`가 있다
    - `tests/practice-site/workflow-frame.html`에 `late`와 `noframe` 처리가 있다
    - `tests/practice-site/workflow-ambiguous.html`에 `id="clicks"`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/practice-workflow.e2e.ts` 7 passed
  </acceptance_criteria>
  <done>iframe 양식·결과 불분명 페이지·변형이 모양대로 동작하고, 뒤 계획 e2e가 쓸 명령판·저장소 도우미가 한 곳에 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- 이 계획은 edge probe 항목을 갖지 않는다(연습 페이지만 만든다 — TMPL-01은 04-03, TMPL-04·06은 04-07이 가진다). 가정한 경계(플래그):
  - 상신 횟수는 출처별 `localStorage`라 다른 출처 iframe에서 상신하면 other.test 쪽 횟수가 오른다.
  - 연습 흐름의 뒤 계획용 페이지(모양이 바뀐 양식, 알림 창 양식, 흉내 낸 보안 키패드 등)는 그 페이지를 쓰는 계획이 만든다 — 여기서는 여러 계획이 함께 쓰는 다섯 쪽만 만든다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시험 브라우저 → 네트워크 | 연습 사이트 밖으로 요청이 나가면 안 된다(D-39) |
| 시험 도구 → 확장 저장소 | 시험 준비 코드가 저장소를 직접 쓴다(제품 코드 아님) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-68 | Information Disclosure | 연습 페이지의 바깥 요청 | medium | mitigate | 인라인 스크립트만, 바깥 주소 grep 0, e2e 1의 `expectNoExternalRequests()` |
| T-04-69 | Information Disclosure | 비밀번호 칸 값이 제출 주소에 실림 | low | mitigate | `#pw`에 name 없음(acceptance grep) + e2e 1이 주소를 확인 |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 설치 없음(D-05) |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/practice-workflow.e2e.ts` 7 passed
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 연습 업무 흐름·iframe 양식·결과 불분명 페이지가 모양대로 동작한다(D-39)
- 뒤 계획 e2e가 명령판·저장소 조작을 한 곳에서 가져다 쓴다
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/practice-site/workflow.html`, `workflow-form.html`, `workflow-result.html`, `workflow-frame.html`, `workflow-ambiguous.html`, `tests/e2e/template-helpers.ts`, `tests/e2e/practice-workflow.e2e.ts`
- 심볼: `openPalette()`, `openPaletteSlot()`, `readStorage()`, `writeStorage()`
- 연습 페이지 변형: `workflow-form.html?altsubmit=1`·`?framed=1`, `workflow-frame.html?late=1`·`?noframe=1`
- 연습 페이지 전역: `localStorage.submitCount`(workflow-result.html)

<output>
Create `.planning/phases/04-templates-activity-log/04-02-SUMMARY.md` when done
</output>
