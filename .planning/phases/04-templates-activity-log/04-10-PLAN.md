---
phase: 04-templates-activity-log
plan: 10
type: execute
wave: 9
depends_on: ["04-09"]
files_modified:
  - src/core/submit-outcome.ts
  - tests/unit/submit-outcome.test.ts
  - src/page/run/outcome-watch.ts
  - src/page/run/step-executor.ts
  - src/worker/run-orchestrator.ts
  - src/shared/template-messages.ts
  - src/core/run-state.ts
  - src/page/template/controller.ts
  - src/core/run-copy.ts
  - tests/unit/run-copy.test.ts
  - tests/practice-site/workflow-inline-success.html
  - tests/practice-site/workflow-steps.html
  - tests/e2e/template-submit.e2e.ts
autonomous: true
requirements: [TMPL-10]
estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "누른 뒤 OUTCOME_WAIT_MS(8초) 안에 맨 위 주소가 바뀌거나, 대상 프레임에 새 문서가 뜨거나, 누른 뒤 새로 나타난 글자에 성공 낱말이 있으면 성공이다(D-23, TMPL-10)"
    - "성공 판정에 쓰는 신호에는 페이지 쪽(main world) 알림 창 소식이 들어가지 않는다 — 판정 함수의 입력 타입에 그 자리가 없다(D-23, D-27)"
    - "시간 안에 성공 신호가 없으면 다시 누르지 않고 '확인 필요'로 멈춘다 — 누름 기록 outcome 'needs-check', 활동 기록 한 줄(D-23, TMPL-10 — 카드 모양은 04-17)"
    - "입력칸·글 상자·선택 목록·contenteditable 안에 나타난 글자는 성공 글자로 치지 않고, 그 글자는 SW로 보내지 않는다(RESEARCH Pitfall 4)"
    - "틀 앞쪽의 다른 제출 성격 단계(중간 제출, 예: 임시저장)와 위험한 버튼 단계도 실행 때 보호된 확인('…할까요?' / '정말 누를까요?')을 거친 뒤에만 눌리고, 중간 제출도 누르기 전에 누름 기록을 먼저 쓴다(D-12, D-23)"
  artifacts:
    - path: "src/core/submit-outcome.ts"
      provides: "제출 결과 판정(순수)"
      exports: ["SUCCESS_WORDS", "isSuccessText", "OUTCOME_WAIT_MS", "judgeOutcome", "OutcomeSignal"]
    - path: "src/page/run/outcome-watch.ts"
      provides: "누른 뒤 새로 나타난 글자에서 성공 낱말 찾기(입력칸 제외)"
      exports: ["watchSuccessText"]
    - path: "src/core/run-copy.ts"
      provides: "중간·위험 단계 확인 문구와 확인 필요 판정"
      exports: ["stepConfirmCopy", "needsConfirm"]
    - path: "tests/practice-site/workflow-inline-success.html"
      provides: "이동 없이 성공 글자를 띄우는 양식(?inert=1·?inputonly=1)"
    - path: "tests/practice-site/workflow-steps.html"
      provides: "위험 버튼·임시저장·신청이 있는 양식"
  key_links:
    - from: "src/worker/run-orchestrator.ts"
      to: "src/core/submit-outcome.ts"
      via: "tabs.onUpdated(url)·frame/ready·run/outcome 신호를 judgeOutcome(signals, now, deadline)로 판정"
      pattern: "judgeOutcome\\("
    - from: "src/page/run/step-executor.ts"
      to: "src/page/run/outcome-watch.ts"
      via: "run/press-submit에서 누르기 직전 watchSuccessText 시작 → 성공이면 run/outcome"
      pattern: "watchSuccessText\\("
    - from: "src/worker/run-orchestrator.ts"
      to: "src/core/run-copy.ts"
      via: "단계를 보내기 전 needsConfirm(step, dangerWords)면 confirm-step 보기"
      pattern: "needsConfirm\\("
  prohibitions:
    - statement: "성공 판정 신호에 페이지 쪽 알림 창 소식을 넣지 않는다"
      status: resolved
      verification: test
    - statement: "결과를 모를 때 제출 버튼을 다시 누르지 않는다"
      status: resolved
      verification: test
    - statement: "중간 제출·위험 단계를 보호된 확인 없이 누르지 않는다"
      status: resolved
      verification: test
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 반복 업무를 한 번 해 보여 틀로 저장하고 이후 "틀 고르기 1번 + 바뀌는 값 2~3번 + 제출 확인 1번"으로 끝내기를, **so that** 되돌릴 수 없는 제출은 항상 확인 뒤에 한 번만 일어나고 문제가 생기면 활동 기록으로 추적할 수 있다.

<objective>
04-09의 제출 경로에 성공 글자·기한·확인 필요를 더하고, 중간 제출·위험 단계도 같은 보호 확인을 거치게 한다.

Purpose: TMPL-10(D-23), D-12 후반(중간 제출·위험 단계 확인). 판정 규칙은 단위 시험으로 고정한다(D-40).
Output: 결과 판정 순수 함수 + 단위 시험, 성공 글자 감시, 확인 필요 멈춤, 단계 확인 문구 + 단위 시험, 연습 페이지 둘, e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-activity-log/04-CONTEXT.md
@.planning/phases/04-templates-activity-log/04-RESEARCH.md
@.planning/phases/04-templates-activity-log/04-09-SUMMARY.md
@docs/design/SYSTEM.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 패키지·새 확장 권한 없음(D-05). 필요해지면 멈추고 `checkpoint:decision`으로 이유 한 줄 + 승인을 받는다.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- tracer·`tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(04-10): …` → 최소 구현 → GREEN 커밋 `feat(04-10): …` → 정리하면 `refactor(04-10): …`. 제목 영어 접두어, 본문 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드). 틀은 `recordTemplate()`(04-06)으로 만든다.
- 오버레이는 Shadow DOM 안, tokens.css 변수만(D-36). 확인 화면은 Phase 1 confirm-dialog(04-05의 title·rows·tone)만 쓴다. 카피 규칙(D-38).
- 저장 API 쓰기·지우기는 `src/worker/storage-writer.ts`의 `enqueue()` 안에만(D-03). 활동 기록은 `logActivity`(04-09)로만.
- 연습 사이트에서만 시험(D-39). 기존 파일은 이 계획이 필요한 줄만 바꾼다.

## Assumes from Phase 3 (브랜치 `origin/claude/phase3-plans-ng6f32`의 계획 이름 — 실제 이름이 다르면 실행 전에 고친다)

- 위험 단어 규칙: 사이트 항목 `dangerWords`가 있으면 그것, 없으면 설정 전역 `dangerWords`(03-19 — content의 `getDangerWords`와 같은 규칙). 오케스트레이터는 시작 주소 출처의 사이트 항목을 읽어 같은 규칙을 쓴다.

<tasks>

<task type="tracer">
  <name>Task 1: 끝에서 끝까지 — 이동 없는 양식에서 누른 뒤 "저장되었습니다"가 나타남 = 성공 → "저장했어요"</name>
  <files>src/core/submit-outcome.ts, src/page/run/outcome-watch.ts, src/page/run/step-executor.ts, src/worker/run-orchestrator.ts, src/shared/template-messages.ts, tests/practice-site/workflow-inline-success.html, tests/e2e/template-submit.e2e.ts</files>
  <read_first>
    - src/worker/run-orchestrator.ts, src/page/run/step-executor.ts (04-09 결과 — run/press-submit, 성공 신호 둘)
    - src/page/collector/collector.ts (MutationObserver 갱신 방식)
    - tests/practice-site/workflow-form.html (04-02 — 양식 모양)
    - .planning/phases/04-templates-activity-log/04-RESEARCH.md (Pitfall 2, Pitfall 4, Open Questions 3)
    - .planning/phases/04-templates-activity-log/04-CONTEXT.md (D-23, D-27, Claude's Discretion "성공 메시지로 볼 글자")
  </read_first>
  <behavior>
    - e2e 5: `workflow-inline-success.html`(title "비용 등록", `#amount`, `button#submit` "저장", 누르면 300ms 뒤 `#status`에 "저장되었습니다", 이동 없음)에서 기록(금액 입력 → 저장)한 틀 실행 → 확인 1초 뒤 Enter → "저장했어요" 알림 → `submit-press` outcome "success", `#clicks` "1"
  </behavior>
  <action>
RED 먼저: e2e 5를 `template-submit.e2e.ts`에 더하고 실패 확인 후 RED 커밋.

`src/core/submit-outcome.ts`(순수, Claude's Discretion 확정 — RESEARCH Open Q3 채택: 기본 목록만, 사이트별 편집 없음): `SUCCESS_WORDS = ['완료', '저장되었습니다', '처리되었습니다', '등록되었습니다', '상신되었습니다', '접수되었습니다', '신청되었습니다', '제출되었습니다']`, `isSuccessText(text)`(공백을 지운 뒤 포함 검사 — '완료'의 끝맺음 조건은 Task 2), `OutcomeSignal = { kind: 'top-url-changed' | 'frame-new-document' | 'success-text'; at: number }` — main world 알림 창 소식은 이 유니온에 없다(D-23, D-27: 판정 함수가 그 소식을 받을 수 없게 타입으로 막는다).

`src/page/run/outcome-watch.ts`: `watchSuccessText(root: Document, onSuccess: () => void, signal: AbortSignal): void` — 누른 **뒤에** 시작하는 MutationObserver(childList·characterData·subtree)가 새로 붙은 노드·바뀐 글자만 보고 `isSuccessText`면 한 번 `onSuccess`. 글자 자체는 SW로 보내지 않는다(성공 여부만). step-executor가 `run/press-submit`에서 누르기 직전에 감시를 켜고 성공이면 `run/outcome { runId, stepIndex, signal: 'success-text' }`. 메시지 `run/outcome`을 더하고, 오케스트레이터는 'submit-pressed' 상태에서 같은 runId·stepIndex·실행 탭일 때 이 신호도 성공으로 친다(04-09의 이동·새 문서와 함께). 연습 페이지 `workflow-inline-success.html`(behavior대로, `#clicks`에 누른 횟수). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-submit.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "5 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/run/step-executor.ts`에 `watchSuccessText(`가 있다
    - `src/core/submit-outcome.ts`의 `OutcomeSignal` kind가 정확히 세 값이다
    - `CI=true pnpm exec playwright test tests/e2e/template-submit.e2e.ts` 5 passed
  </acceptance_criteria>
  <done>이동 없이 성공 글자만 띄우는 양식에서도 제출 성공을 알아차린다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 결과 판정 고정 — 성공 낱말 끝맺음·기한·늦은 신호·입력칸 제외, 결과를 모르면 다시 누르지 않고 확인 필요</name>
  <files>src/core/submit-outcome.ts, tests/unit/submit-outcome.test.ts, src/page/run/outcome-watch.ts, src/worker/run-orchestrator.ts, tests/practice-site/workflow-inline-success.html, tests/e2e/template-submit.e2e.ts</files>
  <read_first>
    - src/core/submit-outcome.ts, src/page/run/outcome-watch.ts, src/worker/run-orchestrator.ts (Task 1 결과)
    - src/core/run-state.ts (applyStop, TERMINAL_STATUSES — needs-check는 끝난 상태)
  </read_first>
  <behavior>
    - `isSuccessText('저장되었습니다.')`·`('처리가 완료되었습니다')`·`('상신 되었습니다')` true, `('저장')`·`('완료 예정일')`·`('')` false
    - `judgeOutcome([], now, deadline)` — now < deadline면 'waiting', now ≥ deadline면 'needs-check'
    - `judgeOutcome([{ kind: 'top-url-changed', at }], …)`·frame-new-document·success-text 하나라도 기한 전이면 'success'
    - 기한이 지난 뒤 도착한 신호(`at >= deadline`)는 성공으로 치지 않는다 → 'needs-check'
    - `OutcomeSignal`에 알림 창 종류가 없다(타입 시험: `// @ts-expect-error`로 `{ kind: 'dialog', at: 0 }`이 거절됨)
    - `OUTCOME_WAIT_MS`는 8000
    - e2e 6: 같은 페이지 `?inert=1`(처음부터 본문에 "처리 완료", 누름에 반응 안 함) → 8초 뒤 "제출 결과를 알 수 없어요. 사이트에서 직접 확인하세요.", `submit-press` outcome "needs-check", `run:<runId>` status "needs-check", 활동 기록 kind "needs-check"와 template-end outcome "needs-check", 버튼은 한 번만 눌림(`#clicks` "1")
    - e2e 7: `?inputonly=1`(누르면 `textarea#note`에 "완료되었습니다"만 넣음) → 성공으로 치지 않고 8초 뒤 확인 필요
  </behavior>
  <action>
RED 먼저: `tests/unit/submit-outcome.test.ts`와 e2e 6·7을 쓰고 실패 확인 후 RED 커밋.

`submit-outcome.ts`: '완료'는 공백 지운 글자에 '완료되었'·'완료했'이 있을 때만 성공, `OUTCOME_WAIT_MS = 8000`, `judgeOutcome(signals, now, deadline): 'success' | 'waiting' | 'needs-check'`. `outcome-watch.ts`: `INPUT`·`TEXTAREA`·`SELECT`·`[contenteditable]` 안 글자는 건너뛴다(Pitfall 4 — 값이 판정·기록에 섞이지 않게). 오케스트레이터: `confirmSubmit`에서 `outcomeDeadline = pressedAt + OUTCOME_WAIT_MS`를 적고, 신호를 `{ kind, at }`로 모아 `judgeOutcome`으로 판정한다(04-09의 둘 중 하나 규칙을 이 함수로 바꿈). 기한 확인은 `setTimeout(OUTCOME_WAIT_MS)`과 신호가 올 때마다 — SW가 잠들어 타이머가 사라지면 04-18 재개가 기한을 본다. 'needs-check'면 **다시 누르지 않고**(D-23, D-24) `setSubmitOutcome('needs-check')`, status 'needs-check'·`endedAt`, run-index에서 빼고, `logActivity`로 `needs-check { runId, templateId, stepIndex }`와 `template-end { outcome: 'needs-check' }`, 맨 위에 `stopped` 보기(behavior 문구 — 카드 모양은 04-17이 `needs-check` 보기로 바꾼다). 연습 페이지에 `?inert=1`·`?inputonly=1` 처리를 더한다. 통과하면 GREEN, 정리하면 REFACTOR 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/submit-outcome.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 9 tests passed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-submit.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "7 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/submit-outcome.ts`에 `export const OUTCOME_WAIT_MS = 8000`, `export function judgeOutcome`이 있다
    - `grep -vE '^\s*(//|\*|/\*)' src/core/submit-outcome.ts | grep -cE 'dialog-seen|dialog/seen'`가 0
    - `src/worker/run-orchestrator.ts`에 `judgeOutcome(`이 있다
    - `pnpm exec vitest run tests/unit/submit-outcome.test.ts` 9개 이상 통과
    - `CI=true pnpm exec playwright test tests/e2e/template-submit.e2e.ts` 7 passed(확인 필요 때 `#clicks` "1")
  </acceptance_criteria>
  <done>성공 판정 규칙(이동·새 문서·새 성공 글자, 기한, 입력칸 제외)이 단위 시험으로 고정되었고, 결과를 모르면 다시 누르지 않고 확인 필요로 멈춘다(D-40).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 중간 제출·위험 단계 확인 — 보호된 확인 뒤에만, 중간 제출도 누름 기록 먼저</name>
  <files>src/core/run-copy.ts, tests/unit/run-copy.test.ts, src/core/run-state.ts, src/shared/template-messages.ts, src/worker/run-orchestrator.ts, src/page/template/controller.ts, tests/practice-site/workflow-steps.html, tests/e2e/template-submit.e2e.ts</files>
  <read_first>
    - src/worker/run-orchestrator.ts, src/page/template/controller.ts (Task 1·2 결과)
    - src/core/run-state.ts (RunViewSchema, WAITING_VIEW_KINDS, canConfirmSubmit — 04-07·04-09)
    - src/core/danger.ts (isDanger)
    - src/entrypoints/content.ts (위험 버튼 확인 문구·색)
    - .planning/phases/04-templates-activity-log/04-CONTEXT.md (D-12)
  </read_first>
  <behavior>
    - 단위: `stepConfirmCopy({ kind: 'intermediate', pageTitle: '출장 신청', word: '저장', label: '임시저장' })`은 `{ title: '출장 신청을 저장할까요?', confirmLabel: '임시저장', tone: 'primary' }`, 위험 단계는 `{ title: '정말 누를까요?', confirmLabel: '행 삭제', tone: 'danger' }`
    - 단위: `needsConfirm(step, dangerWords)` — submit 'intermediate' true, `danger: true` true, 기록 때 danger가 false여도 지금 글자가 위험 단어면 true, 그 밖 false, 'final'은 이 함수가 아니라 제출 확인 경로(false)
    - 단위(run-state): `isRunTopSender(state, { tabId, frameId })`는 실행 탭의 맨 위 프레임일 때만 true
    - e2e 8: `workflow-steps.html`(title "출장 신청": `#purpose`, `button#delete-row` "행 삭제", `button#save-draft` "임시저장", `button#submit` "신청")에서 기록(목적 입력 → 행 삭제 → 임시저장 → 신청)한 틀 실행 → "정말 누를까요?" 확인에서 Esc → 틀이 멈추고 `#deleted`가 "0"
    - e2e 9: 같은 틀 다시 실행 → 행 삭제 확인 1초 뒤 Enter → "출장 신청을 저장할까요?" 확인 1초 뒤 Enter → storage.local `submit-press:<runId>:<임시저장 인덱스>`가 kind "intermediate"·outcome "passed" → 최종 "출장 신청을 신청할까요?" 확인 → Enter → 결과 페이지, 각 버튼은 한 번씩만(`#deleted` "1", `#drafts` "1")
  </behavior>
  <action>
RED 먼저: 단위 behavior를 `run-copy.test.ts`에(`isRunTopSender`도 이 파일에서 import해 시험 — run-state 시험 파일은 이 계획이 고치지 않는다), e2e 8·9를 `template-submit.e2e.ts`에 더하고 실패 확인 후 RED 커밋.

`run-copy.ts`: `stepConfirmCopy(input)`(D-12, D-38 — 사이트 버튼 글자 그대로), `needsConfirm(step, dangerWords)`(`isDanger(step.label, dangerWords)` 재사용). `run-state.ts`: `isRunTopSender(state, sender)`(`canConfirmSubmit`이 이것을 쓰게 바꿈), `RunViewSchema`에 `confirm-step { runId, stepIndex, title, confirmLabel, tone }`, `WAITING_VIEW_KINDS`에 confirm-step. 메시지: `run/answer { runId, stepIndex, answer: 'confirm' | 'cancel' }`(맨 위 → SW; 라우터는 `isRunTopSender` + 같은 stepIndex일 때만 오케스트레이터 `answerStep(runId, stepIndex, answer)`로). 오케스트레이터: 단계를 보내기 전에 `needsConfirm(step, dangerWords)`(dangerWords는 위 Assumes 규칙)이면 status 'awaiting-step-confirm' + `pendingView` confirm-step. `answerStep` 'confirm': 중간 제출이면 `recordSubmitPress({ kind: 'intermediate' })` await ok → `run/press-submit` → 대상 프레임 `run/step-result` 'done'이면 `setSubmitOutcome('passed')` 후 다음 단계(결과 대기 없음); 위험 단계는 기록 없이 `run/step`. 'cancel'이면 `stopRun(runId, 'user')`. controller는 `confirm-step` 보기에 `openConfirm({ name: confirmLabel, title, tone })` + guard, 결과를 `run/answer`로 한 번만. 연습 페이지 `workflow-steps.html`(behavior대로, 누른 횟수 `#deleted`·`#drafts`, "신청"은 workflow-result.html로 GET). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/run-copy.test.ts tests/unit/run-state.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or any test reported as failed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-submit.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "9 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/run-copy.ts`에 `export function needsConfirm`과 `export function stepConfirmCopy`가 있다
    - `src/worker/run-orchestrator.ts`에 `awaiting-step-confirm`, `kind: 'intermediate'`, `needsConfirm(`이 있다
    - `CI=true pnpm exec playwright test tests/e2e/template-submit.e2e.ts` 9 passed
  </acceptance_criteria>
  <done>제출 성격 단계와 위험 단계는 어느 것도 보호된 확인 없이 눌리지 않고, 중간 제출도 누름 기록이 먼저 남는다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- TMPL-10: edge probe `unclassified — review manually`(이 계획은 04-09 소유 항목의 연장). 가정한 경계(플래그):
  - 성공 낱말 목록과 8초 기한은 Claude's Discretion 출발값. 사이트별 성공 낱말 편집은 넣지 않는다(RESEARCH Open Q3 — 기본 목록만).
  - 누르기 전부터 화면에 있던 성공 글자는 치지 않는다(누른 뒤 새로 붙거나 바뀐 글자만).
  - 페이지가 가짜 성공 글자를 띄우면 성공으로 보일 수 있다 — 그 페이지 자신의 결과 표시이므로 받아들이고, 활동 기록에 남는다.
  - 중간 제출은 결과를 기다리지 않는다(다음 단계 요소가 나타나는지로 이어짐). 결과를 모를 때 다시 누르지 않는 규칙은 같다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 페이지 DOM → 성공 판정 | 페이지가 성공 글자를 흉내 낼 수 있다 |
| main world → 확장 | 알림 창 소식은 흉내 낼 수 있다(판정에 쓰지 않음) |
| 맨 위 확인 화면 → SW(run/answer) | 확인은 필터를 통과한 진짜 키 입력에서만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-21 | Spoofing | 성공 판정 | medium | mitigate | 판정 신호는 확장 쪽 관찰(탭 주소 바뀜, 새 문서 보고, 누른 뒤 새로 붙은 글자)만. main world 소식은 타입에서 빠짐(@ts-expect-error 시험). 입력칸 글자 제외 |
| T-04-23 | Elevation of Privilege | 위험·중간 제출 단계 | high | mitigate | `needsConfirm`이면 보호된 확인 없이 `run/step`을 보내지 않음(e2e 8·9), `run/answer`는 `isRunTopSender` + 같은 단계만 |
| T-04-77 | Tampering | 결과 모름 뒤 다시 누름 | high | mitigate | 'needs-check'에서 다시 누르는 경로가 없음(e2e 6 `#clicks` "1"), 재개 때도 같은 규칙(04-18) |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 설치 없음(D-05) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/submit-outcome.test.ts tests/unit/run-copy.test.ts tests/unit/run-state.test.ts` 통과
- `CI=true pnpm exec playwright test tests/e2e/template-submit.e2e.ts tests/e2e/template-run.e2e.ts` 통과(template-submit 9)
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 이동·성공 글자로 성공을 판정하고 아니면 다시 누르지 않고 확인 필요로 멈춘다(ROADMAP 성공 기준 2의 앞부분, TMPL-10)
- 중간 제출·위험 단계도 보호된 확인 뒤에만 눌린다(D-12)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/submit-outcome.ts`, `src/page/run/outcome-watch.ts`, `tests/unit/submit-outcome.test.ts`, `tests/practice-site/workflow-inline-success.html`, `tests/practice-site/workflow-steps.html`
- 심볼: `SUCCESS_WORDS`, `isSuccessText()`, `OUTCOME_WAIT_MS`, `OutcomeSignal`, `judgeOutcome()`, `watchSuccessText()`, `stepConfirmCopy()`, `needsConfirm()`, `isRunTopSender()`, 오케스트레이터 `answerStep()`
- 메시지 type: `run/outcome`, `run/answer`; RunView kind `confirm-step`

<output>
Create `.planning/phases/04-templates-activity-log/04-10-SUMMARY.md` when done
</output>
