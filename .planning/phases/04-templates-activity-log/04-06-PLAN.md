---
phase: 04-templates-activity-log
plan: 06
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - src/core/settings-schema.ts
  - tests/unit/settings-schema.test.ts
  - src/shared/messages.ts
  - src/entrypoints/background.ts
  - src/worker/storage-writer.ts
  - src/core/submit-word.ts
  - tests/unit/submit-word.test.ts
  - src/worker/template-router.ts
  - src/entrypoints/options/submit-section.ts
  - src/entrypoints/options/template-speed-section.ts
  - src/entrypoints/options/main.ts
  - tests/e2e/options-template.e2e.ts
  - tests/e2e/template-helpers.ts
autonomous: true
requirements: [TMPL-03, TMPL-08]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "옵션 페이지에서 사이트별 제출 단어를 정하면 그 사이트에서 기록한 틀의 저장 화면 2/3 판별에 쓰인다 — 예: practice.test에 '결재올림'을 정하면 '결재올림' 버튼이 제출 단계로 표시된다(D-12, TMPL-03)"
    - "사이트별 제출 단어가 있으면 그 사이트에서는 전역 목록을 대신한다. 없으면 전역 목록(설정), 설정이 없으면 기본 단어다"
    - "전역 제출 단어와 틀 기본 속도(천천히·보통·최대, 기본 '보통')가 설정(storage.sync)에 들어가고 옵션 페이지에서 키보드만으로 고친다(D-04, D-19, TMPL-08)"
    - "설정 형식이 한 번 올라가며(이 계획 전 버전 + 1), 이전 형식 설정은 값을 잃지 않고 바뀐다. 변환이 실패하면 원래 값을 두고 알린다(D-03)"
    - "제출 단어·틀 속도 저장 요청은 확장 페이지(옵션 페이지)에서 온 것만 받는다 — 웹 페이지 쪽 content script는 바꿀 수 없다"
    - "뒤 계획 e2e가 틀을 실제 기록 화면으로 만들도록 tests/e2e/template-helpers.ts에 recordTemplate()이 있다(명령판 기록 → 동작 → 저장 화면 세 장을 번호·Enter로 → 저장된 틀 id·name을 돌려줌)"
  artifacts:
    - path: "src/core/settings-schema.ts"
      provides: "설정 형식 한 단계 올림(submitWords·templateSpeed) + 사이트 항목 submitWords?"
      contains: "templateSpeed"
    - path: "src/core/submit-word.ts"
      provides: "사이트·전역·기본 제출 단어 고르기"
      exports: ["submitWordsFor"]
    - path: "src/entrypoints/options/submit-section.ts"
      provides: "옵션 페이지 '제출 단어' 절(전역 + 지금 사이트)"
      exports: ["submitSection"]
    - path: "src/entrypoints/options/template-speed-section.ts"
      provides: "옵션 페이지 '틀 기본 속도' 절"
      exports: ["templateSpeedSection"]
    - path: "tests/e2e/template-helpers.ts"
      provides: "실제 기록 화면으로 틀을 만드는 e2e 도우미"
      exports: ["recordTemplate"]
  key_links:
    - from: "src/worker/template-router.ts"
      to: "src/core/submit-word.ts"
      via: "record/stop 답의 draft.submitWords와 template/save 검증에 submitWordsFor(settings, siteEntry(startUrl의 출처))"
      pattern: "submitWordsFor\\("
    - from: "src/entrypoints/background.ts"
      to: "src/worker/storage-writer.ts"
      via: "isExtensionPage(sender) 뒤에만 setSubmitWords·setTemplateSpeed·updateSite(submitWords)"
      pattern: "setTemplateSpeed"
    - from: "src/core/settings-schema.ts"
      to: "migrate()"
      via: "settingsSpec.migrations의 새 자리가 이전 버전 → 새 버전(기본값 채움)"
      pattern: "migrations"
  prohibitions:
    - statement: "웹 페이지 쪽 content script에서 온 요청으로 제출 단어·틀 속도를 바꾸지 않는다"
      status: resolved
      verification: test
    - statement: "설정 형식 변환은 이전 값을 잃지 않는다(입력 불변, 실패 시 원본 보존)"
      status: resolved
      verification: test
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 반복 업무를 한 번 해 보여 틀로 저장하고 이후 "틀 고르기 1번 + 바뀌는 값 2~3번 + 제출 확인 1번"으로 끝내기를, **so that** 되돌릴 수 없는 제출은 항상 확인 뒤에 한 번만 일어나고 문제가 생기면 활동 기록으로 추적할 수 있다.

<objective>
사이트별·전역 제출 단어와 틀 기본 속도를 설정에 더하고 옵션 페이지에서 고치게 한다. 설정 형식을 한 번 올린다.

Purpose: TMPL-03(D-12 "사이트별로 고칠 수 있음"), TMPL-08의 기본값(D-19 "기본값은 전체 설정(sync)"). 실행 속도 동작은 04-13이 이 값을 읽는다.
Output: 설정 형식 올림 + migrations 새 자리, 사이트 항목 `submitWords?`, `submitWordsFor`, 저장 요청 `setSubmitWords`·`setTemplateSpeed`, 옵션 페이지 절 둘, e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-activity-log/04-CONTEXT.md
@.planning/phases/04-templates-activity-log/04-05-SUMMARY.md
@docs/design/SYSTEM.md
@docs/design/tokens.css
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 패키지·새 확장 권한 없음(D-05). 필요해지면 멈추고 `checkpoint:decision`으로 이유 한 줄 + 승인을 받는다.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- tracer·`tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(04-06): …` → 최소 구현 → GREEN 커밋 `feat(04-06): …` → 정리하면 `refactor(04-06): …`. 제목 영어 접두어, 본문 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드).
- 옵션 페이지는 Phase 3 절 모양 그대로(새 색·서체·radius 없음, tokens.css 변수만 — D-36). 카피 규칙(D-38).
- 저장 API 쓰기·지우기는 `src/worker/storage-writer.ts`의 `enqueue()` 안에만(D-03).
- 연습 사이트에서만 시험(D-39). 기존 파일은 이 계획이 필요한 줄만 바꾼다.

## Assumes from Phase 3 (브랜치 `origin/claude/phase3-plans-ng6f32`의 계획 이름 — 실제 이름이 다르면 실행 전에 고친다)

- 설정 형식 2: `settingsSpec`(current 2, `migrations[1]` = v1→v2), 사이트 항목 스키마 `siteSpec`(키 `site:<origin>`), 전역 `dangerWords`.
- 저장 요청은 `storage/request`의 op 유니온: `setDangerWords { words }`, `updateSite { origin, patch: { phrases?, dangerWords?: string[] | null, sensitiveOverrides?, pins? } }`(`null`이면 필드 삭제), `setSensitiveWords` 등. `background.ts`의 `isExtensionPage(sender)`(확장 URL로 시작) 뒤에서만 설정 화면 op를 받는다(03-18).
- 옵션 페이지: `src/entrypoints/options/main.ts`가 `createOptionsUi({ root, sections })`(`ui.ts`)에 절 `{ number, title, render(ctx) }`를 넘긴다. 예 `dangerSection`(`danger-section.ts` — 전역 목록 줄 + "이 사이트만 따로 정하기"/"전체 목록 따르기"). 지금 사이트는 `?site` 매개변수(ui.ts의 "사이트: {출처}" 줄).

## 설정 형식 번호 규칙 (Phase 5와 함께)

이 계획은 설정 형식을 **지금 버전 + 1**로 올린다. Phase 3 뒤라면 v2 → v3(`settingsSpec.migrations[2]`)다. Phase 5의 `keymap.openBoard`(설정 형식 올림)가 먼저 들어가 있으면 그 다음 번호(v4, `migrations[3]`)를 쓴다 — 둘 중 **나중에 들어가는 쪽이 올린다**. 실행 시작 때 `CURRENT_SCHEMA_VERSION`(또는 `settingsSpec.current`) 값을 SUMMARY에 적고 끝 값과 함께 남긴다.

<tasks>

<task type="tracer">
  <name>Task 1: 끝에서 끝까지 — 옵션 페이지에서 practice.test 제출 단어 "결재올림" → 기록 → 저장 화면 2/3에 "결재올림 제출 단계"</name>
  <files>src/core/settings-schema.ts, src/shared/messages.ts, src/worker/storage-writer.ts, src/core/submit-word.ts, src/worker/template-router.ts, src/entrypoints/options/submit-section.ts, src/entrypoints/options/main.ts, tests/e2e/options-template.e2e.ts, tests/e2e/template-helpers.ts</files>
  <read_first>
    - tests/e2e/template-helpers.ts (openPaletteSlot, readStorage, writeStorage — 04-02), tests/e2e/template-save.e2e.ts (저장 화면 세 장을 키로 넘기는 순서 — 04-05)
    - src/core/settings-schema.ts (Phase 3 v2, siteSpec, settingsSpec)
    - src/shared/messages.ts (storage/request op 유니온, updateSite 패치)
    - src/worker/storage-writer.ts (updateSite — 패치 적용·크기 확인)
    - src/entrypoints/background.ts (isExtensionPage)
    - src/worker/template-router.ts (04-05 — record/stop 답의 draft.submitWords, template/save의 buildTemplateSteps)
    - src/core/submit-word.ts (DEFAULT_SUBMIT_WORDS — 04-05)
    - Phase 3: src/entrypoints/options/main.ts, ui.ts, danger-section.ts (절 모양, 사이트 줄), tests/e2e/options-sections.e2e.ts (옵션 페이지 여는 법)
  </read_first>
  <behavior>
    - e2e 1: 옵션 페이지(`?site=http://practice.test`)의 "제출 단어" 절 → "이 사이트만 따로 정하기" → 단어 칸에 "결재올림" → Enter → storage.sync `site:http://practice.test` 항목의 `submitWords`가 `['결재올림']` → `workflow-form.html?altsubmit=1`에서 기록 → `#amount` 입력 → `#submit`("결재올림") → 둘째 장 4 → Enter(1/3 넘김) → 2/3에 "1 결재올림 제출 단계"
    - e2e 2: 같은 절 "전체 목록 따르기" → 사이트 항목 `submitWords` 없음 → 같은 기록의 2/3에 "제출 버튼을 찾지 못했어요"
  </behavior>
  <action>
RED 먼저: `tests/e2e/options-template.e2e.ts`에 behavior 둘을 쓰고 실패 확인 후 RED 커밋.

0) e2e 도우미(시험 도구, 제품 코드 아님): `tests/e2e/template-helpers.ts`에 `recordTemplate(page, serviceWorker, { startUrl, act, flip?, finalPick?, name? }): Promise<{ id: string; name: string }>` — `startUrl`로 가서 `openPaletteSlot(page, 2, 4)`로 기록을 켜고, `act(page)`(이용자 동작)를 부르고, 둘째 장 4로 끝낸 뒤 저장 화면 1/3에서 `flip`의 번호를 차례로 눌러 매번 바뀜을 바꾸고 Enter, 2/3에서 `finalPick`이 있으면 그 번호 뒤 Enter, 3/3에서 `name`이 숫자면 그 번호·글자면 9 → 글자 → Enter; 끝으로 `readStorage(serviceWorker, 'local', 'templates')`에서 마지막 틀의 id·name을 돌려준다. 이 계획의 e2e 1·2가 이것으로 기록한다. 04-07부터 모든 틀 e2e가 이 도우미로 틀을 만들고, 모양이 틀린 틀(예: 시작 주소 바꾸기)은 `readStorage`로 읽어 고친 뒤 `writeStorage`로 되쓴다.


1) `settings-schema.ts`: 사이트 항목 스키마에 선택 필드 `submitWords?: string[]`(단어 1~20자, 최대 30개)를 더한다 — 선택 필드라 사이트 항목 형식 버전은 그대로(옛 항목은 그 필드가 없을 뿐). `messages.ts`: `updateSite` 패치에 `submitWords?: string[] | null`. `storage-writer.ts`: `updateSite`가 이를 Phase 3 `dangerWords`와 같은 방식으로 적용(`null`이면 필드 삭제, 검사·크기 확인 그대로).

2) `submit-word.ts`: `submitWordsFor(settings, siteEntry: SiteEntry | undefined): string[]` — 이 작업은 `siteEntry?.submitWords ?? DEFAULT_SUBMIT_WORDS`(전역 목록은 Task 2). `template-router.ts`: `record/stop`의 `draft.submitWords`와 `template/save`의 `buildTemplateSteps` 단어를 `submitWordsFor(settings, siteEntry)`로 — 사이트 항목은 세션 `startUrl`의 출처로 `chrome.storage.sync`에서 읽는다(읽기만; 없거나 형식 오류면 undefined). 저장 화면 파일은 고치지 않는다(04-05가 답의 단어를 쓰게 해 두었다).

3) `src/entrypoints/options/submit-section.ts`: `submitSection = { number, title: '제출 단어', render(ctx) }` — Phase 3 `dangerSection`과 같은 모양: 지금 사이트 줄 "이 사이트만 따로 정하기"(누르면 사이트 항목 `submitWords`를 지금 적용 목록의 복사본으로 만들고 편집)/"전체 목록 따르기"(`submitWords: null`), 단어 줄 고르면 [1 빼기], "단어 더하기"(글 입력 줄, Enter 저장). 안내 한 줄 "여기 단어가 든 버튼을 제출 단계로 봐요."(D-38 알림형). 번호는 Phase 3 절 번호 다음 빈 번호. `main.ts`가 절을 등록한다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/options-template.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "2 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/submit-word.ts`에 `export function submitWordsFor`가 있고, `src/worker/template-router.ts`에 `submitWordsFor(`가 있다
    - `tests/e2e/template-helpers.ts`에 `export async function recordTemplate`이 있고 `tests/e2e/options-template.e2e.ts`가 그것을 쓴다
    - `src/entrypoints/options/main.ts`에 `submitSection`이 있다
    - `git log --oneline -F --grep='(04-06)' -- src/page/overlay/template-save.ts`가 아무것도 출력하지 않는다(이 계획의 커밋이 저장 화면 파일을 고치지 않음)
    - `CI=true pnpm exec playwright test tests/e2e/options-template.e2e.ts` 2 passed
  </acceptance_criteria>
  <done>옵션 페이지에서 정한 사이트별 제출 단어가 그 사이트 기록의 제출 단계 판별에 쓰인다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 설정 형식 올림 — 전역 제출 단어·틀 기본 속도, 변환·저장 요청 문지기, 옵션 페이지 "틀 기본 속도"</name>
  <reversibility rating="costly">설정 형식은 sync 저장소에 남아 다른 PC와 이후 형식 변환이 읽는다(Phase 5 번호 규칙은 위 절)</reversibility>
  <files>src/core/settings-schema.ts, tests/unit/settings-schema.test.ts, src/shared/messages.ts, src/entrypoints/background.ts, src/worker/storage-writer.ts, src/core/submit-word.ts, tests/unit/submit-word.test.ts, src/entrypoints/options/submit-section.ts, src/entrypoints/options/template-speed-section.ts, src/entrypoints/options/main.ts, tests/e2e/options-template.e2e.ts</files>
  <read_first>
    - src/core/settings-schema.ts (Task 1 결과, settingsSpec.migrations, defaultSettings)
    - tests/unit/settings-schema.test.ts (기존 시험)
    - src/worker/storage-writer.ts (sync 항목 크기 확인 — 01-14 SYNC_ITEM_LIMIT)
    - src/entrypoints/background.ts (isExtensionPage 뒤 op 분기)
    - src/entrypoints/options/submit-section.ts (Task 1 결과)
    - docs/design/SYSTEM.md (번호 카드, 선택 표시 남색 3px)
  </read_first>
  <behavior>
    - 단위: 이전 형식 설정(Phase 3 기본값)을 `migrate(raw, settingsSpec)` → `{ ok: true, migrated: true }`, 결과에 `submitWords`가 `DEFAULT_SUBMIT_WORDS`, `templateSpeed`가 'normal'이고 다른 값은 그대로(깊은 비교), 입력 객체 불변
    - 단위: 새 형식에서 `templateSpeed: 'fast'`(모르는 값)는 'invalid'
    - 단위: `defaultSettings()`의 schemaVersion이 `CURRENT_SCHEMA_VERSION`이고 `templateSpeed` 'normal'
    - 단위: `submitWordsFor` — 사이트 항목 `submitWords`가 있으면 그것(전역을 대신함), 없으면 `settings.data.submitWords`, 사이트 항목 `submitWords: []`이면 빈 목록(제출 단계 없음)
    - e2e 3: 옵션 페이지 "제출 단어" 절 전역 목록에 "품의" 더하기 → sync `settings`의 `submitWords`에 "품의"
    - e2e 4: 옵션 페이지 "틀 기본 속도" 절에 번호 카드 "1 천천히", "2 보통"(선택 표시), "3 최대" → 3 → sync `settings`의 `templateSpeed`가 'max', "3 최대"가 선택 표시
    - e2e 5: 연습 페이지 content script 쪽에서 `chrome.runtime.sendMessage({ type: 'storage/request', op: { kind: 'setTemplateSpeed', speed: 'max' } })`(Phase 3 op 모양)를 보내면 거절되고 `templateSpeed`는 그대로
  </behavior>
  <action>
RED 먼저: 단위 behavior를 `settings-schema.test.ts`·`submit-word.test.ts`에(기존 시험 유지), e2e 3~5를 `options-template.e2e.ts`에 쓰고 실패 확인 후 RED 커밋.

`settings-schema.ts`(D-03, D-04, D-19): 위 "설정 형식 번호 규칙"대로 `CURRENT_SCHEMA_VERSION`을 하나 올리고 새 데이터 = 이전 데이터 + `submitWords: string[]`, `templateSpeed: 'slow' | 'normal' | 'max'`. `settingsSpec.migrations`의 새 자리는 이전 → 새(기본값 채움, 입력 불변). `defaultSettings()`도 새 형식. `submitWordsFor`는 behavior대로 전역 목록을 쓴다. `messages.ts`: op `setSubmitWords { words: string[] }`, `setTemplateSpeed { speed }`. `background.ts`: 둘 다 `isExtensionPage(sender)` 뒤에만 writer로. `storage-writer.ts`: 둘 다 설정을 읽어 그 필드만 바꿔 새 스키마로 다시 검사 후 쓴다(sync 항목 크기 확인 그대로).

옵션 페이지: `submit-section.ts`에 전역 목록 줄(고르면 [1 빼기]) + "단어 더하기"를 더한다. `src/entrypoints/options/template-speed-section.ts`: `templateSpeedSection = { number, title: '틀 기본 속도', render(ctx) }` — 번호 카드 1 천천히·2 보통·3 최대, 고른 카드 남색 3px(SYSTEM.md 선택 표시), 안내 "틀마다 따로 정한 속도가 먼저예요."; 번호를 누르면 `setTemplateSpeed`. 키보드만으로 다 된다. `main.ts`가 절을 등록한다. 통과하면 GREEN, 정리하면 REFACTOR 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/settings-schema.test.ts tests/unit/submit-word.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or any test reported as failed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/options-template.e2e.ts tests/e2e/template-save.e2e.ts</automated>
    <fails_when>non-zero exit, or fewer than 5 options-template tests passed, or any template-save test failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/settings-schema.ts`에 `templateSpeed`와 `submitWords`가 있고, `CURRENT_SCHEMA_VERSION`이 이 계획 시작 때 값보다 1 크다(SUMMARY에 전·후 값)
    - `src/entrypoints/background.ts`에서 `setSubmitWords`·`setTemplateSpeed`가 `isExtensionPage` 확인 뒤에만 처리된다
    - `src/entrypoints/options/main.ts`에 `templateSpeedSection`이 있다
    - `pnpm exec vitest run tests/unit/settings-schema.test.ts tests/unit/submit-word.test.ts` 전체 통과
    - `CI=true pnpm exec playwright test tests/e2e/options-template.e2e.ts` 5 passed
  </acceptance_criteria>
  <done>전역·사이트별 제출 단어와 틀 기본 속도가 새 설정 형식에 들어가고, 옵션 페이지에서만 바뀐다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- TMPL-03(04-05 소유 항목의 연장). 가정한 경계(플래그):
  - 사이트별 제출 단어가 있으면 그 사이트에서는 전역 목록을 대신한다(더하기가 아님) — 기본 단어를 빼고 싶은 사이트를 위해. 빈 목록이면 그 사이트에는 제출 단계가 표시되지 않는다(위험 버튼 확인은 그대로).
  - 사이트는 기록을 시작한 페이지(`startUrl`)의 출처로 고른다. iframe 안 다른 출처 버튼도 시작 페이지 출처의 단어로 판별한다.
- TMPL-08(04-13 소유): 기본 속도 값만 이 계획. 틀마다 정한 속도(`TemplateV1.speed`)가 있으면 그것이 먼저다(04-13·04-22).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 옵션 페이지 → SW(storage/request) | 이용자가 적은 단어·속도 |
| 웹 페이지 content script → SW | 설정 화면 op를 흉내 낼 수 있다 |
| storage.sync ← 다른 PC | 다른 형식 버전의 설정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Tampering | 설정 형식 변환 | medium | mitigate | migrations 새 자리 입력 불변 + 실패 시 원본 보존(01-14 경로) 단위 시험 |
| T-04-13 | Denial of Service | submitWords 크기 | low | mitigate | 단어 1~20자·30개 상한 zod + sync 항목 8KB 한도 검사(01-14 `SYNC_ITEM_LIMIT`) 그대로 |
| T-04-72 | Elevation of Privilege | setSubmitWords·setTemplateSpeed·updateSite(submitWords) | high | mitigate | `isExtensionPage(sender)` 뒤에만(e2e 5가 content 쪽 요청 거절을 확인) — 웹 페이지가 제출 단어를 비워 제출 확인을 빼게 만들 수 없다 |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 설치 없음(D-05) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/settings-schema.test.ts tests/unit/submit-word.test.ts` 전부 통과
- `CI=true pnpm exec playwright test tests/e2e/options-template.e2e.ts tests/e2e/template-save.e2e.ts` 통과
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 제출 단어를 사이트별로 고칠 수 있다(TMPL-03, D-12)
- 틀 기본 속도가 전체 설정에 있다(TMPL-08의 기본값, D-19)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/entrypoints/options/submit-section.ts`, `src/entrypoints/options/template-speed-section.ts`, `tests/e2e/options-template.e2e.ts`
- 심볼: e2e 도우미 `recordTemplate()`, `submitWordsFor()`, `submitSection`, `templateSpeedSection`, 저장 요청 `setSubmitWords`·`setTemplateSpeed`, `updateSite` 패치 `submitWords`
- 설정 필드(sync `settings`, 형식 한 단계 올림): `submitWords`, `templateSpeed`; 사이트 항목 `submitWords?`; `settingsSpec.migrations` 새 자리

<output>
Create `.planning/phases/04-templates-activity-log/04-06-SUMMARY.md` when done
</output>
