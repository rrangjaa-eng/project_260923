---
phase: 04-templates-activity-log
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - docs/design/SYSTEM.md
  - src/page/overlay/template-save.ts
  - src/page/template/controller.ts
  - src/page/palette/palette-controller.ts
  - src/shared/template-messages.ts
  - src/worker/template-router.ts
  - src/core/record-step.ts
  - tests/unit/record-step.test.ts
  - tests/unit/template-schema.test.ts
  - src/core/frame-tree.ts
  - tests/unit/frame-tree.test.ts
  - src/worker/relay.ts
  - tests/e2e/template-record.e2e.ts
  - tests/e2e/template-save.e2e.ts
autonomous: true
requirements: [TMPL-01]
estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "기록 중에는 명령판 둘째 장 4 카드 글자가 '기록 끝내기'로 바뀌고(자리는 그대로), 저장하거나 버리면 '틀 기록'으로 돌아온다(D-06)"
    - "기록 끝내기 → '틀 저장' 화면(이 계획: 이름 장)이 뜨고, 이름 후보 번호 카드(1 = 마지막 누름 단계의 페이지 제목)와 '9 직접 입력'이 보인다. 번호를 누르면 틀이 storage.local 'templates'에 저장되고 '틀을 저장했어요'가 뜬다(D-06, D-11, TMPL-01)"
    - "저장 화면에서 Esc를 누르면 화면만 닫히고 끝난 기록은 남는다 — 같은 탭에서 둘째 장 4를 다시 고르면 저장 화면이 다시 뜬다(버리기 확인은 04-05)"
    - "기록 단계는 정해진 규칙으로 합쳐진다: 같은 칸은 마지막 값만, 칸을 누른 것은 버림, 체크는 마지막 상태만, 연달은 이동은 마지막만, 1000ms 안 같은 요소 두 번 누름은 한 번, 한 번 직접 입력 단계가 된 칸은 값이 붙지 않음, 300단계 상한(D-07, D-10)"
    - "같은 출처·다른 출처 iframe 안 칸이 각각 프레임 경로 ['id:form-frame']·['id:other-frame']와 프레임 주소로 기록된다 — 경로는 페이지가 보낸 값이 아니라 SW가 프레임 보고로 계산한다(D-07, D-09)"
    - "경로를 모르는 프레임의 단계는 저장하지 않고 '이 칸의 위치를 몰라 기록하지 못했어요. 다시 입력하세요.'를 보여 준다"
  artifacts:
    - path: "src/core/record-step.ts"
      provides: "기록 이벤트 → 단계 목록 합성(순수)"
      exports: ["appendRecordedStep", "MAX_RECORD_STEPS", "PRESS_DEDUP_MS"]
    - path: "src/core/frame-tree.ts"
      provides: "frameId → 프레임 경로 계산"
      exports: ["framePathForFrameId"]
    - path: "src/page/overlay/template-save.ts"
      provides: "'틀 저장' 화면(이 계획: 이름 장)"
      exports: ["openTemplateSave", "closeTemplateSave"]
    - path: "src/worker/relay.ts"
      provides: "탭별 맞춘 프레임 보고 읽기"
      contains: "resolvedFramesFor"
    - path: "src/page/palette/palette-controller.ts"
      provides: "명령 카드 글자 덮어쓰기(자리 고정)"
      contains: "setCommandLabel"
  key_links:
    - from: "src/worker/template-router.ts"
      to: "src/core/record-step.ts"
      via: "record/step마다 appendRecordedStep(session.steps, step, { at, lastPress }) 후 writer.writeRecordSession"
      pattern: "appendRecordedStep\\("
    - from: "src/worker/template-router.ts"
      to: "src/core/frame-tree.ts"
      via: "framePathForFrameId(relay.resolvedFramesFor(tabId), sender.frameId)로 단계의 framePath를 채움"
      pattern: "framePathForFrameId\\("
    - from: "src/page/template/controller.ts"
      to: "src/page/overlay/template-save.ts"
      via: "record/stop 답의 draft로 openTemplateSave → 번호 선택 → template/save"
      pattern: "openTemplateSave\\("
    - from: "src/page/template/controller.ts"
      to: "Phase 3 palette-controller"
      via: "palette.setCommandLabel('template-record', '기록 끝내기' | null)"
      pattern: "setCommandLabel\\("
  prohibitions:
    - statement: "페이지가 보낸 프레임 경로를 틀에 쓰지 않는다 — 경로는 SW가 relay 보고로 계산하고, 모르면 단계를 버린다"
      status: resolved
      verification: test
    - statement: "기록 세션 단계는 MAX_RECORD_STEPS(300)를 넘지 않는다"
      status: resolved
      verification: test
    - statement: "민감 판별로 직접 입력 단계가 된 칸에는 뒤에 온 값이 붙지 않고, 먼저 붙은 값도 지워진다"
      status: resolved
      verification: test
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 반복 업무를 한 번 해 보여 틀로 저장하고 이후 "틀 고르기 1번 + 바뀌는 값 2~3번 + 제출 확인 1번"으로 끝내기를, **so that** 되돌릴 수 없는 제출은 항상 확인 뒤에 한 번만 일어나고 문제가 생기면 활동 기록으로 추적할 수 있다.

<objective>
04-03의 기록 경로에 "틀 저장" 화면(이름 장)을 끼우고, 기록 단계 합치기 규칙과 iframe 안 동작 기록을 더한다.

Purpose: TMPL-01(D-06, D-07, D-09, D-10, D-11의 이름 부분). 저장 화면의 바뀌는 값·제출 단계 장은 04-05가 이 화면에 더한다.
Output: 이름 장 저장 화면, "기록 끝내기" 카드 글자, `appendRecordedStep` + 단위 시험, 틀 형식 단위 시험, `framePathForFrameId` + 단위 시험, relay 읽기, e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-activity-log/04-CONTEXT.md
@.planning/phases/04-templates-activity-log/04-RESEARCH.md
@.planning/phases/04-templates-activity-log/04-03-SUMMARY.md
@docs/design/SYSTEM.md
@docs/design/tokens.css
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 패키지·새 확장 권한 없음(D-05). 필요해지면 멈추고 `checkpoint:decision`으로 이유 한 줄 + 승인을 받는다.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- tracer·`tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(04-04): …` → 최소 구현 → GREEN 커밋 `feat(04-04): …` → 정리하면 `refactor(04-04): …`. 제목 영어 접두어, 본문 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드).
- 오버레이는 Shadow DOM 안, `docs/design/tokens.css` 변수만(D-36). 새 화면은 먼저 SYSTEM.md에 템플릿을 더한다(D-37, DESIGN.md §4-1). 카피는 SYSTEM.md 카피 규칙(D-38).
- 저장 API 쓰기·지우기는 `src/worker/storage-writer.ts`의 `enqueue()` 안에만(D-03).
- 연습 사이트에서만 시험(D-39). 기존 파일은 이 계획이 필요한 줄만 바꾼다.

## Assumes from Phase 3 (브랜치 `origin/claude/phase3-plans-ng6f32`의 계획 이름 — 실제 이름이 다르면 실행 전에 고친다)

- `src/page/palette/palette-controller.ts`의 `createPaletteController(...)` → `registerCommand(id, run)`, `setStatus(text)`, `pushPanel`/`popPanel`. 카드 글자는 `src/core/palette-slots.ts`의 `COMMAND_LABELS`에서 온다. 명령별 글자를 덮어쓰는 자리가 없으므로 이 계획이 `setCommandLabel(id, label | null)` 하나를 더한다(자리·순서·흐림 규칙은 그대로).
- 오버레이 명령판 카드 DOM `.palette-card[data-number]`(`src/page/overlay/palette.ts`).

<tasks>

<task type="tracer">
  <name>Task 1: 끝에서 끝까지 — 기록 중 "기록 끝내기" 글자 → 끝내기 → "틀 저장" 이름 장 → 번호로 저장</name>
  <precondition>`.planning/phases/04-templates-activity-log/04-03-SUMMARY.md`가 있고 `tests/e2e/template-record.e2e.ts`가 5 passed</precondition>
  <files>docs/design/SYSTEM.md, src/page/overlay/template-save.ts, src/page/template/controller.ts, src/page/palette/palette-controller.ts, src/shared/template-messages.ts, src/worker/template-router.ts, tests/e2e/template-record.e2e.ts, tests/e2e/template-save.e2e.ts</files>
  <read_first>
    - docs/design/SYSTEM.md (확인 화면 템플릿, 번호 카드, 상태 표 "틀 기록" 줄 — 04-03)
    - docs/DESIGN.md (§4)
    - src/page/template/controller.ts, src/worker/template-router.ts, src/shared/template-messages.ts (04-03 결과)
    - src/page/overlay/confirm-dialog.ts (가운데 카드 모양, `--dialog-width`, 키 칩 버튼)
    - src/page/input/pipeline.ts (setModal — 필터를 통과한 키만 모달이 받는다)
    - Phase 3: src/page/palette/palette-controller.ts, src/page/overlay/palette.ts, src/core/palette-slots.ts (COMMAND_LABELS)
    - tests/e2e/template-helpers.ts (openPalette, openPaletteSlot, readStorage — 04-02)
  </read_first>
  <behavior>
    - template-record e2e 1(고침): 기록을 켠 뒤 `openPalette` → 9 → 둘째 장 카드 4 글자가 "기록 끝내기"
    - template-record e2e 2(고침): 흐름을 한 뒤 둘째 장 4 → "틀 저장" 화면이 보이고, storage.local `templates`는 그대로, `record-session`의 `stopped`가 true
    - template-save e2e 1: 흐름(메뉴 → 일자·금액 → 상신) 뒤 둘째 장 4 → 화면에 "1 지출결의서", "9 직접 입력" → 1 → "틀을 저장했어요" → `templates`에 틀 1개(name "지출결의서", 단계 6개), `record-session` 없음 → 다시 명령판 둘째 장 카드 4 글자가 "틀 기록"
    - template-save e2e 2: 같은 흐름 뒤 9 → 오버레이 글자 칸에 "월말 지출" 입력 → Enter → 저장된 name "월말 지출"
    - template-save e2e 3: 같은 흐름 뒤 저장 화면에서 Esc → 화면 닫힘, `record-session` 남음(stopped true), `templates` 그대로 → 둘째 장 4 → 저장 화면이 다시 보임
  </behavior>
  <action>
RED 먼저: `template-record.e2e.ts`의 e2e 1·2를 behavior대로 고치고, `tests/e2e/template-save.e2e.ts`를 만들어 behavior 셋을 쓰고 실패 확인 후 RED 커밋.

1) SYSTEM.md(D-37, DESIGN.md §4-1): "오버레이 템플릿"에 **틀 저장** 화면을 더한다 — 확인 화면과 같은 가운데 카드(`--dialog-width`), 제목 줄에 장 표시(세 장 "1/3 바뀌는 값 · 2/3 제출 단계 · 3/3 이름" — 이 계획은 이름 장만 만들어 "틀 저장 · 이름"으로 보이고 04-05가 앞 두 장을 더한다), 본문은 번호 카드 목록(큰 숫자 22px 700 + 글자), 아래 키 칩 버튼 [Enter 다음] [Esc 닫기](04-05에서 1/3의 Esc는 "기록을 버릴까요?" 확인, 2/3·3/3의 Esc는 앞 장). 이름 장 = 이름 후보 번호 카드 + 마지막 "9 직접 입력"(누르면 같은 카드 안에 글자 칸, [Enter 저장]). 상태 표 "틀 기록" 줄에 가득 참 "단계가 너무 많아요. 기록을 끝내세요."를 더한다(Task 2가 씀). 새 색·radius·그림자 없음.

2) `palette-controller.ts`: `setCommandLabel(id: PaletteCommandId, label: string | null)` — 명령별 글자 덮어쓰기 표(null이면 `COMMAND_LABELS` 기본으로). 명령판이 열려 있으면 다시 그린다. 자리·순서·흐림 규칙은 바꾸지 않는다(D-06 고정 자리).

3) 메시지(`template-messages.ts`): `record/stop` 답을 `{ ok: true, empty: true } | { ok: true, draft: { startUrl, startTitle, steps } } | { ok: false, reason }`로 바꾸고 `template/save { name: string(1~60자, 앞뒤 공백 없앤 뒤) }`를 더한다. 라우터: `record/stop`은 세션을 `stopped: true`로 적고 `record/state { recording: false }` 방송 뒤, 단계 0개면 세션을 지우고 `empty`, 아니면 세션을 남긴 채 `draft`를 답한다(04-03의 곧바로 저장을 이것으로 바꾼다). 이미 `stopped`인 같은 탭 세션에 `record/stop`이 오면 같은 `draft`를 다시 답한다(저장 화면 다시 열기). `template/save`는 `sender.frameId === 0`이고 `sender.tab.id === session.tabId`이며 세션이 `stopped`일 때만 — 04-03의 틀 만들기(이름만 이 값으로)를 옮겨 `writer.saveTemplate` 성공 뒤 `clearRecordSession()`, `{ ok: true, saved: { id, name } }`. 끝나지 않은 세션이 있는 동안 다른 탭의 `record/start`는 04-03대로 'already-recording'(끝났지만 저장 전인 세션도 포함).

4) `template-save.ts`: `openTemplateSave({ draft, onSave(name): Promise<{ ok: boolean }>, onClose() })`, `closeTemplateSave()`. 후보 1 = 마지막 press 단계의 `pageTitle`, 비었으면 `startTitle`. 9 = 직접 입력 — 오버레이 안 글자 칸을 열고 포커스, 그동안 숫자 키는 글자로 들어가고 Enter가 저장, Esc는 글자 칸만 닫는다. Esc(글자 칸 밖) = `onClose`. 키는 `pipeline.setModal`로 받는다(D-02 필터를 통과한 입력만). `controller.ts`: 기록을 켜면 `palette.setCommandLabel('template-record', '기록 끝내기')`; 같은 명령에서 이 탭 세션이 있으면(기록 중이든 저장 전이든) `record/stop` → `empty`면 04-03 문구, `draft`면 `openTemplateSave`; `onSave`는 `template/save` → 성공이면 `showToast('틀을 저장했어요')`, `setActivity(null)`, `setCommandLabel('template-record', null)`. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-save.e2e.ts tests/e2e/template-record.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "8 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/design/SYSTEM.md`에 "틀 저장" 템플릿이 있다
    - `src/page/palette/palette-controller.ts`와 `src/page/template/controller.ts`에 `setCommandLabel(`이 있다
    - `src/worker/template-router.ts`에 `template/save`가 있고, `record/stop` 처리에서 `saveTemplate(`를 부르지 않는다(`template/save` 처리만 부른다)
    - `CI=true pnpm exec playwright test tests/e2e/template-save.e2e.ts tests/e2e/template-record.e2e.ts` 8 passed
  </acceptance_criteria>
  <done>기록을 끝내면 "틀 저장" 이름 장이 뜨고 번호 하나(또는 직접 입력)로 저장되며, 카드 글자가 기록 중에만 "기록 끝내기"다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 기록 합치기 규칙 — 마지막 값만·칸 클릭 버림·체크·이동·겹친 누름·직접 입력 칸 보호·300단계 상한 + 틀 형식 시험</name>
  <files>src/core/record-step.ts, tests/unit/record-step.test.ts, tests/unit/template-schema.test.ts, src/worker/template-router.ts, src/shared/template-messages.ts, src/page/template/controller.ts, tests/e2e/template-record.e2e.ts</files>
  <read_first>
    - src/core/template-schema.ts (TemplateStepSchema, RecordSessionV1의 `lastPress`, isWebUrl — 04-03)
    - src/core/fingerprint.ts (isSameElement)
    - src/worker/template-router.ts (Task 1 결과 — record/step 처리)
    - tests/unit/fingerprint.test.ts (describe/it 모양)
    - .planning/phases/04-templates-activity-log/04-CONTEXT.md (D-07, D-10, Claude's Discretion "기록 단계의 정확한 모양")
  </read_first>
  <behavior>
    - 단위(record-step): 같은 페이지(마지막 navigated 뒤)에서 같은 칸(isSameElement + 같은 framePath)의 fill이 다시 오면 새 단계 없이 앞 단계 value를 바꾼다
    - 단위: press 바로 뒤에 같은 요소의 fill이 오면 그 press를 지운다
    - 단위: 같은 칸 check가 다시 오면 앞 단계 checked를 바꾼다
    - 단위: 같은 framePath의 navigated가 연달아 오면 마지막 것만 남는다
    - 단위: 같은 요소 press가 `PRESS_DEDUP_MS`(1000) 안에 두 번 오면 두 번째를 버린다; 1001ms면 둘 다 남는다
    - 단위: manual이 된 칸에 fill이 오면 manual 그대로이고 value 필드가 없다
    - 단위: 같은 페이지에서 fill 뒤 같은 칸 manual이 오면 그 fill 단계가 manual로 바뀌고 value가 없어진다(앞에 넣다 만 값이 남지 않게)
    - 단위: 단계가 `MAX_RECORD_STEPS`(300)이면 붙이지 않고 `overflow: true`; 입력 배열은 바뀌지 않는다(깊은 비교)
    - 단위(template-schema): 올바른 틀 통과; `startUrl` `javascript:`·`data:` 거절; 모르는 단계 kind 거절; manual 단계 입력에 value가 섞여 와도 `TemplateStepSchema.parse` 결과에 value 키가 없다(zod 기본 제거); `schemaVersion` 2는 `migrate(raw, templatesSpec)`에서 'newer-version'; `isWebUrl`은 http·https만 true(`chrome://settings` false)
    - e2e 6: 기록 중 `#amount`에 "1", 다른 칸, 다시 `#amount`에 "128,000" → `record-session`에 `#amount` fill 단계 1개, value "128,000"
    - e2e 7: 기록 중인 탭 맨 위 프레임에 SW가 `record/state { recording: true, notice: 'too-many-steps' }`를 보내면 "단계가 너무 많아요. 기록을 끝내세요."가 보이고 2초 뒤 사라진다
  </behavior>
  <action>
RED 먼저: `tests/unit/record-step.test.ts`에 record-step behavior 여덟을, `tests/unit/template-schema.test.ts`에 형식 behavior를(04-03 구현을 고정하는 시험이라 처음부터 통과해도 된다 — RED는 record-step으로 확인), `template-record.e2e.ts`에 e2e 6·7을 쓰고 실패 확인 후 RED 커밋.

`src/core/record-step.ts`(Claude's Discretion 확정, D-07, D-10 — 순수, DOM·전역 창·확장 API를 쓰지 않음): `MAX_RECORD_STEPS = 300`, `PRESS_DEDUP_MS = 1000`. `appendRecordedStep(steps: readonly TemplateStep[], step: TemplateStep, ctx: { at: number; lastPress?: RecordSessionV1['data']['lastPress'] }): { steps: TemplateStep[]; lastPress?: …; overflow: boolean }` — behavior 규칙 그대로, `structuredClone` 뒤 작업. 같은 칸 판정은 `isSameElement(a.target.fingerprint, b.target.fingerprint)`와 framePath 문자열 비교, "같은 페이지"는 그 framePath의 마지막 navigated 뒤. manual 보호 규칙은 값이 틀로 새지 않게 하는 두 번째 방어다(첫 방어는 04-03의 페이지 쪽 판별). 라우터는 zod `parse` 결과(모르는 키가 빠진 값)만 세션에 적는다.

라우터: 04-03의 "그대로 붙이기"를 `appendRecordedStep(session.steps, step, { at: Date.now(), lastPress: session.lastPress })`로 바꾸고 `lastPress`를 세션에 적는다. `overflow`면 저장하지 않고 그 탭 맨 위 프레임에 `record/state { recording: true, notice: 'too-many-steps' }`를 보낸다 — `record/state` 스키마에 선택 필드 `notice: z.enum(['too-many-steps'])`를 더한다(Task 3이 넓힌다). controller는 notice를 받으면 `showTransientMessage('단계가 너무 많아요. 기록을 끝내세요.', 2000)`. 통과하면 GREEN, 정리하면 REFACTOR 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/record-step.test.ts tests/unit/template-schema.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 14 tests passed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-record.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "7 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/record-step.ts`에 `export const MAX_RECORD_STEPS = 300`, `export const PRESS_DEDUP_MS = 1000`, `export function appendRecordedStep`이 있다
    - `grep -vE '^\s*(//|\*|/\*)' src/core/record-step.ts | grep -cE '\b(document|window|chrome)\.'`가 0
    - `src/worker/template-router.ts`에 `appendRecordedStep(`와 `too-many-steps`가 있다
    - `pnpm exec vitest run tests/unit/record-step.test.ts tests/unit/template-schema.test.ts` 14개 이상 통과
    - `CI=true pnpm exec playwright test tests/e2e/template-record.e2e.ts` 7 passed
  </acceptance_criteria>
  <done>기록 단계 모양이 단위 시험으로 고정되었고(D-40), 직접 입력 칸에는 값이 붙지 않으며, 300단계에서 멈추고 알린다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: iframe 안 동작 기록 — SW가 계산한 프레임 경로(같은 출처·다른 출처)와 모르는 프레임 알림</name>
  <files>src/core/frame-tree.ts, tests/unit/frame-tree.test.ts, src/worker/relay.ts, src/worker/template-router.ts, src/shared/template-messages.ts, src/page/template/controller.ts, tests/e2e/template-record.e2e.ts</files>
  <read_first>
    - src/core/frame-tree.ts (FrameReport, resolveReports, composeTree)
    - tests/unit/frame-tree.test.ts (describe/it 모양)
    - src/worker/relay.ts (reportsByTab, frame/report 처리)
    - src/worker/template-router.ts (Task 2 결과)
    - tests/practice-site/workflow-frame.html (04-02)
  </read_first>
  <behavior>
    - 단위: `framePathForFrameId(resolved, 0)`은 `[]`
    - 단위: 맨 위(0)의 자식 pathKey "id:form-frame"이 frameId 5면 `['id:form-frame']`
    - 단위: 두 단계 아래(0 → 5 → 9, pathKey "id:inner")면 `['id:form-frame', 'id:inner']`
    - 단위: 트리에 없는 frameId는 `undefined`
    - 단위: 자식 목록이 서로를 가리키는 깨진 보고에도 끝나고 `undefined` 또는 첫 경로
    - e2e 8: `workflow-frame.html`에서 기록 중 `#form-frame` 안 `#amount`에 "128,000", `#other-frame`(http://other.test) 안 `#memo`에 "식대" → `record-session`의 두 fill 단계 `target.fingerprint.framePath`가 `['id:form-frame']`·`['id:other-frame']`, `target.frameOrigin`이 "http://practice.test"·"http://other.test"
    - e2e 9: SW가 기록 중인 탭 맨 위 프레임에 `record/state { recording: true, notice: 'unknown-frame' }`를 보내면 "이 칸의 위치를 몰라 기록하지 못했어요. 다시 입력하세요."가 보이고 2초 뒤 사라진다
  </behavior>
  <action>
RED 먼저: 단위 behavior 다섯을 `tests/unit/frame-tree.test.ts`에, e2e 8·9를 `template-record.e2e.ts`에 더하고 실패 확인 후 RED 커밋.

`frame-tree.ts`: `framePathForFrameId(resolved: ReadonlyMap<number, FrameReport>, frameId: number, topFrameId = 0): string[] | undefined` — 맨 위에서 자식(`children[].childFrameId`, `pathKey`)을 따라 너비 우선으로 찾되 방문 집합으로 반복을 막는다(순수). `relay.ts`: `resolvedFramesFor(tabId: number): Map<number, FrameReport>`(그 탭 보고를 `resolveReports`로 맞춘 결과)를 더한다(기존 동작 변경 없음). 라우터(D-09): `record/step`에서 맨 위가 아니면 `framePathForFrameId(relay.resolvedFramesFor(tabId), sender.frameId)`로 경로를 얻어 `target.fingerprint.framePath`를 덮어쓴다 — 페이지가 보낸 경로는 믿지 않는다. 경로를 모르면 단계를 버리고 맨 위 프레임에 `record/state { recording: true, notice: 'unknown-frame' }`. 자식 프레임의 `frame/ready`도 경로를 알면 `navigated { framePath, url }` 단계로 붙이고, 모르면 조용히 건너뛴다(이동은 실행 때 기다림 경계로만 쓰인다 — 04-07). `record/state`의 `notice` enum에 'unknown-frame'을 더하고 controller가 `showTransientMessage('이 칸의 위치를 몰라 기록하지 못했어요. 다시 입력하세요.', 2000)`. e2e 9는 보고가 오기 전의 경합을 만들지 않고 SW에서 `chrome.tabs.sendMessage(tabId, …, { frameId: 0 })`로 알림 경로만 확인한다(경합 시험은 흔들린다 — 버리는 쪽은 단위 `undefined`와 라우터 분기로 확인). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/frame-tree.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or any test reported as failed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-record.e2e.ts tests/e2e/frames.e2e.ts</automated>
    <fails_when>non-zero exit, or fewer than 9 template-record tests passed, or any frames.e2e.ts test failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/frame-tree.ts`에 `export function framePathForFrameId`가 있고, `src/worker/relay.ts`에 `resolvedFramesFor`가 있다
    - `src/worker/template-router.ts`에 `framePathForFrameId(`와 `unknown-frame`이 있다
    - `tests/unit/frame-tree.test.ts` 전체 통과(새 시험 5개 포함)
    - `CI=true pnpm exec playwright test tests/e2e/template-record.e2e.ts` 9 passed, 기존 `frames.e2e.ts` 통과
  </acceptance_criteria>
  <done>같은 출처·다른 출처 iframe 안 칸이 SW가 계산한 프레임 경로·주소와 함께 기록되고, 위치를 모르는 칸은 저장하지 않고 알린다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- 이 계획은 TMPL-01 edge 항목(04-03 소유)의 연장이다. 가정한 경계(플래그):
  - 끝났지만 저장 전인 기록도 "끝나지 않은 세션"으로 본다 — 다른 탭에서 기록을 켜려 하면 04-03과 같은 "다른 탭에서 기록 중이에요"로 막는다. 저장 화면을 닫은 탭에서 둘째 장 4를 고르면 저장 화면이 다시 뜬다.
  - 이름은 앞뒤 공백을 없앤 뒤 1~60자. 직접 입력 칸이 비었을 때 Enter는 아무 일도 하지 않는다.
  - 1000ms 안 같은 요소 두 번 누름은 도우미 대신 누름과 실제 클릭이 겹친 것으로 보고 한 번만 남긴다. 이용자가 일부러 1초 안에 두 번 누른 경우도 한 번이 된다.
  - 경로를 모르는 프레임의 동작은 버리고 알린다(이용자가 다시 하면 보통 그때는 보고가 와 있다).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 자식 프레임 content script → SW | 다른 출처 프레임이 보낸 단계 — 프레임 경로를 흉내 낼 수 있다 |
| 맨 위 content script → SW(template/save) | 이름 글자가 건너온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Denial of Service | 기록 세션 크기 | low | mitigate | `MAX_RECORD_STEPS` 300, 넘으면 붙이지 않고 알림(단위 시험) |
| T-04-70 | Spoofing | record/step의 framePath | medium | mitigate | 라우터가 `framePathForFrameId(relay.resolvedFramesFor(tabId), sender.frameId)`로 덮어쓰고 모르면 버림. frameOrigin은 04-03대로 sender.url |
| T-04-71 | Tampering | template/save | low | mitigate | `sender.frameId === 0` + 세션 탭 일치 + 세션 `stopped`일 때만, 이름은 zod 1~60자, 틀 전체는 `TemplateV1Schema` 통과 후 저장 |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 설치 없음(D-05) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/record-step.test.ts tests/unit/template-schema.test.ts tests/unit/frame-tree.test.ts` 전부 통과
- `CI=true pnpm exec playwright test tests/e2e/template-record.e2e.ts tests/e2e/template-save.e2e.ts tests/e2e/frames.e2e.ts` 통과(template-record 9, template-save 3)
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 기록을 끝내면 저장 화면에서 이름 번호 하나로 틀이 저장된다(TMPL-01, D-06, D-11)
- 기록 단계 합치기와 iframe 안 동작 기록이 시험으로 고정된다(D-07, D-09, D-40)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/record-step.ts`, `src/page/overlay/template-save.ts`, `tests/unit/record-step.test.ts`, `tests/unit/template-schema.test.ts`, `tests/e2e/template-save.e2e.ts`
- 심볼: `appendRecordedStep()`, `MAX_RECORD_STEPS`, `PRESS_DEDUP_MS`, `framePathForFrameId()`, `Relay.resolvedFramesFor()`, `openTemplateSave()`, `closeTemplateSave()`, palette-controller `setCommandLabel()`
- 메시지 type: `template/save { name }`, `record/stop` 답 `draft`, `record/state`의 선택 `notice`('too-many-steps' | 'unknown-frame')
- SYSTEM.md: "틀 저장" 템플릿(이름 장), 상태 표 "틀 기록" 줄의 가득 참 문구

<output>
Create `.planning/phases/04-templates-activity-log/04-04-SUMMARY.md` when done
</output>
