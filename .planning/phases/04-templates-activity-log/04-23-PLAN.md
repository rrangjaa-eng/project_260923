---
phase: 04-templates-activity-log
plan: 23
type: execute
wave: 21
depends_on: ["04-22"]
files_modified:
  - tests/e2e/template-journey.e2e.ts
  - tests/e2e/dom-audit-phase4.e2e.ts
autonomous: true
requirements: [TMPL-01, TMPL-05, TMPL-09, TMPL-12, LOG-02]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "한 번의 브라우저 시험이 틀 기록 → 저장 → 실행(바뀌는 값 입력) → 제출 확인 → 상신 → 모양이 바뀐 양식에서 막힘 → 이 단계만 다시 기록 → 기록 내보내기 미리 보기 → 파일 저장까지 이어서 통과한다(D-41, TMPL-01, TMPL-05, TMPL-09, TMPL-12, LOG-02)"
    - "그 시험 동안 연습 사이트 밖으로 나가는 요청이 하나도 없고, 내보낸 기록 파일에 입력한 값이 들어 있지 않다(D-39, D-32, 설계 8장)"
    - "빌드된 manifest의 권한은 storage·scripting·tabs와 <all_urls>뿐이다(D-05)"
    - "Phase 4의 모든 새 화면은 실행자가 아닌 별도 에이전트가 CI=true로 DOM을 재서 SYSTEM.md·tokens.css와 맞는지 판정하고, 위반을 고친 뒤 전체 게이트를 한 번 돌린다(D-42, D-36, D-37)"
    - "엣지·웨일 확인과 자동 시험이 닿지 않는 항목은 수동 확인 목록으로 남기고, 페이즈 뒤 Post-build는 /review → /qa → /cso(필수) → /ship 순서로 건너뛰지 않는다(D-41, D-42)"
  artifacts:
    - path: "tests/e2e/template-journey.e2e.ts"
      provides: "Phase 4 흐름 전체 e2e + 외부 요청 없음 + 권한 확인"
    - path: "tests/e2e/dom-audit-phase4.e2e.ts"
      provides: "독립 DOM 감사(별도 에이전트 작성)"
  key_links:
    - from: "tests/e2e/template-journey.e2e.ts"
      to: "tests/e2e/fixtures.ts"
      via: "expectNoExternalRequests() — practice.test·other.test·확장 페이지 밖 요청 0"
      pattern: "expectNoExternalRequests\\("
    - from: "tests/e2e/dom-audit-phase4.e2e.ts"
      to: "docs/design/tokens.css"
      via: "getComputedStyle 값과 토큰 값 비교(색·radius·글자·높이)"
      pattern: "getComputedStyle"
  prohibitions:
    - statement: "스크린샷을 눈으로 보고 화면 완료를 판정하지 않는다"
      status: resolved
      verification: judgment
    - statement: "감사 에이전트가 제품 코드를 고치지 않는다(판정과 수정을 나눈다)"
      status: resolved
      verification: judgment
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 반복 업무를 한 번 해 보여 틀로 저장하고 이후 "틀 고르기 1번 + 바뀌는 값 2~3번 + 제출 확인 1번"으로 끝내기를, **so that** 되돌릴 수 없는 제출은 항상 확인 뒤에 한 번만 일어나고 문제가 생기면 활동 기록으로 추적할 수 있다.

<objective>
Phase 4 전체를 한 흐름으로 확인하고, 새 화면을 독립 DOM 감사로 판정한 뒤, 전체 게이트를 한 번 돌리고 수동 확인 목록을 남긴다.

Purpose: D-41(흐름 전체 e2e, 엣지·웨일 수동 목록), D-42(싼 게이트 → 독립 DOM 감사 → 수정 → 전체 게이트 한 번, Post-build `/cso` 필수), D-05(권한 불변), D-39(연습 사이트만).
Output: `template-journey.e2e.ts`, 별도 에이전트의 `dom-audit-phase4.e2e.ts`와 판정, 위반 수정, `CI=true pnpm test` 결과, 수동 확인 목록.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-activity-log/04-CONTEXT.md
@.planning/phases/04-templates-activity-log/04-01-SUMMARY.md
@.planning/phases/04-templates-activity-log/04-18-SUMMARY.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 패키지·새 확장 권한 없음(D-05).
- 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다. 감사 에이전트 프롬프트에도 `verification-before-completion`을 명시한다.
- 커밋 제목 영어 접두어(`test(04-23): …`, `fix(04-23): …`), 본문 한국어.
- 판정은 `CI=true`(프로덕션 빌드). 로컬 dev 통과는 완료 신호가 아니다.
- 화면 수정이 SYSTEM.md를 벗어나야 하면 `docs/design/DECISIONS.md`에 이유를 적고 SYSTEM.md를 먼저 고친다 — 화면 하나만 예외 금지(D-37).
- 전체 게이트(`CI=true pnpm test`)는 이 계획에서 **한 번**만 돌린다(Task 3). 그 전에는 해당 시험 파일만 돌린다.

<tasks>

<task type="tracer">
  <name>Task 1: 끝에서 끝까지 — 기록 → 저장 → 실행 → 확인 → 상신 → 막힘 → 다시 기록 → 기록 내보내기, 외부 요청 0, 권한 확인</name>
  <files>tests/e2e/template-journey.e2e.ts</files>
  <read_first>
    - tests/e2e/fixtures.ts (servePage, expectNoExternalRequests, extensionId, serviceWorker)
    - tests/e2e/template-helpers.ts (openPalette, openPaletteSlot, readStorage, writeStorage — 04-02, recordTemplate — 04-06)
    - tests/e2e/template-record.e2e.ts, template-save.e2e.ts, template-run.e2e.ts, template-submit.e2e.ts, template-blocked.e2e.ts, activity-log.e2e.ts, weekly-stats.e2e.ts (각 조각의 조작 순서 — 04-02~04-16)
    - tests/practice-site/workflow.html, workflow-form.html, workflow-drift.html, workflow-result.html
    - .planning/phases/04-templates-activity-log/04-CONTEXT.md (D-05, D-39, D-41)
  </read_first>
  <action>
먼저 시험을 쓰고 돌린다. 앞 계획들이 조각을 모두 연결했으므로 처음부터 통과할 수 있다 — 실패하면 `systematic-debugging`으로 원인을 찾아 고친 커밋(`fix(04-23): …`)을 따로 남기고, 처음부터 통과하면 그 사실과 출력 요약을 SUMMARY에 적는다(없는 실패를 만들어 RED 커밋을 하지 않는다).

`tests/e2e/template-journey.e2e.ts` 하나의 test(D-41, 연습 사이트만 — D-39):
1) `workflow.html`에서 명령판 둘째 장 4 "틀 기록"(`openPaletteSlot(page, 2, 4)` — `recordTemplate()`과 같은 길이지만 저장 화면을 하나씩 확인하려고 풀어 쓴다) → 메뉴 "지출결의" → `#date` "2026-09-24", `#amount` "128000", `#memo` "거래처 미팅" → 상신 → 명령판으로 기록 끝내기.
2) 틀 저장 화면: 날짜·금액이 "매번 바뀜"으로 미리 표시됨을 확인 → 제출 단계가 "상신" → 이름 후보 1번 → 저장(`readStorage(serviceWorker, 'local', 'templates')`로 틀 1개, steps에 value "128000"은 금액 단계에만).
3) 새 탭 `workflow.html`에서 0 → 첫 장 3 "틀 실행" → 1 → 바뀌는 값 카드에서 날짜는 "오늘 날짜", 금액은 9 직접 입력 "150000" → 메뉴를 거치지 않고 시작 페이지로 가서 단계 실행 → 제출 확인 화면 제목 "지출결의서를 상신할까요?", 요약에 "금액"과 "150000" → 1.1초 뒤 Enter → `workflow-result.html` `#count` "1" → 토스트 "상신했어요" → 누름 기록 outcome 'success'.
4) `servePage('http://practice.test/workflow-form.html', workflow-drift.html 내용)` → 다시 실행 → 금액 단계에서 `--warning` 카드 → 1 "이 단계만 다시 기록" → `#claim-amount`에 "150000" 입력 → "이 단계를 이렇게 바꿀까요?" → 1.1초 뒤 Enter → 저장된 틀의 금액 단계 지문이 바뀜 → 실행이 이어져 제출 확인 화면 → Esc(두 번째 상신 없음, `#count` 그대로 "1").
5) 명령판 셋째 장 3 "기록 내보내기" → 미리 보기 맨 위 주간 통계(`WEEKLY_STATS_PLACEMENT` 'export-preview') + 기록 줄("틀 시작", "상신", "막힘") → Enter → download 이벤트 `tremor-helper-log-{오늘}.json` → 파일 내용에 "150000"·"128000"·"거래처 미팅"이 없다(값은 남기지 않음, D-32).
6) `expectNoExternalRequests()`.

같은 파일의 두 번째 test: `.output/chrome-mv3/manifest.json`(CI=true 빌드 결과)을 읽어 `permissions`를 정렬한 값이 `['scripting', 'storage', 'tabs']`, `host_permissions`가 `['<all_urls>']`이고 `downloads`·`webNavigation`·`alarms`·`debugger`·`contentSettings`가 어디에도 없다(D-05). 통과하면 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-journey.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "2 passed", or "No tests found", or the blockedRequests list printed on failure is non-empty</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/template-journey.e2e.ts`에 `expectNoExternalRequests(`, `servePage(`, `tremor-helper-log-`, `manifest.json`이 있다
    - `CI=true pnpm exec playwright test tests/e2e/template-journey.e2e.ts` 2 passed
  </acceptance_criteria>
  <done>Phase 4 흐름 전체가 한 시험으로 이어서 통과하고, 밖으로 나가는 요청과 새 권한이 없다(D-41, D-05).</done>
</task>

<task type="auto">
  <name>Task 2: 싼 게이트 → 별도 에이전트의 독립 DOM 감사(CI=true) → 위반 수정</name>
  <files>tests/e2e/dom-audit-phase4.e2e.ts</files>
  <read_first>
    - docs/design/SYSTEM.md (Phase 4가 더한 템플릿: 틀 저장, 바뀌는 값 카드, 틀 목록, 진행 표시, 상태 카드, 알림 창 글, 직접 하기 카드, 기록 미리 보기·주간 통계, 옵션 페이지 — 틀)
    - docs/design/tokens.css
    - docs/DESIGN.md (§4 절차)
    - .planning/phases/01-click-helper-foundation/01-16-PLAN.md (Phase 1 독립 DOM 감사 방식)
    - tests/e2e/template-journey.e2e.ts (Task 1 — 각 화면을 여는 순서)
  </read_first>
  <action>
1) 싼 게이트: `pnpm lint && pnpm typecheck && pnpm build`. 실패하면 먼저 고친다.

2) 독립 DOM 감사(D-42, CLAUDE.md 화면 검증 순서): 실행자가 판정하지 않는다. Task 도구로 **별도 에이전트를 model "sonnet"으로** 띄우고, 프롬프트에 다음을 적는다 — "`verification-before-completion` 스킬을 호출한다. 제품 코드(src/, docs/)는 고치지 않는다. 스크린샷을 눈으로 판정하지 않는다. `tests/e2e/dom-audit-phase4.e2e.ts`를 새로 써서 `CI=true pnpm exec playwright test tests/e2e/dom-audit-phase4.e2e.ts`로 돌리고, 화면마다 통과·위반 목록(선택자, 잰 값, 기대 값)을 돌려준다." 감사 대상 화면 13개: (1) 틀 기록 중 표시, (2) 틀 저장 세 장, (3) 틀 목록(틀 없음 카드 포함), (4) 바뀌는 값 카드, (5) 진행 표시(`role="progressbar"`), (6) 단계 확인·제출 확인(요약 줄 포함), (7) 상태 카드(막힘·확인 필요), (8) 다시 기록 확인, (9) 알림 창 글 보기, (10) 직접 하기 카드(민감칸·보안 키패드 두 가지), (11) 기록 미리 보기와 주간 통계, (12) 옵션 화면 제출 단어·틀 기본 속도 절, (13) 옵션 화면 틀 절(내보내기 경고·지우기 확인 포함). 잴 것(D-36): 오버레이가 Shadow DOM 안에 있음, `getComputedStyle`의 font-family에 "IBM Plex Sans KR", 번호 카드 숫자 22px·700, 누르는 대상 높이 56px 이상, 카드 radius가 `--radius-card` 값, 선택 테두리 3px `--accent`, 막힘·확인 필요·비민감 직접 하기 카드 테두리 색이 `--warning`, 민감칸 직접 하기 카드는 `--accent`, 직접 하기 카드에 가림막이 없고 카드 밖 좌표의 `elementFromPoint`가 페이지 요소, 모든 버튼에 키 칩 글자(`Enter`·`Esc`·숫자), 쓰인 색(color·background-color·border-color)이 tokens.css 색 토큰 값 집합 안, 토스트가 오른쪽 아래. 결과를 SUMMARY "DOM 감사" 절에 그대로 옮긴다.

3) 수정: 위반마다 실행자가 고친다(SYSTEM.md와 다른 쪽이 화면이면 화면을, SYSTEM.md 자체를 바꿔야 하면 `docs/design/DECISIONS.md`에 이유를 적고 SYSTEM.md 먼저). 고친 뒤 **감사 파일만** 다시 돌려 모두 통과할 때까지 반복한다(전체 게이트는 Task 3에서 한 번). 고친 파일 목록을 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm build</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/dom-audit-phase4.e2e.ts</automated>
    <fails_when>non-zero exit, or "No tests found", or any audit test reported as failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/dom-audit-phase4.e2e.ts`가 있고 `getComputedStyle`과 `IBM Plex Sans KR`과 `elementFromPoint`를 쓴다
    - SUMMARY의 "DOM 감사" 절에 감사 에이전트 model "sonnet"과 화면별 결과(위의 13개 화면 이름)가 있다
    - `CI=true pnpm exec playwright test tests/e2e/dom-audit-phase4.e2e.ts`가 실패 0으로 끝난다
  </acceptance_criteria>
  <done>Phase 4의 새 화면이 모두 독립 DOM 감사를 통과한다(D-42, D-36).</done>
</task>

<task type="auto">
  <name>Task 3: 전체 게이트 한 번(CI=true pnpm test) + 엣지·웨일 수동 확인 목록 + Post-build 넘김</name>
  <files>.planning/phases/04-templates-activity-log/04-23-SUMMARY.md</files>
  <read_first>
    - package.json (`test` 스크립트 — `pnpm test:unit && pnpm test:e2e`)
    - .planning/phases/04-templates-activity-log/04-01-SUMMARY.md (스파이크 결과 A3·A4·A7·A8)
    - .planning/phases/04-templates-activity-log/04-18-SUMMARY.md (SW 강제 종료 방법)
    - .planning/phases/04-templates-activity-log/04-CONTEXT.md (D-41, D-42)
  </read_first>
  <action>
1) `CI=true pnpm test`를 한 번 돌린다. 실패하면 `systematic-debugging`으로 원인을 찾아 고치고, 고친 부분의 해당 시험 파일만 다시 돌려 통과시킨 뒤 전체를 다시 한 번 돌린다(두 번째 전체 실행은 실패가 있었을 때만). 결과는 요약(통과·실패 수)만 SUMMARY에 적는다.

2) 수동 확인 목록(D-41 — 엣지·웨일, 자동 시험이 닿지 않는 것)을 SUMMARY "수동 확인" 절에 적고 아래 human-check로 넘긴다. 04-01 스파이크가 실패였던 항목만 조건부 줄을 넣는다.

3) SUMMARY 끝에 Post-build 순서를 적는다: `/review` → `/qa`(UI 변경이므로 `/design-review` 포함) → `/cso`(이 페이즈는 가져온 틀 파일, 페이지 쪽 알림 창 소식, 되돌릴 수 없는 제출을 다루므로 필수 — D-42) → `/ship`. 건너뛰려면 먼저 이용자 승인을 받는다(CLAUDE.md).
  </action>
  <verify>
    <automated>CI=true pnpm test</automated>
    <fails_when>non-zero exit, or any unit, integration, or e2e test reported as failed</fails_when>
    <human-check>엣지와 웨일 최신판에서 빌드 결과(.output/chrome-mv3)를 압축 풀린 확장으로 불러 다음을 확인한다. (1) 확장이 오류 없이 켜지고 manifest의 MAIN world·document_start 콘텐츠 스크립트와 matchOriginAsFallback이 받아들여진다(RESEARCH A5). (2) 틀 밖에서는 연습 페이지의 confirm·alert 창이 원래대로 뜬다(D-26). (3) 연습 사이트에서 틀 기록 → 실행 → 제출 확인 → 상신이 된다. (4) 옵션 페이지 틀 내보내기가 파일로 저장되고 그 파일을 다시 가져올 수 있다, 기록 내보내기도 저장된다. (5) 04-01 A7이 실패였다면 틀 실행 중 chrome://serviceworker-internals(웨일은 whale://serviceworker-internals)에서 도우미 SW를 Stop한 뒤 틀이 이어 간다. (6) 04-01 A4가 실패였다면 페이지가 열리자마자 confirm을 부르는 연습 페이지에서 첫 창을 놓치는 한계가 SUMMARY 설명과 같다. (7) 가능한 환경이면 틀 실행 중 확장 새 버전을 올려 실행이 끝난 뒤에 적용되는지 본다(D-25) — 환경이 없으면 "확인 못 함"으로 적는다.</human-check>
  </verify>
  <acceptance_criteria>
    - `CI=true pnpm test`(= `pnpm test:unit && pnpm test:e2e`)가 0으로 끝난다(SUMMARY에 통과 수 요약)
    - SUMMARY에 "수동 확인" 절이 있고 위 (1)~(7)의 결과 또는 "확인 못 함"이 줄마다 있다
    - SUMMARY 끝에 `/review`, `/qa`, `/cso`, `/ship`이 이 순서로 적혀 있고 `/cso` 옆에 "필수"가 있다
  </acceptance_criteria>
  <done>전체 게이트가 한 번에 통과하고, 수동 확인과 Post-build 순서가 남았다(D-41, D-42).</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- 이 계획은 새 edge probe 항목을 갖지 않는다. 20개 항목의 자리: TMPL-01 04-03 · TMPL-02·03 04-05 · TMPL-04·06 04-07 · TMPL-05 `empty`·`encoding`(명시 truth) 04-11 · TMPL-12 04-12 · TMPL-08 04-13 · TMPL-09·10 04-09(TMPL-10 연장 04-10, 확인 필요 카드 04-17) · TMPL-11 04-17 · TMPL-07·13 04-18 · TMPL-14 04-19 · TMPL-15 04-20 · TMPL-16 04-21 · LOG-01 04-15 · LOG-02 04-14 · LOG-03 04-16.
- 흐름 시험은 기존 조각 시험의 조작 순서를 그대로 잇는다 — 조각 시험과 겹치는 확인은 흐름이 끊기지 않는지를 보기 위한 것이다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 확장 → 네트워크 | 이 페이즈는 밖으로 나가는 요청이 없어야 한다(설계 8장) |
| 빌드 → manifest 권한 | 권한이 늘면 이용자 동의 범위가 바뀐다 |
| 실행자 → 완료 판정 | 스스로 판정하면 거짓 통과 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-64 | Information Disclosure | 흐름 전체의 요청 | high | mitigate | `expectNoExternalRequests()`(Task 1) + 내보낸 기록 파일에 입력 값 없음 확인 |
| T-04-65 | Elevation of Privilege | manifest 권한 | high | mitigate | 빌드된 manifest 권한 집합을 정확히 비교(Task 1 두 번째 test, D-05) |
| T-04-66 | Repudiation | 화면 완료 판정 | medium | mitigate | 별도 에이전트(model sonnet)가 CI=true DOM 실측으로 판정, 실행자는 수정만(Task 2, D-42) |
| T-04-67 | Tampering | 페이즈 전체 보안 검토 | high | transfer | Post-build `/cso` 필수(D-42) — 가져온 틀 파일, 페이지 쪽 알림 창 소식, 되돌릴 수 없는 제출 |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 설치 없음(D-05) |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/template-journey.e2e.ts tests/e2e/dom-audit-phase4.e2e.ts` 통과
- `CI=true pnpm test` 한 번 통과
- SUMMARY에 DOM 감사 결과·수동 확인 목록·Post-build 순서
</verification>

<success_criteria>
- Phase 4 흐름 전체가 CI=true에서 통과한다(D-41)
- 새 화면이 독립 DOM 감사를 통과한다(D-42)
- 권한 불변, 외부 요청 0(D-05, 설계 8장)
- 수동 확인 목록과 Post-build 순서(/cso 필수)가 남았다
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/e2e/template-journey.e2e.ts`, `tests/e2e/dom-audit-phase4.e2e.ts`
- SUMMARY 절: "DOM 감사"(화면별 결과), "수동 확인"(엣지·웨일, 조건부 SW Stop), Post-build 순서
- 감사가 찾은 위반을 고친 파일(목록은 SUMMARY에 — 계획 시점에 알 수 없음)

<output>
Create `.planning/phases/04-templates-activity-log/04-23-SUMMARY.md` when done
</output>
