---
phase: 04-templates-activity-log
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - src/core/value-shape.ts
  - tests/unit/value-shape.test.ts
  - src/core/submit-word.ts
  - tests/unit/submit-word.test.ts
  - src/core/template-draft.ts
  - tests/unit/template-draft.test.ts
  - src/core/template-schema.ts
  - src/core/danger.ts
  - src/page/overlay/template-save.ts
  - src/page/overlay/confirm-dialog.ts
  - src/page/template/controller.ts
  - src/shared/template-messages.ts
  - src/worker/template-router.ts
  - src/core/settings-schema.ts
  - tests/unit/settings-schema.test.ts
  - src/shared/messages.ts
  - src/worker/storage-writer.ts
  - src/entrypoints/options/main.ts
  - src/entrypoints/options/index.html
  - tests/e2e/template-save.e2e.ts
  - tests/e2e/options-template.e2e.ts
autonomous: true
requirements: [TMPL-02, TMPL-03]
estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "'틀 저장' 화면 1/3은 기록된 입력 단계를 번호 카드(칸 이름 + 기록한 값)로 늘어놓고, 날짜·금액·전화번호 모양 값은 '매번 바뀜'으로 미리 표시한다. 칸 번호를 누르면 '매번 바뀜 / 항상 같음'이 바뀐다(D-11, TMPL-02)"
    - "8자리 숫자(yyyymmdd)는 칸 이름에 날짜 낱말(일자·날짜·일시)이 있을 때만 날짜로 본다 — 라벨 없이 계좌번호 같은 8자리 숫자를 날짜로 표시하지 않는다(RESEARCH A9)"
    - "'틀 저장' 화면 2/3은 제출 성격 버튼(기본 단어 저장·상신·등록·제출·신청·전송, 사이트별로 고칠 수 있음) 중 마지막으로 누른 것을 제출 단계로 미리 고르고, 이용자는 번호로 바꿀 수 있다. 그 앞의 제출 성격 단계는 '중간 제출'로 표시된다(D-12, TMPL-03)"
    - "틀은 고른 제출 단계에서 끝난다 — 제출 단계 뒤에 기록된 단계는 틀에 넣지 않고 2/3 화면에 그 개수를 알려 준다"
    - "'틀 저장' 화면 3/3은 이름 후보를 번호 카드로 보여 준다: 1 제출 단계 페이지 제목, 2 페이지 제목 + 제출 단어(예: '지출결의서 상신'), 9 직접 입력(D-11)"
    - "저장 화면 1/3에서 Esc를 누르면 '기록을 버릴까요?' 확인 화면(1초 보호, [Enter 버리기] [Esc 취소])이 뜨고, Enter로 확인해야만 기록이 지워진다. 2/3·3/3의 Esc는 앞 장으로 돌아간다"
    - "설정 형식이 3으로 올라가며(migrations[2]) submitWords·siteSubmitWords·templateSpeed(기본 'normal')가 더해진다. v2 설정은 값을 잃지 않고 v3로 바뀐다(D-03, D-04, D-19 기본 속도는 sync)"
    - "옵션 페이지에서 사이트별 제출 단어를 고치면 그 사이트의 틀 저장 화면 2/3 판별에 쓰인다(D-12)"
  artifacts:
    - path: "src/core/value-shape.ts"
      provides: "바뀌는 값 모양 판별 + 오늘 날짜를 같은 모양으로"
      exports: ["detectShape", "renderToday", "ValueShape", "ValueShapeSchema", "DateShape", "DATE_LABEL_WORDS"]
    - path: "src/core/submit-word.ts"
      provides: "제출 성격 버튼 판별"
      exports: ["DEFAULT_SUBMIT_WORDS", "isSubmitLike", "matchedSubmitWord", "submitWordsFor"]
    - path: "src/core/template-draft.ts"
      provides: "기록 → 저장 화면 초안(바뀌는 값 표시·제출 표시·이름 후보)과 선택 적용"
      exports: ["draftFromSteps", "toggleVariable", "setFinalSubmit", "nameCandidates", "buildTemplateSteps"]
    - path: "src/core/settings-schema.ts"
      provides: "설정 형식 3 + migrations[2]"
      contains: "templateSpeed"
  key_links:
    - from: "src/page/overlay/template-save.ts"
      to: "src/core/template-draft.ts"
      via: "draftFromSteps(steps, submitWordsFor(settings, origin))로 1/3·2/3·3/3 화면 내용을 만든다"
      pattern: "draftFromSteps\\("
    - from: "src/worker/template-router.ts"
      to: "src/core/template-draft.ts"
      via: "template/save 선택값을 buildTemplateSteps로 검증·적용(제출 뒤 단계 자르기, variables 만들기)"
      pattern: "buildTemplateSteps\\("
    - from: "src/core/settings-schema.ts"
      to: "migrate()"
      via: "settingsSpec.migrations[2]가 v2 → v3(기본값 채움)"
      pattern: "migrations"
  prohibitions:
    - statement: "칸 이름에 날짜 낱말이 없는 8자리 숫자를 '바뀌는 값(날짜)'으로 자동 표시하지 않는다"
      status: resolved
      verification: test
    - statement: "보호 시간이 있는 확인 없이 기록을 지우지 않는다"
      status: resolved
      verification: test
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 반복 업무를 한 번 해 보여 틀로 저장하고 이후 "틀 고르기 1번 + 바뀌는 값 2~3번 + 제출 확인 1번"으로 끝내기를, **so that** 되돌릴 수 없는 제출은 항상 확인 뒤에 한 번만 일어나고 문제가 생기면 활동 기록으로 추적할 수 있다.

<objective>
04-02의 저장 화면(이름 단계만)을 세 장으로 넓힌다: 1/3 바뀌는 값 고르기, 2/3 제출 단계 고르기, 3/3 이름. 판별 규칙(값 모양, 제출 단어)을 단위 시험으로 고정하고, 사이트별 제출 단어와 틀 기본 속도를 설정 형식 3에 더한다.

Purpose: TMPL-02(D-11), TMPL-03(D-12). 여기서 표시한 "제출 단계"가 04-05의 확인·누름 기록 대상이 된다.
Output: `value-shape.ts`·`submit-word.ts`·`template-draft.ts`+단위 시험, 저장 화면 세 장, 확인 화면 제목·줄·색 선택지, 설정 v3 + 옵션 페이지 편집, e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-activity-log/04-CONTEXT.md
@.planning/phases/04-templates-activity-log/04-RESEARCH.md
@.planning/phases/04-templates-activity-log/04-02-SUMMARY.md
@docs/design/SYSTEM.md
@docs/design/tokens.css
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 패키지·새 확장 권한 없음(D-05). 필요해지면 멈추고 `checkpoint:decision`으로 이유 한 줄 + 승인을 받는다.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- tracer·`tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(04-03): …` → 최소 구현 → GREEN 커밋 `feat(04-03): …` → 정리하면 `refactor(04-03): …`. 제목 영어 접두어, 본문 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드).
- 오버레이는 Shadow DOM 안, tokens.css 변수만(D-36). 화면 모양은 04-02가 SYSTEM.md에 더한 "틀 저장" 템플릿 그대로(D-37). 카피 규칙(D-38).
- 저장 API 쓰기·지우기는 `src/worker/storage-writer.ts`의 `enqueue()` 안에만(D-03).
- 연습 사이트에서만 시험(D-39). 기존 파일은 이 계획이 필요한 줄만 바꾼다.

## Assumes from Phase 3 (PR #9 계획 기준)

- 설정 형식 `schemaVersion 2`와 `settingsSpec.migrations[1]`(v1→v2)이 있다. v2의 필드 배치(컨디션 묶음 등)가 이 계획의 가정과 다르면 새 필드 세 개를 v2 `data`의 최상위에 더하는 방식만 맞춘다.
- `src/core/danger.ts`의 `stripSpaces`가 export되어 있다(없으면 이 계획이 `export` 한 단어만 더한다 — 동작 변경 없음).
- 옵션 페이지 `src/entrypoints/options/index.html`·`main.ts`가 있고 사이트별 위험 단어 편집 칸(SAFE-06, `siteDangerWords`)이 있다. 제출 단어 편집 칸을 그 옆에 같은 모양으로 둔다.
- `updateSettings` 패치 스키마가 Phase 3에서 넓어졌다면 그 모양에 필드 두 개(`siteSubmitWords`, `templateSpeed`)만 더한다.

<tasks>

<task type="tracer">
  <name>Task 1: 끝에서 끝까지 — 저장 화면 1/3 바뀌는 값 → 2/3 제출 단계 → 3/3 이름 → 저장, Esc는 보호된 버리기 확인</name>
  <reversibility rating="costly">`TemplateVariableSchema.shape`를 ValueShape로 확정한다 — 04-04의 오늘 날짜 후보와 04-13 틀 파일이 이 모양을 읽는다</reversibility>
  <files>src/core/value-shape.ts, src/core/submit-word.ts, src/core/template-draft.ts, src/core/template-schema.ts, src/core/danger.ts, src/page/overlay/template-save.ts, src/page/overlay/confirm-dialog.ts, src/page/template/controller.ts, src/shared/template-messages.ts, src/worker/template-router.ts, tests/e2e/template-save.e2e.ts</files>
  <read_first>
    - src/page/overlay/template-save.ts, src/page/template/controller.ts, src/worker/template-router.ts, src/core/template-schema.ts (04-02 결과)
    - src/page/overlay/confirm-dialog.ts (OpenConfirmOptions, TITLE_TEXT, 버튼·색 스타일)
    - src/core/confirm-guard.ts (createConfirmGuard — 호출하는 쪽이 키를 판정)
    - src/entrypoints/content.ts (openDangerConfirm 600행 근처 — 확인 화면 여는 방식)
    - src/core/danger.ts (stripSpaces·isDanger)
    - docs/design/SYSTEM.md ("틀 저장" 템플릿, 확인 화면 템플릿, 컴포넌트 규칙 — 버튼 위계)
    - .planning/phases/04-templates-activity-log/04-RESEARCH.md (Pattern 3, Pattern 4, Pitfall 5)
  </read_first>
  <behavior>
    - e2e 1: 기록 중 `#date` "2026-09-24", `#amount` "128,000", `#phone` "010-1234-5678", `#memo` "거래처 미팅 식대" → `#save-draft`(임시저장) → `#submit`(상신) → 결과 페이지에서 기록 끝내기 → 1/3에 카드 "1 일자 2026-09-24 매번 바뀜", "2 금액 128,000 매번 바뀜", "3 연락처 010-1234-5678 매번 바뀜", "4 적요 거래처 미팅 식대 항상 같음" → 4 → 적요가 "매번 바뀜" → Enter → 2/3에 "1 임시저장 중간 제출", "2 상신 제출 단계"와 "제출 뒤 단계 1개는 틀에 넣지 않아요" → Enter → 3/3에 "1 지출결의서", "2 지출결의서 상신", "9 직접 입력" → 2 → 저장된 틀: name "지출결의서 상신", variables 4개(stepIndex가 네 fill 단계), `#save-draft` press의 submit "intermediate", `#submit` press의 submit "final", 마지막 단계가 `#submit` press(뒤 navigated 없음)
    - e2e 2: 1/3에서 Esc → 확인 화면 제목 "기록을 버릴까요?", 버튼 "Enter 버리기"·"Esc 취소" → 바로 Enter(1초 안) → 아무 일 없음 → 1초 뒤 Enter → 저장 화면 닫힘, storage.session 'record-session' 없음, 'templates' 변화 없음
  </behavior>
  <action>
RED 먼저: `tests/e2e/template-save.e2e.ts`에 behavior 둘을 쓰고 실패 확인 후 RED 커밋.

`value-shape.ts`(D-11, RESEARCH Pattern 3): `DateShape = 'yyyy-mm-dd' | 'yyyy.mm.dd' | 'yyyymmdd' | 'mm/dd' | 'yyyy년 m월 d일'`, `ValueShape = { kind: 'date'; shape: DateShape } | { kind: 'amount' } | { kind: 'phone' } | { kind: 'text' }`와 같은 모양의 zod `ValueShapeSchema`. `detectShape(value: string, label: string): ValueShape` — 이 작업은 yyyy-mm-dd·쉼표 금액·하이픈 휴대전화만(나머지 경계는 Task 2). `renderToday(shape: DateShape, now: Date): string`. `submit-word.ts`(D-12, Pattern 4): `DEFAULT_SUBMIT_WORDS = ['저장', '상신', '등록', '제출', '신청', '전송']`, `isSubmitLike(name, words)`와 `matchedSubmitWord(name, words): string | null`(danger.ts의 `stripSpaces` + 포함 검사를 import해 같은 규칙). `template-draft.ts`(순수): `draftFromSteps(steps, submitWords): TemplateDraft` — `{ fills: Array<{ stepIndex, label, value, variable: boolean, shape }>, submits: Array<{ stepIndex, label, word }>, finalSubmitIndex: number | null, trailingCount: number }`(마지막 submit-like press가 기본 final, 그 뒤 단계 수). `toggleVariable(draft, stepIndex)`, `setFinalSubmit(draft, stepIndex)`, `nameCandidates(draft, steps): string[]`(1 제출 단계 pageTitle, 2 pageTitle + ' ' + word, 중복·빈 값 제거, 60자 자름), `buildTemplateSteps(steps, choices: { finalSubmitIndex: number | null; variableSteps: number[] }, submitWords): { steps; variables } | { error: 'invalid-choice' }` — final 뒤 단계를 자르고, final 앞 submit-like press는 'intermediate', final은 'final', 고른 fill에 `variableId = 'v' + stepIndex`, variables는 `{ id, stepIndex, label, shape: detectShape(value, label) }`. final이나 variable 인덱스가 그 종류 단계가 아니면 error. `template-schema.ts`의 variable `shape`를 `ValueShapeSchema`로 바꾼다(아직 저장된 틀이 없는 같은 단계 안 변경이라 형식 버전은 1 그대로).

`confirm-dialog.ts`: `OpenConfirmOptions`에 선택 필드 `title?: string`(기본 지금 제목), `rows?: ReadonlyArray<{ label: string; value: string }>`(있으면 "누를 버튼: …" 대신 칸 이름 + 값을 `--text-value` 크기 줄로), `tone?: 'danger' | 'primary'`(기본 'danger' = 지금 모양; 'primary'는 테두리·Enter 버튼 채움을 `--accent`)를 더한다. 기존 호출(위험 버튼)은 바뀌지 않는다(Pitfall 5 — 새 모달을 만들지 않는다). `name`은 그대로 Enter 버튼 글자다.

`template-save.ts`: 세 장. 1/3 fills 번호 카드(8개 넘으면 9 = 다음 장), 번호 = 토글, Enter = 다음. 2/3 submits 번호 카드("중간 제출"/"제출 단계" 글자), 번호 = final 바꾸기, trailingCount > 0이면 "제출 뒤 단계 N개는 틀에 넣지 않아요", submit-like가 없으면 "제출 버튼을 찾지 못했어요. 이대로 저장하면 제출 확인 없이 끝나요."(`--warning` 테두리 카드), Enter = 다음. 3/3 이름 후보(1·2) + 9 직접 입력, 번호 = 저장. Esc: 2/3·3/3은 앞 장, 1/3은 `openConfirm({ name: '버리기', title: '기록을 버릴까요?', tone: 'danger', … })` + `createConfirmGuard` — 확인이면 `record/discard` 전송. `template/save` 메시지를 `{ name, finalSubmitIndex, variableSteps }`로 넓히고 `record/discard {}`를 더한다(맨 위 프레임만, 라우터가 `clearRecordSession()`). 라우터의 `template/save`는 `buildTemplateSteps(session.steps, choices, submitWords)`로 만들고 error면 `{ ok: false, reason: 'invalid-choice' }`. 이 작업의 submitWords는 `DEFAULT_SUBMIT_WORDS`(설정 연결은 Task 3). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-save.e2e.ts tests/e2e/confirm.e2e.ts tests/e2e/danger.e2e.ts</automated>
    <fails_when>non-zero exit, or fewer than 2 template-save tests passed, or any existing confirm/danger test failed</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/overlay/confirm-dialog.ts`의 `OpenConfirmOptions`에 `title?`, `rows?`, `tone?`가 있고 새 확인 화면 파일이 생기지 않았다(`ls src/page/overlay | grep -ci confirm`이 1)
    - `src/worker/template-router.ts`에 `buildTemplateSteps(`와 `record/discard`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/template-save.e2e.ts` 2 passed, 기존 `confirm.e2e.ts`·`danger.e2e.ts` 통과
  </acceptance_criteria>
  <done>기록을 끝내면 세 장 저장 화면으로 바뀌는 값·제출 단계·이름을 번호로 고르고 저장하며, 버리기는 보호된 확인을 거친다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 판별 규칙 고정 — 값 모양(날짜 다섯 모양·8자리 라벨 조건·금액·전화)·제출 단어·초안 규칙</name>
  <files>src/core/value-shape.ts, tests/unit/value-shape.test.ts, src/core/submit-word.ts, tests/unit/submit-word.test.ts, src/core/template-draft.ts, tests/unit/template-draft.test.ts</files>
  <read_first>
    - src/core/value-shape.ts, src/core/submit-word.ts, src/core/template-draft.ts (Task 1 결과)
    - tests/unit/danger.test.ts (같은 모양의 단어 판별 시험)
    - .planning/phases/04-templates-activity-log/04-RESEARCH.md (Pattern 3 Trade-offs, Assumptions A9, Open Questions 4)
  </read_first>
  <behavior>
    - detectShape: "2026-09-24"→date yyyy-mm-dd, "2026.09.24"→yyyy.mm.dd, "2026년 9월 24일"→yyyy년 m월 d일, "09/24"→mm/dd
    - detectShape("20260924", "지출 일자")→date yyyymmdd, detectShape("20260924", "계좌")→amount 아님·date 아님(text), detectShape("20260924", "")→text
    - detectShape: "128,000"·"128000원"·"1,280"→amount, "12"→text(4자리 미만)
    - detectShape: "010-1234-5678"·"01012345678"·"02-123-4567"→phone(전화가 금액보다 먼저)
    - detectShape: 앞뒤 공백은 무시("  2026-09-24 ")
    - renderToday: 2026-09-04 기준 다섯 모양이 "2026-09-04", "2026.09.04", "20260904", "09/04", "2026년 9월 4일"
    - isSubmitLike("결재 상신", 기본)→true, ("임시저장")→true, ("저장 목록 보기")→true(포함 규칙), ("조회")→false, (빈 글자)→false, (단어 목록 빈 배열)→false, 공백만 다른 단어("상 신")→true
    - matchedSubmitWord("결재 상신", 기본)→"상신"
    - draftFromSteps: submit-like press가 둘이면 마지막이 final, 앞은 submits 목록에 남음; 없으면 finalSubmitIndex null; final 뒤 단계 수가 trailingCount
    - buildTemplateSteps: final 뒤 단계를 자르고, final 앞 submit-like는 intermediate; final 인덱스가 fill 단계면 error; variableSteps에 press 인덱스가 있으면 error; 입력 배열 불변
    - nameCandidates: pageTitle이 비어 있으면 그 후보를 빼고, 두 후보가 같으면 하나만
  </behavior>
  <action>
RED 먼저: 세 시험 파일에 behavior를 쓰고 실패 확인 후 RED 커밋(Task 1의 최소 구현이 못 하는 경계가 실패해야 한다).

`value-shape.ts`(Claude's Discretion 확정): 날짜 다섯 모양 정규식(RESEARCH Pattern 3 출발값), `DATE_LABEL_WORDS = ['일자', '날짜', '일시']`('일' 한 글자는 '파일'·'메일'에도 들어 있어 쓰지 않는다) — 8자리(yyyymmdd, 19xx·20xx로 시작)는 `stripSpaces(label)`에 이 낱말 중 하나가 있을 때만 날짜(A9, Open Q4 권장: 라벨 필수). 전화 검사를 금액보다 먼저. 금액은 숫자·쉼표 4자 이상 + 선택 "원". 전부 `value.trim()` 뒤 검사. `submit-word.ts`·`template-draft.ts`는 behavior대로 고친다. 모든 함수 순수(입력 불변). 통과하면 GREEN, 정리하면 REFACTOR 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/value-shape.test.ts tests/unit/submit-word.test.ts tests/unit/template-draft.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 25 tests passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/value-shape.ts`에 `DATE_LABEL_WORDS`가 있다
    - `grep -v '^\s*//' src/core/value-shape.ts src/core/submit-word.ts src/core/template-draft.ts | grep -cE 'document|window|chrome\.'`가 0
    - 세 시험 파일 합계 25개 이상 통과
  </acceptance_criteria>
  <done>바뀌는 값 자동 표시와 제출 성격 버튼 판별이 단위 시험으로 고정되었다(D-40).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 설정 형식 3(migrations[2]) + 옵션 페이지의 사이트별 제출 단어·틀 기본 속도</name>
  <reversibility rating="costly">설정 형식 3은 sync 저장소에 남아 다른 PC와 이후 형식 변환이 읽는다</reversibility>
  <files>src/core/settings-schema.ts, tests/unit/settings-schema.test.ts, src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/options/main.ts, src/entrypoints/options/index.html, src/core/submit-word.ts, src/page/overlay/template-save.ts, src/worker/template-router.ts, tests/e2e/options-template.e2e.ts</files>
  <read_first>
    - src/core/settings-schema.ts (Phase 3의 v2, settingsSpec, migrations[1])
    - tests/unit/settings-schema.test.ts
    - src/shared/messages.ts (UpdateSettingsOp patch)
    - src/worker/storage-writer.ts (updateSettings, syncItemBytes 한도)
    - Phase 3: src/entrypoints/options/main.ts, index.html (사이트별 위험 단어 편집 칸)
    - docs/design/SYSTEM.md (컴포넌트 규칙 — 입력·오류, 키 표시)
  </read_first>
  <behavior>
    - 단위: v2 설정(Phase 3 기본값)을 `migrate(raw, settingsSpec)` → `{ ok: true, migrated: true }`, 결과 v3에 `submitWords`가 `DEFAULT_SUBMIT_WORDS`, `siteSubmitWords`가 `[]`, `templateSpeed`가 'normal'이고 v2의 다른 값은 그대로(깊은 비교)
    - 단위: v3 `templateSpeed: 'fast'`(모르는 값)는 'invalid'
    - 단위: `submitWordsFor(settings, 'http://practice.test')`는 그 사이트 항목이 있으면 그 단어(기본을 대신함), 없으면 `settings.data.submitWords`
    - e2e 3: 옵션 페이지에서 사이트 "http://practice.test"의 제출 단어를 "결재올림"으로 저장 → sync 'settings'의 `siteSubmitWords`에 그 항목 → 연습 사이트에서 "결재올림" 버튼(workflow-form.html의 `?altsubmit=1`이면 `#submit` 글자가 "결재올림")을 누른 기록의 2/3 화면에 "결재올림 제출 단계"
    - e2e 4: 옵션 페이지 "틀 기본 속도"에서 "최대"를 고르면 sync 'settings'의 `templateSpeed`가 'max'
  </behavior>
  <action>
RED 먼저: 단위 behavior를 `settings-schema.test.ts`에(기존 시험 유지), e2e를 `tests/e2e/options-template.e2e.ts`에 쓰고 실패 확인 후 RED 커밋.

`settings-schema.ts`(D-03, D-04): `CURRENT_SCHEMA_VERSION = 3`, v3 데이터 = v2 데이터 + `submitWords: string[]`, `siteSubmitWords: Array<{ site: string; words: string[] }>`(Phase 3 `siteDangerWords`와 같은 모양), `templateSpeed: 'slow' | 'normal' | 'max'`. `settingsSpec.migrations[2]`는 v2 → v3 기본값 채움(입력 불변). `defaultSettings()`도 v3. `submit-word.ts`에 `submitWordsFor(settings, origin)`. `messages.ts` `updateSettings` 패치에 선택 필드 `siteSubmitWords`·`templateSpeed`를 더하고, `storage-writer.ts`의 `updateSettings`가 이를 반영(8KB 항목 한도 검사 그대로, 01-14).

옵션 페이지(Phase 3 편집 칸 옆, 같은 모양 — 새 색·radius 없음): "제출 단어" 영역(사이트 주소 칸 + 쉼표로 나눈 단어 칸 + [Enter 저장]), "틀 기본 속도" 영역(천천히·보통·최대 번호 카드 1·2·3, 고른 카드 남색 3px). 키보드만으로 다 된다. `template-save.ts`는 `submitWordsFor(currentSettings, location.origin)`을, 라우터는 `startUrl`의 origin으로 같은 함수를 쓴다(SW는 settings를 writer 읽기 경로로 읽는다). 연습 페이지 `workflow-form.html`에 `?altsubmit=1`이면 `#submit` 글자를 "결재올림"으로 바꾸는 인라인 스크립트를 더한다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/settings-schema.test.ts tests/unit/submit-word.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or any test reported as failed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/options-template.e2e.ts tests/e2e/template-save.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "4 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/settings-schema.ts`에 `CURRENT_SCHEMA_VERSION = 3`, `siteSubmitWords`, `templateSpeed`, `migrations`의 2번 자리가 있다
    - `src/core/submit-word.ts`에 `export function submitWordsFor`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/options-template.e2e.ts tests/e2e/template-save.e2e.ts` 4 passed
  </acceptance_criteria>
  <done>사이트별 제출 단어와 틀 기본 속도가 설정 형식 3에 들어가고, v2 설정은 값을 잃지 않고 바뀐다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- TMPL-02: edge probe `unclassified — review manually`. 가정한 경계(플래그):
  - 바뀌는 값 후보는 fill 단계만(체크·선택 목록의 선택은 "항상 같음"으로 저장). 선택 목록 값을 매번 바꾸고 싶은 경우는 다음 버전.
  - 모양 판별 우선순위: 날짜 → 전화 → 금액 → 글자. 8자리 숫자는 날짜 낱말 라벨이 있어야 날짜.
  - 입력 단계가 0개면 1/3은 "바뀌는 값이 없어요"만 보이고 Enter로 넘어간다.
- TMPL-03: edge probe `unclassified — review manually`. 가정한 경계(플래그):
  - 사이트별 제출 단어가 있으면 그 사이트에서는 기본 목록을 대신한다(더하기가 아님) — 기본 단어를 빼고 싶은 사이트를 위해.
  - 제출 성격 버튼이 하나도 없으면 제출 단계 없이 저장할 수 있다(조회만 하는 틀). 이때 화면이 "제출 확인 없이 끝나요"를 알리고, 위험 버튼 확인(D-12)은 그대로 적용된다.
  - 틀은 제출 단계에서 끝난다(뒤 단계는 자름). 제출 뒤 결과 확인은 04-05가 자동으로 한다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 맨 위 content script → SW(template/save) | 선택값(인덱스)이 건너온다 |
| 옵션 페이지 → SW(updateSettings) | 이용자가 적은 단어 |
| storage.sync ← 다른 PC | 다른 형식 버전의 설정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Tampering | template/save 선택값 | medium | mitigate | 라우터가 `buildTemplateSteps`로 인덱스 종류를 다시 검사하고 틀은 zod 통과 후에만 저장 |
| T-04-11 | Elevation of Privilege | 제출 단계 오표시 | high | mitigate | 제출 단어 판별을 단위 시험으로 고정, 2/3 화면에서 이용자가 눈으로 확인하고 바꿀 수 있음, 제출 버튼을 못 찾으면 `--warning` 카드로 알림 |
| T-04-12 | Tampering | 설정 v2→v3 변환 | medium | mitigate | `migrations[2]` 입력 불변 + 실패 시 원본 보존(01-14 경로) 단위 시험 |
| T-04-13 | Denial of Service | siteSubmitWords 크기 | low | mitigate | sync 항목 8KB 한도 검사(01-14 `SYNC_ITEM_LIMIT`) 그대로 |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 설치 없음(D-05) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/value-shape.test.ts tests/unit/submit-word.test.ts tests/unit/template-draft.test.ts tests/unit/settings-schema.test.ts` 전부 통과
- `CI=true pnpm exec playwright test tests/e2e/template-save.e2e.ts tests/e2e/options-template.e2e.ts tests/e2e/confirm.e2e.ts tests/e2e/danger.e2e.ts` 통과
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 저장 화면에서 날짜·금액·전화번호 칸이 바뀌는 값 후보로 미리 표시되고 번호로 바꿀 수 있다(TMPL-02)
- 제출 성격 버튼 중 마지막 것이 제출 단계로 표시되고 번호로 바꿀 수 있으며, 단어는 사이트별로 고칠 수 있다(TMPL-03)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/value-shape.ts`, `src/core/submit-word.ts`, `src/core/template-draft.ts`, `tests/unit/value-shape.test.ts`, `tests/unit/submit-word.test.ts`, `tests/unit/template-draft.test.ts`, `tests/e2e/template-save.e2e.ts`, `tests/e2e/options-template.e2e.ts`
- 심볼: `DateShape`, `ValueShape`, `ValueShapeSchema`, `detectShape()`, `renderToday()`, `DATE_LABEL_WORDS`, `DEFAULT_SUBMIT_WORDS`, `isSubmitLike()`, `matchedSubmitWord()`, `submitWordsFor()`, `TemplateDraft`, `draftFromSteps()`, `toggleVariable()`, `setFinalSubmit()`, `nameCandidates()`, `buildTemplateSteps()`, `OpenConfirmOptions.title/rows/tone`
- 메시지 type: `record/discard`, `template/save`(`finalSubmitIndex`, `variableSteps`로 넓힘), `updateSettings` 패치에 `siteSubmitWords`·`templateSpeed`
- 설정 필드(sync `settings`, schemaVersion 3): `submitWords`, `siteSubmitWords`, `templateSpeed`; `settingsSpec.migrations[2]`
- 연습 페이지: `workflow-form.html?altsubmit=1`

<output>
Create `.planning/phases/04-templates-activity-log/04-03-SUMMARY.md` when done
</output>
