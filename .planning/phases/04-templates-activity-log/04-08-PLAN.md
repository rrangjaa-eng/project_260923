---
phase: 04-templates-activity-log
plan: 08
type: execute
wave: 7
depends_on: ["04-07"]
files_modified:
  - src/core/value-candidates.ts
  - tests/unit/value-candidates.test.ts
  - src/core/template-schema.ts
  - src/worker/storage-writer.ts
  - src/worker/run-orchestrator.ts
  - src/page/overlay/value-cards.ts
  - tests/e2e/template-run.e2e.ts
  - src/core/run-copy.ts
  - tests/unit/run-copy.test.ts
  - src/core/frame-tree.ts
  - tests/unit/frame-tree.test.ts
  - src/worker/relay.ts
  - src/page/run/step-executor.ts
autonomous: true
requirements: [TMPL-06]
estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "바뀌는 값마다 후보 번호 카드가 뜬다: 그 틀 그 칸의 최근 값(이 PC만, 최대 3개), 오늘 날짜(날짜 모양일 때 기록된 모양 그대로), 기록할 때의 값, 9 직접 입력. 같은 값은 한 번만 보인다(D-14)"
    - "바뀌는 값이 여러 개면 '금액을 고르세요 · 1/2' → '연락처를 고르세요 · 2/2'처럼 차례로 고르고, 고른 값은 다음 실행의 최근 값이 된다(D-14, TMPL-06)"
    - "같은 출처·다른 출처 iframe 안 양식 단계도 그 프레임에서 실행된다 — SW가 단계의 프레임 경로로 frameId를 찾아 그 프레임에만 보낸다(D-01, D-09, TMPL-06)"
    - "대상 iframe이나 요소가 늦게 나타나도 기다림 한도 안이면 이어서 실행한다. 요소 기다림은 문서 바뀜 감시와 시간 재기로만 하고 화면 그리기 틀 콜백에 기대지 않는다(Phase 5 뒤에서 도는 탭에서도 멈추지 않게)"
    - "확인 문구의 조사는 제출 대상 이름의 받침에 맞는다('지출결의서를', '출장 신청을')(D-38)"
  artifacts:
    - path: "src/core/value-candidates.ts"
      provides: "바뀌는 값 후보 조립(순수)"
      exports: ["valueCandidates", "MAX_RECENT_VALUES", "MAX_VALUE_CANDIDATES"]
    - path: "src/core/template-schema.ts"
      provides: "틀별 최근 값 저장 형식"
      contains: "templateRecentKey"
    - path: "src/core/frame-tree.ts"
      provides: "프레임 경로 → frameId"
      exports: ["frameIdForFramePath"]
    - path: "src/worker/relay.ts"
      provides: "프레임 보고 바뀜 알림"
      contains: "onReportsChanged"
    - path: "src/page/run/step-executor.ts"
      provides: "요소 기다림(문서 바뀜 감시 + 시간 재기)"
      contains: "waitForMatch"
  key_links:
    - from: "src/worker/run-orchestrator.ts"
      to: "src/core/value-candidates.ts"
      via: "startRun의 choose-values 보기 후보 = valueCandidates({ shape, recordedValue, recent }, now)"
      pattern: "valueCandidates\\("
    - from: "src/worker/run-orchestrator.ts"
      to: "src/core/frame-tree.ts"
      via: "frameIdForFramePath(relay.resolvedFramesFor(tabId), step.target.fingerprint.framePath)로 보낼 프레임을 고름"
      pattern: "frameIdForFramePath\\("
    - from: "src/worker/run-orchestrator.ts"
      to: "src/worker/relay.ts"
      via: "relay.onReportsChanged(tabId)마다 기다리던 단계의 프레임을 다시 찾음"
      pattern: "onReportsChanged\\("
  prohibitions:
    - statement: "민감칸 값은 최근 값 후보·틀별 최근 값 저장에 들어가지 않는다"
      status: resolved
      verification: test
    - statement: "단계 실행기의 요소 기다림은 화면 그리기 틀 콜백을 쓰지 않는다"
      status: resolved
      verification: test
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 반복 업무를 한 번 해 보여 틀로 저장하고 이후 "틀 고르기 1번 + 바뀌는 값 2~3번 + 제출 확인 1번"으로 끝내기를, **so that** 되돌릴 수 없는 제출은 항상 확인 뒤에 한 번만 일어나고 문제가 생기면 활동 기록으로 추적할 수 있다.

<objective>
04-07의 실행 경로에 바뀌는 값 후보(최근 값·오늘·기록 값·직접 입력)와 iframe 안 단계 실행·늦은 요소 기다림을 더한다.

Purpose: TMPL-06(D-14의 후보, D-01·D-09의 프레임별 실행). 요소 기다림 방식(문서 바뀜 감시 + 시간 재기)은 Phase 5의 뒤에서 도는 탭 실행이 그대로 쓴다.
Output: `valueCandidates` + 단위 시험, 틀별 최근 값 저장, 조사 규칙 단위 시험, `frameIdForFramePath` + 단위 시험, relay 바뀜 알림, `waitForMatch`, e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-activity-log/04-CONTEXT.md
@.planning/phases/04-templates-activity-log/04-RESEARCH.md
@.planning/phases/04-templates-activity-log/04-07-SUMMARY.md
@docs/design/SYSTEM.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 패키지·새 확장 권한 없음(D-05). 필요해지면 멈추고 `checkpoint:decision`으로 이유 한 줄 + 승인을 받는다.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- tracer·`tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(04-08): …` → 최소 구현 → GREEN 커밋 `feat(04-08): …` → 정리하면 `refactor(04-08): …`. 제목 영어 접두어, 본문 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드). 틀은 `recordTemplate()`(04-06)으로 만든다.
- 오버레이는 Shadow DOM 안, tokens.css 변수만(D-36). 화면 모양은 04-07이 SYSTEM.md에 더한 "바뀌는 값 카드" 그대로(D-37). 카피 규칙(D-38).
- 저장 API 쓰기·지우기는 `src/worker/storage-writer.ts`의 `enqueue()` 안에만(D-03).
- 연습 사이트에서만 시험(D-39). 기존 파일은 이 계획이 필요한 줄만 바꾼다.

<tasks>

<task type="tracer">
  <name>Task 1: 끝에서 끝까지 — 값 둘을 차례로(직접 입력 포함) → 실행 → 다음 실행에서 고른 값이 "최근 값" 카드</name>
  <files>src/core/value-candidates.ts, src/core/template-schema.ts, src/worker/storage-writer.ts, src/worker/run-orchestrator.ts, src/page/overlay/value-cards.ts, tests/e2e/template-run.e2e.ts</files>
  <read_first>
    - src/worker/run-orchestrator.ts, src/page/overlay/value-cards.ts, src/core/run-state.ts (04-07 결과 — choose-values 보기, candidates `source`)
    - src/core/value-shape.ts (renderToday, ValueShape — 04-05)
    - src/core/template-schema.ts (TemplateVariableSchema)
    - .planning/phases/04-templates-activity-log/04-RESEARCH.md (Code Example 2)
    - .planning/phases/04-templates-activity-log/04-CONTEXT.md (D-14, Claude's Discretion "최근 값을 몇 개·어디에")
  </read_first>
  <behavior>
    - e2e 7: 금액·연락처가 바뀌는 값인 틀(`recordTemplate`, 일자는 "항상 같음") → 첫 장 3 → 1 → "금액을 고르세요 · 1/2" → 9 → 글자 칸에 "99,000" → Enter → "연락처를 고르세요 · 2/2" → 1(기록 값) → 양식 `#amount` "99,000", `#phone` 기록 값 → 확인 화면에서 Esc
    - e2e 8: 같은 틀을 다시 실행하면 금액 카드 "1 99,000 최근 값", "2 128,000 기록할 때의 값" — storage.local `template-recent:<templateId>`에 `99,000`
  </behavior>
  <action>
RED 먼저: e2e 7·8을 `template-run.e2e.ts`에 더하고 실패 확인 후 RED 커밋.

`src/core/value-candidates.ts`(D-14, Claude's Discretion 확정: 최근 값은 틀마다 따로, 이 PC만, 칸마다 3개 — 순수): `MAX_RECENT_VALUES = 3`, `MAX_VALUE_CANDIDATES = 8`(9는 직접 입력 자리), `valueCandidates(input: { shape: ValueShape; recordedValue: string; recent: readonly string[] }, now: Date): Array<{ value: string; source: 'recent' | 'today' | 'recorded' }>` — 차례 최근 → 오늘(날짜 모양일 때 `renderToday(shape.shape, now)`) → 기록 값, 같은 값은 앞 자리 하나만(이 작업은 최소 모양, 경계는 Task 2). `template-schema.ts`: `templateRecentKey(templateId) = 'template-recent:' + templateId`, `TemplateRecentV1 = { schemaVersion: 1, data: { values: Record<variableId, string[]> } }`. `storage-writer.ts`: `writeTemplateRecent(templateId, values: Record<string, string>)`(storage.local, 칸마다 새 값을 앞에 넣고 중복 제거 후 3개, `enqueue()` 안). 오케스트레이터: `startRun`의 choose-values 후보를 `valueCandidates({ shape, recordedValue, recent })`로(`recent`는 이 키에서 읽음), `provideRunValues`에서 받은 값을 `writeTemplateRecent`로 적는다 — 바뀌는 값은 민감칸이 될 수 없다(민감칸은 기록 때 manual 단계라 variable이 없다, D-10). `value-cards.ts`: 후보의 `source`를 "최근 값"/"오늘"/"기록할 때의 값" 작은 설명으로, 여러 값은 차례로(제목 "…을 고르세요 · n/m" — 조사는 `objectParticle`), 마지막 값 뒤 `run/values`. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-run.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "8 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/worker/run-orchestrator.ts`에 `valueCandidates(`와 `writeTemplateRecent(`가 있다
    - `src/core/template-schema.ts`에 `templateRecentKey`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/template-run.e2e.ts` 8 passed
  </acceptance_criteria>
  <done>값을 여러 개 차례로 고르고, 고른 값이 다음 실행의 최근 값 카드가 된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 후보 규칙과 조사 규칙 고정 — 최근 3개·오늘은 날짜만·중복 하나·8개 상한, 을/를</name>
  <files>src/core/value-candidates.ts, tests/unit/value-candidates.test.ts, src/core/run-copy.ts, tests/unit/run-copy.test.ts, tests/e2e/template-run.e2e.ts</files>
  <read_first>
    - src/core/value-candidates.ts (Task 1 결과), src/core/run-copy.ts (04-07 — objectParticle, submitQuestion)
    - tests/unit/danger.test.ts (describe/it 모양)
  </read_first>
  <behavior>
    - `valueCandidates({ shape: date yyyy-mm-dd, recordedValue: '2026-09-01', recent: ['2026-09-20', '2026-09-19'] }, now 2026-09-24)` → [최근 '2026-09-20', 최근 '2026-09-19', 오늘 '2026-09-24', 기록 '2026-09-01']
    - 최근 값이 4개면 앞 3개만
    - 오늘 날짜가 최근 값과 같으면 한 번만(최근 값 자리에)
    - 기록한 값이 최근 값과 같으면 한 번만
    - 날짜가 아닌 모양(amount)에는 "오늘" 후보가 없다
    - 빈 문자열·공백뿐인 후보는 빠지고, 후보는 최대 8개
    - yyyymmdd 모양 날짜의 오늘은 "20260924"
    - `objectParticle('지출결의서')`→'를', `('출장 신청')`→'을', `('ABC')`→'를', `('')`→'를', `('결재 2건')`→'을'
    - `submitQuestion('지출결의서', '상신')`→"지출결의서를 상신할까요?", `submitQuestion('출장 신청', '등록')`→"출장 신청을 등록할까요?", 앞뒤 공백은 없앤다
    - e2e 9: 일자가 바뀌는 값인 틀(yyyy-mm-dd로 기록) → 일자 카드에 "오늘" 설명과 함께 오늘 날짜(시험이 `new Date()`로 만든 같은 모양 글자)
  </behavior>
  <action>
RED 먼저: `tests/unit/value-candidates.test.ts`·`tests/unit/run-copy.test.ts`에 behavior를, e2e 9를 `template-run.e2e.ts`에 쓰고 실패 확인 후 RED 커밋.

`valueCandidates`는 behavior 규칙대로(입력 불변, 비교는 `trim()` 뒤). `objectParticle`은 글자를 뒤에서부터 보아 처음 만나는 한글 음절(가~힣)의 받침(`(code - 0xac00) % 28 !== 0`)이 있으면 '을', 없으면 '를', 한글 음절이 하나도 없으면 '를'. 두 파일 모두 순수(DOM·전역 창·확장 API를 쓰지 않음). 통과하면 GREEN, 정리하면 REFACTOR 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/value-candidates.test.ts tests/unit/run-copy.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 12 tests passed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-run.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "9 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/value-candidates.ts`에 `export const MAX_RECENT_VALUES = 3`과 `export const MAX_VALUE_CANDIDATES = 8`이 있다
    - `grep -vE '^\s*(//|\*|/\*)' src/core/value-candidates.ts src/core/run-copy.ts | grep -cE '\b(document|window|chrome)\.'`가 0
    - 두 단위 시험 파일 합계 12개 이상 통과
    - `CI=true pnpm exec playwright test tests/e2e/template-run.e2e.ts` 9 passed
  </acceptance_criteria>
  <done>값 후보 차례·중복·상한과 확인 문구 조사가 단위 시험으로 고정되었다(D-40).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: iframe 안 단계 실행과 늦은 프레임·요소 기다림 — 프레임 경로 → frameId, 보고 바뀜 알림, 화면 그리기와 무관한 기다림</name>
  <files>src/core/frame-tree.ts, tests/unit/frame-tree.test.ts, src/worker/relay.ts, src/worker/run-orchestrator.ts, src/page/run/step-executor.ts, tests/e2e/template-run.e2e.ts</files>
  <read_first>
    - src/core/frame-tree.ts (framePathForFrameId — 04-04)
    - src/worker/relay.ts (resolvedFramesFor, frame/report 저장 자리)
    - src/worker/run-orchestrator.ts, src/page/run/step-executor.ts (Task 1·04-07 결과)
    - src/page/collector/collector.ts (보고 갱신 방식 — 이 계획은 고치지 않는다)
    - tests/practice-site/workflow-frame.html (04-02 — `?late=1`)
  </read_first>
  <behavior>
    - 단위: `frameIdForFramePath(resolved, [])`는 0, `['id:form-frame']`는 그 자식 frameId, 두 단계 경로도 찾음, 없는 경로는 undefined, `framePathForFrameId`와 왕복하면 같은 값
    - e2e 10: workflow-frame.html에서 기록한 틀(#form-frame 안 `#amount`, #other-frame(http://other.test) 안 `#memo`, 둘 다 "항상 같음")을 실행 → 두 iframe 안 칸이 기록 값으로 채워진다
    - e2e 11: 같은 틀의 시작 주소를 `workflow-frame.html?late=1`로 바꿔(`writeStorage`) 실행 → iframe이 700ms 늦게 붙어도 두 칸이 채워진다
  </behavior>
  <action>
RED 먼저: 단위 behavior를 `frame-tree.test.ts`에, e2e 10·11을 `template-run.e2e.ts`에 더하고 실패 확인 후 RED 커밋.

`frame-tree.ts`: `frameIdForFramePath(resolved, framePath: readonly string[], topFrameId = 0): number | undefined`(맨 위에서 pathKey를 차례로 따라감, 순수). `relay.ts`: `onReportsChanged(listener: (tabId: number) => void): () => void` — frame/report를 저장할 때마다 부른다(기존 동작 변경 없음). 오케스트레이터(D-01, D-09): 단계의 framePath로 frameId를 찾아 그 프레임에만 `run/step`을 보내고(navigated는 그 framePath 프레임의 `frame/ready`를 기다림), 못 찾으면 `waitingSince`를 적고 `onReportsChanged`가 그 탭에 올 때마다 다시 찾는다. 기다림 한도는 `DEFAULT_ELEMENT_WAIT_MS = 10000`(04-13이 속도별 한도로 옮김), 넘으면 04-07의 not-found 경로(04-11이 막힘 카드로). SW가 잠들어 기다림이 끊겨도 다음 frame/report 이벤트에서 상태를 다시 읽어 이어진다(D-17) — 리스너는 SW 최상위에서 한 번 건다.

`step-executor.ts`: `waitForMatch(step, { timeoutMs, signal }): Promise<Element | null>` — 먼저 한 번 찾고, 없으면 문서 전체를 지켜보는 MutationObserver(childList·subtree·attributes)가 바뀜을 알릴 때마다 다시 찾고, `setTimeout(timeoutMs)`이 끝나면 null. 되풀이 찾기는 바뀜 알림과 짧은 `setTimeout`으로만 모으고 rAF(화면 그리기 틀 콜백)는 쓰지 않는다 — 뒤에서 도는 탭(Phase 5)에서는 그 콜백이 멈추기 때문이다. `signal`이 끊기면 감시를 풀고 null. 04-07의 "못 찾으면 곧바로 not-found"를 이것으로 바꾼다. collector.ts는 고치지 않는다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/frame-tree.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or any test reported as failed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/template-run.e2e.ts tests/e2e/frames.e2e.ts</automated>
    <fails_when>non-zero exit, or fewer than 11 template-run tests passed, or any frames.e2e.ts test failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/frame-tree.ts`에 `export function frameIdForFramePath`가 있고, `src/worker/relay.ts`에 `onReportsChanged`가 있다
    - `src/page/run/step-executor.ts`에 `waitForMatch`와 `MutationObserver`가 있고, `grep -vE '^\s*(//|\*|/\*)' src/page/run/step-executor.ts | grep -c 'requestAnimationFrame'`가 0이다
    - `src/worker/run-orchestrator.ts`에 `frameIdForFramePath(`와 `onReportsChanged(`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/template-run.e2e.ts` 11 passed, 기존 `frames.e2e.ts` 통과
  </acceptance_criteria>
  <done>같은 출처·다른 출처 iframe 안 단계가 그 프레임에서 실행되고, 늦게 붙는 프레임·요소도 기다림 한도 안이면 이어진다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- TMPL-06(04-07 소유 항목의 연장). 가정한 경계(플래그):
  - 최근 값은 틀마다·칸마다 따로 3개, 이 PC(storage.local)만. 다른 틀의 같은 이름 칸과 섞지 않는다.
  - 선택 목록(select)도 바뀌는 값이면 후보 카드로 고르지만, 그 목록에 없는 값을 직접 입력하면 `fillValue`가 맞는 옵션을 못 찾아 04-11 막힘으로 간다.
  - 프레임 보고(relay)는 Phase 1 collector의 갱신 주기를 따른다. collector가 화면 그리기 틀 콜백으로 보고를 모은다면 뒤에서 도는 탭에서 보고가 늦을 수 있다 — Phase 5가 확인할 자리로 남긴다(이 계획은 collector를 고치지 않는다).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SW → 대상 프레임(run/step) | 어느 프레임에 단계를 보낼지 |
| 페이지 DOM → 요소 기다림 | 페이지가 요소를 늦게 붙이거나 끝없이 바꿀 수 있다 |
| storage.local `template-recent:*` | 최근 값 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-79 | Spoofing | 프레임 고르기 | high | mitigate | 프레임은 SW가 relay 보고로 framePath → frameId를 찾아 고르고(페이지가 고르지 않음), 요소는 그 프레임 안에서 `findConfidentMatch` |
| T-04-74 | Denial of Service | 요소 기다림 | low | mitigate | 기다림 한도 `DEFAULT_ELEMENT_WAIT_MS`, `signal`로 감시 해제 — 끝없는 DOM 바뀜에도 한도에서 끝남 |
| T-04-75 | Information Disclosure | 틀별 최근 값 | low | mitigate | 바뀌는 값만 저장(민감칸은 variable이 될 수 없음, D-10), 이 PC storage.local만, 칸마다 3개 |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 설치 없음(D-05) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/value-candidates.test.ts tests/unit/run-copy.test.ts tests/unit/frame-tree.test.ts` 통과
- `CI=true pnpm exec playwright test tests/e2e/template-run.e2e.ts tests/e2e/frames.e2e.ts` 통과(template-run 11)
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 바뀌는 값을 최근 값·오늘·기록 값·직접 입력 번호 카드로 고른다(D-14)
- iframe 안 양식 단계가 그 프레임에서 실행된다(TMPL-06, D-01)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/value-candidates.ts`, `tests/unit/value-candidates.test.ts`, `tests/unit/run-copy.test.ts`
- 심볼: `valueCandidates()`, `MAX_RECENT_VALUES`, `MAX_VALUE_CANDIDATES`, `templateRecentKey()`, `TemplateRecentV1`, writer `writeTemplateRecent()`, `frameIdForFramePath()`, `Relay.onReportsChanged()`, step-executor `waitForMatch()`, 오케스트레이터 `DEFAULT_ELEMENT_WAIT_MS`
- 저장 키: `template-recent:<templateId>`(storage.local, TemplateRecentV1)

<output>
Create `.planning/phases/04-templates-activity-log/04-08-SUMMARY.md` when done
</output>
