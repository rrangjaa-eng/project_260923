---
phase: 04-templates-activity-log
plan: 15
type: execute
wave: 14
depends_on: ["04-14"]
files_modified:
  - src/core/mask-pii.ts
  - tests/unit/mask-pii.test.ts
  - src/worker/activity-log.ts
  - src/core/activity-log-schema.ts
  - tests/unit/activity-log-schema.test.ts
  - src/worker/storage-writer.ts
  - src/core/value-shape.ts
  - tests/practice-site/log-pii.html
  - tests/e2e/activity-log-mask.e2e.ts
autonomous: true
requirements: [LOG-01]
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "활동 기록에 쓰는 모든 글자는 저장 전에 주민번호·계좌번호·카드번호·전화번호 모양을 가린다(숫자를 *로, 구분 기호는 그대로) — 가림은 logActivity 한 곳에서만 한다(D-32, PRIV-01, LOG-01)"
    - "가림의 모양 판정은 Phase 3 sensitive.ts의 모양 함수(looksLikeRrn·looksLikeAccount·looksLikeCard)와 04-05 value-shape의 전화 모양을 그대로 쓴다 — 자릿수 규칙을 새로 쓰지 않고, sensitive.ts는 고치지 않는다"
    - "하루 항목은 MAX_ACTIVITY_PER_DAY(1000)줄을 넘지 않고 넘으면 오래된 줄부터 버린다. 14일(오늘 포함)이 지난 날의 항목은 새 기록을 쓸 때 지워진다(D-33)"
    - "최근 2주 동안의 기록은 이 PC의 storage.local에만 하루 단위로 남는다(D-32, D-33, LOG-01)"
  artifacts:
    - path: "src/core/mask-pii.ts"
      provides: "자유 글자 속 개인정보 모양 가리기(순수)"
      exports: ["maskPii"]
    - path: "src/core/activity-log-schema.ts"
      provides: "하루 상한·14일·색인"
      exports: ["MAX_ACTIVITY_PER_DAY", "ACTIVITY_KEEP_DAYS", "ACTIVITY_INDEX_KEY", "ActivityIndexV1", "appendEntry", "pruneDays"]
    - path: "tests/practice-site/log-pii.html"
      provides: "개인정보 모양 글자가 든 버튼"
  key_links:
    - from: "src/worker/activity-log.ts"
      to: "src/core/mask-pii.ts"
      via: "logActivity 때마다 text = maskPii(text) 뒤 200자 자름 → writer.appendActivity"
      pattern: "maskPii\\("
    - from: "src/core/mask-pii.ts"
      to: "Phase 3 src/core/sensitive.ts"
      via: "숫자 덩어리마다 looksLikeRrn || looksLikeCard || looksLikeAccount, 또는 value-shape PHONE_PATTERN"
      pattern: "looksLikeAccount\\("
    - from: "src/worker/storage-writer.ts"
      to: "src/core/activity-log-schema.ts"
      via: "appendActivity가 appendEntry(상한) → 색인 갱신 → pruneDays의 remove 키 지우기를 한 enqueue 안에서"
      pattern: "pruneDays\\("
  prohibitions:
    - statement: "주민번호·계좌번호·카드번호·전화번호 모양을 가리지 않은 채 활동 기록에 쓰지 않는다"
      status: resolved
      verification: test
    - statement: "개인정보 모양 판정을 두 군데에 따로 두지 않는다 — Phase 3 모양 함수를 그대로 쓴다"
      status: resolved
      verification: test
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 반복 업무를 한 번 해 보여 틀로 저장하고 이후 "틀 고르기 1번 + 바뀌는 값 2~3번 + 제출 확인 1번"으로 끝내기를, **so that** 되돌릴 수 없는 제출은 항상 확인 뒤에 한 번만 일어나고 문제가 생기면 활동 기록으로 추적할 수 있다.

<objective>
활동 기록 입구(`logActivity`)에 개인정보 모양 가리기를 넣고, 하루 상한과 14일 지우기를 더한다.

Purpose: LOG-01(D-32 가림, D-33 하루 단위·14일·상한). 04-17의 알림 창 글, 04-14의 누름 글자가 모두 이 입구를 지나 가려진다.
Output: `maskPii` + 단위 시험, 하루 상한·14일·색인 + 단위 시험, 연습 페이지, e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-activity-log/04-CONTEXT.md
@.planning/phases/04-templates-activity-log/04-RESEARCH.md
@.planning/phases/04-templates-activity-log/04-14-SUMMARY.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 패키지·새 확장 권한 없음(D-05 — `alarms` 권한 쓰지 않음). 필요해지면 멈추고 `checkpoint:decision`으로 이유 한 줄 + 승인을 받는다.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- tracer·`tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(04-15): …` → 최소 구현 → GREEN 커밋 `feat(04-15): …` → 정리하면 `refactor(04-15): …`. 제목 영어 접두어, 본문 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드).
- 저장 API 쓰기·지우기는 `src/worker/storage-writer.ts`의 `enqueue()` 안에만(D-03).
- 연습 사이트에서만 시험(D-39). 기존 파일은 이 계획이 필요한 줄만 바꾼다.

## Assumes from Phase 3 (브랜치 `origin/claude/phase3-plans-ng6f32`의 계획 이름 — 실제 이름이 다르면 실행 전에 고친다)

- `src/core/sensitive.ts`(03-10)가 모양 **함수** `looksLikeRrn(value)`(숫자 6 + 하이픈 선택 + 숫자 7), `looksLikeAccount(value)`(숫자·하이픈·공백만, 숫자 10~16자리), `looksLikeCard(value)`(숫자·하이픈·공백만, 숫자 14~16자리 + `luhnValid`)를 export한다. 이 계획은 sensitive.ts를 고치지 않는다.

<tasks>

<task type="tracer">
  <name>Task 1: 끝에서 끝까지 — 도우미로 "계좌 110-123-456789 확인" 버튼을 누름 → 활동 기록 줄에 "계좌 ***-***-****** 확인"</name>
  <files>src/core/mask-pii.ts, src/worker/activity-log.ts, src/core/value-shape.ts, tests/practice-site/log-pii.html, tests/e2e/activity-log-mask.e2e.ts</files>
  <read_first>
    - src/worker/activity-log.ts (logActivity — 04-09)
    - src/core/value-shape.ts (전화 모양 판정 — 04-05)
    - Phase 3: src/core/sensitive.ts (looksLikeRrn, looksLikeAccount, looksLikeCard)
    - src/entrypoints/content.ts (log/append — 04-14)
    - .planning/phases/04-templates-activity-log/04-RESEARCH.md (Pattern 13, Pitfall 4)
  </read_first>
  <behavior>
    - e2e 1: `log-pii.html`(title "기록 가리기", `button#pii` "계좌 110-123-456789 확인", `button#phone` "010-1234-5678로 전화")에서 도우미 번호표로 `#pii`를 누름 → 오늘 `activity:` 항목의 press 줄 text가 "버튼 '계좌 ***-***-****** 확인' 누름" → `#phone`을 누르면 "버튼 '***-****-****로 전화' 누름" → 오늘 항목 JSON 문자열에 "110-123-456789"·"010-1234-5678"이 없다
  </behavior>
  <action>
RED 먼저: `tests/e2e/activity-log-mask.e2e.ts`에 behavior를 쓰고 실패 확인 후 RED 커밋.

`value-shape.ts`: 전화 모양 판정에 쓰는 정규식을 `export const PHONE_PATTERN`으로 내보낸다(동작 변경 없음 — 이미 상수면 export만). `src/core/mask-pii.ts`(D-32, "한 곳" 원칙 — 순수, DOM·전역 창·확장 API를 쓰지 않음): `maskPii(text: string): string` — 글자에서 "숫자로 시작하고 끝나며 사이에 숫자·한 칸 공백·하이픈만 있는 가장 긴 덩어리"를 차례로 찾고(앞뒤가 숫자가 아닌 자리에서 끊음), 덩어리마다 Phase 3 `looksLikeRrn(run) || looksLikeCard(run) || looksLikeAccount(run)`이거나 `PHONE_PATTERN.test(run)`이면 그 덩어리의 숫자만 `*`로 바꾼다(구분 기호·공백 유지). 자릿수 규칙을 새로 쓰지 않는다 — 판정은 두 곳의 함수·정규식에 맡긴다. `activity-log.ts`의 `logActivity`가 text를 `maskPii` 뒤 200자로 자른다(가림은 이 한 곳). 연습 페이지 `log-pii.html`(behavior대로). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/activity-log-mask.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "1 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/worker/activity-log.ts`에 `maskPii(`가 있다
    - `src/core/mask-pii.ts`에 `from '@/core/sensitive'`와 `PHONE_PATTERN`이 있다
    - `git log --oneline -F --grep='(04-15)' -- src/core/sensitive.ts`가 아무것도 출력하지 않는다(이 계획이 sensitive.ts를 바꾸지 않음)
    - `CI=true pnpm exec playwright test tests/e2e/activity-log-mask.e2e.ts` 1 passed
  </acceptance_criteria>
  <done>도우미로 누른 버튼 글자 속 계좌·전화번호가 가려진 채 기록된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 가림 규칙 고정 — 주민·계좌·카드·전화는 가리고 금액·날짜·짧은 숫자는 그대로, 두 정의가 어긋나지 않음</name>
  <files>src/core/mask-pii.ts, tests/unit/mask-pii.test.ts</files>
  <read_first>
    - src/core/mask-pii.ts (Task 1 결과)
    - Phase 3: tests/unit/sensitive.test.ts (모양 함수 시험 표본)
  </read_first>
  <behavior>
    - `maskPii('주민 900101-1234567 확인')` → '주민 ******-******* 확인'
    - `maskPii('계좌 110-123-456789')` → '계좌 ***-***-******'
    - `maskPii('카드 4111 1111 1111 1111')` → '카드 **** **** **** ****'
    - `maskPii('연락처 010-1234-5678')` → '연락처 ***-****-****', `('02-123-4567')`도 가림
    - `maskPii('금액 128,000원')`은 그대로, `('2026-09-24')`은 그대로(날짜), `('주문 12345')`는 그대로(짧은 숫자)
    - 가려진 결과의 길이·구분 기호 자리는 원문과 같다
    - 한 글자 안에 둘(계좌와 전화)이 있으면 둘 다 가린다
    - 숫자 사이 공백이 두 칸 이상이면 덩어리가 끊긴다('1234  5678'은 그대로)
    - Phase 3 모양 함수가 참인 표본(주민·계좌·카드 각각)을 통째로 넣으면 결과에 숫자가 남지 않는다(두 정의가 어긋나지 않음)
    - 빈 글자는 빈 글자
  </behavior>
  <action>
RED 먼저: `tests/unit/mask-pii.test.ts`에 behavior를 쓰고 실패 확인 후 RED 커밋(Task 1 최소 구현이 못 하는 경계가 실패해야 한다). `maskPii`를 behavior대로 고친다 — 덩어리 찾기는 숫자·한 칸 구분자만 이어 붙이는 한 가지 규칙, 판정은 Phase 3 함수와 `PHONE_PATTERN`에만. 통과하면 GREEN, 정리하면 REFACTOR 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/mask-pii.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 10 tests passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*|/\*)' src/core/mask-pii.ts | grep -cE '\\d\{[0-9]'`가 0(자릿수 규칙을 새로 쓰지 않음)
    - `grep -vE '^\s*(//|\*|/\*)' src/core/mask-pii.ts | grep -cE '\b(document|window|chrome)\.'`가 0
    - `pnpm exec vitest run tests/unit/mask-pii.test.ts` 10개 이상 통과
  </acceptance_criteria>
  <done>가림 규칙이 단위 시험으로 고정되었고 Phase 3 모양 판정과 어긋나지 않는다(D-32, D-40).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 하루 상한·14일 지우기 — 색인으로 지난 날 지우기</name>
  <reversibility rating="costly">`activity-index`와 하루 항목 형식은 14일 동안 저장소에 남는다</reversibility>
  <files>src/core/activity-log-schema.ts, tests/unit/activity-log-schema.test.ts, src/worker/storage-writer.ts, tests/e2e/activity-log-mask.e2e.ts</files>
  <read_first>
    - src/core/activity-log-schema.ts, tests/unit/activity-log-schema.test.ts (04-09·04-14 결과)
    - src/worker/storage-writer.ts (appendActivity, MAX_PRESS_ENTRIES 상한 패턴)
    - .planning/phases/04-templates-activity-log/04-RESEARCH.md (Assumptions A6)
  </read_first>
  <behavior>
    - 단위: `appendEntry(day, entry)` — 1000줄이면 가장 오래된 줄을 버리고 1000줄 유지, 입력 불변
    - 단위: `pruneDays(['2026-09-01', '2026-09-10', '2026-09-11', '2026-09-24'], '2026-09-24')` → `{ keep: ['2026-09-11', '2026-09-24'], remove: ['2026-09-01', '2026-09-10'] }`(오늘 포함 14일 = 09-11부터)
    - 단위: 날짜 문자열이 아닌 색인 항목은 remove로 간다
    - 단위: 달·해가 바뀌는 경계(2026-01-05 기준 2025-12-23은 keep, 2025-12-22는 remove)
    - e2e 2: 시험 준비로 `writeStorage`에 20일 전 날짜의 `activity:` 항목과 그 날짜가 든 `activity-index`를 넣은 뒤 도우미로 한 번 누름 → 20일 전 키가 없어지고 `activity-index`에 오늘이 있다
  </behavior>
  <action>
RED 먼저: 단위 behavior를 `activity-log-schema.test.ts`에(기존 시험 유지), e2e 2를 `activity-log-mask.e2e.ts`에 더하고 실패 확인 후 RED 커밋.

`activity-log-schema.ts`(순수): `MAX_ACTIVITY_PER_DAY = 1000`, `ACTIVITY_KEEP_DAYS = 14`, `ACTIVITY_INDEX_KEY = 'activity-index'`, `ActivityIndexV1 = { schemaVersion: 1, data: { days: string[] } }`, `appendEntry(day, entry)`, `pruneDays(days, today)`(지역 날짜 문자열 비교). writer `appendActivity`: 오늘 항목에 `appendEntry` → 색인에 오늘을 넣고 `pruneDays`의 remove 날짜 키를 지운다(없는 키 지우기는 오류가 아님 — A6; 지우기 실패는 잡아서 알리기만 하고 큐를 막지 않음) — 모두 한 `enqueue()` 안. 지우기는 새 기록을 쓸 때만 한다(`alarms` 권한 없음). 통과하면 GREEN, 정리하면 REFACTOR 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/activity-log-schema.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or any test reported as failed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/activity-log-mask.e2e.ts tests/e2e/activity-log.e2e.ts</automated>
    <fails_when>non-zero exit, or fewer than 2 activity-log-mask tests passed, or any activity-log test failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/activity-log-schema.ts`에 `MAX_ACTIVITY_PER_DAY = 1000`, `ACTIVITY_KEEP_DAYS = 14`, `export function pruneDays`가 있다
    - `src/worker/storage-writer.ts`에 `pruneDays(`와 `appendEntry(`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/activity-log-mask.e2e.ts` 2 passed, `activity-log.e2e.ts` 회귀 없음
  </acceptance_criteria>
  <done>하루 항목은 상한을 넘지 않고, 14일 지난 날은 새 기록 때 지워진다(D-33, D-40).</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- LOG-01: edge probe `unclassified — review manually`. 가정한 경계(플래그):
  - 가림은 모양만 보고 한다(카드 Luhn이 틀려도 10~16자리면 계좌 모양으로 가려진다 — 덜 가리는 쪽보다 더 가리는 쪽이 안전).
  - 하루 상한 1000줄, 넘으면 오래된 줄부터 버림(Claude's Discretion). 글자는 200자에서 자른다(04-09).
  - 14일은 오늘 포함(오늘부터 13일 전까지 남김). 지우기는 새 기록을 쓸 때 한다 — 2주 넘게 아무것도 안 쓰면 옛 기록이 남아 있다가 다음 기록 때 지워진다.
  - 날짜는 이 PC의 지역 시각 기준.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 페이지 글자·알림 창 글 → 활동 기록 | 버튼 글자·칸 이름·창 글에 개인정보가 섞일 수 있다 |
| SW → storage.local(activity:*) | 하루 항목 크기와 보존 기간 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-31 | Information Disclosure | 활동 기록 글자 | high | mitigate | 모든 기록이 `logActivity`의 `maskPii`를 지남(주민·계좌·카드·전화), 판정은 Phase 3 모양 함수 그대로(표본 일치 단위 시험), e2e 1 |
| T-04-33 | Denial of Service | 활동 기록 크기 | low | mitigate | 하루 1000줄 상한 + 14일 지우기(단위 시험, e2e 2) |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 설치 없음(D-05) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/mask-pii.test.ts tests/unit/activity-log-schema.test.ts` 통과
- `CI=true pnpm exec playwright test tests/e2e/activity-log-mask.e2e.ts tests/e2e/activity-log.e2e.ts` 통과
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 활동 기록이 가려진 채, 하루 상한 안에서, 2주 동안만 남는다(LOG-01)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/mask-pii.ts`, `tests/unit/mask-pii.test.ts`, `tests/practice-site/log-pii.html`, `tests/e2e/activity-log-mask.e2e.ts`
- 심볼: `maskPii()`, `PHONE_PATTERN`(value-shape export), `MAX_ACTIVITY_PER_DAY`, `ACTIVITY_KEEP_DAYS`, `ACTIVITY_INDEX_KEY`, `ActivityIndexV1`, `appendEntry()`, `pruneDays()`
- 저장 키: `activity-index`(storage.local, ActivityIndexV1)

<output>
Create `.planning/phases/04-templates-activity-log/04-15-SUMMARY.md` when done
</output>
