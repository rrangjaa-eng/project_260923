---
phase: 04-templates-activity-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/practice-site/workflow-dialogs.html
  - src/core/dialog-policy.ts
  - tests/unit/dialog-policy.test.ts
  - src/entrypoints/dialog-watcher.content.ts
  - tests/e2e/spike-04.e2e.ts
  - tests/e2e/sw-control.ts
autonomous: true
requirements: [TMPL-11, TMPL-13]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "틀이 돌지 않을 때(실행 표시 없음) 연습 페이지의 confirm·alert·prompt는 원래 브라우저 창 그대로 뜬다 — 감시 장치가 가로채지 않는다(D-26)"
    - "실행 표시가 켜진 상태에서 무장(arm)한 뒤 5초(DEFAULT_ARM_WINDOW_MS) 안에 뜬 첫 confirm 하나만 '확인'(true)으로 답하고, 이어지는 두 번째 confirm과 무장하지 않은 confirm은 '취소'(false)로 답한다(D-27)"
    - "실행 표시가 켜진 동안 alert은 창을 띄우지 않고 닫히며(내용은 helper:dialog-seen으로 발행), prompt는 null로 답한다(D-27, Claude's Discretion: prompt)"
    - "감시 장치는 모든 프레임(about:blank·srcdoc 포함)에 document_start로 선언되어, 페이지 head의 첫 인라인 스크립트가 실행되기 전에 confirm이 이미 바뀌어 있다 — 스파이크가 실측해 SUMMARY에 기록한다(D-26, RESEARCH A4)"
    - "isolated world에서 arm 이벤트를 보낸 직후 같은 호출 스택에서 누른 버튼의 onclick 안 confirm이 무장된 상태를 본다 — 스파이크가 실측해 SUMMARY에 기록한다(RESEARCH A3)"
    - "e2e가 확장 service worker를 강제로 멈출 수 있다: 멈춘 뒤 새 SW가 뜨고, storage.session 값은 남고, 옛 SW의 globalThis 표시는 사라진다 — 방법과 결과를 SUMMARY에 기록한다(TMPL-13 시험 방법, D-41, RESEARCH A7)"
    - "manifest의 permissions는 storage·scripting·tabs 그대로다 — 새 권한 없음(D-05)"
  artifacts:
    - path: "src/core/dialog-policy.ts"
      provides: "알림 창 규칙(순수): 실행 표시·무장·첫 confirm만 확인"
      exports: ["createDialogPolicy", "DEFAULT_ARM_WINDOW_MS", "DialogPolicy", "ConfirmDecision"]
    - path: "src/entrypoints/dialog-watcher.content.ts"
      provides: "main world 알림 창 감시 장치(모든 프레임, document_start, manifest 등록)"
    - path: "tests/practice-site/workflow-dialogs.html"
      provides: "제출 뒤 확인 창 + 이어지는 두 번째 확인 창 + 안내 창 + prompt + 이른 패치 검사(D-39)"
    - path: "tests/e2e/sw-control.ts"
      provides: "e2e용 service worker 강제 종료 도우미"
      exports: ["forceStopServiceWorker"]
  key_links:
    - from: "src/entrypoints/dialog-watcher.content.ts"
      to: "src/core/dialog-policy.ts"
      via: "window.confirm·alert·prompt 대체 함수가 policy.decideConfirm/decideAlert/decidePrompt(Date.now())를 동기로 부른다"
      pattern: "decideConfirm\\("
    - from: "src/entrypoints/dialog-watcher.content.ts"
      to: "document CustomEvent"
      via: "helper:run-state·helper:arm-next-confirm를 듣고(문자열 detail을 JSON.parse) helper:dialog-seen을 발행"
      pattern: "helper:arm-next-confirm"
    - from: "tests/e2e/spike-04.e2e.ts"
      to: "tests/e2e/sw-control.ts"
      via: "forceStopServiceWorker(context, page)로 SW를 멈추고 새 SW를 관찰"
      pattern: "forceStopServiceWorker"
  prohibitions:
    - statement: "틀 실행 밖에서는 페이지의 confirm·alert·prompt를 가로채거나 대신 답하지 않는다(원래 창이 그대로 뜬다)"
      status: resolved
      verification: test
    - statement: "main world 감시 장치는 확장 API나 확장 저장소에 닿지 않는다(순수 규칙 모듈만 가져온다)"
      status: resolved
      verification: test
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 반복 업무를 한 번 해 보여 틀로 저장하고 이후 "틀 고르기 1번 + 바뀌는 값 2~3번 + 제출 확인 1번"으로 끝내기를, **so that** 되돌릴 수 없는 제출은 항상 확인 뒤에 한 번만 일어나고 문제가 생기면 활동 기록으로 추적할 수 있다.

<objective>
Phase 4에서 가장 불확실한 두 기술(RESEARCH "진짜 신규 리스크")을 먼저 실측한다: (1) main world 알림 창 감시 장치가 "무장 신호 → 같은 호출 스택의 누르기 → 사이트의 동기 confirm"에 제때 답하는가, (2) Playwright 안에서 확장 service worker를 강제로 멈추고 새 SW를 관찰할 수 있는가. 둘 다 이 계획이 제품 코드(감시 장치 + 규칙)와 시험 도구로 남긴다.

Purpose: TMPL-11(알림 창 규칙의 바탕, D-26·D-27)과 TMPL-13(SW 잠듦 이어 가기 시험 방법, D-41)의 선행 스파이크. Phase 1 Plan 01-12처럼 결과를 SUMMARY의 "스파이크 결과" 표로 남기고, 뒤 계획(04-14·04-17·04-18)이 실패 시 바꿀 방향을 여기서 정한다.
Output: `dialog-policy.ts`+단위 시험, `dialog-watcher.content.ts`(main world), 연습 페이지 `workflow-dialogs.html`, `spike-04.e2e.ts`, `sw-control.ts`, SUMMARY 스파이크 결과 표.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-activity-log/04-CONTEXT.md
@.planning/phases/04-templates-activity-log/04-RESEARCH.md
@.planning/phases/01-click-helper-foundation/01-12-SUMMARY.md
@tests/e2e/fixtures.ts
@wxt.config.ts
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 패키지·새 확장 권한 없음(D-05). 필요해지면 멈추고 `checkpoint:decision`으로 이유 한 줄 + 승인을 받는다.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- tracer·`tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(04-01): …` → 최소 구현 → GREEN 커밋 `feat(04-01): …` → 정리하면 `refactor(04-01): …`. 제목 영어 접두어, 본문 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드).
- 저장 API 쓰기·지우기는 `src/worker/storage-writer.ts`에만(D-03). 시험 준비 코드는 예외.
- 연습 사이트에서만 시험. 실제 회사 시스템 접속·시험 금지(D-39).
- 기존 파일은 이 계획이 필요한 줄만 바꾼다(인접 리팩터·포맷 변경 금지).

## Assumes from Phase 1

- `tests/e2e/fixtures.ts`의 `context`·`serviceWorker`·`extensionId`·`servePage`·`openPopup` 픽스처(Plan 01-01~01-12에서 만듦). 연습 사이트 파일은 `http://practice.test/`와 `http://other.test/` 두 호스트로 같은 폴더에서 서빙된다.
- STATE.md: `chrome.runtime.reload()`는 이 샌드박스에서 새 SW를 관찰 가능하게 깨우지 않았다 — 그래서 이 스파이크가 CDP 방법을 확인한다.

<tasks>

<task type="tracer">
  <name>Task 1: 끝에서 끝까지 — 실행 표시 + 무장 → 누르기 → 첫 confirm만 확인, 두 번째는 취소, 틀 밖은 원래 창</name>
  <files>tests/practice-site/workflow-dialogs.html, src/core/dialog-policy.ts, src/entrypoints/dialog-watcher.content.ts, tests/e2e/spike-04.e2e.ts</files>
  <read_first>
    - wxt.config.ts (지금 permissions·content script 설정)
    - src/entrypoints/content.ts (defineContentScript 선언 모양만 — 1~40행)
    - node_modules/wxt/dist/types.d.mts (MainWorldContentScriptEntrypointOptions: world, registration, matchAboutBlank, matchOriginAsFallback — 600~810행)
    - tests/e2e/fixtures.ts (serviceWorker·servePage 픽스처)
    - .planning/phases/04-templates-activity-log/04-RESEARCH.md (Pattern 8, Code Example 4, Pitfall 1)
    - .planning/phases/04-templates-activity-log/04-CONTEXT.md (D-26, D-27)
  </read_first>
  <behavior>
    - e2e 1(틀 밖): `workflow-dialogs.html`에서 `#confirm-once`를 누르면 Playwright `page.on('dialog')`가 type 'confirm', 메시지 "정말 진행할까요?"를 받는다(가로채지 않음)
    - e2e 2(틀 안): SW에서 `chrome.scripting.executeScript`(world 'ISOLATED')로 한 함수 안에서 차례로 helper:run-state(active true) → helper:arm-next-confirm(windowMs 5000) → `#submit-two-confirms` click → 페이지 `#result`가 "first=true second=false"이고 Playwright dialog 이벤트는 0번
    - e2e 3(이른 패치): 페이지 head 첫 인라인 스크립트가 적은 `document.documentElement.dataset.earlyPatched`가 "true"
  </behavior>
  <action>
RED 먼저: `tests/e2e/spike-04.e2e.ts`에 behavior 셋을 쓰고(연습 페이지·감시 장치가 없어 실패) RED 커밋.

연습 페이지 `tests/practice-site/workflow-dialogs.html`(D-39, 설계 6.8 예시 문구 그대로): head 맨 앞 인라인 스크립트가 `Function.prototype.toString.call(window.confirm)`에 "[native code]"가 없으면 `document.documentElement.dataset.earlyPatched = 'true'`, 있으면 'false'. 본문 버튼 `#confirm-once`(onclick: confirm("정말 진행할까요?") 결과를 `#result`에 "once=true/false"), `#submit-two-confirms`(onclick: 첫 confirm("결재선이 없습니다. 계속할까요?"), 이어서 confirm("이미 상신된 문서입니다") 결과를 `#result`에 "first=…second=…"), `#alert-once`(alert("저장되었습니다") 뒤 `#result`에 "alert-returned"), `#prompt-once`(prompt("사유를 입력하세요") 결과를 `#result`에 "prompt=null" 또는 값), 그리고 `srcdoc` iframe `#blank-frame`(안에 같은 `#confirm-once` 버튼). 외부 요청 없음.

`src/core/dialog-policy.ts`(순수, document·window·chrome 참조 없음): `DEFAULT_ARM_WINDOW_MS = 5000`(Claude's Discretion "첫 확인 창" 시간, RESEARCH Open Q2 출발값). `createDialogPolicy()`가 `{ setRunActive(active: boolean): void; arm(windowMs: number, now: number): void; decideConfirm(now: number): ConfirmDecision }`를 돌려준다. `ConfirmDecision = 'native' | 'accept' | 'reject'`. 규칙(D-27): 실행 표시가 꺼져 있으면 'native'; 켜져 있고 무장 시간 안이면 무장을 즉시 풀고 'accept'(첫 하나만); 그 밖은 'reject'. 이 작업은 confirm만 — alert·prompt와 경계 규칙은 Task 2.

`src/entrypoints/dialog-watcher.content.ts`(D-26): `defineContentScript`에 `matches: ['<all_urls>']`, `allFrames: true`, `world: 'MAIN'`, `runAt: 'document_start'`, `matchAboutBlank: true`, `matchOriginAsFallback: true`, `registration: 'manifest'`(RESEARCH Pattern 8·Pitfall 1 — 런타임 주입은 늦다). `main()`에서 원래 `window.confirm`을 bind해 보관하고 대체 함수를 넣는다: `decideConfirm(Date.now())`가 'native'면 원래 함수 결과, 'accept'면 `helper:dialog-seen`(detail은 JSON 문자열 `{"kind":"expected-confirm","message":…}`)을 발행하고 true, 'reject'면 kind "unexpected-confirm"으로 발행하고 false. `document`의 `helper:run-state`(detail 문자열 `{"active":boolean}`)와 `helper:arm-next-confirm`(detail 문자열 `{"windowMs":number}`)을 듣고 JSON.parse — 형식이 틀리면 무시. detail을 문자열로 주고받는 이유: isolated↔main 두 세계 사이에서 객체 detail은 건너가지 않을 수 있다. 이 파일은 `@/core/dialog-policy` 말고는 아무것도 가져오지 않고 `chrome.` 참조가 없다.

e2e는 `serviceWorker.evaluate`로 탭 id를 찾아 `chrome.scripting.executeScript({ target: { tabId }, world: 'ISOLATED', func })`를 부른다 — func 안에서 run-state·arm 발행과 `#submit-two-confirms` click을 한 동기 함수로 한다(제품의 "arm 직후 같은 스택에서 synthesizePress" 순서를 흉내). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/spike-04.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "3 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/entrypoints/dialog-watcher.content.ts`에 `world: 'MAIN'`, `runAt: 'document_start'`, `allFrames: true`, `matchOriginAsFallback: true`, `registration: 'manifest'`가 있다
    - `grep -v '^\s*//' src/entrypoints/dialog-watcher.content.ts | grep -c 'chrome\.'`가 0을 출력한다
    - `CI=true pnpm exec playwright test tests/e2e/spike-04.e2e.ts`가 3 passed
    - `pnpm build` 뒤 `.output/chrome-mv3/manifest.json`의 content_scripts 중 하나가 `"world": "MAIN"`과 `"run_at": "document_start"`를 가진다
  </acceptance_criteria>
  <done>틀 밖에서는 원래 창이 뜨고, 실행 표시 + 무장 뒤에는 첫 confirm만 확인·두 번째는 취소되는 한 줄 경로가 연습 페이지에서 끝에서 끝까지 돈다. RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 알림 창 규칙의 경계 — 시간 지남·무장 무시·alert 닫기·prompt 취소·실행 끝</name>
  <files>src/core/dialog-policy.ts, tests/unit/dialog-policy.test.ts, src/entrypoints/dialog-watcher.content.ts, tests/e2e/spike-04.e2e.ts</files>
  <read_first>
    - src/core/dialog-policy.ts (Task 1 결과)
    - src/entrypoints/dialog-watcher.content.ts (Task 1 결과)
    - tests/unit/confirm-guard.test.ts (단위 시험 모양 — describe/it)
    - .planning/phases/04-templates-activity-log/04-CONTEXT.md (D-26, D-27, Claude's Discretion "prompt와 beforeunload")
  </read_first>
  <behavior>
    - 실행 표시 꺼짐: decideConfirm·decideAlert·decidePrompt 모두 'native'
    - 실행 표시 꺼진 채 arm → 그 뒤 실행 표시를 켜도 무장되지 않음(decideConfirm 'reject')
    - 켜짐 + arm(5000, t) → decideConfirm(t+4999) 'accept'
    - 켜짐 + arm(5000, t) → decideConfirm(t+5000) 'reject'(시간 지남)
    - 첫 'accept' 뒤 같은 무장 시간 안의 두 번째 decideConfirm은 'reject'
    - 켜짐 + 무장 없음 → decideConfirm 'reject'
    - 켜짐 → decideAlert 'suppress', decidePrompt 'reject'
    - arm 뒤 setRunActive(false) → decideConfirm 'native', 다시 켜도 무장이 남지 않음('reject')
    - arm을 두 번 불러도 'accept'는 한 번뿐
    - `DEFAULT_ARM_WINDOW_MS`는 5000
    - e2e 4: 실행 표시 켜짐에서 `#alert-once` → Playwright dialog 0번, `#result` "alert-returned", 발행된 helper:dialog-seen kind가 "alert"이고 message "저장되었습니다"
    - e2e 5: 실행 표시 켜짐에서 `#prompt-once` → dialog 0번, `#result` "prompt=null"
    - e2e 6: `#blank-frame`(srcdoc) 안의 confirm도 바뀌어 있다(toString에 "[native code]" 없음)
  </behavior>
  <action>
RED 먼저: `tests/unit/dialog-policy.test.ts`에 단위 behavior 열을, `spike-04.e2e.ts`에 e2e 4~6을 쓰고 실패 확인 후 RED 커밋.

`dialog-policy.ts`에 `decideAlert(now): 'native' | 'suppress'`와 `decidePrompt(now): 'native' | 'reject'`를 더하고(D-27 "단순 안내 창은 내용을 활동 기록에 남기고 닫는다"; prompt는 Claude's Discretion으로 실행 중 null — 틀은 prompt에 값을 넣지 않는다), 경계 규칙을 behavior대로 고친다: arm은 실행 표시가 켜져 있을 때만 받는다, setRunActive(false)는 무장도 푼다, 무장 끝 시각은 `now + windowMs` 미만까지 유효. `beforeunload`는 건드리지 않는다(원래 동작 — 가정 절 참고).

`dialog-watcher.content.ts`: `window.alert` 대체 — 'native'면 원래 함수, 'suppress'면 kind "alert"로 helper:dialog-seen 발행 후 undefined. `window.prompt` 대체 — 'native'면 원래 함수, 'reject'면 kind "prompt"로 발행 후 null. 실행 표시가 꺼져 있으면 helper:dialog-seen을 아예 발행하지 않는다. e2e는 isolated world 리스너(executeScript로 설치)가 받은 detail을 `document.documentElement.dataset.seen`에 이어 적게 해 확인한다. 통과하면 GREEN 커밋. 정리할 것이 있으면 REFACTOR 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/dialog-policy.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 10 tests passed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/spike-04.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "6 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/dialog-policy.ts`에 `export const DEFAULT_ARM_WINDOW_MS = 5000`, `decideAlert`, `decidePrompt`가 있다
    - `grep -v '^\s*//' src/core/dialog-policy.ts | grep -cE 'document|window|chrome\.'`가 0을 출력한다(주석 줄 제외)
    - `pnpm exec vitest run tests/unit/dialog-policy.test.ts` 10개 이상 통과
    - `CI=true pnpm exec playwright test tests/e2e/spike-04.e2e.ts` 6 passed
  </acceptance_criteria>
  <done>알림 창 규칙이 단위 시험으로 고정되었고(D-40 "알림 창 규칙(첫 확인 창만 확인)"), 실행 중 alert·prompt 처리와 srcdoc 프레임 적용이 e2e로 확인되었다.</done>
</task>

<task type="auto">
  <name>Task 3: 스파이크 — CDP로 확장 SW 강제 종료 + content script Blob 내려받기</name>
  <files>tests/e2e/sw-control.ts, tests/e2e/spike-04.e2e.ts</files>
  <read_first>
    - tests/e2e/fixtures.ts (context·serviceWorker·extensionId·openPopup)
    - .planning/phases/04-templates-activity-log/04-RESEARCH.md (Code Example 3, Pitfall 3, Pattern 12, Assumptions A3·A4·A7·A8)
    - .planning/STATE.md (runtime.reload 관찰 실패 기록)
    - .planning/phases/01-click-helper-foundation/01-12-SUMMARY.md (스파이크 결과 표 모양)
  </read_first>
  <action>
`tests/e2e/sw-control.ts`에 `forceStopServiceWorker(context: BrowserContext, page: Page): Promise<Worker>`를 만든다(시험 도구, 제품 코드 아님). 방법 1(RESEARCH Code Example 3): `context.newCDPSession(page)` → `Target.getTargets` → type 'service_worker'이고 url이 `chrome-extension://<extensionId>/`로 시작하는 타겟 → `Target.closeTarget`. 방법 2(방법 1이 새 SW를 만들지 못할 때): 확장 페이지(`openPopup`)에 CDP 세션을 붙여 `ServiceWorker.enable` → `ServiceWorker.stopAllWorkers`. 멈춘 뒤 확장 페이지에서 `chrome.runtime.sendMessage`로 SW를 깨우고 `context.waitForEvent('serviceworker')` 또는 `context.serviceWorkers()`의 새 항목을 돌려준다. 어느 방법이 통했는지를 함수 결과 로그로 남긴다.

`spike-04.e2e.ts`에 두 시험을 더한다.
- e2e 7(A7): 멈추기 전 SW에서 `globalThis.__spikeMarker = 1`과 `chrome.storage.session.set({ spike: 1 })`(시험 준비 코드) → `forceStopServiceWorker` → 새 SW에서 `globalThis.__spikeMarker`가 undefined이고 `storage.session`의 spike가 1.
- e2e 8(A8): 연습 페이지에서 executeScript(world 'ISOLATED')로 keydown 리스너를 달고(Enter면 `new Blob`·`URL.createObjectURL`·`<a download="spike.json">` click을 동기로), `page.keyboard.press('Enter')` → `page.waitForEvent('download')`의 `suggestedFilename()`이 "spike.json"이고 내용이 JSON `{"a":1}`. Enter 처리 안에서 동기로 하는 이유: 사용자 동작 안에서만 내려받기가 막히지 않는다(RESEARCH Pattern 12).

두 시험 중 하나가 어떤 방법으로도 통과하지 않으면 그 시험은 `test.fail`이 아니라 지우지 말고 `test.skip(true, '<실측 이유>')`로 두고, SUMMARY에 결과를 적는다 — 이 작업의 목적은 "되는지 알아내기"다. SUMMARY에 "## 스파이크 결과" 표(항목·방법·결과·뒤 계획 영향)를 A3(동기 arm), A4(이른 패치), A7(SW 강제 종료), A8(content script 내려받기) 네 줄로 쓴다. 실패 시 뒤 계획이 바꿀 방향:
- A3 실패 → 04-17은 무장을 이벤트 대신 `document.documentElement`의 `data-helper-armed-until` 속성(두 세계가 같은 DOM을 보므로 동기)으로 바꾼다.
- A4 실패 → 페이지가 열리자마자 부르는 confirm은 원래 창으로 뜬다(범위 밖, 04-17 가정에 적음). 제출 버튼 핸들러 안 confirm은 영향 없음.
- A7 실패(두 방법 모두) → 04-18의 e2e는 SW의 시험 고리 `globalThis.__templateRuns.simulateRestart()`(04-18이 더함 — 메모리 상태를 버리고 시작 경로 `resumeAll`을 다시 부름)로 재개를 확인하고, "chrome://serviceworker-internals에서 Stop 후 이어 가기"를 04-23 수동 확인 목록에 더한다.
- A8 실패 → 04-14의 기록 내보내기 파일 저장은 옵션 페이지에서 한다(명령판 카드는 SW의 `chrome.runtime.openOptionsPage()`로 옵션 페이지를 연다 — 새 권한 없음).
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/spike-04.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary reports fewer than 6 passed, or "No tests found"</fails_when>
    <automated>grep -c "스파이크 결과" .planning/phases/04-templates-activity-log/04-01-SUMMARY.md</automated>
    <fails_when>outputs 0 or the file does not exist</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/sw-control.ts`에 `export async function forceStopServiceWorker`와 `Target.closeTarget`, `ServiceWorker.stopAllWorkers`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/spike-04.e2e.ts`가 8 passed, 또는 6 passed + 실측 이유가 적힌 skip 1~2개
    - `04-01-SUMMARY.md`의 "스파이크 결과" 표에 A3·A4·A7·A8 네 줄이 있고 각 줄에 "통과" 또는 "실패"와 뒤 계획 영향이 적혀 있다
    - `pnpm build` 뒤 manifest permissions를 정렬해 이으면 정확히 "scripting,storage,tabs"다
  </acceptance_criteria>
  <done>SW 강제 종료 방법과 content script 내려받기 가능 여부가 실측되었고, 뒤 계획이 실패 시 따를 방향이 SUMMARY에 적혔다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- TMPL-11: 이 계획은 알림 창 규칙의 바탕만 만든다. edge probe 항목은 04-17이 가진다.
- TMPL-13: 이 계획은 시험 방법만 확인한다. edge probe 항목은 04-18이 가진다.
- 이 계획의 가정(검증 대상):
  - `beforeunload`는 실행 중에도 건드리지 않는다(Claude's Discretion) — 페이지 떠나기 확인은 원래대로 뜨고, 그러면 결과 대기가 끝나 "확인 필요"로 간다(04-10 결과 대기, 04-17 확인 필요 카드). 제출을 넘어서 자동으로 떠나지 않는 편이 안전하다.
  - 페이지가 우리 대체 함수를 다시 덮어쓰면 가로채기는 풀리고 원래 창이 뜬다 — 틀은 결과를 못 받아 "확인 필요"로 멈춘다(재시도 없음). 성공으로 오판하지 않는다.
  - 두 세계 사이 detail은 JSON 문자열로만 주고받는다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 페이지 스크립트 ↔ main world 감시 장치 | 같은 세계에서 돈다 — 페이지가 이벤트를 흉내 내거나 함수를 덮어쓸 수 있다 |
| main world ↔ isolated world | document CustomEvent(문자열 detail)만 오간다 |
| 시험 도구 ↔ 브라우저 | CDP 명령(시험 코드 안에서만) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | helper:run-state·helper:arm-next-confirm | low | accept | 페이지가 흉내 내도 바뀌는 것은 그 페이지 자기 confirm의 답뿐이다(페이지는 원래 confirm을 안 부를 수도 있다). isolated 쪽은 실제 실행이 없으면 helper:dialog-seen을 무시한다(04-10) |
| T-04-02 | Tampering | window.confirm 대체 함수 | medium | mitigate | 페이지가 덮어쓰면 원래 창이 뜨고 틀은 결과를 못 받아 "확인 필요"로 멈춘다(04-10 OUTCOME_WAIT_MS) — 자동 진행 없음 |
| T-04-03 | Elevation of Privilege | dialog-watcher.content.ts | medium | mitigate | main world 파일은 순수 규칙만 가져오고 `chrome.` 참조가 없다(acceptance grep) — 페이지가 이 스크립트를 통해 확장 기능에 닿을 길이 없다 |
| T-04-04 | Information Disclosure | helper:dialog-seen detail | low | accept | 담긴 글은 그 페이지가 띄운 자기 글이다. 활동 기록에 넣기 전 가림(04-15 maskPii) |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 이 계획은 패키지를 설치하지 않는다(새 의존성 0, D-05) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/dialog-policy.test.ts` 10개 이상 통과
- `CI=true pnpm exec playwright test tests/e2e/spike-04.e2e.ts` 8 passed(또는 실측 이유가 적힌 skip 포함 6 passed 이상)
- `pnpm typecheck && pnpm lint` 통과, manifest permissions 불변
- SUMMARY "스파이크 결과" 표 네 줄
</verification>

<success_criteria>
- 틀 밖에서는 알림 창을 가로채지 않고, 틀 안에서는 무장 뒤 첫 confirm 하나만 확인한다는 규칙이 단위 시험과 연습 페이지로 확인되었다(TMPL-11 바탕)
- SW 강제 종료 시험 방법이 정해졌다(TMPL-13 시험 방법)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/dialog-policy.ts`, `tests/unit/dialog-policy.test.ts`, `src/entrypoints/dialog-watcher.content.ts`, `tests/practice-site/workflow-dialogs.html`, `tests/e2e/spike-04.e2e.ts`, `tests/e2e/sw-control.ts`
- 심볼: `createDialogPolicy()`, `DialogPolicy`, `ConfirmDecision`, `DEFAULT_ARM_WINDOW_MS`, `forceStopServiceWorker()`
- 페이지 이벤트(document CustomEvent, detail = JSON 문자열): `helper:run-state` `{active}`, `helper:arm-next-confirm` `{windowMs}`, `helper:dialog-seen` `{kind: 'expected-confirm' | 'unexpected-confirm' | 'alert' | 'prompt', message}`
- manifest: main world content script 하나(`world: MAIN`, `document_start`, 모든 프레임, `match_origin_as_fallback`). 새 권한 없음.

<output>
Create `.planning/phases/04-templates-activity-log/04-01-SUMMARY.md` when done (반드시 "## 스파이크 결과" 표 포함)
</output>
