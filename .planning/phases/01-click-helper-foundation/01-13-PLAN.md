---
phase: 01-click-helper-foundation
plan: 13
type: execute
wave: 13
depends_on: ["01-12"]
files_modified:
  - src/core/unsupported-url.ts
  - tests/unit/unsupported-url.test.ts
  - src/entrypoints/background.ts
  - src/entrypoints/popup/main.ts
  - src/shared/messages.ts
  - src/worker/storage-writer.ts
  - src/entrypoints/content.ts
  - tests/e2e/fixtures.ts
  - tests/e2e/site-toggle.e2e.ts
autonomous: true
requirements: [SAFE-04, SAFE-05, STOR-01]
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "확장 아이콘 메뉴의 '2 이 사이트에서 끄기'를 누르면 지금 사이트(맨 위 페이지의 출처)에서만 도우미가 꺼지고, 다른 사이트 탭에서는 계속 동작한다"
    - "사이트별 끄기와 고정 번호는 storage.sync의 사이트별 항목(site:<origin>)에 저장되어 같은 브라우저 계정의 다른 PC에도 적용되고, 동기화로 값이 바뀌면 열린 탭이 곧바로 따른다"
    - "사이트별 끄기를 바꿔도 그 사이트의 고정 번호는 그대로 남는다"
    - "브라우저 설정 화면·웹스토어 등 확장이 동작하지 않는 페이지에서는 확장 아이콘에 '도울 수 없음'(배지 '없음')이 표시되고, 메뉴도 그 이유와 다음 행동을 한 문장으로 알려 준다"
    - "설정을 빠르게 여러 번 바꿔도 동기화 쓰기 한도에 걸리지 않고 마지막 상태로 저장된다"
    - {statement: "다른 PC(같은 브라우저 계정)에서 사이트별 끄기가 그대로 적용된다(실제 다른 PC 확인)", verification: backstop}
  artifacts:
    - path: "src/core/unsupported-url.ts"
      provides: "'도울 수 없음' 주소 판정"
      exports: ["isUnsupportedUrl"]
    - path: "src/worker/storage-writer.ts"
      provides: "site:<origin> 쓰기, 같은 키 쓰기 합치기, 분당 쓰기 한도 지키기"
      contains: "setSiteDisabled"
  key_links:
    - from: "src/entrypoints/background.ts"
      to: "src/core/unsupported-url.ts"
      via: "tabs.onActivated/onUpdated → isUnsupportedUrl 또는 ping 실패 → action.setTitle/setBadgeText"
      pattern: "isUnsupportedUrl"
    - from: "src/entrypoints/content.ts"
      to: "src/worker/storage-writer.ts"
      via: "site/query로 맨 위 출처를 받고 storage.onChanged(site:<origin>)로 끄기 반영"
      pattern: "site:"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 어느 사이트에서든(iframe 본문 포함) 원하는 요소를 마우스를 대충 가져가거나 숫자 키 하나로 누르고, 방해되면 즉시 끌 수 있기를, **so that** 떨림으로 인한 잘못 누름과 위험한 버튼 오조작 없이 평소 사이트를 빠르게 쓸 수 있다.

<objective>
지금 사이트에서만 끄기와 "도울 수 없음" 표시를 만든다. 사이트별 설정은 사이트별 동기화 항목에 나눠 저장하고, 빠른 연속 변경도 동기화 한도 안에서 마지막 상태로 남긴다.

Purpose: SAFE-04(D-20), SAFE-05(D-21), STOR-01(D-23, D-24). 다른 PC 확인은 수동(D-29).
Output: `unsupported-url.ts`+단위 시험, 확장 아이콘 제목·배지, 메뉴 카드 2와 "도울 수 없음" 안내, `setSiteDisabled` 저장·쓰기 합치기, content script 사이트별 반영, fixture의 대상 탭 지정, e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-click-helper-foundation/01-CONTEXT.md
@.planning/phases/01-click-helper-foundation/01-RESEARCH.md
@.planning/phases/01-click-helper-foundation/01-12-SUMMARY.md
@docs/design/SYSTEM.md
@docs/design/tokens.css
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 승인된 패키지 밖은 설치하지 않는다.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(01-13): …` → 최소 구현 → GREEN 커밋 `feat(01-13): …`. 본문은 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드).
- 팝업 스타일은 `docs/design/tokens.css` 변수만. 배지 색도 새로 만들지 않고 tokens.css의 값을 읽어 쓴다(D-26).
- `chrome.storage.*.set`은 `src/worker/storage-writer.ts`에만(D-24).
- 연습 사이트와 브라우저 내부 페이지(`chrome://version`)에서만 시험. 실제 회사 시스템 접속·시험 금지(D-14).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: "도울 수 없음" 주소 판정 (순수 함수)</name>
  <files>src/core/unsupported-url.ts, tests/unit/unsupported-url.test.ts</files>
  <read_first>
    - .planning/phases/01-click-helper-foundation/01-RESEARCH.md ("Open Questions (RESOLVED)" 6번)
    - .planning/phases/01-click-helper-foundation/01-CONTEXT.md (D-21)
  </read_first>
  <behavior>
    - `chrome://`, `edge://`, `whale://`, `chrome-extension://`, `about:`, `view-source:`, `devtools://`, `chrome-search://`, `data:`로 시작하면 true
    - 크롬 웹스토어(`chromewebstore.google.com`, `chrome.google.com/webstore`), 엣지 추가 기능(`microsoftedge.microsoft.com/addons`), 웨일 스토어(`store.whale.naver.com`)는 true
    - `http://practice.test/`, `https://example.com/a`는 false
    - `file://` 주소는 true(파일 주소 권한을 요청하지 않음)
    - 빈 문자열·잘못된 주소·`undefined`는 true(판정할 수 없으면 도울 수 없음)
    - `https://chrome.google.com/search`처럼 스토어가 아닌 같은 호스트 경로는 false
    - 대소문자가 섞인 스킴(`CHROME://settings`)도 true
  </behavior>
  <action>
RED 먼저: `tests/unit/unsupported-url.test.ts`에 behavior 일곱을 쓰고 실패 확인 후 RED 커밋.

`src/core/unsupported-url.ts`(D-21): `isUnsupportedUrl(url: string | undefined): boolean`. `URL`로 파싱(실패하면 true), 스킴이 `http:`·`https:`가 아니면 true, 스토어 호스트·경로 목록이면 true. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/unsupported-url.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 7 tests passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/unsupported-url.ts`에 `export function isUnsupportedUrl`이 있고 `document`·`window`·`chrome.` 참조가 없다
    - `pnpm exec vitest run tests/unit/unsupported-url.test.ts` 7개 이상 통과
  </acceptance_criteria>
  <done>"도울 수 없음" 판정 규칙이 단위 시험으로 고정되었다. RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 확장 아이콘 "도울 수 없음"과 메뉴 안내, 시험용 대상 탭 지정</name>
  <files>src/entrypoints/background.ts, src/entrypoints/popup/main.ts, tests/e2e/fixtures.ts, tests/e2e/site-toggle.e2e.ts</files>
  <read_first>
    - src/core/unsupported-url.ts
    - src/entrypoints/background.ts, src/entrypoints/popup/main.ts
    - tests/e2e/fixtures.ts (openPopup)
    - docs/design/SYSTEM.md ("상태 표 — 명령판 일부만", "카피 규칙", "색")
    - docs/design/tokens.css (`--muted`)
  </read_first>
  <behavior>
    - e2e: `chrome://version` 탭을 활성으로 하면 SW의 `chrome.action.getTitle({ tabId })`가 "도울 수 없음", `getBadgeText`가 "없음"
    - e2e: 연습 사이트 탭은 제목 "손 떨림 도우미", 배지 ""
    - e2e: `chrome://version` 탭을 대상으로 연 메뉴에 "이 페이지에서는 도울 수 없어요. 다른 탭에서 쓰세요."가 보이고 카드 2(이 사이트에서 끄기)는 없다
  </behavior>
  <action>
RED 먼저: `fixtures.ts`의 `openPopup(target?: Page)`가 대상 페이지의 탭 id를 SW에서 찾아(`chrome.tabs.query({ url })`) `popup.html?tabId=<id>`로 연다(시험에서는 팝업 탭 자신이 활성 탭이 되기 때문). `servePage(path, html, headers?)`가 응답 머리글도 받게 한다(Task 3의 sandbox 페이지용). behavior 셋을 `tests/e2e/site-toggle.e2e.ts`에 쓰고 실패 확인 후 RED 커밋.

`background.ts`(D-21): `tabs.onActivated`·`tabs.onUpdated`(status complete)·시작 때 활성 탭마다 `isUnsupportedUrl(tab.url)`이면 도울 수 없음(응답 없음 판정은 Task 3). 도울 수 없음 = `action.setTitle({ tabId, title: '도울 수 없음' })`, `setBadgeText({ tabId, text: '없음' })`, 배지 색은 `docs/design/tokens.css?raw`에서 `--muted` 값을 읽어 `setBadgeBackgroundColor`(값 복제 금지). 아니면 제목 "손 떨림 도우미", 배지 "".

`popup/main.ts`: 대상 탭 = 쿼리 `tabId`가 있으면 그 탭, 없으면 `chrome.tabs.query({ active: true, currentWindow: true })`. 대상 탭이 도울 수 없음이면 카드 격자 위에 한 문장 "이 페이지에서는 도울 수 없어요. 다른 탭에서 쓰세요."(카피 규칙 "원인. 다음 행동.", `--text-body`)와 카드 1·3·4만(전역 설정). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/site-toggle.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "3 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/entrypoints/background.ts`에 `isUnsupportedUrl`, `setBadgeText`, "도울 수 없음"이 있다
    - `src/entrypoints/background.ts`에 hex 색 리터럴이 없고 `tokens.css?raw` 가져오기가 있다
    - `CI=true pnpm exec playwright test tests/e2e/site-toggle.e2e.ts`가 3 passed
  </acceptance_criteria>
  <done>확장이 동작하지 않는 페이지에서 아이콘과 메뉴가 "도울 수 없음"을 알려 준다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 지금 사이트에서만 끄기·사이트별 동기화 항목·쓰기 합치기</name>
  <files>src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/content.ts, src/entrypoints/background.ts, src/entrypoints/popup/main.ts, tests/e2e/site-toggle.e2e.ts</files>
  <read_first>
    - src/core/settings-schema.ts (siteKey, SiteEntryV1)
    - src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/content.ts
    - .planning/phases/01-click-helper-foundation/01-RESEARCH.md (Pattern 4 — 키 나누기, 분당 한도)
  </read_first>
  <behavior>
    - e2e: 연습 사이트 탭을 대상으로 메뉴 "2 이 사이트에서 끄기" → `site:http://practice.test`의 `data.disabled`가 true, 그 탭의 모드 표시가 1초 안에 사라지고, `other.test` 탭의 모드 표시는 그대로
    - e2e: 다시 "2 이 사이트에서 켜기" → 돌아온다
    - e2e: SW가 미리 `pins`를 넣어 둔 상태에서 끄고 켜도 `pins`가 그대로
    - e2e: `practice.test` 안의 `other.test` iframe도 `practice.test`를 끄면 도우미가 꺼진다(사이트 = 맨 위 페이지 출처)
    - e2e: SW가 `site:http://practice.test`를 직접 바꾸면(다른 PC 동기화 흉내) 1초 안에 따른다
    - e2e: `Content-Security-Policy: sandbox` 머리글로 제공한 연습 페이지(불투명 출처라 content script가 들어가지 않는다고 가정)를 활성으로 하면 1초 뒤 아이콘이 "도울 수 없음"(응답 없음 판정)
    - e2e: 메뉴에서 150번 빠르게 끄고 켜기를 번갈아 보낸 뒤 최종 `disabled`가 마지막 요청과 같고, writer가 기록한 오류(`lastError`·거절)가 없다
  </behavior>
  <action>
RED 먼저: behavior 일곱을 `site-toggle.e2e.ts`에 더하고 실패 확인 후 RED 커밋.

`messages.ts`: `storage/request`의 `op`에 `{ kind: 'setSiteDisabled'; origin: string; disabled: boolean }`, 그리고 `site/ping`(SW → 맨 위, 답 `{ ok: true }`), `site/query`(content → SW, 답 `{ topOrigin }` — SW가 `sender.tab.url`에서 계산).

`storage-writer.ts`(D-23, D-24): `setSiteDisabled`는 `site:<origin>`을 읽어 `SiteEntryV1`로 검사(없으면 `{ disabled: false, pins: [] }`)한 뒤 `disabled`만 바꿔 쓴다(pins 보존, 검사 실패면 쓰지 않음 — D-25). **쓰기 합치기:** 같은 저장 키에 대한 요청이 앞 쓰기가 끝나기 전에 여럿 쌓이면 마지막 값 하나로 합쳐 한 번만 쓴다. **분당 한도:** `storage.sync` 쓰기 시각을 기록해 최근 60초 쓰기가 100번(크롬 한도 120의 여유)에 닿으면 다음 쓰기를 한도가 풀릴 때까지 미룬다(요청은 줄에 남고 합쳐진다). 팝업 요청의 `origin`은 대상 탭 주소의 출처여야 한다(팝업은 확장 페이지라 `sender.url`로 판정할 수 없으므로 SW가 `chrome.tabs.get(tabId)`로 확인 — 요청에 `tabId`를 싣는다).

`content.ts`(D-20): 시작 때 `site/query`로 맨 위 출처를 받아 `site:<topOrigin>`을 읽고 `storage.onChanged`로 따른다. `disabled`면 전역 끄기와 같이 모든 표시를 걷고 입력을 통과시킨다. 맨 위 프레임은 `site/ping`에 답한다.

`background.ts`: 주소 규칙을 통과한 탭은 맨 위 프레임에 `{ type: 'site/ping' }`을 보내 1초 안에 답이 없거나 보내기가 실패하면 도울 수 없음으로 표시한다(RESEARCH "Open Questions (RESOLVED)" 6번).

`popup/main.ts`: 대상 탭이 도울 수 있는 페이지일 때 카드 "2 이 사이트에서 끄기"/"2 이 사이트에서 켜기"(키 `2`·`Numpad2`)와 상태 한 줄 "이 사이트: 켜짐/꺼짐"(`--muted`). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/site-toggle.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "10 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/shared/messages.ts`에 `setSiteDisabled`, `site/ping`, `site/query`가 있다
    - `src/worker/storage-writer.ts`에 60초 창 쓰기 한도 처리와 같은 키 합치기가 있다
    - `grep -rn "storage\.\(sync\|local\)\.set" src/ | grep -v "src/worker/storage-writer.ts"`가 아무것도 출력하지 않는다
    - `CI=true pnpm exec playwright test tests/e2e/site-toggle.e2e.ts`가 10 passed
  </acceptance_criteria>
  <done>지금 사이트에서만 도우미를 끄고 켤 수 있고, 사이트별 동기화 항목이 고정 번호를 보존하며, 빠른 연속 변경도 한도 안에서 마지막 상태로 남는다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- SAFE-04·SAFE-05·STOR-01: edge probe `unclassified — review manually`(Plan 01-02 항목 포함). 가정한 경계:
  - "지금 사이트"는 맨 위 페이지의 출처(`origin`)다. 같은 사이트의 다른 하위 도메인은 따로 본다.
  - "도울 수 없음"은 주소 규칙 + content script 응답 없음(1초)으로 판정한다(RESEARCH "Open Questions (RESOLVED)" 6번). 파일 주소는 권한을 요청하지 않으므로 도울 수 없음.
  - `CSP: sandbox` 최상위 문서에는 content script가 들어가지 않는다고 가정한다. 들어간다면 실행자는 그 시험을 "도울 수 있음" 확인으로 바꾸고 응답 없음 판정의 다른 재현 방법을 SUMMARY에 편차로 적는다.
  - 분당 쓰기 한도 120·항목 8KB는 크롬 문서 값 가정(PITFALLS) — 8KB 크기 검사는 Plan 01-14가 형식 검사와 함께 넣는다.
  - 다른 PC 적용은 자동 시험으로 할 수 없어 backstop truth(수동 확인, D-29).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 팝업·content script → service worker | 사이트별 끄기 요청 |
| storage.sync → content script | 다른 PC가 쓴 사이트별 값 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-36 | Tampering | `setSiteDisabled` | medium | mitigate | `sender.id` 확인, zod 검사, 요청 `origin`이 `chrome.tabs.get(tabId).url`의 출처와 같을 때만 |
| T-01-37 | Denial of Service | 동기화 쓰기 한도 | medium | mitigate | 같은 키 합치기 + 60초 100번 한도, 150번 연타 e2e |
| T-01-38 | Tampering | 사이트별 값(다른 PC) | low | mitigate | 읽을 때 `SiteEntryV1` 검사, 실패하면 기본값(켜짐)으로 동작하고 원본은 쓰지 않음 |
| T-01-39 | Spoofing | 팝업 `?tabId=` | low | accept | 팝업은 `web_accessible_resources`가 아니라 웹 페이지가 열 수 없다. 값은 어느 탭의 출처를 보여 줄지만 정하고, 저장 전 SW가 탭 출처를 다시 확인 |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/unsupported-url.test.ts` 7개 이상 통과
- `CI=true pnpm exec playwright test tests/e2e/site-toggle.e2e.ts` 10 passed
- `pnpm typecheck && pnpm lint` 통과, 단일 저장자 grep 조건 충족
- 다른 PC 적용은 사람 확인(backstop) — 없으면 human_needed
</verification>

<success_criteria>
- 확장 아이콘에서 도우미 전체 또는 지금 사이트에서만 끄고 켤 수 있다(SAFE-04)
- 확장이 동작하지 않는 페이지에서 "도울 수 없음"이 표시된다(SAFE-05)
- 사이트별 끄기·고정 번호가 사이트별 동기화 항목에 저장되어 다른 PC에 적용된다(STOR-01)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/unsupported-url.ts`, `tests/unit/unsupported-url.test.ts`, `tests/e2e/site-toggle.e2e.ts`
- 심볼: `isUnsupportedUrl()`, fixture `openPopup(target?)`
- 메시지: `site/ping`, `site/query`, op `setSiteDisabled`
- 저장 키: `site:<origin>`(storage.sync) 쓰기 시작
- 확장 아이콘: 제목 "도울 수 없음", 배지 "없음"

<output>
Create `.planning/phases/01-click-helper-foundation/01-13-SUMMARY.md` when done
</output>
