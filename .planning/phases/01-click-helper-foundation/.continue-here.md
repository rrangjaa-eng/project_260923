---
context: phase
phase: 01-click-helper-foundation
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-09-26T05:02:51.511Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: editor-frames flaky — `tests/e2e/editor-frames.e2e.ts`의 "document.write로 채운 프레임이 막 생겨도 옛 인스턴스는 조용하다" 시험이 큰 순차 묶음에서 가끔 실패한다(01-18 변경 전 코드에서도 재현, 단독·repeat-each=5는 통과) — 01-19 최종 게이트(0 failed, 0 flaky) 전에 `systematic-debugging`으로 근본 원인을 찾아 고친다. 재시도·완화·건너뛰기 금지.
- [ ] CONSTRAINT: 전체 게이트는 한 번 — 01-19 Task 3에서만, 마지막에 한 번 돌린다. 두 번 돌리게 되면 SUMMARY에 두 번의 결과를 따로 기록한다.
- [ ] CONSTRAINT: `tests/e2e/dom-audit.e2e.ts` 기준 변경 금지 — 감사 기준은 별도 감사 에이전트 소유다.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 가정으로 동작 고정 | noopener 팝업 동작을 재 보지 않고 "미지원"으로 계획에 박았다 → plan-checker 경고 | blocking | 01-19 Task 1 첫 단계에서 프로덕션 빌드로 실측한 뒤에 시험을 쓴다 |
| 고정 대기 | 고정 `waitForTimeout`이 부하 시 flaky를 만들었다(WR-10, hints shortcuts) | advisory | 조건 대기(`expect.poll`, locator 대기)를 쓴다 |
| 연습 페이지 결함 | innerHTML로 넣은 script 미실행, 공유 h1이 중첩 프레임을 밀어냄 | advisory | 시험 실패 시 제품보다 먼저 fixture를 의심하고 SW 쪽 로그로 확인 |

<current_state>
Phase 1 보완 실행 중. 01-17(편집기형 iframe)과 01-18(자식 프레임 머무르기·IN-04·편집기 Esc) 완료·푸시(0ac23f6). 남은 것은 01-19(주소 없는 팝업 + 최종 전체 게이트) 하나. 작업 트리 깨끗함.
</current_state>

<completed_work>

- 01-16: 서체·50ms 측정·독립 DOM 감사(16/16)
- 코드 리뷰: 치명 8·경고 10 모두 재현→TDD 수정(01-REVIEW-FIX.md all_fixed)
- 목표 검증 1차: gaps_found → 요구사항 Complete 표시 되돌림
- 01-17: matchAboutBlank, documentRewriteWatcher + frame/reinject, cleanedUp 가드 7곳 (b12febd)
- 01-18: childConfirmOpen, site/query 재시도·fail-closed·알림, escapeDocumentEditor/resumeDocumentEditor (0ac23f6)
- CLAUDE.md를 260917 구조로 교체 (d7b8c1b)
</completed_work>

<remaining_work>

- 01-19 Task 1: 주소 없는 팝업 사이트 판정 — noopener 먼저 실측, `inheritedSiteOrigin` + 단위 시험 7개
- 01-19 Task 2: 팝업 아이콘·메뉴·누름 기록·안쪽 iframe (`siteOriginOfTab`, `site/ping`이 origin 반환)
- 01-19 Task 3: 최종 게이트 — editor-frames flaky 먼저 해결, editor-frames 기준 개수는 01-18 SUMMARY의 16개
- 이후: gsd-verifier(opus) 재검증 → passed면 `phase.complete` → Post-build `/review` → `/qa` → `/cso` → `/ship`
</remaining_work>

<decisions_made>

- 확인 카드 테두리는 남색 3px 유지(SAFE-02의 빨강은 위험 버튼의 빨간 점선)
- 편집기 Esc 문제는 이번 보완에 포함(01-18 완료)
- 주소 없는 팝업에서도 도우미 동작(사용자 결정). noopener는 실측대로, 구분 장치 금지
- CLAUDE.md는 260917 구조·규칙, §1은 이 프로젝트 내용
- 판단·계획·검토는 Opus, 코드 실행은 Sonnet
</decisions_made>

<blockers>
- editor-frames flaky 시험: 최종 게이트 전 근본 원인 수정 필요(위 제약)
- D-25(깨진 설정일 때 전역 끄기 거부) 사용자 결정 대기 — 이번 범위 밖
</blockers>

## Required Reading (in order)
1. `CLAUDE.md` — 이번 세션에 교체됨(§7 화면 사용성 원칙 포함)
2. `.planning/phases/01-click-helper-foundation/01-19-PLAN.md` — 실행할 계획
3. `.planning/phases/01-click-helper-foundation/01-18-SUMMARY.md`, `01-17-SUMMARY.md` — 바로 앞 변경과 시험 도구
4. `.planning/phases/01-click-helper-foundation/01-VERIFICATION.md` — 닫아야 할 빈 곳

## Critical Anti-Patterns (do NOT repeat these)
- 전체 게이트를 여러 번 돌리기 → 01-19 Task 3에서 한 번만
- 시험 완화로 초록 만들기 → 근본 원인 수정만

## Infrastructure State
- 클라우드 환경 plant8(`env_01X9mcoNj9giznLpgqrJSzcY`), 브랜치 `claude/project-thread-ew8d9u`, PR rrangjaa-eng/project_260923#7
- 푸시 전 훅이 `pnpm lint`를 돌린다 — 커밋 전 lint를 깨끗하게
- 실행자 루트 가드: 새 세션에서 execute-phase가 root-pin.sh를 다시 만든다

<context>
Phase 1의 마지막 보완 계획만 남았다. 01-19가 끝나면 gsd-verifier로 다시 검증하고, 통과하면 로드맵을 완료로 표시한 뒤 Post-build 넷을 실제로 호출한다.
</context>

<next_action>
Start with: `/gsd-execute-phase 1 --gaps-only` (01-19만 남음). 실행자 프롬프트에 editor-frames flaky 제약과 "editor-frames 기준 16개(01-18 SUMMARY)"를 넣는다.
</next_action>
