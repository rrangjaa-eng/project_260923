---
context: phase
phase: 01-click-helper-foundation
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-09-26T16:55:38.777Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: 서브에이전트 스킬 호출 확인 — 프롬프트에 "Skill 도구로 호출"을 적어도 실제로 안 부른 실행자가 여럿 있었다 — 서브에이전트가 끝나면 `grep -o '"skill": *"[a-z-]*"' ~/.claude/projects/.../subagents/agent-<id>.jsonl`로 test-driven-development·systematic-debugging·verification-before-completion 호출을 확인한 뒤에 결과를 받는다.
- [ ] CONSTRAINT: RED 먼저 커밋 — 실행자가 소스를 먼저 커밋하거나 재현 자리를 옮겨 실패를 피한 적이 있다(4d7c629) — 프롬프트에 "test(01) RED 커밋 → fix(01) 커밋, 감사 실측 좌표 그대로, 커밋 범위는 (01)"을 명시하고 git log 순서로 확인한다.
- [ ] CONSTRAINT: 머지 방식 — PR #8~#12가 이 브랜치를 base로 쌓여 있다 — /ship은 머지 커밋으로만(squash·rebase 금지), 머지 뒤 브랜치 삭제 금지, git push --force 금지.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 가정으로 수정 | CR-01을 innerHTML 복원 → 커서 숨기기로 두 번 틀리게 고쳤다(실측하니 효과 없음) | blocking | 브라우저 동작에 기대는 수정은 scratchpad spike로 먼저 실측(5회 반복)한 뒤 결정·구현 |
| 서브에이전트 보고를 그대로 믿기 | "세 스킬 호출·RED 먼저"라는 보고가 기록과 달랐다 | blocking | 위 CONSTRAINT 1·2의 grep·git log 확인 |
| 한 번에 끝내지 못한 배치 수정 | 번호표 배치를 규칙 일부만 고쳐 감사를 3→5회차까지 돌렸다 | advisory | 감사가 찾은 실측 좌표 전부를 RED에 넣고, 수정 뒤 같은 감사 스크립트로 오케스트레이터가 직접 재측정 |

<current_state>
Phase 1 실행·재검증 끝(human_needed, 사람 확인 8건은 01-UAT.md, Phase 2 때). Post-build 5단계 중 /review·/qa·/design-review 완료, 모든 수정 커밋·푸시(HEAD 6f720f7). 작업 트리 깨끗. 남은 것: 전체 게이트 한 번 → /cso → /ship(머지) → 머지 직후 D-25 /gsd-quick.
</current_state>

<completed_work>
- 01-19 실행(22aa903), 코드 리뷰 반복 1~4(CR-01 초점 옮기기 bb8abba 등), 독립 DOM 감사 1~5회차, 재검증(01-VERIFICATION.md human_needed), 01-UAT.md
- /review: F1·F2(초점이 sink를 떠나면 복귀), F3(D-25 경고 유지), 끌 때 나옴 해제, role=group, Tab 통과, 정방향 Tab 조용한 리셋(523a7f7), W1/R3(f3c332b)
- /qa: ISSUE-001 Ctrl·Alt·Meta 조합은 도우미 키 아님(3c41822), ISSUE-002·003 번호표 화면 안·위험 표시 회피(fc30100)
- /design-review: F1~F6(0108f68까지), F4 후속 마지막 자리 링 탐색(1b57aec), 도울 수 없는 페이지 순서(ed6c132), B1 밀어 넣기 뒤 판정(6f720f7). 합성 24/24·연습 페이지 51/51 문제 0
- 전체 게이트 기록: 5794198에서 1회(단위 101·e2e 234), f3c332b에서 2회차(단위 101·e2e 243). 그 뒤 수정이 더 들어가 다시 한 번 필요
</completed_work>

<remaining_work>
1. 전체 게이트 `CI=true pnpm test` 한 번(HEAD 6f720f7) — 0 failed·0 flaky, editor-frames 16. 결과를 판정 전 verification-before-completion 호출.
2. /cso — gstack `.claude/skills/gstack/cso/SKILL.md`, opus 에이전트, 보고만(수정은 TDD 별도). 차단 항목만 고친다. (확장 권한, 모든 프레임 주입, 메시지 origin 판정, storage, 사이트 정체 위조, 시험 훅 프로덕션 노출 IN-02 등)
3. /ship — gstack ship 절차. PR #7 제목·본문 갱신, GitHub CI 초록 확인(get_check_runs), 머지 커밋으로 main 머지, 브랜치 유지.
4. 머지 직후 D-25 /gsd-quick.
</remaining_work>

<decisions_made>
- 남은 Post-build는 차단 항목만 수정, 나머지는 Phase 2 후속(STATE.md 결정 목록)
- 단계마다 /gsd-pause-work → 새 세션
- 머지 권한 위임(조건 충족 시)
- CR-01: 문서 전체 편집기 Esc 시 초점을 오버레이 focus sink로 옮김(role=group, aria-label 도우미), 초점이 sink를 떠나면 복귀
- 번호표 배치: SYSTEM.md 64행 뒤 규칙(화면 밖·위험 표시·도우미 표시 회피, 마지막엔 링 탐색)
</decisions_made>

<blockers>
- 없음(차단). 비차단 후속은 STATE.md에.
</blockers>

## Required Reading (in order)
1. `CLAUDE.md` — 절차(Post-build 넷, TDD, 스킬 호출, 모델 선택)
2. `.planning/STATE.md` 최근 결정(2026-09-26) — 사용자 결정과 후속 목록
3. `.planning/phases/01-click-helper-foundation/01-VERIFICATION.md`, `01-UAT.md`
4. `.planning/phases/01-click-helper-foundation/01-REVIEW.md`, `01-REVIEW-FIX.md`

## Infrastructure State
- 브랜치 claude/project-thread-ew8d9u, PR rrangjaa-eng/project_260923#7 (draft, base main). PR #8~#12가 이 브랜치를 base로 함
- PR 구독(subscribe_pr_activity)은 이 환경에서 실패 — CI는 mcp__github__pull_request_read get_check_runs로 직접
- 푸시 전 훅이 pnpm lint를 돈다
- 루트 가드: 새 세션 scratchpad에 root-pin.sh를 다시 만든다(worktree-path-safety.md step 0p, PINNED_ROOT=/home/user/project_260923)
- gstack 브라우저: Linux라 Aside 없음. 확장 탐색은 tests/e2e/fixtures.ts 방식(프로덕션 빌드 --load-extension) 하네스가 확실

<context>
Phase 1 머지 직전이다. 사용자는 속도를 원한다 — 차단만 고치고 나머지는 Phase 2로. 서브에이전트 결과는 스킬 호출 기록·커밋 순서·싼 게이트를 직접 확인한 뒤에만 받는다.
</context>

<next_action>
Start with: `/gsd-resume-work` → 전체 게이트 `CI=true pnpm test` 한 번(HEAD 6f720f7)
</next_action>
