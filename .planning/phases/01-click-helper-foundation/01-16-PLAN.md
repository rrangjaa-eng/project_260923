---
phase: 01-click-helper-foundation
plan: 16
type: execute
wave: 16
depends_on: ["01-15"]
files_modified:
  - src/page/overlay/mode-indicator.ts
  - src/entrypoints/popup/main.ts
  - wxt.config.ts
  - tests/practice-site/big.html
  - tests/e2e/overlay-perf.e2e.ts
  - tests/e2e/dom-audit.e2e.ts
autonomous: true
requirements: [ELEM-04]
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "요소 5,000개짜리 연습 페이지에서 커서를 옮긴 뒤 강조가 50ms 안에 따라오는 것이 확장을 띄운 브라우저 자동 시험(CI=true, 프로덕션 빌드)으로 측정된다(30번 이상 이동의 95번째 백분위수 < 50ms)"
    - "오버레이와 확장 아이콘 메뉴의 글자는 IBM Plex Sans KR(확장 안에 넣은 파일)로 보이고, 사이트 자신의 서체는 바뀌지 않는다"
    - "실행자가 아닌 별도 감사 에이전트가 CI=true로 오버레이·메뉴 DOM을 실측해 SYSTEM.md·tokens.css와 어긋남이 없다고 판정한다"
    - "전체 게이트(단위 → e2e, lint, typecheck, build)를 CI=true로 한 번 돌려 통과한다"
  prohibitions:
    - statement: "도우미는 페이지 내용·누른 기록·설정을 밖(서버·외부 주소)으로 보내지 않는다 — Phase 1에는 밖으로 나가는 요청이 없다"
      requirement_id: ELEM-04
      category: privacy
      status: resolved
      verification: judgment
  artifacts:
    - path: "tests/practice-site/big.html"
      provides: "누를 수 있는 요소 5,000개 연습 페이지(+ 화면 변화 부하 모드)"
    - path: "tests/e2e/overlay-perf.e2e.ts"
      provides: "서체 확인과 50ms 반응 시간 측정"
    - path: "tests/e2e/dom-audit.e2e.ts"
      provides: "독립 DOM 감사(별도 에이전트가 작성·판정)"
    - path: "src/page/overlay/mode-indicator.ts"
      provides: "오버레이 호스트의 서체 등록"
      contains: "FontFace"
  key_links:
    - from: "wxt.config.ts"
      to: "src/page/overlay/mode-indicator.ts"
      via: "web_accessible_resources의 woff2를 FontFace로 document.fonts에 등록"
      pattern: "web_accessible_resources"
    - from: "tests/e2e/overlay-perf.e2e.ts"
      to: "tests/practice-site/big.html"
      via: "진짜 page.mouse.move → 페이지가 기록한 입력 timeStamp와 테두리 변경 시각(MutationObserver)의 차이"
      pattern: "timeStamp"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 어느 사이트에서든(iframe 본문 포함) 원하는 요소를 마우스를 대충 가져가거나 숫자 키 하나로 누르고, 방해되면 즉시 끌 수 있기를, **so that** 떨림으로 인한 잘못 누름과 위험한 버튼 오조작 없이 평소 사이트를 빠르게 쓸 수 있다.

<objective>
Phase 1을 닫는다: 오버레이·메뉴 서체를 마무리하고, 5,000요소 페이지의 50ms 반응 시간을 자동 시험으로 재고, 별도 에이전트의 독립 DOM 감사를 거친 뒤 전체 게이트를 `CI=true`로 한 번 통과한다.

Purpose: ELEM-04(D-05, ROADMAP 성공 기준 4의 측정 부분), 서체(D-26), CI는 Chromium(D-29), 단위·브라우저 시험 목록(D-30), 밖으로 나가는 요청 없음(D-31). 이 계획이 phase 금지사항 "외부 전송 금지"를 맡는다. 화면 검증 순서는 CLAUDE.md "싼 게이트 → 독립 DOM 감사 → 수정 → 전체 게이트 한 번".
Output: 서체 등록, `big.html`, `overlay-perf.e2e.ts`, `dom-audit.e2e.ts`(감사 에이전트 작성), 전체 게이트 결과.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-click-helper-foundation/01-CONTEXT.md
@.planning/phases/01-click-helper-foundation/01-RESEARCH.md
@.planning/phases/01-click-helper-foundation/01-15-SUMMARY.md
@docs/design/SYSTEM.md
@docs/design/tokens.css
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 승인된 패키지 밖은 설치하지 않는다(Plan 01-01에서 서체 패키지가 거절됐다면 서체 확인은 대체 서체 `'Malgun Gothic', sans-serif` 기준으로 바꾼다).
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(01-16): …` → 최소 구현 → GREEN 커밋 `feat(01-16): …`. 본문은 한국어.
- 완료 판정은 `CI=true`(프로덕션 빌드). 로컬 dev 통과는 완료 신호가 아니다.
- 오버레이·팝업 스타일은 `docs/design/tokens.css` 변수만. 새 색·서체·radius·그림자 금지(D-26).
- UI 판정은 스크린샷 육안이 아니라 DOM 실측이다.
- 연습 사이트에서만 시험. 실제 회사 시스템 접속·시험 금지(D-14).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 오버레이·메뉴 IBM Plex Sans KR 서체 (확장 안 파일)</name>
  <files>src/page/overlay/mode-indicator.ts, src/entrypoints/popup/main.ts, wxt.config.ts, tests/e2e/overlay-perf.e2e.ts</files>
  <read_first>
    - src/page/overlay/mode-indicator.ts (ensureOverlayRoot), src/entrypoints/popup/main.ts, wxt.config.ts
    - node_modules/@fontsource/ibm-plex-sans-kr/ (400·700 CSS와 woff2 파일 이름)
    - .planning/phases/01-click-helper-foundation/01-RESEARCH.md (Pattern 5 — 서체)
    - docs/design/SYSTEM.md ("글자"), docs/design/DECISIONS.md ("서체")
  </read_first>
  <behavior>
    - e2e: 오버레이가 뜬 뒤 페이지에서 `document.fonts.check('700 18px "IBM Plex Sans KR"')`가 true이고 모드 표시의 계산된 `font-family`가 "IBM Plex Sans KR"로 시작한다
    - e2e: 확장 아이콘 메뉴의 카드 글자 계산된 `font-family`도 "IBM Plex Sans KR"로 시작하고 `document.fonts.check`가 true
    - e2e: 연습 페이지 `body`의 계산된 `font-family`는 오버레이가 뜨기 전과 같고, fixture `blockedRequests`가 비어 있다(서체 파일은 확장 안에서만 옴)
  </behavior>
  <action>
RED 먼저: behavior 셋을 `tests/e2e/overlay-perf.e2e.ts`에 쓰고 실패 확인 후 RED 커밋.

서체(D-26, RESEARCH Pattern 5): `wxt.config.ts`에 `@fontsource/ibm-plex-sans-kr`의 400·700 woff2(한글·라틴 유니코드 범위 파일)를 확장 안 `fonts/`로 넣고 `web_accessible_resources: [{ resources: ['fonts/*.woff2'], matches: ['<all_urls>'] }]`. `ensureOverlayRoot()`가 처음 불릴 때 한 번, fontsource CSS의 각 `@font-face`와 같은 `unicode-range`로 `new FontFace('IBM Plex Sans KR', 'url(chrome.runtime.getURL(...))', { weight, unicodeRange })`를 `document.fonts.add` 한다(shadow root 안 `@font-face`는 적용되지 않음). 실패하면 조용히 대체 서체(`var(--font)`의 뒤 순서). 사이트 요소의 서체는 건드리지 않는다. `popup/main.ts`: 확장 페이지이므로 fontsource 400·700 CSS를 문서에 가져온다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/overlay-perf.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "3 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/overlay/mode-indicator.ts`에 `FontFace`와 `document.fonts.add`가 있다
    - `wxt.config.ts`에 `web_accessible_resources`와 `fonts/`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/overlay-perf.e2e.ts`가 3 passed
  </acceptance_criteria>
  <done>오버레이·메뉴가 확장 안의 IBM Plex Sans KR로 보이고 사이트 서체는 그대로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5,000요소 연습 페이지에서 강조 반응 시간 50ms 측정</name>
  <files>tests/practice-site/big.html, tests/e2e/overlay-perf.e2e.ts</files>
  <read_first>
    - tests/e2e/overlay-perf.e2e.ts, tests/e2e/fixtures.ts
    - src/page/collector/collector.ts, src/core/grid-index.ts, src/entrypoints/content.ts (pointermove 60Hz 처리)
    - .planning/phases/01-click-helper-foundation/01-CONTEXT.md (D-04, D-05)
  </read_first>
  <behavior>
    - e2e: `big.html`(누를 수 있는 요소 5,000개 — 버튼·링크·입력칸을 격자로, 화면 여러 장 높이)에서 진짜 `page.mouse.move`로 서로 다른 요소 30곳 이상을 차례로 겨누고(각 이동 뒤 120ms 대기), 페이지가 기록한 "마지막 pointermove의 `event.timeStamp` → 테두리 `[data-part=ring]`의 style·속성 변경을 본 `performance.now()`" 차이의 95번째 백분위수가 50ms 미만이다
    - e2e: 같은 측정을 페이지가 100ms마다 요소 100개를 바꾸는 부하 모드(`big.html?churn=1`)에서도 95번째 백분위수 50ms 미만
  </behavior>
  <action>
RED 먼저: `tests/practice-site/big.html`(D-28)을 만든다 — 인라인 스크립트가 요소 5,000개를 만들고, 측정기를 둔다: `window.addEventListener('pointermove', e => last = e.timeStamp, true)`(도우미가 pointermove를 막지 않으므로 페이지도 받음), `tremor-helper-root`의 열린 shadow root 안 `[data-part="ring"]`을 `MutationObserver`(attributes: style·class·data-*)로 보고 변경 때 `performance.now() - last`를 배열에 넣는다(두 값 모두 같은 문서 시계). `?churn=1`이면 `setInterval(100)`으로 요소 100개의 글자·위치를 바꾼다. behavior 둘을 `overlay-perf.e2e.ts`에 더하고, 측정값(개수·중앙값·p95·최대)을 `testInfo.attach`와 콘솔로 남긴다. 실패 확인(측정기·페이지가 없어서) 후 RED 커밋.

측정이 50ms를 넘으면 `systematic-debugging`으로 원인(수집기 재구성·격자 조회·스타일 계산)을 찾아 `collector.ts`·`grid-index.ts`·`content.ts`를 고친다 — 이 계획 files 밖 파일이므로 고친 내용은 SUMMARY에 편차(Rule 1)로 적는다. 측정 방식(시험 쪽)을 느슨하게 바꿔 통과시키지 않는다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/overlay-perf.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "5 passed", or "No tests found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/practice-site/big.html`이 5,000개 요소를 만들고 `churn` 모드가 있다
    - `tests/e2e/overlay-perf.e2e.ts`에 95번째 백분위수 계산과 `< 50` 단언이 있고 측정 이동이 30번 이상이다
    - `CI=true pnpm exec playwright test tests/e2e/overlay-perf.e2e.ts`가 5 passed
    - SUMMARY에 측정값(중앙값·p95·최대, 부하 모드 포함)이 있다
  </acceptance_criteria>
  <done>5,000요소 페이지에서 강조가 50ms 안에 따라오는 것이 CI=true 자동 시험으로 측정·기록되었다.</done>
</task>

<task type="auto">
  <name>Task 3: 싼 게이트 → 독립 DOM 감사(별도 에이전트) → 수정 → 전체 게이트 한 번</name>
  <files>tests/e2e/dom-audit.e2e.ts</files>
  <read_first>
    - CLAUDE.md ("프론트엔드 — 화면 검증 순서", "코딩 규칙")
    - docs/design/SYSTEM.md, docs/design/tokens.css, docs/DESIGN.md (§6 품질 바닥)
    - tests/e2e/fixtures.ts
  </read_first>
  <action>
1. 싼 게이트: `pnpm lint && pnpm typecheck && CI=true pnpm build`. 실패하면 고치고 다시.

2. 독립 DOM 감사: **이 계획 실행자가 아닌 별도 감사 에이전트**(Task 도구, `model: "sonnet"`, 프롬프트에 `verification-before-completion` 스킬 명시)에게 맡긴다. 감사 에이전트는 SYSTEM.md·tokens.css·DESIGN.md §6만 기준으로 `tests/e2e/dom-audit.e2e.ts`를 작성하고 `CI=true pnpm exec playwright test tests/e2e/dom-audit.e2e.ts`로 DOM을 실측해 판정한다(스크린샷 육안 금지). 감사 항목(최소): 연습 페이지들(`targets.html`, `danger.html`, `frames.html`, `input.html`)에서 모든 오버레이 부품과 확장 아이콘 메뉴의 계산된 스타일 — 색은 tokens.css 값 집합 안(`color`·`background-color`·`border-*-color`·`outline-color`), radius는 radius 토큰 집합 안, `box-shadow`는 모두 `none`, 서체 "IBM Plex Sans KR" 시작, 테두리 두께 5px·바깥 8px·흰 후광 2px, 번호표 ≥28×28, 메뉴 카드 높이 ≥56px, 확인 버튼 ≥64px, 확인 카드 600px, 모드 표시 왼쪽 아래 기본 자리, 글자 대비 4.5:1 이상·UI 3:1 이상, 메뉴 카드 `:focus-visible` 남색 3px, 한글 `word-break: keep-all`, `reducedMotion: 'reduce'`에서 이동·나타남 transition 0(머무르기 진행과 보호 막대는 유지), 확대 110%·200%에서 크기 불변, `blockedRequests` 비어 있음. 감사 에이전트는 어긋남 목록(파일·부품·실측값·기준값)을 돌려준다.

3. 수정: 어긋남마다 해당 오버레이·팝업 파일을 고친다(SYSTEM.md를 벗어나야 하는 경우는 고치지 말고 멈춰서 DECISIONS.md 기록·SYSTEM.md 수정 승인을 요청한다 — DESIGN.md §4.6). 고친 파일·내용은 SUMMARY에 편차로 적는다. 감사 시험을 다시 돌려 통과를 확인한다(감사 시험의 기준을 실행자가 바꾸지 않는다).

4. 전체 게이트 한 번: `CI=true pnpm test`(단위 → e2e 전부) && `pnpm lint` && `pnpm typecheck`. 외부 전송 금지 확인: `grep -rnE "\bfetch\(|XMLHttpRequest|WebSocket|sendBeacon|EventSource" src/`가 아무것도 출력하지 않는다(D-31).
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && CI=true pnpm build</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/dom-audit.e2e.ts</automated>
    <fails_when>non-zero exit, or "No tests found", or fewer than 10 tests passed</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>non-zero exit, or any test reported failed or flaky</fails_when>
    <automated>test -z "$(grep -rnE '\bfetch\(|XMLHttpRequest|WebSocket|sendBeacon|EventSource' src/)"</automated>
    <fails_when>non-zero exit (a network API call exists in src/)</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/dom-audit.e2e.ts`가 있고, SUMMARY에 "감사 에이전트(model: sonnet)가 작성·판정"과 어긋남 목록·수정 결과가 적혀 있다
    - `CI=true pnpm exec playwright test tests/e2e/dom-audit.e2e.ts` 10개 이상 통과
    - `CI=true pnpm test` 전체 통과(한 번)
    - `src/`에 네트워크 API 호출이 없다
  </acceptance_criteria>
  <done>독립 DOM 감사가 SYSTEM.md 일관성을 실측으로 통과시켰고, 전체 게이트가 CI=true로 한 번에 통과했다. 다음은 CLAUDE.md Post-build(`/review` → `/qa` → `/cso` → `/ship`)다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- ELEM-04: edge probe `unclassified — review manually`. 가정한 경계:
  - "강조가 따라온다"는 테두리의 스타일이 새 요소로 바뀌는 시점으로 잰다. 그 뒤 80ms 이동 모션(SYSTEM.md "모션", D-27)은 반응 시간에 넣지 않는다 — 반응(50ms)과 부드러운 이동(80ms)은 설계가 따로 정한 두 값이다.
  - 측정은 CI 기계의 헤드리스 Chromium 기준이다. 이용자 PC(윈도우, 사무용)에서의 체감은 Phase 2 이용자 시험에서 확인한다.
- Edge·Whale 동작과 설치 화면은 수동 확인 목록(D-29, Phase 2 웹스토어 배포 때).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 확장 → 인터넷 | 도우미가 밖으로 데이터를 보낼 수 있는 경로 |
| 웹 페이지 → 확장 파일 | `web_accessible_resources`로 연 서체 파일 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-44 | Information Disclosure | 확장 전체 | high | mitigate | `src/`에 네트워크 API 호출 없음(grep), 모든 e2e의 `blockedRequests` 비어 있음, 서체는 확장 안 파일(D-31) |
| T-01-45 | Information Disclosure | `web_accessible_resources` | low | accept | `fonts/*.woff2`만 연다. 웹 페이지가 확장 설치 여부를 알아챌 수 있으나(지문) 서체 파일 외 정보는 없다 |
| T-01-46 | Repudiation | 감사 판정 | medium | mitigate | 감사는 실행자가 아닌 별도 에이전트가 작성·판정, 실행자는 감사 기준을 바꾸지 않음(CLAUDE.md) |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/overlay-perf.e2e.ts` 5 passed(50ms p95 포함)
- 독립 DOM 감사 `tests/e2e/dom-audit.e2e.ts` 10개 이상 통과(별도 에이전트 판정)
- `CI=true pnpm test`, `pnpm lint`, `pnpm typecheck`, `CI=true pnpm build` 한 번에 통과
- 금지사항(외부 전송)은 verify-work 때 판단 검토: 네트워크 API grep과 `blockedRequests` 결과를 근거로 본다
</verification>

<success_criteria>
- 요소 5,000개 연습 페이지에서 커서 이동 후 강조가 50ms 안에 따라오는 것이 자동 시험으로 측정된다(ELEM-04, ROADMAP 성공 기준 4 측정 부분)
- 오버레이가 SYSTEM.md 서체로 보이고, 독립 DOM 감사를 통과한다(확대 무관 크기 포함)
- 실행 뒤에는 CLAUDE.md Post-build 넷(`/review` → `/qa` → `/cso` → `/ship`)을 건너뛰지 않는다(이 phase는 외부 입력·권한을 다루므로 `/cso` 필수)
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/practice-site/big.html`, `tests/e2e/overlay-perf.e2e.ts`, `tests/e2e/dom-audit.e2e.ts`
- 확장 파일: `fonts/*.woff2`(web_accessible_resources)

<output>
Create `.planning/phases/01-click-helper-foundation/01-16-SUMMARY.md` when done
</output>
