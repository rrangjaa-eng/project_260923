---
phase: 01-click-helper-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/core/grid-index.ts
  - tests/unit/grid-index.test.ts
  - src/core/magnet.ts
  - tests/unit/magnet.test.ts
  - src/page/collector/collector.ts
  - src/page/overlay/ring.ts
  - src/entrypoints/content.ts
  - tests/practice-site/targets.html
  - tests/e2e/magnet.e2e.ts
autonomous: true
requirements: [ELEM-01, ELEM-03, CLICK-01]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "커서를 아주 작은 버튼 근처(요소 크기보다 넓은 범위, 기본 48px 안)로 대충 가져가면 그 버튼이 남색 5px 테두리(요소보다 8px 바깥)로 크게 강조된다"
    - "잡힌 요소는 커서가 다른 요소 쪽으로 충분히(기본 24px 더 가깝게) 옮겨 가기 전까지 바뀌지 않는다"
    - "버튼·링크·입력칸·클릭 처리가 걸린 이미지·role=button 요소가 잡히고, 보이지 않는 요소는 잡히지 않는다"
    - "늦게 나타난 입력칸도 곧 잡히고, 스크롤하거나 잡힌 요소가 사라지면 테두리가 따라가거나 없어진다"
    - "잡힌 것이 없으면 테두리가 없다"
  artifacts:
    - path: "src/core/grid-index.ts"
      provides: "격자 공간 색인(가까운 요소 찾기)"
      exports: ["createGridIndex"]
    - path: "src/core/magnet.ts"
      provides: "자석 잡기 판단(잡는 범위·히스테리시스)"
      exports: ["pickTarget"]
    - path: "src/page/collector/collector.ts"
      provides: "프레임 안 누를 수 있는 요소 수집기(MutationObserver·스크롤 모아서 갱신)"
      exports: ["createCollector"]
    - path: "src/page/overlay/ring.ts"
      provides: "프레임 안 강조 테두리"
      exports: ["showRing", "hideRing"]
    - path: "tests/practice-site/targets.html"
      provides: "연습 사이트: 아주 작은 버튼·이미지 링크·클릭 이미지·긴 양식·늦게 나타나는 입력칸·긴 페이지"
  key_links:
    - from: "src/entrypoints/content.ts"
      to: "src/core/magnet.ts"
      via: "pipeline pointermove(60Hz) → grid.nearby → pickTarget → showRing/hideRing"
      pattern: "pickTarget"
    - from: "src/page/collector/collector.ts"
      to: "src/core/grid-index.ts"
      via: "MutationObserver·scroll을 requestAnimationFrame으로 모아 grid.build"
      pattern: "build\\("
    - from: "src/page/overlay/ring.ts"
      to: "src/page/overlay/mode-indicator.ts"
      via: "ensureOverlayRoot() 공유 shadow root"
      pattern: "ensureOverlayRoot"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 어느 사이트에서든(iframe 본문 포함) 원하는 요소를 마우스를 대충 가져가거나 숫자 키 하나로 누르고, 방해되면 즉시 끌 수 있기를, **so that** 떨림으로 인한 잘못 누름과 위험한 버튼 오조작 없이 평소 사이트를 빠르게 쓸 수 있다.

<objective>
자석 커서의 "잡기"를 만든다: 각 프레임이 누를 수 있는 요소를 모아 격자로 색인하고, 커서에서 가장 가까운 요소를 넓은 범위·히스테리시스로 잡아 굵은 테두리로 강조한다. 누르기는 Plan 01-05.

Purpose: 떨려도 원하는 요소가 안정적으로 잡혀야 한다(CLICK-01, D-10). 요소 인식과 화면 변화 추적(ELEM-01, ELEM-03, D-04), 프레임 안에서 바로 계산(D-02), 상태 표(D-27), 테두리 규칙(D-26).
Output: `grid-index.ts`·`magnet.ts`(순수)+단위 시험, `collector.ts`, `ring.ts`, 연습 페이지 `targets.html`, e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-click-helper-foundation/01-CONTEXT.md
@.planning/phases/01-click-helper-foundation/01-RESEARCH.md
@.planning/phases/01-click-helper-foundation/01-03-SUMMARY.md
@docs/design/SYSTEM.md
@docs/design/tokens.css
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. Plan 01-01에서 승인된 패키지 밖은 설치하지 않는다(공간 색인은 직접 만든다 — flatbush는 승인 목록에 없음).
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → 실패 확인 → RED 커밋 `test(01-04): …` → 최소 구현 → GREEN 커밋 `feat(01-04): …`. 제목은 영어 접두어, 본문은 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드).
- 오버레이 스타일은 `docs/design/tokens.css` 변수만. 새 색·서체·radius·그림자 금지(D-26).
- 연습 사이트에서만 시험. 실제 회사 시스템 접속·시험 금지(D-14, D-28).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 격자 공간 색인과 자석 잡기 판단 (순수 함수)</name>
  <files>src/core/grid-index.ts, tests/unit/grid-index.test.ts, src/core/magnet.ts, tests/unit/magnet.test.ts</files>
  <read_first>
    - src/core/settings-schema.ts (captureMarginPx 48, switchHysteresisPx 24)
    - .planning/phases/01-click-helper-foundation/01-CONTEXT.md (D-04, D-10, D-30)
  </read_first>
  <behavior>
    - grid: 같은 칸의 요소를 찾는다
    - grid: 칸 경계를 넘어 반경 안에 있는 요소를 찾는다
    - grid: 반경 밖 요소는 돌려주지 않는다
    - grid: 빈 색인은 빈 배열
    - grid: 고정 시드 무작위 5,000개 사각형에서 100개 점의 `nearby` 결과가 전수 검사 결과와 같다
    - magnet: 커서가 요소 안이면 거리 0으로 그 요소를 잡는다
    - magnet: 잡는 범위(48px) 안에서 가장 가까운 요소를 잡고, 범위 밖이면 `null`
    - magnet: 현재 잡힌 A보다 B가 24px 미만 더 가까우면 A 유지, 24px 초과 더 가까우면 B로 바뀜
    - magnet: 커서가 A에서 잡는 범위 밖으로 나가면 놓는다(범위 안 다른 요소가 없으면 `null`)
    - magnet: 거리가 같으면 면적이 작은 요소(안쪽 요소)를 고른다
  </behavior>
  <action>
RED 먼저: 두 시험 파일에 behavior 열을 `it`으로 쓴다(시드 무작위는 시험 안의 작은 LCG 함수). 실패 확인 후 RED 커밋.

`src/core/grid-index.ts`(D-04): `createGridIndex<T extends { rect: Rect }>(cellPx = 128)` → `{ build(items: T[]): void; nearby(p: Point, radius: number): T[] }`. 사각형이 걸치는 모든 칸에 넣고, 조회는 `p ± radius`가 걸치는 칸만 본 뒤 점-사각형 거리로 거른다(중복 제거). `Rect = { x, y, w, h }`, `Point = { x, y }`, `rectDistance(p, r)`(안이면 0)도 export.

`src/core/magnet.ts`(D-10): `pickTarget({ cursor, candidates, currentId, captureMarginPx, switchHysteresisPx }): string | null`. 후보는 `{ id: string; rect: Rect }`. 규칙은 behavior 순서대로. 위험한 버튼 예외는 Plan 01-08이 이 함수에 더한다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/grid-index.test.ts tests/unit/magnet.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 10 tests passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/grid-index.ts`와 `src/core/magnet.ts`에 `document`·`window`·`chrome` 참조가 없다
    - `export function pickTarget`과 `export function createGridIndex`가 있다
    - 두 시험 파일 합쳐 10개 이상 통과
  </acceptance_criteria>
  <done>잡기 판단과 공간 색인이 단위 시험 10개로 고정되었다. RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 요소 수집기·강조 테두리·화면 변화 추적</name>
  <files>src/page/collector/collector.ts, src/page/overlay/ring.ts, src/entrypoints/content.ts, tests/practice-site/targets.html, tests/e2e/magnet.e2e.ts</files>
  <read_first>
    - src/core/grid-index.ts, src/core/magnet.ts
    - src/page/input/pipeline.ts (pointermove 처리기 등록 방법)
    - src/page/overlay/mode-indicator.ts (ensureOverlayRoot)
    - docs/design/SYSTEM.md ("오버레이의 바탕 규칙", "크기", "모션", "상태 표 — 자석 커서")
    - docs/design/tokens.css
  </read_first>
  <behavior>
    - e2e: `targets.html`의 12×12px 버튼에서 30px 떨어진 곳에 커서 → 테두리가 보이고, 테두리 상자가 버튼 사각형보다 사방 8px 큼(±1px), 테두리 두께 5px
    - e2e: 모든 요소에서 100px 넘게 떨어진 곳 → 테두리 없음
    - e2e: 버튼 A를 잡은 뒤 B 쪽으로 옮겨 B가 10px 더 가까운 자리 → A 유지, B가 40px 더 가까운 자리 → B
    - e2e: 이미지 링크(`<a><img></a>`), `onclick` 이미지, `role="button"` div가 각각 잡힌다
    - e2e: `display:none`·`visibility:hidden` 버튼 근처에서는 잡히지 않는다
    - e2e: 1초 뒤 나타나는 입력칸 근처에 커서 → 나타난 뒤 200ms 안에 잡힌다
    - e2e: 요소를 잡은 채 `page.mouse.wheel`로 300px 스크롤 → 200ms 안에 테두리가 요소의 새 위치를 따라간다
    - e2e: 잡힌 요소를 페이지 스크립트가 지우면 200ms 안에 테두리가 없어진다
    - e2e: 도우미 전체 끄기 상태에서는 테두리가 나타나지 않는다
  </behavior>
  <action>
RED 먼저: `tests/practice-site/targets.html`(D-28)을 만든다 — 12×12px 버튼 여러 개(각각 클릭 카운터), 이미지 링크, `onclick` 이미지, `role="button" tabindex="0"` div, 숨긴 버튼 둘, 긴 양식(입력칸 20개), `setTimeout(1000)` 뒤 나타나는 입력칸 `#late`, 화면 세 배 높이. 버튼 A·B는 히스테리시스 시험을 위해 60px 간격. `tests/e2e/magnet.e2e.ts`에 behavior 아홉을 쓰고(커서는 `page.mouse.move`, 테두리는 열린 shadow root 안 `[data-part="ring"]`을 Playwright 로케이터로 읽음) 실패 확인 후 RED 커밋.

`src/page/collector/collector.ts`(D-04, ELEM-01, ELEM-03): `createCollector({ signal })` → `{ items(): Item[]; get(id): Element | undefined; onChange(cb) }`. `Item = { id, rect(viewport), name, kind }`. 누를 수 있는 요소: `button`, `a[href]`, `input:not([type=hidden])`, `select`, `textarea`, `summary`, `[role=button|link|checkbox|radio|menuitem|tab|option|switch]`, `[onclick]`, `[tabindex]:not([tabindex="-1"])`, 그리고 `img`·`svg`·`div`·`span` 중 계산된 `cursor: pointer`인 것(페이지 리스너는 content script에서 보이지 않으므로 — 가정 참고). 보이는 것만: 크기 0 아님, `display:none`·`visibility:hidden`·`inert` 조상 없음, 화면(viewport) 안. `disabled`는 뺀다. 이름 = `aria-label` → 연결된 `<label>` 글자 → 글자 내용 → `value`(submit) → `alt`/`title` 순, 공백 정리. `MutationObserver`(subtree, childList, attributes)와 `scroll`(capture, passive)·`resize`를 `requestAnimationFrame` 하나로 모아 한 번에 다시 모으고 `grid.build`(D-04). 요소 id는 `WeakMap<Element, string>`으로 안정적으로.

`src/page/overlay/ring.ts`(D-26, D-27): `ensureOverlayRoot()` 안에 `[data-part="ring"]` 하나. 요소 사각형 + `--ring-offset`(8px) 바깥, 테두리 `--ring-width`(5px) solid `--accent`, 바깥 흰 후광 `outline: var(--halo-width) solid var(--halo)`, radius `--radius-ring`, 그림자 없음, `pointer-events: none`. 잡힘이 바뀌면 `transition: transform var(--motion-ring), width var(--motion-ring), height var(--motion-ring)`(80ms, 움직임 줄이기면 0). `showRing(rect)` / `hideRing()`.

`src/entrypoints/content.ts`: 각 프레임에서(D-02) collector와 grid를 만들고, pipeline의 `pointermove`(1초 60번까지)에서 `grid.nearby(cursor, captureMarginPx)` → `pickTarget` → 테두리 갱신. collector `onChange` 때 현재 잡힌 요소의 새 사각형으로 테두리를 옮기고, 잡힌 요소가 사라졌으면 놓는다. 도우미가 꺼지면 테두리를 지운다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/magnet.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "9 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/collector/collector.ts`에 `MutationObserver`와 `requestAnimationFrame`이 있다
    - `src/page/overlay/ring.ts`에 `--ring-width`·`--ring-offset`·`--motion-ring`이 있고 hex 색 리터럴과 `box-shadow`가 없다
    - `tests/practice-site/targets.html`에 `setTimeout`으로 늦게 나타나는 입력칸이 있다
    - `CI=true pnpm exec playwright test tests/e2e/magnet.e2e.ts`가 9 passed
  </acceptance_criteria>
  <done>커서 근처 요소가 넓은 범위·히스테리시스로 잡혀 SYSTEM.md 테두리로 보이고, 화면 변화·스크롤을 따라간다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- ELEM-01·ELEM-03·CLICK-01: edge probe `unclassified — review manually`. 가정한 경계:
  - "클릭 이벤트가 걸린 이미지"는 content script가 페이지 리스너를 볼 수 없어 `onclick` 속성, role, `tabindex`, 계산된 `cursor: pointer`(img·svg·div·span)로 판정한다. 놓치는 경우는 번호표(Plan 01-06)도 같은 목록을 쓰므로 Phase 2 이용자 시험에서 확인한다.
  - 화면 밖 요소는 잡지 않는다(보이는 요소만, D-04). 스크롤하면 다시 모은다.
  - 잡는 범위 48px·히스테리시스 24px는 초깃값이다(Phase 2에서 맞춤).
- 50ms 반응 시간(5,000요소)은 Plan 01-16에서 잰다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 웹 페이지 DOM → content script | 페이지가 요소 이름·위치·개수를 마음대로 만든다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Denial of Service | `collector.ts` | medium | mitigate | MutationObserver·scroll을 rAF 하나로 모아 프레임당 한 번만 다시 모은다, pointermove는 1초 60번까지(D-04). 5,000요소 반응 시간은 Plan 01-16에서 측정 |
| T-01-12 | Tampering | 요소 이름 | low | accept | 이름은 표시·판별에만 쓰고 실행하지 않는다(`textContent`로만 읽고 `innerHTML`로 넣지 않음) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/grid-index.test.ts tests/unit/magnet.test.ts` 10개 이상 통과
- `CI=true pnpm exec playwright test tests/e2e/magnet.e2e.ts` 9 passed
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 요소 인식과 화면 변화 추적이 동작한다(ELEM-01, ELEM-03)
- 넓은 잡기 범위와 히스테리시스로 잡힌 요소가 굵은 테두리로 보인다(CLICK-01)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/grid-index.ts`, `src/core/magnet.ts`, `tests/unit/grid-index.test.ts`, `tests/unit/magnet.test.ts`, `src/page/collector/collector.ts`, `src/page/overlay/ring.ts`, `tests/practice-site/targets.html`, `tests/e2e/magnet.e2e.ts`
- 심볼: `createGridIndex()`, `rectDistance()`, `Rect`, `Point`, `pickTarget()`, `createCollector()`(`items`·`get`·`onChange`), `Item`, `showRing()`, `hideRing()`
- DOM: shadow root 안 `[data-part="ring"]`

<output>
Create `.planning/phases/01-click-helper-foundation/01-04-SUMMARY.md` when done
</output>
