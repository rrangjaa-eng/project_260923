---
phase: 01-click-helper-foundation
plan: 19
type: execute
wave: 19
depends_on: ["01-16", "01-17", "01-18"]
gap_closure: true
files_modified:
  - src/core/unsupported-url.ts
  - tests/unit/unsupported-url.test.ts
  - src/entrypoints/content.ts
  - src/entrypoints/background.ts
  - src/types/chrome.d.ts
  - src/entrypoints/popup/main.ts
  - tests/practice-site/blank-popup.html
  - tests/e2e/blank-popup.e2e.ts
autonomous: true
requirements: [ELEM-02, SAFE-04, SAFE-05]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "여는 페이지가 window.open('')으로 열고 DOM 넣기나 document.write로 채운 주소 없는 새 창에서도 도우미가 한 번만 돈다(호스트 1개, 번호 누르기 1번). 그 창의 사이트는 여는 쪽 출처다 — 여는 쪽 사이트를 끄면 새 창도 꺼지고, 누른 기록은 여는 쪽 출처 키에 쌓이며, 새 창 안 iframe도 같은 사이트 설정을 따른다 (ELEM-02, SAFE-04)"
    - "그 새 창 탭의 아이콘과 메뉴는 '도울 수 없음'을 보이지 않고, 메뉴의 '이 사이트에서 끄기'가 여는 쪽 사이트를 끈다. 브라우저 내부 페이지, 브라우저가 연 빈 탭, 불투명 출처 창은 그대로 도울 수 없음이다. noopener로 연 새 창은 Task 1 RED 단계에서 실측한 대로 다룬다 — Chrome이 불투명 출처를 주면 도울 수 없음, 만든 쪽 http(s) 출처를 주고 도우미가 들어가면 다른 주소 없는 새 창과 똑같이(그 출처가 사이트) 다룬다 (SAFE-05, D-21)"
    - "전체 게이트(CI=true pnpm test)가 한 번의 실행에서 0 failed·0 flaky로 통과하고, src/에 네트워크 API 호출과 창 사이 메시지 호출이 없다"
  prohibitions:
    - statement: "주소 없는 새 창의 사이트 정체는 Chrome이 준 값(frameId 0의 sender.origin)이나 그 창 문서 자신의 출처로만 정한다 — 페이지나 메시지 본문이 준 출처 문자열로 다른 사이트의 설정을 끄거나 기록을 쌓지 않는다"
      requirement_id: SAFE-04
      category: safety
      status: resolved
      verification: judgment
    - statement: "도우미는 페이지 내용·누른 기록·설정을 밖(서버·외부 주소)으로 보내지 않는다 — 새 창 지원도 확장 안(chrome.runtime·chrome.storage·chrome.tabs)에서만 일어난다 (D-31)"
      requirement_id: ELEM-02
      category: privacy
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/core/unsupported-url.ts"
      provides: "about: 문서가 물려받은 http(s) 출처만 사이트로 인정하는 순수 함수"
      contains: "inheritedSiteOrigin"
    - path: "tests/practice-site/blank-popup.html"
      provides: "연습 페이지: window.open('') 새 창을 DOM 넣기·document.write로 여는 함수, noopener 두 가지(window.open 기능 문자열, rel=noopener 링크), srcdoc iframe 넣기"
    - path: "tests/e2e/blank-popup.e2e.ts"
      provides: "주소 없는 새 창: 도우미 한 번, 사이트 = 여는 쪽 출처, 아이콘·메뉴·기록·자식 프레임"
    - path: "src/entrypoints/background.ts"
      provides: "탭별 맨 위 문서 출처 기록(frameId 0 sender.origin)과 siteOriginOfTab"
      contains: "siteOriginOfTab"
  key_links:
    - from: "src/entrypoints/background.ts"
      to: "src/core/unsupported-url.ts"
      via: "siteOriginOfTab(tabId, tab.url) — about: 탭은 inheritedSiteOrigin(tab.url, frameId 0 메시지의 sender.origin)로 site/query·recordPress·setSiteDisabled·아이콘 판정"
      pattern: "inheritedSiteOrigin"
    - from: "src/entrypoints/content.ts"
      to: "src/core/unsupported-url.ts"
      via: "맨 위 about: 문서는 inheritedSiteOrigin(location.href, window.origin)이 null이면 시작하지 않고, 아니면 그 출처를 사이트로 쓴다"
      pattern: "inheritedSiteOrigin"
    - from: "src/entrypoints/popup/main.ts"
      to: "src/entrypoints/content.ts"
      via: "about: 탭이면 chrome.tabs.sendMessage(tabId, { type: 'site/ping' }, { frameId: 0 }) 답의 origin으로 사이트 카드를 만든다(최종 대조는 SW의 siteOriginOfTab)"
      pattern: "site/ping"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 어느 사이트에서든(iframe 본문 포함) 원하는 요소를 마우스를 대충 가져가거나 숫자 키 하나로 누르고, 방해되면 즉시 끌 수 있기를, **so that** 떨림으로 인한 잘못 누름과 위험한 버튼 오조작 없이 평소 사이트를 빠르게 쓸 수 있다.

<objective>
Gap closure 마지막 계획이다. 사용자 결정(2026-09-26)으로 주소 없는 새 창을 지원하고, phase 마감 게이트를 돈다.
(1) 주소 없는 새 창(`window.open('')`을 여는 쪽이 DOM이나 document.write로 채움, 결재 팝업 등)에서도 도우미가 돈다. 그 창의 사이트는 여는 쪽 출처이고, 아이콘·메뉴가 "도울 수 없음"을 보이지 않는다. 01-17 실행자는 맨 위 about: 가드를 넣지 않도록(넣었으면 되돌리도록) 지시받았다. 전체 지원은 이 계획이 맡는다.
(2) 싼 게이트 → DOM 감사 재실행 → 전체 게이트 한 번 → 네트워크 API·창 사이 메시지 grep. 01-17·01-18·01-19 전체를 한 번에 확인한다.

Purpose: ELEM-02(어느 사이트에서든), SAFE-04(D-20 사이트별 끄기 — 새 창은 여는 쪽 사이트), SAFE-05(D-21 도울 수 없음 표시가 실제와 같음). 이 계획이 phase 금지사항 "새 창의 사이트 정체는 Chrome이 준 값으로만"과 "외부 전송 없음"(D-31 게이트)을 맡는다.
Output: `inheritedSiteOrigin`과 단위 시험, `blank-popup.html`·`blank-popup.e2e.ts`, `content.ts`·`background.ts`·`chrome.d.ts`·`popup/main.ts` 수정, 전체 게이트 결과.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-click-helper-foundation/01-CONTEXT.md
@.planning/phases/01-click-helper-foundation/01-VERIFICATION.md
@.planning/phases/01-click-helper-foundation/01-17-SUMMARY.md
@.planning/phases/01-click-helper-foundation/01-18-SUMMARY.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성 없음.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트를 띄우면 그 프롬프트에도 세 스킬과 `model`을 명시한다).
- tracer·`tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(01-19): …` → 최소 구현 → GREEN 커밋 `feat(01-19): …` 또는 `fix(01-19): …`. 제목은 영어 접두어, 본문은 한국어. 한 커밋 한 의도.
- 완료 판정은 `CI=true`(프로덕션 빌드). 전체 게이트는 마지막 작업(Task 3)에서 **한 번**만 돈다.
- 01-17·01-18이 content.ts를 바꿨다. content.ts의 함수 이름·줄 번호는 이 계획에 적힌 것을 믿지 말고, 작업 전에 `01-17-SUMMARY.md`·`01-18-SUMMARY.md`와 `grep -n`으로 현재 이름을 확인해 쓴다. 다르면 SUMMARY 편차에 적는다.
- 01-17의 `cleanupOldHelper()` 계약과 01-18의 `cleanedUp` 규칙을 지킨다. 새 chrome 리스너·타이머·비동기 이어짐은 정리 뒤 아무것도 하지 않는다.
- CLAUDE.md §7 화면 사용성 원칙: 이 계획은 새 문구·설명·확인 창을 더하지 않는다. 새 창 탭도 기존 아이콘·메뉴 카드로 상태를 보인다. 메뉴는 이용자에게 출처를 고르게 하지 않고, 시스템이 아는 여는 쪽 출처를 쓴다.
- 연습 사이트(`practice.test`, `other.test`)에서만 시험한다. 새 창도 연습 페이지가 `window.open`으로 연다(D-14).

## Planner probe evidence (2026-09-26, 저장소 밖 scratch, `match_about_blank`를 켠 빌드 복사본)

**주소 없는 새 창(Task 1·2):** `match_about_blank`를 켠 빌드 복사본으로 실측했다. 여는 페이지는 `http://practice.test/opener.html`이다.
- `window.open('')` 뒤 DOM으로 버튼을 넣은 새 창: 도우미가 돌았다(호스트 1개, 두 번 빠른 클릭 1회). 그 창의 `location.href`는 `about:blank`, `location.origin`은 `"null"`, `self.origin`은 `http://practice.test`였다. `chrome.tabs`의 그 탭 `url`은 `about:blank`였다. 그 창 content script가 보낸 메시지의 `sender.url`은 `about:blank`, `sender.origin`은 `http://practice.test`였다(Chrome이 채움).
- `window.open('')` 뒤 `document.write`로 채운 새 창: 도우미가 귀가 먹은 상태였다(두 번 클릭 2회, 01-17이 고치는 모양). 그 창의 `location.href`와 `location.origin`은 여는 쪽 주소·출처로 바뀌었다. 하지만 **탭 `url`은 `about:blank`로 남았다.** 그러므로 새 창의 사이트는 탭 URL이 아니라 문서 출처(`sender.origin`, `self.origin`)로 정해야 한다.
- 지금 코드에서는 이런 새 창의 사이트 키가 `"null"`이 된다. `location.origin`이나 `new URL('about:blank').origin`을 쓰기 때문이다. 아이콘은 `isUnsupportedUrl('about:blank')` 때문에 "도울 수 없음"을 보인다.

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): 주소 없는 새 창(window.open('') + 채우기)에서 도우미가 돈다 — 그 창의 사이트는 여는 쪽 출처 (재현 먼저)</name>
  <files>src/core/unsupported-url.ts, tests/unit/unsupported-url.test.ts, src/entrypoints/content.ts, tests/practice-site/blank-popup.html, tests/e2e/blank-popup.e2e.ts</files>
  <read_first>
    - src/core/unsupported-url.ts, tests/unit/unsupported-url.test.ts (`STORE_RULES`, http·https 규칙)
    - src/entrypoints/content.ts — 현재 모습. `main()` 시작부, 01-17의 문서 다시 쓰기 감시, 01-17 맨 위 about: 가드가 없는지(없어야 한다), 01-18 Task 2가 정한 맨 위 `cachedTopOrigin`, `readPinsAndPresses`. `grep -n "cachedTopOrigin\|readPinsAndPresses\|about:\|window.top === window" src/entrypoints/content.ts`로 확인한다
    - .planning/phases/01-click-helper-foundation/01-17-SUMMARY.md (맨 위 about: 가드를 넣지 않았는지·되돌렸는지, editor-frames.e2e.ts에 "새 창에는 도우미가 없다" 시험이 남았는지, frameId 0 다시 넣기 결과)
    - tests/practice-site/popup-target.html, tests/e2e/spike.e2e.ts (새 창을 `context.waitForEvent('page')`로 받는 방식)
    - tests/e2e/site-toggle.e2e.ts (`hasHelperRoot`, SW가 site 항목을 직접 쓰는 방식)
    - 이 계획의 "Planner probe evidence" 새 창 항목
  </read_first>
  <behavior>
    - 단위(`it` 블록 7개): `inheritedSiteOrigin`이 다음을 돌려준다.
      - `('about:blank', 'http://practice.test')` → `'http://practice.test'`
      - `('about:srcdoc', 'https://x.test')` → `'https://x.test'`
      - `('about:blank', 'null')` → null
      - `('about:blank', undefined)` → null
      - `('about:blank', 'https://chromewebstore.google.com')` → null(스토어는 물려받아도 도울 수 없음)
      - `('chrome://version', 'http://practice.test')` → null
      - `('http://practice.test/a', 'http://other.test')` → null(about: 문서에만 쓰는 함수)
    - e2e: `blank-popup.html`의 `openDomPopup()`(여는 쪽이 `window.open('')` 뒤 DOM으로 `#popup-btn`·`#popup-count`·`#popup-input`을 넣음) → 새 창에 `tremor-helper-root`가 정확히 1개이고 모드 표시가 도우미다. 100ms 간격 두 번 클릭 → 카운터 1. F → `#popup-btn` 번호 → 카운터가 정확히 1이다
    - e2e: `openWritePopup()`(`window.open('')` 뒤 document.write로 같은 내용) → 5초 안에 그 창에서 커서를 대면 테두리가 보인다(새 도우미 살아 있음). 호스트가 정확히 1개다. 두 번 클릭 → 1. F → 번호 → 카운터가 정확히 1이다(01-17 다시 넣기가 frameId 0에서도 동작)
    - e2e: DOM 새 창이 열린 채 SW가 `site:http://practice.test`를 `{ schemaVersion: 1, data: { disabled: true, pins: [] } }`로 쓰면 새 창의 호스트가 사라지고 두 번 클릭 → 2다. 다시 disabled false로 쓰면 호스트가 1개로 돌아온다(여는 쪽 사이트 끄기를 따름)
    - e2e(실측한 쪽을 단언): `openNoopenerPopup()`(`window.open('', '_blank', 'noopener')`)과 `#noopener-link`(`target="_blank" rel="noopener"`인 about:blank 링크)로 연 새 창 각각에 대해, RED 단계 실측이 "불투명 출처"였으면 1초 뒤 `tremor-helper-root`가 없다. "만든 쪽 http(s) 출처 + 도우미 들어감"이었으면 호스트가 1개이고 그 창의 사이트가 그 출처다(SW가 그 출처의 site 항목을 끄면 호스트가 사라짐)
  </behavior>
  <action>
RED 먼저. 연습 페이지 `tests/practice-site/blank-popup.html`을 새로 만든다(D-28). 맨 위에는 설명 글자와 `#noopener-link`(`href="about:blank" target="_blank" rel="noopener"`)만 두고, 인라인 스크립트가 전역 함수 넷을 둔다.
- `openDomPopup()`, `openWritePopup()`, `openNoopenerPopup()`: 위 behavior대로 새 창을 연다.
- `addSrcdocFrame(win)`: 이미 연 새 창에 버튼 `#child-btn` 하나가 든 srcdoc iframe `#popup-child`를 넣는다(Task 2가 쓴다).
새 창 버튼의 카운터는 여는 쪽 스크립트가 붙인 click 리스너가 `#popup-count`를 1씩 올린다(document.write 쪽은 새 창 안 인라인 핸들러로 같은 일을 한다). 시험은 `page.evaluate`로 함수를 부르고 새 창은 `context.waitForEvent('page')`로 받는다.
**noopener 실측을 먼저 한다**(프로덕션 빌드, `CI=true`). 시험 쪽에서만 SW에 응답하지 않는 `chrome.runtime.onMessage` 기록 리스너를 걸어 둔다(01-17의 frame/state 기록과 같은 방식, 제품 훅 없음). 그 상태로 두 noopener 경우(`openNoopenerPopup()`, `#noopener-link` 클릭)로 새 창을 열고 세 가지를 잰다: 그 창의 `self.origin`, `tremor-helper-root` 유무, 그 탭 frameId 0 메시지의 `sender.origin`. 결과(불투명 출처 / 만든 쪽 http(s) 출처 + 도우미 들어감)를 SUMMARY에 적고 noopener behavior의 단언을 그 결과로 정한다. 어느 쪽이든 `inheritedSiteOrigin`과 content.ts 가드 말고 따로 구분하는 장치(`window.opener` 검사 등)는 두지 않는다(CLAUDE.md §3.2 — 추측성 장치 금지). 불투명이면 `inheritedSiteOrigin`이 null을 돌려줘 도울 수 없음이고, http(s)면 다른 새 창과 같은 규칙을 탄다.
`unsupported-url.test.ts`에 단위 behavior를, `tests/e2e/blank-popup.e2e.ts`에 e2e behavior를 쓰고 돌린다. 반드시 실패해야 하는 것은 둘이다. 단위 시험(함수 없음)과, 사이트 끄기 시험(새 창의 사이트 키가 `"null"`이라 여는 쪽 사이트를 꺼도 새 창은 켜져 있음)이다. 나머지 e2e는 01-17 뒤라면 이미 통과할 수 있다(probe: DOM 새 창은 이미 돌았다). 처음 실행에서 통과하면 SUMMARY에 특성 시험으로 적는다. RED 커밋.

GREEN:
1. `src/core/unsupported-url.ts`에 순수 함수 `inheritedSiteOrigin(url, documentOrigin): string | null`을 더한다. `url`의 스킴이 `about:`이고 `documentOrigin`이 http·https 출처로 파싱되며 `STORE_RULES` 호스트가 아닐 때만 그 출처를 돌려주고, 나머지는 null이다. `isUnsupportedUrl`은 바꾸지 않는다(about: 주소는 주소만으로는 여전히 도울 수 없음이고, 문서 출처를 아는 쪽이 이 함수를 더 쓴다).
2. `src/entrypoints/content.ts` `main()` 맨 앞(옛 호스트 지우기와 문서 감시보다 앞)에서 `location.protocol`이 `about:`이고 맨 위 프레임일 때 `inheritedSiteOrigin(location.href, window.origin)`을 구한다.
   - 결과가 null이면 시작하지 않고 돌아간다. sandbox 등으로 출처가 불투명한 창, 브라우저가 연 빈 탭이 여기에 해당한다(아이콘의 "도울 수 없음"이 맞는 경우). noopener 새 창은 위 실측이 정한 쪽을 그대로 따른다 — 이 함수 결과 말고 다른 조건을 더하지 않는다.
   - `window.origin`은 확장 격리 세계에서 읽는 그 문서 자신의 출처다(about:blank면 만든 쪽 출처). 페이지가 준 문자열이 아니다.
3. 맨 위 사이트 출처(01-18 Task 2의 `cachedTopOrigin`)는 about: 문서면 위 결과를 쓰고, 그 밖은 01-18 Task 2 그대로(`window.location.origin`) 둔다. file:// 동작(IN-06)은 바꾸지 않는다. document.write 뒤 새로 들어온 도우미는 주소가 여는 쪽 주소로 바뀌어 있으므로 일반 경로를 탄다(probe).
4. `readPinsAndPresses()`는 `window.location.origin` 대신 `cachedTopOrigin`으로 읽는다. 맨 위에서만 불리고, 맨 위는 이 값을 곧바로 안다. 새 창이 `site:null`을 읽지 않게 하려는 것이다.
5. 확인만 한다: 01-17의 "새 창에는 도우미가 없다" 시험·가드는 01-17이 되돌렸거나 01-18 Task 1이 지웠다(01-17·01-18 SUMMARY). 남아 있지 않은지 확인해 SUMMARY에 적는다. 만약 남아 있으면 멈추고 편차로 보고한다(이 계획에서 조용히 고치지 않는다).
통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/unsupported-url.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or the 7 new inheritedSiteOrigin cases are not all in the passed list</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/blank-popup.e2e.ts</automated>
    <fails_when>non-zero exit, or fewer than 4 passed in the summary, or "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/editor-frames.e2e.ts tests/e2e/site-toggle.e2e.ts tests/e2e/hints.e2e.ts tests/e2e/lifecycle.e2e.ts</automated>
    <fails_when>non-zero exit, or any test reported failed or flaky</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/unsupported-url.ts`에 `export function inheritedSiteOrigin`이 있고 `document`·`window`·`chrome` 참조가 없다(순수)
    - `tests/unit/unsupported-url.test.ts`에 `inheritedSiteOrigin` 새 `it` 블록 7개가 있고 모두 통과한다
    - `src/entrypoints/content.ts`에 `inheritedSiteOrigin(location.href, window.origin)` 호출이 있고, `readPinsAndPresses` 안에서 `window.location.origin`을 더는 쓰지 않는다
    - blank-popup.e2e.ts 4 passed 이상(DOM 새 창, document.write 새 창, 여는 쪽 사이트 끄기, noopener 새 창 — 실측한 쪽을 단언)
    - SUMMARY에 noopener 두 경우의 실측(`self.origin`, 도우미 유무, frameId 0 `sender.origin`)과 어느 쪽이었는지가 있다. `src/`에 `window.opener`를 읽는 새 코드가 없다(코드 읽기)
    - editor-frames·site-toggle·hints·lifecycle e2e가 모두 통과한다
  </acceptance_criteria>
  <done>결재 팝업처럼 여는 쪽이 채우는 주소 없는 새 창에서 도우미가 한 번만 돌고, 그 창의 사이트는 여는 쪽 출처라 여는 쪽 사이트 끄기를 따른다. 출처를 물려받지 못한 창은 그대로 도울 수 없음이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 주소 없는 새 창 — 아이콘·메뉴·누른 기록·새 창 안 iframe이 여는 쪽 사이트를 쓴다 (Chrome이 준 sender.origin으로)</name>
  <files>src/entrypoints/background.ts, src/types/chrome.d.ts, src/entrypoints/popup/main.ts, src/entrypoints/content.ts, tests/e2e/blank-popup.e2e.ts</files>
  <read_first>
    - src/entrypoints/background.ts (`updateActionForTab`·`respondsToSitePing`, `site/query` 분기(01-18 Task 2 훅 포함), recordPress의 `tabOrigin`·`frameOrigin`, `setSiteDisabled`의 T-01-36 대조, `chrome.tabs.onRemoved`·`onUpdated`)
    - src/types/chrome.d.ts (`MessageSender`)
    - src/entrypoints/popup/main.ts 305-335행(`renderForTargetTab` — 아이콘 제목으로 도울 수 없음 판정, `new URL(tab.url).origin`)
    - src/entrypoints/content.ts (`site/ping` 응답 자리 — grep으로 확인)
    - tests/e2e/site-toggle.e2e.ts 25-80행(아이콘 제목·배지·메뉴 시험 방식, `openPopup(target)`, chrome://version 탭)
    - 이 계획의 "Planner probe evidence" 새 창 항목(탭 url은 about:blank로 남고 `sender.origin`은 여는 쪽 출처)
  </read_first>
  <behavior>
    - e2e: DOM 새 창 탭의 아이콘 제목이 5초 안에 "손 떨림 도우미"이고 배지가 ""다. 같은 시험 안에서 `chrome://version` 탭은 그대로 "도울 수 없음"·"없음"이다
    - e2e: 새 창 탭을 대상으로 연 메뉴(`openPopup(popupPage)`)에 "도울 수 없음" 안내가 없고 "이 사이트에서 끄기" 카드가 있다. 누르면 SW 저장소의 `site:http://practice.test`가 disabled가 되고, 새 창과 여는 쪽 탭 모두에서 `tremor-helper-root`가 사라진다
    - e2e: 새 창에서 번호로 `#popup-btn`을 누르면 SW `chrome.storage.local`의 `presses:http://practice.test` 기록이 늘고, `presses:null`이나 `about:`로 시작하는 기록 키는 없다
    - e2e: 여는 쪽이 새 창에 `addSrcdocFrame`으로 자식 iframe을 넣은 뒤 SW가 `site:http://practice.test`를 끄면, 그 탭의 frameId 0이 아닌 frame/state가 false가 된다(자식 site/query가 여는 쪽 출처로 답을 받음)
  </behavior>
  <action>
RED 먼저: behavior 넷을 `tests/e2e/blank-popup.e2e.ts`에 더하고 실패를 확인한다. 실패 모양은 이렇다. 아이콘·메뉴는 `about:blank`라서 "도울 수 없음"이다. 기록은 background가 탭 출처 `"null"`과 대조해 거절한다. 자식 site/query의 답은 `"null"` 출처다. RED 커밋.

GREEN. 원칙: 새 창 탭의 사이트는 **Chrome이 채운 값**으로만 정한다. frameId 0이 보낸 메시지의 `sender.origin`이다(probe: about:blank 창에서도 여는 쪽 출처). 메시지 본문이나 페이지가 준 출처 문자열을 기준으로 쓰지 않는다.
1. `src/types/chrome.d.ts`: `MessageSender`에 `origin?: string`을 더한다(Chrome 80+, 보낸 문서의 실제 출처).
2. `src/entrypoints/background.ts`:
   - 기록: 탭별 맨 위 문서 출처 기록(평범한 객체, 탭 id → 출처)을 둔다. onMessage에서 `sender.id` 확인과 parse를 지난 메시지 중 `sender.frameId === 0`이고 `sender.tab?.id`와 `sender.origin`이 있으면 채운다. `tabs.onRemoved`와 `tabs.onUpdated`(status loading)에서 지운다.
   - `siteOriginOfTab(tabId, tabUrl)`: `tabUrl`이 `about:`이면 `inheritedSiteOrigin(tabUrl, 기록된 출처)`, 그 밖은 지금처럼 `new URL(tabUrl).origin`(파싱 실패는 지금처럼 다룬다).
   - 쓰는 곳 셋: `site/query`(없으면 빈 답 — 자식은 01-18 Task 2의 재시도로 기록이 채워질 때까지 기다린다), recordPress의 `tabOrigin`, `setSiteDisabled`의 T-01-36 대조.
   - `updateActionForTab`: 주소가 `about:`로 시작하면 `isUnsupportedUrl` 대신 ping만으로 판정한다. content script는 Task 1 가드 때문에 물려받은 http(s) 출처가 있을 때만 시작하므로, ping에 답이 있으면 도울 수 있다. 그 밖의 주소(브라우저 내부·스토어)는 지금 규칙 그대로다.
   - document.write 다시 넣기 틈에 판정이 "도울 수 없음"으로 굳으면(`systematic-debugging`으로 확인) frameId 0의 `frame/state`를 받을 때 그 탭을 다시 판정하는 것으로 보완하고 SUMMARY에 적는다.
3. `src/entrypoints/content.ts`: 맨 위의 `site/ping` 답에 `origin: cachedTopOrigin`을 더한다. 메뉴 표시용이다.
4. `src/entrypoints/popup/main.ts` `renderForTargetTab`: `tab.url`이 `about:`이면 사이트 출처를 `chrome.tabs.sendMessage(tabId, { type: 'site/ping' }, { frameId: 0 })` 답의 `origin`으로 얻는다. 답이 없거나 http(s)가 아니면 사이트 카드를 만들지 않는다. 그 밖은 지금처럼 URL 출처다. 끄기 요청의 최종 검증은 background의 T-01-36 대조(`siteOriginOfTab`)가 한다.
통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/blank-popup.e2e.ts</automated>
    <fails_when>non-zero exit, or fewer than 8 passed in the summary, or "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/site-toggle.e2e.ts tests/e2e/helper-toggle.e2e.ts tests/e2e/frames.e2e.ts tests/e2e/hints.e2e.ts</automated>
    <fails_when>non-zero exit, or any test reported failed or flaky</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/types/chrome.d.ts`의 `MessageSender`에 `origin?: string`이 있다
    - `src/entrypoints/background.ts`에 `siteOriginOfTab`와 `inheritedSiteOrigin` 사용이 있고, `site/query`·recordPress·`setSiteDisabled`가 모두 `siteOriginOfTab`을 쓴다
    - background.ts가 새 창의 사이트를 정할 때 쓰는 값은 `sender.origin`(frameId 0)과 `tab.url`뿐이다(메시지 본문의 origin 필드를 기준으로 쓰지 않음 — 코드 읽기)
    - blank-popup.e2e.ts 8 passed 이상(Task 1의 4 + 이 작업의 4)
    - site-toggle·helper-toggle·frames·hints e2e가 모두 통과한다. `chrome://version`의 "도울 수 없음" 시험을 포함한다
  </acceptance_criteria>
  <done>주소 없는 새 창 탭의 아이콘·메뉴가 "도울 수 없음"을 보이지 않는다. 메뉴의 "이 사이트에서 끄기"는 여는 쪽 사이트를 끄고, 누른 기록과 새 창 안 iframe은 여는 쪽 출처를 쓴다. 기준은 Chrome이 준 출처뿐이다.</done>
</task>

<task type="auto">
  <name>Task 3: 싼 게이트 → DOM 감사(기준 그대로) → 전체 게이트 한 번 → 외부 전송 grep</name>
  <files>(없음 — 검증 전용. 게이트가 실패하면 systematic-debugging으로 찾은 원인 파일만 고치고 SUMMARY 편차에 적는다)</files>
  <read_first>
    - CLAUDE.md ("코딩 규칙" — CI=true가 완료 신호, "프론트엔드 — 화면 검증 순서")
    - .planning/phases/01-click-helper-foundation/01-REVIEW-FIX.md ("Final Verification" — 전체 게이트 기록 형식과, 실패가 나면 원인 수정 → 해당 파일 재실행 → 전체 게이트 한 번 더를 SUMMARY에 남긴 방식)
    - tests/e2e/dom-audit.e2e.ts (읽기만 — 감사 기준을 바꾸지 않는다)
  </read_first>
  <action>
1. 싼 게이트: `pnpm lint && pnpm typecheck && CI=true pnpm build`. 이어서 빌드 manifest에 `"match_about_blank":true`가 1번 있는지 확인한다(01-17).
2. DOM 감사 재실행: 이 gap이 더한 알림 문구가 기존 토스트 부품으로 뜨므로 `CI=true pnpm exec playwright test tests/e2e/dom-audit.e2e.ts`를 기준 그대로 돌린다. 실패하면 감사 파일이 아니라 오버레이 쪽을 SYSTEM.md에 맞춰 고친다. SYSTEM.md를 벗어나야 한다면 멈추고 DECISIONS.md 기록·승인을 요청한다(DESIGN.md §4.6).
3. 전체 게이트 한 번: `CI=true pnpm test`(단위 → e2e 전부)를 돌려 0 failed·0 flaky를 확인한다. 실패가 나오면 `systematic-debugging`으로 근본 원인을 찾아 고친다(`fix(01-19): …`, 시험 기준을 느슨하게 하지 않는다). 그 파일만 다시 돌려 통과를 확인한 뒤 전체 게이트를 한 번 더 돌린다. SUMMARY에는 두 실행을 따로 적는다. 첫 실행은 결과 수치와 실패한 시험 이름·근본 원인·고친 커밋을, 둘째 실행은 0 failed·0 flaky 수치를 적는다. 실패 없이 한 번에 통과했다면 한 번만 적는다.
4. 외부 전송 금지(D-31)와 창 사이 메시지 금지(D-09) grep이 비어 있는지 확인한다(아래 verify).
5. SUMMARY에 다음을 적는다.
   - 게이트 수치: 단위·e2e 개수와 소요. 이 gap의 파일별 개수는 editor-frames(01-17 SUMMARY 값), dwell 10, site-toggle 15, doc-editor 4 이상, blank-popup 8 이상, 단위 101 이상(94 + `inheritedSiteOrigin` 7)이다.
   - 해소 근거(시험 이름): 01-VERIFICATION gaps의 두 truth, IN-04, 자식 머무르기, 문서 전체 편집기 Esc, 주소 없는 새 창.
   - 이 계획이 다루지 않은 사람 확인 항목: 01-VERIFICATION human_verification, D-25 결정 대기, 한글 IME 실제 조합(CDP로 대신 못 했다면).
   - 다음 단계: CLAUDE.md Post-build(`/review` → `/qa` → `/cso` → `/ship`). 이 gap은 모든 프레임·about: 새 창 주입, 확장 메시지, 저장, 사이트 정체 판정을 건드렸으므로 `/cso`는 필수다.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && CI=true pnpm build && grep -c '"match_about_blank":true' .output/chrome-mv3/manifest.json</automated>
    <fails_when>non-zero exit, or the printed count is not 1</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/dom-audit.e2e.ts</automated>
    <fails_when>non-zero exit, or fewer than 16 passed, or "No tests found"</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>non-zero exit, or any test reported failed or flaky, or the unit summary shows fewer than 101 passed</fails_when>
    <automated>test -z "$(grep -rnE '\bfetch\(|XMLHttpRequest|WebSocket|sendBeacon|EventSource' src/)"</automated>
    <fails_when>non-zero exit (a network API call exists in src/)</fails_when>
    <automated>test -z "$(grep -rnE '\.postMessage\(' src/)"</automated>
    <fails_when>non-zero exit (a cross-window message call exists in src/)</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm lint`, `pnpm typecheck`, `CI=true pnpm build`가 exit 0이고 빌드 manifest에 `"match_about_blank":true`가 1번 있다
    - `CI=true pnpm exec playwright test tests/e2e/dom-audit.e2e.ts` 16 passed. `tests/e2e/dom-audit.e2e.ts`는 이 gap 동안 바뀌지 않았다 — `git log --oneline cf9aaed..HEAD -- tests/e2e/dom-audit.e2e.ts`가 아무것도 출력하지 않는다(cf9aaed = gap 계획 직전 커밋)
    - 마지막 `CI=true pnpm test` 실행이 0 failed·0 flaky다(단위 101 이상, e2e에 dwell 10·site-toggle 15·doc-editor 4 이상·blank-popup 8 이상 포함). 두 번 돌렸다면 SUMMARY가 첫 실행(결과·원인·수정 커밋)과 마지막 깨끗한 실행을 따로 기록한다
    - 네트워크 API grep과 `.postMessage(` grep이 모두 빈 결과다
    - SUMMARY에 gap 해소 근거표와 Post-build 다음 단계(`/cso` 필수)가 있다
  </acceptance_criteria>
  <done>편집기 iframe gap, 두 안전 결함, 문서 전체 편집기 Esc, 주소 없는 새 창이 닫힌 상태로 Phase 1 전체 게이트가 CI=true 한 번에 통과했고, 외부 전송이 없다. 다음은 /gsd-verify-work 재검증과 Post-build 넷이다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

edge probe가 이 계획의 요구사항 셋(ELEM-02·SAFE-04·SAFE-05)을 모두 `unclassified — review manually`(unresolved)로 돌려줬다. 가정한 경계를 적는다(자동으로 해소하지 않는다).

- **ELEM-02 / SAFE-05** (unresolved): 주소 없는 새 창의 사이트는 frameId 0의 `sender.origin` 기록에 기대 판정한다. SW가 다시 시작한 직후, 그 창의 다음 보고(포트 다시 연결 때 강제 보고, CR-08)가 오기 전까지 잠깐 동안은 자식 site/query나 메뉴의 사이트 끄기가 거절될 수 있다. 01-18의 재시도와 다음 보고로 회복된다. document.write 새 창은 탭 URL이 `about:blank`로 남는다(probe). 그래서 URL이 아니라 문서 출처로 판정한다.
- **ELEM-02 / SAFE-05** (unresolved): 새 창이 물려받는 출처는 "창을 만든 문서"의 출처다. 여는 쪽 탭 안 다른 출처 iframe이 창을 열면 그 iframe의 출처가 새 창의 사이트가 된다. 불투명 출처 창과 브라우저가 연 빈 탭은 시작하지 않는다(도울 수 없음). noopener 새 창은 실측 전에는 **unresolved**다. Task 1 RED에서 재어, 불투명이면 도울 수 없음, 만든 쪽 http(s) 출처면 다른 새 창과 같게 다룬다. "noopener는 반드시 도울 수 없음"은 사용자 결정이 아니다(조정자 지시의 정정, 2026-09-26). file:// 동작(IN-06)은 바꾸지 않는다.
- **SAFE-04** (unresolved): 새 창의 사이트를 끄는 것은 곧 여는 쪽 사이트를 끄는 것이다(같은 사이트 키). 새 창만 따로 끄는 선택지는 만들지 않는다(§7 — 이용자 결정 최소).
- D-25 손상 설정의 "도우미 끄기 거절" 제품 결정, 다른 IN-* 항목, 사람 확인 항목은 이 gap이 다루지 않는다(사용자 범위 지정).

## 출처 대조 (Coverage Audit — 이 계획 몫)

| 출처 | 항목 | 담당 |
| --- | --- | --- |
| 사용자 결정 2026-09-26 (ELEM-02·SAFE-04·SAFE-05) | 주소 없는 새 창: 사이트 = 여는 쪽 출처, document.write 한 번, 여는 쪽 사이트 끄기, noopener는 실측대로 | Task 1 |
| 사용자 결정 2026-09-26 (SAFE-05·SAFE-04) | 새 창 탭의 아이콘·메뉴, 누른 기록, 새 창 안 iframe | Task 2 |
| 사용자 규약 | 마지막 gap 계획의 마지막 작업: 싼 게이트 → 전체 게이트 한 번 + 외부 전송 grep, 오버레이를 건드렸으면 DOM 감사 재실행 | Task 3 |
| GAP 1·2, IN-04, 자식 머무르기, 문서 전체 편집기 Esc | — | 01-17, 01-18 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 여는 페이지 → 주소 없는 새 창 | 여는 쪽 스크립트가 새 창 문서를 채운다. 그 창의 출처는 만든 문서에서 물려받는다 |
| content script(새 창) → service worker | 새 창 탭의 사이트 정체(`sender.origin`), 메뉴의 사이트 끄기 대조 |
| 확장 → 인터넷 | 도우미가 밖으로 데이터를 보낼 수 있는 경로(게이트 grep) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-59 | Spoofing | 주소 없는 새 창의 사이트 정체 | high | mitigate | background는 frameId 0 메시지의 Chrome `sender.origin`(http·https, 스토어 제외)과 `tab.url`로만 새 창 탭의 사이트를 정한다. `site/query`·recordPress·`setSiteDisabled`가 모두 이것(`siteOriginOfTab`)과 대조한다. 메뉴가 ping으로 받은 출처는 표시용이고 최종 대조는 SW가 한다. content script는 격리 세계의 `window.origin`을 쓴다(페이지가 바꿀 수 없음) |
| T-01-60 | Elevation of Privilege | about: 새 창으로 넓어진 주입 범위 | medium | mitigate | 맨 위 about: 문서는 Chrome·문서가 준 http(s) 출처(`window.origin`, SW는 `sender.origin`)가 있을 때만 시작한다. 불투명 출처·브라우저가 연 빈 탭·스토어 출처에서는 시작하지 않는다(`inheritedSiteOrigin` 단위 7개). noopener 새 창은 실측대로다. 불투명이면 시작하지 않고, http(s)면 그 출처가 사이트다(다른 사이트 설정을 건드리지 못함, T-01-59 대조). 어느 쪽인지는 e2e가 단언하고 SUMMARY에 적는다 |
| T-01-58 | Information Disclosure | 확장 전체 | high | mitigate | Task 3의 네트워크 API grep이 빈 결과이고, 모든 e2e의 `blockedRequests`가 비어 있다(D-31) |
| T-01-SC | Tampering | 패키지 설치 | low | accept | 새 패키지 없음 |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/unsupported-url.test.ts`(inheritedSiteOrigin 7개 포함)
- `CI=true pnpm exec playwright test tests/e2e/blank-popup.e2e.ts` 8 passed 이상(주소 없는 새 창)
- DOM 감사 16 passed(기준 불변), `CI=true pnpm test` 한 번 0 failed·0 flaky(단위 101 이상, e2e에 editor-frames(01-17 SUMMARY 값)·dwell 10·site-toggle 15·doc-editor 4 이상·blank-popup 8 이상 포함)
- 네트워크 API grep·`.postMessage(` grep 빈 결과
</verification>

<success_criteria>
- 여는 쪽이 채우는 주소 없는 새 창에서 도우미가 한 번만 돌고, 사이트·아이콘·메뉴·기록이 여는 쪽 출처를 따른다(ELEM-02, SAFE-04, SAFE-05)
- Phase 1 전체 게이트가 CI=true 한 번에 통과한다. 이어서 `/gsd-verify-work` 재검증을 하고, CLAUDE.md Post-build 넷(`/review` → `/qa` → `/cso` → `/ship`)을 건너뛰지 않는다(`/cso` 필수 — 모든 프레임·about: 새 창 주입, 확장 메시지, 저장, 사이트 정체 판정을 건드렸다)
</success_criteria>

## Artifacts this phase produces

- 파일(신규): `tests/practice-site/blank-popup.html`, `tests/e2e/blank-popup.e2e.ts`
- 파일(수정): `src/core/unsupported-url.ts`, `tests/unit/unsupported-url.test.ts`, `src/entrypoints/content.ts`, `src/entrypoints/background.ts`, `src/types/chrome.d.ts`, `src/entrypoints/popup/main.ts`
- 심볼(unsupported-url.ts): `inheritedSiteOrigin(url, documentOrigin)`
- 심볼(background.ts): 탭별 맨 위 문서 출처 기록(frameId 0 `sender.origin`), `siteOriginOfTab(tabId, tabUrl)`, about: 탭의 ping 기반 아이콘 판정
- 타입(chrome.d.ts): `MessageSender.origin`
- 메시지 응답: `site/ping` 답에 `origin`(맨 위 content script의 사이트 출처)
- 연습 페이지 함수·id: `blank-popup.html`(`openDomPopup()`, `openWritePopup()`, `openNoopenerPopup()`, `addSrcdocFrame(win)`, 새 창의 `#popup-btn`·`#popup-count`·`#popup-input`·`#popup-child`·`#child-btn`)
- 게이트 결과: SUMMARY에 전체 게이트 수치(실행이 두 번이면 따로)

<output>
Create `.planning/phases/01-click-helper-foundation/01-19-SUMMARY.md` when done
</output>
