---
phase: 03-navigation-input-condition
plan: 10
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/core/sensitive.ts
  - tests/unit/sensitive.test.ts
  - src/core/field-key.ts
  - src/core/recent-values.ts
  - tests/unit/field-key.test.ts
  - tests/unit/recent-values.test.ts
autonomous: true
requirements: [PRIV-01, INPT-01]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "비밀번호 칸(type=password)과 이름·라벨에 비밀번호·비번·PW·인증번호·OTP·보안카드가 들어간 칸은 늘 민감칸이다"
    - "이름·라벨에 주민·계좌·카드가 들어간 칸은 값이 주민번호(13자리)·계좌번호(10~16자리)·카드번호(14~16자리 + Luhn) 모양일 때만 민감칸이다 — '법인카드 구분'에 '개인', '지급계좌 선택'에 '국민은행'은 민감칸이 아니다"
    - "민감칸 판별은 한 곳(src/core/sensitive.ts의 isSensitive)에 있고 판별 단어 목록과 사이트별 이용자 설정을 인자로 받는다 — 이후 틀·활동 기록·AI 전송이 같은 함수를 쓴다"
    - "칸 이름은 name → id → 라벨 → aria → placeholder 순서로 정해지고, 최근 값은 칸당 새것부터 3개, 사이트당 칸 300개까지만 남으며 빈 값·500자 넘는 값은 들어가지 않는다"
  artifacts:
    - path: "src/core/sensitive.ts"
      provides: "민감칸 판별 단일 함수(PRIV-01, D-26) — 이후 Phase 4·6이 가져다 쓴다"
      exports: ["isSensitive", "looksLikeRrn", "looksLikeAccount", "looksLikeCard", "luhnValid"]
    - path: "src/core/field-key.ts"
      provides: "칸 이름(사이트 안에서 칸을 구분하는 키) 만들기"
      exports: ["fieldKeyOf"]
    - path: "src/core/recent-values.ts"
      provides: "최근 값 목록 갱신(칸당 3개, 사이트당 칸 300개)"
      exports: ["addRecentValue"]
  key_links:
    - from: "src/core/sensitive.ts"
      to: "src/core/settings-schema.ts"
      via: "판별 단어 목록 타입(sensitiveWords의 always·shape)을 설정 v2 스키마에서 가져온다 — 이용자가 바꾼 단어가 그대로 판별에 쓰인다"
      pattern: "settings-schema"
    - from: "src/core/recent-values.ts"
      to: "src/core/settings-schema.ts"
      via: "RecentValuesV1 형식(03-03)을 그대로 받아 새 목록을 돌려준다"
      pattern: "RecentValuesV1"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
민감칸 판별 단일 함수(D-25, D-26)와 칸 이름·최근 값 규칙(D-17, Claude's Discretion 4)을 순수 함수로 먼저 만든다. 입력칸을 벗어날 때 이 판별을 거쳐 기록하는 연결은 03-11이, 카드 표시는 03-12가 한다 — 기록이 판별보다 먼저 생기는 순간이 없도록 이 순서로 나눴다.

Purpose: PRIV-01(설계 10장이 단위 시험을 명시), INPT-01의 기록 바탕. 파일을 다른 계획과 나누지 않는 순수 계획이라 설정 v2(03-03) 뒤 곧바로 명령판 사슬과 나란히(wave 4) 돈다.
Output: `sensitive.ts`·`field-key.ts`·`recent-values.ts`+단위 시험.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-03-SUMMARY.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성·새 권한 없음.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시, `model` 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(03-10): …` → 최소 구현 → GREEN 커밋 `feat(03-10): …`. 본문은 한국어.
- 이 계획의 파일은 모두 `src/core/`(순수 — DOM·전역 창·확장 API를 쓰지 않음)와 `tests/unit/`이다. 다른 파일을 건드리지 않는다(같은 wave의 다른 계획과 파일이 겹치지 않아야 나란히 돈다).
- 시험 값도 실제 개인정보가 아닌 가짜 번호만 쓴다.
- `03-RESEARCH.md`(623줄)·`03-PATTERNS.md`는 통째로 읽지 않는다 — `<read_first>`가 가리킨 절만 Grep으로 줄을 찾아 범위로 읽는다(CLAUDE.md '500줄 이상은 range 필수'. 계획 하나의 문맥 예산을 지키려고 `<context>`에서 뺐다).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 민감칸 판별 단일 함수 (순수 함수, 설계 10장 단위 시험)</name>
  <reversibility rating="costly">`isSensitive(field, value, opts)`의 모양은 Phase 4(틀 저장·활동 기록)와 Phase 6(AI 전송)이 그대로 가져다 쓴다 — 나중에 바꾸면 그 호출처를 모두 고쳐야 한다.</reversibility>
  <files>src/core/sensitive.ts, tests/unit/sensitive.test.ts</files>
  <read_first>
    - src/core/danger.ts (stripSpaces와 단어 포함 판정 — 같은 정규화를 쓴다)
    - src/core/settings-schema.ts (sensitiveWords 기본값·모양 enum — 03-03)
    - .planning/phases/03-navigation-input-condition/03-RESEARCH.md — "### Pattern 6: 민감칸 판별", "### Pitfall 4: 민감칸 판별이 값 입력 전(빈 문자열)에 오탐/누락" 두 절과 "## Assumptions Log"의 A6 줄만(Grep으로 줄을 찾아 범위로 읽는다)
    - .planning/phases/03-navigation-input-condition/03-CONTEXT.md (D-25, D-26, D-27)
  </read_first>
  <behavior>
    - `type: 'password'`인 칸은 이름과 상관없이 민감
    - 라벨 "비밀번호 확인", 이름 "userPw", 라벨 "비번"은 민감(대소문자·띄어쓰기 무시 — "비 밀 번 호"도 민감)
    - 라벨 "인증번호", "OTP 번호", "보안카드 번호"는 민감
    - 라벨 "주민등록번호": 값 "900101-1234567"·"9001011234567"이면 민감, "모름"·빈 값이면 민감 아님
    - 라벨 "계좌번호": 값 "123-456-789012"면 민감, "국민은행"이면 아님. 라벨 "지급계좌 선택" + "국민은행"은 아님
    - 라벨 "카드번호": "4111 1111 1111 1111"(Luhn 맞음)이면 민감, "4111 1111 1111 1112"(Luhn 틀림)면 아님
    - 라벨 "법인카드 구분": "개인"이면 아님, "4111111111111111"이면 민감
    - 늘 민감 단어가 모양 단어보다 앞선다: 라벨 "카드 비밀번호" + 빈 값은 민감
    - 이용자 설정 `override: true`면 "적요" 칸도 민감, `override: false`면 비밀번호 칸도 민감 아님(D-27 — 풀기 확인은 03-14)
    - 단어 목록을 인자로 받는다: `always`에 "사번"을 더하면 라벨 "사번"이 민감, `always`에서 "OTP"를 빼면 라벨 "OTP"만 있는 칸은 민감 아님
    - 모양 함수: `looksLikeAccount`는 9자리·17자리·글자 섞임을 거절, `looksLikeRrn`은 하이픈 자리가 6번째 뒤일 때만, 앞뒤 공백은 무시
    - `luhnValid('4111111111111111')` true, `luhnValid('4111111111111112')` false
  </behavior>
  <action>
RED 먼저: behavior 열둘을 `tests/unit/sensitive.test.ts`에 쓰고 실패 확인 후 RED 커밋.

`src/core/sensitive.ts`(D-25, D-26, 순수 — DOM·전역 창·확장 API를 쓰지 않음): `FieldDescriptor = { type?; name?; id?; labelText?; aria?; placeholder? }`, `SensitiveWords`(03-03의 `sensitiveWords` 모양과 같게 settings-schema에서 타입을 가져온다), `isSensitive(field, value, opts: { words: SensitiveWords; override?: boolean | null }): boolean`. 순서: `override`가 true/false면 그대로(D-27) → `type === 'password'` → 이름·id·라벨·aria·placeholder를 이어 붙여 `danger.ts`와 같은 정규화(띄어쓰기 제거, 소문자) 뒤 `always` 단어가 들어 있으면 true → `shape` 단어가 들어 있으면 값이 그 모양일 때만 true. 모양(Claude's Discretion 5): `looksLikeRrn`(숫자 6 + 하이픈 선택 + 숫자 7), `looksLikeAccount`(숫자·하이픈·공백만, 숫자 10~16자리), `looksLikeCard`(숫자·하이픈·공백만, 숫자 14~16자리 + `luhnValid`). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/sensitive.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 12 tests passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/sensitive.ts`에 `export function isSensitive`, `luhnValid`, `looksLikeRrn`, `looksLikeAccount`, `looksLikeCard`가 있다
    - `src/core/sensitive.ts`에 `document`·`window`·`chrome.` 참조가 없다
    - `pnpm exec vitest run tests/unit/sensitive.test.ts` 12개 이상 통과
  </acceptance_criteria>
  <done>민감칸 판별 규칙이 한 함수에 있고 단위 시험으로 고정되었다. RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 칸 이름과 최근 값 목록 (순수 함수)</name>
  <files>src/core/field-key.ts, src/core/recent-values.ts, tests/unit/field-key.test.ts, tests/unit/recent-values.test.ts</files>
  <read_first>
    - src/core/settings-schema.ts (RecentValuesV1, recentKey — 03-03)
    - src/core/fingerprint.ts (식별 묶음 필드 이름 — 같은 낱말을 쓴다)
    - .planning/phases/03-navigation-input-condition/03-CONTEXT.md (D-17 "사이트 + 칸 이름", Claude's Discretion 4)
  </read_first>
  <behavior>
    - `fieldKeyOf({ name: 'memo', id: 'm1' })` = `'name:memo'`, 이름이 없으면 `'id:amount'`, 둘 다 없으면 `'label:적요'`, 그다음 `'aria:…'`, `'placeholder:…'`
    - 아무것도 없으면 null
    - 띄어쓰기를 하나로 줄이고 앞뒤를 자르며 100자에서 자른다
    - `addRecentValue(빈 목록, 'name:memo', '거래처 미팅')` → 칸 하나, 값 하나
    - 같은 칸에 넷을 넣으면 새것부터 3개만 남는다
    - 이미 있는 값을 다시 넣으면 맨 앞으로 옮기고 개수는 그대로
    - 빈 값·공백뿐인 값·500자 넘는 값은 넣지 않는다(목록 그대로)
    - 칸이 300개일 때 새 칸을 넣으면 가장 오래 쓰지 않은 칸(목록 끝)이 빠지고, 값을 넣은 칸은 목록 맨 앞으로 온다
    - 입력 목록 객체는 바뀌지 않는다
  </behavior>
  <action>
RED 먼저: behavior 아홉을 두 시험 파일에 나눠 쓰고 실패 확인 후 RED 커밋.

`src/core/field-key.ts`(D-17, 순수): `fieldKeyOf({ name?, id?, labelText?, aria?, placeholder? }): string | null` — 앞선 것부터 처음 비어 있지 않은 값에 접두어(`name:`·`id:`·`label:`·`aria:`·`placeholder:`)를 붙인다. `src/core/recent-values.ts`(Claude's Discretion 4, D-35의 local 형식, 순수): `addRecentValue(data: RecentValuesV1['data'], fieldKey, value): RecentValuesV1['data']` — 칸당 3개, 사이트당 칸 300개, 값 1~500자, 새 배열을 돌려준다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/field-key.test.ts tests/unit/recent-values.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 9 tests passed</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/field-key.ts`에 `export function fieldKeyOf`, `src/core/recent-values.ts`에 `export function addRecentValue`가 있다
    - 두 파일에 `document`·`window`·`chrome.` 참조가 없다
    - `pnpm exec vitest run tests/unit/field-key.test.ts tests/unit/recent-values.test.ts` 9개 이상 통과
  </acceptance_criteria>
  <done>칸 구분 키와 최근 값 규칙이 단위 시험으로 고정되었다. RED·GREEN 커밋이 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- PRIV-01·INPT-01: edge probe `unclassified — review manually`. 가정한 경계:
  - 모양 규칙(Claude's Discretion 5): 주민번호 = 숫자 6 + 하이픈 선택 + 숫자 7. 계좌번호 = 숫자·하이픈·공백만, 숫자 10~16자리. 카드번호 = 숫자 14~16자리 + Luhn. 모양 단어가 없는 칸("비고")에 번호 모양 값이 들어가도 민감으로 보지 않는다(설계 8장 문장 그대로).
  - 단어는 이름·id·라벨·aria·placeholder를 이어 붙인 글자에서 찾는다(띄어쓰기·대소문자 무시). 영어 이름 "passwd"처럼 목록 단어가 들어 있지 않은 칸은 `type=password`가 아니면 잡히지 않는다 — 이용자가 설정에서 단어를 더한다(PRIV-03, 03-20).
  - 칸 이름은 `name` → `id` → 라벨 → aria → placeholder 순서다. 칸 이름이 없는 칸(`fieldKey` null)은 기록 대상이 아니다(03-11이 따른다).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사이트 칸 설명·값 → `isSensitive` | 사이트가 정한 이름·라벨과 이용자가 친 값(개인정보 가능)이 판별 함수에 들어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-28 | Information Disclosure | 민감칸 판별 규칙 | high | mitigate | 판별을 순수 함수 하나(`isSensitive`)로 모아 호출처마다 다른 판정이 생기지 않게 하고, 늘 민감 단어·모양 단어·Luhn·이용자 설정 순서를 단위 시험 12로 고정한다. 기록 쪽의 "벗어나는 순간 다시 판정"은 03-11 |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/sensitive.test.ts` 12개 이상 통과
- `pnpm exec vitest run tests/unit/field-key.test.ts tests/unit/recent-values.test.ts` 9개 이상 통과
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 비밀번호·인증번호 칸과 번호 모양 값이 든 주민·계좌·카드 칸을 한 함수가 민감으로 판정한다(PRIV-01 규칙)
- 사이트 + 칸 이름별 최근 값 규칙이 고정되었다(INPT-01 바탕)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/core/sensitive.ts`, `src/core/field-key.ts`, `src/core/recent-values.ts`, `tests/unit/sensitive.test.ts`, `tests/unit/field-key.test.ts`, `tests/unit/recent-values.test.ts`
- 심볼: `isSensitive()`, `FieldDescriptor`, `looksLikeRrn()`, `looksLikeAccount()`, `looksLikeCard()`, `luhnValid()`, `fieldKeyOf()`, `addRecentValue()`

<output>
Create `.planning/phases/03-navigation-input-condition/03-10-SUMMARY.md` when done
</output>
