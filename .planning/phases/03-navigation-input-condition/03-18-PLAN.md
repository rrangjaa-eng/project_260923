---
phase: 03-navigation-input-condition
plan: 18
type: execute
wave: 5
depends_on: [03-17]
files_modified:
  - src/entrypoints/options/keymap-section.ts
  - src/entrypoints/options/main.ts
  - src/shared/messages.ts
  - src/worker/storage-writer.ts
  - src/entrypoints/background.ts
  - tests/e2e/options.e2e.ts
autonomous: true
requirements: [PERS-03]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "'키 배치' 절에서 줄을 고르고 새 키를 누르면 그 동작의 키가 바뀌고, 사이트의 도우미가 곧바로 새 키를 따른다"
    - "같은 키를 두 동작에 주거나 Tab·보조키 단독을 주면 저장되지 않고 칸 아래 '!'와 '원인. 다음 행동.' 한 문장이 뜬다"
    - "설정 화면 전용 저장 요청(키 배치 등)은 확장 페이지에서 온 것만 받고, 사이트 쪽(content)에서 보내면 거절된다"
  artifacts:
    - path: "src/entrypoints/options/keymap-section.ts"
      provides: "키 배치 절(다음 키 받기, 충돌 검사, 기본값으로)"
      exports: ["keymapSection"]
    - path: "tests/e2e/options.e2e.ts"
      provides: "키 배치 절 시험(03-17 shell에 더함)"
  key_links:
    - from: "src/entrypoints/options/keymap-section.ts"
      to: "src/worker/storage-writer.ts"
      via: "KeymapSchema.safeParse 통과 → 저장 요청 setKeymap(확장 페이지에서 온 요청만 받음)"
      pattern: "setKeymap"
    - from: "src/entrypoints/background.ts"
      to: "src/worker/storage-writer.ts"
      via: "isExtensionPage(sender)가 참일 때만 설정 화면 전용 op를 writer로 넘긴다"
      pattern: "isExtensionPage"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
03-17의 설정 화면 틀에 "키 배치" 절(D-32)을 붙인다 — 절 → 키 받기 → `setKeymap` → SW(확장 페이지 확인) → writer → 사이트의 도우미가 새 키를 따름까지 한 작업으로 끝까지 잇는다.

Purpose: PERS-03. 설정 화면 전용 저장 요청의 문지기 `isExtensionPage(sender)`도 여기서 생기고 03-19·03-20·03-22가 같이 쓴다.
Output: `keymap-section.ts`, `setKeymap` 저장 요청, background `isExtensionPage()`, `options.e2e.ts`(keymap).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-03-SUMMARY.md
@.planning/phases/03-navigation-input-condition/03-17-SUMMARY.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성·새 권한 없음.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시, `model` 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(03-18): …` → 최소 구현 → GREEN 커밋 `feat(03-18): …`. 본문은 한국어.
- 완료 판정은 `CI=true` e2e. 저장 쓰기는 writer에만(D-03). 새 메시지는 `sender.id` 확인 + zod.
- 화면은 SYSTEM.md와 tokens.css만(D-36), 카피 규칙(D-37).
- `03-RESEARCH.md`(623줄)·`03-PATTERNS.md`는 통째로 읽지 않는다 — 필요하면 해당 절만 Grep으로 줄을 찾아 범위로 읽는다(CLAUDE.md '500줄 이상은 range 필수'. 계획 하나의 문맥 예산을 지키려고 `<context>`에서 뺐다).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: "키 배치" 절 — 다음 키 받기, 충돌·금지 키 거절, 기본값으로</name>
  <files>src/entrypoints/options/keymap-section.ts, src/entrypoints/options/main.ts, src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/background.ts, tests/e2e/options.e2e.ts</files>
  <read_first>
    - src/core/settings-schema.ts (KeymapSchema superRefine, defaultKeymap — 03-03)
    - src/core/keys.ts (keyLabel)
    - src/entrypoints/options/ui.ts, src/entrypoints/options/main.ts (03-17 결과)
    - src/worker/storage-writer.ts, src/entrypoints/background.ts (op 분기, sender 확인)
  </read_first>
  <behavior>
    - e2e `keymap`: 1 → 줄 "1 누르기 · 스페이스바" … "9 틀 실행 느리게 · ←", "0 다음" → 0 → "1 틀 실행 빠르게 · →", "2 번호 1 · 1" … → 0 → 나머지 번호 줄과 "기본값으로"
    - e2e `keymap`: 첫 장에서 1 → "바꿀 키를 누르세요 · Esc 취소" → J → `settings.data.keymap.press === 'KeyJ'`, 줄 값이 "J", 토스트 "저장했어요". 같은 시험에서 `targets.html` 탭의 버튼을 잡고 J → 카운터 +1
    - e2e `keymap`: 1 → Enter → 칸 아래 "! 이미 ‘확인’에 쓰는 키예요. 다른 키를 누르세요.", 설정은 그대로
    - e2e `keymap`: 1 → Tab → "! Tab은 쓸 수 없어요. 다른 키를 누르세요."
    - e2e `keymap`: "8 카드 보조키 · Alt" → 8 → 값이 "Shift"로 바뀌어 저장된다(다시 누르면 Alt)
    - e2e `keymap`: "기본값으로" → 확인 화면 없이 `keymap`이 `defaultKeymap()`과 같아지고 토스트 "기본값으로 돌렸어요"
  </behavior>
  <action>
RED 먼저: behavior 여섯을 `options.e2e.ts`(`describe('keymap')`)에 더하고 실패 확인 후 RED 커밋.

`messages.ts`: `storage/request` op `setKeymap { keymap }`(`KeymapSchema`로 검사). `background.ts`(보안): `setKeymap`과 이후 설정 화면 전용 op는 **확장 페이지에서 온 요청만** 받는다 — `sender.url`이 `chrome.runtime.getURL('/')`로 시작할 때만(`isExtensionPage(sender)` 한 함수로, 03-19·03-20·03-22가 같이 쓴다). content에서 오면 거절. `storage-writer.ts`(D-03): `settings`를 읽어 keymap만 바꿔 `SettingsV2`로 다시 검사 후 쓴다.

`src/entrypoints/options/keymap-section.ts`(D-32, PERS-03): 줄 = 누르기·확인·취소·번호표·명령판·위로 스크롤·아래로 스크롤·카드 보조키·틀 실행 느리게·틀 실행 빠르게·번호 1~9·기본값으로. 동작 줄을 고르면 "바꿀 키를 누르세요 · Esc 취소" 상태로 다음 진짜 keydown(떨림 걸러내기 뒤, 파이프라인 `onKey`)의 `code`를 받아 새 keymap을 만들고 `KeymapSchema.safeParse` — 실패 이유를 한 문장으로(D-37): 중복이면 "이미 ‘{다른 동작}’에 쓰는 키예요. 다른 키를 누르세요.", Tab이면 "Tab은 쓸 수 없어요. 다른 키를 누르세요.", 보조키 단독·숫자 패드도 같은 꼴. 받는 중 Esc는 취소(Esc를 다른 동작에 주려면 "기본값으로"를 쓴다 — 가정 절). 카드 보조키 줄은 Alt ↔ Shift 바꾸기(D-17). `options/main.ts`에서 03-17의 "1 키 배치" 빈 절 본문을 `keymapSection`으로 바꾼다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/options.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "9 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/entrypoints/background.ts`에 `isExtensionPage`와 `getURL(`이 있고 `setKeymap`이 그 확인 뒤에만 처리된다
    - `src/entrypoints/options/keymap-section.ts`에 `KeymapSchema`와 `바꿀 키를 누르세요`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/options.e2e.ts` 9 passed(03-17 shell 3 + 이 작업 6)
  </acceptance_criteria>
  <done>설정 화면에서 도우미 키 배치를 바꾸고, 잘못된 배치는 이유와 함께 거절된다. RED·GREEN 커밋이 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- PERS-03: edge probe `unclassified — review manually`. 가정한 경계:
  - 키를 받는 중 Esc는 취소다. 그래서 다른 동작에 Esc를 줄 수는 없고, 취소 키를 바꾼 뒤 Esc로 돌아오려면 "기본값으로"를 쓴다.
  - 막는 조합(Claude's Discretion 9): 같은 키 두 동작, Tab, 보조키 단독(Alt·Shift·Control·Meta), 숫자 패드 1~9. 검사는 zod(03-03 `KeymapSchema`).
  - 설정 화면 전용 저장 요청은 확장 페이지에서 온 것만 받는다(content가 보내면 거절).
  - "기본값으로"는 확인 화면 없이 돌린다(되돌리기 쉬운 동작 — 다시 바꿀 수 있다).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 확장 페이지(옵션) → SW | 전역 설정 바꾸기 요청 |
| content → SW | 같은 op를 흉내 낼 수 있는 쪽 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-45 | Elevation of Privilege | `setKeymap` 등 설정 화면 op | high | mitigate | `sender.id` 확인 + `isExtensionPage(sender)`(확장 URL로 시작) — content script 경로로는 키 배치를 바꿀 수 없다 |
| T-03-46 | Denial of Service | 조작할 수 없는 키 배치 | high | mitigate | `KeymapSchema` superRefine(중복·Tab·보조키 단독·숫자 패드 거절)을 화면과 writer 둘 다에서 |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/options.e2e.ts` 9 passed
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 이용자가 설정 화면에서 도우미 키 배치를 바꾼다(PERS-03)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/entrypoints/options/keymap-section.ts`
- 심볼: `keymapSection`, background `isExtensionPage()`, 저장 요청 `setKeymap`

<output>
Create `.planning/phases/03-navigation-input-condition/03-18-SUMMARY.md` when done
</output>
