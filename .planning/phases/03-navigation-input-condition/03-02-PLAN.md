---
phase: 03-navigation-input-condition
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/core/palette-slots.ts
  - tests/unit/palette-slots.test.ts
  - src/page/palette/palette-controller.ts
  - src/page/overlay/palette.ts
  - src/page/overlay/mode-indicator.ts
  - src/entrypoints/background.ts
  - tests/e2e/palette.e2e.ts
autonomous: true
requirements: [NAV-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "카드 자리는 D-06 그대로다 — 첫 장 1 뒤로 · 2 작업판 · 3 틀 실행 · 4 조작 방식 바꾸기 · 5 입력 되돌리기 · 6 컨디션 모드 · 7 새로고침 · 8 도우미 끄기 · 9 다음 장, 둘째 장 1 앞으로 · 2 탭 닫기 · 3 새 탭 · 4 틀 기록 · 5 번호 고정 · 6 양식 한 장 보기 · 7 화면 정리(AI) · 8 더블클릭 · 9 다음 장, 셋째 장 1 끌어서 놓기 · 2 민감칸 설정 · 3 기록 내보내기 · 4 맞춤 설정 다시 하기 · 9 첫 장"
    - "첫 화면 키 안내 카드에 스페이스바·1~9·0·↑↓·Esc와 '브라우저 알림 창은 Enter=확인, Esc=취소'가 있다"
    - "명령판에서 키 하나로 새로고침(첫 장 7)·뒤로(첫 장 1)·앞으로(둘째 장 1)·탭 닫기(둘째 장 2)·새 탭(둘째 장 3)이 된다. 뒤로 갈 페이지가 없으면 명령판에 이유와 다음 행동이 한 문장으로 뜬다"
    - "Esc는 둘째·셋째 장에서 이전 장으로, 첫 장에서 명령판을 닫는다. 입력칸 안에서 0은 글자로 들어간다"
    - "다음 단계(Phase 4~6) 기능 카드(작업판·틀 실행·틀 기록·화면 정리(AI)·기록 내보내기)는 자리를 지킨 채 '준비 중'으로 흐리게 보이고, 고르면 명령판이 열린 채 '아직 준비 중이에요. 다음 업데이트에서 쓸 수 있어요.'가 뜬다"
    - "모드 표시가 '도우미 · 스페이스바 누르기, 0 명령판'을 보여 준다"
  prohibitions:
    - statement: "명령판 카드 자리는 쓰는 횟수·최근 사용으로 저절로 바뀌지 않는다 — 기본 자리(D-06) 또는 이용자가 설정에서 바꾼 자리만 쓴다"
      requirement_id: NAV-02
      category: consistency
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/core/palette-slots.ts"
      provides: "다음 단계 명령 목록과 세 장 계산(03-01 위에 더함)"
      exports: ["LATER_PHASE_COMMANDS", "chapterCards"]
    - path: "tests/unit/palette-slots.test.ts"
      provides: "D-06 자리·장별 카드·다음 단계 명령 단위 시험"
    - path: "src/page/palette/palette-controller.ts"
      provides: "장 이동, 하위 판(pushPanel/popPanel), 준비 중 카드, 탭 명령 다섯"
      exports: ["createPaletteController"]
    - path: "tests/e2e/palette.e2e.ts"
      provides: "명령판 열기·장 넘기기·탭 명령·Esc·준비 중 카드·가짜 키 시험"
  key_links:
    - from: "src/page/palette/palette-controller.ts"
      to: "src/core/palette-slots.ts"
      via: "등록되지 않은 명령과 LATER_PHASE_COMMANDS를 muted '준비 중' 카드로 그린다"
      pattern: "LATER_PHASE_COMMANDS"
    - from: "src/page/palette/palette-controller.ts"
      to: "src/entrypoints/background.ts"
      via: "back/forward 거절 → { ok: false, reason: 'no-history' } → 명령판 상태 글"
      pattern: "no-history"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
03-01이 증명한 0 → 명령판 → 7 한 경로(tracer) 위에, 세 장의 고정 카드 자리·나머지 탭 명령 넷·키 안내 카드·"준비 중" 카드·하위 판 틀·모드 표시 문구를 넓힌다(expansion — 새 층을 더하지 않는다).

Purpose: NAV-02(D-05, D-06, D-07). 이후 계획이 붙이는 `pushPanel`/`popPanel`과 `registerCommand` 흐림 풀림이 여기서 생긴다.
Output: `palette-slots.ts` 확장+단위 시험, 컨트롤러 장 이동·하위 판·준비 중 카드, 탭 명령 SW 처리 넷, 키 안내 카드, 모드 표시 문구, `palette.e2e.ts` 확장.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-01-SUMMARY.md
@docs/design/SYSTEM.md
@docs/design/tokens.css
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. Phase 3은 새 의존성도 새 확장 권한도 없다(03-RESEARCH "Standard Stack"). 필요해지면 멈추고 이유 한 줄과 함께 승인을 요청한다(`checkpoint:decision`).
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트에 맡길 때도 프롬프트에 스킬을 적고 `model`을 명시한다).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(03-02): …` → 최소 구현 → GREEN 커밋 `feat(03-02): …`. 커밋 본문은 한국어.
- 완료 판정은 `CI=true` e2e(프로덕션 빌드). 로컬 dev 통과는 완료 신호가 아니다.
- 오버레이 스타일은 `docs/design/tokens.css` 변수만(`?inline`). 새 색·서체·radius·그림자 금지(D-36). 카피는 SYSTEM.md 카피 규칙(D-37).
- 저장 쓰기는 `src/worker/storage-writer.ts`에만(D-03). 새 메시지는 SW에서 `sender.id === chrome.runtime.id` 확인 + `src/shared/messages.ts`의 zod 구별 합집합으로 검사한다.
- 입력은 `isTrusted`만, 떨림 걸러내기가 먼저(D-02).
- 로컬 연습 사이트에서만 시험한다. 실제 회사 시스템 접속·시험 금지(D-39, Phase 1 D-14). 웹 확인이 필요하면 `/browse`만.
- `03-RESEARCH.md`(623줄)·`03-PATTERNS.md`는 통째로 읽지 않는다 — `<read_first>`가 가리킨 절만 Grep으로 줄을 찾아 범위로 읽는다(CLAUDE.md '500줄 이상은 range 필수'. 계획 하나의 문맥 예산을 지키려고 `<context>`에서 뺐다).
- `.planning/` 수동 편집 금지, `git push --force` 금지.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 세 장의 고정 카드 자리·탭 명령·키 안내 카드·준비 중 카드·모드 표시 문구</name>
  <files>src/core/palette-slots.ts, tests/unit/palette-slots.test.ts, src/page/palette/palette-controller.ts, src/page/overlay/palette.ts, src/page/overlay/mode-indicator.ts, src/entrypoints/background.ts, tests/e2e/palette.e2e.ts</files>
  <read_first>
    - src/core/palette-slots.ts, src/page/palette/palette-controller.ts, src/page/overlay/palette.ts (03-01 Task 2 결과)
    - src/page/overlay/mode-indicator.ts (setMode, setHint, 도우미/입력 중 문구)
    - docs/design/SYSTEM.md ("명령판·작업판" 템플릿, "평소 화면"의 모드 표시 문구, "카피 규칙")
    - .planning/phases/03-navigation-input-condition/03-CONTEXT.md (D-05, D-06, D-07, Claude's Discretion 첫 항목)
  </read_first>
  <behavior>
    - 단위: `chapterCards(DEFAULT_PALETTE_SLOTS, 1)`의 1~9 이름이 뒤로·작업판·틀 실행·조작 방식 바꾸기·입력 되돌리기·컨디션 모드·새로고침·도우미 끄기·다음 장
    - 단위: 둘째 장 1~9 = 앞으로·탭 닫기·새 탭·틀 기록·번호 고정·양식 한 장 보기·화면 정리(AI)·더블클릭·다음 장
    - 단위: 셋째 장 1~4 = 끌어서 놓기·민감칸 설정·기록 내보내기·맞춤 설정 다시 하기, 5~8 빈 자리, 9 = 첫 장
    - 단위: `DEFAULT_PALETTE_SLOTS`에 모든 명령이 정확히 한 번, (장, 번호)가 겹치지 않는다
    - 단위: `LATER_PHASE_COMMANDS`는 `board`, `template-run`, `template-record`, `ai-cleanup`, `export-log` 다섯
    - e2e: 첫 장에 `.palette-key-guide`가 있고 "스페이스바", "1~9", "0", "↑↓", "Esc", "브라우저 알림 창은 Enter=확인, Esc=취소" 글자가 모두 있다
    - e2e: 9 → 둘째 장(카드 1 "앞으로"), 9 → 셋째 장(카드 9 "첫 장"), 9 → 첫 장. 둘째 장에서 Esc → 첫 장, 첫 장에서 Esc → 명령판이 닫힌다
    - e2e: `targets.html`에서 `#nav-link`로 `next.html`로 간 뒤 0 → 1 → `targets.html`로 돌아오고, 0 → 9 → 1 → 다시 `next.html`로 간다
    - e2e: 0 → 9 → 3 → 탭이 하나 늘고, 0 → 9 → 2 → 지금 탭이 닫힌다(시험은 연습 사이트 탭 두 개로 한다)
    - e2e: 새 탭에서 연 첫 페이지(뒤로 갈 곳 없음)에서 0 → 1 → 명령판이 열린 채 `.palette-status`에 "뒤로 갈 페이지가 없어요. Esc로 닫으세요."
    - e2e: 0 → 2(작업판) → 명령판이 열린 채 `.palette-status`에 "아직 준비 중이에요. 다음 업데이트에서 쓸 수 있어요.", 그 카드에 `muted` 표시와 "준비 중" 글자
    - e2e: 입력칸(`input.html`의 `#name`)에 초점을 두고 0 → 칸 값에 "0"이 들어가고 `.palette`가 없다
    - e2e: 모드 표시 `.mode-indicator`에 "도우미"와 "스페이스바 누르기, 0 명령판"이 있다
    - e2e: 페이지가 만든 가짜 `KeyboardEvent('keydown', { code: 'Digit0' })`를 보내도 `.palette`가 뜨지 않는다
  </behavior>
  <action>
RED 먼저: 단위 behavior 다섯을 `tests/unit/palette-slots.test.ts`에, e2e 아홉을 `palette.e2e.ts`(`test.describe('palette')`)에 쓰고 실패 확인 후 RED 커밋.

`palette-slots.ts`(D-06): `LATER_PHASE_COMMANDS`(`board`, `template-run`, `template-record`, `ai-cleanup`, `export-log`)를 export한다. 03-01이 만든 `DEFAULT_PALETTE_SLOTS`·`chapterCards`의 자리는 바꾸지 않는다 — 단위 시험이 D-06과 다르다고 하면 D-06 쪽으로 고친다.

`palette-controller.ts`(D-05, D-06): 장 이동 — 1·2장의 9는 다음 장, 3장의 9는 첫 장, 빈 자리 번호는 무시. Esc는 하위 판이 열려 있으면 그 판을 닫고(이후 계획이 `pushPanel(panel)`/`popPanel()`로 쓴다 — 이 작업에서 두 함수를 만든다: `panel = { title, cards, onSelect(n) }`), 2·3장이면 이전 장, 첫 장이면 닫기(닫을 때 `setModal(null)`). `registerCommand`가 없는 명령과 `LATER_PHASE_COMMANDS`는 카드를 `muted` + note "준비 중"으로 그리고, 고르면 닫지 않고 `setStatus("아직 준비 중이에요. 다음 업데이트에서 쓸 수 있어요.")`(Claude's Discretion: 자리는 비우거나 옮기지 않는다). 이 계획 뒤의 Phase 3 계획이 자기 명령을 `registerCommand`로 붙이면 흐림이 저절로 풀린다. 이 작업에서 `back`, `forward`, `close-tab`, `new-tab`을 더 등록한다(`reload`는 03-01): `tab/command`를 보내고 성공하면 닫고, `back`/`forward`가 `{ ok: false, reason: 'no-history' }`이면 열린 채 "뒤로 갈 페이지가 없어요. Esc로 닫으세요."(앞으로면 "앞으로 갈 페이지가 없어요. Esc로 닫으세요.").

`palette.ts`(D-07): 첫 장일 때만 카드 격자 위에 `.palette-key-guide` 카드 — "스페이스바 누르기 · 1~9 고르기 · 0 명령판 · ↑↓ 스크롤 · Esc 취소·뒤로", 둘째 줄 "브라우저 알림 창은 Enter=확인, Esc=취소". 키 이름 글자는 03-03이 키 배치에서 만들도록 `view.keyGuide`를 문자열 두 줄로 받게 바꾼다. 상태 글 `.palette-status`는 `--text-body`, 오류는 `--danger` 글자 + "!".

`background.ts`: 03-01의 `tab/command` 처리(같은 sender·frameId 검사)에 `back` → `chrome.tabs.goBack(tabId)`, `forward` → `goForward`, `close-tab` → `remove`, `new-tab` → `create({ active: true })`를 더한다. goBack/goForward가 거절되면 `{ ok: false, reason: 'no-history' }`.

`mode-indicator.ts`(CONTEXT specifics): 도우미 모드 문구를 "도우미" + 안내 "스페이스바 누르기, 0 명령판"으로(입력 중 문구는 그대로). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/palette-slots.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 5 tests passed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/palette.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "11 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/palette-slots.ts`에 `DEFAULT_PALETTE_SLOTS`, `LATER_PHASE_COMMANDS`, `chapterCards`가 export된다
    - `src/entrypoints/background.ts`에 `tabs.goBack(`, `tabs.goForward(`, `tabs.remove(`, `tabs.create(`가 있다
    - `src/page/palette/palette-controller.ts`에 `pushPanel`과 `popPanel`이 있다
    - `src/page/overlay/palette.ts`에 `브라우저 알림 창은 Enter=확인, Esc=취소`가 있다
    - `src/page/overlay/palette.ts`에 `box-shadow`와 hex 색 리터럴이 없다
    - `pnpm exec vitest run tests/unit/palette-slots.test.ts` 5개 이상 통과, `CI=true pnpm exec playwright test tests/e2e/palette.e2e.ts` 11 passed(03-01 tracer 2 + 이 작업 9)
  </acceptance_criteria>
  <done>세 장의 고정 카드 자리, 탭 명령 다섯, 키 안내 카드, 준비 중 카드, 하위 판 틀, 모드 표시 안내가 동작한다. RED·GREEN 커밋이 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- NAV-02: edge probe `unclassified — review manually`. 가정한 경계(방어 가능한 것은 truths로 올림):
  - "준비 중" 카드(Phase 4~6 기능)는 흐리게 보이고 고르면 명령판을 닫지 않고 상태 글만 보여 준다. 자리는 비우거나 옮기지 않는다(Claude's Discretion 1).
  - 셋째 장의 5~8은 빈 자리(번호 없음 카드, 눌러도 무시), 9는 "첫 장"으로 돌아간다(D-06은 셋째 장 9를 정하지 않았다).
  - "탭 닫기"는 확인 없이 닫는다(설계에 확인 요구가 없다. 명령판에서 0 → 9 → 2 세 번을 차례로 눌러야 한다).
  - "새 탭"은 브라우저 새 탭 페이지를 연다(도우미가 동작하지 않는 페이지 — 01-13 "도울 수 없음" 표시).
  - 모드 표시 문구를 바꾼다. 01-16 DOM 감사가 옛 문구 "도우미"만을 정확히 확인했다면 그 시험도 같이 맞춘다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content script(맨 위 프레임) → service worker | `tab/command`로 탭을 이동·닫기·열기 |
| 사이트 페이지 → content script | 사이트가 만든 가짜 키 이벤트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | `tab/command`의 back·forward·close-tab·new-tab(background.ts) | high | mitigate | 03-01과 같은 `sender.id === chrome.runtime.id` + zod 검사 + `sender.frameId === 0`을 새 명령 넷에도 쓰고, 닫을 탭 id는 요청이 아니라 `sender.tab.id`에서만 쓴다(다른 탭을 닫게 할 수 없음) |
| T-03-02 | Tampering | 명령판 키 입력 | high | mitigate | 파이프라인이 `isTrusted`만 받으므로 사이트가 만든 가짜 키로 명령판을 열거나 명령을 고를 수 없다(e2e: 가짜 `KeyboardEvent`로 0을 보내도 `.palette` 없음 — Task 1 시험) |
| T-03-04 | Information Disclosure | 명령판 화면·상태 글 | low | accept | 명령판은 고정 카드 이름과 고정 안내 글만 보여 주고 페이지 내용을 담지 않는다 |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/palette-slots.test.ts` 5개 이상 통과
- `CI=true pnpm exec playwright test tests/e2e/palette.e2e.ts` 11 passed
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 0을 누르면 큰 번호 카드 명령판과 키 안내 카드가 뜨고, 뒤로·앞으로·새로고침·탭 닫기·새 탭이 키로 된다(NAV-02)
- 카드 자리가 D-06 그대로 고정이다
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/unit/palette-slots.test.ts` (그리고 03-01 파일 확장)
- 심볼: `LATER_PHASE_COMMANDS`, `createPaletteController()`의 `pushPanel`, `popPanel`
- CSS 클래스(시험이 쓰는 것): `.palette-key-guide`, `.palette-status`

<output>
Create `.planning/phases/03-navigation-input-condition/03-02-SUMMARY.md` when done
</output>
