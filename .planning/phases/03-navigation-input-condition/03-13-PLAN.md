---
phase: 03-navigation-input-condition
plan: 13
type: execute
wave: 12
depends_on: [03-12]
files_modified:
  - src/shared/messages.ts
  - src/worker/storage-writer.ts
  - src/entrypoints/background.ts
  - src/entrypoints/content.ts
  - tests/e2e/input-cards.e2e.ts
autonomous: true
requirements: [INPT-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "최근 값·문구가 없는 칸에 뜨는 '자주 쓰는 문구 추가' 카드로, 칸에 적은 글을 이 칸의 자주 쓰는 문구로 저장한다(계정 동기화)"
    - "저장한 문구는 다음에 그 칸에 들어가면 최근 값 뒤에 '문구' 카드로 보이고 Alt+번호로 넣을 수 있다"
    - "iframe 안 칸에서 저장한 문구도 맨 위 사이트의 사이트 항목에 들어간다"
  artifacts:
    - path: "tests/e2e/input-cards.e2e.ts"
      provides: "문구 저장(phrase) 시험 — 03-12의 show·pick에 더함"
  key_links:
    - from: "src/entrypoints/content.ts"
      to: "src/worker/storage-writer.ts"
      via: "저장 요청 savePhrase{fieldKey, text} → site:<맨 위 출처>.phrases"
      pattern: "savePhrase"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
03-12가 띄운 "자주 쓰는 문구 추가" 카드를 고르면 칸에 적은 글을 이 칸의 자주 쓰는 문구로 저장한다(D-19). 카드 → content → SW → writer → `storage.sync` 사이트 항목까지 한 작업으로 끝까지 잇는다.

Purpose: INPT-02(저장). 문구 고치기·빼기는 설정 화면(03-19).
Output: `savePhrase` 저장 요청과 writer 처리, content의 추가 카드 선택, `input-cards.e2e.ts`의 phrase 시험.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-03-SUMMARY.md
@.planning/phases/03-navigation-input-condition/03-12-SUMMARY.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성·새 권한 없음.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시, `model` 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(03-13): …` → 최소 구현 → GREEN 커밋 `feat(03-13): …`. 본문은 한국어.
- 완료 판정은 `CI=true` e2e. 저장 쓰기는 writer에만(D-03). 문구는 sync(D-04).
- 카피 규칙(D-37). 연습 사이트에서만 시험(D-39).
- `03-RESEARCH.md`(623줄)·`03-PATTERNS.md`는 통째로 읽지 않는다 — 필요하면 해당 절만 Grep으로 줄을 찾아 범위로 읽는다(CLAUDE.md '500줄 이상은 range 필수'. 계획 하나의 문맥 예산을 지키려고 `<context>`에서 뺐다).

## 전제 (아직 실행 전인 Phase 1 계획에서 온 가정)

- [01-13 가정] SW는 content 요청의 출처를 `sender.tab.url`에서 계산한다.
- [01-14 가정] 사이트 항목 쓰기 전 `syncItemBytes` 한도 검사(`item-too-large`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: "자주 쓰는 문구 추가" 카드로 이 칸의 문구 저장</name>
  <files>src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/background.ts, src/entrypoints/content.ts, tests/e2e/input-cards.e2e.ts</files>
  <read_first>
    - src/core/settings-schema.ts (SiteEntryV2.phrases — 03-03)
    - src/worker/storage-writer.ts (setPin — 03-09, 같은 사이트 항목 쓰기)
    - src/entrypoints/content.ts (03-12 카드 처리 — `onTypingKey`에서 `add` 카드가 지금은 false를 돌려주는 곳)
    - src/page/overlay/input-cards.ts
  </read_first>
  <behavior>
    - e2e `phrase`: 기록 없는 칸(`input.html`의 메모 칸)에 "거래처 미팅 식대"를 적고 Alt+1(추가 카드) → `site:http://practice.test`의 phrases에 그 칸 이름과 글이 있고 카드 자리에 "자주 쓰는 문구로 저장했어요"가 2초
    - e2e `phrase`: 벗어났다 다시 들어가면 최근 값 뒤에 "문구" 카드로 그 글이 보이고 Alt+번호로 넣을 수 있다
    - e2e `phrase`: 칸이 비어 있을 때 추가 카드를 고르면 저장하지 않고 "칸에 문구를 적은 뒤 Alt+1을 누르세요"
    - e2e `phrase`: `frames.html`의 `#input-leaf`에서 저장한 문구는 맨 위 사이트(`site:http://practice.test`)에 들어간다
  </behavior>
  <action>
RED 먼저: behavior 넷을 `input-cards.e2e.ts`(`describe('phrase')`)에 더하고 실패 확인 후 RED 커밋.

`messages.ts`: `storage/request` op `savePhrase { fieldKey, text }`(글 1~500자, zod). `background.ts`: 출처는 `sender.tab.url`(맨 위 사이트). `storage-writer.ts`(D-03, D-19): 사이트 항목을 `migrate(siteSpec)`로 읽어 같은 칸·같은 글이 이미 있으면 그대로, 없으면 끝에 더하고 `SiteEntryV2`·크기 검사 뒤 쓴다. 한도를 넘으면 `item-too-large`.

`content.ts`(D-19): 추가 카드를 고르면 — 저장 직전에 `isSensitive(descriptor, 지금 값, …)`를 다시 확인해 민감칸이면 무시(추가 카드가 뜨지 않으므로 오지 않지만 다시 확인), 값이 비었으면 note "칸에 문구를 적은 뒤 {보조키}+{n}을 누르세요", 아니면 `savePhrase` → 성공 "자주 쓰는 문구로 저장했어요" 2초, `item-too-large`면 "이 사이트 저장 공간이 가득 찼어요. 설정 화면에서 문구를 정리하세요." 사이트 항목이 바뀌면 떠 있는 카드를 다시 그린다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/input-cards.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "17 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/shared/messages.ts`에 `savePhrase`, `src/worker/storage-writer.ts`에 `phrases`가 있다
    - `grep -rnE "storage\.(sync|local|session)\.set" src/ | grep -v "src/worker/storage-writer.ts" | grep -vE "^[^:]+:[0-9]+:\s*//"`가 아무것도 출력하지 않는다
    - `CI=true pnpm exec playwright test tests/e2e/input-cards.e2e.ts` 17 passed(03-12의 show 7 + pick 6 + 이 작업 4)
  </acceptance_criteria>
  <done>빈 상태 카드로 이 칸의 자주 쓰는 문구를 저장하고, 다음부터 카드로 고른다. RED·GREEN 커밋이 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- INPT-02: edge probe `unclassified — review manually`. 가정한 경계:
  - 문구는 칸 이름별이다(INPT-01 "그 칸(사이트 + 칸 이름)의 … 자주 쓰는 문구"). 문구 고치기·빼기는 설정 화면(03-19).
  - 같은 칸에 같은 글을 다시 저장하면 새로 더하지 않는다(목록 그대로).
  - 다른 PC에서 문구가 보이는지는 사람 확인(backstop, 03-26).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content → writer | `savePhrase`(sync에 남는 글) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-32 | Information Disclosure | 민감칸 문구 저장 | high | mitigate | 민감칸에는 추가 카드가 뜨지 않고(03-12), 저장 직전에 `isSensitive`를 다시 확인해 민감칸이면 저장하지 않는다 |
| T-03-33 | Tampering | `savePhrase` 출처·크기 | medium | mitigate | 출처는 `sender.tab.url`, 글 1~500자(zod), 사이트 항목 8KB 한도 검사 |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/input-cards.e2e.ts` 17 passed
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 자주 쓰는 문구를 칸에서 바로 저장하고 다음부터 카드로 고른다(INPT-02 저장, 고치기는 03-19)
</success_criteria>

## Artifacts this phase produces

- 저장 요청: `savePhrase`
- 시험: `tests/e2e/input-cards.e2e.ts`의 `describe('phrase')`

<output>
Create `.planning/phases/03-navigation-input-condition/03-13-SUMMARY.md` when done
</output>
