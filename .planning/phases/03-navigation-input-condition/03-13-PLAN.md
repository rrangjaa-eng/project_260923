---
phase: 03-navigation-input-condition
plan: 13
type: execute
wave: 13
depends_on: ["03-12"]
files_modified:
  - src/entrypoints/options/condition-section.ts
  - src/entrypoints/options/palette-section.ts
  - src/entrypoints/options/phrases-section.ts
  - src/entrypoints/options/danger-section.ts
  - src/entrypoints/options/sensitive-section.ts
  - src/entrypoints/options/site-section.ts
  - src/entrypoints/options/confirm.ts
  - src/entrypoints/options/main.ts
  - src/shared/messages.ts
  - src/worker/storage-writer.ts
  - src/entrypoints/background.ts
  - src/entrypoints/content.ts
  - tests/e2e/options-sections.e2e.ts
autonomous: true
requirements: [SAFE-06, INPT-02, PRIV-02, PRIV-03, PERS-01]
estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "설정 화면 '컨디션 모드'에서 좋은 날·힘든 날 묶음의 조작 방식·떨림 간격·머무르기 시간·강조 속도·표시 크기를 고치고, 범위를 벗어나면 이유와 함께 거절된다"
    - "설정 화면 '명령판 카드 자리'에서 두 명령의 자리를 바꾸면 명령판이 그 자리대로 보이고, 바꾼 자리도 쓰는 횟수로 움직이지 않는다. '기본 자리로'로 D-06 자리로 돌아간다"
    - "설정 화면 '자주 쓰는 문구'에서 사이트·칸별 문구를 고치고 뺄 수 있다"
    - "설정 화면 '위험한 버튼 단어'에서 전체 목록과 사이트별 목록에 단어를 더하고 뺄 수 있고, 사이트별 목록이 있으면 그 사이트는 그 목록으로 위험 버튼을 가린다"
    - "설정 화면 '민감칸 판별 단어'에서 늘 민감 단어와 번호 모양을 보는 단어를 더하고 뺄 수 있고, 바꾼 목록이 입력 줄이기·최근 값 기록에 곧바로 쓰인다"
    - "설정 화면 '사이트별 민감칸·고정 번호'에서 칸 설정과 고정 번호를 보고 지울 수 있고, 지워서 민감칸이 풀리게 되면 확인 화면(1초 보호)을 거친다"
  artifacts:
    - path: "src/entrypoints/options/condition-section.ts"
      provides: "컨디션 묶음 값 편집 절"
      exports: ["conditionSection"]
    - path: "src/entrypoints/options/palette-section.ts"
      provides: "명령판 카드 자리 바꾸기 절"
      exports: ["paletteSection"]
    - path: "src/entrypoints/options/danger-section.ts"
      provides: "위험 단어 전체·사이트별 편집 절"
      exports: ["dangerSection"]
    - path: "src/entrypoints/options/confirm.ts"
      provides: "설정 화면용 확인 화면(SYSTEM.md 확인 화면 템플릿, createConfirmGuard 보호)"
      exports: ["openOptionsConfirm"]
    - path: "tests/e2e/options-sections.e2e.ts"
      provides: "설정 화면 나머지 절 시험"
  key_links:
    - from: "src/entrypoints/content.ts"
      to: "src/core/settings-schema.ts"
      via: "위험 단어 = 사이트 항목 dangerWords가 있으면 그것, 없으면 전역 dangerWords (collector getDangerWords)"
      pattern: "dangerWords"
    - from: "src/entrypoints/content.ts"
      to: "src/page/palette/palette-controller.ts"
      via: "명령판 카드 자리를 settings.data.paletteSlots에서 받아 바뀌면 다시 그림"
      pattern: "paletteSlots"
    - from: "src/entrypoints/options/site-section.ts"
      to: "src/worker/storage-writer.ts"
      via: "updateSite{origin, patch} — 확장 페이지 요청만(isExtensionPage)"
      pattern: "updateSite"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
설정 화면(03-12 틀)에 나머지 절을 붙인다: 컨디션 묶음 값(D-29), 명령판 카드 자리(D-06 "설정에서 바꾼 자리도 고정"), 자주 쓰는 문구 고치기(D-19), 위험 단어 사이트별(D-16), 민감칸 판별 단어(D-28), 사이트별 민감칸·고정 번호 목록(D-27, D-10).

Purpose: SAFE-06, INPT-02(고치기), PRIV-02(목록에서 보기·지우기), PRIV-03, PERS-01(묶음 값 편집).
Output: 절 여섯, 설정 화면 확인 화면, 저장 요청(`updateCondition`, `setPaletteSlots`, `setDangerWords`, `setSensitiveWords`, `updateSite`), content의 사이트별 위험 단어·카드 자리 반영, e2e.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-12-SUMMARY.md
@docs/design/SYSTEM.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성·새 권한 없음.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시, `model` 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(03-13): …` → 최소 구현 → GREEN 커밋 `feat(03-13): …`. 본문은 한국어.
- 완료 판정은 `CI=true` e2e. 저장 쓰기는 writer에만(D-03). 설정 화면 전용 op는 `isExtensionPage(sender)` 뒤에만(03-12).
- 화면은 SYSTEM.md "설정 화면" 템플릿과 tokens.css만(D-36), 카피 규칙(D-37). 연습 사이트에서만 시험(D-39).

## 전제 (아직 실행 전인 Phase 1 계획에서 온 가정)

- [01-13 가정] 사이트 항목은 `site:<origin>` 사이트별 sync 항목, 쓰기 합치기·분당 상한.
- [01-14 가정] 사이트 항목 쓰기 전 크기 검사(`item-too-large`), 읽기는 `migrate`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: "컨디션 모드" 값 편집과 "명령판 카드 자리" 바꾸기</name>
  <files>src/entrypoints/options/condition-section.ts, src/entrypoints/options/palette-section.ts, src/entrypoints/options/main.ts, src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/background.ts, src/entrypoints/content.ts, tests/e2e/options-sections.e2e.ts</files>
  <read_first>
    - src/entrypoints/options/ui.ts, src/entrypoints/options/keymap-section.ts (03-12 절 붙이는 방식)
    - src/core/settings-schema.ts (ConditionBundle 범위, paletteSlots 검사), src/core/palette-slots.ts (COMMAND_LABELS, DEFAULT_PALETTE_SLOTS)
    - src/entrypoints/content.ts (명령판 컨트롤러를 만들 때 넘기는 slots)
    - src/worker/storage-writer.ts, src/entrypoints/background.ts (isExtensionPage)
  </read_first>
  <behavior>
    - e2e `condition`: 2 → 줄에 좋은 날·힘든 날 다섯 값이 초·%로 보인다(예: "2 좋은 날 떨림 간격 · 0.30초")
    - e2e `condition`: 좋은 날 떨림 간격 줄 → "0.45" Enter → `conditions.good.tremorIntervalMs === 450`, 토스트 "저장했어요"
    - e2e `condition`: 같은 줄에 "5" → "! 떨림 간격은 0.05~2초 사이로 적으세요.", 값 그대로
    - e2e `condition`: 힘든 날 조작 방식 줄 → [1 자석 커서] [2 번호표] [3 자동 순서 강조] → 2 → `conditions.hard.mode === 'hints'`
    - e2e `slots`: 3 → "새로고침"(1장 7) 줄 → "옮길 자리를 누르세요: 장 1~3, 그다음 번호 1~8" → 2, 4 → 1장 7이 "틀 기록", 2장 4가 "새로고침"으로 저장되고, practice 탭에서 0 → 첫 장 7 카드가 "틀 기록"
    - e2e `slots`: "기본 자리로" → `paletteSlots`가 `DEFAULT_PALETTE_SLOTS`와 같다
  </behavior>
  <action>
RED 먼저: behavior 여섯을 `tests/e2e/options-sections.e2e.ts`(`describe('condition')`, `describe('slots')`)에 쓰고 실패 확인 후 RED 커밋.

`messages.ts`: op `updateCondition { condition: 'good' | 'hard', patch: Partial<ConditionBundle> }`, `setPaletteSlots { slots }`. `background.ts`: 둘 다 `isExtensionPage` 뒤에만. `storage-writer.ts`: 읽고 바꾸고 `SettingsV2`로 다시 검사 후 쓴다.

`condition-section.ts`(D-29): 좋은 날 다섯 줄 + 힘든 날 다섯 줄(9개 넘으면 "0 다음"). 시간 값은 초 글로 받아(소수 둘째 자리) ms로, 범위는 `ConditionBundle` 그대로이고 벗어나면 "떨림 간격은 0.05~2초 사이로 적으세요."처럼 이름·범위를 넣은 한 문장. 조작 방식·표시 크기(100·125·150·175·200%)는 번호 선택 판.

`palette-section.ts`(D-06): 명령 20개 줄(명령 이름 · "1장 7" 같은 자리)과 "기본 자리로". 명령 줄 → 장 번호 → 카드 번호(1~8) 두 번 눌러 그 자리의 명령과 바꾼다(빈 자리면 옮긴다). 결과를 zod로 검사 뒤 `setPaletteSlots`.

`content.ts`: 명령판 컨트롤러가 `settings.data.paletteSlots`를 쓰고 설정이 바뀌면 받아 다시 그린다(03-01은 기본 자리를 넘겼다). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/options-sections.e2e.ts -g "condition|slots"</automated>
    <fails_when>non-zero exit, or the summary does not report "6 passed", or "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/palette.e2e.ts tests/e2e/condition.e2e.ts</automated>
    <fails_when>non-zero exit, or any failed test (regression in palette layout or condition switching)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/shared/messages.ts`에 `updateCondition`과 `setPaletteSlots`가 있다
    - `src/entrypoints/content.ts`에 `paletteSlots`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/options-sections.e2e.ts -g "condition|slots"` 6 passed, 회귀 없음
  </acceptance_criteria>
  <done>설정 화면에서 두 묶음 값과 명령판 카드 자리를 바꾼다. RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: "자주 쓰는 문구" 고치기·빼기와 "위험한 버튼 단어" 전체·사이트별</name>
  <files>src/entrypoints/options/phrases-section.ts, src/entrypoints/options/danger-section.ts, src/entrypoints/options/main.ts, src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/background.ts, src/entrypoints/content.ts, tests/e2e/options-sections.e2e.ts</files>
  <read_first>
    - src/core/settings-schema.ts (SiteEntryV2.phrases·dangerWords, siteKey)
    - src/core/danger.ts (isDanger), src/page/collector/collector.ts (getDangerWords 콜백)
    - src/entrypoints/content.ts (collector에 getDangerWords를 넘기는 곳, 사이트 항목 읽기)
    - src/entrypoints/options/ui.ts ("사이트: {출처}" 줄, 사이트 고르기)
  </read_first>
  <behavior>
    - e2e `phrases`: 4(사이트 practice.test) → "1 적요 · 거래처 미팅 식대" → [1 고치기] [2 빼기] → 1 → 칸에 글이 채워져 있고 "거래처 점심"으로 고쳐 Enter → 사이트 항목 phrases가 바뀌고 practice 탭 그 칸의 문구 카드가 새 글
    - e2e `phrases`: 같은 줄 → 2 → 문구가 빠진다
    - e2e `danger`: 5 → "단어 더하기" → "보류" Enter → 전역 `dangerWords`에 "보류", practice 탭의 "보류" 버튼(시험용 페이지)에 번호표 "! 위험"
    - e2e `danger`: "이 사이트만 따로 정하기" → 사이트 항목 `dangerWords`가 전역 목록의 복사본 → 그 목록에서 "삭제" 빼기 → practice.test의 "삭제" 버튼은 위험이 아니고 other.test의 "삭제" 버튼은 여전히 위험
    - e2e `danger`: "전체 목록 따르기" → 사이트 항목 `dangerWords`가 없어지고 practice.test "삭제"가 다시 위험
  </behavior>
  <action>
RED 먼저: behavior 다섯을 `options-sections.e2e.ts`(`describe('phrases')`, `describe('danger')`)에 더하고 실패 확인 후 RED 커밋.

`messages.ts`: op `setDangerWords { words: string[] }`(전역), `updateSite { origin, patch: { phrases?, dangerWords?: string[] | null } }`(`origin`은 http·https 출처 형식만, zod). `background.ts`: 둘 다 `isExtensionPage` 뒤에만. `storage-writer.ts`: `updateSite`는 사이트 항목을 읽어(없으면 빈 v2) 패치를 적용하고(`dangerWords: null`이면 필드 삭제) 검사·크기 확인 후 쓴다.

`phrases-section.ts`(D-19): 지금 사이트(`?site`, 없으면 "0 다른 사이트"로 `site:*` 목록에서 고르기)의 문구를 칸 이름(접두어를 뗀 이름)별로 줄로, 고치기(글 입력 줄)·빼기. `danger-section.ts`(D-16): 전역 목록 줄(고르면 [1 빼기]) + "단어 더하기", 지금 사이트 줄 "이 사이트만 따로 정하기"/"전체 목록 따르기"와 사이트 목록 편집. 안내 한 줄: "결재·상신은 기본 목록에 없어요. 필요하면 더하세요."(D-16).

`content.ts`(SAFE-06): collector의 `getDangerWords`가 맨 위 사이트 항목의 `dangerWords`가 있으면 그것, 없으면 전역 목록을 돌려주고, 사이트 항목이 바뀌면 다시 모은다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/options-sections.e2e.ts -g "condition|slots|phrases|danger"</automated>
    <fails_when>non-zero exit, or the summary does not report "11 passed", or "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/danger.e2e.ts tests/e2e/input-cards.e2e.ts</automated>
    <fails_when>non-zero exit, or any failed test (regression in danger detection or phrase cards)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/shared/messages.ts`에 `setDangerWords`와 `updateSite`가 있고 `background.ts`에서 `isExtensionPage` 뒤에만 처리된다
    - `src/entrypoints/content.ts`의 위험 단어 계산에 사이트 항목 `dangerWords`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/options-sections.e2e.ts -g "condition|slots|phrases|danger"` 11 passed, 회귀 없음
  </acceptance_criteria>
  <done>문구를 고치고 빼며, 위험 단어 목록을 사이트마다 고친다. RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: "민감칸 판별 단어"와 "사이트별 민감칸·고정 번호" — 풀리게 되면 확인</name>
  <files>src/entrypoints/options/sensitive-section.ts, src/entrypoints/options/site-section.ts, src/entrypoints/options/confirm.ts, src/entrypoints/options/main.ts, src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/background.ts, tests/e2e/options-sections.e2e.ts</files>
  <read_first>
    - src/core/sensitive.ts (isSensitive, 모양 이름), src/core/settings-schema.ts (sensitiveWords, SiteEntryV2.sensitiveOverrides·pins)
    - src/core/confirm-guard.ts, src/page/overlay/confirm-dialog.ts (확인 화면 모양과 보호 — 설정 화면용은 같은 규칙으로)
    - docs/design/SYSTEM.md ("확인 화면" 템플릿)
    - src/entrypoints/options/ui.ts
  </read_first>
  <behavior>
    - e2e `sensitive`: 6 → 늘 민감 단어 줄(비밀번호·비번·PW·인증번호·OTP·보안카드)과 모양 단어 줄(주민·계좌·카드) → "단어 더하기"로 "사번" → 라벨 "사번" 칸(시험용 페이지)에 들어가면 "민감칸이라 기록하지 않아요", 친 값이 기록되지 않는다
    - e2e `sensitive`: 늘 민감 단어에서 "OTP"를 빼면 라벨 "OTP"만 있는 칸에 카드가 뜨고 값이 기록된다
    - e2e `sensitive`: 모양 단어 "사업자"를 [2 계좌번호 모양]으로 더하면 라벨 "사업자번호" 칸에 "123-45-67890"을 친 값은 기록되지 않는다
    - e2e `site`: 7 → "적요 · 민감함" 줄 → [1 기본 판별로] → 설정 화면 확인 화면 "이 칸을 민감하지 않음으로 둘까요? Enter = 예"(1초 보호) → 0.5초 Enter 무시 → 1초 뒤 Enter → 그 칸 설정이 빠진다
    - e2e `site`: "3번 · {버튼 이름}" 고정 줄 → [1 고정 풀기] → pins에서 빠지고, practice 탭에서 F → 그 버튼의 번호가 3이 아니다
  </behavior>
  <action>
RED 먼저: behavior 다섯을 `options-sections.e2e.ts`(`describe('sensitive')`, `describe('site')`)에 더하고 실패 확인 후 RED 커밋.

`messages.ts`: op `setSensitiveWords { always: string[]; shape: { word; shape }[] }`, `updateSite` 패치에 `sensitiveOverrides?`, `pins?`. `background.ts`: `isExtensionPage` 뒤에만. `storage-writer.ts`: 검사 후 쓴다.

`sensitive-section.ts`(D-28): 두 목록 줄 + "단어 더하기"(모양 단어는 더한 뒤 [1 주민번호 모양] [2 계좌번호 모양] [3 카드번호 모양]). `site-section.ts`(D-27, D-10): 지금 사이트의 민감칸 설정 줄("{칸 이름} · 민감함/민감하지 않음", 고르면 [1 기본 판별로])과 고정 번호 줄("{번호}번 · {요소 이름}", 고르면 [1 고정 풀기]). 민감함 설정을 지우는 것은 칸이 민감하지 않게 될 수 있으므로 늘 확인 화면을 거친다(값을 모르는 설정 화면에서는 보수적으로 — 가정 절).

`src/entrypoints/options/confirm.ts`: `openOptionsConfirm({ title, body, onResult })` — SYSTEM.md 확인 화면 템플릿(폭 `--dialog-width`, 버튼 높이 `--target-confirm`, 뒤 `--scrim`, 1초 보호 막대 "1초 뒤에 누를 수 있어요"), 보호는 `createConfirmGuard`(진짜 키만, 버튼 클릭 리스너 없음). 03-14 가져오기 덮어쓰기도 이것을 쓴다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/options-sections.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "16 passed", or "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/recent-values.e2e.ts tests/e2e/sensitive-config.e2e.ts tests/e2e/pin.e2e.ts && pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit, or any failed test (regression in sensitive judgement, overrides or pins)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/entrypoints/options/confirm.ts`에 `createConfirmGuard`와 `1초 뒤에 누를 수 있어요`가 있다
    - `src/shared/messages.ts`에 `setSensitiveWords`가 있다
    - `src/entrypoints/options/` 아래 파일에 hex 색 리터럴과 `box-shadow`가 없다
    - `CI=true pnpm exec playwright test tests/e2e/options-sections.e2e.ts` 16 passed, 회귀 없음
  </acceptance_criteria>
  <done>민감칸 판별 단어를 더하고 빼며, 사이트별 민감칸 설정·고정 번호를 보고 지운다(풀리게 되면 확인). RED·GREEN 커밋이 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- SAFE-06·INPT-02·PRIV-02·PRIV-03·PERS-01: edge probe `unclassified — review manually`. 가정한 경계:
  - 사이트별 위험 단어는 "전체 목록을 복사해 이 사이트만 따로" 방식이다(D-16 "사이트마다 더하기·빼기"). 따로 정한 사이트는 이후 전체 목록 변경을 따르지 않는다 — "전체 목록 따르기"로 되돌린다.
  - 문구는 설정 화면에서 고치고 뺀다. 새 문구 더하기는 입력칸 카드(03-09)에서 한다 — 설정 화면에는 칸이 없어 칸 이름을 모른다.
  - 설정 화면에서 민감함 설정을 지우는 것은 늘 확인 화면을 거친다(그 칸의 값·라벨을 설정 화면이 몰라 풀리는지 알 수 없으므로 보수적으로, D-27).
  - 민감칸 설정을 새로 더하는 곳은 명령판 "민감칸 설정"(03-10) 한 곳이다.
  - 카드 자리 바꾸기는 두 명령 자리 맞바꾸기(빈 자리면 옮기기)이고, 셋째 장 9("첫 장")·첫째·둘째 장 9("다음 장")는 바꿀 수 없다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 확장 페이지(옵션) → SW | 전역·사이트 설정 바꾸기 |
| 사이트 페이지 → content | 위험 단어가 바뀐 뒤의 누르기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-48 | Elevation of Privilege | `updateSite`·`setDangerWords`·`setSensitiveWords` | high | mitigate | `isExtensionPage` 뒤에만 — content 경로로 다른 사이트의 위험 단어·민감칸 설정을 바꿀 수 없다 |
| T-03-49 | Tampering | `updateSite.origin` | medium | mitigate | zod로 http·https 출처 형식만, 사이트 항목 v2 검사·8KB 한도 |
| T-03-50 | Elevation of Privilege | 민감칸 설정 지우기 | high | mitigate | 설정 화면 확인 화면도 `createConfirmGuard`(1초, 진짜 키만) — e2e |
| T-03-51 | Denial of Service | 위험 단어 빼기 | medium | accept | 이용자가 스스로 뺀 단어는 확인 없이 눌린다 — 이용자 결정(D-16 "직접 더하는 것은 막지 않는다"와 같은 쪽) |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/options-sections.e2e.ts` 16 passed
- palette·condition·danger·input-cards·recent-values·sensitive-config·pin 회귀 없음
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 위험한 버튼 단어를 사이트마다 고친다(SAFE-06)
- 자주 쓰는 문구를 고친다(INPT-02), 민감칸 판별 단어를 더하고 뺀다(PRIV-03), 사이트별 민감칸 설정을 보고 지운다(PRIV-02)
- 두 묶음 값을 고친다(PERS-01)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/entrypoints/options/condition-section.ts`, `palette-section.ts`, `phrases-section.ts`, `danger-section.ts`, `sensitive-section.ts`, `site-section.ts`, `confirm.ts`, `tests/e2e/options-sections.e2e.ts`
- 심볼: `openOptionsConfirm()`, 저장 요청 `updateCondition`, `setPaletteSlots`, `setDangerWords`, `setSensitiveWords`, `updateSite`
- 동작: 사이트별 위험 단어(`site.dangerWords ?? settings.dangerWords`), 명령판 카드 자리 = `settings.paletteSlots`

<output>
Create `.planning/phases/03-navigation-input-condition/03-13-SUMMARY.md` when done
</output>
