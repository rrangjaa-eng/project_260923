---
phase: 03-navigation-input-condition
plan: 06
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - src/page/nav/scroll.ts
  - src/shared/messages.ts
  - src/worker/relay.ts
  - src/entrypoints/background.ts
  - src/entrypoints/content.ts
  - tests/practice-site/scroll-areas.html
  - tests/practice-site/scroll-inner.html
  - tests/e2e/scroll.e2e.ts
  - src/core/esc-back.ts
  - tests/unit/esc-back.test.ts
  - src/page/input/pipeline.ts
  - src/worker/storage-writer.ts
  - src/types/chrome.d.ts
  - tests/e2e/nav-back.e2e.ts
autonomous: true
requirements: [NAV-01, NAV-03]
estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "↑↓를 누르면 한 화면(스크롤 영역 높이)의 80%씩 부드럽게 스크롤된다"
    - "페이지에 스크롤 영역이 여러 개면(iframe 안 포함) 커서가 있는 영역이 움직이고, 그 영역이 더 갈 수 없으면 바깥 영역(끝내 페이지)이 움직인다"
    - "초점이 iframe 안에 있어도 ↑↓는 커서가 있는 영역을 움직인다"
    - "입력칸 안에서는 ↑↓가 원래대로(커서 이동) 동작하고, 키를 누르고 있어도 한 번만 스크롤된다"
    - "번호·자석 커서·자동 순서 강조로 링크를 눌러 페이지가 바뀐 직후(새 페이지에서 처음 누르는 키가 Esc이고 10초 안) Esc를 누르면 이전 페이지로 돌아간다"
    - "이용자가 마우스로 직접 링크를 누른 경우, 새 페이지에서 다른 키를 먼저 누른 경우, 10초가 지난 경우에는 Esc가 뒤로 가지 않고 사이트로 그대로 간다"
  prohibitions:
    - statement: "명령판·확인 화면·양식 한 장 보기가 열려 있거나 입력 중이거나 끌기·번호표·자동 순서 강조 중일 때 Esc는 뒤로 가기가 되지 않는다"
      requirement_id: NAV-03
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/page/nav/scroll.ts"
      provides: "커서 아래 요소에서 그 방향으로 움직일 수 있는 스크롤 영역 찾기와 80% 스크롤"
      exports: ["findScrollTarget", "scrollByPage"]
    - path: "src/core/esc-back.ts"
      provides: "'직후' 판정(새 문서 첫 키가 Esc, 10초 안)과 같은 탭 링크 판정(순수)"
      exports: ["createEscBack", "isSameTabNavigation", "NAV_MARK_TTL_MS"]
    - path: "tests/practice-site/scroll-areas.html"
      provides: "스크롤 영역 두 개 + iframe 안 스크롤 + 입력칸이 있는 긴 페이지"
  key_links:
    - from: "src/entrypoints/content.ts"
      to: "src/worker/relay.ts"
      via: "cursor/here로 커서가 있는 프레임을 기록 → scroll/key → SW가 그 프레임에 scroll/request"
      pattern: "scroll/request"
    - from: "src/entrypoints/content.ts"
      to: "src/worker/storage-writer.ts"
      via: "링크를 누르기 직전 nav/mark → writer가 세션 저장소 navmark:<tabId> 기록, 새 문서 시작 때 nav/query로 한 번 꺼내 지움"
      pattern: "nav/mark"
    - from: "src/page/input/pipeline.ts"
      to: "src/core/esc-back.ts"
      via: "observeKey가 모달·입력 모드와 상관없이 모든 진짜 keydown을 먼저 보여 줘 첫 키 판정"
      pattern: "observeKey"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
↑↓ 키 스크롤(D-08)과 "링크 이동 직후 Esc = 뒤로"(D-09)를 만든다.

Purpose: NAV-01, NAV-03. 스크롤 영역 찾기는 커서가 있는 프레임에서 바로 하고, 프레임 사이 전달은 SW가 한다(D-01). 뒤로 가기는 도우미가 누른 링크만 기록해(세션 저장소, 새 권한 없음) 판단한다(RESEARCH Pattern 3).
Output: `scroll.ts`, 연습 페이지 둘, `esc-back.ts`+단위 시험, 파이프라인 `observeKey`, 세션 기록, e2e 두 파일.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-RESEARCH.md
@.planning/phases/03-navigation-input-condition/03-02-SUMMARY.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성·새 권한 없음(`storage.session`은 기존 `storage` 권한, `tabs.goBack`은 기존 `tabs` 권한).
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시, `model` 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(03-06): …` → 최소 구현 → GREEN 커밋 `feat(03-06): …`. 본문은 한국어.
- 완료 판정은 `CI=true` e2e. 저장 쓰기(세션 저장소 포함)는 writer에만(D-03). 새 메시지는 `sender.id` 확인 + zod.
- 입력은 `isTrusted`만, 떨림 걸러내기가 먼저(D-02). 연습 사이트에서만 시험(D-39).

## 전제 (아직 실행 전인 Phase 1 계획에서 온 가정)

- [01-14 가정] 새 리스너(`pointermove` 기록, `cursor/here` 보내기)는 content의 `AbortController` `signal`로 정리된다.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ↑↓ 스크롤 — 커서가 있는 영역을 80%씩</name>
  <files>src/page/nav/scroll.ts, src/shared/messages.ts, src/worker/relay.ts, src/entrypoints/background.ts, src/entrypoints/content.ts, tests/practice-site/scroll-areas.html, tests/practice-site/scroll-inner.html, tests/e2e/scroll.e2e.ts</files>
  <read_first>
    - src/entrypoints/content.ts (rAF pointermove 합치기와 lastCursorPos, currentTargetId, inputPipeline.onKey 등록 순서)
    - src/worker/relay.ts (탭별 상태 보관·정리 방식 reportsByTab)
    - src/page/input/mode.ts (deepActiveElement — 열린 shadow root를 따라가는 방식, 조상 올라가기에도 같은 규칙)
    - tests/practice-site/frames.html (iframe 연습 페이지 모양), tests/e2e/fixtures.ts
    - .planning/phases/03-navigation-input-condition/03-RESEARCH.md (Pattern 2, Assumption A3)
  </read_first>
  <behavior>
    - e2e: `scroll-areas.html`에서 커서를 `#box-a` 안 `#in-a` 위에 두고 ↓ → 1초 뒤 `#box-a.scrollTop`이 `round(0.8 × clientHeight)`(±2), `window.scrollY`는 0
    - e2e: `#box-b`를 400까지 내린 상태에서 커서를 `#box-b` 안에 두고 ↑ → `scrollTop`이 400 − 80%(최소 0)
    - e2e: 커서를 상자 밖 페이지 위에 두고 ↓ → `window.scrollY`가 `round(0.8 × innerHeight)`(±2)
    - e2e: `#box-a`가 끝까지 내려간 상태에서 커서를 그 안에 두고 ↓ → 페이지가 움직인다(바깥 영역으로 넘김)
    - e2e: 커서를 `#frame-scroll`(같은 출처 iframe) 안에 두고 ↓ → iframe 문서의 `scrollY`가 늘고 맨 위 `scrollY`는 0
    - e2e: iframe 안 버튼에 초점을 둔 채 커서를 맨 위 페이지(상자 밖)에 두고 ↓ → 맨 위 페이지가 움직인다
    - e2e: `#field`에 초점을 두고 ↓ → 도우미 스크롤이 없다(`scrollY`·상자 `scrollTop` 그대로)
    - e2e: ↓를 1초 누르고 있어도(자동 반복) 한 번만 스크롤된다
  </behavior>
  <action>
RED 먼저: 연습 페이지 둘과 behavior 여덟을 `tests/e2e/scroll.e2e.ts`에 쓰고 실패 확인 후 RED 커밋.

연습 페이지(D-39): `tests/practice-site/scroll-areas.html` — 본문 높이 3000px, `#box-a`·`#box-b`(각각 `overflow: auto`, 높이 300px, 안쪽 2000px, 위쪽에 버튼 `#in-a`·`#in-b`), `#frame-scroll`(높이 300px, `src="scroll-inner.html"`), 입력칸 `#field`. `scroll-inner.html` — 본문 2000px, 버튼 `#in-inner`.

`src/page/nav/scroll.ts`(D-08): `findScrollTarget(start: Element | null, direction: 'up' | 'down'): Element | null` — `start`에서 조상으로 올라가며(열린 shadow root는 host로 건넌다) `overflow-y`가 auto·scroll·overlay이고 `scrollHeight > clientHeight`이며 그 방향으로 더 갈 수 있는 첫 요소, 없으면 `document.scrollingElement`가 그 방향으로 갈 수 있을 때 그것, 아니면 null. `scrollByPage(target, direction)` — 거리 `round(0.8 × (scrollingElement면 innerHeight, 아니면 clientHeight))`, `scrollBy({ top, behavior })`, `prefers-reduced-motion: reduce`면 `'auto'` 아니면 `'smooth'`.

`messages.ts`: `cursor/here {}`(커서가 이 프레임 안에서 움직임), `scroll/key { direction }`(키를 받은 프레임 → SW), `scroll/request { direction }`(SW → 커서 프레임), 응답 `{ scrolled: boolean }`. `relay.ts`: 탭별 `cursorFrameByTab` — `cursor/here`의 `sender.frameId`를 기록(탭 닫힘·로딩 때 지움). `scroll/key` → 기록된 프레임(없으면 0)에 `scroll/request`, 응답이 `scrolled: false`이고 그 프레임이 0이 아니면 frameId 0에 한 번 더. `background.ts`: 세 타입을 relay로.

`content.ts`: 모든 프레임이 진짜 `pointermove`에서 250ms에 한 번까지 `cursor/here`(마지막으로 보낸 뒤 다른 프레임으로 옮겨 왔을 때는 바로). `inputPipeline.onKey`: `keymap.scrollUp`/`scrollDown`이면 `scroll/key` 보내고 true(입력 모드에서는 파이프라인이 처리기를 부르지 않으므로 원래대로). `scroll/request`를 받으면 시작 요소 = 자석이 잡은 요소(`currentTargetId`) 또는 `document.elementFromPoint(lastCursorPos)` → `findScrollTarget` → 있으면 `scrollByPage` 후 `{ scrolled: true }`. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/scroll.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "8 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/nav/scroll.ts`에 `export function findScrollTarget`와 `export function scrollByPage`가 있고 `0.8`이 있다
    - `src/worker/relay.ts`에 `cursorFrameByTab`이 있다
    - `tests/practice-site/scroll-areas.html`에 `id="box-a"`, `id="box-b"`, `id="frame-scroll"`, `id="field"`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/scroll.e2e.ts` 8 passed
  </acceptance_criteria>
  <done>↑↓가 커서가 있는 영역(iframe 포함)을 80%씩 부드럽게 움직인다. RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: "직후" 판정과 같은 탭 링크 판정 (순수 함수)</name>
  <files>src/core/esc-back.ts, tests/unit/esc-back.test.ts</files>
  <read_first>
    - src/core/dwell-timer.ts (시각을 인자로 받는 상태 기계 모양)
    - .planning/phases/03-navigation-input-condition/03-RESEARCH.md (Pattern 3 — Esc 우선순위 1~7)
    - .planning/phases/03-navigation-input-condition/03-CONTEXT.md (Claude's Discretion 3)
  </read_first>
  <behavior>
    - `createEscBack({ armed: true, startedAt: 0 })`: `onKey('Escape', 500, { cancel: 'Escape', blocked: false })` → `'back'`, 그 뒤 `onKey('Escape', 600, …)` → `'pass'`(한 번만)
    - 무장 상태에서 첫 키가 `ArrowDown`이면 `'pass'`, 이어서 Esc도 `'pass'`
    - 무장 상태에서 10,001ms에 Esc → `'pass'`
    - 무장 상태에서 `blocked: true`로 Esc(명령판 닫기 등) → `'pass'`이고 무장이 풀린다(다음 Esc도 `'pass'`)
    - 무장하지 않았으면 Esc → `'pass'`
    - `onPointerDown()` 뒤 Esc → `'pass'`
    - 취소 키를 `KeyX`로 바꿨으면 `KeyX`가 `'back'`, `Escape`는 `'pass'`
    - `isSameTabNavigation({ href: 'http://practice.test/next.html', target: '', currentHref: 'http://practice.test/targets.html' })` → true. `target: '_blank'` → false. `'_self'`·`'_top'` → true
    - `href`가 `javascript:`로 시작하거나 지금 주소와 해시만 다르면 false
    - `NAV_MARK_TTL_MS`는 10000이고 `isFreshMark({ at: 0 }, 10000)` true, `isFreshMark({ at: 0 }, 10001)` false
  </behavior>
  <action>
RED 먼저: behavior 열을 `tests/unit/esc-back.test.ts`에 쓰고 실패 확인 후 RED 커밋.

`src/core/esc-back.ts`(순수 — DOM·전역 창·확장 API를 쓰지 않음, 시각은 인자로; Claude's Discretion 3의 결정): `NAV_MARK_TTL_MS = 10000`, `isFreshMark(mark: { at: number }, now)`, `isSameTabNavigation({ href, target, currentHref })`, `createEscBack({ armed, startedAt, ttlMs = NAV_MARK_TTL_MS })` → `{ onKey(code, now, { cancel, blocked }): 'back' | 'pass', onPointerDown(), armed() }`. 규칙: 무장은 첫 키(무엇이든) 또는 첫 `pointerdown`에서 풀린다. 첫 키가 취소 키이고 `blocked`가 아니며 `now - startedAt ≤ ttlMs`일 때만 `'back'`. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/esc-back.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 10 tests passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/esc-back.ts`에 `createEscBack`, `isSameTabNavigation`, `isFreshMark`, `NAV_MARK_TTL_MS = 10000`이 export된다
    - `src/core/esc-back.ts`에 `document`·`window`·`chrome.` 참조가 없다
    - `pnpm exec vitest run tests/unit/esc-back.test.ts` 10개 이상 통과
  </acceptance_criteria>
  <done>"직후"의 기준과 같은 탭 링크 판정이 단위 시험으로 고정되었다. RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 링크 이동 직후 Esc → 뒤로 (세션 기록 + 첫 키 관찰)</name>
  <files>src/page/input/pipeline.ts, src/worker/storage-writer.ts, src/entrypoints/background.ts, src/shared/messages.ts, src/entrypoints/content.ts, src/types/chrome.d.ts, tests/e2e/nav-back.e2e.ts</files>
  <read_first>
    - src/core/esc-back.ts (Task 2 결과)
    - src/page/input/pipeline.ts (모달·입력 모드·처리기 호출 순서)
    - src/entrypoints/content.ts (pressOrDrag — 모든 도우미 누르기가 지나는 한 곳, isTopFrame, 명령판·확인 화면·끌기·번호표·자동 순서 강조 상태 변수)
    - src/worker/storage-writer.ts (enqueue 큐), src/entrypoints/background.ts, src/types/chrome.d.ts
    - tests/practice-site/targets.html (`#nav-link` → next.html), tests/practice-site/next.html (`#next-heading`)
  </read_first>
  <behavior>
    - e2e: `targets.html`에서 커서로 `#nav-link`를 잡고 스페이스바 → `next.html` → Esc → `targets.html`로 돌아온다
    - e2e: F → `#nav-link`의 번호 → `next.html` → Esc → `targets.html`
    - e2e: 도우미로 `next.html`에 온 뒤 ↓를 먼저 누르고 Esc → 그대로 `next.html`
    - e2e: 도우미로 `next.html`에 온 뒤 10.5초 기다렸다 Esc → 그대로 `next.html`
    - e2e: 이용자가 마우스로 직접 `#nav-link`를 눌러 `next.html`에 온 뒤 Esc → 그대로 `next.html`
    - e2e: 도우미로 온 뒤 0(명령판 열림) → Esc(명령판 닫힘) → Esc → 그대로 `next.html`
  </behavior>
  <action>
RED 먼저: behavior 여섯을 `tests/e2e/nav-back.e2e.ts`에 쓰고 실패 확인 후 RED 커밋.

`pipeline.ts`: `observeKey(handler: (input: { code; mode: 'modal' | 'typing' | 'helper' }) => boolean)` — 떨림 걸러내기를 지난 모든 진짜 keydown을 모달·입력 모드·처리기보다 **먼저** 보여 준다. true를 돌려주면 그 키는 먹히고(preventDefault) 더 가지 않는다. 진짜 `pointerdown`도 관찰자에게 알린다(`observePointerDown`).

`messages.ts`: `nav/mark {}`(맨 위 → SW, 누르기 직전), `nav/query {}`(맨 위 새 문서 시작 → SW, 답 `{ justNavigated: boolean }`). `storage-writer.ts`: `markNav(tabId, at)` — 세션 저장소 `navmark:<tabId>`에 `{ at }`, `takeNavMark(tabId, now)` — 읽고 지운 뒤 `isFreshMark`면 true(세션 저장소 쓰기·지우기는 writer만). `background.ts`: 두 타입은 `sender.frameId === 0`일 때만, 탭 id는 `sender.tab.id`에서만. `chrome.d.ts`: `storage.session` get/set/remove.

`content.ts`(D-09, 맨 위 프레임): `pressOrDrag` 직전, 누를 요소의 `closest('a[href]')`가 `isSameTabNavigation`이면 `nav/mark`를 **응답을 기다린 뒤** 누른다(문서가 내려가기 전에 기록). 시작 때 `nav/query` → `createEscBack({ armed: justNavigated, startedAt: performance.now() })`. `observeKey`로 모든 첫 키를 넘긴다 — `blocked` = 명령판·확인 화면·양식 한 장 보기(03-11이 이 상태를 같은 변수로 더한다)·입력 모드·끌기 시작 상태·번호표·자동 순서 강조 중. `'back'`이면 `tab/command { command: 'back' }`(03-01 경로) 후 true, 아니면 false(사이트로 통과). `observePointerDown` → `onPointerDown()`. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/nav-back.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "6 passed", or "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/palette.e2e.ts tests/e2e/confirm.e2e.ts tests/e2e/drag.e2e.ts tests/e2e/input-filter.e2e.ts</automated>
    <fails_when>non-zero exit, or any failed test (Esc regression in palette, confirm, drag cancel or typing mode)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/input/pipeline.ts`에 `observeKey`가 있다
    - `src/worker/storage-writer.ts`에 `navmark:`와 `takeNavMark`가 있다
    - `grep -rnE "storage\.(sync|local|session)\.set" src/ | grep -v "src/worker/storage-writer.ts" | grep -vE "^[^:]+:[0-9]+:\s*//"`가 아무것도 출력하지 않는다
    - `CI=true pnpm exec playwright test tests/e2e/nav-back.e2e.ts` 6 passed, 회귀 명령 통과
  </acceptance_criteria>
  <done>도우미로 링크를 눌러 이동한 직후 Esc가 뒤로 가고, 그 밖의 경우 Esc는 원래 뜻을 지킨다. RED·GREEN 커밋이 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- NAV-01·NAV-03: edge probe `unclassified — review manually`. 가정한 경계:
  - "직후" = 도우미가 링크를 누른 뒤 10초 안에 새 문서가 시작되었고, 새 문서에서 처음 누르는 키가 Esc이며, 새 문서 시작 뒤 10초 안(RESEARCH 권장 "새 문서의 첫 Esc" + 느린 회사 시스템을 위한 시간 여유). 첫 마우스 누름도 무장을 푼다.
  - 뒤로 가기 대상은 맨 위 프레임의 같은 탭 링크(`target` 없음·`_self`·`_top`)뿐이다. iframe 안에서만 바뀌는 링크, 주소만 바꾸는 한 페이지 앱(history.pushState), 새 탭 링크는 대상이 아니다.
  - Esc 우선순위: 확인 화면 > 명령판 > 양식 한 장 보기 > 입력 중 > 끌기 시작 > 번호표 > 자동 순서 강조 > 링크 직후 뒤로 > 사이트로 통과(RESEARCH Pattern 3 + 이 phase의 새 상태).
  - ↑↓ 거리의 "한 화면"은 움직이는 영역의 보이는 높이(페이지면 창 높이)다. 움직임 줄이기 설정이면 부드러움 없이 바로 옮긴다.
  - SW가 쉬었다 깨도 세션 저장소 기록은 남는다(브라우저를 다시 켜면 사라짐 — 10초 기록이라 문제 없음, RESEARCH Pitfall 2).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content → SW | `nav/mark`, `nav/query`, `scroll/key`, `cursor/here` |
| 사이트 페이지 → content | 사이트가 만든 가짜 키·포인터 이벤트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-20 | Spoofing | `nav/mark` | medium | mitigate | 메시지는 확장 content만 보낼 수 있고(`sender.id`), `frameId === 0`만 받으며 탭 id는 `sender.tab.id` — 사이트가 흉내 낼 경로가 없다(RESEARCH 보안) |
| T-03-21 | Tampering | Esc 뒤로 가기 오작동 | high | mitigate | 첫 키·10초·blocked 규칙(단위 시험) + 기록은 한 번 꺼내면 지움 + 이용자 마우스 이동은 기록하지 않음(e2e) |
| T-03-22 | Denial of Service | `cursor/here` 메시지 폭주 | low | mitigate | 프레임당 250ms에 한 번까지, 프레임이 바뀔 때만 바로 |
| T-03-23 | Information Disclosure | 세션 기록 | low | accept | `navmark:<tabId>`에는 시각만 담고 주소·페이지 내용은 담지 않는다 |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/scroll.e2e.ts` 8 passed
- `pnpm exec vitest run tests/unit/esc-back.test.ts` 10개 이상 통과
- `CI=true pnpm exec playwright test tests/e2e/nav-back.e2e.ts` 6 passed, Esc 회귀 없음
</verification>

<success_criteria>
- ↑↓로 커서가 있는 영역이 80%씩 부드럽게 스크롤된다(NAV-01)
- 번호·자석 커서로 링크를 눌러 이동한 직후 Esc로 이전 페이지로 돌아간다(NAV-03)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/page/nav/scroll.ts`, `src/core/esc-back.ts`, `tests/unit/esc-back.test.ts`, `tests/practice-site/scroll-areas.html`, `tests/practice-site/scroll-inner.html`, `tests/e2e/scroll.e2e.ts`, `tests/e2e/nav-back.e2e.ts`
- 심볼: `findScrollTarget()`, `scrollByPage()`, `createEscBack()`, `isSameTabNavigation()`, `isFreshMark()`, `NAV_MARK_TTL_MS`, pipeline `observeKey()`·`observePointerDown()`, writer `markNav()`·`takeNavMark()`
- 메시지: `cursor/here`, `scroll/key`, `scroll/request`, `nav/mark`, `nav/query`
- 저장 키: `navmark:<tabId>`(storage.session)

<output>
Create `.planning/phases/03-navigation-input-condition/03-06-SUMMARY.md` when done
</output>
