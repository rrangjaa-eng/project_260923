---
phase: 03-navigation-input-condition
plan: 12
type: execute
wave: 11
depends_on: [03-11]
files_modified:
  - docs/design/SYSTEM.md
  - src/page/overlay/input-cards.ts
  - src/entrypoints/content.ts
  - tests/e2e/input-cards.e2e.ts
  - src/page/input/pipeline.ts
  - src/page/input/fill-value.ts
  - tests/practice-site/controlled-input.html
autonomous: true
requirements: [INPT-01]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "입력칸에 들어가면 그 칸 바로 아래에 그 칸(사이트 + 칸 이름)의 최근 값(새것부터 3개)과 자주 쓰는 문구가 번호 카드로 뜬다"
    - "입력칸 안에서 Alt+1~9(키 배치의 카드 보조키)로 카드를 고르면 값이 원래 칸에 들어가고 input·change 이벤트가 함께 발생해 사이트가 알아챈다 — React류 controlled 입력칸도 값을 지킨다"
    - "Alt 없이 누른 숫자는 글자로 들어간다"
    - "최근 값·문구가 없으면 카드 대신 '자주 쓰는 문구 추가' 카드 1개가 뜬다"
    - "민감칸에서는 카드가 뜨지 않고 '민감칸이라 기록하지 않아요'만 보인다"
    - "iframe 안 입력칸의 카드는 그 프레임 안에 뜬다"
  artifacts:
    - path: "src/page/overlay/input-cards.ts"
      provides: "입력칸 아래 번호 카드 줄(최근·문구·빈 상태·민감칸 안내)"
      exports: ["showInputCards", "hideInputCards"]
    - path: "src/page/input/fill-value.ts"
      provides: "네이티브 setter + input/change로 값 채우기, contenteditable은 insertText"
      exports: ["fillValue"]
    - path: "tests/practice-site/controlled-input.html"
      provides: "React류 controlled 입력칸 흉내(값 추적기로 순진한 대입을 무시)"
    - path: "tests/e2e/input-cards.e2e.ts"
      provides: "입력 줄이기 카드 표시·Alt 선택 시험"
  key_links:
    - from: "src/page/input/pipeline.ts"
      to: "src/entrypoints/content.ts"
      via: "입력 모드에서 onTypingKey 처리기 — cardNumberForEvent(e, keymap)가 번호를 주면 그 키를 먹는다"
      pattern: "onTypingKey"
    - from: "src/entrypoints/content.ts"
      to: "src/page/input/fill-value.ts"
      via: "고른 카드 값 → fillValue(el, value, { commit: true })"
      pattern: "fillValue\\("
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
입력 줄이기 카드(D-17, D-18, D-20)를 SYSTEM.md 템플릿대로 띄우고 Alt+1~9로 골라 원래 칸에 값을 넣는다. "자주 쓰는 문구 추가" 카드로 문구를 저장하는 것(D-19)은 03-13이 이 카드 위에 붙인다.

Purpose: INPT-01. 같은 값을 다시 치지 않는다. 문구 고치기·빼기는 설정 화면(03-19).
Output: SYSTEM.md 템플릿 추가, `input-cards.ts`, 파이프라인 `onTypingKey`, `fill-value.ts`, 연습 페이지 `controlled-input.html`, `input-cards.e2e.ts`(show·pick).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-10-SUMMARY.md
@.planning/phases/03-navigation-input-condition/03-11-SUMMARY.md
@docs/design/SYSTEM.md
@docs/design/tokens.css
@docs/DESIGN.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성·새 권한 없음.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시, `model` 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(03-12): …` → 최소 구현 → GREEN 커밋 `feat(03-12): …`. 본문은 한국어. SYSTEM.md 템플릿 추가는 따로 `docs(03-12): …` 커밋.
- 완료 판정은 `CI=true` e2e. 저장 쓰기는 writer에만(D-03). 문구는 sync, 최근 값은 local(D-04).
- 오버레이는 tokens.css 변수만(D-36), 카피 규칙(D-37). 새 화면 모양은 SYSTEM.md 템플릿을 먼저(DESIGN.md §4-1). 연습 사이트에서만 시험(D-39).
- `03-RESEARCH.md`(623줄)·`03-PATTERNS.md`는 통째로 읽지 않는다 — `<read_first>`가 가리킨 절만 Grep으로 줄을 찾아 범위로 읽는다(CLAUDE.md '500줄 이상은 range 필수'. 계획 하나의 문맥 예산을 지키려고 `<context>`에서 뺐다).

## 전제 (아직 실행 전인 Phase 1 계획에서 온 가정)

- [01-13 가정] content는 `site/query`로 맨 위 출처를 알고, SW는 content 요청의 출처를 `sender.tab.url`에서 계산한다.
- [01-15 가정] 카드 크기는 `--overlay-scale`을 곱한다(03-06에서 표시 크기도 곱한다).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 입력 줄이기 카드 템플릿과 표시 — 최근 값·빈 상태·민감칸 안내 (칸이 있는 프레임에서)</name>
  <files>docs/design/SYSTEM.md, src/page/overlay/input-cards.ts, src/entrypoints/content.ts, tests/e2e/input-cards.e2e.ts</files>
  <read_first>
    - docs/design/SYSTEM.md ("오버레이 템플릿", "컴포넌트 규칙 — 번호 카드·키 표시·상태", "상태 표 — 입력 줄이기")
    - docs/DESIGN.md (§4)
    - src/page/overlay/hints.ts, src/page/overlay/palette.ts (카드 모양·styleInjected 방어·tokens `?inline`)
    - src/page/input/fields.ts, src/core/sensitive.ts (03-10·03-11)
    - src/core/keys.ts (keyLabel, cardModifier — 03-03)
    - src/entrypoints/content.ts (03-11의 focusin/focusout 처리, 사이트 항목·최근 값 읽기)
    - tests/practice-site/input.html, tests/practice-site/sensitive.html, tests/practice-site/frames.html
  </read_first>
  <behavior>
    - e2e: `input.html`의 `#name`에 "홍길동"을 치고 벗어난 뒤 다시 들어가면 칸 바로 아래 `.input-cards`에 카드 "Alt+1" · "홍길동" · "최근"
    - e2e: 기록이 없는 칸에 들어가면 `.input-card`가 하나뿐이고 "Alt+1" · "자주 쓰는 문구 추가"
    - e2e: `sensitive.html`의 비밀번호 칸에 들어가면 `.input-card`가 없고 "민감칸이라 기록하지 않아요"가 보인다
    - e2e: "카드번호" 칸이 비어 있을 때 들어가면 카드가 뜬다(아직 번호 모양 값이 없음)
    - e2e: `frames.html`의 `#input-leaf`(중첩 iframe)에 들어가면 `.input-cards`가 그 프레임의 오버레이 안에 있고 맨 위 문서에는 없다
    - e2e: 칸을 벗어나면 카드가 사라진다
    - e2e: 이 사이트에서 도우미를 끈 상태(`site:…disabled`)면 칸에 들어가도 카드가 없다
  </behavior>
  <action>
먼저 템플릿(DESIGN.md §4-1, D-36): `docs/design/SYSTEM.md` "오버레이 템플릿"에 "입력 줄이기 카드 (입력칸 바로 아래)"를 더하고 `docs(03-12)` 커밋 — 칸 아래 8px(`--space-2`), 아래 공간이 모자라면 칸 위. 바탕 `--bg`, `--accent` `--border-strong` 테두리, radius `--radius-card`, 안쪽 여백 `--space-3`. 카드는 한 줄에 3장씩 최대 9장, 각 카드 = 키 칩 "Alt+1"(radius `--radius-key`) + 값 한 줄(40자 넘으면 …, `--text-body`) + 둘째 줄 "최근"/"문구"(`--muted`, `--text-sm`), 카드 바탕 `--surface`, 높이 `--target-min` 이상, radius `--radius-card`. 빈 상태·민감칸은 상태 표 문구 그대로. 마우스를 막지 않는다(pointer-events 없음). 새 토큰 없음.

RED: behavior 일곱을 `tests/e2e/input-cards.e2e.ts`(`describe('show')`)에 쓰고 실패 확인 후 RED 커밋.

`src/page/overlay/input-cards.ts`: `showInputCards(anchor: DOMRect, view: { cards: Array<{ number; label; source: 'recent' | 'phrase' | 'add' }>, note: string | null, modifierLabel: string })`, `hideInputCards()`. 그 프레임의 `ensureOverlayRoot()` 안, `styleInjected` 방어, tokens `?inline`, `--overlay-scale`을 곱한다.

`content.ts`(D-17, D-18, 모든 프레임 — Claude's Discretion 6 "칸이 있는 프레임에서 그린다"): 진짜 `focusin`에서 지원 칸이고 도우미·사이트가 켜져 있으면 — `isSensitive(descriptor, 지금 값, …)`이면 카드 없이 note "민감칸이라 기록하지 않아요", 아니면 최근 값(`recent:<맨 위 출처>`의 그 칸, 최대 3) + 문구(`site:<맨 위 출처>.phrases` 중 그 칸, 목록 순서)를 합쳐 9장까지, 둘 다 없으면 add 카드 하나("자주 쓰는 문구 추가"), 합쳐서 9장 미만이면 끝에 add 카드. 칸 사각형이 바뀌면(스크롤·크기) 다시 붙이고, `focusout`에서 숨긴다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/input-cards.e2e.ts -g "show"</automated>
    <fails_when>non-zero exit, or the summary does not report "7 passed", or "No tests found"</fails_when>
    <automated>grep -q "입력 줄이기 카드" docs/design/SYSTEM.md && pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit (template missing from SYSTEM.md, or type/lint error)</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/design/SYSTEM.md`에 "입력 줄이기 카드" 템플릿이 있고, `git log --oneline --grep='03-12' -- docs/design/tokens.css`가 아무것도 출력하지 않는다(새 토큰 없음)
    - `src/page/overlay/input-cards.ts`에 hex 색 리터럴과 `box-shadow`가 없다
    - `src/entrypoints/content.ts`에 `민감칸이라 기록하지 않아요`와 `자주 쓰는 문구 추가`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/input-cards.e2e.ts -g "show"` 7 passed
  </acceptance_criteria>
  <done>입력칸에 들어가면 그 칸의 최근 값·빈 상태·민감칸 안내가 SYSTEM.md 템플릿대로 칸 아래에 뜬다. docs·RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alt+1~9로 카드 고르기 — 사이트가 알아채게 값 채우기</name>
  <files>src/page/input/pipeline.ts, src/page/input/fill-value.ts, src/entrypoints/content.ts, tests/practice-site/controlled-input.html, tests/e2e/input-cards.e2e.ts</files>
  <read_first>
    - src/page/input/pipeline.ts (입력 모드 처리: Escape 흐리기, 처리기를 부르지 않는 곳)
    - src/core/keys.ts (cardNumberForEvent — 03-03)
    - src/page/overlay/input-cards.ts (Task 1)
    - .planning/phases/03-navigation-input-condition/03-RESEARCH.md — "### Pattern 4: 값 채우기", "### Pattern 5: Alt+1~9", "### Pitfall 3: 네이티브 setter가 최신 프레임워크와 안 맞음" 세 절과 "## Assumptions Log"의 A1·A2·A4 줄만(Grep으로 줄을 찾아 범위로 읽는다)
    - tests/practice-site/input.html (`#editor` contenteditable, 이벤트 카운터)
  </read_first>
  <behavior>
    - e2e `pick`: `#name`에 최근 값 "홍길동"이 있을 때 들어가 Alt+1 → 값 "홍길동", 페이지의 input 리스너와 change 리스너가 각각 1번 불리고, 칸에 "1"이 들어가지 않으며 카드가 사라진다
    - e2e `pick`: 카드가 2장일 때 Alt+5 → 값이 그대로다
    - e2e `pick`: `controlled-input.html`에서 최근 값 "김철수"를 Alt+1로 고르면 `#mirror`가 "김철수"이고 페이지가 다시 그린 뒤에도 칸 값이 "김철수"다
    - e2e `pick`: `#editor`(contenteditable)에 최근 값 "메모"를 Alt+1로 넣으면 글자가 들어가고 input 이벤트가 발생한다
    - e2e `pick`: Alt 없이 1 → 칸에 "1"이 글자로 들어간다
    - e2e `pick`: 카드 보조키를 Shift로 바꾸면 Shift+1이 카드를 고르고 Alt+1은 고르지 않는다
  </behavior>
  <action>
RED 먼저: `controlled-input.html`과 behavior 여섯을 `input-cards.e2e.ts`(`describe('pick')`)에 쓰고 실패 확인 후 RED 커밋.

연습 페이지(RESEARCH Pitfall 3) `tests/practice-site/controlled-input.html`: 입력칸 `#ctl`(`name="ctl"`)과 `#mirror`. 페이지 스크립트가 React처럼 값 추적기를 둔다 — 칸 인스턴스에 자기 `value` 속성을 정의해 대입될 때 추적값을 갱신하고, `input` 이벤트에서 칸 값이 추적값과 같으면 무시, 다르면 상태를 바꾸고 다음 프레임에 칸 값을 상태로 다시 쓰며 `#mirror`에 보인다.

`pipeline.ts`(D-02, D-17): `onTypingKey(handler: (input: { code; altKey; shiftKey; ctrlKey; metaKey }) => boolean)` — 입력 모드에서 떨림 걸러내기를 지난 진짜 keydown을 먼저 넘기고, true면 그 keydown과 짝 keyup을 먹는다. 기존 키 처리기 입력에도 보조키 상태를 더한다(선택 필드).

`src/page/input/fill-value.ts`(D-20): `fillValue(el, value, { commit })` — input·textarea·select는 네이티브 value setter(프로토타입 설명자)로 넣고 `input`(bubbles) 발생, `commit`이면 `change`(bubbles)도. contenteditable은 초점을 두고 내용을 전부 고른 뒤 `document.execCommand('insertText', false, value)`, 실패하면 `textContent` 대입 + `InputEvent('input', { inputType: 'insertText', data: value, bubbles: true })`.

`content.ts`: 카드가 떠 있을 때 `onTypingKey`에서 `cardNumberForEvent(e, keymap)`가 번호 n을 주고 n번 카드가 있으면 — `recent`·`phrase`면 `fillValue(el, 카드 값, { commit: true })` 후 카드를 숨기고 true. `add` 카드 선택은 03-13이 붙인다(이 계획에서는 false — 사이트로 통과). 카드가 없는 번호면 false(사이트로 통과). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/input-cards.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "13 passed", or "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/input-filter.e2e.ts tests/e2e/recent-values.e2e.ts</automated>
    <fails_when>non-zero exit, or any failed test (typing-mode regression)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/input/pipeline.ts`에 `onTypingKey`가 있다
    - `src/page/input/fill-value.ts`에 `export function fillValue`, `insertText`, `'input'`, `'change'`가 있다
    - `tests/practice-site/controlled-input.html`에 `id="mirror"`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/input-cards.e2e.ts` 13 passed, 회귀 명령 통과
  </acceptance_criteria>
  <done>Alt+1~9로 카드 값을 원래 칸에 넣고 사이트가 알아챈다. RED·GREEN 커밋이 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- INPT-01: edge probe `unclassified — review manually`. 가정한 경계:
  - 카드 순서: 최근 값(새것부터 최대 3) → 이 칸의 문구(저장 순서) → 자리가 남으면 "자주 쓰는 문구 추가". 최근 값과 문구가 모두 없으면 추가 카드 하나만(D-18).
  - 카드는 칸이 있는 프레임에 그린다(Claude's Discretion 6 — 맨 위로 보내면 칸 위치 변환과 왕복이 생긴다. 확인 화면·명령판처럼 "하나만 떠야 하는" 화면이 아니다).
  - 카드는 마우스로 누르지 않는다(키로만 — 떨림 걸러내기를 거치는 입력만 받는다).
  - 카드로 채운 값은 03-11의 벗어날 때 기록 규칙(민감 판별 포함)을 그대로 탄다.
  - Alt+1~9가 회사 시스템의 Alt 단축키와 부딪히는지는 회사 시스템 완성 뒤 확인(RESEARCH Open Question 1). 부딪히면 보조키를 Shift로 바꾼다(PERS-03).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content → 사이트 입력칸 | 도우미가 넣는 값과 이벤트 |
| 사이트 페이지 → content | 사이트가 만든 가짜 키 이벤트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-32 | Information Disclosure | 민감칸 카드 | high | mitigate | 민감칸에는 카드(추가 카드 포함)를 띄우지 않고 안내만 보인다(e2e). 저장 직전 다시 확인은 03-13 |
| T-03-34 | Tampering | 값 채우기 | medium | mitigate | 이용자가 진짜 키(Alt+n, 떨림 걸러내기 뒤)로 고른 카드만 채운다. 제출 이벤트는 만들지 않는다 |
| T-03-35 | Spoofing | 가짜 Alt+숫자 | medium | mitigate | 파이프라인이 `isTrusted`만 받는다 |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/input-cards.e2e.ts` 13 passed
- input-filter·recent-values 회귀 없음, `pnpm typecheck && pnpm lint` 통과
- SYSTEM.md에 "입력 줄이기 카드" 템플릿
</verification>

<success_criteria>
- 입력칸을 누르면 그 칸의 최근 값과 자주 쓰는 문구가 번호 카드로 뜨고 Alt+1~9로 고른다(INPT-01)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/page/overlay/input-cards.ts`, `src/page/input/fill-value.ts`, `tests/practice-site/controlled-input.html`, `tests/e2e/input-cards.e2e.ts`
- 심볼: `showInputCards()`, `hideInputCards()`, `fillValue()`, pipeline `onTypingKey()`
- 디자인: SYSTEM.md "입력 줄이기 카드" 템플릿
- CSS 클래스: `.input-cards`, `.input-card`

<output>
Create `.planning/phases/03-navigation-input-condition/03-12-SUMMARY.md` when done
</output>
