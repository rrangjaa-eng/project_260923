---
phase: 03-navigation-input-condition
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - src/shared/messages.ts
  - src/worker/storage-writer.ts
  - src/entrypoints/background.ts
  - src/entrypoints/content.ts
  - src/page/overlay/palette.ts
  - src/page/overlay/mode-indicator.ts
  - tests/e2e/condition.e2e.ts
autonomous: true
requirements: [PERS-01]
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "명령판 '6 컨디션 모드'에서 [1 좋은 날] [2 힘든 날]을 고르면 그 묶음의 조작 방식·떨림 간격·머무르기 시간·강조 속도·표시 크기가 한 번에 바뀐다"
    - "컨디션 카드마다 그 묶음의 값(조작 방식, 떨림 간격 초, 머무르기 초, 강조 속도 초, 표시 크기 %)이 글자로 보이고 지금 묶음은 선택 표시(남색 3px)다"
    - "명령판 '4 조작 방식 바꾸기'에서 [1 자석 커서] [2 번호표] [3 자동 순서 강조] 중 하나를 고르면 지금 묶음의 조작 방식이 바뀐다"
    - "바꾼 결과는 모드 표시에 '힘든 날로 바꿨어요'처럼 글자로 뜨고, 열려 있는 다른 탭에도 곧바로 적용된다"
    - "번호표·테두리·모드 표시·명령판 크기가 표시 크기만큼 커지고, 브라우저 확대(Ctrl +/−)와는 상관없이 그 크기를 지킨다"
  artifacts:
    - path: "tests/e2e/condition.e2e.ts"
      provides: "컨디션 전환·조작 방식 전환·표시 크기 시험"
  key_links:
    - from: "src/entrypoints/content.ts"
      to: "src/worker/storage-writer.ts"
      via: "저장 요청 setActiveCondition / setConditionMode → storage.onChanged로 모든 탭 반영"
      pattern: "setActiveCondition"
    - from: "src/page/overlay/mode-indicator.ts"
      to: "src/core/settings-schema.ts"
      via: "--overlay-scale = activeBundle().displayScale / 페이지 확대 비율"
      pattern: "displayScale"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
명령판에서 "좋은 날 / 힘든 날" 묶음을 한 번에 바꾸고(D-29), 지금 묶음의 조작 방식을 바꾸며(명령판 첫 장 4), 묶음의 "표시 크기"를 오버레이 크기에 반영한다(SYSTEM.md "크기는 컨디션 모드의 표시 크기로만").

Purpose: PERS-01. 그날 손 상태에 맞는 설정을 한 번에 고른다.
Output: 저장 요청 두 개, 명령판 하위 판 둘(선택 표시 포함), 표시 크기 배율, `condition.e2e.ts`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-02-SUMMARY.md
@.planning/phases/03-navigation-input-condition/03-04-SUMMARY.md
@docs/design/SYSTEM.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성·새 권한 없음.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시, `model` 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(03-05): …` → 최소 구현 → GREEN 커밋 `feat(03-05): …`. 본문은 한국어.
- 완료 판정은 `CI=true` e2e. 저장 쓰기는 writer에만(D-03). 새 메시지는 `sender.id` 확인 + zod.
- 오버레이는 tokens.css 변수만(D-36), 카피 규칙(D-37). 연습 사이트에서만 시험(D-39).

## 전제 (아직 실행 전인 Phase 1 계획에서 온 가정)

- [01-15 가정] `ensureOverlayRoot()`가 호스트에 `--overlay-scale`(= 1/확대 비율)을 두고 번호표·테두리·모드 표시·확인 화면·토스트가 그 값을 곱한다. 확대 비율은 `zoom/query`·`zoom/changed`로 받는다. 03-01 Task 1이 존재를 확인했다 — 실제 함수 이름은 01-15 SUMMARY를 따른다.
- [01-14 가정] writer는 읽을 때 `migrate`, 실패면 쓰지 않는다.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 명령판 "컨디션 모드" — 좋은 날 / 힘든 날 한 번에 바꾸기</name>
  <files>src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/background.ts, src/entrypoints/content.ts, src/page/overlay/palette.ts, tests/e2e/condition.e2e.ts</files>
  <read_first>
    - src/core/settings-schema.ts (conditions, activeCondition, activeBundle — 03-02)
    - src/worker/storage-writer.ts (updateSettings: 읽기 → 검사 → 합치기 → 다시 검사 → 쓰기)
    - src/shared/messages.ts, src/entrypoints/background.ts (storage/request op 분기)
    - src/page/palette/palette-controller.ts (pushPanel), src/page/overlay/palette.ts
    - src/entrypoints/content.ts (storage.onChanged('sync')로 설정을 다시 읽는 곳, 파이프라인·머무르기·방식이 묶음을 읽는 곳)
  </read_first>
  <behavior>
    - e2e: 0 → 6 → 하위 판 "컨디션 모드", 카드 "1 좋은 날"(선택 표시)과 "2 힘든 날", 각 카드 아래 값 글자(예: "자석 커서 · 떨림 0.3초 · 머무르기 0.8초 · 강조 1.5초 · 크기 100%")
    - e2e: 2 → `settings.data.activeCondition === 'hard'`, 명령판이 닫히고 모드 표시에 "힘든 날로 바꿨어요"
    - e2e: 좋은 날 떨림 간격 300, 힘든 날 800으로 준비 → 힘든 날에서 스페이스바를 500ms 간격 두 번 → 버튼 카운터 +1, 좋은 날로 바꾼 뒤 같은 동작 → +2
    - e2e: 힘든 날 방식이 자동 순서 강조면 바꾼 즉시 커서를 버튼 위에 올려도 테두리가 없고 스페이스바로 강조가 시작된다
    - e2e: 탭 둘을 연 채 한 탭에서 바꾸면 다른 탭도 곧바로 힘든 날 값으로 동작한다(떨림 간격 확인)
  </behavior>
  <action>
RED 먼저: behavior 다섯을 `tests/e2e/condition.e2e.ts`에 쓰고 실패 확인 후 RED 커밋.

`messages.ts`: `storage/request` op에 `setActiveCondition { condition: 'good' | 'hard' }`, `setConditionMode { mode: 'magnet' | 'hints' | 'auto-cycle' }`(지금 묶음에 적용). `storage-writer.ts`: 두 op를 기존 큐(`enqueue`)로 — `settings`를 `migrate`로 읽어 해당 값만 바꾸고 `SettingsV2`로 다시 검사해 쓴다(실패면 쓰지 않음). `background.ts`: op 분기에 둘을 더한다(content·확장 페이지 모두 보낼 수 있음 — 전역 설정이고 출처가 없다).

`palette.ts`: 카드에 `selected`(남색 `--border-strong` 테두리)와 둘째 줄 `detail`(`--muted`, `--text-sm`)을 받는다.

`content.ts`(D-29, 맨 위): `registerCommand('condition', …)` → `pushPanel({ title: '컨디션 모드', cards: [1 좋은 날, 2 힘든 날] })`, 각 카드 `detail`은 묶음 값을 초·% 글자로(키 이름 규칙 D-37), 지금 묶음은 `selected`. 고르면 `setActiveCondition` 요청 → 닫고 `showTransientMessage('좋은 날로 바꿨어요' | '힘든 날로 바꿨어요', 2000)`. 이미 지금 묶음이면 저장 없이 같은 알림. 설정 변경 반영은 기존 `storage.onChanged` 경로에서 묶음 전체(파이프라인 떨림 간격, 머무르기 시간, 방식, 강조 속도)를 다시 적용하도록 한 곳(`applyBundle()`)으로 모은다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/condition.e2e.ts -g "condition"</automated>
    <fails_when>non-zero exit, or the summary does not report "5 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/shared/messages.ts`에 `setActiveCondition`와 `setConditionMode`가 있다
    - `src/entrypoints/content.ts`에 `registerCommand('condition'`와 `applyBundle`이 있다
    - `CI=true pnpm exec playwright test tests/e2e/condition.e2e.ts -g "condition"` 5 passed
  </acceptance_criteria>
  <done>명령판에서 좋은 날 / 힘든 날을 고르면 묶음 값 전체가 모든 탭에 곧바로 적용된다. RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 명령판 "조작 방식 바꾸기"와 표시 크기 배율</name>
  <files>src/entrypoints/content.ts, src/page/overlay/mode-indicator.ts, src/page/overlay/palette.ts, tests/e2e/condition.e2e.ts</files>
  <read_first>
    - src/page/overlay/mode-indicator.ts (ensureOverlayRoot — 01-15가 `--overlay-scale`을 두는 곳)
    - tests/e2e/zoom.e2e.ts (01-15의 확대 시험 방법 — 01-15 산출물)
    - src/entrypoints/content.ts (applyBundle — Task 1)
    - docs/design/SYSTEM.md ("크기" 절)
  </read_first>
  <behavior>
    - e2e `mode-panel`: 0 → 4 → 하위 판 "조작 방식", 카드 "1 자석 커서"(지금 방식이면 선택 표시), "2 번호표", "3 자동 순서 강조" → 2 → 지금 묶음 `mode === 'hints'`, 모드 표시에 "조작 방식을 번호표로 바꿨어요", 커서를 버튼 위에 올려도 테두리가 없다
    - e2e `display-scale`: 좋은 날 `displayScale: 1`, 힘든 날 `1.5`로 준비 → 번호표 `.hint-label` 폭을 재고 힘든 날로 바꾼 뒤 다시 재면 1.5배(±2px)
    - e2e `display-scale`: 힘든 날(1.5)에서 페이지 확대를 125%로 바꿔도 번호표의 화면 크기(CSS 폭 × 확대 비율)가 확대 전과 같다(±2px)
  </behavior>
  <action>
RED 먼저: behavior 셋을 `condition.e2e.ts`(`describe('mode-panel')`, `describe('display-scale')`)에 더하고 실패 확인 후 RED 커밋.

`content.ts`(맨 위): `registerCommand('mode-switch', …)` → `pushPanel({ title: '조작 방식', cards: [1 자석 커서, 2 번호표, 3 자동 순서 강조] })`, 지금 방식 `selected`. 고르면 `setConditionMode` 요청 → 닫고 `showTransientMessage('조작 방식을 번호표로 바꿨어요', 2000)`(고른 이름으로).

`mode-indicator.ts`(D-29, SYSTEM.md 크기): 01-15가 `--overlay-scale`을 `1/확대 비율`로 두는 곳을 `displayScale / 확대 비율`로 바꾼다 — 배율 두 값(확대 비율, 표시 크기)을 들고 한 함수 `applyOverlayScale()`에서 계산해 호스트에 쓴다. `content.ts`의 `applyBundle()`이 표시 크기를 넘기고, 01-15의 확대 변경 경로는 확대 비율을 넘긴다. 번호표·테두리·모드 표시·확인 화면·토스트·명령판은 이미 `--overlay-scale`을 곱하므로(01-15) 새 CSS는 명령판(`palette.ts`)이 그 변수를 곱하는지만 확인해 빠졌으면 더한다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/condition.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "8 passed", or "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/zoom.e2e.ts tests/e2e/palette.e2e.ts</automated>
    <fails_when>non-zero exit, or any failed test (regression in zoom-independent sizing or palette)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/page/overlay/mode-indicator.ts`에 `applyOverlayScale`와 `displayScale`이 있다
    - `src/page/overlay/palette.ts`에 `--overlay-scale`이 있다
    - `src/entrypoints/content.ts`에 `registerCommand('mode-switch'`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/condition.e2e.ts` 8 passed, zoom·palette 회귀 없음
  </acceptance_criteria>
  <done>명령판에서 조작 방식을 바꾸고, 묶음의 표시 크기가 확대와 상관없이 오버레이 크기를 정한다. RED·GREEN 커밋이 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- PERS-01: edge probe `unclassified — review manually`. 가정한 경계:
  - "조작 방식 바꾸기"(첫 장 4)는 지금 고른 묶음의 방식만 바꾼다(다른 묶음은 그대로). 묶음 값 전체 편집은 설정 화면(03-13).
  - 컨디션 전환은 계정 동기화로 다른 PC에도 따라간다(D-04 컨디션 모드 = sync). 다른 PC 확인은 사람 확인(03-16 human-check).
  - 표시 크기 선택지는 100~200%(스키마 1~2). 명령판 판 자체도 같은 배율을 곱한다(SYSTEM.md "번호표·테두리·모드 표시·카드").

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content·확장 페이지 → writer | 묶음 전환 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Tampering | `setActiveCondition`/`setConditionMode` | medium | mitigate | zod enum만 받고 writer가 결과를 `SettingsV2`로 다시 검사, 실패면 쓰지 않는다 |
| T-03-18 | Denial of Service | 분당 쓰기 한도 | low | mitigate | 01-13의 쓰기 합치기·분당 100회 상한을 그대로 탄다(같은 키 연속 전환은 합쳐진다) |
| T-03-19 | Denial of Service | 표시 크기 | low | accept | 범위 1~2로 묶여 오버레이가 화면을 덮을 만큼 커지지 않는다 |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/condition.e2e.ts` 8 passed
- zoom·palette 회귀 없음, `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 명령판에서 좋은 날 / 힘든 날 묶음(조작 방식·떨림 간격·머무르기 시간·강조 속도·표시 크기)을 한 번에 바꾼다(PERS-01)
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/e2e/condition.e2e.ts`
- 심볼: 저장 요청 `setActiveCondition`, `setConditionMode`, content `applyBundle()`, overlay `applyOverlayScale()`, 명령 `condition`·`mode-switch` 등록
- 명령판 카드 속성: `selected`, `detail`

<output>
Create `.planning/phases/03-navigation-input-condition/03-05-SUMMARY.md` when done
</output>
