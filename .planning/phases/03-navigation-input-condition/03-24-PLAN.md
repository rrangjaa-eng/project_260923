---
phase: 03-navigation-input-condition
plan: 24
type: execute
wave: 19
depends_on: [03-22, 03-23]
files_modified:
  - src/entrypoints/onboarding/index.html
  - src/entrypoints/onboarding/main.ts
  - src/entrypoints/background.ts
  - src/types/chrome.d.ts
  - tests/e2e/fixtures.ts
  - tests/e2e/onboarding.e2e.ts
  - src/shared/messages.ts
  - src/entrypoints/content.ts
  - src/entrypoints/options/main.ts
autonomous: true
requirements: [PERS-02]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "확장을 처음 설치하면 맞춤 설정 탭이 저절로 열리고 세 단계(① 키 안내 ② 떨림 간격 재기 ③ 머무르기 시간 재기)를 차례로 보여 준다"
    - "② 단계에서 큰 버튼을 스페이스바로 여러 번 누르면 같은 키가 의도치 않게 다시 눌린 간격이 재어져 떨림 간격이 정해진다"
    - "③ 단계에서 큰 버튼 위에 커서를 올려 멈췄다고 느낄 때 스페이스바를 누르면 편한 머무르기 시간이 재어진다"
    - "잰 값으로 '좋은 날'(잰 값)과 '힘든 날'(잰 값 ×1.5) 묶음의 떨림 간격·머무르기 시간이 정해지고, 끝 화면에서 Enter로 저장한다. 두 묶음의 조작 방식(힘든 날 = 자동 순서 강조)은 그대로다"
    - "단계마다 Esc로 건너뛸 수 있고 건너뛴 단계는 기본값(Phase 2가 맞춘 defaultSettings 값)을 쓴다"
    - "나중에 명령판 '맞춤 설정 다시 하기'(셋째 장 4)나 설정 화면 '9 맞춤 설정 다시 하기'로 다시 한다"
    - "맞춤 설정 화면의 Enter·Esc도 떨림 걸러내기를 거친다(재는 동안의 스페이스바만 걸러내지 않고 그대로 잰다)"
  artifacts:
    - path: "src/entrypoints/onboarding/index.html"
      provides: "처음 설치 맞춤 설정 확장 페이지 진입점"
    - path: "src/entrypoints/onboarding/main.ts"
      provides: "세 단계 화면·재기·저장"
    - path: "tests/e2e/onboarding.e2e.ts"
      provides: "설치 때 열림·재기·건너뛰기·저장·다시 하기 시험"
  key_links:
    - from: "src/entrypoints/background.ts"
      to: "src/entrypoints/onboarding/index.html"
      via: "runtime.onInstalled(reason 'install') → tabs.create(onboarding.html), tab/command 're-onboard'도 같은 곳"
      pattern: "onboarding.html"
    - from: "src/entrypoints/onboarding/main.ts"
      to: "src/core/calibration.ts"
      via: "잰 간격·머무르기 표본 → computeTremorInterval·computeDwellMs → bundlesFromCalibration → updateCondition 두 번"
      pattern: "bundlesFromCalibration"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
처음 설치 맞춤 설정(D-30, D-31)을 만든다: 세 단계로 떨림 간격과 머무르기 시간을 재어 두 컨디션 묶음의 기본값을 정하고(계산 규칙은 03-23의 `calibration.ts`), 명령판·설정 화면에서 다시 한다.

Purpose: PERS-02. 이용자마다 다른 떨림에 맞춘 출발값.
Output: 맞춤 설정 확장 페이지, 설치 때 열기, 다시 하기 입구 둘, fixture 조정, `onboarding.e2e.ts`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-01-SUMMARY.md
@.planning/phases/03-navigation-input-condition/03-17-SUMMARY.md
@.planning/phases/03-navigation-input-condition/03-19-SUMMARY.md
@.planning/phases/03-navigation-input-condition/03-23-SUMMARY.md
@docs/design/SYSTEM.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성·새 권한 없음.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시, `model` 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(03-24): …` → 최소 구현 → GREEN 커밋 `feat(03-24): …`. 본문은 한국어.
- 완료 판정은 `CI=true` e2e. 저장 쓰기는 writer에만(설정 화면 전용 op `updateCondition` — 03-19).
- 화면은 SYSTEM.md "처음 설치 맞춤 설정" 템플릿(03-17)과 tokens.css만(D-36, D-38), 카피 규칙(D-37).
- 계산 규칙(`computeTremorInterval`·`computeDwellMs`·`bundlesFromCalibration`)은 03-23이 단위 시험으로 고정했다. 이 계획은 `src/core/calibration.ts`를 고치지 않고 가져다 쓴다.
- `03-RESEARCH.md`(623줄)·`03-PATTERNS.md`는 통째로 읽지 않는다 — `<read_first>`가 가리킨 절만 Grep으로 줄을 찾아 범위로 읽는다(CLAUDE.md '500줄 이상은 range 필수'. 계획 하나의 문맥 예산을 지키려고 `<context>`에서 뺐다).

## 전제 (아직 실행 전인 Phase 1 계획에서 온 가정)

- [01-16 가정] IBM Plex Sans KR 서체 파일(`fonts/*.woff2`)을 확장 페이지가 `@font-face`로 쓴다.
- [Phase 2 가정] 건너뛸 때 쓰는 기본값 = 지금 `defaultSettings()`의 좋은 날 값 — 03-01 SUMMARY "전제 확인"에 적힌 Phase 2 결과 값. 재는 범위(떨림 150~1000ms, 머무르기 400~3000ms)가 Phase 2 결과와 크게 다르면 멈추고 보고한다.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 맞춤 설정 페이지 — 세 단계, 재기, 건너뛰기, 저장, 설치 때 열기</name>
  <files>src/entrypoints/onboarding/index.html, src/entrypoints/onboarding/main.ts, src/entrypoints/background.ts, src/types/chrome.d.ts, tests/e2e/fixtures.ts, tests/e2e/onboarding.e2e.ts</files>
  <read_first>
    - docs/design/SYSTEM.md ("처음 설치 맞춤 설정" 템플릿 — 03-17)
    - src/entrypoints/options/main.ts, src/entrypoints/options/ui.ts (확장 페이지 골격·tokens·서체·파이프라인 사용 — 같은 방식)
    - src/core/calibration.ts (03-23), src/core/keys.ts (keyGuideLines)
    - src/page/input/pipeline.ts (createInputPipeline — Enter·Esc 처리)
    - src/entrypoints/background.ts (onInstalled), src/types/chrome.d.ts
    - tests/e2e/fixtures.ts (context·serviceWorker fixture)
    - .planning/phases/03-navigation-input-condition/03-RESEARCH.md — "### Pattern 10: 처음 설치 3단계 맞춤 설정" 절만(Grep으로 줄을 찾아 범위로 읽는다)
  </read_first>
  <behavior>
    - e2e: 새로 설치된 확장(시험 context)에서 맞춤 설정 탭(`onboarding.html`)이 저절로 열리고 "맞춤 설정", "1/3", 키 안내 줄, "브라우저 알림 창은 Enter=확인, Esc=취소"가 보인다
    - e2e: Enter → "2/3" → 스페이스바 8번(앞 4번은 120ms 뒤 한 번 더 눌러 다시 눌림을 흉내) → "떨림 간격 0.17초"
    - e2e: Enter → "3/3" → 5번 동안 커서를 큰 버튼 위에 올리고 900ms 뒤 스페이스바 → "머무르기 0.90초"~"1.00초"(시험 시간 흔들림 허용)
    - e2e: Enter → 끝 화면에 "좋은 날: 떨림 간격 0.17초 · 머무르기 …"와 "힘든 날: 떨림 간격 0.26초 · …" → Enter → `conditions.good.tremorIntervalMs === 170`, `conditions.good.dwellMs`가 900~1000, `conditions.hard.tremorIntervalMs === 255`, `conditions.hard.dwellMs === Math.round(good.dwellMs × 1.5)`, 토스트 "저장했어요"
    - e2e: 세 단계 모두 Esc → 끝 화면 값이 `defaultSettings()` 좋은 날 값 → Enter → 그 값으로 저장
    - e2e: 끝 화면에서 Esc → 저장하지 않는다(설정 그대로)
    - e2e: 1단계에서 Enter를 100ms 간격으로 두 번 → "2/3"에서 멈춘다(떨림 걸러내기)
  </behavior>
  <action>
RED 먼저: behavior 일곱을 `tests/e2e/onboarding.e2e.ts`(`describe('onboarding')`, fixture 옵션 `keepOnboarding: true`)에 쓰고 실패 확인 후 RED 커밋. `fixtures.ts`: 설치 때 맞춤 설정 탭이 열리므로, 기본은 serviceWorker 준비 뒤 `onboarding.html` 탭이 나타나면(최대 5초 기다림) 닫고 시작한다. `keepOnboarding: true`면 닫지 않는다(기존 시험이 탭 하나를 가정하므로).

`background.ts`: `runtime.onInstalled`에서 `details.reason === 'install'`이면 `chrome.tabs.create({ url: chrome.runtime.getURL('/onboarding.html') })`(update 때는 열지 않는다, 01-14의 새로 넣기는 그대로). `chrome.d.ts`: onInstalled `details.reason`, `tabs.getCurrent`.

`src/entrypoints/onboarding/index.html` + `main.ts`(D-30, 템플릿대로): tokens·서체, 설정은 읽기만. 탐색 키(Enter·Esc)는 `createInputPipeline`의 `onKey`(D-02 — 걸러냄). 재기는 파이프라인보다 먼저 등록한 `window` capture `keydown`·`pointerenter` 원본 기록으로 한다(재기 대상 스페이스바는 걸러내면 안 된다; `repeat`인 자동 반복은 뺀다).
- ① 키 안내: `keyGuideLines(keymap)` 두 줄을 큰 카드로. Enter·Esc 모두 다음 단계.
- ② 떨림 간격: 큰 버튼(높이 `--target-confirm` 이상) "스페이스바로 한 번 누르세요". 한 번에 첫 스페이스바를 받은 뒤 1.5초 동안 들어온 스페이스바를 다시 눌림으로 보고 첫 누름과의 간격을 모은다, 8번("3/8번째"), 사이에는 버튼 테두리가 `--success`로 바뀌어 다음 준비를 알린다. Esc = 건너뛰기(기본값).
- ③ 머무르기: 큰 버튼이 세 자리(왼쪽·가운데·오른쪽)를 돌아가며 5번, 커서가 버튼에 처음 들어온 때부터 버튼 안에서 스페이스바를 누를 때까지의 시간을 모은다(버튼 밖 스페이스바는 무시). Esc = 건너뛰기.
- 끝(D-31): 모은 표본을 03-23의 `computeTremorInterval`·`computeDwellMs`로 바꾸고(건너뛴 값의 fallback은 `defaultSettings()`의 좋은 날 값), 두 묶음 값을 초 글자(`Math.round(ms / 10) / 100`을 소수 둘째 자리까지)로, [Enter 저장] [Esc 저장하지 않고 닫기]. 저장 = `bundlesFromCalibration` → `updateCondition` 두 번(좋은 날·힘든 날, 03-19의 설정 화면 전용 op) → 토스트 "저장했어요" → Esc로 탭 닫기(`chrome.tabs.getCurrent` → `remove`). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/onboarding.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "7 passed", or "No tests found"</fails_when>
    <automated>CI=true pnpm test</automated>
    <fails_when>non-zero exit, or any failed test (the install-time onboarding tab broke existing e2e via the fixture)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/entrypoints/background.ts`에 `reason === 'install'`과 `onboarding.html`이 있다
    - `src/entrypoints/onboarding/main.ts`에 `createInputPipeline`과 `bundlesFromCalibration`이 있고 hex 색 리터럴이 없다
    - `tests/e2e/fixtures.ts`에 `keepOnboarding`이 있다
    - `CI=true pnpm exec playwright test tests/e2e/onboarding.e2e.ts` 7 passed, `CI=true pnpm test` 통과
  </acceptance_criteria>
  <done>설치하면 맞춤 설정이 열려 두 값을 재고 두 묶음을 정하며, 단계마다 건너뛸 수 있다. RED·GREEN 커밋이 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 다시 하기 입구 — 명령판 셋째 장 4, 설정 화면 9</name>
  <files>src/shared/messages.ts, src/entrypoints/background.ts, src/entrypoints/content.ts, src/entrypoints/options/main.ts, tests/e2e/onboarding.e2e.ts</files>
  <read_first>
    - src/shared/messages.ts, src/entrypoints/background.ts (`tab/command` — 03-01·03-02)
    - src/entrypoints/content.ts (registerCommand)
    - src/entrypoints/options/main.ts (절 9 자리 — 03-17)
  </read_first>
  <behavior>
    - e2e `again`: 연습 사이트에서 0 → 9 → 9 → 4 → 새 탭으로 `onboarding.html`이 "1/3"으로 열린다
    - e2e `again`: 설정 화면에서 9 → 새 탭으로 `onboarding.html`이 열린다
  </behavior>
  <action>
RED 먼저: behavior 둘을 `onboarding.e2e.ts`(`describe('again')`)에 더하고 실패 확인 후 RED 커밋.

`messages.ts`: `tab/command`의 command에 `'re-onboard'`. `background.ts`: `re-onboard`면 `chrome.tabs.create({ url: getURL('/onboarding.html') })`(frameId 0 확인은 그대로). `content.ts`: `registerCommand('re-onboard', …)` → `tab/command` 뒤 명령판 닫기(셋째 장 4 자리는 D-06 고정 자리 그대로 — 자리를 옮기지 않는다). `options/main.ts`: 절 9 "맞춤 설정 다시 하기"를 고르면 같은 주소로 새 탭(D-30). 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/onboarding.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "9 passed", or "No tests found"</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/shared/messages.ts`에 `'re-onboard'`, `src/entrypoints/content.ts`에 `registerCommand('re-onboard'`가 있다
    - `CI=true pnpm exec playwright test tests/e2e/onboarding.e2e.ts` 9 passed
  </acceptance_criteria>
  <done>명령판과 설정 화면에서 맞춤 설정을 다시 한다. RED·GREEN 커밋이 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- PERS-02: edge probe `unclassified — review manually`. 가정한 경계(Claude's Discretion 7·8):
  - 맞춤 설정은 확장 페이지 새 탭(`onboarding.html`)이고 설치(install) 때만 저절로 열린다(업데이트 때는 열지 않는다).
  - 계산 경계(떨림 간격 90% 값 + 50ms·150~1000ms, 머무르기 가운데 값·400~3000ms, 힘든 날 ×1.5)는 03-23이 고정했다.
  - 두 묶음의 조작 방식·강조 속도·표시 크기는 바꾸지 않는다(힘든 날 기본 방식 = 자동 순서 강조, 03-03 승인·구현).
  - "건너뛴 단계는 기본값"의 기본값은 `defaultSettings()`(Phase 2가 맞춘 값)이다 — 다시 하기에서 건너뛰면 이용자가 고친 값 대신 기본값이 끝 화면에 나오고, Esc로 저장하지 않을 수 있다.
  - 재는 스페이스바는 떨림 걸러내기를 거치지 않는다(걸러내면 잴 수 없다). 화면을 넘기는 Enter·Esc는 거친다(D-02).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 맞춤 설정 페이지(확장 페이지) → writer | 두 묶음 값 저장 |
| content → SW | `tab/command re-onboard` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-57 | Denial of Service | 잘못 잰 값 | medium | mitigate | 범위 자르기·표본 모자람 기본값은 03-23(단위 시험). 이 계획은 끝 화면에서 값을 보여 준 뒤 Enter로만 저장한다(Esc = 저장 안 함, e2e) |
| T-03-58 | Elevation of Privilege | `updateCondition` | medium | mitigate | 확장 페이지 요청만(`isExtensionPage`, 03-18) — 맞춤 설정 페이지는 확장 페이지 |
| T-03-59 | Spoofing | `re-onboard` | low | mitigate | `tab/command`와 같은 frameId 0·`sender.tab.id` 규칙, 여는 주소는 확장 자기 페이지로 고정 |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/onboarding.e2e.ts` 9 passed
- `CI=true pnpm test` 통과(설치 때 열리는 탭이 기존 시험을 깨지 않음)
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 처음 설치 맞춤 설정으로 떨림 간격·머무르기 시간이 재어져 두 컨디션 묶음 기본값이 정해지고, 명령판·설정 화면에서 다시 한다(PERS-02)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/entrypoints/onboarding/index.html`, `src/entrypoints/onboarding/main.ts`, `tests/e2e/onboarding.e2e.ts`
- 심볼: 명령 `re-onboard` 등록, fixture 옵션 `keepOnboarding`
- 메시지: `tab/command`의 `re-onboard`

<output>
Create `.planning/phases/03-navigation-input-condition/03-24-SUMMARY.md` when done
</output>
