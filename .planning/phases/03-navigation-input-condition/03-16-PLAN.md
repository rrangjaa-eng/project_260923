---
phase: 03-navigation-input-condition
plan: 16
type: execute
wave: 16
depends_on: ["03-15"]
files_modified:
  - tests/e2e/privacy-sweep.e2e.ts
  - tests/e2e/dom-audit-03.e2e.ts
  - .planning/phases/03-navigation-input-condition/03-16-SUMMARY.md
autonomous: true
requirements: [NAV-02, CLICK-05, PERS-01, PERS-02, PERS-03, INPT-01, INPT-03, PRIV-01, PRIV-02, STOR-03, STOR-04]
estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Phase 3을 모두 실행한 코드가 싼 게이트(lint·typecheck·CI=true build)를 통과하고, 저장 쓰기는 writer 한 곳에만 있고, 확장 권한은 storage·scripting·tabs와 모든 사이트 host 권한 하나 그대로이며, 의존성은 Phase 3 시작 때(03-01 baseline-commit)와 같다"
    - "연습 사이트에서 Phase 3 기능(최근 값 기록, 입력 줄이기 카드, 양식 한 장 보기, 설정 파일 내보내기)을 모두 거쳐도 민감칸 일곱 칸의 값이 동기화·이 PC·세션 저장소와 내보낸 파일 어디에도 없고, 업무 칸 값(적요·법인카드 구분)은 그대로 기록된다"
    - "별도 감사 에이전트가 Phase 3 화면 전부(명령판과 하위 판, 자동 순서 강조, 입력 줄이기 카드, 양식 한 장 보기, 칸 고르기·확인 화면, 설정 화면, 맞춤 설정, 확장 아이콘 메뉴 경고 카드, 토스트)를 SYSTEM.md·tokens.css 기준으로 CI=true에서 DOM 실측해 통과시켰다"
    - "힘든 날 표시 크기 1.25에서 명령판 카드가 1.25배로 커지고, 브라우저 확대 110%·200%에서는 크기가 그대로다"
    - "확인 화면이 아닌 도우미 화면(명령판, 카드, 양식 한 장 보기)은 사이트를 덮는 어두운 면을 만들지 않는다"
    - "전체 게이트(CI=true pnpm test → lint → typecheck)가 한 번에 통과한다"
    - {statement: "엣지·웨일(실제 브라우저)에서 명령판·입력 줄이기 카드·양식 한 장 보기·설정 화면·맞춤 설정이 크롬과 같게 동작한다", verification: backstop}
    - {statement: "크롬에서 내보낸 설정 파일을 엣지·웨일에서 가져오면 같은 설정으로 동작한다", verification: backstop}
    - {statement: "같은 브라우저 계정의 다른 PC에서 키 배치·컨디션 묶음·카드 자리·자주 쓰는 문구·민감칸 설정이 그대로 적용되고, 최근 입력 값은 넘어가지 않는다", verification: backstop}
  prohibitions:
    - statement: "도우미 코드는 바깥(인터넷·회사 시스템)으로 어떤 요청도 보내지 않는다 — 연습 사이트 시험 중 막힌 요청 목록도 비어 있다"
      requirement_id: PRIV-01
      category: privacy
      status: resolved
      verification: judgment
    - statement: "Phase 3은 새 확장 권한(downloads 등)과 새 의존성을 더하지 않는다. 설정 화면·맞춤 설정 페이지는 웹 페이지가 열 수 있는 자원(web_accessible_resources)에 들어가지 않는다"
      requirement_id: STOR-04
      category: security
      status: resolved
      verification: judgment
  artifacts:
    - path: "tests/e2e/privacy-sweep.e2e.ts"
      provides: "Phase 3 기능 전체를 거친 뒤 저장소 세 곳·화면·내보낸 파일에 민감값이 없는지 보는 종합 시험"
    - path: "tests/e2e/dom-audit-03.e2e.ts"
      provides: "별도 감사 에이전트가 쓴 Phase 3 화면 DOM 실측 감사"
  key_links:
    - from: "tests/e2e/privacy-sweep.e2e.ts"
      to: "src/worker/storage-writer.ts"
      via: "민감칸 입력 → recordRecentValue 거절 → serviceWorker에서 sync·local·session 전체 읽기 → 민감값 글자 없음"
      pattern: "recent:"
    - from: "tests/e2e/dom-audit-03.e2e.ts"
      to: "docs/design/SYSTEM.md"
      via: "SYSTEM.md 색·형태·크기·간격·모션·템플릿 값을 기준 집합으로 삼아 계산된 스타일 실측"
      pattern: "getComputedStyle"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
Phase 3 게이트(D-41): 싼 게이트와 정적 점검 → 민감값 종합 점검 시험 → 별도 에이전트의 독립 DOM 감사 → 수정 → 전체 게이트 한 번. 자동으로 할 수 없는 엣지·웨일·다른 PC 확인은 end-of-phase human-check로 모은다(D-40).

Purpose: 15개 계획이 따로 통과한 기능들이 한데 모였을 때도 SYSTEM.md 일관성(D-36, D-37, D-38), 민감값 보호(PRIV-01, D-26, D-35), 권한·의존성·외부 전송 금지가 지켜지는지 한 번에 판정한다.
Output: `privacy-sweep.e2e.ts`, 감사 에이전트가 쓴 `dom-audit-03.e2e.ts`, 어긋남 수정, 전체 게이트 결과, 요구사항 ↔ 시험 대조표(SUMMARY).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-01-SUMMARY.md
@docs/design/SYSTEM.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성·새 권한 없음(필요해 보이면 멈추고 `checkpoint:decision`으로 승인 요청).
- 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다. 수정이 코드 동작을 바꾸면 `test-driven-development`(회귀 시험 먼저). 서브에이전트 프롬프트에도 스킬을 명시하고 `model`을 명시한다.
- 시험은 로컬 연습 사이트(`http://practice.test`)에서만 한다. 실제 회사 시스템·인터넷으로 나가지 않는다(fixture의 `blockedRequests`).
- 이 계획의 새 시험 둘은 기능 구현이 아니라 검증 시험이라 RED 단계가 없다(TDD 대상 아님). 시험이 실패하면 시험을 약하게 고치지 않고 원인 파일을 고친다.
- 커밋: 제목 영어 접두어(`test(03-16): …`, `fix(03-16): …`), 본문 한국어.

## 전제 (아직 실행 전인 Phase 1 계획에서 온 가정)

- [01-16 가정] `tests/e2e/dom-audit.e2e.ts`(Phase 1 감사)가 있고, 감사 방식(색 집합·radius 집합·`box-shadow` 없음·서체·대비·확대·움직임 줄이기·`blockedRequests`)의 본보기가 된다. 서체 IBM Plex Sans KR이 오버레이·확장 페이지에 등록되어 있다.
- [01-13~01-15 가정] 확대 보정(`--overlay-scale`)과 `zoom.e2e.ts`가 있다 — 03-05가 표시 크기를 곱했다.
- [03-01 결과] `03-01-SUMMARY.md`에 `baseline-commit: <40자 sha>` 줄이 있다(의존성 비교 기준).

<tasks>

<task type="auto">
  <name>Task 1: 싼 게이트·정적 점검, 그리고 민감값 종합 점검 시험</name>
  <files>tests/e2e/privacy-sweep.e2e.ts</files>
  <precondition>`.planning/phases/03-navigation-input-condition/03-01-SUMMARY.md`에 `baseline-commit:` 뒤 40자 sha 한 줄이 있고, 03-02~03-15 SUMMARY가 모두 있다</precondition>
  <read_first>
    - CLAUDE.md ("프론트엔드 — 화면 검증 순서", "코딩 규칙")
    - .planning/phases/03-navigation-input-condition/03-01-SUMMARY.md (baseline-commit 줄)
    - wxt.config.ts (권한 목록)
    - tests/e2e/fixtures.ts (context·serviceWorker·`blockedRequests`·`keepOnboarding`)
    - tests/practice-site/sensitive.html (칸 이름·라벨 — 03-08)
    - tests/e2e/recent-values.e2e.ts (칸에 치고 벗어나기, 저장소 읽기 방식 — 03-08)
    - tests/e2e/input-cards.e2e.ts (입력 줄이기 카드 읽기 — 03-09), tests/e2e/form-view.e2e.ts (양식 한 장 보기 열기 — 03-11), tests/e2e/settings-file.e2e.ts (내보낸 파일 받기 — 03-14)
  </read_first>
  <action>
1. 싼 게이트(D-41): `pnpm lint && pnpm typecheck && CI=true pnpm build`. 실패하면 `systematic-debugging`으로 원인을 찾아 고치고 다시.

2. 정적 점검(verify 둘째~여섯째 줄을 그대로 돌린다):
- 외부 전송 금지(PRIV-01): 01-16 Task 3과 같은 네트워크 API 목록으로 `src/`를 grep해 아무것도 없어야 한다.
- 저장 쓰기 한 곳: 저장소 세 곳(sync·local·session)의 쓰기·지우기 호출이 `src/worker/storage-writer.ts` 밖에 없어야 한다.
- 권한: 빌드된 `.output/chrome-mv3/manifest.json`의 `permissions`가 storage·scripting·tabs 셋, `host_permissions`가 모든 사이트 하나, `web_accessible_resources`에 설정·맞춤 설정·메뉴 페이지가 없다. 어긋나면 기준을 넓히지 말고 멈춰서 보고한다.
- 의존성: `package.json`·`pnpm-lock.yaml`이 03-01 baseline-commit과 같다.
- Phase 3 시험 파일이 모두 있다(e2e 20개, 단위 10개 — verify 여섯째 줄 목록). 없는 파일이 있으면 그 계획이 실행되지 않은 것이므로 멈추고 보고한다.

3. `tests/e2e/privacy-sweep.e2e.ts`(`describe('sweep')`, D-26, D-35, PRIV-01) — 시험용 가짜 값만 쓴다. 민감값 일곱: 비밀번호 칸 "Pw-sweep-7788", 인증번호 "Q7X-OTP-5521", 보안카드 번호 "SC-7731-Z", `userPw` 칸 "sweep-pass-01", 주민등록번호 "900101-1234567", 카드번호 "4111 1111 1111 1111", 계좌번호 "123-456-789012"(숫자 모양 셋은 붙여 쓴 글자 "9001011234567"·"4111111111111111"·"123456789012"도 함께 찾는다). 업무 값(대조군): 적요 "거래처 미팅 식대", 법인카드 구분 "개인".
- 시험 1 `record`: `sensitive.html`에서 위 칸마다 키보드로 치고 Tab으로 벗어난다 → serviceWorker에서 저장소 세 곳 전체를 `get(null)`로 읽어 JSON 글자로 합친 것에 민감값 글자가 하나도 없고, 대조군 두 값은 있다.
- 시험 2 `cards`: 민감칸마다 초점을 두면 입력 줄이기 판에 번호 카드 없이 "민감칸이라 기록하지 않아요"만 있고 판 글자에 민감값이 없다. 적요 칸에서는 "거래처 미팅 식대" 카드가 뜬다(대조군).
- 시험 3 `form`: 민감칸에 값을 채운 뒤 명령판 0 → 9 → 6(양식 한 장 보기) → 판 글자에 민감값이 없고, 민감칸 이름은 목록 끝 "원래 화면에서 입력하세요"에 있고, 적요 값은 판에 있다.
- 시험 4 `export`: 시험 1을 거친 뒤 설정 화면에서 "1 모든 설정 내보내기" → 받은 파일 글자에 민감값이 없고 `recent:`도 없으며 JSON으로 읽힌다(03-14 시험과 같은 방식으로 파일을 받는다).
- 네 시험 모두 끝에서 `blockedRequests`가 비어 있다.
시험이 실패하면 `systematic-debugging`으로 원인 계획(03-08·03-09·03-11·03-14)의 파일을 찾아 회귀 시험을 먼저 더한 뒤 고친다(시험 기준은 낮추지 않는다). 고친 파일·원인은 SUMMARY 편차에 적는다. 통과하면 커밋 `test(03-16): add phase 3 privacy sweep`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && CI=true pnpm build</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>test -z "$(grep -rnE '\bfetch\(|XMLHttpRequest|WebSocket|sendBeacon|EventSource' src/)"</automated>
    <fails_when>non-zero exit (a network API call exists in src/)</fails_when>
    <automated>test -z "$(grep -rnE 'storage\.(sync|local|session)\.(set|remove|clear)\(' src/ | grep -v '^src/worker/storage-writer\.ts:')"</automated>
    <fails_when>non-zero exit (a storage write/remove/clear call exists outside src/worker/storage-writer.ts)</fails_when>
    <automated>node -e "const m=JSON.parse(require('fs').readFileSync('.output/chrome-mv3/manifest.json','utf8'));const p=[...(m.permissions??[])].sort().join(',');const h=m.host_permissions??[];const w=(m.web_accessible_resources??[]).flatMap(x=>x.resources??[]);if(p!=='scripting,storage,tabs'||h.length!==1||!/all_urls/.test(h[0])||w.some(r=>/options|onboarding|popup/.test(r))){console.error(JSON.stringify({p,h,w}));process.exit(1)}"</automated>
    <fails_when>non-zero exit (permissions differ from storage/scripting/tabs, host permissions are not the single all-urls entry, or an extension page is web-accessible), or the manifest file is missing</fails_when>
    <automated>BASE=$(grep -oE 'baseline-commit: [0-9a-f]{40}' .planning/phases/03-navigation-input-condition/03-01-SUMMARY.md | head -1 | cut -d' ' -f2) && test -n "$BASE" && git diff --quiet "$BASE" -- package.json pnpm-lock.yaml</automated>
    <fails_when>non-zero exit (baseline-commit line missing, or package.json / pnpm-lock.yaml changed during Phase 3 without an approved dependency decision)</fails_when>
    <automated>for f in palette palette-frames palette-panels settings-v2 auto-cycle condition scroll nav-back pin dblclick recent-values input-cards undo sensitive-config form-view options options-sections settings-file sync-quota onboarding; do test -f "tests/e2e/$f.e2e.ts" || { echo "missing e2e $f"; exit 1; }; done; for f in palette-slots settings-v2 keys auto-hint-cycle esc-back sensitive field-key recent-values settings-file calibration; do test -f "tests/unit/$f.test.ts" || { echo "missing unit $f"; exit 1; }; done</automated>
    <fails_when>non-zero exit (a Phase 3 test file is missing — its plan did not run)</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/privacy-sweep.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "4 passed", or "No tests found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm lint && pnpm typecheck && CI=true pnpm build` 통과
    - `src/`에 네트워크 API 호출이 없고, 저장 쓰기·지우기 호출은 `src/worker/storage-writer.ts`에만 있다
    - 빌드된 manifest 권한이 storage·scripting·tabs, host 권한 하나, 확장 페이지는 web-accessible이 아니다
    - `package.json`·`pnpm-lock.yaml`이 03-01 baseline-commit과 같다
    - `tests/e2e/privacy-sweep.e2e.ts`에 `describe('sweep')`와 `blockedRequests`가 있고 `CI=true pnpm exec playwright test tests/e2e/privacy-sweep.e2e.ts` 4 passed
  </acceptance_criteria>
  <done>싼 게이트와 정적 점검이 통과했고, Phase 3 기능 전체를 거쳐도 민감값이 어디에도 남지 않는 것이 한 시험으로 확인되었다. 커밋이 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 독립 DOM 감사(별도 에이전트) → 어긋남 수정</name>
  <files>tests/e2e/dom-audit-03.e2e.ts</files>
  <read_first>
    - CLAUDE.md ("프론트엔드 — 화면 검증 순서", "모델 선택")
    - docs/design/SYSTEM.md (색·글자·간격·형태·크기·모션·오버레이 템플릿·상태 표·카피 규칙, 03-09·03-12가 더한 템플릿), docs/design/tokens.css, docs/design/DECISIONS.md, docs/DESIGN.md (§4, §6 품질 바닥)
    - tests/e2e/dom-audit.e2e.ts (Phase 1 감사 본보기 — 01-16)
    - tests/e2e/fixtures.ts
  </read_first>
  <action>
1. 감사 맡기기(D-41): **이 계획 실행자가 아닌 별도 감사 에이전트**에게 Task 도구로 맡긴다 — `model: "opus"`(CLAUDE.md 모델 선택: 점검·검토는 Opus. 01-16은 sonnet이었지만 이 규칙을 따른다), 프롬프트에 `verification-before-completion` 스킬 호출과 "스크린샷 육안 판정 금지, DOM 계산값으로만 판정"을 명시한다. 감사 에이전트는 SYSTEM.md·tokens.css·DESIGN.md §6만 기준으로(구현 코드를 기준으로 삼지 않는다) `tests/e2e/dom-audit-03.e2e.ts`(`describe('audit-03')`)를 쓰고 `CI=true pnpm exec playwright test tests/e2e/dom-audit-03.e2e.ts`로 판정한다. 감사 항목(최소, 시험 16개 이상):
- 명령판(`targets.html`, 0): 3열 격자·카드 사이 16px, 카드 높이 56px 이상·radius 12, 큰 숫자 22px·700, 모든 카드에 키 칩(radius 6), 선택 카드 남색 3px, 키 안내 카드, 셋째 장까지 넘겨 카드 자리가 SYSTEM.md 고정 자리와 같음, "준비 중" 카드 글자 대비 4.5:1 이상.
- 모드 표시: "스페이스바 누르기, 0 명령판", 18px, 흰 후광 2px, 왼쪽 아래.
- 조작 방식·컨디션 하위 판(03-05): 같은 카드 규칙. 힘든 날(표시 크기 1.25)로 바꾸면 카드 높이·숫자 크기가 좋은 날의 1.25배(±1px).
- 자동 순서 강조(03-04): 강조 테두리 남색 5px·radius 8·후광 2px, 이동 80ms.
- 번호 고정·더블클릭 고르기 상태(03-07): 번호표 28×28 이상·radius 6, 상태 글자.
- 입력 줄이기 카드(03-09): 번호 카드 규칙, `Alt` 키 칩, 빈 상태 "자주 쓰는 문구 추가" 카드, 민감칸 상태 글자.
- 양식 한 장 보기(`form-long.html`, 03-11): 한 줄에 한 칸, 라벨 18px 이상, 칸 높이 56px 이상, 선택 목록 번호 카드, [Esc 원래 화면으로] 키 칩, 빈 상태·못 옮긴 칸 글자.
- 칸 고르기와 확인 화면 변형(03-10): 확인 카드 폭 600px(좁으면 화면 − 32px)·radius 14·남색 3px, 버튼 높이 64px, 1초 보호 막대, 뒤 `--scrim`.
- 어두운 면: 확인 화면(오버레이·설정 화면 확인 둘)만 사이트나 페이지를 덮는 면이 있고 명령판·카드·양식 한 장 보기에는 없다.
- 설정 화면(`options.html`, 03-12~03-14): 한 열 폭 `--dialog-width`, 줄 높이 56px 이상, 키 칩, `:focus-visible` 남색 3px, 설정 파일 절, 확인 창 규칙.
- 맞춤 설정(`onboarding.html`, 03-15): 큰 버튼 높이 64px 이상, 단계 글자, 끝 화면 버튼 키 칩.
- 확장 아이콘 메뉴(03-12, 03-14): "5 설정 열기" 카드 56px 이상, 용량 경고 카드 `--warning` 테두리.
- 토스트: 오른쪽 아래, 글자 15px 이상.
- 모든 화면 공통: 색은 tokens.css 값 집합 안(`color`·`background-color`·`border-*-color`·`outline-color`, 투명 제외), radius는 토큰 집합 안(0 포함), `box-shadow`는 모두 `none`, 서체 "IBM Plex Sans KR" 시작, 글자 15px 이상, 한글 `word-break: keep-all`, 글자 대비 4.5:1·UI 3:1 이상.
- 확대 110%·200%에서 명령판·입력 카드·양식 한 장 보기 크기 그대로. `reducedMotion: 'reduce'`에서 나타남·이동 transition 0(머무르기 원·보호 막대는 남음).
- 모든 시험 끝에서 `blockedRequests` 비어 있음.
감사 에이전트는 어긋남 목록(파일·부품·실측값·기준값)을 돌려주고, 시험 파일은 감사 에이전트가 커밋한다(커밋 본문에 "감사 에이전트(model: opus)가 작성" 문구).

2. 수정: 어긋남마다 해당 오버레이·확장 페이지 파일을 고친다(D-36, D-37). SYSTEM.md를 벗어나야 맞는 경우는 고치지 말고 멈춰서 DECISIONS.md 기록과 SYSTEM.md 수정 승인을 요청한다(DESIGN.md §4, D-38). 실행자는 감사 시험 파일을 고치지 않는다 — 기준이 틀렸다고 보이면 감사 에이전트에게 근거를 보내 다시 맡긴다. 고친 뒤 감사 시험과 싼 게이트만 다시 돌린다(전체 게이트는 Task 3에서 한 번). 커밋 `fix(03-16): …`, 고친 파일·실측값은 SUMMARY 편차에 적는다.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/dom-audit-03.e2e.ts</automated>
    <fails_when>non-zero exit, or "No tests found", or fewer than 16 tests passed</fails_when>
    <automated>L=$(git log --format=%H -- tests/e2e/dom-audit-03.e2e.ts) && G=$(git log --grep='감사 에이전트' --format=%H -- tests/e2e/dom-audit-03.e2e.ts) && test -n "$L" && test "$L" = "$G"</automated>
    <fails_when>non-zero exit (the audit file is uncommitted, or some commit touching it was not made by the audit agent)</fails_when>
    <automated>pnpm lint && pnpm typecheck</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/dom-audit-03.e2e.ts`가 있고 `describe('audit-03')`, `getComputedStyle`, `blockedRequests`, `reducedMotion`이 있다
    - 그 파일을 건드린 모든 커밋 본문에 "감사 에이전트"가 있다
    - `CI=true pnpm exec playwright test tests/e2e/dom-audit-03.e2e.ts` 16개 이상 통과
    - SUMMARY에 "감사 에이전트(model: opus)가 작성·판정", 어긋남 목록, 수정 결과가 적혀 있다
  </acceptance_criteria>
  <done>Phase 3 화면 전부가 별도 에이전트의 DOM 실측 감사로 SYSTEM.md 일관성을 통과했다.</done>
</task>

<task type="auto">
  <name>Task 3: 전체 게이트 한 번, 수동 확인 목록, 요구사항 대조</name>
  <files>.planning/phases/03-navigation-input-condition/03-16-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Phase 3 요구사항 19개 문장)
    - .planning/phases/03-navigation-input-condition/03-CONTEXT.md (D-40 수동 확인 목록, D-41)
    - 03-01~03-15 SUMMARY (각 계획의 시험 파일·통과 수)
  </read_first>
  <action>
1. 전체 게이트 한 번(D-41): `CI=true pnpm test`(단위 → e2e 전부, Phase 1 시험 포함) → `pnpm lint` → `pnpm typecheck`. 실패하면 `systematic-debugging`으로 원인을 찾고, 동작을 바꾸는 수정은 회귀 시험을 먼저 더한다. 실패한 시험 파일만 다시 돌려 통과를 본 뒤 전체 게이트를 다시 한 번 돌린다(통과한 게이트를 거듭 돌리지 않는다). 이 작업은 감사·종합 점검 시험 파일을 고치지 않는다. 게이트 실패 수정으로 바뀐 소스 파일은 SUMMARY 편차에 적는다.

2. 요구사항 대조: SUMMARY에 표 `| 요구사항 | 확인한 시험 파일 | 통과 수 |`를 19줄(CLICK-05, SAFE-06, STOR-03, STOR-04, NAV-01~06, INPT-01~03, PRIV-01~03, PERS-01~03)로 적는다. 각 줄은 03-01~03-16 SUMMARY의 시험 파일과 이번 전체 게이트 결과로 채운다. 자동 시험이 없는 부분(엣지·웨일, 브라우저 사이 파일 옮기기, 다른 PC 동기화)은 "수동 확인(human-check)"으로 적는다(D-40).

3. 다음 단계 안내를 SUMMARY 끝에 적는다: `/gsd-verify-work` → CLAUDE.md Post-build `/review` → `/qa` → `/cso`(민감칸·설정 파일 가져오기라는 외부 입력을 다뤄 필수) → `/ship`. 이 계획은 그 명령들을 대신하지 않는다.
  </action>
  <verify>
    <automated>CI=true pnpm test && pnpm lint && pnpm typecheck</automated>
    <fails_when>non-zero exit, or any test reported failed or flaky</fails_when>
    <human-check>
      <test>엣지와 웨일에 빌드한 확장(`.output/chrome-mv3`)을 개발자 모드로 불러와 연습 사이트에서: 0 명령판 세 장 넘기기, 입력칸 최근 값 카드(Alt+1), 양식 한 장 보기(0 → 9 → 6), 설정 화면 열기(메뉴 5), 맞춤 설정 다시 하기(0 → 9 → 9 → 4)</test>
      <expected>크롬과 같은 화면·같은 결과. 확대 110%·200%에서 카드 크기 그대로. 오류 알림 없음</expected>
      <why_human>Playwright 자동 시험은 Chromium만 띄운다. 엣지·웨일 실제 브라우저 확인은 수동 목록이다(D-40, Phase 1 D-29)</why_human>
    </human-check>
    <human-check>
      <test>크롬 설정 화면 "1 모든 설정 내보내기"로 받은 파일을 엣지(또 웨일) 설정 화면 "3 가져오기"로 가져온다. 키 배치 하나와 자주 쓰는 문구 하나를 크롬에서 바꿔 둔 뒤 한다</test>
      <expected>확인(1초 보호) 뒤 "가져왔어요", 바꾼 키 배치와 문구가 엣지·웨일에서 그대로 동작. 최근 입력 값은 넘어가지 않음</expected>
      <why_human>서로 다른 실제 브라우저 사이 파일 옮기기는 자동 시험 환경에 없다(STOR-04, D-34)</why_human>
    </human-check>
    <human-check>
      <test>같은 크롬 계정으로 로그인한 다른 PC에서 확장을 켜고 연습 사이트를 연다(첫 PC에서 키 배치·컨디션 묶음·카드 자리·문구·민감칸 설정을 바꾼 뒤)</test>
      <expected>바꾼 설정이 그대로 적용됨. 첫 PC의 최근 입력 값은 보이지 않음</expected>
      <why_human>chrome.storage.sync의 실제 계정 동기화는 자동 시험으로 만들 수 없다(D-35, 03-10·03-14 backstop)</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `CI=true pnpm test && pnpm lint && pnpm typecheck`가 한 번에 통과(실패·flaky 없음)
    - SUMMARY에 요구사항 19줄 대조표가 있고, 각 줄에 시험 파일 또는 "수동 확인(human-check)"이 적혀 있다
    - SUMMARY 끝에 `/gsd-verify-work` → `/review` → `/qa` → `/cso` → `/ship` 안내가 있다
  </acceptance_criteria>
  <done>Phase 3 전체 게이트가 CI=true로 한 번에 통과했고, 요구사항 19개가 시험 또는 수동 확인 목록에 대응되었다. 다음은 `/gsd-verify-work`와 CLAUDE.md Post-build다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- NAV-02, CLICK-05, PERS-01~03, INPT-01, INPT-03, PRIV-01, PRIV-02, STOR-03, STOR-04: edge probe `unclassified — review manually`. 이 계획은 기능을 더하지 않고 앞 계획들의 경계를 한데 모아 다시 잰다. 가정한 게이트 경계:
  - "민감값이 어디에도 없다"는 저장소 세 곳(sync·local·session) 전체 JSON, 입력 줄이기 판 글자, 양식 한 장 보기 판 글자, 내보낸 설정 파일 글자로 판정한다. 숫자 모양 값은 붙여 쓴 글자로도 찾는다. 메모리 안 되돌리기 값(03-10, WeakRef)은 저장소가 아니라 대상이 아니다.
  - 독립 DOM 감사 에이전트 모델은 opus다 — CLAUDE.md "점검·검토는 Opus"를 따랐다(01-16 본보기는 sonnet).
  - 의존성 비교 기준은 03-01이 SUMMARY에 적은 baseline-commit이다. 그 사이 승인된 의존성 결정이 있었다면 이 점검은 실패하고, 실행자는 멈춰서 그 결정 기록을 보고한다(기준을 바꾸지 않는다).
  - 엣지·웨일, 브라우저 사이 파일 옮기기, 다른 PC 동기화는 자동으로 할 수 없어 backstop truth와 `<human-check>`로 end-of-phase에 모은다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 웹 페이지(민감칸 입력) → content → SW writer | 이용자가 친 값이 저장소로 가는 길 |
| 확장 → 바깥 네트워크 | 있어서는 안 되는 경계(외부 전송 금지) |
| 설정 화면 → 내려받는 파일 | 사용자 PC 파일로 나가는 설정 |
| 실행자 → 감사 시험 | 감사 기준을 실행자가 바꿀 수 있는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-60 | Information Disclosure | 민감값이 저장소·화면·파일로 남음 | high | mitigate | `privacy-sweep.e2e.ts`가 기능 넷을 거친 뒤 저장소 세 곳·판 글자·내보낸 파일을 모두 뒤져 민감값 글자 없음(붙여 쓴 숫자 포함)과 대조군 기록 있음을 함께 확인 |
| T-03-61 | Information Disclosure | 외부 전송 | high | mitigate | `src/` 네트워크 API grep이 비어 있고, 모든 e2e·감사 시험 끝에서 `blockedRequests`가 비어 있음 |
| T-03-62 | Elevation of Privilege | manifest 권한·web-accessible 자원 | medium | mitigate | 빌드된 manifest에서 권한 셋·host 권한 하나·확장 페이지 비공개를 기계로 확인. 어긋나면 기준을 넓히지 않고 멈춤 |
| T-03-63 | Tampering | writer 밖 저장 쓰기(검사 우회) | medium | mitigate | 저장 쓰기·지우기 호출이 `storage-writer.ts` 밖에 없는지 grep |
| T-03-64 | Tampering | 의존성 공급망 | medium | mitigate | `package.json`·`pnpm-lock.yaml`을 03-01 baseline-commit과 비교, 달라지면 실패(설치 작업 없음) |
| T-03-65 | Tampering | 감사 기준 조작 | low | mitigate | 감사 시험은 별도 에이전트만 쓰고 커밋한다 — 그 파일의 모든 커밋 본문에 "감사 에이전트"가 있는지 확인 |
</threat_model>

<verification>
- 싼 게이트 `pnpm lint && pnpm typecheck && CI=true pnpm build` 통과, 정적 점검 다섯(외부 전송·저장 쓰기·권한·의존성·시험 파일) 통과
- `CI=true pnpm exec playwright test tests/e2e/privacy-sweep.e2e.ts` 4 passed
- 독립 DOM 감사 `tests/e2e/dom-audit-03.e2e.ts` 16개 이상 통과(별도 에이전트 작성·판정)
- 전체 게이트 `CI=true pnpm test && pnpm lint && pnpm typecheck` 한 번 통과
- end-of-phase human-check 셋(엣지·웨일, 브라우저 사이 파일, 다른 PC)
</verification>

<success_criteria>
- Phase 3의 모든 화면이 SYSTEM.md 기준 독립 감사를 통과하고, 민감값 보호·외부 전송 금지·권한·의존성이 지켜진 채 전체 게이트가 CI=true로 통과한다(NAV-02, CLICK-05, PERS-01~03, INPT-01, INPT-03, PRIV-01, PRIV-02, STOR-03, STOR-04)
- 자동으로 할 수 없는 확인은 human-check로 모여 `/gsd-verify-work`의 UAT로 넘어간다
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/e2e/privacy-sweep.e2e.ts`, `tests/e2e/dom-audit-03.e2e.ts`
- 심볼: 없음(검증 시험만). 감사 수정은 SUMMARY 편차에 파일별로 적는다
- 메시지: 없음

<output>
Create `.planning/phases/03-navigation-input-condition/03-16-SUMMARY.md` when done
</output>
