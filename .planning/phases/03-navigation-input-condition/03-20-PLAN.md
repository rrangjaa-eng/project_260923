---
phase: 03-navigation-input-condition
plan: 20
type: execute
wave: 17
depends_on: [03-19]
files_modified:
  - src/entrypoints/options/sensitive-section.ts
  - src/entrypoints/options/site-section.ts
  - src/entrypoints/options/confirm.ts
  - src/entrypoints/options/main.ts
  - src/shared/messages.ts
  - src/worker/storage-writer.ts
  - src/entrypoints/background.ts
  - tests/e2e/options-sections.e2e.ts
autonomous: true
requirements: [PRIV-02, PRIV-03]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "설정 화면 '민감칸 판별 단어'에서 늘 민감 단어와 번호 모양을 보는 단어를 더하고 뺄 수 있고, 바꾼 목록이 입력 줄이기·최근 값 기록에 곧바로 쓰인다"
    - "설정 화면 '사이트별 민감칸·고정 번호'에서 칸 설정과 고정 번호를 보고 지울 수 있고, 지워서 민감칸이 풀리게 되면 확인 화면(1초 보호)을 거친다"
  artifacts:
    - path: "src/entrypoints/options/sensitive-section.ts"
      provides: "민감칸 판별 단어(늘 민감·모양) 편집 절"
      exports: ["sensitiveSection"]
    - path: "src/entrypoints/options/site-section.ts"
      provides: "사이트별 민감칸 설정·고정 번호 보기·지우기 절"
      exports: ["siteSection"]
    - path: "src/entrypoints/options/confirm.ts"
      provides: "설정 화면용 확인 화면(SYSTEM.md 확인 화면 템플릿, createConfirmGuard 보호)"
      exports: ["openOptionsConfirm"]
    - path: "tests/e2e/options-sections.e2e.ts"
      provides: "민감칸 판별 단어·사이트별 절 시험(03-19 절 시험에 더함)"
  key_links:
    - from: "src/entrypoints/options/site-section.ts"
      to: "src/worker/storage-writer.ts"
      via: "updateSite{origin, patch: { sensitiveOverrides?, pins? }} — 확장 페이지 요청만(isExtensionPage)"
      pattern: "updateSite"
    - from: "src/entrypoints/options/confirm.ts"
      to: "src/core/confirm-guard.ts"
      via: "설정 화면 확인도 Phase 1과 같은 createConfirmGuard(1초 보호, 진짜 키만)"
      pattern: "createConfirmGuard"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 마우스 없이 명령판과 키로 이동·되돌리기를 하고, 긴 입력 양식을 큰 칸 화면과 최근 값·문구 카드로 채우며, 그날 손 상태에 맞는 설정 묶음을 한 번에 고르기를, **so that** 손 상태가 나쁜 날에도 잘못 누르거나 같은 값을 다시 치지 않고 평소 업무 사이트를 끝까지 쓸 수 있다.

<objective>
설정 화면에 남은 두 절을 붙인다: 민감칸 판별 단어(D-28)와 사이트별 민감칸·고정 번호 목록(D-27, D-10). 민감칸이 풀리게 되는 지우기는 설정 화면용 확인 화면(1초 보호)을 거친다.

Purpose: PRIV-03, PRIV-02(목록에서 보기·지우기). 설정 화면용 확인 화면은 03-22 가져오기 덮어쓰기도 쓴다.
Output: 절 둘, `openOptionsConfirm()`, 저장 요청 `setSensitiveWords`와 `updateSite` 패치 확장, `options-sections.e2e.ts`(sensitive·site).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-input-condition/03-CONTEXT.md
@.planning/phases/03-navigation-input-condition/03-19-SUMMARY.md
@docs/design/SYSTEM.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 새 의존성·새 권한 없음.
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 호출한다(서브에이전트 프롬프트에도 명시, `model` 명시).
- `tdd="true"` 작업: 실패 시험 먼저 → RED 커밋 `test(03-20): …` → 최소 구현 → GREEN 커밋 `feat(03-20): …`. 본문은 한국어.
- 완료 판정은 `CI=true` e2e. 저장 쓰기는 writer에만(D-03). 설정 화면 전용 op는 `isExtensionPage(sender)` 뒤에만(03-18).
- 화면은 SYSTEM.md "설정 화면"·"확인 화면" 템플릿과 tokens.css만(D-36), 카피 규칙(D-37). 연습 사이트에서만 시험(D-39).
- `03-RESEARCH.md`(623줄)·`03-PATTERNS.md`는 통째로 읽지 않는다 — 필요하면 해당 절만 Grep으로 줄을 찾아 범위로 읽는다(CLAUDE.md '500줄 이상은 range 필수'. 계획 하나의 문맥 예산을 지키려고 `<context>`에서 뺐다).

## 전제 (아직 실행 전인 Phase 1 계획에서 온 가정)

- [01-13 가정] 사이트 항목은 `site:<origin>` 사이트별 sync 항목, 쓰기 합치기·분당 상한.
- [01-14 가정] 사이트 항목 쓰기 전 크기 검사(`item-too-large`), 읽기는 `migrate`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: "민감칸 판별 단어"와 "사이트별 민감칸·고정 번호" — 풀리게 되면 확인</name>
  <files>src/entrypoints/options/sensitive-section.ts, src/entrypoints/options/site-section.ts, src/entrypoints/options/confirm.ts, src/entrypoints/options/main.ts, src/shared/messages.ts, src/worker/storage-writer.ts, src/entrypoints/background.ts, tests/e2e/options-sections.e2e.ts</files>
  <read_first>
    - src/core/sensitive.ts (isSensitive, 모양 이름), src/core/settings-schema.ts (sensitiveWords, SiteEntryV2.sensitiveOverrides·pins)
    - src/core/confirm-guard.ts, src/page/overlay/confirm-dialog.ts (확인 화면 모양과 보호 — 설정 화면용은 같은 규칙으로)
    - docs/design/SYSTEM.md ("확인 화면" 템플릿)
    - src/entrypoints/options/ui.ts, src/entrypoints/options/danger-section.ts (03-19 — 사이트 고르기·`updateSite` 쓰는 방식)
  </read_first>
  <behavior>
    - e2e `sensitive`: 6 → 늘 민감 단어 줄(비밀번호·비번·PW·인증번호·OTP·보안카드)과 모양 단어 줄(주민·계좌·카드) → "단어 더하기"로 "사번" → 라벨 "사번" 칸(시험용 페이지)에 들어가면 "민감칸이라 기록하지 않아요", 친 값이 기록되지 않는다
    - e2e `sensitive`: 늘 민감 단어에서 "OTP"를 빼면 라벨 "OTP"만 있는 칸에 카드가 뜨고 값이 기록된다
    - e2e `sensitive`: 모양 단어 "사업자"를 [2 계좌번호 모양]으로 더하면 라벨 "사업자번호" 칸에 "123-45-67890"을 친 값은 기록되지 않는다
    - e2e `site`: 7 → "적요 · 민감함" 줄 → [1 기본 판별로] → 설정 화면 확인 화면 "이 칸을 민감하지 않음으로 둘까요? Enter = 예"(1초 보호) → 0.5초 Enter 무시 → 1초 뒤 Enter → 그 칸 설정이 빠진다
    - e2e `site`: "3번 · {버튼 이름}" 고정 줄 → [1 고정 풀기] → pins에서 빠지고, practice 탭에서 F → 그 버튼의 번호가 3이 아니다
  </behavior>
  <action>
RED 먼저: behavior 다섯을 `options-sections.e2e.ts`(`describe('sensitive')`, `describe('site')`)에 더하고 실패 확인 후 RED 커밋.

`messages.ts`: op `setSensitiveWords { always: string[]; shape: { word; shape }[] }`, 03-19의 `updateSite` 패치에 `sensitiveOverrides?`, `pins?`를 더한다. `background.ts`: `isExtensionPage` 뒤에만. `storage-writer.ts`(D-03): 검사 후 쓴다.

`sensitive-section.ts`(D-28): 두 목록 줄 + "단어 더하기"(모양 단어는 더한 뒤 [1 주민번호 모양] [2 계좌번호 모양] [3 카드번호 모양]). `site-section.ts`(D-27, D-10): 지금 사이트의 민감칸 설정 줄("{칸 이름} · 민감함/민감하지 않음", 고르면 [1 기본 판별로])과 고정 번호 줄("{번호}번 · {요소 이름}", 고르면 [1 고정 풀기]). 민감함 설정을 지우는 것은 칸이 민감하지 않게 될 수 있으므로 늘 확인 화면을 거친다(값을 모르는 설정 화면에서는 보수적으로 — 가정 절). `options/main.ts`에서 절 6·7의 빈 본문을 두 절로 바꾼다.

`src/entrypoints/options/confirm.ts`: `openOptionsConfirm({ title, body, onResult })` — SYSTEM.md 확인 화면 템플릿(폭 `--dialog-width`, 버튼 높이 `--target-confirm`, 뒤 `--scrim`, 1초 보호 막대 "1초 뒤에 누를 수 있어요"), 보호는 `createConfirmGuard`(진짜 키만, 버튼 클릭 리스너 없음). 03-22 가져오기 덮어쓰기도 이것을 쓴다. 통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/options-sections.e2e.ts</automated>
    <fails_when>non-zero exit, or the summary does not report "16 passed", or "No tests found"</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/recent-values.e2e.ts tests/e2e/sensitive-config.e2e.ts tests/e2e/pin.e2e.ts && pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit, or any failed test (regression in sensitive judgement, overrides or pins)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/entrypoints/options/confirm.ts`에 `createConfirmGuard`와 `1초 뒤에 누를 수 있어요`가 있다
    - `src/shared/messages.ts`에 `setSensitiveWords`가 있다
    - `src/entrypoints/options/` 아래 파일에 hex 색 리터럴과 `box-shadow`가 없다
    - `CI=true pnpm exec playwright test tests/e2e/options-sections.e2e.ts` 16 passed(03-19의 11 + 이 작업 5), 회귀 없음
  </acceptance_criteria>
  <done>민감칸 판별 단어를 더하고 빼며, 사이트별 민감칸 설정·고정 번호를 보고 지운다(풀리게 되면 확인). RED·GREEN 커밋이 있다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- PRIV-02·PRIV-03: edge probe `unclassified — review manually`. 가정한 경계:
  - 설정 화면에서 민감함 설정을 지우는 것은 늘 확인 화면을 거친다(그 칸의 값·라벨을 설정 화면이 몰라 풀리는지 알 수 없으므로 보수적으로, D-27).
  - 민감칸 설정을 새로 더하는 곳은 명령판 "민감칸 설정"(03-14) 한 곳이다.
  - 판별 단어 목록은 전역 설정(sync)이다 — 사이트별 판별 단어는 없다(사이트별은 칸 설정으로).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 확장 페이지(옵션) → SW | 판별 단어·사이트 민감칸 설정·고정 번호 바꾸기 |
| 설정 화면 확인 화면 | 되돌리기 어려운 지우기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-48 | Elevation of Privilege | `setSensitiveWords`·`updateSite`(sensitiveOverrides·pins) | high | mitigate | `isExtensionPage` 뒤에만 — content 경로로 다른 사이트의 민감칸 설정·판별 단어를 바꿀 수 없다 |
| T-03-50 | Elevation of Privilege | 민감칸 설정 지우기 | high | mitigate | 설정 화면 확인 화면도 `createConfirmGuard`(1초, 진짜 키만) — e2e(0.5초 Enter 무시) |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/options-sections.e2e.ts` 16 passed
- recent-values·sensitive-config·pin 회귀 없음, `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 민감칸 판별 단어를 더하고 뺀다(PRIV-03)
- 사이트별 민감칸 설정을 보고 지우며, 풀리게 되면 확인한다(PRIV-02)
</success_criteria>

## Artifacts this phase produces

- 파일: `src/entrypoints/options/sensitive-section.ts`, `site-section.ts`, `confirm.ts`
- 심볼: `openOptionsConfirm()`, 저장 요청 `setSensitiveWords`, `updateSite`의 `sensitiveOverrides?`·`pins?`

<output>
Create `.planning/phases/03-navigation-input-condition/03-20-SUMMARY.md` when done
</output>
