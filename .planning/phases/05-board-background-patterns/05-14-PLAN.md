---
phase: 05-board-background-patterns
plan: 14
type: execute
wave: 11
depends_on: ["05-08"]
files_modified:
  - docs/design/SYSTEM.md
  - src/core/template-options.ts
  - tests/unit/template-options.test.ts
  - src/worker/storage-writer.ts
  - src/shared/messages.ts
  - tests/e2e/blocking.e2e.ts
autonomous: true
requirements: [BG-03]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "이용자가 틀 실행 시작 화면에서 그 틀의 '이미지·광고 막기'를 번호로 켜고 끌 수 있고, 설정은 틀마다 남는다(기본은 꺼짐)"
    - "켠 틀을 뒤에서 실행하면 05-08의 경로대로 실행 탭에서만 막히고, 끈 틀은 막히지 않는다(세션 규칙 없음)"
    - "켠 틀을 '여기서 실행'(포그라운드)해도 규칙은 그 탭 하나만 가리키고, 다른 탭에서는 이미지가 그대로 보인다"
    - "끄면 그 틀 항목이 지워지고 없는 틀의 항목은 쓸 때 정리되어 설정 크기가 늘지 않는다 — 동기화 영역이면 한 항목 8KB를 넘는 쓰기는 하지 않고 알린다"
  prohibitions:
    - statement: "Phase 4 틀 스키마(대조표 P4-8 파일)를 바꾸면 안 된다 — 차단 설정은 05-08이 만든 따로 된 templateOptions에만 쓴다"
      requirement_id: BG-03
      category: scope
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/core/template-options.ts"
      provides: "setBlocking(순수) — 켜기·끄기·없는 틀 정리"
      exports: ["setBlocking"]
    - path: "src/worker/storage-writer.ts"
      provides: "단일 저장자 op setTemplateBlocking(알려진 틀 id만, 8KB 확인)"
      contains: "setTemplateBlocking"
    - path: "docs/design/SYSTEM.md"
      provides: "틀 실행 시작 화면 — 이미지·광고 막기 카드 항목"
      contains: "이미지·광고 막기 켜기"
    - path: "tests/unit/template-options.test.ts"
      provides: "설정 기본값·켜고 끄기·정리 단위 시험"
  key_links:
    - from: "Phase 4 틀 실행 시작 화면(대조표 P4-7 파일)"
      to: "src/worker/storage-writer.ts"
      via: "storage/request op setTemplateBlocking { templateId, on }"
      pattern: "setTemplateBlocking"
    - from: "src/worker/storage-writer.ts"
      to: "src/core/template-options.ts"
      via: "setBlocking(options, templateId, on, liveTemplateIds) → TEMPLATE_OPTIONS_KEY에 쓰기"
      pattern: "setBlocking"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 탭·자주 가는 곳·뒤에서 도는 틀의 진행 상황·확인 대기를 작업판 한 곳에서 번호로 처리하고 틀로 만들 만한 반복을 도우미가 알려 주기를, **so that** 틀을 뒤쪽 탭에서 돌리는 동안 다른 탭에서 계속 일할 수 있다.

<objective>
틀 실행 시작 화면에서 틀마다 이미지·광고 막기를 번호로 켜고 끌 수 있게 한다. 먼저 한 경로를 꿴다: SYSTEM.md에 카드 항목 → 시작 화면의 "이미지·광고 막기 켜기" 카드 → 단일 저장자 op `setTemplateBlocking` → 틀 설정(`templateOptions`) → 05-08의 뒤에서 실행이 그 설정대로 막거나 막지 않음. 이어서 설정 정리 규칙(끄면 지움·없는 틀 정리·8KB)을 단위로 굳히고 포그라운드 실행을 확인한다.

이 일은 원래 05-08의 Task 3이었다. 05-08의 files_modified(18개)가 계획당 15개 미만 예산을 넘어 떼어 냈고, 05-08이 만든 설정 스키마(`template-options.ts`)·차단 경로(`run-blocking.ts`)·`blocking.e2e.ts` 위에 선다. 내용(행동·문구·시험)은 그대로이고, 새 계획의 첫 작업을 tracer로 두려고 카드 경로와 정리 규칙을 두 작업으로 나눴다.

Purpose: BG-03 "틀마다 켜고 끌 수 있다"(D-15), D-21(SYSTEM.md에 먼저 더한 뒤 만든다), D-22(카피 — 동작 단어), D-03(단일 저장자·형식 버전·8KB).
Output: SYSTEM.md 카드 항목, 시작 화면 카드, `setBlocking()`, 저장소 op `setTemplateBlocking`, `template-options.test.ts`, `blocking.e2e.ts`의 켜고 끄기 e2e 셋.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-board-background-patterns/05-CONTEXT.md
@.planning/phases/05-board-background-patterns/05-01-SUMMARY.md
@.planning/phases/05-board-background-patterns/05-08-SUMMARY.md
@.planning/phases/05-board-background-patterns/05-11-SUMMARY.md
@docs/design/SYSTEM.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. strict, `any` 금지, 시크릿 금지, 새 패키지 없음.
- `test-driven-development` → 구현, 실패 시 `systematic-debugging`, 완료 전 `verification-before-completion` 스킬 호출(서브에이전트 프롬프트에도).
- `tdd="true"`/`tracer`: RED 커밋 `test(05-14): …` → GREEN `feat(05-14): …`. SYSTEM.md는 `docs(05-14): …`. 본문 한국어. 완료 판정 `CI=true`.
- **이 계획 전체의 전제:** 05-11-SUMMARY.md "결정"이 `approve-host-access` 또는 `approve-plain`이고 05-08이 끝났다. `reject`이면(05-08이 "BG-03 미룸"으로 멈춤) 어떤 작업도 하지 않고 05-14-SUMMARY.md에 "BG-03 미룸(05-11 결정 reject)"만 적고 멈춘다.
- Phase 4 틀 스키마(대조표 P4-8 파일)는 바꾸지 않는다. 틀 실행 시작 화면(대조표 P4-7 파일)에는 카드 하나만 더하고 기존 카드·번호는 그대로 둔다.
- 권한·세션 규칙 코드(`wxt.config.ts`, `ad-domains.ts`, `run-blocking.ts`)는 05-08 소유다 — 이 계획은 읽기만 한다.

## 실행 순서 (wave 근거)

- wave 11(05-08 다음), depends_on `05-08`.
- 코드 의존: 05-08 — `template-options.ts`(`TEMPLATE_OPTIONS_KEY`·`templateOptionsSpec`·저장 영역), `run-blocking.ts`의 설정 읽기(켜진 틀만 막음), `blocking.e2e.ts`·`ads.html`·픽스처의 가짜 광고 호스트. 05-04(대조표 P4-7 틀 실행 시작 화면의 "뒤에서 실행" 카드 — 이 계획이 그 옆에 카드를 하나 더 더한다)·05-03(저장소 op 추가 방식 `pinFavorite`)은 05-08을 거쳐 이미 끝나 있다.
- 병렬 불가: 05-08과 `src/core/template-options.ts`·`tests/e2e/blocking.e2e.ts`를, 05-09와 `src/worker/storage-writer.ts`·`src/shared/messages.ts`를 함께 고친다. 같은 wave 계획은 files_modified가 겹치면 안 된다.
- 이 계획을 기다리는 계획: 05-09(wave 12, 파일 공유 순서), 05-10(감사: 틀 시작 화면 막기 카드).

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 시작 화면 "이미지·광고 막기" 카드 → 설정 저장 → 뒤에서 실행이 그 설정대로 막음 (한 경로)</name>
  <precondition>05-01 대조표 P4-7(틀 실행 시작 화면과 카드 자리)·P4-8(틀 저장 위치)이 '같음' 또는 '이름·위치만 다름'이고, 05-08-SUMMARY.md에 templateOptions의 저장 영역이 적혀 있다</precondition>
  <files>docs/design/SYSTEM.md, src/core/template-options.ts, src/worker/storage-writer.ts, src/shared/messages.ts, tests/e2e/blocking.e2e.ts</files>
  <read_first>
    - 대조표 P4-7이 가리키는 틀 실행 시작 화면 파일(05-04가 "뒤에서 실행" 카드를 더한 곳)
    - src/core/template-options.ts, src/worker/run-blocking.ts, tests/e2e/blocking.e2e.ts (05-08)
    - src/worker/storage-writer.ts (op 추가 방식 — Plan 01-13·01-14, 05-03 pinFavorite)
    - src/entrypoints/popup/main.ts (켜기·끄기 카드 문구 방식 — "도우미 끄기"처럼 동작 단어)
    - docs/design/SYSTEM.md (카피 규칙, 작업판 템플릿)
  </read_first>
  <behavior>
    - e2e: 틀 실행 시작 화면에 "이미지·광고 막기 켜기" 카드(번호는 그 화면의 다음 빈 번호)가 있다. 그 번호를 누르면 카드 글자가 "이미지·광고 막기 끄기"로 바뀌고 저장소의 그 틀 항목이 켜짐이다. 화면을 닫았다 열어도 켜짐이다
    - e2e: 같은 카드를 다시 눌러 끈 뒤 뒤에서 실행하면 실행 탭 `img === 'load'`, `getSessionRules()`가 빈 배열
  </behavior>
  <action>
RED 먼저: D-21대로 SYSTEM.md 항목을 먼저 더해 `docs(05-14)` 커밋하고, e2e 둘을 `blocking.e2e.ts`에 더해 실패 확인 후 RED 커밋.

- `docs/design/SYSTEM.md`(D-21, D-22): 작업판 템플릿 아래 "틀 실행 시작 화면 — 이미지·광고 막기 카드" 한 항목을 더한다. 번호 카드 하나에 동작 단어를 쓴다: 꺼져 있으면 "이미지·광고 막기 켜기", 켜져 있으면 "이미지·광고 막기 끄기". Phase 1 메뉴의 켜기·끄기 카드와 같은 방식이다. 켜진 상태 표시는 카드 선택 표시가 아니라 글자로 한다. 새 토큰 없음.
- `template-options.ts`: `setBlocking(options, templateId, on, liveTemplateIds)`(순수) — 새 객체를 돌려준다. 켜면 그 틀 항목을 넣고, 끄면 그 틀 항목을 지운다(없는 틀 정리는 Task 2).
- `storage-writer.ts`: op `setTemplateBlocking { templateId, on }`. 대조표 P4-8의 틀 목록에 있는 id만 받는다. 저장 영역은 05-08이 정한 곳이다.
- `messages.ts`: `storage/request`의 op 유니온에 `setTemplateBlocking`을 더한다.
- 틀 실행 시작 화면(대조표 P4-7 파일, Phase 4 소유 화면에 카드만 더함): 카드 하나를 그 화면의 다음 빈 번호에 더한다. 누르면 `storage/request { op: setTemplateBlocking }`을 보내고, 응답 뒤 글자를 바꾼다. 기존 카드·번호는 그대로. 키 입력은 그 화면의 기존 통로(`isTrusted` + 떨림 필터)를 그대로 쓴다.

통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/blocking.e2e.ts tests/e2e/background-run.e2e.ts</automated>
    <fails_when>non-zero exit, or fewer than 8 tests from blocking.e2e.ts reported as passed (05-08의 6 + 이 작업 2), or any background-run.e2e.ts test fails</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/design/SYSTEM.md`에 "이미지·광고 막기 켜기"와 "이미지·광고 막기 끄기"가 있다
    - `src/worker/storage-writer.ts`와 `src/shared/messages.ts`에 `setTemplateBlocking`이 있다
    - 대조표 P4-8의 틀 스키마 파일은 이 계획에서 바뀌지 않았다: `git log --format=%H --grep="(05-14)" -- <P4-8 파일>` 출력이 비어 있다(05-14 커밋 중 그 파일을 건드린 것이 없음)
    - `CI=true pnpm exec playwright test tests/e2e/blocking.e2e.ts` 8개 이상, `background-run.e2e.ts` 회귀 없음
  </acceptance_criteria>
  <done>이용자가 틀 실행 시작 화면에서 번호로 그 틀의 이미지·광고 막기를 켜고 끄고, 뒤에서 실행이 그 설정을 따른다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 설정 정리 규칙(끄면 지움·없는 틀 정리·8KB)과 포그라운드 실행 굳히기</name>
  <files>tests/unit/template-options.test.ts, src/core/template-options.ts, src/worker/storage-writer.ts, tests/e2e/blocking.e2e.ts</files>
  <read_first>
    - src/core/template-options.ts, src/worker/storage-writer.ts (Task 1)
    - src/core/settings-schema.ts (`syncItemBytes` — Plan 01-13, 형식 버전 spec 모양 — Plan 01-14)
    - src/worker/run-blocking.ts (05-08 — 포그라운드 실행에서 onRunsChanged → applyForTab)
  </read_first>
  <behavior>
    - 단위: `templateOptionsSpec` 기본값은 빈 byTemplate이다. 모르는 틀 id는 차단 꺼짐으로 읽는다
    - 단위: `setBlocking(options, templateId, on)`은 새 객체를 돌려준다. 끄면 그 틀 항목을 지워 크기가 늘지 않는다. `liveTemplateIds`에 없는 id는 쓸 때 정리된다
    - e2e: 켠 틀을 "여기서 실행"(포그라운드)하면 `getSessionRules()`의 규칙이 그 탭 하나만 가리키고, 다른 탭의 `ads.html` 새로 고침은 `img === 'load'`
  </behavior>
  <action>
RED 먼저: 단위 둘을 새 파일 `tests/unit/template-options.test.ts`, e2e 하나를 `blocking.e2e.ts`에 더하고 실패 확인 후 RED 커밋.

- `template-options.ts`: `setBlocking`이 쓸 때 `liveTemplateIds`에 없는 항목을 정리한다(Task 1은 켜기·끄기만 했다).
- `storage-writer.ts`: `setTemplateBlocking`의 저장 영역이 sync면 `syncItemBytes`로 8KB를 넘지 않는지 확인하고, 넘으면 쓰지 않고 알린다(D-03).
- 포그라운드 e2e는 05-08의 `run-blocking.ts` 경로(onRunsChanged에서 새 실행을 보면 `applyForTab`)를 확인한다. 실패하면 `systematic-debugging`으로 쫓고, 이 계획의 files 밖(05-08 소유 파일)을 고쳐야 하면 멈추고 알린다.

통과하면 GREEN 커밋.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/template-options.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 2 tests reported as passed</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/blocking.e2e.ts tests/e2e/background-run.e2e.ts</automated>
    <fails_when>non-zero exit, or fewer than 9 tests from blocking.e2e.ts reported as passed, or any background-run.e2e.ts test fails</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/template-options.ts`에 `export function setBlocking`이 있고 `chrome.` 참조가 없다
    - `pnpm exec vitest run tests/unit/template-options.test.ts` 2개 이상 통과
    - `CI=true pnpm exec playwright test tests/e2e/blocking.e2e.ts` 9개 이상, `background-run.e2e.ts` 회귀 없음
  </acceptance_criteria>
  <done>설정은 끄거나 틀이 사라지면 줄고 8KB를 넘지 않으며, 켠 틀은 포그라운드로 돌아도 그 탭에서만 막는다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- BG-03(켜고 끄기): edge probe `unclassified — review manually`. 가정한 경계:
  - 카드는 틀 실행 시작 화면에만 있다. 번호는 그 화면의 다음 빈 번호다.
  - 켜진 상태 표시는 글자로 한다(카드 선택 표시 아님) — Phase 1 메뉴의 켜기·끄기 카드와 같은 방식.
  - 기본은 꺼짐. 끄면 그 틀 항목을 지운다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 틀 시작 화면(content script) → service worker | 틀마다 차단 설정 쓰기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-36 | Tampering | 틀 차단 설정 쓰기 | medium | mitigate | 단일 저장자 op, 알려진 틀 id만, 형식 버전·spec, sync 8KB 확인 |
| T-05-53 | Spoofing | 페이지가 막기 카드를 누름 | low | mitigate | 카드 키는 시작 화면의 기존 통로(`isTrusted` + 떨림 필터)로만, SW는 `sender.id` + zod로 op를 받는다 |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/template-options.test.ts` 2개 이상 통과
- `CI=true pnpm exec playwright test tests/e2e/blocking.e2e.ts` 9개 이상, `background-run.e2e.ts` 회귀 없음
- `pnpm typecheck && pnpm lint` 통과
</verification>

<success_criteria>
- 이용자가 틀마다 이미지·광고 막기를 번호로 켜고 끌 수 있고, 켠 틀은 그 실행 탭에서만 막는다(ROADMAP 성공 기준 3 뒷부분, BG-03 — 05-08과 함께)
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/unit/template-options.test.ts`
- 심볼: `setBlocking()`
- 저장소 op: `setTemplateBlocking`
- SYSTEM.md: 틀 실행 시작 화면 막기 카드
- 화면: 틀 실행 시작 화면 "이미지·광고 막기 켜기/끄기" 카드

<output>
Create `.planning/phases/05-board-background-patterns/05-14-SUMMARY.md` when done
</output>
