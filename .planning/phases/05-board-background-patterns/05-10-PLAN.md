---
phase: 05-board-background-patterns
plan: 10
type: execute
wave: 11
depends_on: ["05-09"]
files_modified:
  - tests/e2e/phase5-dom-audit.e2e.ts
  - src/page/overlay/board.ts
  - src/page/overlay/board-view.ts
  - src/page/overlay/queue-screens.ts
  - src/page/overlay/pattern-card.ts
  - src/page/overlay/mode-indicator.ts
  - src/entrypoints/popup/main.ts
autonomous: true
requirements: [BORD-01, BORD-02, BORD-03, BORD-04, BG-01, BG-02, BG-03, PATN-01]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Phase 5의 모든 새 화면(작업판 네 구획·빈 상태·닫기 확인·값 고르기·제출 확인·확인 대기 칩·진행 카드·경고 카드·반복 패턴 질문 카드·메뉴의 작업판 카드·틀 시작 화면 막기 카드)이 SYSTEM.md와 tokens.css만 따른다는 것이 별도 에이전트의 DOM 실측으로 확인된다"
    - "전체 게이트(`CI=true pnpm test` 단위 → e2e 전부, lint, typecheck, build)가 한 번에 통과한다"
    - "src/ 안에 밖으로 나가는 네트워크 API 호출이 없고, 시험 중 밖으로 나간 요청(blockedRequests)이 없다"
    - "숨은 탭 속도 측정 숫자(05-04 표, 05-05 5분 넘게 숨은 탭)가 SUMMARY에 모여 있다"
    - "자동화할 수 없는 확인(엣지·웨일, 다른 PC 동기화, 실제 회사 시스템)이 수동 확인 목록으로 남고, 다음 절차 Post-build(`/review` → `/qa` → `/cso` → `/ship`)가 적혀 있다"
  prohibitions:
    - statement: "실행자가 독립 DOM 감사의 기준이나 감사 시험을 고쳐서 통과시키면 안 된다 — 고치는 것은 화면 코드이고, SYSTEM.md를 벗어나야 하면 멈추고 DECISIONS.md 기록·승인을 받는다"
      requirement_id: BORD-01
      category: process
      status: resolved
      verification: judgment
    - statement: "Phase 5에 밖으로 나가는 요청을 더하면 안 된다(D-04, 설계 8장)"
      requirement_id: BG-03
      category: privacy
      status: resolved
      verification: judgment
  artifacts:
    - path: "tests/e2e/phase5-dom-audit.e2e.ts"
      provides: "Phase 5 화면 독립 DOM 감사(별도 에이전트 작성·판정)"
  key_links:
    - from: "tests/e2e/phase5-dom-audit.e2e.ts"
      to: "docs/design/tokens.css"
      via: "계산된 스타일 값이 토큰 값 집합 안인지 실측"
      pattern: "tokens.css"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 탭·자주 가는 곳·뒤에서 도는 틀의 진행 상황·확인 대기를 작업판 한 곳에서 번호로 처리하고 틀로 만들 만한 반복을 도우미가 알려 주기를, **so that** 틀을 뒤쪽 탭에서 돌리는 동안 다른 탭에서 계속 일할 수 있다.

<objective>
Phase 5를 닫는다. 저장소 규칙의 화면 검증 순서대로 싼 게이트 → 별도 에이전트의 독립 DOM 감사(`CI=true`, 스크린샷 육안 금지) → 수정 → 전체 게이트 한 번을 거친다. 이어서 요구사항별 시험 대응표, 속도 숫자, 수동 확인 목록, Post-build 안내를 SUMMARY에 모은다.

Purpose: D-30(UI 완료 판정), D-29(시험 목록, 엣지·웨일 수동), D-13(측정 기록), D-04(밖으로 나가는 요청 없음), CLAUDE.md "프론트엔드 — 화면 검증 순서"·"Post-build 넷은 건너뛰지 않는다".
Output: `tests/e2e/phase5-dom-audit.e2e.ts`(감사 에이전트 작성), 어긋남 수정, 전체 게이트 결과, SUMMARY(대응표·숫자·수동 목록·다음 절차).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-board-background-patterns/05-CONTEXT.md
@.planning/phases/05-board-background-patterns/05-04-SUMMARY.md
@.planning/phases/05-board-background-patterns/05-05-SUMMARY.md
@.planning/phases/05-board-background-patterns/05-08-SUMMARY.md
@docs/design/SYSTEM.md
@docs/design/tokens.css
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. strict, `any` 금지, 시크릿 금지, 새 패키지 없음.
- 실패를 쫓기 전에 `systematic-debugging`, "완료"를 말하기 전에 `verification-before-completion` 스킬 호출(감사 에이전트 프롬프트에도 명시).
- 전체 게이트는 이 계획에서 **한 번만** 돈다(Task 2). 수정은 Task 1에서 싼 게이트와 감사 시험으로만 확인한다.
- 수정 커밋: `fix(05-10): …`(본문 한국어). 감사 시험 추가: `test(05-10): …`.

## 실행 순서 (wave 근거)

- wave 11(마지막), depends_on `05-09`.
- 코드 의존: 모든 기능 계획 — 감사할 화면(05-02~05-09, 05-12의 경고 카드 문구)과 SUMMARY에 모을 숫자·결정(05-04·05-05 속도, 05-11 스파이크·권한 결정, 05-08 권한 이름). 05-09가 직렬 사슬의 끝이라 05-01~05-09·05-11·05-12가 모두 먼저 끝난다(전이 의존). 그래서 depends_on에는 05-09만 적는다.
- 병렬 불가: 전체 게이트는 모든 기능이 들어온 뒤에만 뜻이 있다.

<tasks>

<task type="auto">
  <name>Task 1: 싼 게이트 → 독립 DOM 감사(별도 에이전트) → 수정</name>
  <files>tests/e2e/phase5-dom-audit.e2e.ts, src/page/overlay/board.ts, src/page/overlay/board-view.ts, src/page/overlay/queue-screens.ts, src/page/overlay/pattern-card.ts, src/page/overlay/mode-indicator.ts, src/entrypoints/popup/main.ts</files>
  <read_first>
    - CLAUDE.md ("프론트엔드 — 화면 검증 순서")
    - docs/design/SYSTEM.md (작업판 템플릿 전체, 확인 화면, 모드 표시, 컴포넌트 규칙, 상태 표, 카피 규칙), docs/design/tokens.css, docs/DESIGN.md (§4, §6)
    - .planning/phases/01-click-helper-foundation/01-16-SUMMARY.md (Phase 1 감사 방식과 어긋남 기록 형식)
    - tests/e2e/fixtures.ts, tests/e2e/run-helpers.ts (화면을 띄우는 방법)
  </read_first>
  <action>
1. 싼 게이트: `pnpm lint && pnpm typecheck && CI=true pnpm build`. 실패하면 고치고 다시.

2. 독립 DOM 감사는 **이 계획 실행자가 아닌 별도 감사 에이전트**에게 맡긴다(D-30).
   - 맡기는 법: Task 도구, `model: "sonnet"`, 프롬프트에 `verification-before-completion` 스킬 명시.
   - 기준: SYSTEM.md·tokens.css·DESIGN.md §6만.
   - 산출물: 감사 에이전트가 `tests/e2e/phase5-dom-audit.e2e.ts`를 쓰고 `CI=true pnpm exec playwright test tests/e2e/phase5-dom-audit.e2e.ts`로 DOM을 실측해 판정한다(스크린샷 육안 금지).
   - 화면을 띄우는 곳: 연습 페이지 `targets.html`, `frames.html`, `bg-form.html`, `repeat-form.html`, `ads.html`, 확장 아이콘 메뉴.
   - 감사할 화면(감사 체크리스트 — 화면 ID 13개. 감사 에이전트는 화면마다 시험을 하나 이상 쓰고, 시험 이름을 화면 ID로 시작한다. 예: `S04 닫기 확인 …`):
     - S01 작업판 첫 장과 네 구획
     - S02 빈 상태 넷
     - S03 다음 장
     - S04 닫기 확인(틀이 도는 탭 첫 줄 포함)
     - S05 값 고르기와 직접 입력
     - S06 제출 확인
     - S07 "확인 대기 N" 칩
     - S08 진행 카드(막대 포함)
     - S09 경고 카드(탭 닫힘·로그아웃·확인 필요 이유 문장)
     - S10 멈춘 카드 상세
     - S11 반복 패턴 질문 카드
     - S12 메뉴의 작업판 카드와 메뉴 안 작업판
     - S13 틀 시작 화면의 "뒤에서 실행"·"이미지·광고 막기" 카드
   - 감사 항목(최소 — 화면마다 해당하는 항목을 모두 잰다):
     - 색: 계산된 `color`·`background-color`·`border-*-color`·`outline-color`가 tokens.css 값 집합 안에 있다.
     - 형태: radius는 radius 토큰 집합 안(카드 12, 확인 카드 14), `box-shadow`는 모두 `none`, 서체는 "IBM Plex Sans KR"로 시작한다.
     - 크기: 카드 격자 3열·간격 16px, 누르는 대상 높이 56px 이상, 번호 22px·700, 확인 버튼 64px 이상, 확인 카드 600px, 진행 막대 높이 8px이고 채움은 `--accent`·바탕은 `--surface`.
     - 선택·경고 표시: 선택 카드는 남색 3px(`aria-current` 또는 `:focus-visible`), 경고 카드는 `--warning` 테두리.
     - 칩·질문 카드: 칩은 남색 3px 테두리·숫자 700·`pointer-events: none`. 질문 카드는 오른쪽 아래에 있고 떠 있는 동안 `document.activeElement`가 바뀌지 않는다.
     - 글자: 대비 4.5:1 이상·UI 3:1 이상, 한글 `word-break: keep-all`, 모든 버튼에 키 칩이 있다.
     - 모션·확대: `reducedMotion: 'reduce'`에서 나타남 transition 0(보호 막대는 유지), 확대 110%·200%에서 크기가 변하지 않는다.
     - 문구: 빈 상태·오류 문구가 SYSTEM.md 문장과 글자 그대로 같다.
     - 네트워크: `blockedRequests`가 비어 있다.
   - 감사 에이전트는 어긋남 목록(파일·부품·실측값·기준값)을 돌려준다.

3. 수정: 어긋남마다 해당 화면 파일을 고친다.
   - SYSTEM.md를 벗어나야 하는 경우는 고치지 말고 멈춘다. DECISIONS.md 기록과 SYSTEM.md 수정 승인을 요청한다(DESIGN.md §4.6).
   - 고친 파일과 내용은 SUMMARY에 편차로 적는다.
   - 감사 시험을 다시 돌려 통과를 확인한다. 감사 시험의 기준은 실행자가 바꾸지 않는다.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && CI=true pnpm build</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>CI=true pnpm exec playwright test tests/e2e/phase5-dom-audit.e2e.ts --reporter=list</automated>
    <fails_when>non-zero exit, or "No tests found", or the report lists any skipped test, or fewer than 13 tests passed (13 = 감사 체크리스트의 화면 ID S01~S13 수)</fails_when>
    <automated>for i in $(seq -w 1 13); do grep -q "S$i " tests/e2e/phase5-dom-audit.e2e.ts || { echo "missing S$i"; exit 1; }; done</automated>
    <fails_when>non-zero exit (감사 체크리스트의 화면 ID 중 시험 이름에 없는 것이 있다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/phase5-dom-audit.e2e.ts`가 있고, 05-10-SUMMARY.md에 "감사 에이전트(model: sonnet)가 작성·판정"과 어긋남 목록·수정 결과가 있다
    - `CI=true pnpm exec playwright test tests/e2e/phase5-dom-audit.e2e.ts`가 건너뜀 없이 통과하고, 감사 체크리스트의 화면 ID S01~S13이 모두 시험 이름에 있다(통과 수 하한 13은 이 체크리스트의 화면 수에서 나온다 — 화면이 늘면 체크리스트와 하한을 함께 늘린다)
    - `docs/design/tokens.css`에 05-10 커밋이 없다: `git log --format=%H --grep="(05-10)" -- docs/design/tokens.css` 출력이 비어 있다
  </acceptance_criteria>
  <done>Phase 5의 모든 새 화면이 SYSTEM.md 일관성을 별도 에이전트의 DOM 실측으로 통과했다.</done>
</task>

<task type="auto">
  <name>Task 2: 전체 게이트 한 번 + 요구사항 대응표·속도 숫자·수동 확인 목록·Post-build 안내</name>
  <files>.planning/phases/05-board-background-patterns/05-10-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-board-background-patterns/05-04-SUMMARY.md ("숨은 탭 속도 측정"), 05-05-SUMMARY.md ("5분 넘게 숨은 탭"), 05-11-SUMMARY.md ("스파이크 결과", "결정")
    - .planning/phases/05-board-background-patterns/05-08-SUMMARY.md (권한 이름, A4 시험 방식)
    - CLAUDE.md ("Post-build 넷은 건너뛰지 않는다")
  </read_first>
  <action>
1. 전체 게이트 한 번: `CI=true pnpm test`(단위 → e2e 전부) && `pnpm lint` && `pnpm typecheck`. 실패하면 `systematic-debugging`으로 원인을 찾아 고친 뒤 **실패한 파일만** 다시 돌려 확인하고, 마지막에 전체 게이트를 한 번 더 돌린다. 이 경우 SUMMARY에 "전체 게이트 2회"와 이유를 적는다.

2. 밖으로 나가는 요청 없음(D-04)은 verify의 grep 명령으로 확인한다.

3. 05-10-SUMMARY.md에 다음을 적는다.
   - **요구사항 대응표:** BORD-01~04, BG-01~03, PATN-01마다 그것을 증명하는 시험 파일과 시험 이름을 적는다(`board.e2e.ts`, `favorites.e2e.ts`, `board-entry.e2e.ts`, `background-run.e2e.ts`, `background-speed.e2e.ts`, `queue.e2e.ts`, `queue-confirm.e2e.ts`, `run-stop.e2e.ts`, `blocking.e2e.ts`, `pattern.e2e.ts`, 단위 시험들).
   - **숨은 탭 속도:** 05-04·05-05 숫자를 한 표로 모은다.
     - 행: 보이는 탭 / 숨은 탭(스로틀링 유지) / 5분 넘게 숨은 탭.
     - 열: 단계 간격 중앙값 / p95 / 전체 시간.
     - 조건을 함께 적는다: Playwright 기본 인자 셋을 뺀 크로미움, 헤드리스 여부.
   - **수동 확인 목록**(D-29, `/qa` 때 사람이 확인):
     - 엣지·웨일에서 작업판(단축키 기본 `B` 포함)·확인 대기·배지가 동작하는지, 그리고 크롬과 다를 수 있는 세 API(RESEARCH Open Questions 3): 이미지·광고 막기(`declarativeNetRequest` 세션 규칙, 05-11 결정의 권한 이름), SW가 다시 떠도 확인 대기가 이어지는지(`chrome.storage.session`), 열린 탭 최근 쓴 순서(`tabs.lastAccessed` — 없으면 원래 순서)
     - 다른 PC 같은 계정에서 자주 가는 곳이 동기화되는지
     - 작업판 단축키(기본 `B`)가 회사 시스템 단축키와 겹치지 않는지
     - 여러 창에서 탭 이동 시 창 초점이 따라오는지
     - 도울 수 없는 탭에서 메뉴 작업판을 키보드만으로 쓸 수 있는지
     - 실제 회사 시스템의 로그인 만료가 로그아웃으로 판정되는지(판정 규칙 5단계)
     - 설치·업데이트 화면에 새 권한 경고가 늘지 않았는지(05-11 결정이 approve-host-access일 때)
   - **다음 절차:** CLAUDE.md Post-build를 건너뛰지 않는다 — `/review` → `/qa`(UI 변경이므로 `/design-review` 포함) → `/cso` → `/ship`.
     - `/cso`는 필수다. 이 phase가 새 권한을 더했고, 탭 사이 메시지로 제출을 넘기며, 페이지 입력을 관찰하기 때문이다.
     - 페이지 경계의 주요 점검 대상: `board/answer`, `run/page-signal`, `pattern/entry`, 세션 차단 규칙 정리.
  </action>
  <verify>
    <automated>CI=true pnpm test</automated>
    <fails_when>non-zero exit, or any test reported failed or flaky</fails_when>
    <automated>pnpm lint && pnpm typecheck</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>test -z "$(grep -rnE '\bfetch\(|XMLHttpRequest|WebSocket|sendBeacon|EventSource' src/)"</automated>
    <fails_when>non-zero exit (a network API call exists in src/)</fails_when>
  </verify>
  <acceptance_criteria>
    - `CI=true pnpm test` 전체 통과(SUMMARY에 횟수 기록)
    - `src/`에 네트워크 API 호출이 없다
    - 05-10-SUMMARY.md에 "요구사항 대응표"(여덟 ID 모두), "숨은 탭 속도"(세 행), "수동 확인 목록", "다음 절차"(`/review`·`/qa`·`/cso`·`/ship`)가 있다
  </acceptance_criteria>
  <done>Phase 5가 전체 게이트를 CI=true로 통과했고, 요구사항별 증거·속도 숫자·수동 확인 목록·다음 절차가 한 곳에 모였다. 다음은 CLAUDE.md Post-build다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

- 여덟 요구사항: edge probe `unclassified — review manually`. 이 계획은 앞 계획들이 정한 경계를 검증만 하고 새 경계를 만들지 않는다.
- 전체 게이트가 한 번에 통과하지 않으면 실패한 파일만 고쳐 확인한 뒤 전체 게이트를 한 번 더 돌린다(두 번째 전체 실행은 SUMMARY에 이유와 함께 기록).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 실행자 ↔ 감사 에이전트 | 판정 기준의 독립성 |
| 확장 → 네트워크 | 밖으로 나가는 요청(없어야 함) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-45 | Tampering | 감사 기준 | medium | mitigate | 감사 시험은 별도 에이전트가 쓰고 실행자는 기준을 바꾸지 않음. SYSTEM.md 이탈은 멈추고 승인 |
| T-05-46 | Information Disclosure | 밖으로 나가는 요청 | high | mitigate | src/ 네트워크 API grep 0, 모든 e2e에서 `blockedRequests` 비어 있음 |
| T-05-47 | Repudiation | 검증 결과 | low | mitigate | 전체 게이트 횟수·어긋남·수정·수동 목록을 SUMMARY에 남김 |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/phase5-dom-audit.e2e.ts` 통과, 화면 ID S01~S13 모두 시험 이름에 있음(별도 에이전트 판정)
- `CI=true pnpm test`, `pnpm lint`, `pnpm typecheck`, `CI=true pnpm build` 통과
- src/ 네트워크 API 없음
</verification>

<success_criteria>
- ROADMAP Phase 5 성공 기준 1~4가 시험으로 증명되고(대응표), 화면이 SYSTEM.md 감사를 통과한다(D-30)
- 다음 절차(Post-build 넷)가 SUMMARY에 적혀 있다
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/e2e/phase5-dom-audit.e2e.ts`
- 결과물: 05-10-SUMMARY.md의 요구사항 대응표, 숨은 탭 속도 표, 수동 확인 목록, 다음 절차, 감사 어긋남·수정 기록

<output>
Create `.planning/phases/05-board-background-patterns/05-10-SUMMARY.md` when done
</output>
