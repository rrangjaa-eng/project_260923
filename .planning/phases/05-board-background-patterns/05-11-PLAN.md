---
phase: 05-board-background-patterns
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/fixtures.ts
  - tests/e2e/phase5-spike.e2e.ts
  - tests/practice-site/spike-dnr.html
  - tests/practice-site/img/dot.svg
autonomous: true
requirements: [BG-01, BG-03]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "declarativeNetRequest 세션 규칙 차단이 Playwright context.route와 함께 있을 때 관찰되는지(A4)가 실측되어 BG-03 시험 방식이 (가) 기존 픽스처 또는 (나) 로컬 서버 중 하나로 정해진다"
    - "Playwright 기본 인자 중 백그라운드 스로틀링을 끄는 세 인자를 뺀 브라우저에서도 확장이 올라가고(A3), 숨은 탭의 setTimeout 사슬 간격과 SW→숨은 탭 메시지 왕복 시간이 숫자로 기록된다(A2 기준선)"
    - "새 권한 declarativeNetRequestWithHostAccess에 대한 이용자 결정(approve-host-access, 2026-09-24)과 그 권한이 스파이크 A4와 맞는지(권한 호환)가 SUMMARY에 기록되고, 이 계획에서 wxt.config.ts의 권한은 바뀌지 않는다"
    - "픽스처 정리 뒤에도 기존 e2e(skeleton, site-toggle)가 그대로 통과한다"
  prohibitions:
    - statement: "새 권한은 이 계획에서 제품 manifest(wxt.config.ts)에 넣지 않고(2026-09-24 이용자 승인을 받았지만 넣는 것은 05-08만 한다), Phase 1 D-13의 금지 권한(디버거·탐색 감시·콘텐츠 설정)은 이 phase에서도 넣지 않는다 — 스파이크는 제품이 아닌 임시 시험용 확장으로만 한다"
      requirement_id: BG-03
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "tests/e2e/fixtures.ts"
      provides: "launchExtensionContext(옵션: 확장 경로, 백그라운드 스로틀링 유지, 라우팅 관찰) — 기존 context 픽스처가 이것을 부른다. test.use로 켜는 옵션 픽스처 keepBackgroundThrottling(05-04 속도 측정이 쓴다). 확장자별 응답 형식"
      contains: "launchExtensionContext"
    - path: "tests/e2e/phase5-spike.e2e.ts"
      provides: "A4(DNR × context.route), A3(ignoreDefaultArgs), A2(숨은 탭 기준선) 스파이크와 test-results/phase5-spike.json"
    - path: "tests/practice-site/spike-dnr.html"
      provides: "이미지 한 장을 싣는 스파이크 페이지"
  key_links:
    - from: "tests/e2e/phase5-spike.e2e.ts"
      to: "tests/e2e/fixtures.ts"
      via: "test.use({ keepBackgroundThrottling: true }) → context 픽스처 → launchExtensionContext / launchExtensionContext({ extensionPath: 임시 확장 })"
      pattern: "launchExtensionContext"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 탭·자주 가는 곳·뒤에서 도는 틀의 진행 상황·확인 대기를 작업판 한 곳에서 번호로 처리하고 틀로 만들 만한 반복을 도우미가 알려 주기를, **so that** 틀을 뒤쪽 탭에서 돌리는 동안 다른 탭에서 계속 일할 수 있다.

(ROADMAP Phase 5 **Goal** 문장을 세 칸에 나눠 옮긴 것이다. 새 내용은 더하지 않았다.)

<objective>
RESEARCH의 미확인 가정 셋(A4 DNR × 픽스처 라우팅, A3 ignoreDefaultArgs, A2 숨은 탭 타이머)을 실측하고, 이미 받은 새 권한 승인(2026-09-24, `approve-host-access`)이 스파이크 결과와 맞는지 확인해 기록한다. 이 일은 원래 05-01의 Task 2·3이었다. 05-01의 계약 대조와 파일이 겹치지 않고 서로의 결과를 쓰지 않아, 따로 떼어 같은 wave에서 병렬로 돌린다.

Purpose: D-13(숨은 탭 측정의 전제), D-15·D-16(세션 규칙 + 새 권한 승인 — 이용자가 2026-09-24 승인). 이 계획은 스파이크와 결정 기록이다 — 제품 코드는 쓰지 않는다. 그래서 첫 작업이 tracer가 아니다(제품 코드의 첫 tracer는 05-02).
Output: 픽스처 `launchExtensionContext`, 스파이크 결과(`test-results/phase5-spike.json` + SUMMARY 숫자), 권한 결정 기록과 스파이크 호환 확인.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-board-background-patterns/05-CONTEXT.md
@.planning/phases/05-board-background-patterns/05-RESEARCH.md
@.planning/phases/05-board-background-patterns/05-PATTERNS.md
@.planning/phases/01-click-helper-foundation/01-CONTEXT.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 이 계획은 패키지를 설치하지 않는다(필요해지면 멈추고 이유 한 줄과 함께 승인 요청).
- 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 **호출**한다. 픽스처를 고칠 때는 `test-driven-development`도 호출한다. 서브에이전트에 맡기면 그 프롬프트에 세 스킬을 적는다.
- 커밋 제목은 영어 접두어(`test(05-11): …`, `docs(05-11): …`), 본문은 한국어. 완료 판정은 `CI=true`.
- **제품 manifest(`wxt.config.ts`)는 이 계획에서 건드리지 않는다.** 스파이크용 확장은 시험이 `os.tmpdir()` 아래 임시 폴더에 만들고 시험이 끝나면 지운다(커밋되는 것은 시험 코드뿐).
- `.planning/STATE.md`·`ROADMAP.md`·`REQUIREMENTS.md`는 편집하지 않는다(다른 스레드 소유). 스파이크 숫자·결정은 이 계획의 SUMMARY에만 쓴다.
- 연습 사이트(practice.test·other.test)와 브라우저 내부 페이지에서만 시험. 회사 시스템 접속 금지.

## 실행 순서 (wave 근거)

- 계획 번호는 실행 순서가 아니다. 이 계획은 05-01에서 떼어 낸 뒤 번호를 11로 받았고, **wave 1**에서 05-01과 **병렬**로 돈다.
- depends_on 없음. 05-01과 files_modified가 겹치지 않는다(05-01은 `tests/unit/phase5-contract.test.ts`만 만든다). 05-01의 대조표도 쓰지 않는다 — 스파이크는 Phase 1 산출물(`site/ping`, 빌드된 `.output`)만 쓴다.
- 05-01이 대조표 불일치로 멈춰도 이 계획의 결과(시험 코드·결정)는 제품 코드가 아니므로 그대로 남는다.
- 이 계획을 기다리는 계획: 05-04(픽스처 `launchExtensionContext`·옵션 픽스처 `keepBackgroundThrottling`, 스파이크 A2·A3), 05-08·05-14(권한 결정, 스파이크 A4), 05-10(결정·스파이크 숫자를 SUMMARY에 모음). 05-02·05-03은 이 계획을 기다리지 않는다.
- 옵션 픽스처 `keepBackgroundThrottling`은 원래 05-04 Task 2가 `fixtures.ts`에 더하던 것이다. 05-04의 files_modified를 계획당 15개 미만으로 줄이려고, `fixtures.ts`를 처음 고치는 이 계획으로 옮겼다(A3가 같은 인자 제거를 이미 확인하므로 여기서 함께 검증된다).

<tasks>

<task type="auto">
  <name>Task 1: 스파이크 — DNR × 픽스처 라우팅(A4), 스로틀링 인자 제거(A3), 숨은 탭 기준선(A2)</name>
  <files>tests/e2e/fixtures.ts, tests/e2e/phase5-spike.e2e.ts, tests/practice-site/spike-dnr.html, tests/practice-site/img/dot.svg</files>
  <read_first>
    - tests/e2e/fixtures.ts (context 픽스처 56~106행: launchPersistentContext, context.route)
    - tests/e2e/spike.e2e.ts (Phase 1 스파이크의 결과표 남기는 방식)
    - .planning/phases/05-board-background-patterns/05-RESEARCH.md ("Playwright로 측정하기 — 결정적 발견", "미해결 위험: Playwright 네트워크 가로채기와 DNR", Pitfall 1, "Open Questions (RESOLVED)" 1)
    - playwright.config.ts
  </read_first>
  <action>
**픽스처 정리(동작 불변):** `tests/e2e/fixtures.ts`의 `context` 픽스처 본문을 export 함수 `launchExtensionContext(opts: { extensionPath?: string; keepBackgroundThrottling?: boolean; servedPages: ServedPages; blockedRequests: string[]; onRouted?: (url: string) => void })`로 뽑아낸다. 기존 `context` 픽스처는 이 함수를 기본값으로 부른다. `keepBackgroundThrottling: true`면 `ignoreDefaultArgs`에 세 인자(`--disable-background-timer-throttling`, `--disable-backgrounding-occluded-windows`, `--disable-renderer-backgrounding`)만 배열로 넣는다(전체 무시 금지, RESEARCH A3). `onRouted`는 route 콜백이 받은 URL을 알린다(A4 관찰용). `keepBackgroundThrottling`은 옵션 픽스처로도 내보낸다(`[false, { option: true }]`) — 시험 파일이 `test.use({ keepBackgroundThrottling: true })`로 켜면 `context` 픽스처가 그 값을 `launchExtensionContext`에 넘긴다(05-04 속도 측정이 이 길을 쓴다). 연습 사이트 파일 응답의 `contentType`을 확장자로 정한다: `.svg` → `image/svg+xml`, `.js` → `text/javascript; charset=utf-8`, 그 밖 → 지금처럼 `text/html; charset=utf-8`.

**연습 파일:** `tests/practice-site/img/dot.svg`(작은 원 하나, 16×16), `tests/practice-site/spike-dnr.html`(제목 "DNR 스파이크", `<img id="dot" src="http://practice.test/img/dot.svg">`, 로드 결과를 `document.body.dataset.img = 'loaded'|'failed'`로 적는 onload/onerror).

**`tests/e2e/phase5-spike.e2e.ts`**(결과는 `test-results/phase5-spike.json`과 콘솔 표, 01-12 스파이크와 같은 방식 — 시험은 관찰이 끝났는지를 확인하고, 관찰값 자체로 실패하지 않는다):
- A4: `os.tmpdir()` 아래 임시 폴더에 시험용 확장(manifest v3, 권한 `declarativeNetRequestWithHostAccess`, host `<all_urls>`, 빈 service worker)을 쓰고 `launchExtensionContext({ extensionPath })`로 띄운다. `spike-dnr.html` 탭 둘(A·B)을 열고 SW에서 A의 탭 id로 세션 규칙(이미지 차단, `condition.tabIds: [A]`)을 더한 뒤 두 탭을 새로 고친다. A·B의 `data-img`, A에서 `requestfailed`의 `errorText`, `onRouted`가 A의 `dot.svg`를 봤는지를 기록한다. 판정: **(가)** A 실패·B 성공·A의 이미지가 route에 오지 않음 → 기존 픽스처로 BG-03 시험 가능. **(나)** A도 성공(route가 먼저 응답) → 같은 시험을 `context.route` 없이 한 번 더: Node `http.createServer`로 `tests/practice-site`를 127.0.0.1 임의 포트에서 제공하고 `--host-resolver-rules=MAP practice.test 127.0.0.1:<port>, MAP * ~NOTFOUND`로 띄워(밖으로 나가는 요청 없음) A 실패·B 성공이 보이는지 기록. 임시 폴더는 `afterAll`에서 지운다.
- A3: `test.describe` 안에서 `test.use({ keepBackgroundThrottling: true })`로 띄운 기본 `context`(제품 확장 .output — 옵션 픽스처를 거쳐 `launchExtensionContext({ keepBackgroundThrottling: true })`와 같은 길)에서 SW가 뜨는지, `chrome://version`의 명령줄(`#command_line` 글자)에 세 인자가 없는지 기록·확인한다(이 둘은 실패하면 시험 실패 — 이후 측정의 전제다).
- A2: 같은 컨텍스트에서 연습 페이지 두 탭을 열어 앞 탭을 숨긴다(`document.visibilityState === 'hidden'` 확인). 숨은 탭과 보이는 탭에서 각각 (1) 페이지 안 `setTimeout(…, 100)` 10번 사슬의 실제 간격(중앙값·최댓값), (2) SW에서 `chrome.tabs.sendMessage(tabId, { type: 'site/ping' }, { frameId: 0 })` 20번 왕복 시간(중앙값·p95)을 잰다. 숨은 탭의 타이머 간격이 900ms 이상으로 늘어나면 "스로틀링 재현됨", 아니면 "재현 안 됨(헤드리스)"으로 기록한다.

05-11-SUMMARY에 "스파이크 결과" 절: A4 판정((가)/(나)와 근거 값), A3 확인, A2 숫자 표. A2가 "재현 안 됨"이면 05-04의 측정을 어떻게 할지(헤드 모드 + 가상 화면이 이 환경에 있는지 `command -v xvfb-run`으로 확인한 결과 포함)를 한 줄로 적는다.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/phase5-spike.e2e.ts tests/e2e/skeleton.e2e.ts tests/e2e/site-toggle.e2e.ts</automated>
    <fails_when>non-zero exit, or "No tests found", or the report lists any failed test, or test-results/phase5-spike.json is absent afterwards</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/fixtures.ts`에 `export async function launchExtensionContext`와 `ignoreDefaultArgs`가 있고, 기존 `context` 픽스처가 이것을 부른다
    - `tests/e2e/fixtures.ts`의 `keepBackgroundThrottling`이 `option: true` 옵션 픽스처이고, `phase5-spike.e2e.ts`의 A3가 `test.use({ keepBackgroundThrottling: true })`로 통과한다(명령줄에 세 인자 없음)
    - `git diff --quiet HEAD -- wxt.config.ts`가 0으로 끝난다(제품 권한 불변)
    - `test-results/phase5-spike.json`에 `a4`, `a3`, `a2` 세 키가 있다
    - 05-11-SUMMARY.md "스파이크 결과"에 A4 판정((가) 또는 (나)), A3 확인, A2 숫자가 있다
  </acceptance_criteria>
  <done>BG-03 시험 방식과 숨은 탭 측정 방식이 실측으로 정해졌고, 픽스처가 `test.use({ keepBackgroundThrottling: true })`로 스로틀링을 켠 브라우저를 띄울 수 있다. 기존 시험은 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: 새 권한 결정 기록(이미 답함 — approve-host-access, 2026-09-24)과 스파이크 호환 확인</name>
  <files>.planning/phases/05-board-background-patterns/05-11-SUMMARY.md</files>
  <read_first>
    - wxt.config.ts (현재 permissions: storage·scripting·tabs, host <all_urls>)
    - .planning/phases/05-board-background-patterns/05-RESEARCH.md ("권한 선택" 표)
    - .planning/phases/05-board-background-patterns/05-CONTEXT.md (D-16 — 승인 기록)
    - 이 계획 Task 1의 스파이크 결과(A4, `test-results/phase5-spike.json`의 `a4`)
  </read_first>
  <action>
**결정은 이미 났다 — 이 작업은 이용자에게 묻지 않고 멈추지도 않는다(스파이크가 권한을 쓸 수 없다고 보인 경우만 예외, 아래).** BG-03(틀마다 이미지·광고 차단, 그 틀의 탭에서만)을 위해 새 권한 `declarativeNetRequestWithHostAccess`를 제품 manifest에 넣을지의 질문(D-15, D-16, 설계 11장 ⑤의 "새 권한은 만들 때 승인" 선례)에, 이용자(랑쟈)가 2026-09-24 09:48 UTC 프로젝트 대화에서 "declarativeNetRequestWithHostAccess 허용"이라고 답했다. 고른 선택지는 `approve-host-access`다. 제시했던 다른 둘 — `approve-plain`(일반 `declarativeNetRequest`, 설치 경고가 하나 더 뜸)과 `reject`(BG-03을 미룸) — 은 고르지 않았다. 일반 `declarativeNetRequest`는 승인되지 않았다.

기록으로 남기는 근거(질문 때 보인 그대로):
- 이유 한 줄: 이미지·광고 요청을 막을 수 있는 MV3의 유일한 방법이 declarativeNetRequest이고, 세션 규칙에 탭 조건(`tabIds`)을 걸어 **그 틀을 실행하는 탭에서만** 막는다(전역 규칙은 쓰지 않는다).
- `declarativeNetRequestWithHostAccess`는 이미 있는 host `<all_urls>` 범위 안에서만 동작해 설치 화면에 **새 경고를 띄우지 않는다**(RESEARCH, Chrome 문서).
- 쓰는 곳: service worker의 세션 규칙 추가·삭제뿐. 규칙은 브라우저를 닫으면 사라지고, 틀이 끝나거나 멈추거나 탭이 닫히면 걷어 낸다(05-08).
- 넣지 않는 것(그대로): 디버거·탐색 감시·콘텐츠 설정 권한(Phase 1 D-13).
- manifest에 넣는 일은 05-08만 한다. 이 작업은 `wxt.config.ts`를 건드리지 않는다.

**할 일 — 스파이크 호환 확인:** Task 1의 A4 결과(임시 시험용 확장이 바로 이 권한으로 세션 규칙을 걸었다)가 승인된 권한과 맞는지 판정한다.
- **호환:** A4 판정이 (가)이거나, (나)이면서 로컬 서버 재시험에서 A 실패·B 성공이 보였다.
- **쓸 수 없음:** 임시 확장이 이 권한으로 세션 규칙을 더하지 못했거나(`updateSessionRules` 오류), 기존 픽스처와 로컬 서버 두 방식 모두에서 A의 이미지가 막히지 않았다.

05-11-SUMMARY.md에 "결정" 절을 쓴다: 선택지 id `approve-host-access`, 날짜 2026-09-24, 이용자의 말 "declarativeNetRequestWithHostAccess 허용", 그리고 판정 한 줄 — `권한 호환: 호환` 또는 `권한 호환: 쓸 수 없음` — 과 근거 값(A4 판정, A·B의 `data-img`, `errorText`).

**쓸 수 없음이면:** 결정을 바꾸지 않는다. `approve-plain`이나 미룸을 스스로 고르지 않는다. 판정과 근거를 SUMMARY에 적은 뒤 여기서 멈추고 이용자에게 보고한다 — 이용자가 승인한 것이 달라지기 때문이다. 이용자가 다시 정하기 전까지 05-08·05-14는 "BG-03 미룸"으로 멈춘다.
  </action>
  <verify>
    <automated>grep -q "approve-host-access" .planning/phases/05-board-background-patterns/05-11-SUMMARY.md && grep -q "2026-09-24" .planning/phases/05-board-background-patterns/05-11-SUMMARY.md && grep -q "권한 호환: 호환" .planning/phases/05-board-background-patterns/05-11-SUMMARY.md && git diff --quiet HEAD -- wxt.config.ts</automated>
    <fails_when>non-zero exit (결정 기록이 없거나, 스파이크가 권한을 쓸 수 없다고 보였거나, wxt.config.ts가 바뀜)</fails_when>
  </verify>
  <acceptance_criteria>
    - 05-11-SUMMARY.md "결정" 절에 선택지 id `approve-host-access`, 날짜 2026-09-24, 이용자의 말 "declarativeNetRequestWithHostAccess 허용"이 있다
    - 같은 절에 `권한 호환:` 판정 한 줄과 근거 값(A4)이 있다
    - 이 작업에서 `wxt.config.ts`는 바뀌지 않았다(`git diff --quiet HEAD -- wxt.config.ts`)
  </acceptance_criteria>
  <done>이용자가 2026-09-24 승인한 `declarativeNetRequestWithHostAccess`(approve-host-access)가 SUMMARY에 기록되었고, 스파이크 A4가 이 권한과 맞는다고 확인되어 05-08이 질문 없이 권한을 더할 수 있다. 맞지 않으면 실행자가 멈추고 보고했다.</done>
</task>

</tasks>

## 가정 (spec-less edge probe)

edge probe가 이 phase의 한국어 요구사항 8개를 모두 `unclassified`로 돌려줬다(자동 분류 실패). 이 계획(BG-01·BG-03 전제 확인)의 가정:
- 스파이크의 "스로틀링 재현" 기준은 숨은 탭 `setTimeout(100)` 간격 900ms 이상(크롬의 1초 정렬, RESEARCH).
- A4 (나)의 로컬 서버는 시험 안에서만 쓰는 Node 내장 `http` 모듈이라 새 의존성이 아니다.
- 권한 결정은 05-01의 대조표와 상관없다. 대조표가 불일치로 멈춰도 결정은 유효하다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시험용 임시 확장 → 브라우저 | 스파이크가 쓰는 확장(제품 아님)이 네트워크 차단 규칙을 건다 |
| 시험 브라우저 → 네트워크 | 연습 사이트 밖으로 나가는 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | 제품 manifest | high | mitigate | 이 계획은 `wxt.config.ts`를 바꾸지 않는다(acceptance `git diff --quiet`). 권한 추가는 05-08에서만(이용자 승인 2026-09-24, Task 2가 스파이크와의 호환을 확인한 뒤) |
| T-05-02 | Information Disclosure | 스파이크 (나) 로컬 서버 | low | mitigate | 127.0.0.1에만 묶고 `MAP * ~NOTFOUND`로 다른 호스트 해석을 막아 밖으로 나가는 요청이 없다 |
| T-05-03 | Tampering | 픽스처 정리 | medium | mitigate | 기존 `context` 픽스처 동작 불변 — skeleton·site-toggle e2e를 같은 명령으로 함께 돌린다 |
</threat_model>

<verification>
- `CI=true pnpm exec playwright test tests/e2e/phase5-spike.e2e.ts tests/e2e/skeleton.e2e.ts tests/e2e/site-toggle.e2e.ts` 통과, `test-results/phase5-spike.json` 생성
- `pnpm typecheck && pnpm lint` 통과
- 권한 결정이 05-11-SUMMARY에 기록, `wxt.config.ts` 불변
</verification>

<success_criteria>
- BG-03 시험 방식과 숨은 탭 측정 방식이 실측으로 정해졌다
- 새 권한은 이용자 결정 뒤에만 들어간다
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/e2e/phase5-spike.e2e.ts`, `tests/practice-site/spike-dnr.html`, `tests/practice-site/img/dot.svg`
- 심볼: 픽스처 `launchExtensionContext(opts)`(옵션 `extensionPath`, `keepBackgroundThrottling`, `onRouted`), 옵션 픽스처 `keepBackgroundThrottling`(`test.use`로 켬)
- 결과물: `test-results/phase5-spike.json`(a4·a3·a2), 05-11-SUMMARY의 "스파이크 결과", "결정"

<output>
Create `.planning/phases/05-board-background-patterns/05-11-SUMMARY.md` when done
</output>
