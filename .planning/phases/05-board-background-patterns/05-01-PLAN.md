---
phase: 05-board-background-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/phase5-contract.test.ts
  - tests/e2e/fixtures.ts
  - tests/e2e/phase5-spike.e2e.ts
  - tests/practice-site/spike-dnr.html
  - tests/practice-site/img/dot.svg
autonomous: false
requirements: [BG-01, BG-03]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Phase 1(계획 13~16)·Phase 3·Phase 4 산출물이 이 phase의 가정(D-24~D-27, 명령판 작업판 자리, 키 배치, 민감칸 판별, 모달 키 통로, 오버레이 입력칸)과 한 줄씩 대조되어 '같음 / 이름·위치만 다름(매핑) / 다름·없음'으로 SUMMARY 대조표에 남는다"
    - "대조표에 '다름·없음'이 하나라도 있으면 Phase 5 제품 코드를 한 줄도 쓰기 전에 멈추고 불일치 목록과 항목별 선택지를 이용자에게 돌려준다"
    - "매핑이 확인된 계약은 tests/unit/phase5-contract.test.ts가 실제 Phase 3·4 모듈을 가져와 확인하고, 나중에 Phase 3·4가 바뀌면 이 시험이 먼저 실패한다"
    - "declarativeNetRequest 세션 규칙 차단이 Playwright context.route와 함께 있을 때 관찰되는지(A4)가 실측되어 BG-03 시험 방식이 (가) 기존 픽스처 또는 (나) 로컬 서버 중 하나로 정해진다"
    - "Playwright 기본 인자 중 백그라운드 스로틀링을 끄는 세 인자를 뺀 브라우저에서도 확장이 올라가고(A3), 숨은 탭의 setTimeout 사슬 간격과 SW→숨은 탭 메시지 왕복 시간이 숫자로 기록된다(A2 기준선)"
    - "새 권한 declarativeNetRequestWithHostAccess에 대한 이용자 결정이 SUMMARY에 기록되고, 이 계획에서 wxt.config.ts의 권한은 바뀌지 않는다"
    - "픽스처 정리 뒤에도 기존 e2e(skeleton, site-toggle)가 그대로 통과한다"
  prohibitions:
    - statement: "새 권한은 이용자 승인 전에 제품 manifest(wxt.config.ts)에 넣지 않고, Phase 1 D-13의 금지 권한(디버거·탐색 감시·콘텐츠 설정)은 이 phase에서도 넣지 않는다 — 스파이크는 제품이 아닌 임시 시험용 확장으로만 한다"
      requirement_id: BG-03
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "tests/unit/phase5-contract.test.ts"
      provides: "Phase 3·4 산출물에 대한 Phase 5의 계약 시험(대조표의 매핑을 실제 모듈로 확인)"
    - path: "tests/e2e/fixtures.ts"
      provides: "launchExtensionContext(옵션: 확장 경로, 백그라운드 스로틀링 유지, 라우팅 관찰) — 기존 context 픽스처가 이것을 부른다. 확장자별 응답 형식"
      contains: "launchExtensionContext"
    - path: "tests/e2e/phase5-spike.e2e.ts"
      provides: "A4(DNR × context.route), A3(ignoreDefaultArgs), A2(숨은 탭 기준선) 스파이크와 test-results/phase5-spike.json"
    - path: "tests/practice-site/spike-dnr.html"
      provides: "이미지 한 장을 싣는 스파이크 페이지"
  key_links:
    - from: "tests/e2e/phase5-spike.e2e.ts"
      to: "tests/e2e/fixtures.ts"
      via: "launchExtensionContext({ keepBackgroundThrottling: true }) / launchExtensionContext({ extensionPath: 임시 확장 })"
      pattern: "launchExtensionContext"
    - from: "tests/unit/phase5-contract.test.ts"
      to: "Phase 4 실행 상태 스키마·활동 기록 함수, Phase 3 민감칸 판별 함수"
      via: "대조표 P3-·P4- 행이 가리키는 실제 export를 import해 표본을 safeParse·typeof로 확인"
      pattern: "safeParse"
---

## Phase Goal

**As a** 손 떨림이 있는 이용자, **I want to** 탭·자주 가는 곳·뒤에서 도는 틀의 진행 상황·확인 대기를 작업판 한 곳에서 번호로 처리하고 틀로 만들 만한 반복을 도우미가 알려 주기를, **so that** 틀을 뒤쪽 탭에서 돌리는 동안 다른 탭에서 계속 일할 수 있다.

(ROADMAP Phase 5 **Goal** 문장을 세 칸에 나눠 옮긴 것이다. 새 내용은 더하지 않았다.)

<objective>
Phase 5를 짓기 전에 바닥을 확인한다. 이 phase는 Phase 3·4가 아직 계획·실행 중일 때 미리 썼으므로(CONTEXT "앞선 단계 의존"), 합쳐진 Phase 1·3·4 결과가 이 계획의 가정과 맞는지 먼저 대조하고, 틀리면 짓지 않고 멈춘다. 이어서 RESEARCH의 미확인 가정 셋(A4 DNR × 픽스처 라우팅, A3 ignoreDefaultArgs, A2 숨은 탭 타이머)을 실측하고, 새 권한 승인을 받는다.

Purpose: D-24~D-27(Phase 4 가정)·CONTEXT "앞선 단계 의존"(다르면 이 계획을 먼저 고친다), D-13(숨은 탭 측정의 전제), D-15·D-16(세션 규칙 + 새 권한 승인). 이 계획은 가정 확인·스파이크 게이트다 — 제품 코드의 첫 tracer는 05-02가 이끈다.
Output: SUMMARY 대조표(가정 ↔ 실제, 매핑), `tests/unit/phase5-contract.test.ts`, 픽스처 `launchExtensionContext`, 스파이크 결과(`test-results/phase5-spike.json` + SUMMARY 숫자), 권한 결정.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-board-background-patterns/05-CONTEXT.md
@.planning/phases/05-board-background-patterns/05-RESEARCH.md
@.planning/phases/05-board-background-patterns/05-PATTERNS.md
@.planning/phases/01-click-helper-foundation/01-CONTEXT.md
</context>

## 실행 규칙 (모든 작업 공통)

- pnpm만. TypeScript strict, `any` 금지, 시크릿 금지. 이 계획은 패키지를 설치하지 않는다(필요해지면 멈추고 이유 한 줄과 함께 승인 요청).
- 구현 전 `test-driven-development`, 실패를 쫓기 전 `systematic-debugging`, 완료 선언 전 `verification-before-completion` 스킬을 **호출**한다. 서브에이전트에 맡기면 그 프롬프트에 세 스킬을 적는다.
- 커밋 제목은 영어 접두어(`test(05-01): …`, `docs(05-01): …`), 본문은 한국어. 완료 판정은 `CI=true`.
- **제품 manifest(`wxt.config.ts`)는 이 계획에서 건드리지 않는다.** 스파이크용 확장은 시험이 `os.tmpdir()` 아래 임시 폴더에 만들고 시험이 끝나면 지운다(커밋되는 것은 시험 코드뿐).
- `.planning/STATE.md`·`ROADMAP.md`·`REQUIREMENTS.md`는 편집하지 않는다(다른 스레드 소유). 대조표·스파이크 숫자·결정은 이 계획의 SUMMARY에만 쓴다.
- 연습 사이트(practice.test·other.test)와 브라우저 내부 페이지에서만 시험. 회사 시스템 접속 금지.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 앞선 단계 산출물 대조와 계약 시험 (멈춤 규칙 포함)</name>
  <files>tests/unit/phase5-contract.test.ts</files>
  <read_first>
    - .planning/phases/05-board-background-patterns/05-CONTEXT.md ("앞선 단계 의존", D-24~D-27, Claude's Discretion)
    - .planning/phases/05-board-background-patterns/05-RESEARCH.md ("Phase 4 최소 계약", Assumptions Log A5·A6)
    - .planning/phases/01-click-helper-foundation/01-13-SUMMARY.md ~ 01-16-SUMMARY.md (없으면 그 사실이 곧 불일치 P1-A)
    - .planning/phases/03-navigation-input-condition/*-PLAN.md, *-SUMMARY.md (합쳐진 Phase 3)
    - .planning/phases/04-*/*-PLAN.md, *-SUMMARY.md (합쳐진 Phase 4)
    - src/shared/messages.ts, src/core/settings-schema.ts, src/entrypoints/content.ts (현재 모양)
  </read_first>
  <behavior>
    - 대조표 행마다(아래 23행) 가정·찾은 곳(파일:줄 또는 export 이름)·판정(같음/매핑/다름·없음)·매핑 내용이 SUMMARY에 있다
    - 계약 시험: Phase 4 실행 상태 스키마(P4-1)가 D-24 필드를 가진 표본 기록을 통과시키고, 필드 하나를 뺀 표본은 거절한다
    - 계약 시험: Phase 4 상태 값 집합이 일곱 뜻(도는 중·값 기다림·제출 확인 기다림·막힘·확인 필요·끝남·멈춤)을 모두 표현한다(매핑한 이름으로)
    - 계약 시험: 답 메시지 모양(P4-2)이 값 답과 제출/취소 답을 둘 다 받는다
    - 계약 시험: 활동 기록 함수(P4-5)·민감칸 판별 함수(P3-3)·틀 기록 시작(P4-9)·실행 멈춤(P4-12)이 함수로 export되어 있다
    - 계약 시험: 명령판 첫 장 카드 자리 목록(P3-1)의 2번이 작업판이다
  </behavior>
  <action>
대조한다(D-24~D-27, CONTEXT "앞선 단계 의존"). 먼저 `git log --oneline -30`과 `git ls-files src tests .planning/phases`로 Phase 1 계획 13~16, Phase 3, Phase 4가 이 브랜치에 합쳐졌는지 본다. 합쳐지지 않았으면 그것 자체가 불일치다 — 멈춘다.

아래 23행을 하나씩 코드에서 찾아(Grep으로 위치, 필요한 범위만 Read) SUMMARY의 "가정 대조표"(열: ID · 가정 · 찾은 곳 · 판정 · 매핑)에 적는다. 판정은 셋뿐이다: **같음**, **이름·위치만 다름**(뜻과 모양은 같고 이름·파일만 다름 → 매핑 열에 실제 이름), **다름·없음**(뜻이나 모양이 다르거나 없음).
- P1-A: Phase 1 계획 13~16 SUMMARY 네 개와 산출물(`site/ping`, `migrate`·`settingsSpec`·`siteSpec`·`syncItemBytes`·`SYNC_ITEM_LIMIT`, `showToast`, `--overlay-scale`, 서체 등록)
- P1-B: 확장 아이콘 메뉴(`src/entrypoints/popup/main.ts`) 카드 번호 사용 현황과 다음 빈 번호(Phase 1 기준 5)
- P3-1: 명령판 모듈, 첫 장 2번 "작업판" 고정 자리, 카드가 동작을 부르는 방식(함수 이름)
- P3-2: 키 배치 설정 구조(keymap 필드·형식 버전·PERS-03 설정 화면이 키 목록을 읽는 곳), 새 키를 더하는 방법(선택 필드 허용 여부 또는 `settingsSpec.migrations`)
- P3-3: 민감칸 판별 함수(export 이름·인자·값 모양 판별에 입력 값이 필요한지)
- P3-4: 명령판 모달 키 처리(떨림 필터를 거치는 방법, 자식 프레임 키 전달 통로, 카드를 마우스로 누르는 방식)
- P3-5: 오버레이 안 입력칸 처리(양식 한 장 보기·입력 줄이기에서 모달 중 글자 입력을 받는 방법)
- P3-6: 모드 표시 변경(문구, 오른쪽에 칩을 붙일 자리)
- P3-7: 도우미 모드에서 번호표가 없을 때 숫자 키의 뜻(사이트로 통과하는지)
- P4-1: 실행 상태 저장(storage.session 키 이름·스키마·상태 값 목록·단계마다 쓰는지·템플릿 이름·시작 주소 포함 여부)
- P4-2: 값 고르기·제출 확인에서 멈추고 SW의 답을 기다리는 모양, 답 메시지 모양, 실행 탭 안 화면과의 관계(D-25)
- P4-3: 기다림 내용(후보 값, 제출 요약 줄, 제출 버튼 이름)이 있는 곳(세션 기록 또는 실행 탭 메모리)
- P4-4: 실행 잠금과 "이미 도는 틀" 안내 화면, 거기에 카드를 더할 자리(D-26)
- P4-5: 활동 기록 함수(이름·항목 형식·값을 빼는 방식)(D-27)
- P4-6: 제출 직전 "누름" 기록(storage.local 키)과 "확인 필요" 전이 함수(TMPL-13)
- P4-7: 틀 실행 시작 경로(시작 화면, 시작 주소, 어느 탭에서 도는지), 시작 화면에 카드를 더할 자리
- P4-8: 틀 저장 형식(스키마·틀마다 설정 필드·단일 저장자 op)
- P4-9: 틀 기록 시작 경로(TMPL-01) — 탭과 주소를 주고 시작할 수 있는지
- P4-10: 실행기 단계 진행 방식(content script 타이머 사슬인지 SW 조정인지, 요소 기다림이 MutationObserver인지)
- P4-11: 지금 탭에서 틀 실행·기록 중인지 알 수 있는 곳
- P4-12: 실행을 이유와 함께 멈추는 경로
- P4-13: Phase 4 연습 양식과 e2e 도우미(틀 넣기·실행 시작 방법)

**멈춤 규칙:** 한 행이라도 "다름·없음"이면 계약 시험도 제품 코드도 쓰지 않고 멈춘다(Rule 4 — 구조 변경). `## CHECKPOINT REACHED`로 불일치 목록을 돌려주고, 행마다 선택지 둘을 적는다: (ㄱ) Phase 5 계획이 그 자리에 최소 고리를 더한다(어떤 파일에 어떤 이름으로), (ㄴ) Phase 3·4를 먼저 고친다. 이용자 답을 받은 뒤에만 이어 간다.

모두 "같음" 또는 "매핑"이면: RED 먼저 — `tests/unit/phase5-contract.test.ts`에 behavior 여섯을 쓴다(매핑 열의 실제 export를 import, 표본 기록은 값 없이 이름·숫자만). 실제 모듈이 있으니 곧바로 통과할 수 있다 — 이 시험은 새 기능이 아니라 계약 고정이므로, 표본에서 필드 하나를 빼면 실패하는지를 먼저 확인해 시험이 진짜로 잡는다는 것을 RED 증거로 SUMMARY에 남긴 뒤 커밋한다(`test(05-01): …`).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/phase5-contract.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or fewer than 6 tests reported as passed</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - 05-01-SUMMARY.md에 "가정 대조표"가 있고 P1-A~P4-13 23행이 모두 판정과 함께 있다
    - "다름·없음" 행이 있으면 이 작업은 코드 없이 멈췄고 불일치 목록·선택지가 이용자에게 전달됐다
    - 멈추지 않았으면 `pnpm exec vitest run tests/unit/phase5-contract.test.ts`가 6개 이상 통과한다
  </acceptance_criteria>
  <done>Phase 5가 기대는 Phase 1·3·4의 모양이 확인되어 매핑이 한 곳(SUMMARY 대조표)에 있고, 계약 시험이 그 매핑을 지킨다. 어긋나면 짓기 전에 멈췄다.</done>
</task>

<task type="auto">
  <name>Task 2: 스파이크 — DNR × 픽스처 라우팅(A4), 스로틀링 인자 제거(A3), 숨은 탭 기준선(A2)</name>
  <files>tests/e2e/fixtures.ts, tests/e2e/phase5-spike.e2e.ts, tests/practice-site/spike-dnr.html, tests/practice-site/img/dot.svg</files>
  <read_first>
    - tests/e2e/fixtures.ts (context 픽스처 56~106행: launchPersistentContext, context.route)
    - tests/e2e/spike.e2e.ts (Phase 1 스파이크의 결과표 남기는 방식)
    - .planning/phases/05-board-background-patterns/05-RESEARCH.md ("Playwright로 측정하기 — 결정적 발견", "미해결 위험: Playwright 네트워크 가로채기와 DNR", Pitfall 1)
    - playwright.config.ts
  </read_first>
  <action>
**픽스처 정리(동작 불변):** `tests/e2e/fixtures.ts`의 `context` 픽스처 본문을 export 함수 `launchExtensionContext(opts: { extensionPath?: string; keepBackgroundThrottling?: boolean; servedPages: ServedPages; blockedRequests: string[]; onRouted?: (url: string) => void })`로 뽑아낸다. 기존 `context` 픽스처는 이 함수를 기본값으로 부른다. `keepBackgroundThrottling: true`면 `ignoreDefaultArgs`에 세 인자(`--disable-background-timer-throttling`, `--disable-backgrounding-occluded-windows`, `--disable-renderer-backgrounding`)만 배열로 넣는다(전체 무시 금지, RESEARCH A3). `onRouted`는 route 콜백이 받은 URL을 알린다(A4 관찰용). 연습 사이트 파일 응답의 `contentType`을 확장자로 정한다: `.svg` → `image/svg+xml`, `.js` → `text/javascript; charset=utf-8`, 그 밖 → 지금처럼 `text/html; charset=utf-8`.

**연습 파일:** `tests/practice-site/img/dot.svg`(작은 원 하나, 16×16), `tests/practice-site/spike-dnr.html`(제목 "DNR 스파이크", `<img id="dot" src="http://practice.test/img/dot.svg">`, 로드 결과를 `document.body.dataset.img = 'loaded'|'failed'`로 적는 onload/onerror).

**`tests/e2e/phase5-spike.e2e.ts`**(결과는 `test-results/phase5-spike.json`과 콘솔 표, 01-12 스파이크와 같은 방식 — 시험은 관찰이 끝났는지를 확인하고, 관찰값 자체로 실패하지 않는다):
- A4: `os.tmpdir()` 아래 임시 폴더에 시험용 확장(manifest v3, 권한 `declarativeNetRequestWithHostAccess`, host `<all_urls>`, 빈 service worker)을 쓰고 `launchExtensionContext({ extensionPath })`로 띄운다. `spike-dnr.html` 탭 둘(A·B)을 열고 SW에서 A의 탭 id로 세션 규칙(이미지 차단, `condition.tabIds: [A]`)을 더한 뒤 두 탭을 새로 고친다. A·B의 `data-img`, A에서 `requestfailed`의 `errorText`, `onRouted`가 A의 `dot.svg`를 봤는지를 기록한다. 판정: **(가)** A 실패·B 성공·A의 이미지가 route에 오지 않음 → 기존 픽스처로 BG-03 시험 가능. **(나)** A도 성공(route가 먼저 응답) → 같은 시험을 `context.route` 없이 한 번 더: Node `http.createServer`로 `tests/practice-site`를 127.0.0.1 임의 포트에서 제공하고 `--host-resolver-rules=MAP practice.test 127.0.0.1:<port>, MAP * ~NOTFOUND`로 띄워(밖으로 나가는 요청 없음) A 실패·B 성공이 보이는지 기록. 임시 폴더는 `afterAll`에서 지운다.
- A3: `launchExtensionContext({ keepBackgroundThrottling: true })`로 제품 확장(.output)을 띄워 SW가 뜨는지, `chrome://version`의 명령줄(`#command_line` 글자)에 세 인자가 없는지 기록·확인한다(이 둘은 실패하면 시험 실패 — 이후 측정의 전제다).
- A2: 같은 컨텍스트에서 연습 페이지 두 탭을 열어 앞 탭을 숨긴다(`document.visibilityState === 'hidden'` 확인). 숨은 탭과 보이는 탭에서 각각 (1) 페이지 안 `setTimeout(…, 100)` 10번 사슬의 실제 간격(중앙값·최댓값), (2) SW에서 `chrome.tabs.sendMessage(tabId, { type: 'site/ping' }, { frameId: 0 })` 20번 왕복 시간(중앙값·p95)을 잰다. 숨은 탭의 타이머 간격이 900ms 이상으로 늘어나면 "스로틀링 재현됨", 아니면 "재현 안 됨(헤드리스)"으로 기록한다.

SUMMARY에 "스파이크 결과" 절: A4 판정((가)/(나)와 근거 값), A3 확인, A2 숫자 표. A2가 "재현 안 됨"이면 05-04의 측정을 어떻게 할지(헤드 모드 + 가상 화면이 이 환경에 있는지 `command -v xvfb-run`으로 확인한 결과 포함)를 한 줄로 적는다.
  </action>
  <verify>
    <automated>CI=true pnpm exec playwright test tests/e2e/phase5-spike.e2e.ts tests/e2e/skeleton.e2e.ts tests/e2e/site-toggle.e2e.ts</automated>
    <fails_when>non-zero exit, or "No tests found", or the report lists any failed test, or test-results/phase5-spike.json is absent afterwards</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/fixtures.ts`에 `export async function launchExtensionContext`와 `ignoreDefaultArgs`가 있고, 기존 `context` 픽스처가 이것을 부른다
    - `git diff --quiet HEAD -- wxt.config.ts`가 0으로 끝난다(제품 권한 불변)
    - `test-results/phase5-spike.json`에 `a4`, `a3`, `a2` 세 키가 있다
    - 05-01-SUMMARY.md "스파이크 결과"에 A4 판정((가) 또는 (나)), A3 확인, A2 숫자가 있다
  </acceptance_criteria>
  <done>BG-03 시험 방식과 숨은 탭 측정 방식이 실측으로 정해졌고, 픽스처가 스로틀링을 켠 브라우저를 띄울 수 있다. 기존 시험은 그대로다.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: 새 권한 승인 — declarativeNetRequestWithHostAccess (이유 한 줄)</name>
  <read_first>
    - wxt.config.ts (현재 permissions: storage·scripting·tabs, host <all_urls>)
    - .planning/phases/05-board-background-patterns/05-RESEARCH.md ("권한 선택" 표)
    - 이 계획 Task 2의 스파이크 결과(A4)
  </read_first>
  <decision>BG-03(틀마다 이미지·광고 차단, 그 틀의 탭에서만)을 위해 새 권한 `declarativeNetRequestWithHostAccess`를 제품 manifest에 넣을지 정한다(D-15, D-16, 설계 11장 ⑤의 "새 권한은 만들 때 승인" 선례). 승인 전에는 05-08의 권한 추가 작업을 하지 않는다.</decision>
  <context>
이유 한 줄: 이미지·광고 요청을 막을 수 있는 MV3의 유일한 방법이 declarativeNetRequest이고, 세션 규칙에 탭 조건(`tabIds`)을 걸어 **그 틀을 실행하는 탭에서만** 막는다(전역 규칙은 쓰지 않는다).

- `declarativeNetRequestWithHostAccess`는 이미 있는 host `<all_urls>` 범위 안에서만 동작해 설치 화면에 **새 경고를 띄우지 않는다**(RESEARCH, Chrome 문서). 같은 목적의 `declarativeNetRequest`는 설치 경고("모든 페이지의 콘텐츠 차단")가 하나 더 뜬다.
- 쓰는 곳: service worker의 세션 규칙 추가·삭제뿐. 규칙은 브라우저를 닫으면 사라지고, 틀이 끝나거나 멈추거나 탭이 닫히면 걷어 낸다(05-08).
- 넣지 않는 것(그대로): 디버거·탐색 감시·콘텐츠 설정 권한(Phase 1 D-13).
- 스파이크(Task 2)는 제품이 아닌 임시 시험용 확장으로만 이 권한을 썼고, 결과는 SUMMARY "스파이크 결과"에 있다(A4 (가)/(나)).
- 거절하면: 05-08(BG-03)을 이번 phase에서 짓지 않고, ROADMAP 성공 기준 3의 뒷부분("그 탭에서만 요청이 막힌다")이 충족되지 않는다 — 다음 버전으로 미루는 것을 이용자가 확인해야 한다.
  </context>
  <options>
    <option id="approve-host-access">
      <name>declarativeNetRequestWithHostAccess 승인 (추천)</name>
      <pros>설치 화면 경고가 늘지 않는다. BG-03을 계획대로 짓는다.</pros>
      <cons>manifest 권한 목록이 하나 늘어난다(웹스토어 심사 때 이유를 적어야 한다).</cons>
    </option>
    <option id="approve-plain">
      <name>declarativeNetRequest(일반) 승인</name>
      <pros>권한 이름이 더 널리 알려져 있다.</pros>
      <cons>설치·업데이트 때 경고가 하나 더 뜬다. 기능은 같다.</cons>
    </option>
    <option id="reject">
      <name>승인하지 않음 — BG-03을 미룸</name>
      <pros>권한이 늘지 않는다.</pros>
      <cons>틀마다 이미지·광고 차단이 없다. ROADMAP 성공 기준 3 일부 미충족.</cons>
    </option>
  </options>
  <resume-signal>approve-host-access, approve-plain, 또는 reject를 입력</resume-signal>
  <acceptance_criteria>
    - 05-01-SUMMARY.md "결정" 절에 이용자가 고른 선택지 id와 날짜가 있다
    - 이 작업에서 `wxt.config.ts`는 바뀌지 않았다(`git diff --quiet HEAD -- wxt.config.ts`)
  </acceptance_criteria>
</task>

</tasks>

## 가정 (spec-less edge probe)

edge probe가 이 phase의 한국어 요구사항 8개를 모두 `unclassified`로 돌려줬다(자동 분류 실패). 각 계획이 요구사항별 경계를 직접 정해 `must_haves.truths`로 적고, 정하지 못한 것은 이 절에 가정으로 남긴다. 이 계획(BG-01·BG-03 전제 확인)의 가정:
- "합쳐졌다" = 이 브랜치의 `git ls-files`에 Phase 3·4 코드와 SUMMARY가 있는 상태. 다른 브랜치에만 있으면 없음으로 본다.
- 스파이크의 "스로틀링 재현" 기준은 숨은 탭 `setTimeout(100)` 간격 900ms 이상(크롬의 1초 정렬, RESEARCH).
- A4 (나)의 로컬 서버는 시험 안에서만 쓰는 Node 내장 `http` 모듈이라 새 의존성이 아니다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시험용 임시 확장 → 브라우저 | 스파이크가 쓰는 확장(제품 아님)이 네트워크 차단 규칙을 건다 |
| 시험 브라우저 → 네트워크 | 연습 사이트 밖으로 나가는 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | 제품 manifest | high | mitigate | 이 계획은 `wxt.config.ts`를 바꾸지 않는다(acceptance `git diff --quiet`). 권한 추가는 Task 3 결정 뒤 05-08에서만 |
| T-05-02 | Information Disclosure | 스파이크 (나) 로컬 서버 | low | mitigate | 127.0.0.1에만 묶고 `MAP * ~NOTFOUND`로 다른 호스트 해석을 막아 밖으로 나가는 요청이 없다 |
| T-05-03 | Tampering | 픽스처 정리 | medium | mitigate | 기존 `context` 픽스처 동작 불변 — skeleton·site-toggle e2e를 같은 명령으로 함께 돌린다 |
| T-05-SC | Tampering | 패키지 설치 | low | accept | 이 계획은 패키지를 설치하지 않는다(RESEARCH "Package Legitimacy Audit": 대상 없음) |
</threat_model>

<verification>
- 가정 대조표 23행이 SUMMARY에 있고, "다름·없음"이 있으면 멈췄다
- `pnpm exec vitest run tests/unit/phase5-contract.test.ts` 6개 이상 통과(멈추지 않은 경우)
- `CI=true pnpm exec playwright test tests/e2e/phase5-spike.e2e.ts tests/e2e/skeleton.e2e.ts tests/e2e/site-toggle.e2e.ts` 통과, `test-results/phase5-spike.json` 생성
- 권한 결정이 SUMMARY에 기록, `wxt.config.ts` 불변
</verification>

<success_criteria>
- Phase 5가 기대는 앞선 단계의 모양이 실제와 맞음이 확인되었거나, 맞지 않는 점이 짓기 전에 이용자에게 전달되었다
- BG-03 시험 방식과 숨은 탭 측정 방식이 실측으로 정해졌다
- 새 권한은 이용자 결정 뒤에만 들어간다
</success_criteria>

## Artifacts this phase produces

- 파일: `tests/unit/phase5-contract.test.ts`, `tests/e2e/phase5-spike.e2e.ts`, `tests/practice-site/spike-dnr.html`, `tests/practice-site/img/dot.svg`
- 심볼: 픽스처 `launchExtensionContext(opts)`(옵션 `extensionPath`, `keepBackgroundThrottling`, `onRouted`)
- 결과물: `test-results/phase5-spike.json`(a4·a3·a2), SUMMARY의 "가정 대조표"(P1-A~P4-13 매핑 — 이후 모든 Phase 5 계획이 이 이름을 쓴다), "스파이크 결과", "결정"

<output>
Create `.planning/phases/05-board-background-patterns/05-01-SUMMARY.md` when done
</output>
